{
  "name": "tests-firestore-rules",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint --max-warnings=0 .",
    "format": "prettier --check .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "vitest": "^3.2.4"
  }
}
