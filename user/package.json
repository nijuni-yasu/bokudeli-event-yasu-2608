{
  "name": "user",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "yarn": "Use npm instead"
  },
  "scripts": {
    "lint": "eslint --max-warnings=0 .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "VUE_DEVTOOLS= vite",
    "build": "npm -w common run build && vite build",
    "build:types": "vue-tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@channel.io/channel-web-sdk-loader": "^2.0.0",
    "@shokujii/base": "file:../base",
    "@shokujii/common": "file:../common",
    "@tinymce/tinymce-vue": "^6"
  },
  "devDependencies": {
    "unplugin-auto-import": "^0.17.8",
    "unplugin-vue-router": "^0.10.0",
    "vite-plugin-pwa": "^1.0.0",
    "vite-plugin-vue-layouts": "^0.11.0"
  }
}
