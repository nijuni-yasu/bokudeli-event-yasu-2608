export const buildTwitterUrl = (id: string) => {
  return 'https://twitter.com/' + id
}

export const buildFacebookUrl = (id: string) => {
  return 'https://www.facebook.com/' + id
}

export const buildInstagramUrl = (id: string) => {
  return 'https://www.instagram.com/' + id
}
