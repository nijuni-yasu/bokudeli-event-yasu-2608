# エンタープライズ実装計画

本ドキュメントは、`08_エンタープライズ` 配下の各仕様書（仕様概要・アーキテクチャ・各詳細仕様・PF版移行/共通化・EventMemberOrder 論点・マイページ友人）の決定事項を踏まえ、フェーズ分けした実装計画を整理する。

## 全体方針（各仕様書からの確定事項）

- **パッケージ**: `user` をコピーして `enterprise` パッケージを新規作成する（パターンC: コピー＋継続マージ）。共通部分は可能な範囲で `base` / `common` に寄せる。
- **テナント分離**: 単一 Firebase Project + `enterprise_id` による論理分離。custom claims（`enterprise_id` / `enterprise_role` / `user_type`）で制御する。
- **データ基盤の前提**: EventMemberOrder 設計（`members/{uid}/member_orders` + `stripes`）が本番反映済みであること。
- **着手順の鉄則**（`06_EventMemberOrder対応論点` §7.1）: ① EventMemberOrder リファクタ → ② `pay_community_bill_off_amount` 対応 → ③ エンプラ版。①② が本番反映された後に ③ へ着手する。**①② は実装済み**のため、③ に着手可能な状態。

---

## Phase 0: 前提整備（PF版で先行実装）

エンプラ着手の地盤。すべて PF版として実装し、後で `enterprise` にマージする。

| #   | 項目                                                                                                       | 参照                          | 状態             |
| :-- | :------------------------------------------------------------------------------------------------------- | :---------------------------- | :--------------- |
| 0-1 | EventMemberOrder リファクタの本番反映                                                                       | `07_リファクタリング/05`       | **実装済み**     |
| 0-2 | `pay_community_bill_off_amount`（割引参加）対応の本番反映                                                    | `01_マネタイズ/03`             | **実装済み**     |
| 0-3 | user→base 機能移行の残件消化（`community/events.vue` の `CommunityEvents.vue` 化、公開ページ Phase 3、横断付随作業） | `05_userをbaseに機能移行` §10 | Phase 2完了、残あり |
| 0-4 | 共通新機能: イベント中止・返金、最小催行人数、繰り返し作成（最大10件）、`community_bill` 割引参加、明示的アカウント作成画面 | `05_共通新機能`               | 未（enterprise と**並行開発**） |

> **判断ポイント**:
> - **0-1 / 0-2 は実装済み**のため、エンプラ版（Phase 1 以降）に着手可能。
> - **0-4 は enterprise パッケージ作成・開発と並行**して進める想定。PF版で先行実装した結果を `enterprise` へマージするため、**マージ負荷が発生する前提**で運用する（`05_userとenterpriseの共通化` のコピー＋継続マージ方針）。マージ負荷は週次/機能単位のマージ運用（横断タスク参照）で軽減する。
> - 0-3（user→base 移行の残件）も同様に並行可。base へ寄せた分は enterprise マージ時のコンフリクトを減らす方向に働く。

---

## Phase 1: 共通基盤（common / パッケージ / Hosting）

エンプラ全機能の土台。

- **1-1 common スキーマ拡張**（`02_全体構成` §6.1）
  - 既存に optional 追加: `User`(`user_type`, `enterprise_id`) / `Community`(`enterprise_id`, `approval_status`) / `Event`(`enterprise_id`, `allow_guest`, `guest_payment`, `enterprise_subsidy_settings`、`EVENT_PAYMENT_VALUES` に `enterprise_subsidy` 追加) / `EventMember`(`enterprise_id`, `is_guest`) / `EventMemberOrder`(`enterprise_id`, `pay_enterprise_subsidy_amount`) / `EventStripe`(同上)
  - 新規: `Enterprise.ts`（Enterprise / EnterpriseMember）、`EnterpriseSubsidySettings`
  - 新規ユーティリティ: `paymentEnterpriseSubsidyAmount.ts`（`computePaymentEnterpriseSubsidyAmount` / `isPaymentEnterpriseSubsidyAmountConsistent`）
  - ※ `shokujii-common-schemas` スキル準拠（DbSchema=Timestamp / AppSchema=EpochMillis）
- **1-2 `enterprise` パッケージ作成**: `user` コピー、ルート `package.json` の workspaces 追加、依存（`@shokujii/base`, `@shokujii/common`）
- **1-3 Hosting / ドメイン**: `firebase.json` に `enterprise` target 追加、サブドメイン方式、`getEnterpriseByDomain`（ホスト名→`enterprise_id` 解決、ブランディング動的適用）
- **1-4 Storage パス設計**: `/enterprises/{eid}/...` 配下に集約（`02_全体構成` §3.7）。Storage Rules も同時。

---

## Phase 2: 認証・テナント分離

`04_認証・アカウント管理` が正本。

- **2-1 ログインフロー**: メールドメイン制限 + 6桁OTP（`requestEnterpriseEmailLogin` / `confirmEnterpriseEmailLogin`）。ドメイン検証はフロント＋Functions 二重。
- **2-2 Custom Claims**: `enterprise_id` / `enterprise_role` / `user_type` の設定（作成・ロール変更・停止時）。
- **2-3 セッション管理**: 60分無操作で `signOut()`、停止時 `revokeRefreshTokens()` + `disabled`。
- **2-4 Security Rules 基盤**: `/enterprises/*` の read/write、`enterprise_id == null` または自社のみ可視の共通パターン（`02_全体構成` §5 / `06_論点` §4）。

---

## Phase 3: 全社管理者画面・アカウント管理

`04_全社管理者画面` が正本。`enterprise/src/pages/admin/*` に実装。

- **3-1 全社設定** `/admin/settings`（会社名・ロゴ・テーマカラー）
- **3-2 アカウント管理** `/admin/members`（一覧・個別追加・編集・停止/復活・CSV一括作成 `createEnterpriseMembers`）
- **3-3 コミュニティ承認** `/admin/communities`（承認/却下フロー、`approval_status`、CSV一括作成、通知）
- **3-4 割引設定** `/admin/discount`（固定/割合・月額上限）
- **3-5 ナビゲーションガード**: `enterprise_role == "admin"` 検証

---

## Phase 4: イベント・決済・割引（enterprise_subsidy）

`04_割引・決済` + `06_EventMemberOrder対応論点` が正本。最も実装が重い中核。

- **4-1 イベント作成**: `enterprise_subsidy` をデフォルト化、`enterprise_subsidy_settings` スナップショット（新規作成時 + draft 保存時に再取得）
- **4-2 注文フロー**:
  - `addToCart`: `event_payment` で分岐し `pay_enterprise_subsidy_amount` 算出。月額上限の残枠に応じて切り下げ（超過分は自己負担 = `undefined`）
  - `confirmOrder` / `createStripeCheckoutSession`: 整合再検証 + `monthly_usage` 加算（Transaction）
  - `stripeWebhook`: `stripes` に補助合計セット + `monthly_usage` 加算
  - `cancelOrders`: イベント開催月キーで `monthly_usage` 減算
- **4-3 月額上限**: `/enterprises/{eid}/members/{uid}` の `monthly_usage`（"YYYY-MM" 辞書、イベント開催月基準）
- **4-4 base カート/領収書 UI**: `pay_enterprise_subsidy_amount` を考慮した `totalDiscount` / `totalPayment` 算出（`02_全体構成` §6.2）
- **4-5 indexes**: `collectionGroup('member_orders')` の `enterprise_id` 複合インデックス3種

---

## Phase 5: ゲスト参加・メール配信制御

- **5-1 ゲスト参加**: `allow_guest` / `guest_payment`（無料/自己負担）、`members.is_guest`、請求先は全社管理者に一本化（MVP）。`04_ゲスト参加` 正本。
- **5-2 メール配信制御**: `enterprise_id`（イベント/コミュニティ）と `user_type`（全ユーザー向け）で送信スキップ。業務必須メール（注文確定・キャンセル）は除外しない。`04_メール配信` 正本。

---

## Phase 6: ダッシュボード・監査ログ

- **6-1 監査ログ基盤**（MVP から必須・後付け不可）: `writeAuditLog` 共通ユーティリティ、`/enterprises/{eid}/audit_logs`、Cloud Logging 併用。各 Functions に記録を埋め込み。`/admin/audit-logs` 閲覧。`04_監査ログ` 正本。
- **6-2 ダッシュボード** `/admin`（月別・メンバー別の利用回数/人数/金額 + CSV）。`04_ダッシュボード` 正本。

---

## Phase 7: マイページ・友人（たべとも）

`07_マイページ・友人` が正本。PF版 Callable に認可レイヤを追加（claims 分岐、duplicate 回避）。

- `assertEnterpriseProfileAccess` + 各 Callable（`getUserProfilePreview` / `getUserFriends` / `getUserFriendMeetLog` / `getUserFoods`）の `enterprise_id` フィルタ
- `recountUserProfileCounts` のスコープ上書き
- `enterprise` UI: ログイン必須リダイレクト、認可エラー、部署表示、SNS・友人招待非表示、コミュニティ `approval_status` フィルタ
- 単体テスト（他社 denied / 未ログイン unauthenticated / 停止 not-found / ゲスト denied）
- ※ MVP 着手前に OQ-1〜5 の合意が必要

---

## 横断タスク（各フェーズで並行）

| 項目                                          | 反映先                              |
| :-------------------------------------------- | :---------------------------------- |
| Security Rules 追加・更新                      | Phase 2〜6 で逐次（`02`§5 / `06`§4） |
| firestore.indexes.json                        | Phase 4・7                          |
| Storage Rules                                 | Phase 1                             |
| user→enterprise マージ運用（週次/機能単位）    | 全期間                              |
| router/utils 同期（user / enterprise / admin） | base 変更の都度                     |
| lint・format（`/lint-and-format`）            | 各 PR 完了前必須                    |

---

## MVP スコープ（仕様概要より）

- **MVP に含む**: OTP ログイン、全社管理者画面、福利厚生割引（固定 + 割合・月額上限・超過自己負担）、3 支払方式、ゲスト参加（無料/自己負担）、繰り返し作成、自動中止、ダッシュボード基本 + CSV、最低限メール、セッションタイムアウト、基本監査ログ、マイページ/友人の認可。

---

## 将来項目（MVP 後に実装予定）

各詳細仕様で「将来 / Phase 2」として切り出された項目を、ドメイン別に集約する。MVP のスコープ外であり、MVP リリース後に順次着手する。出典を併記する。

### 認証・アカウント管理（`04_認証` 補足 A〜F ほか）

- SSO ログイン（Identity Platform + SAML / OIDC、企業ごと）
- OTP 有効期限の短縮（エンプラのみ。`pass_code` コレクション分離 or クエリ分岐が必要）
- OTP 最大試行回数の制限 / 再送間隔の制限 / ドメイン列挙攻撃へのレート制限
- アカウント作成完了メール
- 本人によるセルフ登録
- PF版アカウントとの統合（昇格フロー。MVP は重複エラー）
- サーバーサイドセッション管理（タブ閉じ等の完全記録）

### 割引・決済（`04_割引・決済` §3.6 ほか）

- 給与天引き対応（CSV 出力）
- 月次バッチ再集計（`monthly_usage` のずれ検知・補正の安全網。Scheduled Function）
- 古い `pass_code` 等のエントリ削除バッチ（3ヶ月以上前）
- Stripe Webhook signing secret の分離（要件が出た時点で再検討）
- 割引ルールの拡張

### イベント管理（`04_イベント管理` ほか）

- 繰り返し作成の一括編集連動（1つ編集 → 他にも反映）
- 自動中止の閾値をイベント単位で上書き

### ゲスト参加（`04_ゲスト参加` §12 ほか）

- ゲストへの補助適用
- ゲスト請求先の選択（全社管理者 / コミュニティ管理者）
- `monthly_usage` コレクションへの「ゲスト負担合計」分離（集計高速化）

### ダッシュボード（`04_ダッシュボード` ほか）

- `monthly_usage` 月次集計コレクション導入（月初バッチで事前計算、集計高速化）
- 専用カウンタ（`EnterpriseMember.monthly_session_count` 等）
- 企業負担 vs 自己負担の比率（積み上げ棒グラフ）/ 上位 N 名ランキング
- 「コミュニティ主催者負担額」を別軸として表示
- 集計結果のキャッシュ

### 監査ログ（`04_監査ログ` 補足 ほか）

- CSV エクスポート / Cloud Storage への長期エクスポート
- ログ保持期間設定（Firestore TTL）
- `cart_add` / `cart_remove` 等の操作ログ追加
- `order_ids` から `member_orders` 詳細画面への遷移
- プロフィール閲覧ログ（マイページ EP-13）

### 全社管理者画面（`04_全社管理者画面` 補足 A〜E）

- テーマカラーの管理者編集 / `allowed_email_domains` の管理者編集
- メンバーの完全削除
- 監査ログ CSV エクスポート
- コミュニティ承認の画面内通知

### マイページ・友人（`07_マイページ・友人` §6.2 / EP-12, 13）

- ユーザー / 企業単位の公開範囲設定（`friends_visibility` / `profile_visibility`）
- プロフィール閲覧の監査ログ
- backfill の `enterprise_id` 範囲指定
- 注文履歴の会社負担額内訳 UI
- 全社管理者向け従業員プロフィール一覧
- `purgeUserFriends` と退職・削除請求フローの統合
- `stripes` の全社管理者 read 可否の再検討

### セキュリティ・インフラ（`01_仕様概要` / `02_全体構成` §4.3）

- IP アドレス制限
- 追加のデータ暗号化（Firebase デフォルト以上）
- セッションタイムアウトの企業ごとカスタマイズ（`session_timeout_minutes`）
- 同時ログイン制限（1アカウント1デバイス）
- 物理分離（金融・医療系向けに企業ごと Project へ移行）

### アーキテクチャ・共通化（`05_base依存整理` §2.1 / `05_共通化` §3.2）

- `@/router/utils` 依存の provide/inject 化（path 生成の抽象化）
- マージ負荷が増えた場合の `base` への共通化（パターンA）再検討

---

## 仕様抜け漏れチェック（Phase 1 着手前）

全詳細仕様（`04_*` 8 本）・`02_全体構成`・`05_*`・`06_論点`・`07_マイページ・友人` と現行コードを突き合わせた精査結果。**着手前に潰すべき決定漏れ・薄い領域・Rules の穴**を重要度順に整理する。各項目は合意のうえ該当仕様書へ反映する。

### A. 着手前に合意が必要な未決定論点（ブロッカー級）

| # | 箇所 | 内容 | 影響 Phase | 反映 |
|:--|:--|:--|:--|:--|
| A-1 | `04_ゲスト参加` §8.3 | ゲスト本人の `members` / `member_orders` read Rules | 2 / 5 | **確定** → `02` §5.2, `04_ゲスト参加` §8.3, `04_割引・決済` §9 |
| A-2 | `07_マイページ・友人` §8 | OQ-1〜5・EP-6 | 7 | **確定** → `07` §8 |
| A-3 | `04_イベント管理` §6.1 | `is_public` / URL 限定公開 | 4 | **確定** → `04_イベント管理` §6.1（E-7）, `02` §5.2 |
| A-4 | `04_メール配信` §4.2 | `enterprise_id` 方式A（Event denormalize） | 1 | **確定** → `02` §3.4, `04_メール配信` §4.2 |

### B. 仕様自体が欠けている / 薄い領域

| # | 領域 | 不足内容 | 反映 |
|:--|:--|:--|:--|
| B-1 | **請求書（invoice）発行フロー** | MVP=集計まで、PDF は Phase 2 | **確定** → `04_請求` §2, `04_ダッシュボード` §3.1 |
| B-2 | **企業オンボーディング** | `createEnterprise` + 初代 admin | **確定** → `04_オンボーディング` §3, `04_認証` §1.2, `04_監査ログ` §3.2 |
| B-3 | **`users_personal_information` admin read** | Functions 集約 `getEnterpriseMembers` | **確定** → `04_認証` §9.1, `04_全社管理者画面` §4.1 |
| B-4 | **未認証 Callable 濫用対策** | MVP で App Check | **確定** → `04_認証` §1.2（8-2）, §4.0/§4.1 |
| B-5 | **i18n 方針** | 共通=base/ja.ts、固有=enterprise/ja.ts、差分=props | **確定** → `02` §1.4（追記） |

### C. スキーマ・命名の確認事項（Phase 1 で確定）

- **C-1** `EnterpriseSubsidySettings` は「仮決定」（`06_論点` §2.2）。Phase 1 で正式確定。
- **C-2** `monthly_usage` / `monthly_order_count` は `Record<string, number>`（日付キーだが値は数値）。`shokujii-common-schemas` の Timestamp / EpochMillis ルールは `created_at` / `updated_at` のみ対象、辞書フィールドは対象外という整理でよいか確認。
- **C-3** フィールド名は実コードと一致確認済み（`event_start_datetime` / `event_deadline_datetime` ともに `common/src/schemas/Event.ts` に存在。月キー算出＝開催月、自動中止＝注文期限で整合 OK）。
- **C-4** ゲスト未認証閲覧が依存する **`event.community_account` がイベント側に残る**前提（EventMemberOrder リファクタの削除対象は members / member_orders / stripes のみ）。Phase 5 着手前に再確認。

### D. Phase 0 前提の実装状況（コード確認結果）

`0-4` を並行とする件、コード上の現状は以下。**依存順に注意**。

| 前提機能 | 状態（コード確認） |
|:--|:--|
| 0-1 EventMemberOrder | 実装済み（`EventMemberOrder.ts` / `EventStripe.ts`） |
| 0-2 community_bill 割引参加 | 実装済み（`paymentCommunityBillOffAmount.ts` + `community_bill_settings.type='discount'`） |
| イベント中止・返金 | 存在（`base/src/apis/eventCancellation.ts` / `functions/default/src/eventCancellation.ts`） |
| 繰り返し作成 | 存在（`eventCopyRepeat.ts`）。エンプラの「最大10件・パターン指定」UI まで揃うか要確認 |
| 最小催行人数 | 部分的（`eventCancellationReasons.ts` / `partnerShopDeliverable.ts`）。自動中止の閾値ロジックの完成度を要確認 |
| 明示的アカウント作成画面 | 未確認 |

> **自動中止（Phase 4 / イベント管理）は「中止・返金」と「最小催行」の完成が前提**。0-4 を並行する場合、自動中止の着手は中止 / 最小催行の本番反映後に並べる。

### E. 軽微だが拾っておくべき連動

- ダッシュボードの「利用回数」を `audit_logs`（`order_create`, `target_id == null`）から導出 → **監査ログのベストエフォート欠損で実数より少なく出る**可能性を MVP で許容する旨、関係者合意（`04_ダッシュボード` 2.1）。
- `community_bill` のエンプラ請求先（コミュニティ）と、`01_仕様概要` の「請求先は全社管理者に一本化」（＝ゲスト費用負担先）の **2 つの“請求先”概念**が混同されやすい。`04_割引・決済` §4.1 で整理済みだが実装時の用語統一に注意。

### 推奨アクション

1. ~~**A 群（A-1〜A-4）を関係者合意 → 各仕様書へ追記**~~ → **完了**（2026-06 反映済み）
2. ~~**B-1 請求書 / B-2 オンボーディング** を MVP に含めるか線引き~~ → **完了**（MVP=集計 / `createEnterprise`。`04_請求` / `04_オンボーディング` 参照）
3. Phase 1 着手時に **C-1〜C-4** を `shokujii-common-schemas` 準拠で確定（実装フェーズ）

---

## クリティカルパス（依存順サマリ）

```
Phase0(0-1,0-2) → Phase1(common+pkg+hosting) → Phase2(認証/claims)
   → Phase3(管理者画面) ┐
   → Phase4(決済/割引)  ┼→ Phase6(ダッシュボード/監査ログ)
   → Phase5(ゲスト/メール)┘
   → Phase7(マイページ/友人)
```

---

## 参照ドキュメント

- `01_エンタープライズ_仕様概要.md`
- `02_エンタープライズ_全体構成・アーキテクチャ.md`
- `04_エンタープライズ_詳細仕様_認証・アカウント管理.md` / `_全社管理者画面.md` / `_割引・決済.md` / `_イベント管理.md` / `_ゲスト参加.md` / `_メール配信.md` / `_ダッシュボード.md` / `_監査ログ.md` / `_請求.md` / `_オンボーディング.md`
- `05_エンタープライズ_PF版_共通新機能.md` / `_userをbaseに機能移行.md` / `_userとenterpriseの共通化.md` / `_baseの依存関係整理.md`
- `06_エンタープライズ_EventMemberOrder対応論点.md`
- `07_エンタープライズ_マイページ・友人.md`
