# セキュリティ（チェックシート・技術詳細・ポリシー）

> **原典**: \`90_アーカイブ/v0.1原典/03_*\` 4 ファイルを現行化して集約。

---

# 1. セキュリティチェックシート（想定問答）

大企業の導入審査で提出を求められるセキュリティチェックシートへの回答テンプレート。
各回答は現時点の仕様・設計に基づく。未定の項目は方針決定後に更新する。

**準備状況サマリ:**

| 状況 | 質問数 | 説明 |
|:--|:--|:--|
| 回答可能 | 約25問 | 既存ドキュメントから回答可能 |
| 要決定 | 約5問 | 方針だけ決めれば回答可能（SLA 数値等） |
| 未定 | 約8問 | 検討・整備が必要（脆弱性診断、プライバシーポリシー等） |
| 要確認 | 約2問 | 事実ベースの回答が必要（過去のインシデント実績等） |

**参照ドキュメント:**
- `01_仕様概要.md` — 仕様概要・決定事項
- `02_アーキテクチャ.md` — 技術設計（認証、セッション管理、監査ログ、Storage パス設計）
- `03_エンタープライズ_セキュリティ対応_技術詳細.md` — 技術調査済みセキュリティ施策（SSO、IP制限、暗号化、物理分離）
- `03_エンタープライズ_セキュリティ対応_全項目一覧.md` — セキュリティ全項目のステータス管理


# 1. アクセス制御・認証

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 1-1 | 認証方式は何か | 企業メールドメイン制限 + 6桁OTP（ワンタイムパスワード）。Phase 2 で SSO（SAML/OIDC）対応予定 | 回答可能 |
| 1-2 | 多要素認証（MFA）に対応しているか | MVP では OTP 認証のみ。Phase 2 で SMS / 認証アプリによる MFA に対応予定（Identity Platform） | 回答可能 |
| 1-3 | パスワードポリシーはあるか | パスワードレス認証（OTP）を採用しているため、パスワードポリシーは該当しない | 回答可能 |
| 1-4 | アカウントロックアウト機能はあるか | Phase 2 で対応予定（OTP を一定回数間違えたらアカウントをロック） | 要決定 |
| 1-5 | セッションタイムアウトはあるか | 1週間無操作で自動ログアウト | 回答可能 |
| 1-6 | 同時ログインの制限はあるか | MVP では制限なし。Phase 2 で対応検討 | 要決定 |
| 1-7 | 管理者によるアカウント停止が即時反映されるか | `revokeRefreshTokens()` によりリフレッシュトークンを即時無効化。最大1時間以内にログアウトされる | 回答可能 |
| 1-8 | 権限管理の仕組みは | Firebase Auth の custom claims で `enterprise_role`（admin / member）を管理。Firestore Security Rules で制御 | 回答可能 |
| 1-9 | 退職者のアカウント削除プロセスは | 全社管理者がアカウント停止（`disableEnterpriseMember`）→ Firebase Auth ユーザー無効化 + セッション即時取消。**従業員による自己削除 UI は提供しない**（`04_認証` §6.3） | 回答可能 |
| 1-10 | SSO に対応しているか | Phase 2 で Identity Platform を使い SAML/OIDC に対応予定。Azure AD、Okta 等に接続可能 | 回答可能 |


# 2. データ保護・暗号化

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 2-1 | 保存データは暗号化されているか | Firestore はすべてのデータを AES-256 で自動暗号化（Google 管理鍵）。追加設定不要 | 回答可能 |
| 2-2 | 暗号鍵の管理方式は | MVP: Google 管理鍵（自動ローテーション）。将来的に CMEK（Cloud KMS による自社管理鍵）へ移行可能 | 回答可能 |
| 2-3 | 通信は暗号化されているか | Firebase Hosting / Cloud Functions はデフォルトで TLS 1.2 以上。追加設定不要 | 回答可能 |
| 2-4 | 個人情報の暗号化は | MVP: Firestore のデフォルト暗号化。必要に応じてフィールドレベル暗号化（Tink + Cloud KMS）を導入可能 | 回答可能 |
| 2-5 | 暗号鍵のローテーションはしているか | Google 管理鍵は自動ローテーション。CMEK 導入後は手動/自動ローテーションを設定可能 | 回答可能 |


# 3. ネットワークセキュリティ

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 3-1 | IPアドレス制限は可能か | Phase 2 で対応予定。Cloud Functions での API レベル制限（推奨）と Cloud Armor での完全制限（オプション）の2方式を用意 | 回答可能 |
| 3-2 | DDoS 対策はあるか | GCP インフラの自動 DDoS 防御が適用される。Cloud Armor（WAF）の追加導入も可能 | 回答可能 |
| 3-3 | WAF は導入しているか | MVP では未導入。Cloud Armor で導入可能（Phase 2 以降で必要に応じて） | 回答可能 |
| 3-4 | API のレート制限はあるか | Phase 2 で対応予定。Cloud Functions で実装 | 要決定 |
| 3-5 | TLS のバージョンは | TLS 1.2 以上（Firebase デフォルト） | 回答可能 |


# 4. データ保管・管理

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 4-1 | データの保管場所（リージョン）は | Google Cloud 東京リージョン（asia-northeast1）。日本国内に保管 | 回答可能 |
| 4-2 | データのバックアップ体制は | Firestore ポイントインタイムリカバリ（PITR: 7日間）。定期エクスポートの設定を予定 | 要設定確認 |
| 4-3 | データの復旧手順は | PITR により任意の時点にデータ復旧可能。手順書は要整備 | 要決定 |
| 4-4 | テナント間のデータ分離はどうしているか | `enterprise_id` による論理分離。Firestore Security Rules で `request.auth.token.enterprise_id == resource.data.enterprise_id` を強制し、他社データへのアクセスを防止 | 回答可能 |
| 4-5 | データの物理分離は可能か | 対応可能。`enterprise_id` でフィルタし、特定企業のみ別 Firebase Project に移行する手順を整備済み。ただし追加費用が発生 | 回答可能 |
| 4-6 | 契約終了時のデータ削除は | `enterprise_id` で全データを抽出・一括削除可能。Storage も `/enterprises/{enterpriseId}/` 以下に格納しており企業単位で削除可能 | 回答可能 |
| 4-7 | データの持ち出し（エクスポート）は可能か | Phase 2 で管理者画面からの CSV/JSON エクスポート機能を予定 | 要決定 |


# 5. ログ・監査

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 5-1 | 操作ログ（監査ログ）は記録しているか | ログイン/ログアウト、管理者操作（アカウント作成・停止、コミュニティ承認・却下、割引設定変更）、注文/キャンセルを記録。Firestore + Cloud Logging に二重出力 | 回答可能 |
| 5-2 | ログの保持期間は | Firestore: 無期限（MVP）。Cloud Logging: デフォルト30日。Phase 2 で TTL / 長期保存設定を検討 | 回答可能 |
| 5-3 | ログの改ざん防止策は | クライアントからの直接書き込みは Security Rules で禁止（`allow write: if false`）。Cloud Functions（Admin SDK）からのみ書き込み可能 | 回答可能 |
| 5-4 | ログへのアクセス権限は | 全社管理者のみ閲覧可能。Security Rules で `enterprise_role == "admin"` を検証 | 回答可能 |
| 5-5 | 不正アクセスの検知は可能か | 監査ログで異常なログインパターンの確認が可能。自動アラートは Phase 2 で検討 | 要決定 |


# 6. インシデント対応

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 6-1 | セキュリティインシデント発生時の通知体制は | 要定義（検知後○時間以内にメール通知、等） | **未定** |
| 6-2 | インシデント対応のエスカレーションフローは | 要定義 | **未定** |
| 6-3 | 過去にセキュリティインシデントが発生したことはあるか | （事実に基づき回答） | **要確認** |


# 7. 可用性・SLA

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 7-1 | SLA（稼働率保証）はあるか | GCP SLA に基づき設定予定（Firestore: 99.999%, Hosting: 99.95%）。shokujii としての SLA は GCP より低く設定が一般的（例: 99.9%） | 要決定 |
| 7-2 | 障害発生時の連絡体制は | 要定義（連絡先、エスカレーションフロー） | **未定** |
| 7-3 | 過去の障害実績は | （事実に基づき回答） | **要確認** |
| 7-4 | 災害対策（DR）はあるか | GCP の東京リージョンは複数ゾーンで冗長化。Firestore は自動でリージョン内の複数ゾーンにレプリケーション | 回答可能 |


# 8. 脆弱性管理

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 8-1 | 脆弱性診断は実施しているか | 要検討（実施頻度・方法を決める） | **未定** |
| 8-2 | ペネトレーションテストは実施しているか | 要検討（第三者診断の実施を検討） | **未定** |
| 8-3 | 脆弱性パッチの適用体制は | 要定義（npm audit の実施頻度、依存関係のアップデート方針） | **未定** |
| 8-4 | XSS / CSRF 対策はしているか | XSS: Vue テンプレートエンジンによるデフォルトエスケープ。CSRF: Firebase Auth トークンによる API 認証で対策 | 回答可能 |


# 9. 外部委託・サブプロセッサー

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 9-1 | 利用している外部サービス（サブプロセッサー）は | 下記一覧の通り | 回答可能 |
| 9-2 | 各サブプロセッサーのセキュリティ認証は | 下記一覧に記載 | 回答可能 |
| 9-3 | 再委託先はあるか | GCP / Stripe / SendGrid が利用するインフラ。各社の再委託先リストは公式サイトで公開 | 回答可能 |

## サブプロセッサー一覧

| サービス | 提供元 | 用途 | データ保管場所 | セキュリティ認証 |
|:--|:--|:--|:--|:--|
| Firebase / Cloud Firestore | Google | データベース | 東京リージョン（asia-northeast1） | SOC1/2/3, ISO27001, ISO27017, ISO27018 |
| Firebase Authentication | Google | 認証 | GCP インフラ | 同上 |
| Firebase Hosting | Google | Web ホスティング | GCP CDN | 同上 |
| Cloud Functions for Firebase | Google | サーバーレス API | 東京リージョン | 同上 |
| Cloud Storage for Firebase | Google | ファイルストレージ | 東京リージョン | 同上 |
| Stripe | Stripe, Inc. | 決済処理 | Stripe インフラ | PCI DSS Level 1, SOC1/2 |
| SendGrid | Twilio Inc. | メール配信（OTP、通知） | Twilio インフラ | SOC2, ISO27001 |


# 10. 法令・個人情報保護

| # | 質問 | 回答 | 状況 |
|:--|:--|:--|:--|
| 10-1 | 個人情報保護方針はあるか | 要整備（エンタープライズ版向けプライバシーポリシーの作成が必要） | **未定** |
| 10-2 | 取得する個人情報の範囲は | メールアドレス、氏名、所属企業、注文履歴。決済情報は Stripe 側で保持（shokujii にカード番号等は保存されない） | 回答可能 |
| 10-3 | 個人情報の利用目的は | 認証、サービス提供（注文・通知）、利用状況の集計 | 回答可能 |
| 10-4 | 開示・削除請求への対応は | 要整備（対応フローの定義が必要） | **未定** |

---

# 2. セキュリティ対応 技術詳細


MVPでは最小限のセキュリティ対策（企業メールドメイン制限 + Firestore Security Rules）で運用する。
本ドキュメントでは、Phase 2 以降で必要に応じて導入するセキュリティ施策の実装可能性と具体的な方法を記載する。

**すべての施策は Firebase / GCP の機能で実現可能である。**

※ ビジネス優先度順（企業から要求される頻度・導入阻害度が高い順）に記載。

| 優先度 | 項目 | 難易度 | 推奨フェーズ | 優先度の根拠 |
|:--|:--|:--|:--|:--|
| 1 | SSO（SAML/OIDC） | 低〜中 | Phase 2 | 大企業が最も求める機能。導入の必須条件になりうる |
| 2 | IPアドレス制限（API層） | 中 | Phase 2 | セキュリティチェックシートで頻出。API層のみで実質的に十分 |
| 3 | データ暗号化（CMEK） | 低（コード変更不要） | Phase 2 | コード変更不要で導入容易だが要求頻度は SSO/IP より低い |
| 4 | フィールドレベル暗号化 | 中（クエリ制約あり） | 必要時のみ | 適用範囲が限定的。医療・金融等の特殊要件 |
| 5 | IPアドレス制限（完全） | 高（構成複雑・コスト大） | 大企業要件時のみ | API層制限で十分なケースが大半 |
| 6 | 物理分離（企業ごとProject） | 高（運用コスト大） | 大企業要件時のみ | 最も工数が大きい。コンプライアンス必須時のみ |


# 1. SSO（SAML/OIDC）

## 1.1 概要

Firebase Auth を Identity Platform にアップグレードし、企業ごとにテナントを作成して異なる認証プロバイダー（Azure AD、Okta 等）を設定する。**最も実装が確実な施策。**

大企業が最も求める機能であり、「自社の Azure AD / Okta でログインしたい」は導入の必須条件になることが多い。Phase 2 で最優先に対応すべき。

## 1.2 実装手順

### サーバー側: テナント作成 + SAMLプロバイダー設定

```typescript
// 1. テナント作成（Admin SDK）
const tenant = await getAuth().tenantManager().createTenant({
  displayName: 'Company A',
  emailSignInConfig: { enabled: true, passwordRequired: false },
})

// 2. SAML プロバイダー追加（Admin SDK）
await getAuth().tenantManager().authForTenant(tenant.tenantId)
  .createProviderConfig({
    providerId: 'saml.company-a-azure-ad',
    displayName: 'Company A Azure AD',
    enabled: true,
    idpEntityId: 'https://sts.windows.net/{tenant-id}/',
    ssoURL: 'https://login.microsoftonline.com/{tenant-id}/saml2',
    x509Certificates: ['-----BEGIN CERTIFICATE-----\n...'],
    rpEntityId: 'https://company-a.shokujii.com',
    callbackURL: 'https://PROJECT_ID.firebaseapp.com/__/auth/handler',
  })
```

### フロントエンド: 企業設定に応じてログインフローを切り替え

```typescript
// enterprise パッケージ内のログイン処理
if (enterprise.sso_provider) {
  // SSO ログイン
  auth.tenantId = enterprise.tenant_id
  const provider = new SAMLAuthProvider(enterprise.sso_provider)
  await signInWithPopup(auth, provider)
} else {
  // OTP ログイン（MVP と同じ）
  await sendOtp(email)
}
```

### Firestore スキーマ追加（/enterprises/{enterpriseId}）

```typescript
{
  // Phase 2 で追加
  tenant_id?: string       // Identity Platform テナントID
  sso_provider?: string    // SAMLプロバイダーID（例: "saml.company-a-azure-ad"）
}
```

## 1.3 料金

| 認証方式 | 無料枠 | 超過後 |
|:--|:--|:--|
| メール+OTP（Tier 1） | 月49,999 MAUまで無料 | $0.0055/MAU〜 |
| SAML/OIDC（Tier 2） | 月49ユーザーまで無料 | $0.015/MAU |

SSO を使う企業は通常それなりの規模のため、料金は導入企業への月額費用に転嫁できる。

## 1.4 導入の流れ

1. GCPコンソールで Identity Platform にアップグレード（ワンクリック。既存ユーザーはそのまま引き継がれる）
2. SSO が必要な企業ごとにテナントを作成し、SAML/OIDC プロバイダーを設定
3. SSO が不要な企業はそのまま OTP ベースのログインを継続（テナント作成不要）
4. enterprise パッケージのログイン画面で、企業設定に応じてフローを分岐


# 2. IPアドレス制限

## 2.1 Firebase の制約

Firebase Hosting 単体にはIPアドレス制限機能がない。これが最大の制約。
Firestore Security Rules にもIPアドレス情報は渡らないため、Rules でのIP制限は不可。

## 2.2 方式A: Cloud Functions でIPチェック（推奨）

フロントエンド（HTML/JS）のアクセスは制限しないが、**データアクセス（API呼び出し）をIP制限**する方式。追加インフラコスト不要で、企業ごとの個別設定も容易。

### 実装イメージ

```typescript
// functions/default/src 内の共通ミドルウェア
export const enterpriseCallable = onCall(async (request) => {
  const uid = request.auth?.uid
  if (!uid) throw new HttpsError('unauthenticated', '認証が必要です')

  const enterpriseId = request.auth.token.enterprise_id
  const enterprise = await getEnterprise(enterpriseId)

  // IP制限が設定されている企業の場合のみチェック
  if (enterprise.allowed_ips && enterprise.allowed_ips.length > 0) {
    const clientIp = request.rawRequest.headers['x-forwarded-for']
    if (!enterprise.allowed_ips.includes(clientIp)) {
      throw new HttpsError('permission-denied', '許可されていないIPアドレスです')
    }
  }

  // 本来の処理...
})
```

### Firestore スキーマ追加（/enterprises/{enterpriseId}）

```typescript
{
  // Phase 2 で追加
  allowed_ips?: string[]  // 許可IPアドレスリスト（未設定 = 制限なし）
}
```

### メリット
- ロードバランサー不要。追加インフラコストなし
- 企業ごとに `/enterprises/{enterpriseId}` の `allowed_ips` を設定するだけ
- IP制限が不要な企業はフィールドを空にするだけでスキップ

### 制限
- フロントエンド（HTML/JS）自体は読み込める（ログイン画面は表示される）
- ログイン・データ取得はIP制限されるため、実質的に利用は不可能

## 2.3 方式B: Cloud Load Balancer + Cloud Armor（完全なIP制限）

フロントエンドへのアクセス自体をIPで制限する方式。Firebase Hosting の前段にロードバランサーを配置する。

### 構成

```
ユーザー → Cloud Load Balancer → Cloud Armor（IP制限） → Firebase Hosting
                                    ↓ 拒否
                              403 Forbidden
```

### 実装手順

```bash
# 1. Cloud Armor ポリシー作成
gcloud compute security-policies create shokujii-enterprise-policy

# 2. 特定IPレンジのみ許可
gcloud compute security-policies rules create 1000 \
  --security-policy=shokujii-enterprise-policy \
  --src-ip-ranges="203.0.113.0/24" \
  --action=allow \
  --description="Company A office IP"

# 3. デフォルト: 拒否
gcloud compute security-policies rules update 2147483647 \
  --security-policy=shokujii-enterprise-policy \
  --action=deny-403
```

### 注意点
- Firebase Hosting のデフォルトドメイン（`*.web.app`）には Cloud Armor を適用できない。**カスタムドメイン必須**
- ロードバランサーの月額費用が発生（最低でも月$20程度〜）
- 企業ごとに異なるIP制限を設定するには、Cloud Armor のルールをドメインごとに管理する必要があり運用が複雑
- 方式Aで実質的に十分なケースが多いため、方式Bは金融・医療系等の厳格な要件がある場合のみ検討する


# 3. データ暗号化

## 3.1 デフォルト暗号化（MVP）

Firestore はすべてのデータを保存時に自動で暗号化（AES-256、Google管理の鍵）している。追加の設定は不要で、復号もシステムが自動で行う。MVPではこれで十分。

## 3.2 CMEK（顧客管理暗号鍵）

Google管理の鍵ではなく、自社で管理する暗号鍵で Firestore のデータを暗号化する方式。**コード変更不要**で導入できる。

### メリット
- 鍵の無効化・破棄でデータへのアクセスを即時遮断できる（企業の契約終了時に有用）
- 鍵のローテーション、監査ログを自社で管理可能
- Firestore の SLA・パフォーマンスに影響なし

### 実装手順

```bash
# 1. Cloud KMS で鍵を作成
gcloud kms keyrings create shokujii-keyring --location=asia-northeast1
gcloud kms keys create shokujii-key \
  --keyring=shokujii-keyring \
  --location=asia-northeast1 \
  --purpose=encryption

# 2. Firestoreデータベースに鍵を紐付け（DB作成時に指定）
gcloud firestore databases create \
  --location=asia-northeast1 \
  --kms-key-name=projects/PROJECT_ID/locations/asia-northeast1/keyRings/shokujii-keyring/cryptoKeys/shokujii-key
```

### コスト
Cloud KMS の料金（鍵の管理 + 暗号化/復号オペレーション）。Firestore は5分ごとに暗号化/復号を実行するため、一般的に低額。

### 注意点
- CMEK は **Firestore データベース作成時** に設定する必要がある。既存DBへの後付けは不可
- 将来的に CMEK を導入する場合は、新規DBを作成してデータを移行する必要がある

## 3.3 フィールドレベル暗号化

特定の機密フィールド（例: 従業員の個人情報）のみ、アプリ側で暗号化してから Firestore に書き込む方式。

### 実装イメージ

```typescript
// Google の暗号化ライブラリ Tink + Cloud KMS を使用
import { AeadKeyTemplates, KeysetHandle, Aead } from '@aspect/tink'

// 暗号化して書き込み
const aead = await keysetHandle.getPrimitive(Aead)
const encrypted = await aead.encrypt(
  Buffer.from(personalData),
  Buffer.from(enterpriseId)  // AAD（追加認証データ）にテナントIDを使用
)
await setDoc(doc(db, 'users_personal_information', userId), {
  encrypted_data: encrypted,
})

// 復号して読み取り
const decrypted = await aead.decrypt(encrypted, Buffer.from(enterpriseId))
```

### 注意点
- **暗号化したフィールドでは Firestore のクエリ（where, orderBy）が使えなくなる**
- 検索が必要なフィールドは暗号化できないため、適用対象は個人情報等の限定的なフィールドに限る
- 暗号化/復号の処理コストがフロントエンドまたは Functions に発生する


# 4. 物理分離（企業ごとProject）

## 4.1 概要

`01_仕様概要.md` および `02_アーキテクチャ.md` で決定した通り、MVPでは物理分離は行わない。ここでは将来的に特定企業のみ物理分離が必要になった場合の移行手順を記載する。

## 4.2 移行手順（特定企業のみ分離する場合）

```
1. 新規 Firebase Project を作成
2. enterprise_id でフィルタして該当企業のデータをエクスポート
   → gcloud firestore export でコレクション単位でエクスポート
   → enterprise_id でフィルタして新Projectにインポート
3. Firebase Auth ユーザーを新Projectに移行
   → Admin SDK でユーザーデータをエクスポート → 新Projectにインポート
   → custom claims を再設定
4. Functions / Hosting / Security Rules を新Projectにデプロイ
5. DNS を新Projectの Hosting に切り替え
6. 旧Projectから該当企業のデータを削除
```

## 4.3 最大の課題: 飲食店データの扱い

分離先のProjectでも飲食店データ（partners / shops / menus）が必要になる。対応方式は以下のいずれか。

| 方式 | 内容 | 工数 |
|:--|:--|:--|
| **データコピー** | 飲食店データを分離先Projectにコピーし、定期同期する | 高（同期基盤の開発が必要） |
| **API経由で取得** | 分離先ProjectのアプリがメインProjectの Functions 経由で飲食店データを取得する | 中（APIの開発が必要） |

いずれも大きな開発工数が発生するため、物理分離は「コンプライアンス上必須」となった企業に限定し、追加費用を請求する前提で対応する。

## 4.4 enterprise_id 設計による移行容易性

`02_アーキテクチャ.md` で定義した通り、すべてのドキュメントに `enterprise_id` を持たせているため、特定企業のデータのみを抽出・移行することは技術的に可能。全企業を一斉に分離する必要はなく、要件が出た企業のみ個別対応できる。

---

# 3. セキュリティ対応 全項目一覧


エンタープライズ版に関連するセキュリティ・コンプライアンス項目の全体像とステータスを管理する。
各項目の技術詳細は「参照先」のドキュメントを参照。

**ステータス定義:**

| ステータス | 意味 |
|:--|:--|
| **MVP対応済み** | MVP の設計・実装に組み込み済み |
| **検討済み** | 技術調査・方針決定が完了。実装は Phase 2 以降 |
| **ドラフト済み** | ドラフトは作成したが最終化していない（法務チェック・経営判断待ち等） |
| **回答準備済み** | セキュリティチェックシートへの回答が準備できている |
| **要検討** | 未着手。技術調査または方針決定が必要 |

**関連ドキュメント:**

| 略称 | ドキュメント |
|:--|:--|
| `02_アーキテクチャ` | `02_アーキテクチャ.md` |
| `_技術詳細` | `03_エンタープライズ_セキュリティ対応_技術詳細.md` |
| `_チェックシート` | `03_エンタープライズ_セキュリティチェックシート.md` |
| `_ポリシー等` | `03_エンタープライズ_セキュリティポリシー_インシデント対応フロー_プライバシーポリシー.md` |
| `_Pマーク・ISO` | `03_エンタープライズ_Pマーク・ISO取得について.md` |


# 1. 認証・アクセス制御

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| A1 | **SSO（SAML/OIDC）** | 検討済み | Phase 2 | `_技術詳細` 1章 |
| A2 | **セッション管理（タイムアウト・即時無効化）** | MVP対応済み | MVP | `02_アーキテクチャ` 4.3 |
| A3 | **監査ログ** | MVP対応済み | MVP | `02_アーキテクチャ` 7章 |
| A4 | **多要素認証（MFA）** | 要検討 | Phase 2 | — |
| A5 | **アカウントロックアウト** | 要検討 | Phase 2 | — |


# 2. データ保護・暗号化

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| B1 | **デフォルト暗号化（AES-256）** | MVP対応済み | MVP | `_技術詳細` 3.1 |
| B2 | **Storage パス設計（enterprise_id）** | MVP対応済み | MVP | `02_アーキテクチャ` 3.5 |
| B3 | **CMEK（顧客管理暗号鍵）** | 検討済み | Phase 2 | `_技術詳細` 3.2 |
| B4 | **フィールドレベル暗号化** | 検討済み | 必要時のみ | `_技術詳細` 3.3 |


# 3. ネットワーク・インフラ

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| C1 | **IPアドレス制限（API層）** | 検討済み | Phase 2 | `_技術詳細` 2章 |
| C2 | **IPアドレス制限（完全：Cloud Armor）** | 検討済み | 大企業要件時のみ | `_技術詳細` 2.3 |
| C3 | **データ保管リージョンの明示** | 回答準備済み | MVP〜 | `_チェックシート` 4-1 |
| C4 | **DDoS 対策の説明** | 回答準備済み | MVP〜 | `_チェックシート` 3-2 |
| C5 | **通信暗号化（TLS）の説明** | 回答準備済み | MVP〜 | `_チェックシート` 3-5 |
| C6 | **API レート制限** | 要検討 | Phase 2 | — |


# 4. 監査・コンプライアンス

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| D1 | **サブプロセッサーの開示** | 回答準備済み | MVP〜 | `_チェックシート` 9章、`_ポリシー等` Part 1 セクション6 |
| D2 | **データ削除・契約終了時の対応** | 検討済み | Phase 2 | `02_アーキテクチャ` 3.5、`_ポリシー等` Part 3 セクション5.3 |
| D3 | **第三者監査・認証（Pマーク / ISO）** | 検討済み | 中期 | `_Pマーク・ISO` |
| D4 | **個人情報保護法対応（プライバシーポリシー）** | ドラフト済み | Phase 2 | `_ポリシー等` Part 3（法務チェック待ち） |
| D5 | **データ保持ポリシー** | 要検討 | Phase 2 | — |
| D6 | **データエクスポート（ポータビリティ）** | 要検討 | Phase 2 | — |


# 5. アプリケーションセキュリティ

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| E1 | **XSS / CSRF 対策** | 回答準備済み | MVP〜 | `_チェックシート` 8-4（Vue デフォルト対策。コード監査は未実施） |
| E2 | **脆弱性診断・ペネトレーションテスト** | 要検討 | Phase 2 | — |
| E3 | **Content Security Policy（CSP）** | 要検討 | Phase 2 | — |
| E4 | **ファイルアップロード制限** | 要検討 | Phase 2 | — |


# 6. 運用・SLA

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| F1 | **インシデント対応プロセス** | ドラフト済み | MVP〜 | `_ポリシー等` Part 2（担当者・連絡先の決定待ち） |
| F2 | **セキュリティポリシー** | ドラフト済み | MVP〜 | `_ポリシー等` Part 1（経営層の承認待ち） |
| F3 | **バックアップ・リカバリ** | 回答準備済み | MVP〜 | `_チェックシート` 4-2（実環境の設定確認が必要） |
| F4 | **SLA（稼働率保証）** | ドラフト済み | MVP〜 | `_チェックシート` 7-1（具体的な数値の経営判断待ち） |
| F5 | **障害時の連絡体制** | ドラフト済み | MVP〜 | `_ポリシー等` Part 2 セクション5-6（具体的な担当者の決定待ち） |
| F6 | **脆弱性パッチ適用体制** | 要検討 | MVP〜 | — |


# 7. 物理分離

| # | 項目 | ステータス | 想定フェーズ | 参照先 |
|:--|:--|:--|:--|:--|
| G1 | **物理分離（企業ごとProject）** | 検討済み | 大企業要件時のみ | `_技術詳細` 4章 |


---


# ステータスサマリ

| ステータス | 件数 | 項目 |
|:--|:--|:--|
| **MVP対応済み** | 4件 | セッション管理、監査ログ、デフォルト暗号化、Storage パス設計 |
| **検討済み** | 7件 | SSO、CMEK、フィールドレベル暗号化、IP制限（API層）、IP制限（完全）、データ削除対応、第三者認証方針、物理分離 |
| **ドラフト済み** | 4件 | プライバシーポリシー、インシデント対応、セキュリティポリシー、SLA、障害時連絡体制 |
| **回答準備済み** | 5件 | サブプロセッサー、リージョン、DDoS、TLS、XSS/CSRF、バックアップ |
| **要検討** | 7件 | MFA、アカウントロックアウト、APIレート制限、データ保持ポリシー、データエクスポート、脆弱性診断、CSP、ファイルアップロード制限、脆弱性パッチ |

**次に着手すべき項目:**

| 優先度 | 項目 | 理由 |
|:--|:--|:--|
| 高 | ドラフト済み項目の最終化 | 担当者決定・経営判断・法務チェックのみで完了。技術作業なし |
| 中 | D5 データ保持ポリシー | プライバシーポリシーの最終化に必要（保管期間の決定） |
| 中 | F6 脆弱性パッチ適用体制 | npm audit の頻度を決めるだけ。チェックシートで聞かれる |
| 低 | A4 MFA / A5 ロックアウト | Phase 2 の実装。SSO 対応後に検討 |

---

# 4. セキュリティポリシー・インシデント対応・プライバシー


本ドキュメントは、エンタープライズ版の導入審査・セキュリティチェックシート対応に向けた社内ドラフトである。
最終版は経営層の承認および法務チェックを経て確定する。

**ドキュメント状態: ドラフト（未承認）**


---


# Part 1: 情報セキュリティポリシー


## 1. 基本方針

株式会社〇〇（以下「当社」）は、shokujii サービスの開発・運用において、お客様からお預かりする情報資産を適切に保護し、信頼されるサービスを継続的に提供することを最重要事項と位置づけます。

当社は、以下の原則に基づき、情報セキュリティの確保に取り組みます。

1. お客様のデータを機密性・完全性・可用性の観点から適切に保護します
2. 法令および契約上の要求事項を遵守します
3. 情報セキュリティに関するリスクを継続的に評価し、適切な対策を講じます
4. 全従業員に対して情報セキュリティの重要性を周知し、教育を実施します
5. 情報セキュリティに関する施策を定期的に見直し、改善します


## 2. 適用範囲

- shokujii サービス（プラットフォーム版・エンタープライズ版）の開発・運用に関わる全業務
- 上記に関わる当社の全従業員・業務委託者


## 3. データ保護

### 3.1 暗号化

| 対象 | 方式 |
|:--|:--|
| 保存データ | AES-256 による自動暗号化（Google Cloud 管理鍵） |
| 通信データ | TLS 1.2 以上による暗号化 |
| 決済情報 | Stripe が PCI DSS Level 1 準拠環境で処理。当社にカード情報は保存されない |

### 3.2 データ保管

- すべてのデータは Google Cloud 東京リージョン（asia-northeast1）に保管する
- 日本国外へのデータ転送は原則行わない

### 3.3 テナント分離

- 企業ごとに `enterprise_id` を付与し、論理的にデータを分離する
- Firestore Security Rules により、他社データへのアクセスを技術的に防止する


## 4. アクセス制御

### 4.1 ユーザーアクセス

- 企業メールドメインによるログイン制限
- Firebase Auth の custom claims による権限管理（全社管理者 / 一般従業員）
- 60分無操作でのセッション自動タイムアウト

### 4.2 管理アクセス

- Firebase / GCP コンソールへのアクセスは最小限の担当者に限定する
- 本番環境への直接的なデータ操作は原則禁止し、Cloud Functions 経由で実行する
- GCP の IAM により、開発者・運用者の権限を最小権限の原則で設定する


## 5. 監査・ログ

- ログイン/ログアウト、管理者操作、注文/キャンセル等の操作ログを記録する
- ログは Firestore および Cloud Logging に保存する
- ログへのアクセスは全社管理者（顧客側）および当社の運用担当者に限定する
- クライアントからのログの直接改ざんは Security Rules で技術的に防止する


## 6. 外部委託管理

当社サービスは以下の外部サービスを利用している。各サービスのセキュリティ認証を確認の上、利用を決定している。

| サービス | 提供元 | 用途 | セキュリティ認証 |
|:--|:--|:--|:--|
| Firebase / GCP | Google | インフラ全般 | SOC1/2/3, ISO27001/27017/27018 |
| Stripe | Stripe, Inc. | 決済処理 | PCI DSS Level 1, SOC1/2 |
| SendGrid | Twilio Inc. | メール配信 | SOC2, ISO27001 |


## 7. 継続的改善

- 本ポリシーは年1回以上見直しを行う
- セキュリティインシデント発生時は、再発防止策を講じた上で必要に応じてポリシーを更新する


## 承認

| 項目 | 内容 |
|:--|:--|
| 制定日 | 〇〇年〇〇月〇〇日 |
| 最終更新日 | 〇〇年〇〇月〇〇日 |
| 承認者 | 〇〇（代表取締役 / CTO） |


---


# Part 2: インシデント対応フロー


## 1. 目的

情報セキュリティインシデントが発生した際に、被害の拡大を防止し、迅速に復旧し、影響を受ける顧客に適切に通知するための手順を定める。


## 2. インシデントの定義

以下の事象をセキュリティインシデントと定義する。

| 分類 | 事象例 |
|:--|:--|
| データ漏洩 | 顧客データへの不正アクセス、意図しないデータ公開 |
| サービス停止 | shokujii サービスの全面または一部停止 |
| 不正アクセス | 第三者によるシステムへの不正侵入 |
| データ改ざん | 顧客データの意図しない変更・削除 |
| 脆弱性の悪用 | 既知または未知の脆弱性を利用した攻撃 |


## 3. 重大度分類

| 重大度 | 定義 | 例 |
|:--|:--|:--|
| **Critical** | データ漏洩またはサービス全面停止 | 顧客の個人情報が外部に流出、Firestore への全アクセス不能 |
| **High** | 一部機能停止または特定企業のデータに影響 | 特定企業のデータが他社から閲覧可能、決済機能の停止 |
| **Medium** | パフォーマンス低下、軽微な不具合 | レスポンスタイムの大幅な悪化、一部画面の表示崩れ |
| **Low** | 潜在的リスク（直接的な影響なし） | 脆弱性の発見（未悪用）、依存パッケージのセキュリティアラート |


## 4. 対応フロー

```
検知 → トリアージ → 初動対応 → 顧客通知 → 原因調査 → 復旧 → 振り返り
```

### 4.1 検知

| 検知方法 | 説明 |
|:--|:--|
| Cloud Logging アラート | エラー率の急増、異常なリクエストパターンを自動検知 |
| 監査ログの異常 | 通常と異なるログインパターン、大量のデータアクセス |
| ユーザーからの報告 | 導入企業の全社管理者からの連絡 |
| 外部からの報告 | セキュリティ研究者等からの脆弱性報告 |
| 依存パッケージのアラート | npm audit、GitHub Dependabot 等 |

### 4.2 トリアージ（重大度判定）

- 検知者は速やかに対応責任者に報告する
- 対応責任者は重大度を判定し、以下のタイムラインに従い対応を開始する

### 4.3 対応タイムライン

| 重大度 | 初動対応 | 顧客通知 | 復旧目標 |
|:--|:--|:--|:--|
| **Critical** | 検知後 **1時間以内** | 検知後 **4時間以内** | 最優先で対応 |
| **High** | 検知後 **4時間以内** | 検知後 **24時間以内** | 24時間以内 |
| **Medium** | 翌営業日まで | 必要に応じて | 計画的に対応 |
| **Low** | 1週間以内に評価 | 通知不要 | 計画的に対応 |

### 4.4 初動対応

1. 被害の拡大防止（影響範囲の特定・隔離）
2. 証拠の保全（ログの保存、スクリーンショット等）
3. 応急措置の実施（アクセス遮断、機能停止等）

### 4.5 顧客通知

- 影響を受ける企業の全社管理者に対してメールで通知する
- 通知内容: 発生事象、影響範囲、対応状況、今後の予定
- Critical / High の場合、第一報は簡潔に速やかに送り、詳細は後続の報告で補完する

**通知テンプレート（第一報）:**

```
件名: 【重要】shokujii セキュリティインシデントのご報告

〇〇株式会社 ご担当者様

平素よりshokujiiをご利用いただきありがとうございます。

下記の事象が発生いたしましたのでご報告いたします。

■ 発生日時: 〇〇年〇〇月〇〇日 〇〇:〇〇
■ 事象概要: （概要を記載）
■ 影響範囲: （影響範囲を記載）
■ 現在の対応状況: （対応状況を記載）
■ 今後の予定: （予定を記載）

詳細が判明次第、改めてご報告いたします。
ご不明点がございましたら、下記までお問い合わせください。

連絡先: 〇〇
```

### 4.6 原因調査・復旧

1. 根本原因の特定
2. 恒久的な修正の実施
3. 修正の検証
4. サービスの復旧確認

### 4.7 振り返り（ポストモーテム）

インシデント収束後、1週間以内にポストモーテムを実施する。

| 項目 | 内容 |
|:--|:--|
| タイムライン | 検知から復旧までの経緯 |
| 根本原因 | なぜ発生したか |
| 影響範囲 | 何が・誰が影響を受けたか |
| 対応の評価 | 対応は適切だったか、改善点はあるか |
| 再発防止策 | 技術的・運用的な再発防止策 |


## 5. 対応体制

| 役割 | 担当 | 責務 |
|:--|:--|:--|
| **対応責任者** | 〇〇（要決定） | 重大度判定、対応方針の決定、顧客通知の承認 |
| **技術対応** | 開発チーム | 原因調査、修正実施、復旧作業 |
| **顧客対応** | 〇〇（要決定） | 顧客への通知・問い合わせ対応 |
| **経営報告** | 〇〇（要決定） | Critical の場合、経営層への報告 |

> **TODO:** 対応責任者・顧客対応・経営報告の具体的な担当者を決定する


## 6. 連絡先

| 連絡先 | 宛先 | 用途 |
|:--|:--|:--|
| 社内緊急連絡先 | 〇〇（要決定） | インシデント検知時の第一報 |
| 顧客向け連絡先 | 〇〇（要決定） | 導入企業からの報告受付 |

> **TODO:** 具体的な連絡先（メールアドレス、電話番号等）を決定する


---


# Part 3: プライバシーポリシー（エンタープライズ版）ドラフト

> **注意:** 本ドラフトは技術的なデータ取扱いの観点からの骨格であり、法務チェックを経て最終化する必要がある。


## 1. 個人情報の取得と利用目的

### 1.1 取得する個人情報

| 情報 | 取得元 | 保管先 |
|:--|:--|:--|
| メールアドレス | ログイン時にユーザーが入力 | Firebase Authentication |
| 氏名 | アカウント作成時に管理者が登録 or ユーザーが入力 | Firestore（`/users`） |
| 所属企業・部署 | アカウント一括作成時に管理者が登録 | Firestore（`/enterprises/{id}/members`） |
| 注文履歴 | サービス利用時に自動記録 | Firestore（`/orders`） |
| 操作ログ | サービス利用時に自動記録 | Firestore（`/audit_logs`）、Cloud Logging |
| IPアドレス | アクセス時に自動取得（監査ログ用） | Firestore（`/audit_logs`）、Cloud Logging |

### 1.2 取得しない情報

| 情報 | 理由 |
|:--|:--|
| クレジットカード番号 | Stripe が PCI DSS 準拠環境で処理。当社システムにカード情報は一切保存されない |
| パスワード | パスワードレス認証（OTP）を採用しているため不要 |


## 2. 利用目的

| 利用目的 | 対象データ |
|:--|:--|
| 本人認証 | メールアドレス |
| サービス提供（注文処理、通知送信） | 氏名、メールアドレス、注文履歴 |
| 利用状況の集計・ダッシュボード表示 | 注文履歴、所属企業・部署 |
| セキュリティ確保（不正アクセス検知） | 操作ログ、IPアドレス |
| カスタマーサポート | 氏名、メールアドレス、注文履歴 |

上記以外の目的で個人情報を利用することはありません。


## 3. 第三者提供・外部委託

当社は、以下のサービスに個人情報の処理を委託しています。

| 委託先 | 委託する情報 | 目的 | 所在国 |
|:--|:--|:--|:--|
| Google LLC（Firebase） | 全ユーザーデータ | データベース保管、認証処理 | 日本（東京リージョン） |
| Stripe, Inc. | 決済に必要な情報 | 決済処理 | 米国（PCI DSS 準拠） |
| Twilio Inc.（SendGrid） | メールアドレス | OTP 送信、通知メール配信 | 米国 |

上記以外の第三者に個人情報を提供することはありません（法令に基づく場合を除く）。


## 4. データの保管

### 4.1 保管場所

すべてのデータは Google Cloud 東京リージョン（asia-northeast1）に保管します。

### 4.2 暗号化

- 保存時: AES-256 による自動暗号化
- 通信時: TLS 1.2 以上による暗号化

### 4.3 アクセス制御

- 企業ごとにデータを論理的に分離し、他社のデータにはアクセスできない設計
- 当社スタッフのデータアクセスは、カスタマーサポートおよび障害対応に必要な場合に限定

### 4.4 保管期間

| データ | 保管期間 |
|:--|:--|
| アカウント情報 | 契約期間中。契約終了後〇〇日以内に削除（**要決定**） |
| 注文履歴 | 契約期間中。契約終了後〇〇日以内に削除（**要決定**） |
| 操作ログ（Firestore） | 契約期間中（Phase 2 で TTL 設定を検討） |
| 操作ログ（Cloud Logging） | 30日間（GCP デフォルト） |

> **TODO:** 契約終了後のデータ保管期間を決定する（一般的には30日〜90日）


## 5. 個人情報の開示・訂正・削除

### 5.1 ユーザー本人からの請求

ユーザー本人は、当社に対して以下の請求を行うことができます。

| 請求 | 対応方法 |
|:--|:--|
| **開示請求** | 当社が保有する本人の個人情報の内容を開示 |
| **訂正請求** | 個人情報の内容が事実と異なる場合の訂正 |
| **削除請求** | 個人情報の削除 |

### 5.2 企業管理者からの請求

導入企業の全社管理者は、管理者画面から以下の操作を行えます。

| 操作 | 方法 |
|:--|:--|
| メンバー情報の確認 | 管理者画面のメンバー一覧 |
| メンバー情報の変更 | 管理者画面のメンバー編集 |
| アカウント停止（論理削除） | 管理者画面のアカウント管理 |

### 5.3 契約終了時

契約終了時は、当該企業に関するすべてのデータを削除します。

- Firestore: `enterprise_id` でフィルタし一括削除
- Firebase Authentication: 該当ユーザーの一括削除
- Cloud Storage: `/enterprises/{enterpriseId}/` 以下を一括削除
- 削除完了後、企業の求めに応じて**削除証明書**を発行可能

> **TODO:** 削除証明書のテンプレートを作成する


## 6. お問い合わせ窓口

個人情報の取扱いに関するお問い合わせは、以下までご連絡ください。

| 項目 | 内容 |
|:--|:--|
| 窓口名称 | 〇〇（要決定） |
| メールアドレス | 〇〇（要決定） |
| 対応時間 | 平日 〇〇:〇〇 〜 〇〇:〇〇（要決定） |


---


# 未決定事項まとめ（TODO）

本ドラフトの最終化に向けて、以下の事項を決定する必要がある。

| # | 項目 | 所在 | 決定に必要なこと |
|:--|:--|:--|:--|
| 1 | インシデント対応責任者 | Part 2 セクション5 | 社内の役割分担の決定 |
| 2 | 顧客向け連絡先 | Part 2 セクション6 | 専用メールアドレス等の用意 |
| 3 | インシデント通知SLA の最終確認 | Part 2 セクション4.3 | 経営判断（ドラフト案: Critical 4時間、High 24時間） |
| 4 | 契約終了後のデータ保管期間 | Part 3 セクション4.4 | 法務確認（一般的には30日〜90日） |
| 5 | 削除証明書テンプレート | Part 3 セクション5.3 | テンプレート作成 |
| 6 | プライバシーポリシーの法務チェック | Part 3 全体 | 弁護士によるレビュー |
| 7 | セキュリティポリシーの経営層承認 | Part 1 承認欄 | 代表取締役 / CTO の承認 |
| 8 | 問い合わせ窓口の設置 | Part 3 セクション6 | 専用窓口の用意 |
