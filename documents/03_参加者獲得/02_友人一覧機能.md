# 友人一覧機能（たべとも一覧）

> **関連仕様**: イベントへの招待（注文完了モーダル・メール送信）は [02_友人招待機能.md](./02_友人招待機能.md) を参照する。  
> **テンプレート**: 本仕様の構成は [02_仕様書追加ルールとテンプレート.md](../00_仕様概要/02_仕様書追加ルールとテンプレート.md) に準拠する。

## 1. 概要

### 1.1 機能の目的

食事会（イベント）に**同席した履歴**に基づき「たべとも」を把握し、**ユーザーページ**で振り返りやすくする。本人だけでなく**他者のユーザーページからも閲覧でき**、関係の可視化を通じてサービス内のつながりを広げる。

### 1.2 主要な機能

- ユーザーページ（`/u/:userId`）に「**たべとも**」タブを設け、**友人（同席者）**を一覧表示する。本人・他のログインユーザー・**未ログインユーザー**を問わず**同じ一覧**を返す（Phase 1: 完全公開）
- 同席回数・初回・最終の会合日（イベント基準日時）を表示する
- 同席者関係のデータを Firestore 上に持ち、**リリース時バッチ**と**注文（参加）確定時の双方向更新**で整備する（**総浚いは行わない**）
- 将来的にユーザー設定で**公開範囲（完全公開／友人限定／友人の友人まで／非公開）**および**表示項目**を制御できるよう拡張する（Phase 2、別途仕様）

### 1.3 対象ユーザー

- **たべとも一覧に表示される対象**: 本サービスにログインし、**イベントに参加（注文）**する一般ユーザー
- **閲覧者**: 本人・他のログインユーザー・**未ログインユーザー**いずれも閲覧可（Phase 1: 完全公開）

### 1.4 用語・名称

- **友人（たべとも）**: 同一イベントに**参加が確定した**相手。一度同席で関係が成立。本仕様のタブ名は「**たべとも**」。[02_マイページ.md](./02_マイページ.md) のプロフィール UI ではコピー上「**ともだち**」表記（同一データ・同一 Callable）

## 2. ねらい

### 2.1 ビジネス目標

- **リピート参加・紹介**につながる「関係の可視化」で、プロダクトの体験価値を高める
- 参加者同士の**つながり**を土台に、関連機能（例: 友人招待）の利用機会を生む

### 2.2 ユーザー価値

- 「**いつ・どの食事会で一緒だったか**」を思い出しやすくする
- 同席回数を通じた**関係の深まり**を感じられ、再参加の動機付けに寄与する

### 2.3 成功指標（KPI）

- ユーザーページ「たべとも」タブの**閲覧数・閲覧率**（本人 / 他者の内訳も計測対象）
- **たべとも**として登録された**関係の件数**の推移
- 同一ユーザーあたりの**平均同席者数**（指標化する場合は計測定義を合わせる）

## 3. 前提条件

### 3.1 技術的前提条件

- **クライアント**: `user` アプリ（Vue 3 + Vite + Vuetify 3、パス `/user/`）
- **バックエンド**: Firebase Functions `functions/default`（TypeScript）
- **データストア**: Cloud Firestore。友人データは `users` の**サブコレクション**として保持
- イベント・注文（参加確定）のデータが既存どおり存在し、**参加確定**の判定ができること
- Firestore フィールド命名は**プロジェクト規約**に従う（`common` では **snake_case** ・ `created_at` / `updated_at` 等。詳細は [3.4 フィールド命名と Db/App の扱い](#34-フィールド命名と-dbapp-の扱い)）

### 3.2 ビジネス的前提条件

- 「一緒にいた」の根拠は、**当該イベントへの参加（注文）が確定した**こと（`order.status === 'ordered'`）に基づく
- 友人成立タイミングは**注文確定時**（イベント終了を待たない）。`event_at` には **`events/{event_id}.event_start_datetime`（イベント開始日時）**を格納する
- **総浚い**（注文のたびに二人の**過去全イベント**を再探索する）**は採用しない**
- **デノーマライズ（コピー）方針は採らない**: 相手の `user_name` / `user_image_url` などの**プロフィール情報を `friends` ドキュメントへコピー保持しない**。表示に必要な相手情報は**表示時に `users` を解決**して返す。  
  - 理由: 今後 `users` 側に**タグ**・**SNS リンク**・自己紹介など**追加プロフィール項目**を表示する可能性があり、その都度 `friends` 側のスキーマ追加と**伝播トリガー**を増やすコストを避ける。**`users` を真とする一元化**で、新項目追加時はクライアント取得側のみの対応で済む構造を維持する
- **公開範囲（Phase 1）**: 任意のユーザーページのたべとも一覧は、**閲覧者の認証有無を問わず**取得できる「**完全公開**」を既定とする
- **公開範囲（Phase 2 / 別途仕様）**: ユーザー設定で**公開範囲（完全公開／友人限定／友人の友人まで／非公開）**および**表示項目（プロフィール写真／表示名／同席回数／日付などをチェックボックスで選択）**を制御できるようにする。詳細は別途仕様化する

### 3.3 依存関係

- [02_友人招待機能.md](./02_友人招待機能.md): 招待候補の取得等は、本仕様の友人データ・`GET` 方針に従う。**Callable は本仕様の `getUserFriends` を流用**することを基本とし、招待モーダルでの絞り込み（検索 q、人数の上限を緩める等）が必要になった段階で**派生 Callable**を追加検討する（R-8）
- 注文・イベントのドキュメント・ステータス（例: `ordered`）: 同席者一覧の解決元

### 3.4 フィールド命名と Db/App の扱い

- 本仕様の **DB（Firestore）上のフィールド名**は **snake_case** で記載する（[02_仕様書追加ルールとテンプレート.md](../00_仕様概要/02_仕様書追加ルールとテンプレート.md) 3.2 に準拠）
- 実装時は `common/src/schemas` の **DbSchema / AppSchema** および `TimestampSchema` / `EpochMillisSchema` のパターンに合わせる（既存: `EventMemberOrder` 等）

## 4. 画面仕様

### 4.1 画面遷移

- 任意の閲覧者（本人 / 他のログインユーザー / 未ログイン）が **ユーザーページ（`/u/:userId`）**を開く
- タブ（または同等 UI）で **「たべとも」**を選択
- **対象ユーザーが存在しない / 退会済み（`is_deleted === true`）**の場合は、ユーザーページ全体として「**存在しないユーザーです**」と表示し、たべともタブは出さない（実装はユーザーページ側に集約。本仕様では Callable が `HttpsError('not-found')` を返すまでを担保する）
- 一覧でカードをタップした場合の遷移（例: 相手の公開プロフィール）がある場合は **Phase 2 以降**で定義可

### 4.2 UI 要素

#### 4.2.1 表示要素（「たべとも」タブ内）

- **友人 1 人あたり**（`base/src/components/FriendCard.vue`）  
  - プロフィール写真・表示名（**相手のユーザーページへリンク**）  
  - **一緒に参加した回数**（`meet_count`）  
  - **最後に会った日** / **初めて会った日**（`last_met_at` / `first_met_at`。同席 1 回のみの関係は 1 行にまとめる）  
  - **「同席イベントの記録を見る」** → `FriendMeetLogDialog`（クリック時に `getUserFriendMeetLog` で遅延取得。一覧 API に `meet_log` は含めない）
- **一覧**: カード型レイアウト
- **無限スクロール**（`IncrementalLoader` + `useUserFriendsStore`）。初回 N 件＋ `has_more` の継ぎ足し
- **日付と限定公開イベント**: カードの日付は **`friends` サブコレの冗長フィールド**（限定公開イベント由来も含む）。**meet log** は [02_マイページ.md](./02_マイページ.md) **§4.2.0 / §4.2.6** に従う（限定公開も本人・他者とも表示、リンクは本人のみ、限定公開タグなし）

#### 4.2.2 ソート・件数

- デフォルト: **同席回数**の多い順
- 切替: **最後に会った日**の**新しい**順
- 1 回あたり **10 件**（仕様上の目安。実装は `limit` パラメータと揃えてもよい）

#### 4.2.3 状態表示

- 初回・追加読み込み時の**ローディング**
- **空状態**（たべとも 0 人）のメッセージ（R-7・[02_マイページ.md](./02_マイページ.md) §4.2.7 と整合）:
  - **本人ページ**: `user.friend_empty_tab`（「ともだちは、まだいません。」相当）。**CTA は出さない**（ソートトグルも非表示）
  - **他者ページ**: `user.friend_empty_other`（「{name} さんにはまだ「ともだち」がいません。」相当）
- 取得失敗時の**エラー表示**と、可能な範囲の**再試行**

## 5. 技術仕様・データ構造設計

### 5.1 Firestore スキーマ設計

#### 5.1.1 コレクション構造

**新規サブコレクション**: `users/{user_id}/friends/{friend_user_id}`


| 項目         | 内容                                                                         |
| ---------- | -------------------------------------------------------------------------- |
| 親          | `users` コレクションの `user_id`（Firebase Auth UID）                               |
| 子ドキュメント ID | 友人側の `friend_user_id`（相手の UID）                                             |
| 説明         | ログインユーザー `user_id` 視点の「**たべとも** 1 人分」の関係。双方向（A-B と B-A）の **2 ドキュメント**を維持する |


既存のツリーに対する追記例:

```
users/{user_id}
  /friends/{friend_user_id}  （本仕様で新設）
```

#### 5.1.2 ドキュメントスキーマ（論理名・型）


| フィールド           | 型               | 必須  | 説明                                                            |
| --------------- | --------------- | --- | ------------------------------------------------------------- |
| `first_met_at`  | Timestamp       | 必須  | 一覧用。**派生**。`event_history[].event_at` の最小。同一 `event_id` は 1 件 |
| `last_met_at`   | Timestamp       | 必須  | 一覧用・ソート用。**派生**。**最大**                                        |
| `meet_count`    | number (int)    | 必須  | 同席した**イベント**数。`event_history` のユニーク `event_id` 件数と一致          |
| `event_history` | array of object | 必須  | `{ event_id, community_id, event_at }`。**event_id 単位で重複なし**    |
| `created_at`    | Timestamp       | 必須  | 初回作成                                                          |
| `updated_at`    | Timestamp       | 必須  | 最終更新                                                          |

> **配列上限への備え**: `event_history` を配列で持つため、**Firestore の 1 ドキュメント 1MB 制限**に長期で抵触する懸念がある。Phase 1 では配列のままとし、件数が多くなる兆候が見えた段階で **`users/{user_id}/friends/{friend_user_id}/meetings/{event_id}` サブコレクションへの分離**を Phase 3 で検討する（直近 N 件のみ配列で保持する案を含む）。

> **デノーマライズしない方針**: 上記スキーマには **`user_name` / `user_image_url` などの相手プロフィールフィールドを含めない**。理由は 3.2 に記載のとおり、将来 `users` 側に**タグ・SNS リンク・自己紹介**などの追加項目を表示する可能性があり、`users` を**真**として表示時に解決する方が、追加項目発生時の改修コストとデータ整合リスク（伝播トリガー失敗による不整合）を抑えられるため。表示に必要な相手情報は **5.5.1 の Callable 内で `users` を都度解決**する。


`event_history[]` 各要素:


| フィールド          | 型         | 必須  | 説明                                                                                       |
| -------------- | --------- | --- | ---------------------------------------------------------------------------------------- |
| `event_id`     | string    | 必須  | 対象イベント ID                                                                                |
| `community_id` | string    | 必須  | 当該イベントが属するコミュニティ ID（B-2 / 将来のコミュニティ別フィルタや分析のために保持）                                      |
| `event_at`     | Timestamp | 必須  | **会合の基準日時**。`events/{event_id}.event_start_datetime` を**注文確定時点でスナップショット**として格納（後述 S-8）。 |


**整合**: `first_met_at` / `last_met_at` / `meet_count` は `event_history` と矛盾しないよう、更新トランザクションまたは単一箇所の更新ロジックで保つ。

> **`event_at` の不変性**: `events.event_start_datetime` は管理者が後から変更しうるが、`event_history[].event_at` は**注文確定時点の値で固定**する（後追いで書き換えない）。理由は、過去の同席実績の `first_met_at` / `last_met_at` がイベントの予定変更で動くと UX 上違和感があり、双方向ドキュメントの整合維持コストも増えるため。

#### 5.1.3 Zod スキーマ定義（配置イメージ）

`common` に `UserFriend`（仮名）用の Db / App スキーマを追加する。**DbSchema は `TimestampSchema`、AppSchema は `EpochMillisSchema`** で揃える（既存 `EventMemberOrder` 等と同じ慣習）。例（実装時にファイル名・export 名を確定）:

```typescript
// common/src/schemas/userFriend.ts（仮の配置）
import { z } from 'zod'
import { TimestampSchema, EpochMillisSchema } from './firebase/index.js'

const EventHistoryEntryDbSchema = z.object({
  event_id: z.string().nonempty(),
  community_id: z.string().nonempty(),
  event_at: TimestampSchema,
})

const EventHistoryEntryAppSchema = z.object({
  event_id: z.string().nonempty(),
  community_id: z.string().nonempty(),
  event_at: EpochMillisSchema,
})

export const UserFriendDbSchema = z.object({
  first_met_at: TimestampSchema,
  last_met_at: TimestampSchema,
  meet_count: z.number().int().nonnegative(),
  event_history: z.array(EventHistoryEntryDbSchema),
  created_at: TimestampSchema,
  updated_at: TimestampSchema,
})

export const UserFriendAppSchema = z.object({
  first_met_at: EpochMillisSchema,
  last_met_at: EpochMillisSchema,
  meet_count: z.number().int().nonnegative(),
  event_history: z.array(EventHistoryEntryAppSchema),
  created_at: EpochMillisSchema,
  updated_at: EpochMillisSchema,
})
```

### 5.2 フロントエンド（`user`)

- **主要ページ**: ユーザーページ `/u/:userId`（「たべとも」タブ）。本人・他者・未ログイン閲覧者を問わず**常時表示**（フィーチャーフラグでの出し分けは行わない）
- **コンポーネント**（仮名）: `FriendsList.vue`、`FriendCard.vue`
- **データ取得**: **Callable 一本化**（5.5 と整合）。クライアントから `users/{user_id}/friends` を直接読まない。理由は、相手プロフィール（`users` 解決）・退会判定・**将来のオプトアウト判定（公開範囲設定）**・将来の `users` 追加項目展開を**サーバー側で一元実装**するため
- **状態管理**: Pinia の **`useFriendsStore`**（仮）を **`target_user_id` ごとに分離**して定義し（store id に `target_user_id` を含める）、招待モーダル等での再利用と他者ページとの取り違えを防ぐ（R-6）

### 5.3 Firestore インデックス

サブコレクション `users/{user_id}/friends` で **ソート + カーソル**を行うため、`firestore.indexes.json` に以下の複合インデックスを追加する（S-9：tie-breaker は `friend_user_id` 昇順で安定化）。

| インデックス                                                    | 用途                                  |
| ---------------------------------------------------------- | ----------------------------------- |
| `users/{user_id}/friends`: `meet_count desc, friend_user_id asc` | 既定ソート（同席回数順）                        |
| `users/{user_id}/friends`: `last_met_at desc, friend_user_id asc` | ソート切替（最後に会った日順）                     |

> **インデックス削除の作法**（R-3）: Phase 3 で `event_history` を `meetings` サブコレクションへ分離する場合、上記インデックスのうち**不要になるもの**は `firestore.indexes.json` から削除し、デプロイ時に Firestore コンソールでも削除されたことを確認する（自動削除されないため、放置するとコストが残る）。

### 5.4 Firestore セキュリティルール（方針）

クライアントは `friends` を**直接読まず**、Callable（5.5.1）経由で取得する。Phase 1 の Callable は**未ログインでも呼び出せる**完全公開だが、ルールは**読み書きとも禁止**のままとし、Functions の Admin SDK からのみアクセスする（M-5 と整合）。

```javascript
// 例（抜粋。実装時に match パスと申請用ルールで確定）
// match /users/{userId}/friends/{friendId} {
//   allow read, write: if false; // クライアント直接アクセスは全面禁止。Callable 経由のみ
// }
```

> **補足**: 「自分のサブコレクションは自分が読める」を許す設計も論理的には成り立つが、`users` 解決・退会フィルタ・追加プロフィール項目の展開・**将来のオプトアウト判定（公開範囲設定）**を**サーバー一箇所**で実装する方針を優先する。将来直 read を許可する必要が出たら、当節で明示的に方針転換する。

### 5.5 Functions 設計

| 種別           | 役割           | 内容 |
| ------------ | ------------ | ---- |
| **注文副作用** | 注文（参加）**確定／取消** | `applyOrderConfirmedSideEffects` / `applyOrderCanceledSideEffects` から、当該イベントの**他参加者**と**双方向**で `event_history` を冪等更新／削除（`event_id` 単位で重複不可、5.6.1 詳細） |
| **バッチ Callable** | **リリース時**    | 管理者専用 Callable（`backfillUserFriends`）。既存 `member_orders` から一括で友人ドキュメントを構築・補正。`dry_run` / 範囲指定 / `resume_token` を受ける（5.6.2 詳細） |
| **Callable** | 友人一覧         | 公開（`getUserFriends`）。**未ログインでも呼び出せる**（Phase 1 は完全公開）。リクエストに **`target_user_id` を必須**で受け、対象ユーザーの友人を返す。**カーソル方式**で `limit` / `sort_by` / `cursor` を受け、`sort_by` は `meet_count` または `last_met_at`。**相手の `user_name` / `user_image_url` 等は `users` を都度解決して返却**（friends にはコピーを持たない） |

注: [02_仕様書追加ルールとテンプレート.md](../00_仕様概要/02_仕様書追加ルールとテンプレート.md) の原則どおり、**可能な限り**読取は Firestore 直でもよいが、**他ユーザープロフィールの解決**・**退会フィルタ**・**将来のオプトアウト判定（公開範囲設定）**が伴うため、**本仕様では Callable 一本に寄せる**ことを推奨する。

#### 5.5.0 実装上の規約（プロジェクト共通）
- **Logger**: `console.log` / `console.error` および `firebase-functions` の `logger` を直接使わず、`createModuleLogger` を使用する
- **Store 経由**: Firestore へのアクセスは必ず **store 関数**を経由し、reference には **`withConverter`** を付与する（zod バリデーションを通す）
- **`set` 前の `get`**: `withConverter` + `set` で既存ドキュメントを更新する場合、必ず**先に `get` して既存データを引き継いでから**更新する。友達履歴更新では、対象友人ドキュメント（A→B / B→A）について、トランザクション内で `get → 既存 event_history に append（event_id 重複なし）→ 派生フィールド再計算 → set` とする
- **Callable**: 引数にクラスインスタンス等のオブジェクトを渡さず、ID とプリミティブで受け渡す
- **users の解決**: 友人一覧 Callable では、`friends` を読み取った後に**ユニークな `friend_user_id` 集合**を作り、`users` を **`Promise.all` による並列取得**で解決する。同一ページ内の重複は自然になくなるが、ループ内の逐次 `await` は避ける（N+1 回避）
- **退会／削除ユーザーの扱い**（S-1）:
  - **対象ユーザー本人（`target_user_id`）が存在しない / `is_deleted === true`** の場合は、Callable は `HttpsError('not-found', '存在しないユーザーです')` を返す。フロント側ではユーザーページ全体の「**存在しないユーザーです**」表示に集約するため、たべともタブはそもそも描画しない（4.1 と整合）
  - **友人側（`friend_user_id`）**: A-7 のとおり、`users/{friend_user_id}` が **存在しない** または **`is_deleted === true`** の場合は、当該友人を**レスポンスから除外**する。除外後 1 ページが 0 件でも `has_more` が true のとき、`resolveUserFriendsList` は **最大 20 ページ**（`MAX_PROFILE_PREVIEW_SKIP_PAGES`）までカーソルを進め、表示可能な友人を `limit` 件集める（`getUserFriends`・プロフィールプレビュー共通）
- **同伴者の扱い**（S-2 / A-5）: 友人関係の集合 `Others` は **`member_orders` の `user_id` 集合**から算出する。同伴者数を表す `EventMember.member_count` は**参照しない**（同伴者は `user_id` を持たないため、そもそも集合に入らない）。これにより「アカウントを持たない同伴者を友人化しない」を実装で担保する

#### 5.5.1 友人一覧（API イメージ）

**Callable 名**: `getUserFriends`（C-3）

**リクエスト（例）**

```json
{
  "target_user_id": "string",
  "limit": 10,
  "sort_by": "meet_count",
  "cursor": null
}
```

| フィールド            | 型                                  | 必須 | 説明                                                                                                                    |
| ---------------- | ---------------------------------- | -- | --------------------------------------------------------------------------------------------------------------------- |
| `target_user_id` | string                             | 必須 | 一覧を取得する対象ユーザーの `user_id`。**閲覧者（`auth.uid`）と一致しない場合も許可**（Phase 1 は完全公開）。Phase 2 で公開範囲設定（オプトアウト）を導入予定                  |
| `limit`          | number                             | 任意 | 1〜50。既定 10                                                                                                            |
| `sort_by`        | `'meet_count'` \| `'last_met_at'`  | 任意 | 既定 `meet_count`                                                                                                       |
| `cursor`         | string \| null                     | 任意 | 直前ページ末尾の不透明トークン。サーバー側で base64 エンコードしたソートキー（後述）を扱う                                                                    |

**認可（Phase 1）**

| 閲覧者              | 取得可否 | 備考                                  |
| ----------------- | ---- | ----------------------------------- |
| 未ログイン            | 可    | 完全公開                                |
| ログインユーザー（本人）   | 可    | `target_user_id == auth.uid`        |
| ログインユーザー（他者）   | 可    | 完全公開                                |

> Phase 2 で `users/{target}` 側に**公開範囲設定（完全公開／友人限定／友人の友人まで／非公開）**と**表示項目制御**を導入する想定（別途仕様）。本 Callable 内で当該設定を参照して `HttpsError('permission-denied')` 等で制御する。

**レスポンス（例）** ※ `user_name` / `user_image_url` は **`users/{friend_user_id}` から都度解決**して詰める（`friends` ドキュメントには保持しない）。`user_name` は必須、`user_image_url` は未設定ユーザーは**空文字 `''`**（`User.AppSchema` の `null → ''` 変換と整合）。`total` は**返さない**（カーソル方式のため）。

```json
{
  "friends": [
    {
      "user_id": "string",
      "user_name": "string",
      "user_image_url": "",
      "meet_count": 0,
      "first_met_at": 0,
      "last_met_at": 0
    }
  ],
  "next_cursor": null,
  "has_more": false
}
```

`first_met_at` / `last_met_at` は **`EpochMillisSchema` 相当の数値（ms）** で返す（AppSchema と整合）。

**DB ↔ API のフィールド命名マッピング**（C-1 / C-2）

| API レスポンス        | DB の真名（`users` コレクション）       | 備考                                                           |
| ---------------- | ----------------------------- | ------------------------------------------------------------ |
| `user_name`      | `users.user_name`             | DB と同名で揃える（既存 `User` スキーマに準拠）                                |
| `user_image_url` | `users.user_image_url`        | DB と同名で揃える。**未設定時は空文字 `''`**（`UserAppSchema` の `null → ''` 変換と整合） |
| `user_id`        | `users.user_id` / ドキュメント ID   | —                                                            |

> **方針**: API レスポンスのフィールド名は **DB の真名と完全一致**させる（C-1 / C-2 の確定事項）。これにより API 層での命名変換が不要となり、フロントから `User` AppSchema を直接利用できる。

**カーソルの中身（サーバー内部）**

- `sort_by=meet_count` の時: `(meet_count, friend_user_id)` の組（tie-breaker は `friend_user_id` 昇順、S-9）
- `sort_by=last_met_at` の時: `(last_met_at, friend_user_id)` の組
- 上記タプルを base64(JSON) で不透明文字列化してフロントへ返却

**Callable 内の処理順（概略）**:
1. リクエストを zod でバリデーション（`target_user_id` の必須・`limit` の範囲・`sort_by` の enum・`cursor` の形式）。NG は `HttpsError('invalid-argument')`
2. **対象ユーザーの存在確認**: `users/{target_user_id}` を取得し、**未取得 / `is_deleted === true`** の場合は `HttpsError('not-found', '存在しないユーザーです')`
3. **認可判定**: Phase 1 は完全公開のため常に許可。Phase 2 で公開範囲設定を確認し、不可なら `HttpsError('permission-denied')`
4. `users/{target_user_id}/friends` を `orderBy(<sort_by>, 'desc'), orderBy('friend_user_id', 'asc')` ＋ `startAfter(cursor)` ＋ `limit(limit + 1)` でクエリ（`+1` は `has_more` 判定用）
5. 取得した友人の `friend_user_id` 集合に対し、`users` を **並列取得**（5.5.0 参照）
6. `users` が**未取得 / `is_deleted === true`** の `friend_user_id` を**除外**し、`user_name` / `user_image_url` を埋めて整形（`UserAppSchema` をそのまま使えば `user_image_url` の `null → ''` 変換も自動で行われる、S-1）
7. 6 の結果が `limit` 未満かつ Firestore 側 `has_more` が true のとき、**最大 20 ページ**まで手順 4〜6 を繰り返す（`resolveUserFriendsList`。退会者のみの空ページ対策）
8. 一覧レスポンスには **`event_history` / `meet_log` を含めない**。カード用の `first_met_at` / `last_met_at` は **`friends` サブコレの冗長フィールド**をそのまま返す
9. 末尾の `+1 件目` の有無で `has_more` を判定し、最終要素から `next_cursor` を生成
10. 将来 `users` のタグ・SNS リンク等を一覧に出す際は、**本ステップで返却フィールドを足すだけ**で済む（`friends` のスキーマ・注文副作用を一切触らない）

**内部**の関係更新（注文確定・キャンセル後の副作用 / バッチ）は、HTTP の `POST /api/friends/update` を公開せず **共有モジュール**（`friendsService`、後述 5.5.2）経由に寄せる。双方向（A→B / B→A）で**同一ロジック**を必須とする（5.6・5.7 参照）。

#### 5.5.1.1 `getUserFriendMeetLog`（同席イベント記録）

**Callable 名**: `getUserFriendMeetLog`

**リクエスト（例）**

```json
{
  "target_user_id": "string",
  "friend_user_id": "string"
}
```

| フィールド | 型 | 必須 | 説明 |
| --- | --- | --- | --- |
| `target_user_id` | string | 必須 | プロフィール所有者（`users/{target}/friends` を読む） |
| `friend_user_id` | string | 必須 | 同席記録を見る相手の `user_id` |

**認可（Phase 1）**: `getUserFriends` と同様（完全公開）。対象ユーザーが存在しない / 退会済みは `not-found`。友人ドキュメントが無い場合は `not-found`（「友人が見つかりません」）。

**レスポンス（例）**

```json
{
  "meet_log": [
    {
      "event_id": "string",
      "community_id": "string",
      "event_at": 0,
      "event_name": "string",
      "community_account": "string"
    }
  ],
  "meet_count": 0
}
```

- `meet_log[]` の各要素: `event_id`, `community_id`, `event_at`（ms）, `event_name`, `community_account`, **`is_public`**, **`is_linkable`**（`events` から都度解決。取得不可は `event_name: null` 等）
- **限定公開（確定・2026-06-02）**: [02_マイページ.md](./02_マイページ.md) §4.2.0 と同一。削除済みイベントは除外。**`is_public == false` も本人・他者とも `meet_log` に含める**。`is_linkable` は **プロフィール所有者本人が閲覧しているときのみ** `true`（それ以外は `false`）。限定公開タグは UI で出さない
- **リンク判定**: `profileItemVisibility.ts` の `computeProfileItemLinkableToViewer`（または同等）を **`getUserProfilePreview` と共有**する
- 型は `common/src/apis/userFriends.ts` の `UserFriendMeetLogItem` / `GetUserFriendMeetLogResponse`
- UI: `base/src/components/FriendMeetLogDialog.vue`（`is_linkable` でイベント行の `<router-link>` を制御。本人 / 他者でダイアログタイトルを切り替え）

#### 5.5.2 ファイル配置とモジュール責務（C-4 / R-1 / R-2）

既存 `functions/default/src/` の規約（**1 機能 = 1 トップレベル `.ts` + `stores/` + `utils/`**、テストは `.test.ts` を隣接配置）に揃え、本機能の実装を Callable エントリ・注文副作用・ストア・業務ロジックに分割する。

```
functions/default/src/
├── userFriends.ts                       # ① エントリ：Callable 3 種を export
├── orderConfirmedSideEffects.ts         # 注文確定後の副作用から友達履歴を追加
├── orderCanceledSideEffects.ts          # 注文キャンセル後の副作用から友達履歴を削除
│
├── stores/
│   └── userFriend.ts                    # ② Firestore store（withConverter 付き ref と get/set/delete）
│
├── utils/
│   ├── friendsService.ts                # ③ 業務ロジック（add/remove 履歴 / runBackfill 等）
│   ├── friendsService.test.ts
│   ├── userFriendsResolver.ts           # ④ 一覧・meet log 解決（resolveUserFriendsList / resolveUserFriendMeetLog）
│   ├── userFriendsResolver.test.ts
│   └── profileItemVisibility.ts         # MAX_PROFILE_PREVIEW_SKIP_PAGES 等（マイページと共有）
│
└── index.ts                             # 既存：上記 export を中継するだけ
```

##### ① `userFriends.ts`（Callable エントリは薄く保つ）

Callable 本体。認可・バリデーション・レスポンス整形だけ行い、業務ロジックは `friendsService` に委譲する。

| export                   | 種別     | 概要                                                                                       |
| ------------------------ | -------- | ------------------------------------------------------------------------------------------ |
| `getUserFriends`         | Callable | 認証任意（Phase 1 は完全公開）。5.5.1 のリクエスト／レスポンス契約。`resolveUserFriendsList` 経由 |
| `getUserFriendMeetLog`   | Callable | 5.5.1.1。1 友人分の同席イベント全件（ダイアログ専用） |
| `backfillUserFriends`    | Callable | 管理者専用。`isSupport()` 相当チェックの後、`friendsService.runBackfill` を呼ぶ                              |

##### ② `stores/userFriend.ts`（既存 `stores/memberOrder.ts` に倣う）

`withConverter` 付き ref と `runTransaction` 対応の薄いヘルパ。

| export                                          | 役割                                                |
| ----------------------------------------------- | ------------------------------------------------- |
| `userFriendRef(uid, friendUid)`                 | `withConverter(userFriendConverter)` 付きの DocumentReference |
| `getUserFriend(uid, friendUid, tx?)`            | get（任意で `transaction.get` を使用）                    |
| `setUserFriend(uid, friendUid, data, tx?)`      | set（任意で `transaction.set`）。`5.5.0` の「`set` 前の `get`」規約に従う |
| `deleteUserFriend(uid, friendUid, tx?)`         | delete（`event_history` 空化時の物理削除に使用）                |

##### ③ `utils/friendsService.ts`（R-2）

注文副作用・Callable・テストから**同じ関数**を呼ぶための業務ロジック層。

| メソッド                                                                                  | 役割                                                                          |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `addEventToFriendHistory({ event_id, community_id, event_at, user_ids })`             | 同イベントの確定参加者集合に対し、A↔B 双方向の `friends` ドキュメントへ `event_history` を追加（5.6.1 と整合） |
| `removeEventFromFriendHistory({ event_id, user_ids })`                                | 当該 `event_id` を `event_history` から除去し、`event_history` が空ならドキュメント削除             |
| `recomputeDerived(eventHistory)`                                                      | `event_history` から `meet_count` / `first_met_at` / `last_met_at` を再計算（純粋関数）  |
| `runBackfill({ dry_run, community_id, event_id_from, event_id_to, resume_token })`    | リリース時バッチ本体（5.6.2）。500 件単位の `BulkWriter` / `WriteBatch`、`resume_token` 返却     |
| `purgeUserFriends(user_id)`                                                           | （Phase 2）当該ユーザーの `friends` を `collectionGroup('friends')` で全方位削除（S-7）         |

##### このブレークダウンが満たす要件

- **既存規約との整合**: `memberOrders.ts` + `stores/memberOrder.ts` + `utils/order.ts` の 3 層構成と同じレイヤリング
- **テスト容易性（T-1）**: `recomputeDerived` が純粋関数なので Firestore モック不要
- **注文副作用とバッチで共通ロジック**: `addEventToFriendHistory` / `removeEventFromFriendHistory` を双方から呼び、5.6.1 と 5.6.2 で同等ロジックを保証
- **エントリの薄さ**: `userFriends.ts` は Callable の認可・入力検証・レスポンス整形だけ。注文確定・キャンセルに伴う更新は副作用モジュールに集約
- **再利用**: 招待機能（`02_友人招待機能.md`）も `getUserFriends` を**直接 import** または流用しやすい

### 5.6 処理フロー

#### 5.6.1 注文（参加）確定・キャンセル後の副作用処理フロー

注文確定・キャンセルは `member_orders` の Firestore Trigger ではなく、既存の注文処理のトランザクションが成功したあとに 1 回だけ呼ばれる副作用モジュールに集約する。

| 呼び出し元 | 副作用モジュール | 友達履歴の処理 | 備考 |
| --- | --- | --- | --- |
| `confirmOrder` | `applyOrderConfirmedSideEffects` | `addEventToFriendHistory` | `in_cart → ordered` の確定後に実行 |
| `stripeWebhook` | `applyOrderConfirmedSideEffects` | `addEventToFriendHistory` | Stripe 決済完了により `ordered` 化したあとに実行 |
| `cancelOrders` | `applyOrderCanceledSideEffects` | `removeEventFromFriendHistory` | `ordered → canceled` のキャンセル後に実行 |

`member_orders` 単位の Trigger を使わないことで、1 回の注文確定で複数 `member_orders` が `ordered` になるケースでも N 回発火せず、メール送信・参加者再集約・友達履歴更新を 1 つの副作用フローで扱える。

**処理ステップ**

1. 注文確定・キャンセルのトランザクションが成功したあと、対象イベントを取得して副作用モジュールを呼ぶ
2. 同イベントの確定済み注文者を取得し、`P = { 当該 event の status==='ordered' な user_id 集合 }` を作る。同伴者は対象外（S-2 / A-5: 同伴者は `member_count` のみで `user_id` を持たないため自然に集合に含まれない）
3. 注文確定時は `P` を `addEventToFriendHistory` に渡す。キャンセル時は、**当該ユーザーについて同一イベントに `status === 'ordered'` の注文が残っていない** ときだけ `removeEventFromFriendHistory` を実行する（メニュー単位の**部分キャンセル**では友人履歴を維持。RC-45）。削除対象ユーザー集合は `P + キャンセル実行ユーザー`（キャンセルしたユーザーが `ordered` 集合から外れたあとでも当該ユーザーを履歴削除対象に含める）
4. `event_start_datetime` を `event_at` とする（**注文確定時点で固定**して以後書き換えない、S-8）
5. `P` 内の各ペア `(A, B)` について、**`runTransaction`** で次を実行（A-B 双方向を**同一トランザクション内**で処理）
   1. **read を先に**: `users/{A}/friends/{B}` と `users/{B}/friends/{A}` を `transaction.get`
   2. 注文確定の場合: `event_history` に `{event_id, community_id, event_at}` を**`event_id` 単位で重複なし**にマージ
   3. キャンセルの場合: `event_history` から `event_id` 一致要素を**全削除**。同一 `event_id` の注文は B-3 / A-1 の通り 1 つに正規化されているため、削除後の再計算で問題なし
   4. `meet_count` = `event_history` のユニーク件数、`first_met_at` = `event_at` の最小、`last_met_at` = 最大、`updated_at` を再計算（REMOVE で `event_history` が空になったら**ドキュメント自体を `transaction.delete`**）
   5. **write を後に**: `transaction.set`（`withConverter` 経由）または `transaction.delete`
6. **競合への備え**（S-4）: Firestore の自動リトライに任せつつ、Phase 1 では発生頻度をモニタ。多発するイベントが見つかった場合 Phase 2 で**イベント単位デバウンス**（Pub/Sub or Cloud Tasks で集約）を検討。リリース時バッチ（5.6.2）が**整合のセーフティネット**として再実行可能であることが前提
7. 失敗時は `createModuleLogger` でログし、**8.4 の運用方針**に従って補正バッチで再処理する

**実行環境（S-5）**

- 副作用は `confirmOrder` / `stripeWebhook` / `cancelOrders` の実行環境内で処理する
- 200 人クラスのイベントでの書き込み量・実行時間は Phase 1 で計測し、必要に応じて Phase 2 でイベント単位の非同期集約を検討する

#### 5.6.2 リリース時バッチの処理フロー

**起動方式（S-6）**

- **管理者専用 Callable**（`backfillUserFriends`）を Phase 1 で実装
- 認可: `firestore.rules` の `isSupport()` 相当のチェックを Functions 側でも明示（`auth.token` を確認）。判定ロジックは既存の admin Functions の作法に揃える
- 引数（プリミティブのみ、5.5.0 規約準拠）:
  - `dry_run: boolean`（既定 `true`。書き込みせず統計だけ返す）
  - `community_id?: string`（範囲を絞る）
  - `event_id_from?: string` / `event_id_to?: string`（イベントレンジ）
  - `resume_token?: string`（前回の中断地点から再開）
- リリース直後の初回投入は `dry_run` で件数とコストの見立て → 本実行 → 異常時は `resume_token` で部分再実行という運用を想定

**処理ステップ**

1. 上記 Callable を **support / 管理者**のみが叩く形で開始
2. 対象期間の `events` を走査し、各 `eventId` について **`status === 'ordered'`** の `member_orders.user_id` 集合 `P` を作る（同伴者は対象外、S-2）
3. `P` 内の任意の 2 人 `(A, B)`（`A !== B`、A-8）に対し、5.6.1 手順 5 と**同等のロジック**（`friendsService` を共有、R-2）を実行
4. 大量データに備え、**500 件単位**で `BulkWriter` または `WriteBatch` を分割し、`event_id` 単位で**チェックポイント** `resume_token` を返す（途中失敗からの再開を可能にする）
5. 進捗・失敗を `createModuleLogger` で記録し、再実行可能（冪等）であることを保証
6. `dry_run` 時は実際の write を行わず、追加見込みの `friends` ドキュメント数 / `event_history` 追加件数 / 想定コスト概算を返却

**ユーザー削除との関係（S-7）**

- ユーザー A の退会時に**自動削除はしない**（A-7 のとおり「表示で非表示」する）。`friends` ドキュメントは残置
- 個人情報の物理削除要請があった場合は、上記 Callable の派生として `purgeUserFriends(user_id)` を**別途用意**（Phase 2）。A 視点 / 他者視点（`users/{B}/friends/{A}`）の双方を `collectionGroup('friends')` で削除する
- Phase 1 では物理削除フローは未実装であることを 8.2 に明記

### 5.7 データ整合性の保証

- **冪等性**: 同一 `event_id` の `event_history` への二重追加を行わない（`get → ユニーク化 → set`）
- **双方向の整合**: `users/{A}/friends/{B}` と `users/{B}/friends/{A}` の更新は**同一 `runTransaction` 内**で行い、片側だけ書かれる状態を作らない
- **派生フィールド**: `first_met_at` / `last_met_at` / `meet_count` は **`event_history` を真とする**再計算（書き込み箇所を 1 つに集約）
- **プロフィール情報の真は `users`**: `user_name` / `user_image_url` 等は `friends` に**コピーを持たない**ため、`users` 更新時の**伝播トリガー（onUpdated）は不要**。`users` の値が常に最新として表示される
- **障害時の補正**: 双方向の不整合や注文副作用の失敗が起きた場合、**リリース時バッチと同等のロジック**を一部のユーザー / イベント範囲で再実行できるようにしておく

### 5.8 エラーハンドリング（技術）

- **Callable**: 認証失敗、バリデーション、`HttpsError` の扱い（既存 Functions 方針に合わせる）
- **注文副作用**: `applyOrderConfirmedSideEffects` / `applyOrderCanceledSideEffects` 内で失敗したステップは `createModuleLogger` で error を残し、他の副作用（参加者再集約・メール送信等）を可能な範囲で継続する。友達履歴の補正はリリース時バッチで再実行可能にする
- **プロフィール解決の部分失敗**: `users` 取得時に**個別にエラー**となる相手が出ても、**ページ全体は成功**として返す方針（その相手のみ除外し、`createModuleLogger` で warn を残す）
- 一覧: 空・タイムアウト・部分失敗の扱い（クライアント側はカード単位での縮退表示は行わず、Callable 側で「返せる分のみ返す」設計とする）

## 6. データマイグレーション（必要な場合）

### 6.1 新規のための「初期取り込み」

- 本仕様のサブコレクションは**新設**である。**リリース時バッチ**が、事実上の**初期データ投入**（過去分）を担う

### 6.2 既存スキーマ変更が発生する場合

- フィールド追加・型変更は [02_仕様書追加ルールとテンプレート.md](../00_仕様概要/02_仕様書追加ルールとテンプレート.md) の「6. データマイグレーション」に従い、**Callable（管理者向け）や専用スクリプト**、**本番前検証**を定義する

## 7. 実装優先度

### 7.1 Phase 1（必須）

- Firestore スキーマ（`UserFriend` Db/App、`event_history` に `community_id` を含む）と **security rules**（クライアント直 read/write 全面禁止、5.4）
- **共有モジュール `friendsService`**（5.5.2）
- **注文確定・キャンセル後の副作用**（`applyOrderConfirmedSideEffects` / `applyOrderCanceledSideEffects` から友達履歴を更新、5.6.1）
- **管理者専用バッチ Callable**（`dry_run` / 範囲指定 / `resume_token`、5.6.2）
- **友人一覧 Callable**（`getUserFriends`、`target_user_id` 必須・**完全公開**、カーソル方式、対象ユーザー存在確認、`users` 並列解決、退会フィルタ）と**ユーザーページ `/u/:userId` の「たべとも」タブ**（フィーチャーフラグ廃止・常時表示、Pinia ストアを `target_user_id` ごとに分離、空状態 CTA は本人のみ表示）
- **存在しない / 退会済みユーザーの URL** は、ユーザーページ全体として「**存在しないユーザーです**」と表示（たべとも仕様の範囲では Callable の `HttpsError('not-found')` で担保し、UI 表示はユーザーページの共通実装に集約）
- 5.3 の**複合インデックス**追加と、8.6 の**段階公開（Sandbox → パイロット → 全体）**
- 監視メトリクス（8.5）と注文副作用失敗時の運用手順（8.4）

### 7.2 Phase 2（将来対応）

- **公開範囲設定（オプトアウト）**: ユーザー設定で「友人一覧の公開範囲」を**完全公開／友人限定／友人の友人まで／非公開**から選択できるようにする。**表示項目（プロフィール写真／表示名／同席回数／日付など）の表示可否もチェックボックスで個別制御**する。詳細仕様は別途定義し、`users/{user_id}` 配下に設定フィールドを追加して `getUserFriends` 内で判定する
- 詳細な**同席履歴** UI（`event_history` の一覧化）
- カードからの**プロフィール遷移**等
- 分析・A/B 用のイベント計測

### 7.3 Phase 3（最適化・任意）

- パフォーマンス・**キャッシュ**の強化
- 運用用メトリクス・ダッシュボード連携

## 8. 注意事項

### 8.1 既存システムへの影響

#### 8.1.1 データベース

- **新規**: `users/{user_id}/friends` サブコレクション
- **既存**: `communities/{communityId}/events/{eventId}/members/{memberId}/member_orders/{orderId}` 等の**読取**がバッチ・注文副作用で増加。コストとクォータを踏まえ**段階実行**を検討
- **インデックス**追加: 5.3 のクエリに応じて `firestore.indexes.json` を更新

#### 8.1.2 アプリケーション

- **user**: ユーザーページ `/u/:userId` のタブ・ルーティング
- **Functions**: 注文副作用 / バッチ / Callable
- 既存の注文フローに、友達履歴更新の**遅延・失敗**が与える影響（副作用は失敗をログに残し、補正バッチで再実行可能にする）

#### 8.1.3 運用

- バッチ初回の**所要時間**・**再実行**手順
- ストレージ・**読取/書取**料金の見積り

### 8.2 セキュリティ考慮事項

- **read**: Phase 1 は**完全公開**。任意ユーザーの `friends` を**未ログインを含む誰でも**取得可能（クライアント直 read は禁止し、Callable 経由のみ）
- **相手の表示名・写真**: 公開プロフィール方針に**準拠**（既存 `users` の公開範囲と同等）
- **公開範囲設定（オプトアウト、Phase 2）**: 「自分の友人一覧を非公開／友人限定／友人の友人まで／完全公開」をユーザー設定で切り替え、`getUserFriends` 側で判定する。表示項目の選択（チェックボックス）も同節で扱う。詳細は別途仕様
- **存在しない / 退会済みユーザー**（S-1 / S-7）: ユーザーページ全体として「**存在しないユーザーです**」と表示する。Callable は `HttpsError('not-found')` を返す
- 招待・外部連携は [02_友人招待機能.md](./02_友人招待機能.md) と**整合**
- **退会／物理削除運用**（S-7）: Phase 1 では退会ユーザーの `friends` ドキュメントは**物理削除しない**（表示時にフィルタ）。個人情報削除要請に応える物理削除フロー（`purgeUserFriends` 等）は Phase 2 で別途定義する

### 8.3 パフォーマンス考慮事項

- 一覧の**ページング**、**不要なユーザーフィールド**の省く取得
- 相手人数が多いイベントでの **注文副作用内書き込み**（ペアごとの transaction 数、必要に応じた非同期集約）
- **`users` の N+1 回避**: 友人一覧 Callable では、ページ分の `friend_user_id` を**一括収集 → `Promise.all` で並列取得**する（5.5.0 参照）。1 ページ `limit=10` であれば追加読み取りは最大 10 件で、全体読み取りは `friends` 10 件 ＋ `users` 10 件 = **20 件 / ページ**程度に収まる
- **頻繁な再表示への備え**: クライアント側の**短時間のキャッシュ**（同セッション内でのスクロール戻り等）を Phase 2 以降で検討

### 8.4 エラーハンドリング（プロダクト）

- ネットワークエラー時の**再試行**、一覧失敗の**メッセージ**（4.2.3 と整合）
- **注文副作用の競合への運用方針**（S-4）:
  - Phase 1 は Firestore の**自動リトライ**に委ねる。同イベント内の同時注文確定で `runTransaction` 競合が発生しても、最終的に整合する設計
  - 友達履歴更新の失敗が **N 回連続**（仮: 3 回）の場合は `createModuleLogger` で error を出し、運用アラート（メトリクス R-4）でキャッチ
  - 復旧手段: **リリース時バッチ Callable**（5.6.2）を該当 `event_id` だけ範囲指定して実行する。これがセーフティネット
  - Phase 2 で発火頻度が高いイベントが見つかれば、**Pub/Sub or Cloud Tasks による集約**を検討

### 8.5 運用・監視（R-4）

**メトリクス**

- 注文副作用（`addEventToFriendHistory` / `removeEventFromFriendHistory`）: 成功率 / 平均実行時間 / リトライ回数 / `event_id` ごとの最大 transaction 競合回数
- Callable `getUserFriends`: レスポンス時間 p50 / p95、エラー率（`HttpsError` の code 別）
- リリース時バッチ Callable: 完了時間、処理 event 数、追加された `friends` ドキュメント数、`dry_run` と本実行の差分
- KPI（2.3 と接続）: 「たべとも」タブ閲覧 PV / UU、`friends` 件数推移、1 ユーザー平均同席者数

**ログ**

- 注文副作用 / バッチの失敗理由（`createModuleLogger` での error）
- **冪等性監査**: 同一 `event_id` の二重 append が起きた件数（0 が正常）
- 退会ユーザー除外でレスポンスから外した件数（過度に多ければ要注意）

### 8.6 ロールアウト戦略（R-5）

リリースは段階公開とし、各段で**ロールバック手順**を準備する。Phase 1 では**フィーチャーフラグによる UI 出し分けは行わない**ため、配布単位（環境）で公開を制御する。

| 段 | 内容 | ロールバック |
|---|---|---|
| 1. Sandbox 検証 | Sandbox 環境（fork リポ）に注文副作用 / バッチ / Callable / UI をデプロイし、内部アカウントで動作確認 | Sandbox 環境のロールバック（再デプロイ）。本番には影響しない |
| 2. パイロットコミュニティ | 本番にデプロイ後、リリース時バッチ Callable を `dry_run=true` でコスト見積もり → 本実行 → パイロットコミュニティから動作確認 | バッチを停止。`friends` ドキュメントは残置でも問題なし |
| 3. 全体公開 | 全期間・全コミュニティをバッチで構築 | 問題発生時は `purgeUserFriends` 群で部分整理 |

> Phase 2 の公開範囲設定（オプトアウト）導入時は、**既定値（既存ユーザーは「完全公開」のまま）**と移行通知を別途仕様で定義する。

---

## 付記: 品質チェック（抜粋）

仕様として以下を満たすこと（詳細は [02_仕様書追加ルールとテンプレート.md](../00_仕様概要/02_仕様書追加ルールとテンプレート.md) 5.1 品質基準）。

- 概要・ねらい・前提が記載されている
- Firestore のパス・フィールド（snake_case）・**更新タイミング**が記載されている
- 画面（ユーザーページ「たべとも」）の表示・ソートが記載されている
- Functions（注文副作用 / バッチ / Callable）の役割が記載されている
- Phase 1 / 2、注意事項（既存影響・セキュリティ）が記載されている

