# sandbox2606 / 2607 / 2608 環境構築手順

機能開発用の個人 sandbox を **3 本並行**で用意する手順書。PF 版（user + partner）のみ。エンプラ（enterprise / `tabete.co`）は対象外。

- 共通の新規プロジェクト手順: [firebaseプロジェクト新規作成.md](./firebaseプロジェクト新規作成.md)
- Terraform 詳細: [terraform/README.md](../../terraform/README.md)
- Firestore 移行: [firestoreデータ移行手順_手動.md](./firestoreデータ移行手順_手動.md)
- Storage 移行: [storageデータ移行手順_手動.md](./storageデータ移行手順_手動.md)

---

## 1. 概要

| 項目 | 内容 |
| ---- | ---- |
| 目的 | 機能開発ごとに独立したデプロイ先を 3 本確保する |
| 対象 remote | `sandbox2606` / `sandbox2607` / `sandbox2608` |
| スコープ | PF 版（user + partner + functions + firestore + storage） |
| データ移行元 | **`bokudeli-event-test`（development）** |
| 用途分担 | なし（空いている sandbox に push する運用） |

---

## 2. 命名規則

| 要素 | パターン | 例（2606） |
| ---- | -------- | ---------- |
| git remote 名 | `sandbox2606` | `sandbox2606` |
| GitHub リポジトリ | `nijuni-yasu/bokudeli-event-yasu-2606` | 2607 / 2608 も同型 |
| GCP プロジェクト ID | `bokudeli-event-yasu-2606` | 2607 / 2608 も同型 |
| user Hosting URL | `https://bokudeli-event-yasu-2606.web.app` | Firebase 既定 |
| partner Hosting URL | `https://bokudeli-event-yasu-2606-admin.web.app` | `<PROJECT_ID>-admin` |

---

## 3. 推奨実行順

```text
2606: Phase 1〜5（テンプレ確立）
2607: Phase 1〜5（Firestore export を共用）
2608: Phase 1〜5（同上）
```

Terraform はプロジェクト切替のたびに `.terraform` を削除するため、**2606 を 1 本通してから横展開**するのが安全。

### 3.1 作業進捗（2026-08-30 時点）

| 項目 | 2606 | 2607 | 2608 |
| ---- | ---- | ---- | ---- |
| Firebase / GCP プロジェクト | ✅ 作成済 | ✅ | ✅ |
| GitHub リポジトリ | ✅ | ✅ | ✅ |
| Web アプリ（SDK 設定） | ✅ | ✅ | ✅ |
| git remote（本リポジトリ） | ✅ | ✅ | ✅ |
| GitHub Variables（PF 用） | ✅ | ✅ | ✅ |
| ローカル `.env`（user/partner） | ✅ | ✅ | ✅ |
| コード push | ✅ `release/sandbox-initial`（`development` は hook により要手動） | ✅ | ✅ |
| Blaze + Terraform apply | ✅ | ✅ | ✅ |
| GitHub Secret（`GCLOUD_SERVICE_KEY`） | ✅ | ✅ | ✅ |
| GCP Secret Manager（12 件・値） | ✅ 2605 からコピー | ✅ | ✅ |
| GitHub Environment（`development`） | ✅ | ✅ | ✅ |
| 初回デプロイ | ✅ ローカル CLI | ✅ | ✅ |
| test データ移行 | ⏳ | ⏳ | ⏳ |

**補助スクリプト**（`terraform/scripts/`）:

- `bootstrap-sandbox-terraform.sh <2606|2607|2608>` — init.sh + plan + apply
- `setup-sandbox-github-vars.sh <2606|2607|2608>` — Variables 再設定（ローカル `.env` から）

**手動 push（初回コード投入）** — `development` への push はエージェント hook でブロックされるため、端末で実行:

```bash
git fetch --unshallow origin   # shallow clone の場合のみ
git push sandbox2606 origin/development:development
git push sandbox2607 origin/development:development
git push sandbox2608 origin/development:development
```

初回は `release/sandbox-initial` ブランチへ push 済み（デフォルトブランチ）。上記で `development` にリネーム相当の push を行うと GitHub Actions の workflow も有効化される。

---

## 4. Phase 1: 基盤構築（環境ごと）

各 `sandbox260X` について [firebaseプロジェクト新規作成.md](./firebaseプロジェクト新規作成.md) の手順 1〜7 に従う。PF 版のみの差分は下記。

### 4.1 GitHub / Firebase

- [ ] GitHub リポジトリ作成（例: `nijuni-yasu/bokudeli-event-yasu-2606`）
- [ ] Firebase プロジェクト作成（ID: `bokudeli-event-yasu-2606`）
- [ ] **Blaze プラン**にアップグレード

### 4.2 Terraform

```bash
cd terraform
chmod +x init.sh
./init.sh
# GCP プロジェクト ID: bokudeli-event-yasu-2606
# GitHub リポジトリ: nijuni-yasu/bokudeli-event-yasu-2606
# GitHub Environment: 空欄（Enter）
```

- [ ] `terraform.tfvars` に **`enterprise_base_domain` は書かない**（PF 版のみ）
- [ ] `terraform plan` → `terraform apply`
- [ ] default bucket が 409 の場合: [terraform/README.md](../../terraform/README.md) の import 手順 → 再 apply
- [ ] 次の環境へ行く前に **`rm -rf .terraform`**

### 4.3 GitHub Actions 設定（PF 版）

各 sandbox リポジトリの **Settings → Secrets and variables → Actions**。

#### Variables

| Variable | 内容 |
| -------- | ---- |
| `PROJECT_ID` / `PROJECT_NUMBER` | `init.sh` で自動登録済み |
| `FIREBASERC` | **user + partner** target のみ（enterprise 不要） |
| `USER_ENV` | `user/.env` 相当（Firebase Web SDK 設定） |
| `PARTNER_ENV` | `partner/.env` 相当 |
| `FUNCTIONS_ENV` | `functions/default/.env` 相当（`ENTERPRISE_BASE_DOMAIN` は enterprise 未デプロイでも **Functions デプロイに必須**。例: `sandbox2606.tabete.co`。`setup-sandbox-github-vars.sh` が設定） |

#### Secrets

| Secret | 内容 |
| ------ | ---- |
| `GCLOUD_SERVICE_KEY` | `firebase-deploy@<PROJECT_ID>.iam.gserviceaccount.com` の JSON キー |
| SendGrid / Slack / LINE 等 | 使う機能分（development と同系統で可） |

#### GCP Secret Manager（Functions 用）

[firebaseプロジェクト新規作成.md §7](./firebaseプロジェクト新規作成.md) の 12 件に**値のみ**登録。Stripe は Test mode で統一（[Stripe 環境構築手順](../07_リファクタリング/03_stripe決済の環境構築手順.md)）。

**PF 版で不要なもの**

- `ENTERPRISE_ENV`
- `deploy_enterprise.yml`
- `tabete.co` DNS / Hosting カスタムドメイン

---

## 5. Phase 2: 初回デプロイ（環境ごと）

推奨順:

```text
firestore → storage → functions → user → partner
```

```bash
# development ブランチ未作成時は --ref release/sandbox-initial
gh workflow run deploy_firestore.yml --repo nijuni-yasu/bokudeli-event-yasu-2606 --ref development
gh workflow run deploy_storage.yml  --repo nijuni-yasu/bokudeli-event-yasu-2606 --ref development
gh workflow run deploy_functions.yml --repo nijuni-yasu/bokudeli-event-yasu-2606 --ref development
gh workflow run deploy_user.yml     --repo nijuni-yasu/bokudeli-event-yasu-2606 --ref development
gh workflow run deploy_partner.yml  --repo nijuni-yasu/bokudeli-event-yasu-2606 --ref development
```

Stripe Webhook（決済テストする場合）: Functions デプロイ後に Dashboard 登録 → Signing secret を Secret Manager へ → Functions 再デプロイ。

---

## 6. Phase 3: データ移行（`bokudeli-event-test` → 各 sandbox）

正本: [firestoreデータ移行手順_手動.md](./firestoreデータ移行手順_手動.md) / [storageデータ移行手順_手動.md](./storageデータ移行手順_手動.md)

### 6.1 移行元（全環境共通）

| 変数 | 値 |
| ---- | -- |
| `PROJECT_A` | `bokudeli-event-test` |
| `BUCKET_A`（Firestore） | `bokudeli-event-test-firestore-backups` |
| `BUCKET_A`（Storage） | Console で確認（`*.appspot.com` または `*.firebasestorage.app`） |

### 6.2 効率化（3 環境まとめて）

1. **Firestore export は 1 回だけ**（test から最新取得、または既存 backup フォルダ利用）
2. export を **2606 / 2607 / 2608 各 import バケット**へアップロード
3. 各 sandbox で import → rules/indexes → 0004 → Storage コピー

### 6.3 環境ごとのチェックリスト（2606 を例）

| # | タスク | メモ |
| - | ------ | ---- |
| 1 | B の import バケット + Firestore SA IAM | `{project}-firestore-imports` |
| 2 | A の export を B へ import | import 前に B の Firestore は空 |
| 3 | rules + indexes デプロイ | CI or `firebase deploy` |
| 4 | **0004 メール削除** | `yarn run util -- -m sandbox2606`（Storage **前**） |
| 5 | `pass_code.user_email` 手動削除 | 0004 対象外 |
| 6 | Storage A→B コピー | B バケット空にしてから。バケット名は Console 実値 |
| 7 | `storage.rules` デプロイ | |
| 8 | 検証 | ログイン・イベント・画像・注文 |

> development データは本番 PII よりリスクは低いが、**0004 は引き続き推奨**（誤送信防止）。test データに `/enterprises` があっても PF 版のみなら無視してよい。

---

## 7. Phase 4: ローカル開発設定（本リポジトリ）

`nijuniinc/bokudeli-event-new` 作業リポジトリ側:

```bash
git remote add sandbox2606 git@github.com:nijuni-yasu/bokudeli-event-yasu-2606.git
git remote add sandbox2607 git@github.com:nijuni-yasu/bokudeli-event-yasu-2607.git
git remote add sandbox2608 git@github.com:nijuni-yasu/bokudeli-event-yasu-2608.git
```

ローカル `.env`（**コミットしない**）:

| ファイル | 用途 |
| -------- | ---- |
| `user/.env.sandbox2606` | user ローカル DEV |
| `partner/.env.sandbox2606` | partner ローカル DEV |
| `functions/default/.env.sandbox2606` | Functions ローカル |

起動確認:

```bash
npm -w user run dev -- -m sandbox2606
```

---

## 8. Phase 5: 並行開発での使い方

| やり方 | 内容 |
| ------ | ---- |
| 明示 push | `git push sandbox2606 HEAD:feature/xxx` |
| ブランチ記憶 | `git config branch.feature/xxx.sandboxRemote sandbox2606` |
| WIP デプロイ | `/github-sandbox-wip-deploy` スキル |

3 本は用途固定ではなく、**空いている sandbox に push** する運用でよい。

---

## 9. 動作確認（環境ごと）

- [ ] user / partner が表示される
- [ ] ログイン（test データのユーザー）
- [ ] イベント一覧・詳細・注文フロー
- [ ] チャット画像添付（Storage CORS — Terraform apply 済みなら OK）
- [ ] Stripe Test 決済（必要なら）
- [ ] GitHub Actions 5 workflow 成功

---

## 10. 作業見積もり（目安）

| フェーズ | 1 環境目 | 2〜3 環境目 |
| -------- | -------- | ----------- |
| Phase 1〜2（基盤 + 初回デプロイ） | 2〜3 h | 1〜1.5 h |
| Phase 3（データ移行） | 1〜2 h | 0.5〜1 h（export 共用） |
| Phase 4〜5 | 30 min | 20 min |
| **3 環境合計** | | **おおよそ 1 日** |

---

## 11. 関連ドキュメント更新

構築完了後、以下を更新する。

- [firebaseプロジェクト新規作成.md](./firebaseプロジェクト新規作成.md) の環境一覧・remote 一覧
- [gcp-logging-error-analysis/references/environments.md](../../.agents/skills/gcp-logging-error-analysis/references/environments.md)
