# PR #2099 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-23 14:50 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: fix/2098
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2099
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型コメント id:4776016045、Codex 接続案内ボイラープレートのみのレビュー本文）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | Copilot の PR 変更サマリ<br>具体指摘はインライン 2 件に分離済み |
| [x] | RC-2 | 3457317580 | ✅ 対応済み | 📌 スコープ内 | 💾 データ | 🔧 微修正 | M | scanOrdersBatch が 1 バッチで pageSize を超えるイベントを返し得る<br>Firestore 読み取りと初回表示負荷が増える。残り枠で打ち切りを推奨 |
| [x] | RC-3 | 3457317610 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | pendingKeys ループ内で docs.map を毎回生成<br>バッチ内で orders 配列を 1 回だけ作ると簡潔 |
| [x] | RC-4 | 3457329630 | ✅ 対応済み | 📌 スコープ内 | 💾 データ | 🔧 微修正 | S | 本人以外閲覧でも注文履歴ストアが setup で reload<br>isOwner 確定後の遅延生成または enabled 相当で抑制 |
| [x] | RC-5 | 3457329638 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | MAX_SCAN 上限で有効イベント 0 のとき hasMore が true のまま<br>IncrementalLoader が再発火せず空表示＋ローダー停止の恐れ |
| [x] | RC-6 | 3457329643 | ✅ 対応済み | 📌 スコープ内 | 💾 データ | 🔧 微修正 | M | 1 バッチで最大 ORDERS_SCAN_BATCH 件のイベントを一括追加<br>RC-2 と同根。pageSize 件に制限して読み取り量を揃える |
| [x] | RC-7 | 3457510369 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | MAX_SCAN 上限到達後も events 件数が増えずローダー停止<br>showOrdersTabEmpty + setupAutoLoadWhenEmpty で追読み継続 |
| [x] | RC-8 | 3457510373 | ✅ 対応済み | 📌 スコープ内 | 💾 データ | 📐 リファクタ | M | 参加イベントストアが未使用の member_orders を並列取得<br>userEventList から注文 fetch を削除 |
| [x] | RC-9 | 3457510376 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 注文履歴 reload の race で古い next が続行<br>loadGeneration + paginationExecutor.clear を導入 |
| [x] | RC-10 | なし | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | loadOrdersForEvent が loadGeneration 未チェック<br>reload 後に古い注文詳細が orderStateByEventId へ書き戻る |

---

**識別子**: RC-1（GitHub id: なし・Copilot PR レビュー本文）

**レビュワー**: Copilot

**指摘箇所**: PR トップレベル

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

## Pull request overview

注文履歴タブが参加イベント（`Event.members`）起点のストアを参照していたため、全キャンセル後にイベントカードが消えてしまう不具合を、`member_orders` collectionGroup 起点の取得に分離して解消する PR です。`EventMemberOrder` 移行後の「参加イベントタブ」と「注文履歴タブ」のデータ取得方針を分け、v2.9 以前と同様にキャンセル済みイベントも注文履歴に残るようにします。

**Changes:**
- 注文履歴タブ専用に、`member_orders` collectionGroup を `user_id` でスキャンしてイベントを列挙する Pinia ストアを追加
- `member_orders` サブコレクション取得ロジックを共通化し、参加イベントタブ側のストアから共通処理を利用
- user 側のプロフィール画面（注文履歴タブ）を新ストアへ切替、仕様ドキュメントも方針分離を追記

### Reviewed changes

Copilot reviewed 6 out of 6 changed files in this pull request and generated 2 comments.

**コメント要約**: Copilot による PR 全体の変更サマリとレビュー対象ファイル一覧。修正依頼はインライン 2 件に分かれているため、本コメント単体でのコード対応は不要。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 変更内容の要約であり、具体的な修正指示はインラインコメントに集約されている。サマリとして妥当で、本 PR の目的説明も差分と一致する。

---

**識別子**: RC-2（GitHub id: 3457317580）

**レビュワー**: Copilot

**指摘箇所**: `base/src/stores/userOrderHistoryList.ts:125`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+        pendingKeys.add(eventId)
+      }
+
+      for (const eventId of pendingKeys) {
+        const order = docs.map((d) => d.data()).find((o) => o.event_id === eventId && o.status !== 'in_cart')
```

**レビュワーのコメント（原文）**:

[must] `scanOrdersBatch()` が 1 回のスキャンで `pageSize` を超える数のイベントを解決し得るため、`next()` 1 回で大量のイベント/注文を取得してしまい、読み取りコスト・表示負荷が想定より増えます。`pageSize` 件のユニークイベントを見つけた時点でカーソル（`lastOrderSnapshot`）をその orderDoc に合わせて止め、次回以降に残りをスキャンするようにするとページング意図に沿います。

**コメント要約**: 1 回の scanOrdersBatch が ORDERS_SCAN_BATCH 内のユニーク event_id をすべて newEvents に載せるため、pageSize 6 の設計と乖離する。<br>カーソルを pageSize 到達時点の order ドキュメントに合わせて打ち切る改善が望ましい。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 💾 データ

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 指摘は妥当。pendingKeys はバッチ内の非 in_cart 注文からユニーク event_id を集めるため、1 バッチで最大 24 イベント＋各イベントの member_orders 並列取得が走る。機能バグではないが IncrementalLoader の pageSize 設計と Firestore 読み取りコストの両面で改善余地がある。RC-6 と同根のため一括で直すとよい。

**対応内容**: `scanOrdersBatch(maxNewEvents)` に変更し、枠到達時に `lastOrderSnapshot` を該当 orderDoc に設定して打ち切る。

---

**識別子**: RC-3（GitHub id: 3457317610）

**レビュワー**: Copilot

**指摘箇所**: `base/src/stores/userOrderHistoryList.ts:127`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+      for (const eventId of pendingKeys) {
+        const order = docs.map((d) => d.data()).find((o) => o.event_id === eventId && o.status !== 'in_cart')
```

**レビュワーのコメント（原文）**:

[nits] `pendingKeys` ループ内で毎回 `docs.map(d => d.data())` を生成しており、バッチ内で無駄な配列生成が発生します（小さいですが）。先に 1 回だけ `orders` 配列を作って使い回すと簡潔です。

**コメント要約**: pendingKeys の for ループ内で docs.map を繰り返し呼んでいる。<br>バッチ先頭で orders 配列を 1 回だけ作るリファクタで十分。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は正しい。パフォーマンス影響は小さいが、可読性・無駄削減として採用してよい。RC-2 / RC-6 の scanOrdersBatch 改修と同時に直すと自然。

**対応内容**: `batchEntries` をバッチ先頭で 1 回構築し、単一ループで走査（`pendingKeys` 二重ループ廃止）。

---

**識別子**: RC-4（GitHub id: 3457329630）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:102`

**該当コード（レビュー時点の diff）**:

```diff
 const userEventListStore = useUserEventListByUserId(profileUserId)
-const { events: userEvents, totalCount: userEventsTotalCount, orderStateByEventId } = storeToRefs(userEventListStore)
+const { events: userEvents, totalCount: userEventsTotalCount } = storeToRefs(userEventListStore)
+
+const userOrderHistoryStore = useUserOrderHistoryByUserId(profileUserId)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  本人以外では注文履歴ストアを起動しないでください**

他者プロフィールや未ログイン状態でこのコンポーネントを開いた場合でも、setup 時点で注文履歴ストアが生成され、ストア側の初期 `reload()` により `member_orders` の collectionGroup クエリが走ります。注文履歴タブ自体は `v-if="isOwner"` で隠しているため、本人以外の閲覧で不要な注文履歴取得と Firestore 読み取りコストが発生します。`isOwner` が確定してから生成/起動する、またはストアに enabled 相当を渡して本人時だけ `reload()` する形にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: setup で常に useUserOrderHistoryByUserId が走り reload するため、他人プロフィール閲覧時も CG クエリが発生する。<br>isOwner 確定後の遅延生成、またはストアの enabled 相当で本人時のみ取得。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 💾 データ

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当。注文履歴タブは isOwner 時のみ表示だが、ストアは profileUserId 向けに常時初期化される。Firestore ルール上 member_orders は読み取り可能なためセキュリティ上の漏洩ではないが、不要な読み取りコストと設計上の無駄がある。userFoods のように本人時のみ起動するパターンに揃えるとよい。

**対応内容**: ストア末尾の自動 `reload()` を削除。`UserProfilePage` で `isOwner` + `exists` の watch により本人確定後のみ `reload()`。注文完了 watch にも `isOwner` ガードを追加。

---

**識別子**: RC-5（GitHub id: 3457329638）

**レビュワー**: Codex

**指摘箇所**: `base/src/stores/userOrderHistoryList.ts:154`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+          for (let i = 0; i < MAX_SCAN_ITERATIONS && ordersHasMore && collectedNewEvents.length < pageSize; i++) {
+            const { newEvents, ordersHasMore: batchHasMore } = await scanOrdersBatch()
+            ordersHasMore = batchHasMore
+            collectedNewEvents.push(...newEvents)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  スキャン上限到達時も次の読み込みへ進めてください**

先頭から 240 件分が `in_cart` や既読イベント、削除済みイベントでスキップされるユーザーでは、この上限でループを抜けても `ordersHasMore` は true のまま、かつ `collectedNewEvents` が 0 なので `events.length` が変わりません。`IncrementalLoader` は `loadedCount/totalCount` の変化か交差時にしか再度 `load` を出さないため、まだ過去に `ordered/canceled` があっても注文履歴が空表示＋ローダーのまま止まります。上限到達時は内部で継続スキャンするか、再発火できる状態を更新してください。

Useful? React with 👍 / 👎.

**コメント要約**: MAX_SCAN_ITERATIONS 到達かつ collectedNewEvents が 0 のとき hasMore が true のまま残るエッジケースがある。<br>IncrementalLoader が再 load せず、過去注文があるユーザーで空表示のまま止まる恐れ。継続スキャンまたは再発火可能な状態更新が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 指摘は妥当で再現シナリオも具体的。先頭バッチが in_cart・削除済み・既読のみのユーザーで、有効イベントが MAX_SCAN_ITERATIONS × ORDERS_SCAN_BATCH より奥にあると、初回 next で 0 件かつ hasMore true のまま loader が動かない。本 PR の注文履歴表示復元の目的に対し、一部ユーザーで履歴が永遠に出ない実害がある。

**対応内容**: `MAX_SCAN_ITERATIONS` による早期打ち切りを廃止。`while (ordersHasMore && collected < pageSize)` で継続スキャン。安全上限 `MAX_SCAN_BATCHES_PER_NEXT`（50）のみ設置。

---

**識別子**: RC-6（GitHub id: 3457329643）

**レビュワー**: Codex

**指摘箇所**: `base/src/stores/userOrderHistoryList.ts:157`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+            const { newEvents, ordersHasMore: batchHasMore } = await scanOrdersBatch()
+            ordersHasMore = batchHasMore
+            collectedNewEvents.push(...newEvents)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  pageSize を超えるイベントを一度に追加しないでください**

1 回の `member_orders` バッチ内に別イベントの注文が多いユーザーでは、既定 `pageSize` は 6 でも `ORDERS_SCAN_BATCH` の最大 24 件分のイベントがそのまま追加されます。注文履歴タブの 1 ページあたり表示数と読み取り量を `pageSize` で抑える前提が崩れ、初回表示で最大 24 件のイベント取得と注文サブコレクション取得が走るため、残り枠だけを追加するよう制限してください。

Useful? React with 👍 / 👎.

**コメント要約**: scanOrdersBatch が 1 回で最大 24 イベントを返し、next の pageSize 制御が無効化される。<br>collectedNewEvents への push 前に残り枠で打ち切るか、scan 内で上限を守る必要がある。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 💾 データ

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: RC-2 と同根で指摘は妥当。マージ必須のクラッシュではないが、ページング設計と読み取りコストの一貫性のため修正推奨。RC-2 とまとめて scanOrdersBatch と next の両方で pageSize 残量を意識する実装がよい。

**対応内容**: RC-2 と同一改修（`scanOrdersBatch(maxNewEvents)` + `next()` の残り枠ループ）。

---

## 評価セッション（第 2 回）

- **評価日時**: 2026-06-23 16:00 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` + 実装）
- **ブランチ名**: fix/2098
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2099
- **Outdated 除外件数**: 3（GitHub id: 3457317580, 3457317610, 3457329638）
- **レビュー非該当スキップ件数**: 5（レビュー依頼 id:4776016045, 4776268005, 4776458724、Codex 利用上限 id:4776457189, 4776460149）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-7 | 3457510369 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | MAX_SCAN 上限到達後も events 件数が増えずローダー停止<br>showOrdersTabEmpty + setupAutoLoadWhenEmpty で追読み継続 |
| [x] | RC-8 | 3457510373 | ✅ 対応済み | 📌 スコープ内 | 💾 データ | 📐 リファクタ | M | 参加イベントストアが未使用の member_orders を並列取得<br>userEventList から注文 fetch を削除 |
| [x] | RC-9 | 3457510376 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 注文履歴 reload の race で古い next が続行<br>loadGeneration + paginationExecutor.clear を導入 |

---

**識別子**: RC-7（GitHub id: 3457510369）

**レビュワー**: Codex

**指摘箇所**: base/src/stores/userOrderHistoryList.ts:175

**該当コード（レビュー時点の diff）**:

（diff_hunk 省略・MAX_SCAN_BATCHES_PER_NEXT 到達時 console.warn のみ）

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  上限到達時も再読み込み可能にする**

新しい差分では `while` 化されたものの、`MAX_SCAN_BATCHES_PER_NEXT` 到達時は `console.warn` するだけで `hasMore` も `events.length` も変わりません。注文履歴タブを直リンクで開いている状態など、`IncrementalLoader` がすでに交差判定済みのときに先頭 1200 件が `in_cart`・既読イベント・削除済みイベントでスキップされると、まだ奥に有効な `ordered/canceled` があっても props 変化が起きず次の `load` が再発火しないため、空表示＋ローダーのまま止まります。

**コメント要約**: 50 バッチ上限で有効イベント 0 件のとき IncrementalLoader が再発火せず停止し得る。<br>フードタブ同様の自動 next が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 指摘は妥当。UserProfilePage に showOrdersTabEmpty と setupAutoLoadWhenEmpty を追加し、hasMore かつ 0 件の間は空文言を出さず自動 next する。

**対応内容**: user/src/components/profile/UserProfilePage.vue に showOrdersTabEmpty、注文履歴タブ用 setupAutoLoadWhenEmpty を追加。

---

**識別子**: RC-8（GitHub id: 3457510373）

**レビュワー**: Codex

**指摘箇所**: user/src/components/profile/UserProfilePage.vue:100

**該当コード（レビュー時点の diff）**:

```diff
 const userEventListStore = useUserEventListByUserId(profileUserId)
-const { events: userEvents, totalCount: userEventsTotalCount, orderStateByEventId } = storeToRefs(userEventListStore)
+const { events: userEvents, totalCount: userEventsTotalCount } = storeToRefs(userEventListStore)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  使わない注文詳細取得を止める**

この変更で `UserProfilePage` は参加イベントストアの `orderStateByEventId` を参照しなくなり、リポジトリ全体を `rg "orderStateByEventId"` で確認しても残る利用元は注文履歴ストア側だけです。一方で `useUserEventListByUserId` は各ページ取得時に引き続き全イベントの `member_orders` を並列取得するため、プロフィール/イベントタブを開くだけで表示に使わない注文サブコレクション読み取りが pageSize 件分発生し、本人では注文履歴ストアとも重複取得になります。

**コメント要約**: イベントタブ用ストアが UI 未使用の member_orders を page 取得ごとに fetch している。<br>注文履歴ストアと重複読み取りになる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内（本 PR に含める方針で実装）

**ラベル**: 💾 データ

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: 指摘は事実。ProfileEventCard は注文非参照のため eager fetch は不要。

**対応内容**: base/src/stores/userEventList.ts から orderStateByEventId、loadOrdersForEvent、loadOrdersForEventsParallel、reloadOrdersForEvent を削除。

---

**識別子**: RC-9（GitHub id: 3457510376）

**レビュワー**: Codex

**指摘箇所**: base/src/stores/userOrderHistoryList.ts:199

**該当コード（レビュー時点の diff）**:

（reload 内で cursor リセット後 next が実行中タスクによりブロックされる箇所）

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  reload 中の実行中タスクを破棄してください**

注文完了クエリ付きでマイページを開く場合など、本人判定の watch とクエリ watch が連続して `reload()` を呼ぶと、ここでカーソルや `loadedEventIds` だけを初期化したあと `next()` は実行中タスクがあるため戻り、古いタスクがリセット済みカーソルで続行します。タイミング次第で同じ注文を再スキャンして重複イベントを追加したり、注文完了後に意図した新しい読み直しが走らないため、reload 世代を持つか executor を安全にクリアして古い結果を破棄してください。

**コメント要約**: reload 時に進行中 next が止まらず、古いタスクが reset 後の cursor で続行する race。<br>フードタブ userFoods と同型。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: userFoods.ts と同パターンの loadGeneration を導入すべき。指摘は妥当。

**対応内容**: base/src/stores/userOrderHistoryList.ts に loadGeneration、reload 時の paginationExecutor.clear、next 完了時の世代チェックを追加。

---

## 評価セッション（2026-06-23 16:37 JST・shokujii-code-review）

- **評価日時**: 2026-06-23 16:37 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: fix/2098
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2099
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-10 | なし | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | loadOrdersForEvent が loadGeneration 未チェック<br>reload 後に古い注文詳細が orderStateByEventId へ書き戻る |

---

**識別子**: RC-10（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/stores/userOrderHistoryList.ts:62`

**該当コード（レビュー時点の diff）**:

```diff
+    const loadOrdersForEvent = async (event: BokudeliEvent) => {
+      const id = event.event_id
+      if (userId === '') return
+      try {
+        patchOrderState(id, { loading: true, error: null })
+        const list = await fetchMemberOrdersForUser(event.community_id, event.event_id, userId)
+        patchOrderState(id, { orders: list, loading: false, error: null })
+      } catch (e) {
+        console.error('Failed to fetch member_orders:', e)
+        patchOrderState(id, { orders: null, loading: false, error: e })
+      }
+    }
+
+    const loadOrdersForEventsParallel = async (newEvents: BokudeliEvent[]) => {
+      await Promise.all(newEvents.map((ev) => loadOrdersForEvent(ev)))
+    }
```

**レビュワーのコメント（原文）**:

🚨 **必須修正** [🔧微修正/S]: reload 後の古い注文取得が orderStateByEventId を更新できるため、RC-9 の race 対策が注文詳細側に残っています。

next 本体は loadGeneration で保護されているが、loadOrdersForEventsParallel 内の loadOrdersForEvent は世代を見ていない。reload 直後に古い fetchMemberOrdersForUser が返ると、現在世代の orderStateByEventId に古い注文一覧・loading・error が書き戻される。キャンセル直後や Stripe リダイレクト直後の再取得で、古い注文状態が一時的または最終的に表示される可能性がある。

loadOrdersForEvent に generation を渡し、fetch 前後と catch で generation !== loadGeneration なら patchOrderState しない形にする。reloadOrdersForEvent は現在世代の loadGeneration を渡せば既存挙動を保てる。

**コメント要約**: RC-9 で next の世代チェックは入ったが、イベント単位の注文詳細 fetch は未保護。<br>reload 後に古い fetchMemberOrdersForUser 完了で orderStateByEventId が汚染される。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-9 と同根の race で、注文履歴カードの注文詳細表示に直接影響する。events 配列側は世代チェック済みだが orderStateByEventId は未保護のため、キャンセル後リロードや連続 reload で古い注文状態が残る実害がありうる。loadGeneration を loadOrdersForEvent にも渡す修正が妥当。

**対応内容**: loadOrdersForEvent に generation 引数を追加し、fetch 前後と catch で generation !== loadGeneration なら patchOrderState しない。loadOrdersForEventsParallel と reloadOrdersForEvent から適切な generation を渡す。

---
