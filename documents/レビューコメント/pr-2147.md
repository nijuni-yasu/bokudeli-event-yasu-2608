# PR #2147 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-07-02 18:50 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: feat/2090
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2147
- **REVIEW_REQUEST_SINCE**: 2026-07-02T09:35:48Z
- **partial**: true（Codex usage limits / 接続案内のみ。Copilot レビューは取得）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 3（レビュー依頼定型文 #4864293243、Codex limits #4864295069、Codex 接続案内 #4864343575）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 4864342541 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | confirmEmailRegistration の Promise.all 順序<br>saveUser 失敗時に pass code 削除でリトライ不可 |
| [x] | RC-2 | 4864342541 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | rejectNewUserOnLogin 失敗時 Auth セッション残存<br>catch で best-effort signOut が必要（RC-15 追補） |
| [x] | RC-3 | 4864342541 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | register の rejectExistingUserOnRegister 失敗時<br>register_fail 表示のまま留まる |

---

**識別子**: RC-1（GitHub id: 4864342541・Copilot トップレベル内 [must] 節）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/user.ts` — `confirmEmailRegistration`

**該当コード（レビュー時点の diff）**:

```ts
const [token] = await Promise.all([
  getAuth().createCustomToken(uid),
  deletePassCode(passCodeDocument.id),
  saveUser(new ShokujiiUser(uid, { user_email: email })),
])
```

**レビュワーのコメント（原文）**:

**[must] `functions/default/src/user.ts` — `confirmEmailRegistration` の部分失敗による不整合**

`createUser` 成功後に `Promise.all([createCustomToken, deletePassCode, saveUser])` を並列実行しているため、`saveUser` が失敗した場合に以下の不整合状態が発生します。

- Auth ユーザー: 作成済み（孤立）
- Firestore ユーザー: 未作成
- pass code: 削除済み（リトライ不可）

`deletePassCode` は `saveUser` が成功してから実行するよう順序を分けることを推奨します。

**コメント要約**: saveUser 失敗時も deletePassCode が並列で走り pass code が失われリトライ不能になる

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当。RC-2 で createUser 失敗の already-exists は対応済みだが、createUser 成功後の saveUser 失敗時の pass code 削除順序は未対応。token 生成と saveUser を先に完了し deletePassCode は直列化すべき。

**対応**: saveUser 成功後に deletePassCode を実行するよう順序を変更。saveUser 失敗時は pass code 未削除を Vitest で検証。

---

**識別子**: RC-2（GitHub id: 4864342541・Copilot トップレベル内 [imo] 節）

**レビュワー**: Copilot

**指摘箇所**: `user/src/router/index.ts` — `/login` redirect で `rejectNewUserOnLogin` 失敗時

**該当コード（レビュー時点の diff）**:

```ts
try {
  await rejectNewUserOnLogin(userCredential)
} catch (err) {
  console.error(err)
  window.alert(/* login_fail */)
  return  // ← undefined: /login へ進むが Auth セッションは残っている
}
```

**レビュワーのコメント（原文）**:

**[imo] `user/src/router/index.ts` — `/login` redirect で `rejectNewUserOnLogin` 失敗時に Auth セッションが残る**

`rejectNewUserOnLogin` の `user.delete()` が失敗した場合、catch ブロックで `return`（undefined）して `/login` へのナビゲーションを続行しますが、この時点で Auth セッション（削除失敗のアカウント）が残ったままです。

ログイン済み状態のまま `/login` に進むと、`isLoggedIn` ガードが `/` へリダイレクトし、その後 `updateProfileFromProviders` が Shokujii ユーザー未存在でエラーになる不整合フローになりえます。

delete が失敗してもベストエフォートで `signOut` を呼び、セッションをクリアする方が安全です。

**コメント要約**: RC-15 で helper 内 signOut をやめた結果、delete 失敗時にセッションが残りログイン済みガードと矛盾しうる

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-15 の意図（delete 失敗を成功扱いにしない）は維持しつつ、catch 側で best-effort signOut を追加するのが妥当。孤児 Auth は残るがログイン済み扱いの誤遷移は防げる。login.vue の DEV ポップアップ経路も同様。

**対応**: signOutBestEffort を authEntryGuards に追加。router / login.vue の rejectNewUserOnLogin catch から呼び出し。

---

**識別子**: RC-3（GitHub id: 4864342541・Copilot トップレベル内 [nits] 節）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/register/index.vue` — `rejectExistingUserOnRegister` 失敗時

**該当コード（レビュー時点の diff）**:

```ts
if (aui?.isNewUser === false) {
  notification.show($t('register.already_registered'), 'warning')
  await rejectExistingUserOnRegister()  // ← ここで throw
  await router.push(getLogin())        // ← 実行されない
  return
}
```

**レビュワーのコメント（原文）**:

**[nits] `user/src/pages/register/index.vue` — `rejectExistingUserOnRegister` 失敗時の catch 処理**

OAuth で `isNewUser === false` のとき `rejectExistingUserOnRegister()` が例外をスロー（signOut 失敗など）すると、catch ブロックに落ちて `register.register_fail` が表示され、`/login` へのリダイレクトが実行されません。

signOut 失敗は稀ですが、発生した場合はユーザーが「新規登録できませんでした」というミスリーディングなエラーを見たまま登録ページに留まります。

**コメント要約**: signOut 失敗時に already_registered 後 register_fail 表示で /login へ行けない

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当だが発生頻度は低い。router 側 RC-13 分岐も同様。rejectExistingUserOnRegister を try/catch し already_registered 表示後は /login 誘導を優先するのがよい。

**対応**: router / register/index.vue で rejectExistingUserOnRegister を個別 try/catch し、失敗時も /login へ誘導。

---

## 評価セッション

- **評価日時**: 2026-07-02 19:52 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: feat/2090
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2147
- **REVIEW_REQUEST_SINCE**: 2026-07-02T10:41:33Z
- **partial**: true（Codex usage limits / 接続案内のみ。Copilot レビューは取得）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 3（レビュー依頼定型文 #4864823789、Codex limits #4864824901、Codex 接続案内 #4864848925）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-4 | 4864847865 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | pass-code reSendPassCode で already-exists 未通知<br>submit と同様の login 誘導が必要 |
| [ ] | RC-5 | 4864847865 | 👌 修正不要 | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | register の linkRequestDialog が到達不能<br>RC-14 意図的。削除は任意クリーンアップ |
| [ ] | RC-6 | 4864847865 | 👌 修正不要 | 📌 スコープ内 | — | 👀 確認のみ | — | createCustomToken 失敗時の Auth 孤立<br>実運用稀。TODO/バッチは別 Issue 可 |

---

**識別子**: RC-4（GitHub id: 4864847865・Copilot トップレベル内 [imo] 節）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/pass-code.vue` — `reSendPassCode`

**該当コード（レビュー時点の diff）**:

```ts
} catch (error) {
  console.warn('Error resending pass code:', error)  // ← 通知なし
}
```

**レビュワーのコメント（原文）**:

**[imo] `user/src/pages/pass-code.vue` — `reSendPassCode` で `register` モードの `already-exists` エラーが通知されない**

`submit` では `functions/already-exists` を検知して通知とログインリダイレクトを行っていますが、`reSendPassCode` では同エラーを `console.warn` のみで握り潰しています。

複数タブ操作や別デバイスで同メールを登録した場合など、再送時に `already-exists` が返ると画面は変わらずユーザーへフィードバックがないまま操作が止まります。`submit` と同様に `already-exists` を検知してログイン誘導する処理を追加することを推奨します。

**コメント要約**: register モードの再送で already-exists が silent fail。submit と対称の UX が必要

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当。submit 側 L93-96 と同パターンを reSendPassCode に追加すれば解消。入口分離 #2090 の一貫性にも合う。

**対応**: reSendPassCode の catch で register モードかつ functions/already-exists 時に already_registered 通知と /login へ誘導。

---

**識別子**: RC-5（GitHub id: 4864847865・Copilot トップレベル内 [nits] 節）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/register/index.vue` — `linkRequestDialogParams`

**該当コード（レビュー時点の diff）**:

（RC-14 により `/register` 起点の credential 衝突は pid 付き `/login` へ進めず `/login` のみ）

**レビュワーのコメント（原文）**:

**[nits] `user/src/pages/register/index.vue` — `linkRequestDialogParams` ダイアログが到達できないフロー**

`/register` で同エラーが発生した場合は `window.alert` → `{ path: '/login' }` に直接リダイレクトされるため、`register/index.vue` の `linkRequestDialogParams` ダイアログが表示される経路が現在の実装では存在しません。

**コメント要約**: RC-14 後 register 側 pid リンクダイアログがデッドコード化

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-14 で register 起点の credential 衝突を pass-code / pid リンクへ進めない設計は意図的。デッドコード削除は保守改善として任意（本 PR 必須ではない）。login 側ダイアログは存続。

---

**識別子**: RC-6（GitHub id: 4864847865・Copilot トップレベル内 [nits] 節）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/user.ts` — `confirmEmailRegistration`

**該当コード（レビュー時点の diff）**:

（RC-1 対応後: createCustomToken → saveUser → deletePassCode）

**レビュワーのコメント（原文）**:

**[nits] `functions/default/src/user.ts` — `confirmEmailRegistration` で `createCustomToken` 失敗時の Auth 孤立**

前回指摘（`saveUser` 失敗によるリトライ不可）は対応済み。ただし `createCustomToken` 失敗時は Auth 作成済み・pass code 残存で、リトライは already-exists になる不整合が残る。TODO コメントやバッチ手段の記載を推奨。

**コメント要約**: createCustomToken 失敗時の Auth 孤児。稀。ドキュメント/TODO 程度

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 実運用でほぼ発生しない。RC-1 と同種の完全復旧には idempotent 登録や Admin 削除が必要で S 超。必要なら別 Issue で TODO または bokudeli-event-batch 検討。

---

## 評価セッション（2026-07-03 ・shokujii-code-review）

- **評価日時**: 2026-07-03 JST
- **評価者**: Cursor Agent（shokujii-code-review）
- **ブランチ名**: feat/2090
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2147
- **参照仕様**: [15_アカウント作成.md](../03_参加者獲得/15_アカウント作成.md)（仕様書レビュー後の実装突合）
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-7 | なし・エージェントレビュー | 🚨 必須修正 | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | confirmEmailRegistration saveUser 失敗時 Auth 孤児<br>passCode 残存でも OTP 再試行不能（§6 E-8） |
| [x] | RC-8 | なし・エージェントレビュー | 🚨 必須修正 | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | OAuth 復帰時 updateProfileFromProviders 失敗を握りつぶし<br>Auth ログイン済み・Firestore 未作成 |
| [x] | RC-9 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | 🐛 実害 | 📋 仕様追加 | M | SNS 新規登録で Firestore メール重複未チェック<br>§6 E-11 |
| [x] | RC-10 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | register 登録済みメールで already-exists 時<br>/login へ遷移しない（pass-code と非対称） |
| [x] | RC-11 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | pass-code「戻る」i18n が register 時も<br>「ログイン画面へ戻る」固定（§6 E-12） |
| [x] | RC-12 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | deletePassCode 失敗時 Auth のみ rollback<br>Firestore 孤児が残る |
| [x] | RC-13 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | currentUser.linkProvider が<br>updateProfileFromProviders 失敗を握りつぶす |

---

**識別子**: RC-7（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/user.ts:137` — `confirmEmailRegistration`

**該当コード**:

```ts
  const uid = user.uid
  const token = await getAuth().createCustomToken(uid)
  await saveUser(
    new ShokujiiUser(uid, {
      user_email: email,
    }),
  )
  await deletePassCode(passCodeDocument.id)
  return { token }
```

**レビュワーのコメント（原文）**:

🚨 **必須修正** [🐛 実害 / 🔧 微修正 / M]: `confirmEmailRegistration` の saveUser 失敗で Auth 孤児（§6 E-8）

RC-1 で passCode 削除順序は直列化済みだが、`createUser` 成功後に `saveUser` が失敗すると **Auth のみ存在・Firestore 未作成**のまま残る。passCode は残るが OTP 再試行時は `email-already-exists` → `/login` 誘導となり、Firestore 未作成のため `requestEmailLogin` も `not-found` で **メール OTP ログイン不能**になりうる。

**コメント要約**: saveUser 失敗時 Auth rollback なし。ユーザー登録不能状態

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: [15_アカウント作成.md](../03_参加者獲得/15_アカウント作成.md) §5.2 / §6 E-8 に記載の既知リスク。RC-1 は passCode 順序のみで Auth 孤児の復旧は未対応。Vitest 已有 saveUser 失敗ケースに `deleteUser` ロールバックを追加すべき。処理順序は `createUser` → `saveUser` → `deletePassCode` → `createCustomToken` が望ましい（RC-6 の createCustomToken 失敗も緩和）。

**対応**: saveUser / deletePassCode 失敗時に `deleteUser` で Auth rollback。処理順序を `createUser` → `saveUser` → `deletePassCode` → `createCustomToken` に変更。Vitest で rollback と passCode 未削除を検証。

---

**識別子**: RC-8（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/router/index.ts:246` — OAuth 復帰時 `updateProfileFromProviders`

**該当コード**:

```ts
        const response = await updateProfileFromProviders(userCredential).catch((error) => {
          console.error('updateProfileFromProviders error:', error)
        })
        shokujiiUser = response?.data?.user
      // ...
      if (shokujiiUser == null) {
        return getRedirectPath() ?? '/'
      }
```

**レビュワーのコメント（原文）**:

🚨 **必須修正** [🐛 実害 / 🔧 微修正 / M]: OAuth 復帰時 `updateProfileFromProviders` 失敗を握りつぶし

SNS 新規登録（§4.6）の本番経路で Functions 失敗時も **Auth ログイン済みのまま** `redirectPath` または `/` へ進む。Firestore ユーザーが作られない不正状態になりうる。

**コメント要約**: SNS 登録で Firestore 未作成のままログイン状態

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 仕様 §4.6「updateProfileFromProviders で Firestore 作成」と矛盾。失敗時は signOut + エラー表示 + `/register` または `/login` へ。`/register` 起点かつ `isNewUser` なら Auth delete も検討（E-8 と同種）。

**対応**: `authEntryGuards.handleProfileUpdateFailure` を追加。router で `.catch` 握りつぶしを廃止し、失敗時 signOut / 新規 Auth delete + エラー表示 + 適切な path へリダイレクト。Vitest 追加。

---

**識別子**: RC-9（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/user.ts:230` — `updateProfileFromProviders`

**該当コード**:

```ts
  user = user ?? new ShokujiiUser(uid, {})
  const providerData = (await getAuth().getUser(uid)).providerData
  for (const provider of providerData) {
    user.user_email = user.user_email || provider.email
    // ...
  }
  if (!_.isEqual(originalUser, user)) {
    await saveUser(user)
  }
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🐛 実害 / 📋 仕様追加 / M]: SNS 新規登録で Firestore メール重複未チェック（§6 E-11）

メール経路は `getUserIdFromEmail` で拒否するが、SNS `/register` + `isNewUser=true` は **Firestore メール重複の事前チェックなし**。OAuth 衝突に至らないと同一メールの別 uid が作られうる。

**コメント要約**: メール/SNS で登録済み判定レイヤ不一致。二重アカウントリスク

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: 仕様 §3.2 / §6 E-11 に「起こりうる」と明記。意図的許容でなければ `updateProfileFromProviders` または router ガードで provider email を `getUserIdFromEmail` 検証し `already-exists` 相当を返すべき。

**対応**: `updateProfileFromProviders` で Firestore 新規ユーザー時に `getUserIdFromEmail` チェックを追加。重複時 `already-exists`。Vitest 追加。

---

**識別子**: RC-10（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/register/index.vue:66` — `handleRegister` catch

**該当コード**:

```ts
    if (providerId === 'custom' && error instanceof FirebaseError && error.code === 'functions/already-exists') {
      notification.show($t('register.already_registered'), 'warning')
    } else {
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [👤 UX / 🔧 微修正 / S]: `/register` 登録済みメールで `/login` へ遷移しない

§6 E-2 は「`/login` 誘導」とあるが、実装は **トーストのみ**。`pass-code.vue` の submit / reSend（RC-4 対応済み）は `router.push(getLogin())` するため非対称。

**コメント要約**: register/index already-exists は警告のみ。pass-code と UX 不一致

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: pass-code と同パターンで `await router.push(getLogin())` を追加するのが自然。または仕様 E-2 を「トーストのみ」に統一（§3.2.1 と E-2 の矛盾解消）。

**対応**: `register/index.vue` の `functions/already-exists` catch に `router.push(getLogin())` を追加。

---

**識別子**: RC-11（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/pass-code.vue:159` / `user/src/locales/messages/ja.ts:624` — pass-code「戻る」

**該当コード**:

```ts
// pass-code.vue goBack
const goBack = () => {
  if (mode === 'register') {
    router.push(getRegister())
  } else {
    router.push(getLogin())
  }
}
```

```ts
// ja.ts
back: 'ログイン画面へ戻る',
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [👤 UX / 🔧 微修正 / S]: pass-code「戻る」文言と実装の不一致（§6 E-12）

`mode=register` では `/register` に戻るが、i18n `passcode.back` は「ログイン画面へ戻る」固定。

**コメント要約**: register モードで戻る先とボタン文言が不一致

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 仕様 §4.7 / §6 E-12 に記載済み。`mode` で `$t` を分岐するか `passcode.back_register` を追加。

**対応**: `ja.ts` に `passcode.back_register` を追加。`pass-code.vue` で mode に応じて `$t` を分岐。

---

**識別子**: RC-12（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/user.ts:138` — `confirmEmailRegistration` rollback

**該当コード**:

```ts
  try {
    await saveUser(...)
    await deletePassCode(passCodeDocument.id)
  } catch (error) {
    await getAuth().deleteUser(uid)
    throw error
  }
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🐛 実害 / 🔧 微修正 / S]: deletePassCode 失敗時 Auth のみ rollback

saveUser 成功後に deletePassCode が失敗すると Auth だけ delete され Firestore ユーザーが残る。

**コメント要約**: deletePassCode 失敗時の Firestore 孤児

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-7 の Auth rollback 拡張。saveUser 成功後の失敗では Firestore も削除し OTP 再試行可能に戻す。

**対応**: `deleteNewUserDocuments` を store に追加。saveUser 成功後の失敗時は Firestore + Auth rollback。Vitest 追加。

---

**識別子**: RC-13（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/stores/currentUser.ts:219` — `linkProvider`

**該当コード**:

```ts
    updateProfileFromProviders(userCredential).catch((error) => {
      console.error('updateProfileFromProviders error:', error)
    })
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🐛 実害 / 🔧 微修正 / S]: linkProvider が updateProfileFromProviders 失敗を握りつぶす

プロフィール SNS 連携で Functions 失敗時も呼び出し元は成功扱いになる。

**コメント要約**: 設定画面経路の updateProfileFromProviders 失敗が UI に届かない

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-8 は router OAuth 復帰経路のみ。base currentUser の fire-and-forget は別経路の実害。

**対応**: `await updateProfileFromProviders` に変更。register/complete.vue のエラー表示を `profile.linkage_failed` に統一。

---
