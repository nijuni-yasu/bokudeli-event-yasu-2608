# PR #2004 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-11 21:15 JST
- **ブランチ名**: `feat/1740`
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/2004](https://github.com/nijuniinc/bokudeli-event-new/pull/2004) (#2004)
- **Outdated 除外件数**: 0件

### RC 別対応チェックリスト

- **RC-1**（GitHub id: 4420527633 / Copilot まとめ）— ✅ 対応済み（`functions/default/src/eventCancellation.ts` 文字列 falsy チェック / 2026-05-11 コミット b6f58db0）
- **RC-2**（GitHub id: 4420527633 / Copilot まとめ）— ✅ 対応済み（`functions/default/src/eventCancellation.ts` `Date.now()` 直接使用 / 2026-05-11 コミット b6f58db0）
- **RC-3**（GitHub id: 4420527633 / Copilot まとめ）— ✅ 対応済み（`functions/default/src/eventCancellation.ts` `as` キャスト撤去 / 2026-05-11 コミット b6f58db0）
- **RC-4**（GitHub id: 4420527633 / Copilot まとめ）— ✅ 対応済み（`functions/default/src/eventMenusSelection.ts` `calculatedStatus` 統一 / 2026-05-11 コミット b6f58db0）
- **RC-5**（GitHub id: 4420527633 / Copilot まとめ）— ✅ 対応済み（`functions/default/src/memberOrders.ts` `isCanceled()` 利用 / 2026-05-11 コミット b6f58db0）
- **RC-6**（GitHub id: 4420527633 / Copilot まとめ）— ✅ 対応済み（`user/src/components/manage/event/overview.vue` `alert()` を `useNotification` に置換 / 2026-05-11 コミット 1263881a）
- **RC-7**（GitHub id: 3218758471）— ✅ 対応済み（`getCommunity()` undefined 時の `not-found` 返却 / 2026-05-11 コミット b6f58db0）
- **RC-8**（GitHub id: 3218736325）— ✅ 対応済み（終了済みイベントのキャンセル拒否、`calculatedEventStatus` で判定 / 2026-05-11 コミット b6f58db0）
- **RC-9**（GitHub id: 3218758497）— ✅ 対応済み（参加者判定の `ordered` 注文確認＋トランザクション化 / 2026-05-11 コミット b6f58db0）
- **RC-10**（GitHub id: 3218758417）— 👌 修正不要（キャンセル理由に文字数上限なし。`eventCancellation.ts`・`overview.vue`・`08_イベントキャンセル_参加者なし.md` で確認済み）
- **RC-11**（自己レビュー）— ✅ 対応済み（`overview.vue` `canCancel` を `calculatedEventStatus` で判定 / 2026-05-11 コミット 1263881a）
- **RC-12**（自己レビュー）— 🚨 修正推奨（`cancel_support_dialog` にサポート連絡先を表示、仕様書 §2.4 未実装）
- **RC-13**（自己レビュー）— 🟡 提案（`event_canceled` 時の `cancel_reason` / `canceled_at` / `canceled_by` 必須化を zod refine で担保）
- **RC-14**（自己レビュー）— 🟡 提案（`executeCancelEvent` の catch でエラー種別を区別したメッセージにする）
- **RC-15**（自己レビュー）— ✅ 対応済み（`overview.vue` `executeCancelEvent` で `eventStore.event` をローカルに取り `null` 時 return。`!` 排除）
- **RC-16**（自己レビュー）— 🟡 提案（`[eventId]/index.vue` `menuDisabled` switch に網羅性チェック追加）
- **RC-17**（GitHub id: 4420495065）— 👌 修正不要（レビュー依頼の定型コメント）
- **RC-18**（GitHub id: 4420528605）— 👌 修正不要（Codex コネクタ接続案内）
- **RC-19**（GitHub id: 4422444021）— 👌 修正不要（レビュー依頼の定型コメント）
- **RC-20**（GitHub id: 4422483272 / Copilot `overview.vue` L150）— ✅ 対応済み（`executeCancelEvent` の non-null assertion 排除。RC-15 と同一修正）
- **RC-21**（GitHub id: 4422483272 / Copilot `overview.vue` L244）— 👌 修正不要（`grey-400` は [imo]・デザイン判断の範囲）
- **RC-22**（GitHub id: 4422483272 / Copilot `overview.vue` L133）— 👌 修正不要（冗長 null チェックは型絞り込み目的として許容）
- **RC-23**（GitHub id: 4422483272 / Copilot `admin/.../order` L178）— 👌 修正不要（`!== ''` は防御的で害なし）
- **RC-24**（GitHub id: 4422484496）— 👌 修正不要（Codex コネクタ接続案内）
- **RC-25**（GitHub id: 3220324976）— 👌 修正不要（RC-8 への対応完了コメント）
- **RC-26**（GitHub id: 3220325055）— 👌 修正不要（RC-7 への対応完了コメント）
- **RC-27**（GitHub id: 3220326340）— 👌 修正不要（RC-9 への対応完了コメント）
- **RC-28**（GitHub id: 3219508132）— 🚨 修正推奨（SendGrid テンプレート ID が `TBD` のまま・メール送信失敗を握りつぶす）
- **RC-29**（GitHub id: 3220329813）— 👌 修正不要（RC-28 への対応方針の返信。実装は RC-28）
- **RC-30**（GitHub id: 3220330824）— 👌 修正不要（`in_cart` が残っていても主催者キャンセル可とするプロダクト判断）
- **RC-31**（GitHub id: 3220371691）— 👌 修正不要（RC-30 と同根・同一判断）
- **RC-32**（GitHub id: 3219508146）— 👌 修正不要（仕様で理由文字の上限なし。RC-10 と同一論点）
- **RC-33**（GitHub id: 3220325038）— 👌 修正不要（RC-10 スレッドへの仕様説明の返信）
- **RC-34**（GitHub id: 3220329867）— 👌 修正不要（RC-32 スレッドへの仕様説明の返信）

---

**識別子**: RC-1（GitHub id: 4420527633 / Copilot トップレベルまとめ）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**`functions/default/src/eventCancellation.ts` L28**
[must] 文字列への falsy チェック

`!communityId || !eventId || !cancelReason` は文字列に falsy チェックを使っており、プロジェクト規約では `!= null` または `!== ''` を使う必要があります。

```ts
// NG
if (!communityId || !eventId || !cancelReason) {

// OK（型で string が保証されていれば空文字チェックのみ）
if (communityId === '' || eventId === '' || cancelReason === '') {
```

```

**コメント要約**: `cancelEvent` の引数バリデーションで文字列に対して `!` による falsy チェックを使っており、プロジェクト規約違反。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: `shokujii-code-review.md` の「比較・falsy チェック」項目で「文字列に falsy チェックを使っていないか（`!= null` または `!== ''` を使う）」が明記されており、規約違反。`CancelEventRequest` の型は `string` なので、空文字チェック（`=== ''`）に修正する。

---

**識別子**: RC-2（GitHub id: 4420527633 / Copilot トップレベルまとめ）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**functions/default/src/eventCancellation.ts` L66**
[must] `Date.now()` を直接使用している

プロジェクト規約では `Date` の直接使用は禁止で、`luxon` を使う必要があります。

```ts
// NG
canceled_at: Date.now(),

// OK
import { DateTime } from 'luxon'
canceled_at: DateTime.now().toMillis(),
```

```

**コメント要約**: `canceled_at` の値生成に `Date.now()` を直接使っているが、規約により `luxon` を使うべき。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: `shokujii-code-review.md` の「日付・時刻処理」項目で「`Date` オブジェクトを直接使っていないか（`luxon` を使う）」と明記されている。`DateTime.now().toMillis()` に置き換えるのが規約準拠。

---

**識別子**: RC-3（GitHub id: 4420527633 / Copilot トップレベルまとめ）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**functions/default/src/eventCancellation.ts` L68**
[must] `as` によるキャストを使用している

`} as Partial<ShokujiiEvent>` はプロジェクト規約で禁止されている型キャストです。`updateEvent` の引数型を見直してキャストなしで渡せるようにしてください。

```

**コメント要約**: `updateEvent` 呼び出し時に `as Partial<ShokujiiEvent>` でキャストしているが、規約により `as` 禁止。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: `shokujii-code-review.md` の「TypeScript・型安全性」項目で「`as` によるキャストを使用していないか（型推論で解決できるはず）」と明記されている。`updateEvent` のシグネチャ側で `Partial<ShokujiiEvent>` を受け取れる型に整え、キャスト不要にすべき。実装上は `as` 撤去のみで `tsc -b` 通過を確認した（型推論で解決された）。

---

**識別子**: RC-4（GitHub id: 4420527633 / Copilot トップレベルまとめ）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**functions/default/src/eventMenusSelection.ts` L85**
[imo] `calculatedStatus` と `eventStatus` が混在している

`calculatedEventStatus` は `event_canceled` を既に返します（`Event` クラスの早期リターン）。`eventStatus === 'event_canceled'` を `calculatedStatus === 'event_canceled'` に統一できます。

```ts
// 現状（混在）
if (calculatedStatus === 'finished' || eventStatus === 'event_canceled') {

// 提案（統一）
if (calculatedStatus === 'finished' || calculatedStatus === 'event_canceled') {
```

```

**コメント要約**: メニュー更新ガード条件で `calculatedStatus`（計算済み）と `eventStatus`（生値）が混在しているので `calculatedStatus` に統一すべき。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: `Event.calculatedEventStatus` の getter は `event_canceled` を最優先で返すよう実装している（本 PR の `common/src/schemas/Event.ts` 改修内容）ため、`calculatedStatus` のみで判定しても動作は等価。同じ意味の判定で 2 つのソースを混ぜると将来の混乱を招くので統一すべき。`imo` だが妥当な指摘。

---

**識別子**: RC-5（GitHub id: 4420527633 / Copilot トップレベルまとめ）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**functions/default/src/memberOrders.ts` L44, L172**
[nits] `isCanceled()` メソッドを使わず `event_status.value` を直接参照している

`Event` クラスには `isCanceled()` メソッドが定義されています。

```ts
// 現状
if (eventData.event_status.value === 'event_canceled') {

// 提案
if (eventData.isCanceled()) {
```

```

**コメント要約**: `addToCart` / `confirmOrder` のキャンセルガードで `event_status.value` を直接参照しているが、本 PR で追加した `isCanceled()` メソッドを使うべき。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: 本 PR で `Event` クラスに `isCanceled()` メソッドを追加した目的（カプセル化と呼び出し側の可読性向上）に沿うため、利用側でも統一して使うのが望ましい。`nits` だが、追加したメソッドが活かされる形に揃えるべき。

---

**識別子**: RC-6（GitHub id: 4420527633 / Copilot トップレベルまとめ）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**user/src/components/manage/event/overview.vue` L135**
[must] `alert()` を使用している

`alert()` はブラウザネイティブのダイアログでプロジェクトの UI スタイルと合いません。`v-dialog` や `v-snackbar` などのコンポーネントを使ってエラーを表示してください。

```

**コメント要約**: キャンセル失敗時のエラー表示に `alert()` を使っているが、プロジェクトの UI スタイルに合わない。

**判断結果**: ✅ 対応済み（2026-05-11 コミット 1263881a）

**判断理由**: プロジェクトは Vuetify ベースで UI を構築しており、エラー表示も Vuetify のコンポーネント（`v-snackbar` 等）に統一されている。`alert()` はネイティブダイアログでスタイル不整合かつ i18n も効かないため、Vuetify ベースのエラー UI に置き換えるべき。実装では `useNotification` の snackbar（color: 'error'）に置換した。

---

**識別子**: RC-7（GitHub id: 3218758471）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

[must] `getCommunity()` が見つからず `undefined` を返した場合でも `permission-denied` になってしまいます（`community?.hasRole` が `undefined` → `!undefined`）。存在しないコミュニティIDの場合は `not-found` 等で明確に返し、`community` が取得できた場合のみ role 判定するよう分岐してください。

```

**コメント要約**: `getCommunity()` が `undefined` を返したときに `community?.hasRole(...)` が `undefined` となり、現状コードでは `permission-denied` として扱われる。コミュニティ未存在は `not-found` で返し、存在する場合のみ role 判定する分岐にすべき。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: クライアントから見て「コミュニティ未存在」と「権限不足」は意味が異なるエラー。同じ `permission-denied` で返すと UI 側のメッセージ・行動分岐が崩れる。`community == null` のときに `not-found`、それ以外で `manager` 判定して `permission-denied` を返す素直な分岐に直すのが妥当。

---

**識別子**: RC-8（GitHub id: 3218736325）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  終了済みイベントのキャンセルを拒否する**

`event_status.value` だけで判定しているため、`accepting_order` のまま終了時刻を過ぎたイベント（`calculatedEventStatus` は `finished`）でもこの条件を通過してキャンセル実行できてしまいます。`common/src/schemas/Event.ts` の `calculatedEventStatus` は時刻で `finished` を返す設計なので、現状だと「終了済みはキャンセル不可」という仕様をサーバー側で担保できず、終了後イベントの状態更新や店舗向けキャンセル通知が発生します。

Useful? React with 👍 / 👎.

```

**コメント要約**: `cancelEvent` の状態判定が `event_status.value` だけのため、生値が `accepting_order` のままでも終了時刻を過ぎたイベント（`calculatedEventStatus === 'finished'`）はキャンセル可能になっている。サーバー側で「終了済みはキャンセル不可」を担保できておらず、店舗への誤通知につながる。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: 仕様書 `documents/02_主催者獲得と継続/08_イベントキャンセル_参加者なし.md` でも「終了済みイベントはキャンセル対象外」を前提にしている。`Event.calculatedEventStatus` が時刻で `finished` を返す設計である以上、サーバー側のバリデーションも `calculatedEventStatus` をベースに行うべき。RC-4 と方向性が一致しており、`cancelEvent` 側でも同様に `calculatedEventStatus` を使ったガードに統一する。

---

**識別子**: RC-9（GitHub id: 3218758497）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

[must] Phase 1 の「参加者なし」判定を `event.members.length` だけに依存すると、`ordered` の注文が存在しても `members` 再集約（`recalcEventMembers`）前のタイミングでは 0 のままになり、キャンセルできてしまう可能性があります。また、このチェック〜更新がトランザクションで保護されていないため競合にも弱いです。`ordered` 注文の有無（例: `event.hasOrderedOrders()`）をトランザクション内で確認し、同じトランザクションでステータス更新まで行う形にしてください。

```

**コメント要約**: Phase 1 の「参加者なし」判定が `event.members.length` のみで、`recalcEventMembers` 反映前は `ordered` 注文があっても 0 のままになり、誤って Phase 1 経路でキャンセルされるリスクがある。トランザクション内で `ordered` 注文の有無を確認し、ステータス更新まで同一トランザクションで行うべき。

**判断結果**: ✅ 対応済み（2026-05-11 コミット b6f58db0）

**判断理由**: `members` は `recalcEventMembers` の遅延反映に依存しており、`addToCart`/`confirmOrder` 直後はカウントがずれている可能性がある。Phase 1 のスコープ（参加者ゼロのみキャンセル可）を担保するには、`ordered` 注文の有無をトランザクション内で読み、同じトランザクションでステータスを更新する設計が安全。`shokujii-code-review.md` の「Firestore / Store パターン」「レースコンディション」項目とも整合する。実装では `runTransaction` 内で `tEvent.hasOrderedOrders(transaction)` を確認し `tEvent.updateEvent(..., transaction)` でステータス更新まで同一トランザクションに含めた。

---

**識別子**: RC-10（GitHub id: 3218758417）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

[must] キャンセル理由が1000文字超でも「確認へ進む」ボタンが有効なままで、`proceedToConfirm()` も上限チェックをしていないため、サーバ側で `invalid-argument` になって失敗し得ます。クライアント側でも `<= 1000` を満たすときだけ進める（ボタン disabled 条件・`proceedToConfirm` の両方）ようにしてください。

```

**コメント要約**: 当初、キャンセル理由に上限がないとサーバーで `invalid-argument` になり得る、として 1000 文字以下のクライアントチェックを求めた指摘。

**判断結果**: 👌 修正不要

**判断理由**: **仕様上、キャンセル理由に文字数上限は設けない**（`documents/02_主催者獲得と継続/08_イベントキャンセル_参加者なし.md` および実装で整合）。`functions/default/src/eventCancellation.ts` はプリセット文言の一致または「その他」の `trim` 後非空のみ検証し、50〜1000 文字の zod 制約はない。`user/src/components/manage/event/overview.vue` も「その他」時は 1 文字以上のみで、`counter`・上限ルールは残っていない。**指摘当時の前提（サーバに文字数バリデーションあり）は現在は成立しない**ため、追加のクライアント上限は不要。

---

## 評価セッション

- **評価日時**: 2026-05-11 21:18 JST
- **ブランチ名**: `feat/1740`
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2004 (#2004)
- **Outdated 除外件数**: 0件
- **対象**: `shokujii-code-review` スキルに沿った自己レビュー。RC-1〜RC-10 で挙がっていない論点のみ追加。

### RC 別対応チェックリスト

- [x] **RC-11**（自己レビュー）— ✅ 対応済み（`overview.vue` `canCancel` を `calculatedEventStatus` で判定 / 2026-05-11 コミット 1263881a）
- [ ] **RC-12**（自己レビュー）— 🚨 修正推奨（`cancel_support_dialog` にサポート連絡先を表示、仕様書 §2.4 未実装）
- [ ] **RC-13**（自己レビュー）— 🟡 提案（`event_canceled` 時の `cancel_reason` / `canceled_at` / `canceled_by` 必須化を zod refine で担保）
- [ ] **RC-14**（自己レビュー）— 🟡 提案（`executeCancelEvent` の catch でエラー種別を区別したメッセージにする）
- [x] **RC-15**（自己レビュー）— ✅ 対応済み（`overview.vue` `executeCancelEvent` の `!` 排除）
- [ ] **RC-16**（自己レビュー）— 🟡 提案（`[eventId]/index.vue` `menuDisabled` switch に網羅性チェック追加）

---

**識別子**: RC-11（自己レビュー）

**レビュワー**: 自己レビュー（shokujii-code-review）

**レビューのコメント（原文）**:

```

🔴 必須修正: `user/src/components/manage/event/overview.vue` の `canCancel` が `eventStore.event?.event_status.value` の生値だけで判定しており、`accepting_order` のまま終了時刻を過ぎた `calculatedEventStatus === 'finished'` のイベントでも「イベントをキャンセルする」ボタンが表示されてしまう。仕様書 `documents/02_主催者獲得と継続/08_イベントキャンセル_参加者なし.md §2.2` でも「キャンセル済み・イベント終了」では非表示が要件。`event_status.value === 'applying_reservation' || calculatedEventStatus === 'accepting_order'` のように `calculatedEventStatus` ベースに揃えるか、生値に加えて時刻チェックも入れて、終了済みではボタンが出ないようにする。RC-8 はサーバ側の同種の話題だが、本指摘は UI 側の独立した修正が必要。

```

**コメント要約**: 主催者画面のキャンセルボタン表示判定 `canCancel` が `event_status.value` の生値依存のため、終了時刻を過ぎた `accepting_order` イベントでもボタンが表示される。`calculatedEventStatus` ベースの判定に揃えて仕様書 §2.2 の「終了したイベントでは非表示」に従わせる。

**判断結果**: ✅ 対応済み（2026-05-11 コミット 1263881a）

**判断理由**: 仕様書 §2.2 で `event_canceled` / `finished` 時はボタン非表示が明記されている。RC-8 はサーバ側で「終了済みのキャンセル拒否」を担保する話だが、UI 側でもボタンが表示されてしまうとユーザーが押せて → サーバで `failed-precondition` 失敗 → `alert()` で「キャンセルに失敗しました」と汎用エラーが出る、という体験になり UX が悪い。`Event.calculatedEventStatus` を使ってボタン表示を判定するのが正しい設計。

---

**識別子**: RC-12（自己レビュー）

**レビュワー**: 自己レビュー（shokujii-code-review）

**レビューのコメント（原文）**:

```

🔴 必須修正: `user/src/locales/messages/ja.ts` の `manage.event.cancel_support_dialog.description` が「参加者への返金が必要なため、サポートへ連絡してください」のみで、サポート連絡先（メールアドレスや問い合わせフォーム URL 等）が表示されていない。仕様書 `08_イベントキャンセル_参加者なし.md §2.4` で「サポート連絡先を表示する」と明記されているため、Phase 1 の参加者あり時暫定対応として連絡先（例: SUPPORT_MAIL の `support@…` や問い合わせフォームへのリンク）を案内 UI に追加する必要がある。

```

**コメント要約**: 参加者あり時のサポート案内ダイアログ（`cancel_support_dialog`）に、仕様書 §2.4 で求められている「サポート連絡先」が含まれていない。連絡先（メールアドレスや問い合わせフォーム URL）を表示する。

**判断結果**: 🚨 修正推奨

**判断理由**: 仕様書 §2.4 のテキストを満たすには連絡手段を明示する必要がある。現状ではユーザーが「どこへ連絡すればいいか」分からず行動が止まる。`functions/default/src/utils/mail.ts` の `SUPPORT_MAIL` を参考に、UI 側で表示するサポート用の問い合わせ先（メールアドレスかフォーム URL）を共通化して案内に含めるべき。

---

**識別子**: RC-13（自己レビュー）

**レビュワー**: 自己レビュー（shokujii-code-review）

**レビューのコメント（原文）**:

```

🟡 提案: `common/src/schemas/Event.ts` の `EventDbSchema` で `event_status.cancel_reason` / `canceled_at` / `canceled_by` を 3 つともシンプルな `optional()` にしているため、`event_status.value === 'event_canceled'` なのに 3 フィールドが欠けたり、逆に `event_canceled` 以外のときに値が入っていたりするデータを zod が検出できない。仕様書 §3.3 でも「event_canceled 時のみ」と明記されているので、`superRefine` で「`value === 'event_canceled'` のときだけ 3 フィールドが必須」「それ以外では 3 フィールドが未設定」を保証する形に整える方が、データ整合性とドキュメント間の対応が取れる。

```

**コメント要約**: `event_canceled` ステータスと 3 つのキャンセルフィールド（`cancel_reason` / `canceled_at` / `canceled_by`）の整合性が zod レベルで担保されていない。`superRefine` 等でステータスとフィールドのセット保証を追加する。

**判断結果**: 🟡 提案

**判断理由**: `shokujii-code-review.md` の「スキーマ設計（Zod / common）」項目で「新規フィールドを optional にしていないか（新規追加フィールドは基本 required）」が挙げられている。本件は「他ステータスでは値を持たない」という正当な理由から個別 optional にしているが、`event_canceled` ステータスと 3 フィールドの整合性は仕様書で要件化されているため、zod の `superRefine` で守るのが Shokujii の規約姿勢に合う。修正コストと得られる安全性のバランス次第なので必須までは付けない。

---

**識別子**: RC-14（自己レビュー）

**レビュワー**: 自己レビュー（shokujii-code-review）

**レビューのコメント（原文）**:

```

🟡 提案: `user/src/components/manage/event/overview.vue` の `executeCancelEvent` の `catch` 句が `error` を捨てて `alert($t('manage.event.cancel_failed'))` の固定文言だけを出している。Functions 側からは `unauthenticated` / `permission-denied` / `not-found` / `failed-precondition`（参加者あり、ステータス不正、終了済み）/ `invalid-argument`（文字数）など複数の HttpsError コードが返り得るため、すべて同じ「キャンセルに失敗しました」になるとユーザーが原因を判断できない。最低でも `error.message`（または `error.code` で分岐したメッセージ）を併記し、本来不可能な参加者ありや終了済みは UI 側でも事前ガード（RC-11）を入れて辿り着かないようにする。あわせて RC-6（alert→Vuetify 化）と統合して対応する。

```

**コメント要約**: キャンセル失敗時のエラーハンドリングがエラー種別を区別せず固定メッセージで返すため、ユーザーが原因を把握できない。`error.code` / `error.message` をベースに分岐させる。

**判断結果**: 🟡 提案

**判断理由**: Phase 1 で想定しているエラーパターン（参加者あり・ステータス不正・終了済み）は UI 側で事前にガードできれば本来到達しないが、競合状態でサーバーから返ることはあり得る。情報が落ちる UX は改善余地あり。RC-6（`alert()` → Vuetify 化）と一緒に対応するのが効率的。

---

**識別子**: RC-15（自己レビュー）

**レビュワー**: 自己レビュー（shokujii-code-review）

**レビューのコメント（原文）**:

```

🟡 提案: `user/src/components/manage/event/overview.vue` の `executeCancelEvent` で `eventStore.event!.community_id` のように非 null assertion (`!`) を使っている。`shokujii-code-review.md` の「TypeScript・型安全性」項目では `as` キャストを禁じており、`!` も同じく型推論を強制的に上書きする操作なので避けたい。`canCancel` で表示制御しているとはいえ、ハンドラ実行時に `eventStore.event` が `null` だった場合のガードを早期 return か `if (eventStore.event == null) return` で書く方が型安全。

```

**コメント要約**: `eventStore.event!.community_id` の非 null assertion (`!`) は型推論を強制上書きしており、規約の精神（`as` キャスト禁止）に反する。早期 return で null ガードを書く。

**判断結果**: ✅ 対応済み

**判断理由**: `executeCancelEvent` で `const e = eventStore.event` とし、`e == null` なら return、続けて `communityId: e.community_id` で `cancelEvent` を呼ぶ実装に変更済み（`user/src/components/manage/event/overview.vue`）。指摘どおり `!` を使わずに型安全にガードできている。

---

**識別子**: RC-16（自己レビュー）

**レビュワー**: 自己レビュー（shokujii-code-review）

**レビューのコメント（原文）**:

```

🟡 提案: `user/src/pages/c/[communityId]/e/[eventId]/index.vue` の `menuDisabled` computed の switch が `default: return null` で逃げており、`base/src/components/EventStatusChip.vue` のような `_exhaustiveCheck: never = ...` の網羅性チェックが入っていない。今回 `event_canceled` を追加できているが、将来 `EventStatusType` を拡張したときに silent に `null` を返してメニュー無効化判定が漏れるリスクがある。`EventStatusChip.vue` と同じパターンで `default` ブロックに `const _exhaustiveCheck: never = event.value.calculatedEventStatus` を入れて網羅性をコンパイル時に強制する形にすると、追加漏れを防げる。

```

**コメント要約**: `menuDisabled` の switch に網羅性チェック（`never` チェック）が無い。`EventStatusChip.vue` と同じパターンで型レベルで漏れを防ぐ。

**判断結果**: 🟡 提案

**判断理由**: `EventStatusChip.vue` で同じ enum を扱う際にすでに網羅性チェックを入れているのに対し、本ファイルでは未実装。Shokujii 規約の「将来流用できる部分を最初から汎用化しているか」「冗長なチェックを追加していないか」と整合的に、TypeScript の `never` ブランチで漏れを検出する仕組みに揃えるのが望ましい。

---

## 評価セッション

- **評価日時**: 2026-05-12 01:21 JST
- **ブランチ名**: `feat/1740`
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2004 (#2004)
- **Outdated 除外件数**: 0件

### RC 別対応チェックリスト

- [x] **RC-17**（GitHub id: 4420495065）— 👌 修正不要
- [x] **RC-18**（GitHub id: 4420528605）— 👌 修正不要
- [x] **RC-19**（GitHub id: 4422444021）— 👌 修正不要
- [x] **RC-20**（GitHub id: 4422483272 / Copilot `overview.vue` L150）— ✅ 対応済み
- [x] **RC-21**（GitHub id: 4422483272 / Copilot `overview.vue` L244）— 👌 修正不要
- [x] **RC-22**（GitHub id: 4422483272 / Copilot `overview.vue` L133）— 👌 修正不要
- [x] **RC-23**（GitHub id: 4422483272 / Copilot `admin/.../order` L178）— 👌 修正不要
- [x] **RC-24**（GitHub id: 4422484496）— 👌 修正不要
- [x] **RC-25**（GitHub id: 3220324976）— 👌 修正不要
- [x] **RC-26**（GitHub id: 3220325055）— 👌 修正不要
- [x] **RC-27**（GitHub id: 3220326340）— 👌 修正不要
- [ ] **RC-28**（GitHub id: 3219508132）— 🚨 修正推奨
- [x] **RC-29**（GitHub id: 3220329813）— 👌 修正不要
- [x] **RC-30**（GitHub id: 3220330824）— 👌 修正不要
- [x] **RC-31**（GitHub id: 3220371691）— 👌 修正不要
- [x] **RC-32**（GitHub id: 3219508146）— 👌 修正不要
- [x] **RC-33**（GitHub id: 3220325038）— 👌 修正不要
- [x] **RC-34**（GitHub id: 3220329867）— 👌 修正不要

---

**識別子**: RC-17（GitHub id: 4420495065）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```

@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。

```

**コメント要約**: AI レビュワーへの依頼文（プロセス指示）であり、コードに対する指摘ではない。

**判断結果**: 👌 修正不要

**判断理由**: コード品質のレビュー対象ではない。`review-comments-evaluate` の目的外に近いが、取得対象には含まれるため「修正不要」と明記する。

---

**識別子**: RC-18（GitHub id: 4420528605）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**レビューのコメント（原文）**:

```

To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).

```

**コメント要約**: Codex 利用のためのアカウント接続案内。

**判断結果**: 👌 修正不要

**判断理由**: コードレビューではなくプロダクト案内。

---

**識別子**: RC-19（GitHub id: 4422444021）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```

@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。

```

**コメント要約**: RC-17 と同内容のレビュー依頼。

**判断結果**: 👌 修正不要

**判断理由**: RC-17 と同様。

---

**識別子**: RC-20（GitHub id: 4422483272 / Copilot `overview.vue` L150）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**user/src/components/manage/event/overview.vue` L150**
[must] non-null assertion `!` の使用

`eventStore.event!.community_id` は `!` による non-null assertion です。プロジェクト規約で禁止されている `as` キャストと同様、型チェックを回避します。`canCancel` が true の場合は `event != null` が保証されていますが、コード上は保証されていないため早期リターンや optional chaining で対処してください。

```ts
// NG
communityId: eventStore.event!.community_id,

// OK
const e = eventStore.event
if (e == null) return
await cancelEvent({
  communityId: e.community_id,
  ...
})
```

```

**コメント要約**: `executeCancelEvent` 内の `eventStore.event!.community_id` が non-null assertion で型安全を損なう。早期 return 等で置き換えるべき。

**判断結果**: ✅ 対応済み

**判断理由**: Copilot の提案どおり `const e = eventStore.event` と `e == null` 時の早期 return、`communityId: e.community_id` に修正済み（`user/src/components/manage/event/overview.vue`）。RC-15（自己レビュー）と同一の実装対応。

---

**識別子**: RC-21（GitHub id: 4422483272 / Copilot `overview.vue` L244）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**user/src/components/manage/event/overview.vue` L244**
[imo] テーマカラーの直接指定

`color="grey-400"` は Vuetify のテーマカラー外の値を直接指定しています。プロジェクト規約ではテーマカラーを使う必要があります。削除ボタンは `color` 指定なし（デフォルト）、コピーボタンも指定なしですが、キャンセルボタンだけ `grey-400` を指定している理由があればご確認ください。

```

**コメント要約**: キャンセルボタンだけ `grey-400` を直指定している点をテーマ準拠で見直せないか。

**判断結果**: 👌 修正不要

**判断理由**: [imo] レベル。視覚的に他ボタンより弱めたい意図があれば合理的。規約違反の確定よりデザイン判断の範囲。必要なら別チケットでテーマトークン化を検討でよい。

---

**識別子**: RC-22（GitHub id: 4422483272 / Copilot `overview.vue` L133）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**user/src/components/manage/event/overview.vue` L133**
[nits] 冗長な null チェック

`canProceedToCancelConfirm` が `true` を返す条件には `sel != null` の保証が含まれているため、直後の `if (sel == null) return` は冗長です。ただし TypeScript の型絞り込みのために残す意図であれば許容範囲です。

```ts
const proceedToConfirm = () => {
  if (!canProceedToCancelConfirm.value) return
  const sel = cancelPresetSelection.value
  if (sel == null) return  // ← canProceedToCancelConfirm が true なら不要
  ...
}
```

```

**コメント要約**: `canProceedToCancelConfirm` 真の直後の `sel == null` ガードは論理的に冗長な場合がある。

**判断結果**: 👌 修正不要

**判断理由**: Copilot 自身が「型絞り込みのため残すなら許容」と述べており、TS の制御フロー解析と computed の関係では明示ガードを残すのは一般的。必須リファクタではない。

---

**識別子**: RC-23（GitHub id: 4422483272 / Copilot `admin/.../order` L178）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

`**admin/src/pages/order/[eventId].vue` L178**
[nits] `cancel_reason !== ''` は冗長チェック

サーバー側で `reasonToStore.length < 1` のときはエラーを返すため、`cancel_reason` が `''` になることはありません。`!= null` のチェックのみで十分です。

```html
<!-- 現状 -->
v-if="eventStore.event.event_status.value === 'event_canceled' &&
      eventStore.event.event_status.cancel_reason != null &&
      eventStore.event.event_status.cancel_reason !== ''"

<!-- 提案 -->
v-if="eventStore.event.event_status.value === 'event_canceled' &&
      eventStore.event.event_status.cancel_reason != null"
```

```

**コメント要約**: `cancel_reason !== ''` はサーバ仕様上冗長かもしれない。

**判断結果**: 👌 修正不要

**判断理由**: 空文字を弾く防御的表示条件は害がなく、将来のデータ不整合やクライアント生成データにも強い。[nits] の整理提案に留まる。

---

**識別子**: RC-24（GitHub id: 4422484496）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**レビューのコメント（原文）**:

```

To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).

```

**コメント要約**: RC-18 と同様の接続案内。

**判断結果**: 👌 修正不要

**判断理由**: RC-18 と同様。

---

**識別子**: RC-25（GitHub id: 3220324976）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```

ご指摘のとおりのため、`cancelEvent` 内のステータス判定を `tEvent.calculatedEventStatus` ベースに変更しました。`finished` / `order_closed` 等では `failed-precondition` で拒否されます。

```

**コメント要約**: RC-8（終了済みキャンセル）対応として calculatedEventStatus ベースに変更した旨の報告。

**判断結果**: 👌 修正不要

**判断理由**: 新たなレビュー指摘ではなく、既知コメントへの実装返信。妥当性の評価対象は元スレ（RC-8）側。

---

**識別子**: RC-26（GitHub id: 3220325055）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```

`getCommunity` が `null` のときは `HttpsError('not-found', ...)` を返すようにし、`hasRole` 前に分岐を入れました。

```

**コメント要約**: RC-7 対応（community 未取得は not-found）の報告。

**判断結果**: 👌 修正不要

**判断理由**: RC-25 と同様の対応完了コメント。

---

**識別子**: RC-27（GitHub id: 3220326340）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```

トランザクション内で `tEvent.hasOrderedOrders(transaction)` を追加し、`members` が空でも `ordered` 注文がある場合は拒否するようにしました。

```

**コメント要約**: RC-9 対応（ordered 注文をトランザクション内で確認）の報告。

**判断結果**: 👌 修正不要

**判断理由**: RC-25 と同様。

---

**識別子**: RC-28（GitHub id: 3219508132）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**レビューのコメント（原文）**:

```

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  有効な SendGrid テンプレートIDを設定する**

`EVENT_CANCELLATION_TEMPLATE_ID` が `'TBD'` のままなので、キャンセル時の店舗通知メール送信は本番でも常に SendGrid 側で失敗します。しかもこの失敗は `catch` で握りつぶして処理自体は成功扱いで返るため、主催者はキャンセル完了と認識したまま店舗に通知されない状態が発生します。

Useful? React with 👍 / 👎.

```

**コメント要約**: テンプレ ID がプレースホルダのままだとメール送信が失敗し、catch で握りつぶされるとキャンセル成功と誤認しうる。

**判断結果**: 🚨 修正推奨

**判断理由**: 本番前に実 ID への置換または feature flag での無効化・明示エラーなど、運用上の欠陥を塞ぐ必要がある。ログだけに頼ると検知漏れのリスクが高い。`shokujii-functions-implementation` の外部 API 失敗扱いとしても見直し余地あり（少なくとも「マージ前チェックリスト」項目として必須）。

---

**識別子**: RC-29（GitHub id: 3220329813）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```

ご指摘の通り、マージ前に SendGrid 側でテンプレート作成し、定数を実 ID に差し替える予定です。現状はプレースホルダの 'TBD' のままなので、本番適用前に必ず置き換えます。送信失敗は catch で握りつぶし続けるのは意図どおり（キャンセル自体は成功させる）ですが、テンプレ未設定時はログ監視で検知する想定です。

```

**コメント要約**: マージ前にテンプレ ID を差し替える方針。送信失敗はキャンセル成功は維持。

**判断結果**: 👌 修正不要

**判断理由**: 指摘への方針表明であり新たなコード指摘ではない。リスクの本体は RC-28 の「未設定のまま本番に出さない」運用で、`catch` の設計思想はプロダクト判断として評価対象外に近い。

---

**識別子**: RC-30（GitHub id: 3220330824）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**レビューのコメント（原文）**:

```

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  カート注文があるイベントのキャンセルを拒否する**

`members.length` と `ordered` 注文だけを見ているため、`in_cart` 注文が残っているイベントをキャンセルできてしまいます。`cancelEvent` は注文を削除しない一方で、Stripe の `createStripeCheckoutSession` 側には `event_canceled` チェックがないため（`functions/default/src/stripe.ts` のステータス判定は期限・定員のみ）、参加者がキャンセル後でも既存カートから決済を進められる経路が残ります。少なくともキャンセル時に `in_cart` を含む注文存在チェックを入れて、参加者ありイベントを必ず弾く必要があります。

Useful? React with 👍 / 👎.

```

**コメント要約**: `hasOrderedOrders` は `ordered` のみで `in_cart` を見ていないため、カートのみの注文が残ったイベントが Phase 1 条件をすり抜けうる、とする指摘。Stripe 側にキャンセルチェックが無い点にも言及。

**判断結果**: 👌 修正不要

**判断理由**: プロダクト判断として **`in_cart` 注文が残っているイベントでも主催者によるイベントキャンセルは許容する**（Phase 1 の「参加者なし」判定は `members` と `ordered` にフォーカスする）。レビューが想定する「必ず `in_cart` まで弾け」は採用しない。Stripe 経路の扱いは別途方針があればそちらで管理すればよく、本指摘をブロッキングとしない。

---

**識別子**: RC-31（GitHub id: 3220371691）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**レビューのコメント（原文）**:

```

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  キャンセル時に未確定カート注文を失効させる**

`members` と `ordered` 注文だけを条件にキャンセル可否を判定しているため、`in_cart` の注文が残っているイベントでもキャンセルが通ってしまいます。`createStripeCheckoutSession` 側にはキャンセル状態チェックが無く（`functions/default/src/stripe.ts` のイベント取得〜締切/定員チェック部分）、既存の `order_ids` で決済セッションを作って支払いまで進めてしまえるため、キャンセル済みイベントに新規決済が発生します。キャンセル時に `in_cart` を拒否/無効化するか、Stripe 側にも `event.isCanceled()` ガードを追加してください。

Useful? React with 👍 / 👎.

```

**コメント要約**: RC-30 と同根。`in_cart` をキャンセル条件に含めるか Stripe でガードせよ、という指摘。

**判断結果**: 👌 修正不要

**判断理由**: RC-30 と同一のプロダクト判断（`in_cart` 残存でも主催者キャンセル可）。重複指摘のため実装タスクとはしない。

---

**識別子**: RC-32（GitHub id: 3219508146）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**レビューのコメント（原文）**:

```

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  キャンセル理由の上限超過時は確認画面へ進ませない**

入力ルールでは 1000 文字上限を表示している一方で、確認画面への遷移条件が 50 文字未満のみを弾く実装になっているため、1001文字以上でも確認・実行まで進めてしまいます。このケースではサーバー側で `invalid-argument` になり、ユーザーは最終送信時に失敗通知だけ受け取るため、フロントのバリデーション挙動と不一致になります。

Useful? React with 👍 / 👎.

```

**コメント要約**: 1000 文字超でも確認画面に進めるためサーバとフロントで不一致。

**判断結果**: 👌 修正不要

**判断理由**: 返信（RC-33 / RC-34）および実装により、キャンセル理由は文字数上限なしの仕様。指摘の前提（上限あり）が成立しない。RC-10 と同旨の論点（別コメント ID）。

---

**識別子**: RC-33（GitHub id: 3220325038）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```

仕様として `cancel_reason` の文字数上限を設けない方針に変更しました。クライアントは「その他」選択時のみ `trim` 後1文字以上を `canProceedToConfirm` / `proceedToConfirm` で担保し、サーバーはプリセット一致 or トリム後非空のみ検証しています。

```

**コメント要約**: 上限なし仕様への変更とクライアント・サーバの検証内容の説明。

**判断結果**: 👌 修正不要

**判断理由**: レビューへの仕様説明。RC-32 の判断根拠となる。

---

**識別子**: RC-34（GitHub id: 3220329867）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```

キャンセル理由は仕様として文字数上限を設けない方針に変更したため、1000文字超でも確認へ進める形です。サーバー側も上限チェックはしていません（プリセット一致または「その他」の trim 後非空のみ）。

```

**コメント要約**: RC-33 と同趣旨の補足。

**判断結果**: 👌 修正不要

**判断理由**: RC-33 と同様。
```

