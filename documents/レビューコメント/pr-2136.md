# PR #2136 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-30 15:20 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: feat/2090
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2136
- **REVIEW_REQUEST_SINCE**: 2026-06-30T06:08:14Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 #4840402104、Codex 接続案内ボイラープレート PRR_kwDOJXF1N88AAAABEgwCeA）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | PRR_kwDOJXF1N88AAAABEgvZog | 👌 修正不要 | 📌 スコープ内 | — | 👀 確認のみ | — | Copilot の PR 概要サマリ<br>具体修正指示なし。参照用 |
| [x] | RC-2 | 3496549550 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | confirmEmailRegistration の createUser 失敗を already-exists へ<br>計画 §6 エッジケースと一致。Functions のみ |
| [x] | RC-3 | 3496558378 | ✅ 対応済み | 📌 スコープ内 | 👤 UX, 🐛 実害 | 📋 仕様追加 | M | /login の SNS で未登録アカウントが作成される<br>#2090 完了条件と矛盾。router で isNewUser 拒否等 |
| [x] | RC-4 | 3496558382 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | login の /register リンクが full reload<br>redirectPath ループ。RouterLink 化 |
| [x] | RC-5 | 3496549529 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | pass-code の mode をランタイム検証<br>不正 state 時の Callable 誤呼び出し防止 |
| [x] | RC-6 | 3496549511 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | email 欠落時も pass-code が続行<br>v-if ガードまたは early return 必須 |
| [x] | RC-7 | 3496558385 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | in-app から /register 時に元 URL 未保持<br>inapp-login コピー先が / になる |
| [x] | RC-8 | 3510915491 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ, 🐛 実害 | 🔧 微修正 | S | signOut のみでは Auth 孤児が残り isNewUser バイパス<br>拒否時に user.delete() 等が必要 |
| [x] | RC-9 | 3510915501 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | in-app ガード後の afterEach が redirectPath を上書き<br>RC-7 修正が不完全 |
| [x] | RC-10 | 3510915505 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 📋 仕様追加 | M | /register で既存 SNS ユーザーも登録成功扱い<br>login 導線へ戻す分岐が必要 |
| [ ] | RC-11 | 3510915511 | 🟡 修正提案 | 📌 スコープ内 | 🐛 実害 | 📋 仕様追加 | M | hosting と functions のデプロイ順未保証<br>functions/not-found リスク。運用または CI で順序保証 |
| [x] | RC-12 | 3511514565 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | ログイン必須→/login 経由の in-app で redirect 未保存<br>コピー URL が /login になる。RC-7/9 追補 |
| [x] | RC-13 | 3511514572 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 📋 仕様追加 | M | /register 既存 SNS 拒否後も Auth セッション残存<br>ログイン済みガードで / 等へ。signOut 必要 |
| [x] | RC-14 | 3511514575 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 📋 仕様追加 | M | /register のメール衝突時 pass-code login へ<br>登録入口が既存メールログイン化。RC-10 追補 |
| [x] | RC-15 | 3511514581 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ, 🐛 実害 | 🔧 微修正 | S | user.delete 失敗時も signOut 続行<br>孤児残存で RC-8 バイパスしうる。RC-8 追補 |

---

**識別子**: RC-1（GitHub id: PRR_kwDOJXF1N88AAAABEgvZog）

**レビュワー**: Copilot

**指摘箇所**: （インライン指摘なし）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

## Pull request overview

PF版のメール認証で「未登録メールを /login 経由で暗黙作成してしまう」挙動を解消するため、ログインと新規登録を **別 Callable / 別 UI 導線** に分離し、関連する API 型・クライアント・Functions・ドキュメント・デプロイワークフローを一貫更新する PR です。

**Changes:**
- Functions: `requestEmailLogin/confirmEmailLogin` をログイン専用へ変更し、`requestEmailRegistration/confirmEmailRegistration` を新設
- common/base/user: Callable の型・クライアント・画面を更新し、`/login` と `/register` を分離（`/pass-code` は mode で呼び分け）
- CI/Docs: PF Functions デプロイ対象に registration Callable を追加、関連仕様書を更新、Functions のユニットテスト追加

**コメント要約**: PR 変更の概要サマリ。具体的な修正依頼は含まない。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 変更内容の要約のみで、コード修正を求める指摘ではない。

---

**識別子**: RC-2（GitHub id: 3496549550）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/user.ts:139`

**該当コード（レビュー時点の diff）**:

```diff
+  const user = await getAuth().createUser({ email, emailVerified: true })
+  const uid = user.uid
+  const [token] = await Promise.all([
+    getAuth().createCustomToken(uid),
+    deletePassCode(passCodeDocument.id),
+    saveUser(
+      new ShokujiiUser(uid, {
+        user_email: email,
+      }),
+    ),
+  ])
+  return { token }
+})
```

**レビュワーのコメント（原文）**:

[must] confirmEmailRegistration で `getAuth().createUser()` が `auth/email-already-exists` 等を返した場合、現状は HttpsError に変換されず内部エラー扱いになり得ます。`requestEmailRegistration` と同様に `already-exists` へマッピングして、クライアント側がハンドリングできるようにしてください（あわせて logger.warn で事象を記録すると追跡しやすいです）。

**コメント要約**: Auth のみ存在するメールで confirm 時に内部エラーになる可能性<br>already-exists へマッピングし UI 誘導を可能にする

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 実装計画 §6 エッジケースとして記載済み。request 側は拒否するが confirm の TOCTOU 競合では valid。FirebaseAuthError を catch して HttpsError already-exists に変換するのが妥当。6958f1ffb で対応済み。

---

**識別子**: RC-3（GitHub id: 3496558378）

**レビュワー**: Codex

**指摘箇所**: `user/src/pages/login.vue:78`

**該当コード（レビュー時点の diff）**:

```diff
-              <div v-html="$t('login.please_login_or_register_below')" />
+              <div>{{ $t('login.please_login_below') }}</div>
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  SNSログインで新規作成を防いでください**

このコミットで `/login` をログイン専用導線にしていますが、同じページの SNS ボタンは引き続き `handleLogin` から `signInByProviderService` を呼ぶだけなので、未登録の Google/Facebook/X アカウントでも Firebase Auth の通常の OAuth サインインとして新規ユーザーが作成され、その後 `updateProfileFromProviders` で Shokujii の user doc も作成されます。メールでは未登録を `not-found` にしている一方、SNS では `/login` から新規登録できてしまうため、OAuth リダイレクト後の `isNewUser` をログイン導線では拒否するなどのガードが必要です。

**コメント要約**: /login の SNS が未登録ユーザーを新規作成しうる<br>#2090 のログイン導線分離目的と矛盾。OAuth 後ガードが必要

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX, 🐛 実害

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: #2090 完了条件「PF版ログイン導線で暗黙作成されない」に対し、メールのみ分離で SNS は未対応。計画では SNS 退行なしとあるが、新規 SNS は /register へ誘導し /login では isNewUser を拒否する設計が必要。router/index.ts の OAuth 復帰処理で分岐するのが自然。46b0064f6 で対応済み。

---

**識別子**: RC-4（GitHub id: 3496558382）

**レビュワー**: Codex

**指摘箇所**: `user/src/pages/login.vue:156`

**該当コード（レビュー時点の diff）**:

```diff
+            <v-row justify="center" class="py-2 text-subtitle-2">
+              <div v-html="$t('login.link_to_register')" />
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  新規登録リンクは SPA 遷移にしてください**

ログイン必須ページから `/login` にリダイレクトされた後にこの通常の `<a href="/register">` を押すと、SPA 内遷移ではなくページ再読み込みになります。`sessionStorage` の `redirectPath` が残った状態で `/register` が初回表示され、今回 `router/index.ts` の初回ガードも `/register` を対象にしたため、元のログイン必須ページへ戻されて再び `/login` へ飛び、新規登録に進めません。`RouterLink` やクリックハンドラで SPA 遷移にするか、`/register` 初回表示では保存済みリダイレクトを消費しないようにしてください。

**コメント要約**: ja.ts の a href による /register リンクが full reload<br>redirectPath と初回ガードで登録導線に入れない実害あり

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘の再現経路は妥当。i18n の HTML リンクを RouterLink 化するか router.push に変更すれば解消。register 側 link_to_login も同様に確認推奨。46b0064f6 で対応済み。

---

**識別子**: RC-5（GitHub id: 3496549529）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/pass-code.vue:27`

**該当コード（レビュー時点の diff）**:

```diff
+const mode = (history.state?.mode as PassCodeMode | undefined) ?? 'login'
```

**レビュワーのコメント（原文）**:

[must] `mode` を `as PassCodeMode` でキャストすると、想定外の値が入ってもそのまま `'register'` 扱い等になり得て、Callable の呼び分けが壊れます。history.state から取り出す値はランタイムで `'login' | 'register'` を検証してフォールバックしてください。

**コメント要約**: mode の型キャストのみでランタイム検証なし<br>不正値で Callable 誤呼び出しのリスク

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 防御的コーディングとして妥当。mode === register のときのみ register、それ以外 login に正規化するヘルパーで十分。46b0064f6 で対応済み。

---

**識別子**: RC-6（GitHub id: 3496549511）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/pass-code.vue:32`

**該当コード（レビュー時点の diff）**:

```diff
 if (_email == null) {
   if (isLogin) {
     await router.push(getHomePath())
   } else {
-    await router.push(getLogin())
+    await router.push(mode === 'register' ? getRegister() : getLogin())
   }
 }
 const email = _email as string
```

**レビュワーのコメント（原文）**:

[must] history.state に email が無い場合に router.push していますが、この後も同コンポーネントの処理が継続し、`email` を前提にした watch / API 呼び出し / テンプレート描画へ進み得ます（ナビゲーション失敗時に特に顕在化）。`email` が無いときは以降のロジックを実行しない構造（例: email がある場合だけ watch/submit/reSend を定義、もしくはテンプレートを v-if でガード）にしてください。

**コメント要約**: email 欠落時に router.push 後も setup が続行<br>watch と template が undefined email を参照しうる

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 現行コードは `const email = _email as string` で強制キャストしており指摘どおり。v-if="email" でテンプレートガードするか、email 無し時は return 相当の構造にすべき。46b0064f6 で対応済み。

---

**識別子**: RC-7（GitHub id: 3496558385）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:104`

**該当コード（レビュー時点の diff）**:

```diff
-    if (to.path === '/login' && isInApp) {
+    if ((to.path === '/login' || to.path === '/register') && isInApp) {
       return {
         path: '/inapp-login',
         query: to.query,
       }
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  アプリ内ブラウザでも登録URLを保持してください**

`/register` をアプリ内ブラウザ用ガードに追加していますが、このリダイレクトでは元の `to.fullPath` を state/query に保持していません。`/inapp-login` 側は `getRedirectPath(false)` をコピーする作りで、初回に外部リンク等から `/register` を開いた場合は afterEach が保存する `from.fullPath`（多くは `/`）しか残らないため、ユーザーが Chrome/Safari へコピーしても新規登録画面ではなくホーム等に移動してしまいます。`/register` をブロックする場合は、コピー対象として元の `/register` を明示的に保存してください。

**コメント要約**: in-app ガードで /register へ飛ばす際 fullPath 未保存<br>inapp-login の URL コピーが /register にならない

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: /login ブロック時と同様に state または query で元パスを渡す必要がある。inapp-login.vue 側の対応もセットで検討。46b0064f6 で setRedirectPath(to.fullPath) を追加済み（RC-9 で afterEach 上書きの追補指摘あり）。

---

## 評価セッション

- **評価日時**: 2026-07-02 15:30 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: feat/2090
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2136
- **REVIEW_REQUEST_SINCE**: 2026-07-02T06:14:38Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 #4862745680、Codex 接続案内ボイラープレート）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-8 | 3510915491 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ, 🐛 実害 | 🔧 微修正 | S | signOut のみでは Auth 孤児が残り isNewUser バイパス<br>拒否時に user.delete() 等が必要 |
| [x] | RC-9 | 3510915501 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | in-app ガード後の afterEach が redirectPath を上書き<br>RC-7 修正が不完全 |
| [x] | RC-10 | 3510915505 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 📋 仕様追加 | M | /register で既存 SNS ユーザーも登録成功扱い<br>login 導線へ戻す分岐が必要 |
| [ ] | RC-11 | 3510915511 | 🟡 修正提案 | 📌 スコープ内 | 🐛 実害 | 📋 仕様追加 | M | hosting と functions のデプロイ順未保証<br>functions/not-found リスク。運用または CI で順序保証 |

---

**識別子**: RC-8（GitHub id: 3510915491）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:191`

**該当コード（レビュー時点の diff）**:

```diff
@@ -180,6 +185,19 @@ export const setupRouter = (router: Router) => {
         return getRedirectPath() ?? undefined
       }
 
+      if (to.path === '/login' && userCredential != null) {
+        const aui = getAdditionalUserInfo(userCredential)
+        if (aui?.isNewUser === true) {
+          await signOut(getAuth())
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  ログイン拒否時に作成済み Auth ユーザーを削除してください**

未登録ユーザーが /login から SNS 認証すると、Firebase Auth 側では新規ユーザーが作成された後にここへ到達しますが、`signOut()` だけだとその Auth ユーザーが残ります。次に同じ SNS で /login すると `isNewUser` が false になり、後続の `updateProfileFromProviders` が Firestore ユーザーを作成してしまうため、ログイン導線からの新規登録拒否をバイパスできます。拒否時はサインアウト前に作成直後の `userCredential.user` を削除する必要があります。

**コメント要約**: RC-3 対応の signOut だけでは Auth 孤児が残り、2 回目以降 isNewUser=false で暗黙登録をバイパスしうる<br>拒否時は user.delete() 等で Auth ユーザーを削除する必要あり

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ, 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘の再現経路は妥当。RC-3 実装の signOut のみでは #2090 完了条件を満たさない。OAuth 復帰直後は userCredential.user.delete() が可能なため、signOut 前に削除するのが自然。`authEntryGuards.ts` の `rejectNewUserOnLogin` を router / login.vue に適用済み。

---

**識別子**: RC-9（GitHub id: 3510915501）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:106`

**該当コード（レビュー時点の diff）**:

```diff
@@ -98,15 +102,16 @@ export const setupRouter = (router: Router) => {
   const isInApp = isInAppBrowser(navigator.userAgent)
   router.beforeEach((to) => {
-    if (to.path === '/login' && isInApp) {
+    if ((to.path === '/login' || to.path === '/register') && isInApp) {
+      setRedirectPath(to.fullPath)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  in-app 用の保存パスが上書きされないようにしてください**

in-app ブラウザで `/register` を直接開くケースでは、ここで `/register` を保存して `/inapp-login` へリダイレクトしますが、その直後の `afterEach` が最終遷移先 `/inapp-login` に対して `from.fullPath`（初期表示なら `/` など）を再保存するため、コピーされる URL が登録ページではなく元ページになります。`/inapp-login` へのリダイレクト時は `afterEach` で上書きしない、または state/query で元の `to.fullPath` を渡して保存するようにしてください。

**コメント要約**: RC-7 の setRedirectPath が afterEach で上書きされ in-app URL コピーが再び壊れる<br>inapp-login 遷移時は afterEach をスキップする等が必要

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: afterEach は `/inapp-login` を対象外にしているが、from が `/` のとき setRedirectPath(from.fullPath) が走る経路があり指摘は妥当。RC-7 対応の追補として afterEach 先頭で `to.path === '/inapp-login'` を return する修正を適用済み。

---

**識別子**: RC-10（GitHub id: 3510915505）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:130`

**該当コード（レビュー時点の diff）**:

```diff
@@ -122,7 +127,7 @@ export const setupRouter = (router: Router) => {
-    if (['/login', '/register/complete', '/profile'].includes(to.path)) {
+    if (['/login', '/register', '/register/complete', '/profile'].includes(to.path)) {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  /register の既存 SNS 認証を拒否してください**

`/register` の SNS リダイレクト復帰もこのブロックに入りますが、`getAdditionalUserInfo(userCredential)?.isNewUser` が false の既存アカウントでもそのまま `updateProfileFromProviders` に進むため、新規登録専用入口が既存 SNS アカウントのログインとして機能します。ログイン/新規登録を分離するなら、`to.path === '/register'` で既存ユーザーだった場合は警告してログイン導線へ戻すなど、登録成功扱いにしない分岐が必要です。

**コメント要約**: /register から既存 SNS ユーザーがログインできてしまう<br>入口分離の観点では /login へ誘導する分岐が必要

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: #2090 の入口分離意図と整合。既存ユーザーの /register 利用はログイン相当のため、警告して /login へ戻す設計が自然。メール登録の already-exists 拒否と対称。router 初回 OAuth 復帰と register.vue ポップアップ経路に分岐を追加済み。

---

**識別子**: RC-11（GitHub id: 3510915511）

**レビュワー**: Codex

**指摘箇所**: `.github/workflows/deploy_functions.yml:83`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+              echo "args=--force --only functions:...functions:requestEmailRegistration,functions:confirmEmailRegistration,..." >> "$GITHUB_OUTPUT"
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  新 Callable を hosting より先に出すよう調整してください**

この行で `requestEmailRegistration` / `confirmEmailRegistration` を PF Functions のデプロイ対象に追加していますが、確認した `.github/workflows/deploy_user.yml` は `user/**` / `base/**` push で別 workflow として走り、この `deploy_functions.yml` との `needs` や順序制御がありません。このコミットは user 側も同時に新 Callable を呼び始めるため、development/production への push で hosting が先に公開されたり Functions job だけ失敗したりすると、メール新規登録が `functions/not-found` で壊れます。Functions 先行デプロイか互換期間を置くなど、公開順序を保証してください。

**コメント要約**: deploy_user と deploy_functions に順序制御がなく hosting 先行で Callable 未デプロイリスク<br>CI で functions 先行または運用ルールの明文化が必要

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: 指摘は事実。現状は手動で functions+user 同時デプロイで緩和。development マージ時の CI 順序保証は未実装のため、ドキュメント化または workflow 連携の検討が妥当。

---

## 評価セッション

- **評価日時**: 2026-07-02 17:25 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` manual）
- **ブランチ名**: feat/2090
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2136（CLOSED。作業正本: #2147）
- **REVIEW_REQUEST_SINCE**: 2026-07-02T08:19:57Z（第3回 Codex レビュー分）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 1（引き継ぎコメント #4863661028）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-12 | 3511514565 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | ログイン必須→/login 経由の in-app で redirect 未保存<br>コピー URL が /login になる。RC-7/9 追補 |
| [x] | RC-13 | 3511514572 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 📋 仕様追加 | M | /register 既存 SNS 拒否後も Auth セッション残存<br>ログイン済みガードで / 等へ。signOut 必要 |
| [x] | RC-14 | 3511514575 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 📋 仕様追加 | M | /register のメール衝突時 pass-code login へ<br>登録入口が既存メールログイン化。RC-10 追補 |
| [x] | RC-15 | 3511514581 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ, 🐛 実害 | 🔧 微修正 | S | user.delete 失敗時も signOut 続行<br>孤児残存で RC-8 バイパスしうる。RC-8 追補 |

---

**識別子**: RC-12（GitHub id: 3511514565）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:110`

**該当コード（レビュー時点の diff）**:

```diff
@@ -98,15 +106,16 @@ export const setupRouter = (router: Router) => {
   const isInApp = isInAppBrowser(navigator.userAgent)
   router.beforeEach((to) => {
-    if (to.path === '/login' && isInApp) {
+    if ((to.path === '/login' || to.path === '/register') && isInApp) {
+      setRedirectPath(to.fullPath)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  in-appコピーURLに元の遷移先を保持してください**

アプリ内ブラウザで未ログインのユーザーが招待URLなどログイン必須ページを開くと、後段の認証ガードは `/login` へ `state.redirect` 付きで飛ばしますが、この in-app ガードが `to.fullPath`（つまり `/login`）を保存し、さらに `/inapp-login` の afterEach はスキップされるため、コピーされるURLが元の招待/管理ページではなくログイン画面になります。ログイン必須ページからのリダイレクト時は元の `redirect` を保存する必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: ログイン必須ページ→`/login`（state.redirect 付き）→in-app ガードで `to.fullPath` のみ保存<br>招待 URL 等が失われコピー先が `/login` になる。RC-7/9 の未カバー経路

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘の再現経路は妥当。RC-9 の afterEach skip 後も、in-app beforeEach の `setRedirectPath(to.fullPath)` が `/login` 固定になるケースが残る。`history.state?.redirect ?? to.fullPath` で保存すれば解消。

**対応**: in-app beforeEach で `setRedirectPath((history.state?.redirect as string | undefined) ?? to.fullPath)` に変更（`user/src/router/index.ts`）。

---

**識別子**: RC-13（GitHub id: 3511514572）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:213`

**該当コード（レビュー時点の diff）**:

```diff
+      if (to.path === '/register' && userCredential != null) {
+        const aui = getAdditionalUserInfo(userCredential)
+        if (aui?.isNewUser === false) {
+          const i18n = getI18n()
+          window.alert(
+            i18n.global.t('register.already_registered'),
+          )
+          return getRedirectPath() ?? { path: '/login', query: to.query }
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  登録画面の既存SNS認証ではサインアウトしてください**

既存SNSアカウントで `/register` のOAuth復帰に入ると、この新しい分岐は警告後にリダイレクト先を返すだけで、`handleRedirect` により作成済みのFirebase Authセッションを残します。そのため保存済みリダイレクトがログイン必須ページならそのまま入れてしまい、保存先が無い場合も `/login` へ戻しても後段のログイン済みガードで `/` に送られるため、登録入口が依然として既存SNSログインとして機能します。既存コメントへの追加根拠は、修正後の分岐にも `signOut` が無い点です。

Useful? React with 👍 / 👎.

**コメント要約**: RC-10 の alert+redirect だけでは OAuth 後の Auth セッションが残る<br>ログイン済み扱いで登録入口バイパス。拒否時は signOut が必要（delete は不要）

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: #2090 入口分離の観点で RC-10 対応は不完全。既存ユーザーは正当な Auth のため delete せず signOut のみ。`/login` へ戻す前に signOut し、getRedirectPath 消費タイミングも整理が必要。

**対応**: `rejectExistingUserOnRegister()` を追加し、router の RC-10 分岐と `register/index.vue` の DEV ポップアップ経路で alert 後に signOut。

---

**識別子**: RC-14（GitHub id: 3511514575）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:151`

**該当コード（レビュー時点の diff）**:

```diff
             return {
               path: '/pass-code',
-              state: { email },
+              state: { email, mode: 'login' },
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  登録時の既存メール衝突をログインに進めないでください**

`/register` からOAuth復帰して `auth/account-exists-with-different-credential` になり、既存アカウントがメールOTP/カスタムトークン系で `existingProviderId` が取れない場合、この分岐はそのまま `/pass-code` を `mode: 'login'` で開くため、新規登録専用入口が既存メールアカウントのログイン導線になります。このブロックは `/register` の既存ユーザー拒否分岐より前に return するので、登録画面では `already_registered` を出してログイン画面へ戻すなど、ログイン確定に進めない分岐が必要です。

Useful? React with 👍 / 👎.

**コメント要約**: /register 起点の credential 衝突で pass-code login へ直行<br>既存メールのログイン導線化。to.path === '/register' なら拒否して /login へ

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: 指摘は妥当。account-exists-with-different-credential ハンドラは login/register 共通のため、/register 復帰時は pass-code login ではなく already_registered 表示+login 誘導が #2090 と整合。RC-10 の OAuth isNewUser 拒否と対称。

**対応**: `to.path === '/register'` 時は pass-code / pid リンクへ進めず `register.already_registered` を alert して `/login` へリダイレクト。

---

**識別子**: RC-15（GitHub id: 3511514581）

**レビュワー**: Codex

**指摘箇所**: `user/src/router/authEntryGuards.ts:11`

**該当コード（レビュー時点の diff）**:

```diff
+export async function rejectNewUserOnLogin(userCredential: UserCredential): Promise<void> {
+  try {
+    await userCredential.user.delete()
+  } catch (err) {
+    console.error('Failed to delete rejected Auth user:', err)
+  }
+  await signOut(getAuth())
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  削除失敗時は拒否を成功扱いしないでください**

`user.delete()` が一時的な通信失敗などで拒否された場合、この helper はエラーを握りつぶして `signOut` まで進むため、Auth 側に孤児ユーザーが残ります。その状態で同じSNSを `/login` から再試行すると `isNewUser` が false になり、後続の `updateProfileFromProviders` でShokujiiユーザーを作れてしまうので、削除失敗時は拒否フローを中断するかサーバー側削除にフォールバックしてください。新しい根拠は、追加された helper の `catch` が削除失敗をログ出力だけで成功扱いしている点です。

Useful? React with 👍 / 👎.

**コメント要約**: rejectNewUserOnLogin で delete 失敗時も signOut して拒否完了扱い<br>RC-8 と同種の孤児残存。失敗時は throw または呼び出し側で中断

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ, 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-8 の通常経路は delete+signOut で改善済みだが、delete 失敗時の catch で signOut 続行は RC-8 バイパスを再開しうる。helper を boolean 返却または throw にし、呼び出し側で /register 誘導を止めるのが妥当。Admin SDK フォールバックはスコープ外（Callable 新設は M 以上）。

**対応**: `rejectNewUserOnLogin` の catch を削除し delete 失敗時は throw。router / `login.vue` で catch して login_fail 表示、`/register` へ進めない。Vitest で delete 失敗時 signOut 未呼び出しを検証。

---
