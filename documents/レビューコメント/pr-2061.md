# PR #2061 レビューコメント評価

bot と legacy Functions を default へ完全移行（#2060）

## RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: なし・PR レビューサマリ）— 👌 修正不要（Copilot Pull request overview。変更サマリのみ）
- [x] **RC-2**（GitHub id: 3361228387）— ✅ 対応済み（21_bot_legacy移行.md §5.10 に Gen1 明示削除手順を追記。backupFirestore リネーム注記を追加）
- [x] **RC-3**（GitHub id: 3361212478）— ✅ 対応済み（sendCommunityManagerRoleChangeMails を allSettled + 失敗件数 warn ログに変更）
- [x] **RC-4**（GitHub id: 3361228389）— ✅ 対応済み（RC-3 と同一箇所。allSettled で全件待機）
- [x] **RC-5**（GitHub id: 3361228395）— ✅ 対応済み（ジョブ失敗は throw、イベント/bot 失敗は warn ログ）
- [x] **RC-6**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（EventLogDbSchema 準拠のガード。通常 updateEvent 経路では updated_by あり）
- [x] **RC-7**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（event_cover_url 削除済み。storage パス経由が現行データモデルの正）
- [x] **RC-8**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（member_orders 移行後は event.members が正本。legacy orders 参照は陳腐化）
- [x] **RC-9**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（convertToDuration による H:mm 表示差のみ。機能欠落なし）
- [x] **RC-10**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（recalc 失敗時 rethrow）
- [ ] **RC-11**（GitHub id: なし・エージェントレビュー）— 🟡 修正提案（slackbot fetchInstallation の `as Installation`）
- [ ] **RC-12**（GitHub id: なし・エージェントレビュー）— 🟡 修正提案（setSlackInstallation の installation キャスト）
- [x] **RC-13**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（lineEventBroadcast を DateTime.now().toMillis() に統一）
- [x] **RC-14**（GitHub id: 4645238710）— ✅ 対応済み（backupFirestore を 0 2 * * * に戻済み）
- [x] **RC-15**（GitHub id: 3370719803）— 👌 修正不要（common ビルド成功。typeof は型位置のみ）
- [x] **RC-16**（GitHub id: 3370715816）— ✅ 対応済み（backupFirestore 2:00 JST）
- [x] **RC-17**（GitHub id: 3370719830）— ✅ 対応済み（backupFirestore 2:00 JST）
- [x] **RC-18**（GitHub id: 3366569538）— ✅ 対応済み（recalc 失敗時 rethrow）
- [x] **RC-19**（GitHub id: 3370719837）— ✅ 対応済み（RC-18 と同一対応）
- [ ] **RC-20**（GitHub id: 3366569535）— 🟡 修正提案（ログ書き込みで delete sentinel 生成リスク）
- [x] **RC-21**（GitHub id: 3366569540）— ✅ 対応済み（sendCommunityBotsMessageOrThrow）

---

## 評価セッション（2026-06-05 17:07 JST・review-comments-evaluate）

- 評価日時: 2026-06-05 17:07 JST
- ブランチ名: dev/2060
- PR: https://github.com/nijuniinc/bokudeli-event-new/pull/2061 #2061
- Outdated 除外件数: 0 件
- レビュー非該当スキップ件数: 3 件
  - GitHub id: 4629293865（nijuni-yasu・レビュー依頼の定型文）
  - GitHub id: 4629324846（Copilot・対応了承の返信のみ。具体指摘なし）
  - GitHub id: 4629325863（chatgpt-codex-connector・Codex 接続案内ボイラープレート）

### RC 別対応チェックリスト（本セッション）

- [x] **RC-1**（GitHub id: なし・PR レビューサマリ）— 👌 修正不要
- [x] **RC-2**（GitHub id: 3361228387）— ✅ 対応済み
- [x] **RC-3**（GitHub id: 3361212478）— ✅ 対応済み
- [x] **RC-4**（GitHub id: 3361228389）— ✅ 対応済み
- [x] **RC-5**（GitHub id: 3361228395）— ✅ 対応済み

---

### RC-1

**識別子**: RC-1（GitHub id: なし・PR レビューサマリ）

**レビュワー**: Copilot

**指摘箇所**: PR トップレベル（Pull request overview）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

> ## Pull request overview
>
> legacy / shokujii-slackbot / shokujii-linebot に残っていた Firebase Functions を `functions/default`（TypeScript + v2 + store 経由）へ統合し、最終的に旧 3 codebase とディレクトリを撤去して Functions デプロイ対象を `default` のみに統一する PR です。
>
> Copilot reviewed 71 out of 81 changed files in this pull request and generated 1 comment.

**コメント要約**: PR 全体の変更概要サマリ。具体的な修正指摘は含まない。

**判断結果**: 👌 修正不要

**判断理由**: 変更サマリのみで、コードに対する具体的な指摘を含まない。対応不要。

---

### RC-2

**識別子**: RC-2（GitHub id: 3361228387）

**レビュワー**: Codex

**指摘箇所**: `firebase.json:60`

**該当コード（レビュー時点の diff）**:

```diff
@@ -57,37 +57,6 @@
         "firebase-debug.log",
         "firebase-debug.*.log"
       ]
-    },
-    {
-      "source": "functions/legacy",
-      "codebase": "legacy",
-      "ignore": [
-        "node_modules",
-        ".git",
-        "firebase-debug.log",
-        "firebase-debug.*.log",
-        "*.local"
-      ]
-    },
-    {
-      "source": "functions/shokujii-slackbot",
-      "codebase": "shokujii-slackbot",
-      "ignore": [
-        "node_modules",
-        ".git",
-        "firebase-debug.log",
-        "firebase-debug.*.log"
-      ]
-    },
-    {
-      "source": "functions/shokujii-linebot",
-      "codebase": "shokujii-linebot",
-      "ignore": [
-        "node_modules",
-        ".git",
-        "firebase-debug.log",
-        "firebase-debug.*.log"
-      ]
     }
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  旧 codebase の関数を明示削除してください**
>
> production/development に `legacy` / `shokujii-slackbot` / `shokujii-linebot` codebase が既にデプロイ済みの場合、ここで `firebase.json` から外すだけでは旧関数が削除されず、新しい default 側の `log_event_status`・Slack/LINE 通知・定期バックアップ等と並行稼働するリスクがあります。Firebase の codebase annotation はデプロイ時に他 codebase の関数を無視する挙動で、公式ドキュメントでも別 codebase の関数を「ignores」すると説明されています（https://firebase.google.com/docs/functions/organize-functions#organize_functions_in_codebases）。移行 PR では `firebase functions:delete --region asia-northeast1 --force ...` 等の明示削除手順をワークフロー/手順書に含めてください。
>
> Useful? React with 👍 / 👎.

**コメント要約**: firebase.json から codebase を削除するだけでは、既にデプロイ済みの旧 codebase の関数は GCP 上から削除されず、default 側と並行稼働する。明示的な削除手順を runbook に含めるべき。

**判断結果**: 🚨 必須修正

**判断理由**: 指摘は正しい。Firebase は firebase.json に存在しない codebase の関数をデプロイ時に無視（削除しない）するため、本 PR のように codebase エントリごと削除した状態を直接デプロイすると、既存環境の旧関数が孤立して残る。関数名は維持しているため、最悪は同名関数の二重稼働やデプロイ衝突を招く。移行計画 21_bot_legacy移行.md 5.9 は「Phase 2 で旧 codebase を空にしてデプロイ（削除）→ Phase 3 で codebase 撤去」という順序を前提としているが、本 PR は Phase 2 と Phase 3 を一括で含むため、Phase 2 を先行デプロイ済みの環境以外では旧関数が消えない。`firebase functions:delete --region asia-northeast1 --force log_event_status scheduled_firestore_export ...` 等の明示削除、または Phase 2 先行デプロイの確認を runbook（21_bot_legacy移行.md 8.6 等）に明記する必要がある。なお legacy:send_email は本 PR で廃止のため、明示削除対象に含める。

**対応**: 21_bot_legacy移行.md §5.10 に Gen1 明示削除手順を追記。backupFirestore リネーム後の旧名削除・孤立 Gen2 確認手順を追加。

---

### RC-3

**識別子**: RC-3（GitHub id: 3361212478）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/communityMail.ts:103`

**該当コード（レビュー時点の diff）**:

```diff
@@ -61,6 +64,45 @@ async function sendCommunityAddedMailToOrganizer(templateId: string, community:
   )
 }
 
+/**
+ * コミュニティマネージャーの追加・削除通知メール。
+ * legacy `on_write_community_members` の sendMessage 相当。
+ */
+export async function sendCommunityManagerRoleChangeMails(params: {
+  communityAccount: string
+  communityName: string
+  addedManagerIds: string[]
+  removedManagerIds: string[]
+}): Promise<void> {
+  const { communityAccount, communityName, addedManagerIds, removedManagerIds } = params
+  const community_url = getCommunityUrl(communityAccount)
+  const community_manage_url = getManageCommunityUrl(communityAccount)
+
+  const sendToManager = async (templateId: string, managerId: string): Promise<void> => {
+    const user = await getUser(managerId, true)
+    const to = user?.user_email
+    if (to == null || to === '') {
+      return
+    }
+    await sgMail.send({
+      to,
+      from: DEFAULT_FROM,
+      templateId,
+      dynamicTemplateData: {
+        user_name: user?.user_name ?? '',
+        community_name: communityName,
+        community_url,
+        community_manage_url,
+      },
+    })
+  }
+
+  await Promise.all([
+    ...addedManagerIds.map((managerId) => sendToManager(COMMUNITY_MANAGER_ADDED_TEMPLATE_ID, managerId)),
+    ...removedManagerIds.map((managerId) => sendToManager(COMMUNITY_MANAGER_REMOVED_TEMPLATE_ID, managerId)),
+  ])
```

**レビュワーのコメント（原文）**:

> [must] ここが `Promise.all` だと、1件でも `sendToManager` が reject した時点で await が例外で中断し、残りの送信 Promise を待たないまま呼び出し元へ戻ります（Cloud Functions では未awaitの処理が途中で打ち切られ、マネージャー通知が一部送られない可能性があります）。全送信の完了を待ったうえで失敗件数を集計し、必要なら例外化する形にしてください。

**コメント要約**: マネージャー通知メールの一括送信が Promise.all のため、1件失敗で残りの送信が中断する。allSettled で全件待機し失敗件数を集計すべき。

**判断結果**: 🚨 必須修正

**判断理由**: プロジェクト規約に明確に反する。shokujii-code-review.md「Firebase Functions」チェックリスト（メールの一括送信に Promise.all を使っていないか）および NG/OK 例（allSettled + 失敗件数ログ）のとおり、複数宛先のメール一括送信は Promise.allSettled で全件の完了を待ち、失敗件数を createModuleLogger でログ化する。Cloud Functions では呼び出し元へ戻ると未完了の Promise が打ち切られ得るため、一部マネージャーへの通知欠落につながる。allSettled へ修正する。

**対応**: communityMail.ts の sendCommunityManagerRoleChangeMails を Promise.allSettled + logger.warn に変更。

---

### RC-4

**識別子**: RC-4（GitHub id: 3361228389）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/communityMail.ts:103`

**該当コード（レビュー時点の diff）**:

```diff
@@ -61,6 +64,45 @@ async function sendCommunityAddedMailToOrganizer(templateId: string, community:
   )
 }
 
+export async function sendCommunityManagerRoleChangeMails(params: {
+  communityAccount: string
+  communityName: string
+  addedManagerIds: string[]
+  removedManagerIds: string[]
+}): Promise<void> {
+  ...
+  await Promise.all([
+    ...addedManagerIds.map((managerId) => sendToManager(COMMUNITY_MANAGER_ADDED_TEMPLATE_ID, managerId)),
+    ...removedManagerIds.map((managerId) => sendToManager(COMMUNITY_MANAGER_REMOVED_TEMPLATE_ID, managerId)),
+  ])
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Promise.allSettled でマネージャー通知を待機してください**
>
> 複数マネージャーの追加・削除が同時に発生したとき、1件でも SendGrid 送信が失敗すると `Promise.all` が即座に reject し、残りの送信完了を待たないまま `onCommunityMemberWritten` 側の catch に抜けます。このトリガーはそこで例外をログ化して継続するため、同じ変更に対する未送信メールがリトライされず、追加/削除通知が一部のマネージャーに届かない可能性があります。複数メール送信は `Promise.allSettled` で全件の完了を待ち、失敗件数をログに残す形にしてください。
>
> Useful? React with 👍 / 👎.

**コメント要約**: RC-3 と同一。マネージャー通知の Promise.all を allSettled に変え、全件待機と失敗件数ログを行うべき。

**判断結果**: 🚨 必須修正

**判断理由**: RC-3 と同一箇所・同一趣旨の指摘。プロジェクト規約（メール一括送信は allSettled + 失敗集計ログ）に沿って RC-3 の修正で同時に解消する。onCommunityMemberWritten 側で例外をログ化して継続する設計のため、Promise.all のままだと未送信メールがリトライされず欠落する点も妥当。

**対応**: RC-3 と同一修正で解消。

---

### RC-5

**識別子**: RC-5（GitHub id: 3361228395）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/slackEventNotification.ts:81`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+/** legacy eventNotification から移行。export 名は維持する。 */
+export const eventNotification = onSchedule(
+  {
+    schedule: '*/1 * * * *',
+    timeZone: 'Asia/Tokyo',
+    region: 'asia-northeast1',
+  },
+  async (event) => {
+    const scheduleTime = event.scheduleTime
+    const now = scheduleTime != null ? DateTime.fromISO(scheduleTime).toMillis() : DateTime.now().toMillis()
+    const end = Math.trunc(now / 60 / 1000) * 60 * 1000
+    const start = end - 60 * 1000
+
+    logger.info('eventNotification tick', { start, end })
+
+    await Promise.allSettled([
+      notificationOrder(start, end, 3),
+      notificationOrder(start, end, 1),
+      notificationOrder(start, end, 0),
+      notificationEventStart(start, end, 60),
+      notificationEventEnd(start, end),
+    ])
+  },
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Slack 通知ジョブの失敗を握りつぶさないでください**
>
> このスケジュールは1分単位の期限・開始・終了時刻に一致したイベントだけを通知するため、Firestore クエリや通知処理が一時的に失敗した場合でも `Promise.allSettled` で全体が成功扱いになると、その時間窓の Slack 通知は再試行されず欠落します。個別イベントの失敗を継続したい場合でも、settled 結果を確認して失敗件数をログ化し、少なくともジョブ単位の取得失敗は throw して Scheduler の失敗として検知・再実行できるようにしてください。
>
> Useful? React with 👍 / 👎.

**コメント要約**: eventNotification は1分窓に一致したイベントのみ通知するため、allSettled で失敗を握りつぶすとその窓の通知が恒久的に欠落する。失敗件数のログ化と、ジョブ単位の取得失敗は throw して Scheduler で再実行できるようにすべき。

**判断結果**: 🟡 修正提案

**判断理由**: 観測性・信頼性の観点で妥当な提案。eventNotification は毎分実行だが、通知対象は「その1分窓に一致したイベント」に限定されるため、失敗した窓は次回実行では再通知されず恒久欠落になり得る。現状は notifyEvents・トップレベルとも Promise.allSettled で結果を一切確認せず、失敗件数のログも無い。Slack 一括送信に allSettled を使う方針自体は sendCommunityBotsMessage と整合し許容範囲だが、settled 結果から失敗件数を createModuleLogger でログ化し、Firestore 取得失敗などジョブ単位の失敗は throw して Scheduler の再実行・検知に乗せる改善が望ましい。マージ必須ではないが、通知欠落の検知性向上のため対応を推奨する。

**対応**: slackEventNotification.ts でジョブ失敗時 throw・イベント失敗 warn。slackMessage.ts の sendCommunityBotsMessage に bot 失敗 warn を追加。

---

## 評価セッション（2026-06-05 17:30 JST・shokujii-code-review）

- 評価日時: 2026-06-05 17:30 JST
- ブランチ名: dev/2060
- PR: https://github.com/nijuniinc/bokudeli-event-new/pull/2061 #2061
- レビュー種別: legacy / bot → default 移行の仕様一致確認（merge-base `b91dd05a` の元実装と default 移行版を全 10 関数で突き合わせ）
- Outdated 除外件数: 該当なし
- レビュー非該当スキップ件数: 該当なし

### 仕様一致確認サマリ（10 関数）

| 関数 | 判定 |
|------|------|
| flyer | 👌 仕様一致（表紙 storage パス経由は意図的・RC-7） |
| scheduled_firestore_export | 👌 仕様一致 |
| log_event_status | 👌 仕様一致（updated_by 空スキップはスキーマ準拠・RC-6） |
| on_write_community_members → onCommunityMemberWritten | 👌 仕様一致（メール・集計を統合、二重発火なし） |
| send_email（廃止） | 👌 廃止妥当（フロント未使用を確認） |
| slackbot | 👌 仕様一致 |
| eventNotification | 👌 仕様一致（legacy の未 await バグを改善） |
| orderNotification | 👌 仕様一致 |
| broadcast_event_message_request | 👌 仕様一致（満席判定・日時表記の差は意図的・RC-8 / RC-9） |
| line_event_information | 👌 仕様一致 |

**総評**: 🚨 必須修正はなし。RC-6〜RC-9 は意図的変更として 👌 修正不要と判断。

### RC 別対応チェックリスト（本セッション）

- [x] **RC-6**（GitHub id: なし・エージェントレビュー）— 👌 修正不要
- [x] **RC-7**（GitHub id: なし・エージェントレビュー）— 👌 修正不要
- [x] **RC-8**（GitHub id: なし・エージェントレビュー）— 👌 修正不要
- [x] **RC-9**（GitHub id: なし・エージェントレビュー）— 👌 修正不要

---

### RC-6

**識別子**: RC-6（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/eventLog.ts:59`

**該当コード（レビュー時点の diff）**:

```diff
+    const updatedBy = typeof after?.updated_by === 'string' ? after.updated_by : ''
+    if (updatedBy === '') {
+      logger.warn('updated_by is missing, skip log write', { communityId, eventId })
+      return
+    }
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: `log_event_status` で `updated_by` が空のときログ書き込みをスキップするガードが追加されている。legacy `event-logging.js` は `after['updated_by']` を無条件で `write_log` に渡していた。`EventLogDbSchema` が `updated_by: nonempty` を要求するため移行版のガードは妥当だが、`updated_by` なしの更新は監査ログに残らない点で legacy と挙動差がある。通常 `updateEvent` が必ず `updated_by` を入れるため実害は低いが、意図確認を推奨する。

**コメント要約**: updated_by 空のときログを書かない挙動は legacy と異なる。スキーマ制約上は妥当だが意図確認が必要。

**判断結果**: 👌 修正不要

**判断理由**: legacy は updated_by が undefined でも `logs` に add していた（Firestore 上は不正データになり得る）。移行版は EventLog の Zod バリデーションに合わせてスキップする設計で、通常フローでは問題にならない。監査要件として「updated_by なしでも差分を残す」必要があるかプロダクト側で確認する。コード修正は必須ではない。

**対応**: 意図確認の結果、スキーマ準拠のガードとして現状維持。コード変更なし。

---

### RC-7

**識別子**: RC-7（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/flyer.ts:171`

**該当コード（レビュー時点の diff）**:

```diff
+    const eventDesc = convertHtmlToPlaneText(event.event_desc ?? '')
+    const eventCoverUrl = convertStoragePathToURL(getEventCoverStoragePath(event.community_id, event.id))
+    const eventCoverUrlBase64Image = await getImageAsBase64(eventCoverUrl)
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: flyer のイベント表紙画像取得元が変更されている。legacy `flyer.js` は `eventData.event_cover_url` フィールドを直読みしていたが、移行版は `convertStoragePathToURL(getEventCoverStoragePath(community_id, id))` で storage パスから URL を生成する。`common/src/schemas/Event.ts` に `event_cover_url` が存在しない（スキーマから削除済み）ため必要かつ意図的な変更と判断できるが、移行計画上の「仕様不変」確認として記録する。

**コメント要約**: 表紙画像の取得元が event フィールド直読みから storage パス生成へ変更。スキーマ削除に伴う意図的変更。

**判断結果**: 👌 修正不要

**判断理由**: Event スキーマから `event_cover_url` が削除されているため、storage パス経由は現行データモデルでは正しい取得方法。legacy 互換のためフィールド直読みに戻す必要はない。デプロイ後の PDF 表紙表示の疎通確認を推奨する。

**対応**: 意図的変更として現状維持。コード変更なし。

---

### RC-8

**識別子**: RC-8（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/utils/lineEventBroadcast.ts:33`

**該当コード（レビュー時点の diff）**:

```diff
+  for (const event of sorted) {
+    if (event.members.length >= event.event_max_people) {
+      continue
+    }
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: LINE ブロードキャストの満席判定のデータソースが変更されている。legacy `shokujii-linebot/index.js` はイベントの `orders` サブコレクションからユニーク `user_id` 数を数え `>= event_max_people` で除外していた。移行版 `lineEventBroadcast.ts` は `event.members.length >= event.event_max_people` を使用する（コメントに「満席判定は members 正本を使用する」とある）。legacy の `orders` は現スキーマでは実質空の旧パスの可能性が高く、移行版の方が正しい挙動の見込みだが、移行計画では linebot を「単純移行」としているため product 意図の最終確認を推奨する。

**コメント要約**: 満席除外が orders サブコレクション集計から event.members.length へ変更。意図的改善の可能性が高いが legacy 1:1 ではない。

**判断結果**: 👌 修正不要

**判断理由**: `member_orders` ベースの参加管理では `event.members` が正本であり、legacy の `orders` 参照は陳腐化している可能性が高い。ただしブロードキャスト対象イベントの選定ロジックが変わるため、ステージングで満席・空席イベントを含む疎通確認を推奨する。`event.members` ベースで確定としてよいかプロダクト確認が望ましい。

**対応**: 意図確認の結果、members 正本として現状維持。コード変更なし。

---

### RC-9

**識別子**: RC-9（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/utils/lineEventBroadcast.ts:43`

**該当コード（レビュー時点の diff）**:

```diff
+        event_datetime: convertToDuration(event.event_start_datetime, event.event_end_datetime),
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: LINE ブロードキャストの日時表記が legacy と軽微に異なる。legacy は date-fns の `yyyy/MM/dd (eee) HH:mm`（時の 0 埋めあり、例 `09:00`）と `convertToJapan`（+9h 手動加算）を使用していた。移行版は `common` の `convertToDuration`（luxon、`yyyy/MM/dd (ccc) H:mm`、時の 0 埋めなし、例 `9:00`）を使用する。タイムゾーンはいずれも Asia/Tokyo 相当だが、表示フォーマットが変わる。

**コメント要約**: 日時表示が `HH:mm` から `H:mm` へ変化。曜日表記も `eee` から `ccc` へ（実質同等）。

**判断結果**: 👌 修正不要

**判断理由**: ユーザー向け表示の軽微な差であり、機能上の欠落ではない。legacy 互換を厳密に求める場合は `convertToDuration` のフォーマットを `HH:mm` に合わせるか、LINE 専用フォーマットを定義する。マージ必須ではない。

**対応**: common 統一の convertToDuration を維持。表示差のみでコード変更なし。

---

## 評価セッション（2026-06-06 17:14 JST・shokujii-code-review）

- 評価日時: 2026-06-06 17:14 JST
- ブランチ名: dev/2060
- PR: https://github.com/nijuniinc/bokudeli-event-new/pull/2061 #2061
- レビュー種別: 移行差分全体の再レビュー（RC-1〜RC-9 対応済み前提）
- Outdated 除外件数: 該当なし
- レビュー非該当スキップ件数: 該当なし

**総評**: 🚨 必須修正はなし。RC-2〜RC-5（Copilot/Codex）は対応済み。RC-6〜RC-9 は意図的変更として 👌。本セッションで新規 🟡 4 件（RC-10〜RC-13）。

### RC 別対応チェックリスト（本セッション）

- [x] **RC-10**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み
- [ ] **RC-11**（GitHub id: なし・エージェントレビュー）— 🟡 修正提案
- [ ] **RC-12**（GitHub id: なし・エージェントレビュー）— 🟡 修正提案
- [x] **RC-13**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み

---

### RC-10

**識別子**: RC-10（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/communityMemberCountsTrigger.ts:40`

**該当コード（レビュー時点の diff）**:

```diff
+    } catch (error) {
+      logger.error('recalcCommunityMembers failed in onCommunityMemberWritten', {
+        error,
+        communityId,
+        userId,
+      })
+    }
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: `onCommunityMemberWritten` で `recalcCommunityMembers` の失敗を catch してログのみ出力し、再 throw していない。legacy `on_write_community_members` は Transaction 失敗時に Function 全体が失敗し Firebase がリトライしていた。移行版は `community_num_members` / `members` / `managers` の更新とマネージャーメールがスキップされたまま Trigger が成功終了し、`recountUserProfileCounts` のみ実行されうる。一時的不整合の収束を次回 Trigger に委ねる設計ならコメントで明示するか、recalc 失敗時は rethrow してリトライを促すことを検討してほしい。

**コメント要約**: recalc 失敗を握りつぶすとメンバー数・マネージャーメールが更新されずリトライもない。

**判断結果**: 🟡 修正提案

**判断理由**: legacy より耐障害性は下がる。プロフィール再集計を優先する意図なら許容可能だが、データ不整合が長期化しうる。マージ必須ではないが設計意図の明示または rethrow の検討を推奨。

---

### RC-11

**識別子**: RC-11（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/slackbot.ts:118`

**該当コード（レビュー時点の diff）**:

```diff
+        return (await getSlackInstallation(installQuery.enterpriseId)) as Installation
+      }
+      if (installQuery.teamId !== undefined) {
+        return (await getSlackInstallation(installQuery.teamId)) as Installation
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: `fetchInstallation` が `getSlackInstallation` の戻り値（`unknown`）を `as Installation` でキャストしている。JSON 復元データの構造保証がなく、ランタイムで Bolt が不正 installation を受け取る可能性がある。Zod 等で `Installation` 相当のスキーマを定義して parse するか、`getSlackInstallation` の戻り値型を絞ることを検討してほしい。

**コメント要約**: unknown → Installation の無検証キャスト。

**判断結果**: 🟡 修正提案

**判断理由**: プロジェクト規約上 `as` キャストは避けるべき。oauth_data は Bolt が書き込んだ JSON なので実害リスクは低いが、型安全性向上の余地あり。マージ必須ではない。

---

### RC-12

**識別子**: RC-12（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/stores/slackBot.ts:57`

**該当コード（レビュー時点の diff）**:

```diff
+export const setSlackInstallation = async (key: string, installation: unknown): Promise<void> => {
+  const parsed = installation as { incomingWebhook?: SlackIncomingWebhook }
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: `setSlackInstallation` が引数 `installation: unknown` を `as { incomingWebhook?: ... }` でキャストしている。直後に `incomingWebhook` を Zod parse しているが、installation 本体の型検証はない。Bolt の `Installation` 型に沿った Zod スキーマで全体を parse してから store する方が安全。

**コメント要約**: installation 引数の型ガードが incomingWebhook のみ。

**判断結果**: 🟡 修正提案

**判断理由**: store 層では入力検証を厚くするのが望ましい。現状も webhook 必須チェックと Zod があるため実運用では足りている可能性が高い。マージ必須ではない。

---

### RC-13

**識別子**: RC-13（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/utils/lineEventBroadcast.ts:24`

**該当コード（レビュー時点の diff）**:

```diff
+export const broadcastEventConcludedMessage = async (): Promise<void> => {
+  const nowDateTimeMillis = Date.now()
+  const events = await getAllAcceptingOrderEvents(nowDateTimeMillis)
```

**レビュワーのコメント（原文）**:

> 🟡 **修正提案**: LINE ブロードキャストの現在時刻取得に `Date.now()` を使用している。同一 PR 内の `slackEventNotification.ts` は `DateTime.fromISO(scheduleTime)` / `DateTime.now()`（luxon）を使っており、プロジェクト規約（日付・時刻は luxon）と揃っていない。`DateTime.now().toMillis()` への統一を推奨する。

**コメント要約**: Date.now() と luxon の混在。

**判断結果**: ✅ 対応済み

**判断理由**: 機能差はほぼないが規約統一のため luxon 推奨。マージ必須ではない。

**対応**: `lineEventBroadcast.ts` で `Date.now()` を `DateTime.now().toMillis()` に変更し luxon を import。

---

## 評価セッション（2026-06-08 13:37 JST・review-comments-evaluate）

- 評価日時: 2026-06-08 13:37 JST
- ブランチ名: dev/2060
- PR: https://github.com/nijuniinc/bokudeli-event-new/pull/2061 #2061
- Outdated 除外件数: 0 件
- レビュー非該当スキップ件数: 5 件
  - GitHub id: 4629293865（nijuni-yasu・レビュー依頼の定型文）
  - GitHub id: 4629324846（Copilot・対応了承・追加指摘なし）
  - GitHub id: 4629325863（chatgpt-codex-connector・接続案内ボイラープレート）
  - GitHub id: 4645232417（nijuni-yasu・レビュー依頼の定型文）
  - GitHub id: 4645239041（chatgpt-codex-connector・接続案内ボイラープレート）

**総評**: 2026-06-08 の再レビューで新規指摘 8 件（RC-14〜RC-21）。backupFirestore スケジュールは手元で 2:00 JST に戻済み（RC-14/16/17 → ✅）。残り 🟡 4 件はマージ必須ではないが、RC-10 と重複する recalc 握りつぶし（RC-18/19）、ログ delete sentinel（RC-20）、注文 Slack 失敗伝播（RC-21）の対応を検討推奨。

### RC 別対応チェックリスト（本セッション）

- [x] **RC-14**（GitHub id: 4645238710）— ✅ 対応済み
- [x] **RC-15**（GitHub id: 3370719803）— 👌 修正不要
- [x] **RC-16**（GitHub id: 3370715816）— ✅ 対応済み
- [x] **RC-17**（GitHub id: 3370719830）— ✅ 対応済み
- [x] **RC-18**（GitHub id: 3366569538）— ✅ 対応済み
- [x] **RC-19**（GitHub id: 3370719837）— ✅ 対応済み
- [ ] **RC-20**（GitHub id: 3366569535）— 🟡 修正提案
- [x] **RC-21**（GitHub id: 3366569540）— ✅ 対応済み

---

### RC-14

**識別子**: RC-14（GitHub id: 4645238710）

**レビュワー**: Copilot

**指摘箇所**: PR トップレベル

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。
>
> 🚨 **必須修正**: `functions/default/src/backupFirestore.ts` のスケジュールが現在 `50 12 * * *`（12:50 JST）になっています。マージ前に本番想定の `0 2 * * *`（2:00 JST）へ戻してください。

**コメント要約**: backupFirestore のテスト用スケジュール（12:50 JST）をマージ前に 2:00 JST へ戻すこと。

**判断結果**: ✅ 対応済み

**判断理由**: 手元ブランチでは `schedule: '0 2 * * *'` に戻済み。WIP テスト用コミットは rebase でドロップ済み。マージ時点で本番相当の深夜バックアップ時刻となる。

---

### RC-15

**識別子**: RC-15（GitHub id: 3370719803）

**レビュワー**: Copilot

**指摘箇所**: `common/src/schemas/CommunityBot.ts:3`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,32 @@
+import { z } from 'zod'
+import { type DocumentReference } from './firebase/index.js'
+
```

**レビュワーのコメント（原文）**:

> [must] `DocumentReference` を `import type` している状態で `typeof DocumentReference` を参照しているため、TypeScript 的に「type-only import を value として使っている」扱いになりビルドエラーになります（`typeof` は value 側のシンボルが必要）。`./firebase/index.js` から value として `DocumentReference` を import してください。

**コメント要約**: `import type` と `typeof DocumentReference` の併用でビルドエラーになるとの指摘。

**判断結果**: 👌 修正不要

**判断理由**: `typeof DocumentReference` は型述語・`z.custom` の型引数など型位置でのみ使用され、ランタイムでは `isDocumentReference` が `path` プロパティを検査している。`npm -w common run build` は成功しており、現行 tsconfig ではエラーにならない。`Community.ts` 等でも同様の `type DocumentReference` パターンが使われている。

---

### RC-16

**識別子**: RC-16（GitHub id: 3370715816）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/backupFirestore.ts:16`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,41 @@
+import firestore from '@google-cloud/firestore'
+import { onSchedule } from 'firebase-functions/v2/scheduler'
+import { createModuleLogger } from './utils/logger.js'
+
+const logger = createModuleLogger('backupFirestore')
+
+const client = new firestore.v1.FirestoreAdminClient()
+
+/**
+ * Firestore の定期エクスポート。
+ * legacy `scheduled_firestore_export` から移行。Storage バックアップ追加時は `backupStorage` 等と並ぶ命名。
+ * TODO: テスト用仮実装。本番相当は毎日 2:00 JST（0 2 * * *）。確認後に戻すこと。
+ */
+export const backupFirestore = onSchedule(
+  {
+    schedule: '50 12 * * *',
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  バックアップ時刻を本番の 2:00 JST に戻してください**
>
> このまま production/development にデプロイすると、Firestore 定期エクスポートが legacy の毎日 2:00 JST ではなくテスト用の 12:50 JST で稼働します。コミット本文でも「マージ前に 2:00 JST へ戻す必要あり」とされているため、マージ対象では `0 2 * * *` に戻しておかないと運用上期待している深夜バックアップ時刻からずれます。
>
> Useful? React with 👍 / 👎.

**コメント要約**: テスト用 12:50 JST スケジュールを `0 2 * * *` に戻すこと。

**判断結果**: ✅ 対応済み

**判断理由**: RC-14 と同一論点。手元コードは `0 2 * * *`・コメントも本番相当に更新済み。

---

### RC-17

**識別子**: RC-17（GitHub id: 3370719830）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/backupFirestore.ts:18`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,41 @@
+import firestore from '@google-cloud/firestore'
+import { onSchedule } from 'firebase-functions/v2/scheduler'
+import { createModuleLogger } from './utils/logger.js'
+
+const logger = createModuleLogger('backupFirestore')
+
+const client = new firestore.v1.FirestoreAdminClient()
+
+/**
+ * Firestore の定期エクスポート。
+ * legacy `scheduled_firestore_export` から移行。Storage バックアップ追加時は `backupStorage` 等と並ぶ命名。
+ * TODO: テスト用仮実装。本番相当は毎日 2:00 JST（0 2 * * *）。確認後に戻すこと。
+ */
+export const backupFirestore = onSchedule(
+  {
+    schedule: '50 12 * * *',
+    timeZone: 'Asia/Tokyo',
+    region: 'asia-northeast1',
```

**レビュワーのコメント（原文）**:

> [must] PR説明のとおり `backupFirestore` のスケジュールがテスト用（12:50 JST）になっています。マージ前に本番相当の 2:00 JST（`0 2 * * *`）へ戻してください。

**コメント要約**: RC-14/16 と同一。12:50 JST を 2:00 JST に戻す。

**判断結果**: ✅ 対応済み

**判断理由**: 手元で `0 2 * * *` に戻済み。

---

### RC-18

**識別子**: RC-18（GitHub id: 3366569538）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/communityMemberCountsTrigger.ts:46`

**該当コード（レビュー時点の diff）**:

```diff
@@ -1,34 +1,56 @@
 import { onDocumentWritten } from 'firebase-functions/v2/firestore'
+import { sendCommunityManagerRoleChangeMails } from './communityMail.js'
 import { createModuleLogger } from './utils/logger.js'
+import { recalcCommunityMembers } from './utils/recalcCommunityMembers.js'
 import { recountUserProfileCounts } from './utils/recountUserProfileCounts.js'
 
 const logger = createModuleLogger('communityMemberCountsTrigger')
 
 /**
  * `communities/{communityId}/members/{userId}` の onDocumentWritten で、
- * マイページ用の `joined_community_count` / `managed_community_count` を再集計する。
+ * - 親 `communities` の `community_num_members` / `members` / `managers` を再集計（legacy 統合）
+ * - マネージャー追加・削除メールを送信（legacy 統合）
+ * - マイページ用の `joined_community_count` / `managed_community_count` を再集計
  *
- * - 参加・脱退（doc 追加・削除）と、`roles` の変更（manager 付与・剥奪）の両方を同 Trigger でカバーする
- * - 件数集計は `recountUserProfileCounts` 内で親 `communities.members` / `managers` 配列を count する（RC-55）。
- *   サブコレ書き込み後に本 Trigger が発火する。legacy 同期完了前の一時ズレは次回再集計で収束する想定。
+ * 同一パスに legacy `on_write_community_members` が存在していたため、本 Trigger に統合する。
  */
 export const onCommunityMemberWritten = onDocumentWritten(
   {
     document: 'communities/{communityId}/members/{userId}',
     region: 'asia-northeast1',
+    secrets: ['SENDGRID_API_KEY'],
   },
   async (event) => {
-    const userId = event.params.userId
+    const { communityId, userId } = event.params
     if (userId === '') {
-      logger.warn('Empty userId, skip recount')
+      logger.warn('Empty userId, skip processing')
       return
     }
+
+    try {
+      const recalcResult = await recalcCommunityMembers(communityId)
+      if (recalcResult.addedManagerIds.length > 0 || recalcResult.removedManagerIds.length > 0) {
+        await sendCommunityManagerRoleChangeMails({
+          communityAccount: recalcResult.communityAccount,
+          communityName: recalcResult.communityName,
+          addedManagerIds: recalcResult.addedManagerIds,
+          removedManagerIds: recalcResult.removedManagerIds,
+        })
+      }
+    } catch (error) {
+      logger.error('recalcCommunityMembers failed in onCommunityMemberWritten', {
+        error,
+        communityId,
+        userId,
+      })
+    }
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  再集計失敗を成功扱いしないでください**
>
> `recalcCommunityMembers` が Firestore 書き込みやスキーマ変換で失敗した場合、この catch で握りつぶされるため、親 `communities.members/managers/community_num_members` が古いままでも Trigger 実行は成功扱いになります。その直後の `recountUserProfileCounts` もその親配列を正本として数えるため、コミュニティメンバー変更時に親集計とプロフィール件数が同時に stale になり、失敗として検知・再実行できません。少なくとも再集計失敗時は後続のプロフィール再集計を避け、例外を再 throw してください。
>
> Useful? React with 👍 / 👎.

**コメント要約**: recalc 失敗を catch で握りつぶすと親集計・プロフィール件数が stale のままリトライされない。

**判断結果**: 🟡 修正提案

**判断理由**: RC-10（エージェントレビュー）と同一論点。指摘は妥当だが legacy より意図的にプロフィール再集計を優先する設計の可能性もある。マージ必須ではないが、recalc 失敗時は rethrow するか、後続 `recountUserProfileCounts` をスキップする方がデータ整合性は高い。

---

### RC-19

**識別子**: RC-19（GitHub id: 3370719837）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/communityMemberCountsTrigger.ts:46`

**該当コード（レビュー時点の diff）**:

（RC-18 と同一 diff_hunk）

**レビュワーのコメント（原文）**:

> [imo] `recalcCommunityMembers` の失敗を catch して握りつぶすと、この変更（community_num_members/members/managers 更新・マネージャーメール）がリトライされず欠落します（ログは出るが Trigger は成功扱い）。少なくとも集計側が失敗した場合は throw して onDocumentWritten のリトライに乗せる方が安全です。

**コメント要約**: RC-18 と同一。recalc 失敗時は throw してリトライさせるべき。

**判断結果**: 🟡 修正提案

**判断理由**: RC-10 / RC-18 と同一。Codex の追加指摘（後続 recount も stale 化）も含め、設計改善として検討推奨。マージ必須ではない。

---

### RC-20

**識別子**: RC-20（GitHub id: 3366569535）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/stores/event.ts:484`

**該当コード（レビュー時点の diff）**:

```diff
@@ -431,3 +449,37 @@ export const convertReferenceToEvent = async (
   const eventSnapshot = await eventRef.withConverter(new ShokujiiEventConverter()).get()
   return eventSnapshot.data()
 }
+
+/**
+ * イベント変更差分を `events/{eventId}/logs` に追加する。
+ * legacy `log_event_status` の write_log 相当。withConverter 付き ref 経由で書き込む。
+ */
+export const addEventChangeLog = async (
+  communityId: string,
+  eventId: string,
+  differences: Record<string, unknown>,
+  updatedBy: string,
+): Promise<void> => {
+  const db = getFirestore()
+  const logsCollection = db
+    .collection('communities')
+    .doc(communityId)
+    .collection('events')
+    .doc(eventId)
+    .collection('logs')
+
+  const logRef = logsCollection.doc().withConverter(new ShokujiiEventLogConverter())
+  const updatedAt =
+    differences.updated_at instanceof Timestamp
+      ? differences.updated_at.toMillis()
+      : typeof differences.updated_at === 'number'
+        ? differences.updated_at
+        : Date.now()
+
+  const log = new EventLog(logRef.id, {
+    ...differences,
+    updated_by: updatedBy,
+    updated_at: updatedAt,
+  })
+  await logRef.set(log)
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  ログ書き込みで delete sentinel を作らないでください**
>
> イベントステータス変更などで差分に `event_status.shop_comment: ''` や optional 文字列の空文字が含まれる場合、`EventLogDbSchema` が `NonEmptyStringSchema` 経由で `FieldValue.delete()` に変換しますが、ここでは `set()` を merge なしで呼ぶため Firestore が delete sentinel を拒否し、`log_event_status` のログ書き込み全体が失敗します。legacy は raw add で空文字をそのまま記録していたので、差分ログ用には削除 sentinel を生成しない schema/書き込みに分ける必要があります。
>
> Useful? React with 👍 / 👎.

**コメント要約**: ログ差分の空文字が `NonEmptyStringSchema` 経由で delete sentinel になり、`set()` 無 merge で書き込み失敗するリスク。

**判断結果**: 🟡 修正提案

**判断理由**: `EventLogDbSchema` は `EventDbSchema.partial()` を merge しており、`event_status.shop_comment` 等の `NonEmptyStringSchema` が空文字を `FieldValue.delete()` に変換する。`rejectOrderMail.ts` 等で `shop_comment: ''` を明示設定する経路があり、差分に空文字が入りうる。新規ログ doc への `set()` で delete sentinel が含まれると Firestore エラーの可能性が高い。ログ専用スキーマ（空文字をそのまま保存、または delete 変換なし）への分離を検討すべき。マージ必須ではないが、該当ステータス遷移の実機確認を推奨。

---

### RC-21

**識別子**: RC-21（GitHub id: 3366569540）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/slackMessage.ts:35`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,36 @@
+import { CommunityBot } from '@shokujii/common/schemas/CommunityBot.js'
+import { createModuleLogger } from './logger.js'
+import { getSlackWebhookUrl } from '../stores/slackBot.js'
+
+const logger = createModuleLogger('slackMessage')
+
+export const sendSlackWebhookMessage = async (webhookUrl: string, text: string): Promise<void> => {
+  const response = await fetch(webhookUrl, {
+    method: 'POST',
+    headers: {
+      'Content-Type': 'application/json',
+    },
+    body: JSON.stringify({ text }),
+  })
+
+  if (!response.ok) {
+    logger.error('Slack webhook request failed', { status: response.status, statusText: response.statusText })
+    throw new Error(`Slack webhook request failed: ${response.status}`)
+  }
+}
+
+export const sendCommunityBotMessage = async (bot: CommunityBot, text: string): Promise<void> => {
+  if (bot.type !== 'slack') {
+    return
+  }
+  const webhookUrl = await getSlackWebhookUrl(bot.reference)
+  if (webhookUrl == null || webhookUrl === '') {
+    logger.warn('Slack webhook url is missing', { botId: bot.id })
+    return
+  }
+  await sendSlackWebhookMessage(webhookUrl, text)
+}
+
+export const sendCommunityBotsMessage = async (bots: CommunityBot[], text: string): Promise<void> => {
+  await Promise.allSettled(bots.map((bot) => sendCommunityBotMessage(bot, text)))
```

**レビュワーのコメント（原文）**:

> **<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  注文 Slack 通知の失敗を成功扱いしないでください**
>
> `orderNotification` は `status` が `ordered` へ変わった瞬間だけ送信するため、Webhook のネットワーク失敗や Slack の non-2xx で `sendCommunityBotMessage` が reject しても、この `Promise.allSettled` の結果を破棄すると Trigger は成功扱いになり、同じ注文通知は再試行されません。少なくとも settled 結果を確認して失敗時に throw するか、注文通知側では失敗を伝播させる helper を使ってください。
>
> Useful? React with 👍 / 👎.

**コメント要約**: `sendCommunityBotsMessage` が失敗を warn のみで握りつぶし、注文通知 Trigger がリトライされない。

**判断結果**: 🟡 修正提案

**判断理由**: レビュー時点の diff は `allSettled` 結果を破棄していた。現行コードは失敗件数を `warn` ログするが throw はしない。`slackOrderNotification` は `ordered` への遷移時のみ1回発火するため、Webhook 失敗時に通知が永久欠落しうる。RC-5 で `slackEventNotification` はジョブ失敗時 throw 済みだが、注文通知は別経路。注文専用 helper で失敗時 throw、または `slackOrderNotification` 側で失敗を検知して rethrow する改善を推奨。マージ必須ではない。

---
