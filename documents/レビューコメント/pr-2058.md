# PR #2058 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-12 18:50 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: feat/955
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2058
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 7（レビュー依頼定型文 3 件、Copilot 承知返信 2 件、Codex 接続案内 2 件）

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: 3361460904）— ✅ 対応済み（Materio formatDateToMonthShort 依存を解消）
- [x] **RC-2**（GitHub id: 3402322638）— ✅ 対応済み（RC-1 同一論点）
- [x] **RC-3**（GitHub id: 3366767938）— ✅ 対応済み（formatChatListTimestamp で JST 表示）
- [x] **RC-4**（GitHub id: 3355142476）— ✅ 対応済み（waitForMembership + navigateToChat prop）
- [x] **RC-5**（GitHub id: 3355160653）— ✅ 対応済み（resolveChatRoomPath + navigate-room emit）
- [x] **RC-6**（GitHub id: 3361460908）— ✅ 対応済み（membership 外 roomId → room_not_found + redirect）
- [x] **RC-7**（GitHub id: 3402195593）— ✅ 対応済み（遅延 membership 10 秒待機後 openRoom）
- [x] **RC-8**（GitHub id: 3402322664）— ✅ 対応済み（isNearBottom 条件付き自動スクロール）
- [x] **RC-9**（GitHub id: 3360806059）— ✅ 対応済み（RC-8 同一論点）
- [x] **RC-10**（GitHub id: 3402195599）— ✅ 対応済み（RC-8 同一論点）
- [x] **RC-11**（GitHub id: 3355160626）— ✅ 対応済み（RC-8 同一論点）
- [x] **RC-12**（GitHub id: 3355142473）— ✅ 対応済み（PerfectScrollbar ps.element 参照）
- [x] **RC-13**（GitHub id: 3361460924）— ✅ 対応済み（ChatLog 送信者プロフィールを getDoc 一回取得 + ルーム切替で Map クリア）
- [x] **RC-14**（GitHub id: 3366767940）— ✅ 対応済み（userId 切替時 unsubscribeActiveRoom）
- [x] **RC-15**（GitHub id: 3361460916）— ✅ 対応済み（mergeMessages で既存履歴を保持）
- [x] **RC-16**（GitHub id: 3402195585）— ✅ 対応済み（RC-15 同一論点）
- [x] **RC-17**（GitHub id: 3355160587）— ✅ 対応済み（RC-15 同一論点）
- [x] **RC-18**（GitHub id: 3361460922）— ✅ 対応済み（deleteUserAccount chat cleanup）
- [x] **RC-19**（GitHub id: 3373518072）— ✅ 対応済み（last_read_at is timestamp / == request.time）
- [x] **RC-20**（GitHub id: 3402322668）— ✅ 対応済み（RC-19 同一論点）
- [x] **RC-21**（GitHub id: 3355142479）— ✅ 対応済み（messages create hasOnly で processed 拒否）
- [x] **RC-22**（GitHub id: 3402322621）— ✅ 対応済み（messages create hasOnly）
- [x] **RC-23**（GitHub id: 3402195557）— ✅ 対応済み（RC-21 同一論点）
- [x] **RC-24**（GitHub id: 3402195563）— 👌 修正不要（仕様 §5.4: Zod 2000 文字 + Rules 8000 バイト意図どおり）
- [x] **RC-25**（GitHub id: 3402322629）— 👌 修正不要（RC-24 同一論点）
- [x] **RC-26**（GitHub id: 3355142468）— 👌 修正不要（RC-24 同一論点）
- [x] **RC-27**（GitHub id: 3360806063）— 👌 修正不要（archiveChatRoom 冪等 + throw で再試行可）
- [x] **RC-28**（GitHub id: 3355142453）— ✅ 対応済み（送信者 preview 更新）
- [x] **RC-29**（GitHub id: 3360806065）— ✅ 対応済み（onChatMessageCreated retry: true + claimed=false 時は冪等副作用のみ再実行）
- [x] **RC-30**（GitHub id: 3402322644）— ✅ 対応済み（DB 再読込 + 削除済みなら副作用スキップ）
- [x] **RC-31**（GitHub id: 3355142459）— ✅ 対応済み（claimMessageProcessed claim-first）
- [x] **RC-32**（GitHub id: 3402322633）— ✅ 対応済み（RC-31 同一論点）
- [x] **RC-33**（GitHub id: 3355160530）— ✅ 対応済み（RC-31 同一論点）
- [x] **RC-34**（GitHub id: 3355142490）— ✅ 対応済み（last_message_at 比較で古い Trigger をスキップ）
- [x] **RC-35**（GitHub id: 3402195594）— ✅ 対応済み（RC-34 同一論点）
- [x] **RC-36**（GitHub id: 3373518083）— ✅ 対応済み（last_read_at >= created_at なら未読加算スキップ）
- [x] **RC-37**（GitHub id: 3402195576）— ✅ 対応済み（RC-36 同一論点）
- [x] **RC-38**（GitHub id: 3402322672）— ✅ 対応済み（RC-36 同一論点）
- [x] **RC-39**（GitHub id: 3355142486）— ✅ 対応済み（last_read_at ガードで対応。FieldValue.increment 原子化は不採用）
- [x] **RC-40**（GitHub id: 3402195569）— ✅ 対応済み（RC-31 claim-first で副作用重複を防止）
- [x] **RC-41**（GitHub id: 3361460911）— ✅ 対応済み（onEventArchived retry: true）
- [x] **RC-42**（GitHub id: 3402322652）— 👌 修正不要（saveChatRoom merge + transaction）
- [x] **RC-43**（GitHub id: 3366767941）— 👌 修正不要（§6.1 lazy / マイグレーション役割分担）
- [x] **RC-44**（GitHub id: 3402322661）— 👌 修正不要（RC-43 同一論点）
- [x] **RC-45**（GitHub id: 3402322671）— ✅ 対応済み（既存 room から preview 引き継ぎ）
- [x] **RC-46**（GitHub id: 3373518090）— ✅ 対応済み（アーカイブ event への join 拒否）

---

## 主要 RC 詳細（代表項目）

**識別子**: RC-15（GitHub id: 3361460916）

**レビュワー**: Codex

**指摘箇所**: `base/src/stores/chat.ts:330`

**該当コード（レビュー時点の diff）**:

```diff
    messagesUnsubscribe = onSnapshot(messagesQuery, (snapshot) => {
      const incoming = snapshot.docs.map((docSnapshot) => toMessageItem(docSnapshot.data())).reverse()
-      messages.value = mergeMessages([], incoming)
+      messages.value = mergeMessages([], incoming)  // 毎回 existing を空にしている
```

**レビュワーのコメント（原文）**:

P2: 最新 50 件の snapshot で古い履歴を捨てないでください。loadOlderMessages で追加した履歴が onSnapshot 更新で消える。

**コメント要約**: subscribeMessages が毎回 mergeMessages に空配列を渡しており、ページネーションで読み込んだ古いメッセージが失われる。

**判断結果**: 🚨 必須修正

**判断理由**: 現行コード 330 行目が mergeMessages([], incoming) のまま。mergeMessages 自体は実装済みだが購読側で使われておらず、Copilot RC-17 と同一の実バグ。チャット履歴 UX の根幹に関わる。

---

**識別子**: RC-22（GitHub id: 3402322621）

**レビュワー**: Codex

**指摘箇所**: `firestore.rules:184`

**該当コード（レビュー時点の diff）**:

```diff
                allow create: if isChatRoomWritable(roomId)
                              && request.resource.data.message_type == 'user'
                              && request.resource.data.sender_user_id == request.auth.uid
                              && request.resource.data.body is string
                              && request.resource.data.body.size() > 0
                              && request.resource.data.body.size() <= 8000
                              && request.resource.data.created_at == request.time;
```

**レビュワーのコメント（原文）**:

P1: messages create の許可フィールドを制限してください。hasOnly 等がなく processed 等の余計なキーをクライアントが送れる。

**コメント要約**: Rules で許可キーが固定されておらず、processed フィールドをクライアントが create 時に含められる。

**判断結果**: 🚨 必須修正

**判断理由**: onChatMessageCreated の冪等性が processed フラグに依存。Rules で拒否しないとメンバーが processed: true 付きメッセージを作成し Trigger を bypass できる。RC-21 / RC-23 も同一論点。

---

**識別子**: RC-24（GitHub id: 3402195563）

**レビュワー**: Codex

**指摘箇所**: `firestore.rules:189`

**該当コード（レビュー時点の diff）**:

```diff
                              && request.resource.data.body.size() <= 8000
```

**レビュワーのコメント（原文）**:

P2: Rules の本文上限をスキーマと合わせてください。Zod と UI は 2000 文字。

**コメント要約**: body.size 上限 8000 バイトは Zod の 2000 文字と不一致。

**判断結果**: 🚨 必須修正

**判断理由**: 仕様書も UTF-8 8000 は Zod 2000 文字との整合用だが、クライアントバリデーションをすり抜けて oversized 本文を Rules 経由で保存可能。Rules を 6000 バイト程度に揃えるか hasOnly とセットで整合を取るべき。

---

**識別子**: RC-30（GitHub id: 3402322644）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:29`

**該当コード（レビュー時点の diff）**:

```diff
  const message = new ChatMessage(messageId, snapshot.data())
  const preview = buildMessagePreview(message)
```

**レビュワーのコメント（原文）**:

P1: 取り消し済みの最新状態を読んでから preview を作ってください。投稿直後 recall だと Trigger が取り消し前スナップショットで preview を上書きしうる。

**コメント要約**: Phase 2 recall 追加後、遅延した onChatMessageCreated が削除前 body で last_message を更新するレースがある。

**判断結果**: 🚨 必須修正

**判断理由**: recall で body 削除後も Trigger が event.data のスナップショットから preview を生成する。isDeletedUserMessage チェックまたは DB 再読込が必要。Phase 2 マージ後の新規リスク。

---

**識別子**: RC-31（GitHub id: 3402322633）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:36`

**該当コード（レビュー時点の diff）**:

```diff
  const alreadyProcessed = await db.runTransaction(async (transaction) => {
    return isMessageProcessed(roomId, messageId, transaction)
  })
  if (alreadyProcessed) {
    return
  }
  // ... 副作用 ...
  await db.runTransaction(async (transaction) => {
    setMessageProcessed(roomId, messageId, transaction)
  })
```

**レビュワーのコメント（原文）**:

P1: 処理済み化を副作用より先に原子的に行ってください。重複 Trigger で unread 二重加算等が起きうる。

**コメント要約**: processed の read-check と write-claim が分離しており、並行実行で副作用が重複する。

**判断結果**: 🚨 必須修正

**判断理由**: 開始時は read のみ、終了時 set。同一 message の並行 Trigger で両方が false を読んで進む可能性が残る。transaction 内で claim してから副作用、または FieldValue.increment と created_at 条件付き last_message 更新が必要。RC-32 / RC-33 / RC-40 同一。

---

**識別子**: RC-28（GitHub id: 3355142453）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:1`

**該当コード（レビュー時点の diff）**:

```diff
      let updated = updateMembershipLastMessage(membership, { preview, lastMessageAt })
      if (shouldIncrementUnread && memberUserId !== senderUserId) {
        updated = incrementMembershipUnread(updated)
      }
```

**レビュワーのコメント（原文）**:

P2: 送信者の membership も最新メッセージで更新してください。

**コメント要約**: 送信者を continue で skip すると送信者側一覧 preview が古いまま。

**判断結果**: ✅ 対応済み

**判断理由**: 現行コードは送信者も updateMembershipLastMessage 対象。unread のみ送信者除外。指摘時点の skip は解消済み。

---

**識別子**: RC-19（GitHub id: 3402322668）

**レビュワー**: Codex

**指摘箇所**: `firestore.rules:64`

**該当コード（レビュー時点の diff）**:

```diff
        function chatMembershipUpdateAllowed() {
            return request.resource.data.diff(resource.data).affectedKeys()
                    .hasOnly(['last_read_at', 'unread_count', 'updated_at'])
                && request.resource.data.unread_count == 0
                && request.resource.data.updated_at == request.time;
        }
```

**レビュワーのコメント（原文）**:

P1: last_read_at の型も Rules で制限してください。

**コメント要約**: last_read_at に Timestamp 以外を入れても通る可能性。

**判断結果**: 🚨 必須修正

**判断理由**: chatMembershipUpdateAllowed に last_read_at == request.time または is timestamp 検証が無い。RC-20 同一。

---

## 重複 RC の扱い

RC-2, RC-9〜11, RC-16〜17, RC-20, RC-23, RC-25〜26, RC-32〜33, RC-35, RC-37〜38, RC-44 は、上記代表 RC と同一論点のため判断結果・理由を共有する。原文は GitHub PR #2058 の該当インラインコメントを参照。

## 優先対応サマリ

| 優先度 | テーマ | RC |
|--------|--------|-----|
| 高 | Firestore Rules 強化（hasOnly / processed 拒否 / body 上限 / last_read_at 型） | RC-19〜26 |
| 高 | onChatMessageCreated 冪等性（processed 原子的 claim） | RC-31〜33, RC-40 |
| 高 | subscribeMessages が古い履歴を捨てる | RC-15〜17 |
| 高 | recall 後 Trigger レース | RC-30 |
| 中 | スクロール・ページネーション UX | RC-8〜12 |
| 中 | 未読加算と既読の競合 | RC-36〜39（✅ 対応済み） |
| 中 | sync / archive / lazy 作成の edge case | RC-28〜29, RC-41〜46（RC-29, 41 ✅） |
| 低 | Materio 配下変更・JST 表示・ルーティング責務 | RC-1〜5, RC-13〜14, RC-18（RC-13 ✅） |

---

## 評価セッション

- **評価日時**: 2026-06-14 19:11 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: feat/955
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2058
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 19（トップレベルのレビュー依頼定型文・Copilot 承知返信・Codex 接続案内 / レビューサマリ・Copilot overview・Copilot エラー通知など）
- **対象**: 前セッション（RC-1〜RC-46）以降に追加された新規インラインコメント 23 件（RC-47〜RC-69）。Phase 2/3（送信取り消し `recallChatMessage` 追加、`onChatMessageCreated` の retry/未読ロジック改修、`chatRoomDisplay` 追加）に対する指摘が中心。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-47 | 3403558676 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | Materio 配下に新規 composable を追加（Codex P1）<br>`base/src/composable/useResponsiveSidebar.ts` へ移設済み |
| [x] | RC-48 | 3403550707 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | RC-47 同一論点（Copilot）<br>useResponsiveSidebar を base/src へ移設済み |
| [x] | RC-49 | 3403550678 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | formatters を Materio 配下に新規追加（Copilot）<br>`base/src/utils/avatarText.ts` へ移設済み |
| [x] | RC-50 | 3403550513 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | — | profilePath の返り値型を ReturnType<ResolveUserPathFn> に整合 |
| [x] | RC-51 | 3403550649 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | — | 空の useChat.ts を削除 |
| [x] | RC-52 | 3409341504 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | — | 購読再発火時に oldestMessageSnapshot を初回のみ設定 |
| [x] | RC-53 | 3403550547 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | — | openRoom の markAsRead 二重呼び出しを解消 |
| [x] | RC-54 | 3403558670 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | — | cache 残存時も listener を必ず張る |
| [x] | RC-55 | 3403550446 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | — | RC-54 同一対応 |
| [ ] | RC-56 | 3403558665 | 🟡 修正提案 | ❓ 要確認 | 💾 データ, 👤 UX | 🔧 微修正 | M | room inactive で membership 同期が途中終了（Codex P1）<br>再試行で membership.is_active が true 残留しうる |
| [x] | RC-57 | 3403408202 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | — | onDocumentCreated に retry 未指定<br>現行は `retry: true` 指定済み |
| [ ] | RC-58 | 3403408186 | 🟡 修正提案 | ❓ 要確認 | 💾 データ | 📐 リファクタ | M | claim 後の失敗で未読が再計上されない<br>claim と副作用の冪等境界。RC-61 と併せ検討 |
| [ ] | RC-59 | 3403408205 | 🟡 修正提案 | ❓ 要確認 | 💾 データ, 👤 UX | 🔧 微修正 | M | 順序逆転メッセージの未読が加算されない<br>未読加算を last_message 更新と分離 |
| [ ] | RC-60 | 3403558669 | 🟡 修正提案 | ❓ 要確認 | 💾 データ, 👤 UX | 🔧 微修正 | M | RC-59 同一論点（Codex P1）<br>shouldApplyLastMessage false で membership ごとスキップ |
| [ ] | RC-61 | 3403558681 | 🟡 修正提案 | ❓ 要確認 | 💾 データ | 📐 リファクタ | M | 未読加算が read+set で非原子的（Codex P1）<br>同時投稿で加算が後勝ちロス。FieldValue.increment 化 |
| [x] | RC-62 | 3403550603 | 👌 修正不要 | — | — | 👀 確認のみ | — | FieldValue/Timestamp import 追加要求<br>現実装は未使用のため不要（tombstone 化採用時のみ） |
| [x] | RC-63 | 3403550625 | 👌 修正不要 | — | — | 👀 確認のみ | — | 未使用 import 整理要求<br>現実装で markChatMessageDeleted/saveChatMessage は使用中 |
| [ ] | RC-64 | 3403408192 | 🟡 修正提案 | ❓ 要確認 | 💾 データ | 🔧 微修正 | M | 取り消し再計算が force で新着を上書き<br>再計算中の新着 last_message を巻き戻しうる |
| [x] | RC-65 | 3403408197 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 🔧 微修正 | — | recall に member_user_ids 在籍チェックを追加 |
| [x] | RC-66 | 3403558686 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 🔧 微修正 | — | RC-65 同一対応 |
| [ ] | RC-67 | 3403550582 | 🟡 修正提案 | ❓ 要確認 | 💾 データ | 📐 リファクタ | S | saveChatMessage(set) で processed 等が消える<br>create トリガーは再発火しないため現状無害だが update 化が堅牢 |
| [x] | RC-68 | 3409341501 | 👌 修正不要 | — | — | 👀 確認のみ | — | undefined preview 保存で失敗するとの指摘<br>convertToDb が undefined を除去済み。誤指摘 |
| [ ] | RC-69 | 3409341503 | 🟡 修正提案 | ❓ 要確認 | 💾 データ | 🔧 微修正 | M | チャット同期の catch が再 throw せず成功扱い<br>失敗時に再試行されない。意図的か要確認 |
| [x] | RC-70 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | ChatAttachmentImage.vue 新設で deep watch 撤去<br>添付取得を子コンポーネント自己完結化 |
| [x] | RC-71 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | — | ChatAttachmentImage.vue で getUrl 多重呼び出し解消<br>非 null アサーション撤去 |
| [x] | RC-72 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | buildAttachment に型ガード済み contentType を渡す<br>as キャスト撤去 |
| [x] | RC-73 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | 画像送信 setDoc が withConverter 非経由<br>既存 addDoc と同一パターン。rules で検証 |
| [x] | RC-74 | なし | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📋 仕様追加 | — | isValidChatAttachmentPath に content_type/byte_size 検証追加<br>Storage rules と整合 |
| [x] | RC-75 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | Storage read ごとに firestore.get 1 read<br>メンバー限定 read の設計上必要なコスト |

---

**識別子**: RC-47（GitHub id: 3403558676）

**レビュワー**: Codex

**指摘箇所**: `base/materio/@core/composable/useResponsiveSidebar.ts:1`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,24 @@
+import type { Ref } from 'vue'
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Materio 直下ではなく project 側に移してください**

AGENTS.md の Materio ルールで「`base/materio/`（`@core` / `@layouts` 含む）は原則変更禁止」とされており、このチャット用 helper をテンプレート配下に追加するとアップストリーム取り込み時の diff/競合を増やします。プロジェクト固有の composable は `base/src/` 側に置き、ChatApp から `@shokujii/base/...` で参照する形にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: チャット用 composable を `base/materio/@core/` 配下に新規追加している。AGENTS.md の「Materio 配下は原則変更禁止・不足 util は base/src へ」に抵触するため、`base/src/` へ移すか upstream 例外を明記すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: —

**判断理由**: `base/src/composable/useResponsiveSidebar.ts` へ移設し、`ChatApp.vue` の import を `@shokujii/base/composable/useResponsiveSidebar.js` に切替。`base/materio/@core/composable/useResponsiveSidebar.ts` は削除済み。RC-48 と同一対応。

---

**識別子**: RC-48（GitHub id: 3403550707）

**レビュワー**: Copilot

**指摘箇所**: `base/materio/@core/composable/useResponsiveSidebar.ts:6`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,24 @@
+import type { Ref } from 'vue'
+import { useDisplay } from 'vuetify'
+
+export const useResponsiveLeftSidebar = (mobileBreakpoint: Ref<boolean> | undefined = undefined) => {
+  const { mdAndDown, name: currentBreakpoint } = useDisplay()
+
```

**レビュワーのコメント（原文）**:

[must] `AGENTS.md` の「`base/materio/` は原則変更禁止」ルールと整合しないため、Materio 配下に新規 composable を追加するのは避けたいです。`base/src/` 側へ移すか、どうしても Materio 側が必要な理由（upstream 同等など）をルールに明記してください。

**コメント要約**: RC-47 と同一の指摘。useResponsiveSidebar を Materio 配下に追加している点を Copilot も [must] で指摘。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: —

**判断理由**: RC-47 と同一対応。`useResponsiveLeftSidebar` を `base/src/composable/useResponsiveSidebar.ts` へ移設済み。

---

**識別子**: RC-49（GitHub id: 3403550678）

**レビュワー**: Copilot

**指摘箇所**: `base/materio/@core/utils/formatters.ts:5`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,19 @@
+import { isToday } from './helpers'
+
+export const avatarText = (value: string) => {
+  if (!value) return ''
+  const nameArray = value.split(' ')
```

**レビュワーのコメント（原文）**:

[must] `AGENTS.md` に「`base/materio/` は原則変更禁止・不足 util は `base/src/` 側へ追加」と追記されていますが、本 PR では `base/materio/@core/utils/formatters.ts` を新規追加しています。プロジェクト固有の依存を増やすとテンプレ更新時の追従コストが上がるため、可能なら `base/src/`（または `common`）へ移し、参照側の import を切り替えてください（もしくは upstream 由来で例外なら、その旨をルール側に明記）。

**コメント要約**: formatters.ts を `base/materio/@core/utils/` に新規追加。RC-47/48 と同じく Materio 変更禁止規約に抵触。`base/src` か `common` へ移すか例外明記を提案。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: —

**判断理由**: `avatarText` を `base/src/utils/avatarText.ts` へ移設し、`ChatApp.vue` / `ChatContact.vue` の import を `@shokujii/base/utils/avatarText.js` に切替。未使用の `formatDateToMonthShort` は破棄。`base/materio/@core/utils/formatters.ts` は削除済み。

---

**識別子**: RC-50（GitHub id: 3403550513）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/chat/ChatLog.vue:65`

**該当コード（レビュー時点の diff）**:

```diff
+const profilePath = (senderUserId: string): string | undefined => {
+  if (!canLinkToProfile(senderUserId) || props.resolveProfilePath == null) {
+    return undefined
+  }
+  return props.resolveProfilePath(senderUserId)
+}
```

**レビュワーのコメント（原文）**:

[must] `profilePath` の返り値型が `string` になっていますが、`ResolveUserPathFn` は `RouteLocationRaw` を返す想定なので型が不整合です（オブジェクト形式のルート指定が渡ると型エラー/意図しない挙動になります）。`ReturnType<ResolveUserPathFn>` などに合わせてください。

**コメント要約**: `profilePath` の戻り型が `string | undefined` だが `resolveProfilePath`（`ResolveUserPathFn`）は `RouteLocationRaw` を返す。型不整合で、オブジェクト形式のルートが渡ると不具合になる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: `profilePath` の戻り型を `ReturnType<ResolveUserPathFn> | undefined` に変更し、`ResolveUserPathFn`（`RouteLocationRaw`）との型整合を取った。

---

**識別子**: RC-51（GitHub id: 3403550649）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/chat/useChat.ts:3`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,3 @@
+export const useChat = () => {
+  return {}
+}
```

**レビュワーのコメント（原文）**:

[must] 空の `useChat()` が追加されていますが、このファイルは現状どこからも参照されておらず、API としても意味を持ちません（将来の誤用/混乱の元になります）。不要であればファイル自体を削除し、必要なら責務を持つ実装にしてから追加してください。

**コメント要約**: 空実装の `useChat()` がどこからも import されておらず、デッドコード。削除するか責務を持たせるべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: 未参照の `base/src/components/chat/useChat.ts` を削除した。

---

**識別子**: RC-52（GitHub id: 3409341504）

**レビュワー**: Codex

**指摘箇所**: `base/src/stores/chat.ts:365`

**該当コード（レビュー時点の diff）**:

```diff
+    messagesUnsubscribe = onSnapshot(messagesQuery, (snapshot) => {
+      const incoming = snapshot.docs.map((docSnapshot) => toMessageItem(docSnapshot.data())).reverse()
+      messages.value = mergeMessages(messages.value, incoming)
+      oldestMessageSnapshot = snapshot.docs[snapshot.docs.length - 1] ?? null
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  ページングカーソルを最新 50 件へ戻さないでください**

過去ログを `loadOlderMessages()` で読み込んだ後でも、新着や `processed` 更新で最新 50 件の購読が再発火すると、この代入で `oldestMessageSnapshot` が最新ページの末尾に戻ります。その状態でさらに上へスクロールすると同じ古い 50 件を再取得してしまい、50 件単位の重複ロードを挟まないとさらに古い履歴へ進めません。購読更新時は、既に読み込んだ最古カーソルを巻き戻さないようにしてください。

Useful? React with 👍 / 👎.

**コメント要約**: `subscribeMessages` の onSnapshot 再発火時に `oldestMessageSnapshot` が最新ページ末尾へ戻る。`loadOlderMessages` で進めたカーソルが巻き戻り、上スクロール時に同じ 50 件を再取得する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: `subscribeMessages` の onSnapshot 内で `oldestMessageSnapshot` / `hasMoreMessages` を `oldestMessageSnapshot == null` の初回のみ設定するよう変更。`loadOlderMessages` で進めたカーソルが購読再発火で巻き戻らない。

---

**識別子**: RC-53（GitHub id: 3403550547）

**レビュワー**: Copilot

**指摘箇所**: `base/src/stores/chat.ts:400`

**該当コード（レビュー時点の diff）**:

```diff
+  const openRoom = (roomId: string, userId: string) => {
+    subscribeRoom(roomId)
+    subscribeMessages(roomId, userId)
+    void markAsRead(roomId, userId)
+  }
```

**レビュワーのコメント（原文）**:

[must] `openRoom()` 内で `markAsRead()` を呼んだ直後に `subscribeMessages()` の初回 snapshot でも `markAsRead()` が走るため、ルームオープン時に同内容の書き込みが二重に発生します（無駄な write / コスト増）。どちらか片方に寄せてください。

**コメント要約**: `openRoom` で `markAsRead` を直接呼びつつ、`subscribeMessages` の初回 snapshot でも `markAsRead` が走り、ルームオープン時に二重 write。どちらかに寄せるべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: `openRoom` 内の `void markAsRead(roomId, userId)` を削除。既読化は `subscribeMessages` の初回 snapshot（`activeRoomId === roomId` 時）に一本化し、二重 write を解消した。

---

**識別子**: RC-54（GitHub id: 3403558670）

**レビュワー**: Codex

**指摘箇所**: `base/src/stores/chatRoomDisplay.ts:44`

**該当コード（レビュー時点の diff）**:

```diff
+const ensureEventListener = (communityId: string, eventId: string): void => {
+  const key = eventCacheKey(communityId, eventId)
+  if (listeners.has(key)) {
+    return
+  }
+
+  const cached = cache.get(key)
+  if (cached != null) {
+    notifySubscribers(key, cached)
+    return
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  キャッシュ再利用時も購読を張り直してください**

一度全購読者がいなくなると unsubscribe 側で listener だけ削除され cache は残るため、その後同じイベントを再購読してもこの cached 分岐で return して Firestore listener が復活しません。チャット画面へ戻った後や `unsubscribeAllEventRoomDisplays()` 後は、イベント名・カバー画像の変更がチャット一覧/ヘッダーに反映されなくなるので、キャッシュを即時表示しつつ listener は作成してください。

Useful? React with 👍 / 👎.

**コメント要約**: 全購読解除で listener だけ削除され cache が残る。再購読時に `cached != null` の分岐で early return し Firestore listener が復活せず、イベント名・カバー更新が反映されなくなる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: `ensureEventListener` の `cached != null` 早期 return を削除。`listeners.has(key)` ガードは残し、cache 有無に関わらず onSnapshot を必ず張る。即時表示は `subscribeEventRoomDisplay` の `onUpdate(cached)` が担う。

---

**識別子**: RC-55（GitHub id: 3403550446）

**レビュワー**: Copilot

**指摘箇所**: `base/src/stores/chatRoomDisplay.ts:45`

**該当コード（レビュー時点の diff）**:

```diff
+const ensureEventListener = (communityId: string, eventId: string): void => {
+  const key = eventCacheKey(communityId, eventId)
+  if (listeners.has(key)) {
+    return
+  }
+
+  const cached = cache.get(key)
+  if (cached != null) {
+    notifySubscribers(key, cached)
+    return
+  }
```

**レビュワーのコメント（原文）**:

[must] `cache` に値がある場合に `ensureEventListener` が早期 return してしまい、再購読後にイベント名/カバー画像の更新を購読できません（キャッシュが stale になる可能性があります）。キャッシュがあっても listener を張るようにし、必要なら初期表示だけ cached を流すに留めてください。

**コメント要約**: RC-54 と同一。cache あり時に early return し再購読後の更新を購読できない。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: RC-54 と同一対応。`ensureEventListener` の cache 早期 return を削除し、再購読後も listener が復活するよう修正した。

---

**識別子**: RC-56（GitHub id: 3403558665）

**該当コード（レビュー時点の diff）**:

```diff
+export async function archiveChatRoom(roomId: string): Promise<void> {
+  const room = await getChatRoom(roomId)
+  if (room == null) {
+    logger.info('Chat room not found; skip archive', { roomId })
+    return
+  }
+  if (!room.is_active) {
+    logger.info('Chat room already archived', { roomId })
+    return
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  アーカイブ済みでも membership 同期を続行してください**

`saveChatRoom(setChatRoomInactive(room))` までは成功した後に membership 更新の途中で Functions が失敗すると、retry では room がすでに inactive のためここで即 return し、残りの `chat_memberships.is_active` が true のまま残ります。イベント削除・キャンセル時に一覧側は membership を見て有効チャットとして表示し続けるため、room が inactive でも membership の同期処理は冪等に再実行してください。

Useful? React with 👍 / 👎.

**コメント要約**: room を inactive に保存後、membership 更新の途中で失敗すると、retry 時に `!room.is_active` で即 return し残りの membership が is_active=true のまま残る。room inactive でも membership 同期は冪等に再実行すべき。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ, 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 現行 16-19 行で room already inactive 時に early return。membership 更新の部分失敗→retry で残り membership が同期されない窓がある。前セッション RC-27 は「冪等＋throw で再試行可」と判断したが、本指摘は early return が membership 側の冪等性を壊す点をより具体化したもの。room の状態と membership 同期の判定を分離するのが妥当。**今回は対応せず（要確認）。理由: room/membership 状態分離の設計判断が必要で、微修正スコープ外。**

---

**識別子**: RC-57（GitHub id: 3403408202）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:18`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,103 @@
+import { onDocumentCreated } from 'firebase-functions/v2/firestore'
+...
+export const onChatMessageCreated = onDocumentCreated('chat_rooms/{roomId}/messages/{messageId}', async (event) => {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  メッセージ作成 Trigger の retry を有効にしてください**

仕様では Trigger 失敗時を Functions の retry に任せる前提ですが、この `onDocumentCreated` は path だけを渡しており `retry: true` が指定されていません。メッセージ作成後に一時的な Firestore エラーやタイムアウトが起きると再配信されず、該当メッセージの last_message / unread 更新が欠落するため、Firestore v2 trigger の options で retry を明示してください。

**コメント要約**: `onDocumentCreated` に `retry: true` がなく、一時エラー時に再配信されず last_message/unread 更新が欠落する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: 現行 19-20 行は `onDocumentCreated({ document: 'chat_rooms/{roomId}/messages/{messageId}', retry: true }, ...)` で retry を明示済み。指摘は解消済み。

---

**識別子**: RC-58（GitHub id: 3403408186）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:31`

**該当コード（レビュー時点の diff）**:

```diff
+  const claimed = await db.runTransaction(async (transaction) => {
+    return claimMessageProcessed(roomId, messageId, transaction)
+  })
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  processed 取得後の失敗で再処理不能にしないでください**

ここで先に `processed: true` を書いてから room / membership 更新へ進むため、claim 後に一時的な Firestore エラーやタイムアウトが起きると、次回リトライは `claimed` が false になって即 return し、そのメッセージの最終プレビュー・未読数更新が永久に欠落します。既存コメントとは別に、現在の差分では副作用完了前に再実行ガードだけが永続化されていることを確認しました。副作用と同じ冪等状態で完了扱いにするか、失敗時に再実行できる状態管理にしてください。

**コメント要約**: claim（processed:true）を副作用より先に確定するため、claim 後に失敗するとリトライで claimed=false となり未読等が再計上されない。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: 現行は claim 後 `!claimed` で early return せず冪等な副作用（last_message は比較で再適用、未読は last_read_at ガード）を再実行する設計だが、未読加算は `shouldIncrementMembershipUnread` が `claimed` を条件にしているため、claim 成功直後・batch commit 前にクラッシュすると retry（claimed=false）で未読が加算されない窓が残る。RC-61 の FieldValue.increment 原子化と併せた設計見直しが必要。プレビュー欠落の指摘部分は現行では再適用されるため誤り。

---

**識別子**: RC-59（GitHub id: 3403408205）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:60`

**該当コード（レビュー時点の diff）**:

```diff
+    const shouldApplyLastMessage = lastMessageAt >= (room.last_message_at ?? 0)
+    ...
+    const shouldIncrementUnread = message.message_type === 'user' && shouldApplyLastMessage
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  古い順で処理されないメッセージも未読加算してください**

複数のユーザーメッセージが短時間に作られて新しい方の Trigger が先に room の `last_message_at` を進めると、後から処理された古い方は `shouldApplyLastMessage` が false になり、本文自体は未読なのに受信者の `unread_count` が加算されません。最終メッセージ更新の可否と未読加算は分け、送信者以外にはメッセージごとに加算されるようにしてください。

**コメント要約**: 未読加算が `shouldApplyLastMessage` に紐づくため、順序逆転で後処理される古いメッセージの未読が加算されない。last_message 更新と未読加算を分離すべき。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ, 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 現行 `shouldIncrementMembershipUnread` 117 行が `!shouldApplyLastMessage` で未読加算を抑止。順序逆転時に未読が漏れる点は現コードでも該当。未読加算は `last_read_at < created_at` を条件に独立させるのが妥当。RC-60 と同一論点。**今回は対応せず（要確認）。理由: RC-61（非原子的 increment）とセットで扱うべき。単独修正は二重加算リスクがある。**

---

**識別子**: RC-60（GitHub id: 3403558669）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:74`

**該当コード（レビュー時点の diff）**:

```diff
+        if (!shouldApplyLastMessage) {
+          continue
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  古い順で届いた未読も加算してください**

複数メッセージのトリガーが作成順どおりに処理される保証はないため、新しいメッセージが先に `room.last_message_at` を進めた後で古いメッセージのトリガーが来ると、ここで membership 更新ごとスキップされ、その古いメッセージ分の `unread_count` が増えません。プレビュー更新だけを `shouldApplyLastMessage` で抑止し、未読加算は `last_read_at < message.created_at` なら実行するよう分離してください。

**コメント要約**: RC-59 と同一。`if (!shouldApplyLastMessage) continue` が membership 更新ごとスキップし未読加算も止まる。プレビュー抑止と未読加算を分離すべき。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ, 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: RC-59 と同一論点。現行 73-75 行の `continue` が未読加算ごとスキップしている。判断・対応方針を共有する。**今回は対応せず（要確認）。理由: RC-61（非原子的 increment）とセットで扱うべき。単独修正は二重加算リスクがある。**

---

**識別子**: RC-61（GitHub id: 3403558681）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/onChatMessageCreated.ts:96`

**該当コード（レビュー時点の diff）**:

```diff
+        if (
+          shouldIncrementMembershipUnread({ ... })
+        ) {
+          updated = incrementMembershipUnread(updated)
+        }
+
+        const ref = getChatMembershipRef(memberUserId, roomId)
+        batch.set(ref, updated, { merge: true })
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  未読加算を原子的に更新してください**

同じルームで複数メッセージがほぼ同時に作成されると、各 trigger が同じ `membership.unread_count` を読んで `incrementMembershipUnread` した値を `batch.set` するため、後勝ちで片方の加算が失われます。たとえば未読 0 の受信者に 2 件連続投稿されると両方が 1 を書き、バッジが 1 件少なくなるので、membership ごとに transaction / `FieldValue.increment` 相当の原子的な加算にしてください。

**コメント要約**: `unread_count` を read→incrementMembershipUnread→batch.set する非原子的更新のため、同時投稿で加算が後勝ちロスする。FieldValue.increment 等で原子化すべき。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: 現行 77-97 行は `membership.unread_count` を読んだ値ベースで `batch.set`。同時 trigger で lost update が起きうる。`FieldValue.increment(1)` への置換で原子化が妥当。RC-58 の claim 後失敗時の未読欠落も、increment 化＋created_at 条件で last_message を別管理すれば併せて解消できる。

---

**識別子**: RC-62（GitHub id: 3403550603）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/recallChatMessage.ts:2`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,117 @@
+import { HttpsError, onCall } from 'firebase-functions/v2/https'
+import { getFirestore } from 'firebase-admin/firestore'
```

**レビュワーのコメント（原文）**:

[must] 上記の `FieldValue` / `Timestamp` を使用するため、`firebase-admin/firestore` の import を追加してください。

**コメント要約**: `FieldValue` / `Timestamp` を使う前提で import 追加を要求。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 現行 `recallChatMessage.ts` は `FieldValue` / `Timestamp` を使用していない（`getFirestore`・`saveChatMessage(set)` ベース）。RC-67 の tombstone 化（update + FieldValue.delete）を採用する場合のみ必要になる派生提案。現状コードでは不要。

---

**識別子**: RC-63（GitHub id: 3403550625）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/recallChatMessage.ts:16`

**該当コード（レビュー時点の diff）**:

```diff
+import {
+  getChatMessage,
+  listRecentChatMessages,
+  markChatMessageDeleted,
+  saveChatMessage,
+} from './stores/chatMessage.js'
```

**レビュワーのコメント（原文）**:

[must] 送信取り消し実装を `update` ベースに切り替える場合、`markChatMessageDeleted` / `saveChatMessage` は未使用になります。lint エラー回避のため import を整理してください。

**コメント要約**: update ベース実装に切り替えると `markChatMessageDeleted` / `saveChatMessage` が未使用になるため import 整理を要求。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 「update ベースに切り替える場合」を前提とした条件付き指摘。現行 101-105 行で `markChatMessageDeleted` / `saveChatMessage` は使用中であり未使用 import ではない。RC-67 の対応を採用した場合にのみ再検討。

---

**識別子**: RC-64（GitHub id: 3403408192）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/recallChatMessage.ts:35`

**該当コード（レビュー時点の diff）**:

```diff
+  const messages = await listRecentChatMessages(roomId, 50)
+  const { preview, lastMessageAt } = resolveLastMessagePreviewFromMessages(messages)
+
+  const updatedRoom = updateChatRoomLastMessage(room, { preview, lastMessageAt }, { force: true })
+  if (updatedRoom != null) {
+    await saveChatRoom(updatedRoom)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  取り消し再計算で新しい最終メッセージを戻さないでください**

最新メッセージ取り消しの再計算中に、`listRecentChatMessages()` の直後へ別ユーザーの新着が入り、その新着 Trigger が先に `last_message_at` を更新した場合でも、この `force: true` の保存が古い再計算結果で room / memberships の最終メッセージを上書きできます。取り消し時の再計算も現在の `last_message_at` と比較するトランザクションにするなど、新着のほうが新しい場合は書き戻さないようにしてください。

**コメント要約**: 取り消し再計算が `force: true` で last_message_at の比較をスキップするため、再計算中に入った新着を古い結果で上書きしうる。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 現行 33・50 行が `force: true` で last_message_at の比較を無効化。再計算中の新着とのレースで古い preview に巻き戻る可能性がある。最新メッセージ取り消し時のみ実行される（108-110 行）とはいえ、`listRecentChatMessages` と save の間に窓がある。transaction 化または created_at 比較が望ましい。低頻度の edge case のため ❓ 要確認。**今回は対応せず（要確認）。理由: 低頻度 race の transaction 化は微修正の範囲外。**

---

**識別子**: RC-65（GitHub id: 3403408197）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/recallChatMessage.ts:90`

**該当コード（レビュー時点の diff）**:

```diff
+    if (message.sender_user_id !== uid) {
+      throw new HttpsError('permission-denied', '自分のメッセージのみ取り消せます')
+    }
+
+    const room = await getChatRoom(roomId)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  離脱済みユーザーの取り消しを拒否してください**

注文キャンセルなどで `member_user_ids` と `chat_memberships` から外れた後でも、過去に自分が送った `room_id` / `message_id` を知っていれば、この Callable は送信者一致と room が active であることだけで本文削除と last_message 再計算を実行できます。Security Rules 上は離脱後に同じ room を読めない設計なので、`room.member_user_ids` に現在の uid が含まれることも検証してから取り消しを許可してください。

**コメント要約**: 離脱（注文キャンセル）後のユーザーでも sender 一致＋room active だけで過去メッセージを取り消せる。`room.member_user_ids.includes(uid)` の検証が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: `is_active` チェックの後に `room.member_user_ids.includes(uid)` の在籍検証を追加。離脱/非メンバーが過去の自分のメッセージを取り消す経路を塞ぎ、Security Rules のメンバー限定と整合した。RC-66 と同一対応。

---

**識別子**: RC-66（GitHub id: 3403558686）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/recallChatMessage.ts:96`

**該当コード（レビュー時点の diff）**:

```diff
+    const room = await getChatRoom(roomId)
+    if (room == null) {
+      throw new HttpsError('not-found', 'チャットルームが見つかりません')
+    }
+    if (!room.is_active) {
+      throw new HttpsError('failed-precondition', 'このチャットは終了しています')
+    }
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  非メンバーの取り消しを拒否してください**

送信後に注文キャンセルや退会で `member_user_ids` / membership から外れたユーザーでも、過去に送った `room_id` と `message_id` を保持していればこの Callable で active なルーム内の自分の過去メッセージを取り消せます。Firestore Rules 側の read/write ではルームメンバーに限定しているので、Callable でも `room.member_user_ids.includes(uid)` を確認して、現在アクセス権のないチャットを変更できないようにしてください。

**コメント要約**: RC-65 と同一。非メンバーが過去の自分のメッセージを取り消せる。`member_user_ids.includes(uid)` 検証を追加すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: RC-65 と同一対応。`member_user_ids.includes(uid)` 在籍チェックを追加した。

---

**識別子**: RC-67（GitHub id: 3403550582）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/recallChatMessage.ts:105`

**該当コード（レビュー時点の diff）**:

```diff
+    const deletedMessage = markChatMessageDeleted(message, {
+      deletedByUserId: uid,
+      deletedDisplayName,
+    })
+    await saveChatMessage(roomId, deletedMessage)
```

**レビュワーのコメント（原文）**:

[must] 送信取り消しで `saveChatMessage()`（= set で全上書き）しているため、トリガーの冪等性用 `processed` フィールド等、将来追加/内部用フィールドを消してしまいます。`body` は確実に削除しつつ、それ以外の既存フィールドは保持するように `update` + `FieldValue.delete()` で tombstone 化してください。

**コメント要約**: 取り消しで `saveChatMessage`（set 全上書き）するため、`processed` 等の内部/将来フィールドが消える。`update` + `FieldValue.delete()` で body のみ削除する tombstone 化を推奨。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: 現行 `saveChatMessage` は `ref.set(message)` で全上書き、`markChatMessageDeleted` は processed を持たない ChatMessage を生成するため processed は消える。ただし `onChatMessageCreated` は create トリガーで、set は update イベントのため再発火しない＝現状は機能的に無害。将来フィールド追加時の堅牢性として update + FieldValue.delete 化は妥当だが、必須ではないため ❓ 要確認。

---

**識別子**: RC-68（GitHub id: 3409341501）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/stores/chatMembership.ts:77`

**該当コード（レビュー時点の diff）**:

```diff
+  return new ChatMembership(params.roomId, {
+    room_id: params.roomId,
+    room_type: 'event',
+    is_active: params.isActive,
+    unread_count: 0,
+    last_message_at: lastMessageAt,
+    last_message_preview: params.lastMessagePreview,
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  undefined の preview を保存しないでください**

新規イベントチャット作成時など既存の最終メッセージがない場合、`params.lastMessagePreview` は `undefined` ですが、この行で `last_message_preview` キーを明示的に渡すため `ChatMembership` に undefined フィールドが残ります。`toFirestore()` は `convertToDb` で `...membership` をそのまま返し、このリポジトリには `ignoreUndefinedProperties` 設定も見当たらないため、`saveChatMembership(...).set(..., { merge: true })` が Firestore の "Cannot use undefined as a Firestore value" で失敗し、最初の参加者の membership/room 作成ができなくなります。

**コメント要約**: `last_message_preview` に undefined を渡すと `toFirestore()` が undefined を残し、Firestore 保存が "Cannot use undefined" で失敗するとの指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 誤指摘。`common/src/schemas/ChatMembership.ts` の `convertToDb`（35 行）は `Object.fromEntries(Object.entries(base).filter(([, v]) => v !== undefined))` で undefined フィールドを除去している。`toFirestore()` はその結果を `ChatMembershipDbSchema.parse` するため、undefined の `last_message_preview` は保存対象に含まれず "Cannot use undefined" にはならない。

---

**識別子**: RC-69（GitHub id: 3409341503）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/syncEventChatMember.ts:156`

**該当コード（レビュー時点の diff）**:

```diff
+  try {
+    if (freshEvent.members.includes(userId)) {
+      ...
+      await joinEventChatMember(freshEvent, userId)
+    } else {
+      await leaveEventChatMember(freshEvent, userId)
+    }
+  } catch (error) {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  チャット同期失敗を成功扱いにしないでください**

この `catch` が例外を再 throw せずに終了するため、注文確定/キャンセル後の `joinEventChatMember` や `leaveEventChatMember` が一時的な Firestore エラーで失敗しても呼び出し側では成功扱いになり、chat membership の作成・削除が再試行されません。その場合、注文は確定済みなのにユーザーだけチャットに入れない、またはキャンセル後も一覧に残る状態が次の注文更新まで固定されるので、失敗を上位へ伝えてリトライさせるか、永続的な再同期キューに逃がしてください。

**コメント要約**: `syncEventChatMember` の catch がログのみで再 throw しないため、同期失敗が呼び出し側で成功扱いになり、membership の作成/削除が再試行されない。

**判断結果**: 🟡 修正提案

**PRスコープ**: ❓ 要確認

**ラベル**: 💾 データ

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 現行 156-164 行は error をログ出力のみで握り潰す。一時的な Firestore エラーで join/leave に失敗しても呼び出し側（orderConfirmed/CanceledSideEffects）は成功扱いになり、ユーザーがチャットに入れない/退出できない不整合が次の注文更新まで残る。ただし注文確定フロー自体を chat 同期失敗で巻き戻さないための意図的な握り潰しの可能性もあるため、再 throw か再同期キューかは設計判断として ❓ 要確認。**今回は対応せず（要確認）。理由: 注文確定を chat 同期失敗で巻き戻さない意図の可能性があり、設計判断が必要。**

---

## 評価セッション（2026-06-15 13:38 JST・shokujii-code-review）

- **評価日時**: 2026-06-15 13:38 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: feat/955
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2058
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし
- **対象**: Phase 3.1 画像添付（未コミット差分）。common スキーマ / Firestore・Storage rules / functions chatPreview / base store・UI・composable

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-70 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | ChatAttachmentImage.vue 新設で deep watch 撤去<br>添付取得を子コンポーネント自己完結化 |
| [x] | RC-71 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | — | ChatAttachmentImage.vue で getUrl 多重呼び出し解消<br>非 null アサーション撤去 |
| [x] | RC-72 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | — | buildAttachment に型ガード済み contentType を渡す<br>as キャスト撤去 |
| [x] | RC-73 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | 画像送信 setDoc が withConverter 非経由<br>既存 addDoc と同一パターン。rules で検証 |
| [x] | RC-74 | なし | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📋 仕様追加 | — | isValidChatAttachmentPath に content_type/byte_size 検証追加<br>Storage rules と整合 |
| [x] | RC-75 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | Storage read ごとに firestore.get 1 read<br>メンバー限定 read の設計上必要なコスト |

---

**識別子**: RC-70（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/components/chat/ChatLog.vue:164`

**該当コード（レビュー時点の diff）**:

```diff
+watch(
+  () => store.messages,
+  (messages) => {
+    syncAttachmentUrls(messages)
+  },
+  { immediate: true, deep: true },
+)
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📐リファクタ/M]: `base/src/components/chat/ChatLog.vue` の添付 URL 同期が `watch(() => store.messages, ..., { deep: true })` で全メッセージ配列を深い監視している。メッセージ追加・既読更新などのたびに O(n) で走り、チェックリストの「watch の多用／パフォーマンス」に該当する。
→ 既存の `collectSenderIds` と同様に「添付を持つ `messageId + storage_path` のキー一覧」を返す関数を作り、それを浅く監視する（または既存の senderIds 監視とまとめる）と軽くなる。`loadAttachment` 自体がキーで de-dup しているので、キー一覧の変化だけ拾えば十分。

**コメント要約**: 添付 URL 同期が `store.messages` の deep watch で全件走査される。
既存の senderIds 監視と同様、添付キー一覧の浅い watch に変更するとパフォーマンスが改善する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: —

**判断理由**: `ChatAttachmentImage.vue` を新設し、添付 Blob 取得・Object URL 管理を子コンポーネントに移管。`ChatLog.vue` から `useChatAttachmentUrls` と deep watch を削除済み。

---

**識別子**: RC-71（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/components/chat/ChatLog.vue:260`

**該当コード（レビュー時点の diff）**:

```diff
+              <button
+                v-else-if="getUrl(message.id, attachment) != null"
+                type="button"
+                class="chat-attachment-button"
+                @click="openExpandedImage(getUrl(message.id, attachment)!, attachment.file_name)"
+              >
+                <VImg
+                  :src="getUrl(message.id, attachment)"
+                  :alt="attachment.file_name"
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `ChatLog.vue` テンプレートで `getUrl(message.id, attachment)` を `v-else-if` / `:src` / `@click` の 3 箇所で都度呼び、さらに `getUrl(...)!` で非 null アサーションしている。可読性・再評価コストの両面で冗長。
→ `attachment` ループ内で `const url = getUrl(...)` を算出する小コンポーネント（例: `ChatAttachmentImage.vue`）に切り出すか、メッセージ単位で URL を解決した派生データを使うと `!` も消せる。

**コメント要約**: テンプレート内で `getUrl` を 3 回呼び出し、非 null アサーション `!` を使用している。
小コンポーネント化または派生データ化で可読性と再評価コストを改善できる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: —

**判断理由**: `ChatAttachmentImage.vue` に表示・expand emit を集約し、テンプレート内の `getUrl` 多重呼び出しと非 null アサーションを撤去済み。

---

**識別子**: RC-72（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/stores/chat.ts:456`

**該当コード（レビュー時点の diff）**:

```diff
+  const buildAttachment = async (roomId: string, messageId: string, imageFile: File): Promise<ChatAttachment> => {
+    const { width, height } = await getImageDimensions(imageFile)
+    const attachmentId = crypto.randomUUID()
+    const storagePath = getChatAttachmentStoragePath(roomId, messageId, attachmentId)
+    return {
+      storage_path: storagePath,
+      content_type: imageFile.type as ChatAttachment['content_type'],
+      file_name: imageFile.name,
+      byte_size: imageFile.size,
+      width,
+      height,
+    }
+  }
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📐リファクタ/S]: `base/src/stores/chat.ts` の `buildAttachment` で `content_type: imageFile.type as ChatAttachment['content_type']` と `as` キャストを使っている。直前に型ガード `isAllowedChatAttachmentMimeType` があるので、これを活かせばキャストを排除できる。
→ `sendMessage` 側で `const contentType = imageFile.type; if (!isAllowedChatAttachmentMimeType(contentType)) throw ...` とローカル const に対してガードすれば `contentType` が `ChatAttachmentImageMimeType` に絞り込まれる。これを `buildAttachment(roomId, messageId, imageFile, contentType)` に渡せば `as` 不要。

**コメント要約**: `buildAttachment` で MIME 型に `as` キャストしている。
`sendMessage` 側で型ガード後の `contentType` を引数で渡せばキャスト不要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: —

**判断理由**: `sendMessage` で型ガード済み `contentType` を `buildAttachment` 第 4 引数に渡し、`as` キャストを撤去済み。

---

**識別子**: RC-73（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/stores/chat.ts:516`

**該当コード（レビュー時点の diff）**:

```diff
+      const payload: Record<string, unknown> = {
+        message_type: 'user',
+        sender_user_id: userId,
+        attachments: [attachment],
+        created_at: serverTimestamp(),
+      }
+      if (hasBody) {
+        payload.body = trimmedBody
+      }
+      await setDoc(messageRef, payload)
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: `base/src/stores/chat.ts` の `setDoc(messageRef, payload)` が plain object リテラルで書き込み、`withConverter`（ChatMessage の Zod 検証）を経由していない点。チェックリストの「withConverter 必須」に形式上ひっかかるが、既存の `addDoc(collection(...), {...})` と完全に同一の確立済みパターンであり、書き込み内容は `firestore.rules` で検証される。本 PR スコープでの新規逸脱ではないため対応不要（将来 converter 化するなら別 Issue が適切）。

**コメント要約**: 画像送信の `setDoc` が withConverter 非経由で plain object を書き込んでいる。
既存テキスト送信の `addDoc` と同一パターン。Firestore rules で検証されるため対応不要。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: テキストのみ送信（`addDoc` without converter）と整合。messageId 先採番の都合上 `setDoc` が必要で、rules 側で attachments の shape を検証している。converter 経由化は別 Issue でよい。

---

**識別子**: RC-74（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `firestore.rules:78`

**該当コード（レビュー時点の diff）**:

```diff
+        function isValidChatAttachmentPath(roomId, messageId) {
+            let prefix = 'chat_rooms/' + roomId + '/' + messageId + '/';
+            let attachment = request.resource.data.attachments[0];
+            return attachment.storage_path is string
+                && attachment.storage_path.matches('^' + prefix + '.+')
+                && attachment.content_type is string
+                && attachment.file_name is string
+                && attachment.byte_size is int
+                && attachment.width is int
+                && attachment.height is int;
+        }
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📋仕様追加/S]: `firestore.rules` の `isValidChatAttachmentPath` は型（`is string` / `is int`）と `storage_path` の prefix だけを検証し、`content_type` のホワイトリストと `byte_size <= 10MB` を見ていない。実体アップロードは `storage.rules` で MIME/サイズを強制するので実害は小さいが、Firestore ドキュメント単体では任意の `content_type` 文字列や巨大な `byte_size` を書けてしまう。
→ 多層防御として `attachment.content_type in ['image/png', ...]` と `attachment.byte_size <= 10*1024*1024` を rule に追加すると、Firestore レコードと Storage 制約が一致する。計画では「最低ライン: 枚数 + prefix 一致」と明記されているため必須ではない。

**コメント要約**: Firestore rules が attachments の content_type ホワイトリストと byte_size 上限を検証していない。
Storage rules で実体は保護されるが、多層防御として Firestore 側にも追加するとよい。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 📋 仕様追加

**想定工数**: —

**判断理由**: `isValidChatAttachmentPath` に `content_type` ホワイトリストと `byte_size > 0 && byte_size <= 10MB` を追加済み。

---

**識別子**: RC-75（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `storage.rules:51`

**該当コード（レビュー時点の diff）**:

```diff
+    match /chat_rooms/{roomId}/{messageId}/{attachmentId} {
+      allow read: if isChatRoomMemberStorage(roomId);
+      allow create: if isChatRoomWritableStorage(roomId)
+        && request.resource.size < 10 * 1024 * 1024
+        && isAllowedChatImageType();
+      allow update, delete: if false;
+    }
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: `storage.rules` の read 判定が画像 1 枚取得ごとに `firestore.get(chat_rooms/{roomId})` を 1 read 消費する点。メンバー限定 read の担保として必要なコストで、`getBlob` 方式（`getDownloadURL` 不採用）の設計とも整合。許容範囲。

**コメント要約**: Storage read ごとに `firestore.get` で chat_rooms を参照し Firestore read が発生する。
メンバー限定 read を強制する設計上のトレードオフ。getDownloadURL 不採用方針と整合。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: Phase 3.1 仕様 §1.4 で `getBytes`/`getBlob` + Object URL を採用し `getDownloadURL` を不採用としている。Storage rules によるメンバー検証はこの設計の前提コスト。最適化（キャッシュ等）は Phase 3.2 以降で検討可。

---
