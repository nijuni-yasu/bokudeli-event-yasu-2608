# PR #2167 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-07-06 16:46 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: feat/2162
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2167
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 5（Bugbot 未有効、Codex usage limits×2、レビュー依頼定型文、Copilot 承知返信、Codex 接続案内）
- **partial evaluate**: true（Codex substantive レビューなし）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 3527153319 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | `/profile` 連携戻りで前回ログイン provider が上書きされる<br>`/profile` 早期 return 後に recordLastLoginFromCredential を移動 |

---

**識別子**: RC-1（GitHub id: 3527153319）

**レビュワー**: Copilot

**指摘箇所**: `user/src/router/index.ts:271`

**該当コード（レビュー時点の diff）**:

```diff
@@ -265,6 +266,10 @@ export const setupRouter = (router: Router) => {
         return await handleProfileUpdateFailure(to.path, to.query, userCredential)
       }
 
+      if (userCredential != null) {
+        recordLastLoginFromCredential(userCredential)
+      }
```

**レビュワーのコメント（原文）**:

`/profile` はアカウント連携（link）の戻りとして扱っているのに、この位置で `recordLastLoginFromCredential` を呼ぶと「前回ログイン」が“連携に使った provider”で上書きされます（ログイン成功方法の記録にならない）。`/profile` 分岐の return 後に移動するか、`to.path !== '/profile'` 条件を付けてください。

**コメント要約**: アカウント連携の redirect 復帰（`/profile`）でも recordLastLoginFromCredential が実行され、前回ログイン表示が連携 provider で上書きされる。保存処理を `/profile` 早期 return の後へ移動するか、path 条件で除外すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: router 内コメントでも `/profile` はリンク用途と明記されており、連携フローとログインフローは別。指摘どおり保存位置をずらせば意図した UX（ログイン成功手段のみ記録）に合致する。データ破壊ではないが、前回ログインバッジの表示が誤るため修正推奨。

**対応内容**: `recordLastLoginFromCredential` を `/profile` 早期 return の後へ移動（2026-07-06）。

---
