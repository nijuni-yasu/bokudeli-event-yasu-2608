# PR #1845 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-11 22:48 JST
- **ブランチ名**: fix/1834
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/1845 (#1845)
- **Outdated 除外件数**: 0 件（API 取得時点で `position != null` のもののみ。古いコメントも `position` を持っているため評価対象に含める）

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: 2938584492）— ✅ 対応済み（`async_payment_succeeded` / `async_payment_failed` ハンドラを実装）
- [x] **RC-2**（GitHub id: 2938601800）— ✅ 対応済み（`stripeRefunds.ts` 削除、`cancelOrders.ts` で `instanceof Stripe.errors.StripeError` 分岐済み）
- [x] **RC-3**（GitHub id: 2938601844）— ✅ 対応済み（`payment_method_types` を削除し動的設定へ移行）
- [x] **RC-4**（GitHub id: 2945803693）— ✅ 対応済み（`optionalDeleteField` で `undefined` → `FieldValue.delete()` 自動変換）
- [x] **RC-5**（GitHub id: 2945803746）— ✅ 対応済み（`UserSuccessJoinEventDialog.vue` に `isLoadingOrder`・ローディング UI・閉じる導線を追加）
- [x] **RC-6**（GitHub id: 2945803773）— ✅ 対応済み（`subscribeOrderForUser` 自体が削除され `eventStore.orders` 経由に変更）
- [x] **RC-7**（GitHub id: 2945803791）— ✅ 対応済み（`functions/default/src/stripe.ts` で `{ url }` のみ返却、`onCall` の第2型引数で `CreateStripeCheckoutSessionResponse` を明示）
- [x] **RC-8**（GitHub id: 2945805033）— ✅ 対応済み（`subscribeOrderForUser` 自体が削除）
- [x] **RC-9**（GitHub id: 2945805041）— ✅ 対応済み（現在の実装は `event` が存在すれば常に閉じるボタンを描画）
- [x] **RC-10**（GitHub id: 3166129171）— ✅ 対応済み（`optionalDeleteField` で対応）
- [ ] **RC-11**（GitHub id: 3218545219）— 👌 非対応（本 PR では見送り。P2・同一イベントに複数 `processing` が長期残存するレアケース向けの厳密化は別途検討可）
- [ ] **RC-12**（GitHub id: 3218860434）— 👌 非対応（RC-11 と同根のため RC-11 と同様に見送り）
- [x] **RC-13**（GitHub id: 3219298077）— ✅ 対応済み（`createOrder` を `set(..., { merge: true })` に変更し、新規 doc 作成時も `FieldValue.delete()` を許容する）
- [x] **RC-14**（GitHub id: 3219298082）— ✅ 対応済み（`isLoadingOrder` で watch をガード、1 秒待機後に `isProcessing` 等を再チェックしてからシェアプロンプト表示）
- [x] **RC-15**（GitHub id: 3220379003）— ✅ 対応済み（`failed_async_payment_intent` で遅延 `completed` ガード）
- [x] **RC-16**（GitHub トップコメント id: 4420697229）— ✅ 対応済み（must は optionalDeleteField・isLoadingOrder・url 返却等で解消。isPosted の imo は 👌 修正不要）
- [x] **RC-17**（GitHub トップコメント id: 4422466703）— ✅ 対応済み／👌 任意（再レビューで肯定。merge 理由コメント・member.vue exhaustive は任意）
- [x] **RC-18**（GitHub id: 3220328261）— ✅ 対応済み（実装者返信・isLoadingOrder 対応の表明）
- [x] **RC-19**（GitHub id: 3220329505）— ✅ 対応済み（実装者返信・閉じる導線の表明）
- [x] **RC-20**（GitHub id: 3220329939）— 👌 修正不要（P2・見送り方針。RC-11 と整合）
- [x] **RC-21**（GitHub id: 3220330110）— 👌 修正不要（RC-11 と同根・見送り。RC-12 と整合）
- [x] **RC-22**（GitHub id: 3220330434）— ✅ 対応済み（実装者返信・watch / シェアダイアログの表明）
- [x] **RC-23**（GitHub id: 3220328451）— ✅ 対応済み（実装者返信・subscribeOrderForUser 削除の表明）
- [x] **RC-24**（GitHub id: 3220329297）— ✅ 対応済み（実装者返信・設計変更と P2 見送りの表明）
- [x] **RC-25**（GitHub id: 3220330283）— ✅ 対応済み（実装者返信・createOrder merge true の表明）
- [x] **RC-26**（GitHub id: 3220327311）— ✅ 対応済み（実装者返信・async_payment ハンドラの表明）
- [x] **RC-27**（GitHub id: 3220327798）— ✅ 対応済み（実装者返信・payment_method_types 削除の表明）
- [x] **RC-28**（GitHub id: 3220328619）— ✅ 対応済み（実装者返信・stripe onCall 型・url のみ返却の表明）
- [x] **RC-29**（GitHub id: 3220327509）— ✅ 対応済み（実装者返信・stripeRefunds 削除・cancelOrders 分岐の表明）
- [x] **RC-30**（GitHub id: 3220328003）— ✅ 対応済み（実装者返信・optionalDeleteField の表明）
- [x] **RC-31**（GitHub id: 3220329683）— ✅ 対応済み（実装者返信・FieldValue.delete 相当の表明）

---

**識別子**: RC-1（GitHub id: 2938584492）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  PayPay追加時は非同期決済イベントを処理してください**

`payment_method_types` に `paypay` を追加したことで Checkout が非同期決済を取りうるようになりましたが、現状の Webhook は `checkout.session.completed` のみを処理し（`functions/default/src/stripeWebhook.ts:34-38`）、`payment_status` を確認せず `order.status = 'ordered'` に更新しています（同 `76-79`）。このままだと PayPay 選択時に支払い未確定または失敗ケースでも注文を確定してしまい、注文状態と入金状態が不整合になります。

Useful? React with 👍 / 👎.
```

**コメント要約**: PayPay は非同期決済になり得るため、`async_payment_succeeded` / `async_payment_failed` を含む Webhook ハンドリングを追加すべき。

**判断結果**: ✅ 対応済み

**判断理由**: PR #1845 で `stripeWebhook.ts` を全面改修し、`HANDLED_EVENT_TYPES` に `async_payment_succeeded` / `async_payment_failed` を追加。`payment_status === 'unpaid'` の場合は `handleAsyncPaymentPending` で `processing` に、成功時は `handleOrderConfirmation` で `ordered` に、失敗時は `handleAsyncPaymentFailed` で `in_cart` に巻き戻すフローを実装済み。仕様書 `documents/01_マネタイズと決済/06_PayPay決済_遅延決済.md` にも明記。

---

**識別子**: RC-2（GitHub id: 2938601800）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `...(isStripeError && { ... })` は `false | object` をスプレッドしてしまうため、TypeScript 的にエラーになりやすく（また可読性も落ちます）、`error.type/code/requestId` へのアクセスも型安全に担保できません。`if (error instanceof Stripe.errors.StripeError) { ... }` で分岐してログ用のオブジェクトを組み立てるか、三項演算子で `{}` を返す形にして、StripeError の時だけ追加フィールドを載せるようにしてください。
```

**コメント要約**: `...(isStripeError && { ... })` の Spread 構文は型安全でない。`if (error instanceof Stripe.errors.StripeError)` 分岐に書き換えるべき。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘対象の `stripeRefunds.ts` は PR で削除済み。同等のロジックは `functions/default/src/cancelOrders.ts` L230-243 にあり、レビュー指摘どおり `if (error instanceof Stripe.errors.StripeError) { errorContext.stripeErrorType = ...; ... }` という型ガード方式で実装済み。

---

**識別子**: RC-3（GitHub id: 2938601844）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `payment_method_types` の `as Stripe.Checkout.SessionCreateParams['payment_method_types']` キャストは型チェックを無効化してしまい、文字列の打ち間違い等をコンパイル時に検出できなくなります。可能なら Stripe SDK/型定義の更新でキャスト無しにするか、少なくとも「このキャストが必要な理由（現行の Stripe 型に paypay が無い等）」をコメントで残して、将来の保守時に不要なキャストが残り続けないようにしてください。
```

**コメント要約**: `payment_method_types` の型キャストを避けるか、必要理由をコメントで残す。

**判断結果**: ✅ 対応済み

**判断理由**: PR #1845 で `payment_method_types` 自体をコードから削除し、Stripe ダッシュボードの Payment Methods 設定に決済方法選択を委譲した（Stripe 推奨の dynamic payment methods 方式）。Stripe Node SDK も v20.4.1 へアップグレード済みのため、そもそも `paypay` の型は最新版で定義されており、キャストの必要性自体が消滅している。

---

**識別子**: RC-4（GitHub id: 2945803693）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] async_payment_failed で `order.payment_intent = undefined` を `saveOrder(..., { merge: true })` に渡すと、Firestore Admin SDK 側で `undefined` フィールドが含まれてエラーになる可能性が高いです（ignoreUndefinedProperties 設定が見当たりません）。payment_intent を消したい場合は FieldValue.delete() 相当で削除するか、converter/toFirestore 側で undefined キーを落とす形にしてください。
```

**コメント要約**: `payment_intent = undefined` を `merge: true` set に渡すと `Cannot use 'undefined'` エラーになる。`FieldValue.delete()` か converter での除去で対応すべき。

**判断結果**: ✅ 対応済み

**判断理由**: 直近のコミットで `common/src/schemas/EventMemberOrder.ts` の `EventMemberOrderDbSchema` の `processing_payment_intent` / `processing_at` を `optionalDeleteField(...)` でラップ。`undefined` を自動的に `FieldValue.delete()` に変換するため、`saveOrder` 側のコードはそのまま動作する。

---

**識別子**: RC-5（GitHub id: 2945803746）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `props.userId` が渡されているケースで、注文がまだ読めていない/存在しない（`order === null`）場合に `isLoadingOrder` でも `isProcessing` でも `showFullContent` でもなくなり、ダイアログ本文が何も描画されません。注文未取得/未作成/権限不足などのフォールバック表示（例: エラーメッセージ＋閉じるボタン、または一定時間はローディング継続）を追加してください。
```

**コメント要約**: 注文未読込時のフォールバック表示（スピナーや閉じる導線）が必要。

**判断結果**: ✅ 対応済み

**判断理由**: `base/src/components/UserSuccessJoinEventDialog.vue` で `sessionId` / `userId` ありかつ `eventStore.orders == null` のとき `isLoadingOrder` を true にし、スピナー＋`success_join_event_dialog.loading`（`base/src/locales/messages/ja.ts` 追加）と閉じるボタンを表示。本カード本文の flash を抑止する。`isProcessing` は `orders == null` の間は false とし空配列への誤フォールバックをやめた（RC-14 の watch 対応と一体）。

---

**識別子**: RC-6（GitHub id: 2945803773）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `subscribeOrderForUser` は該当注文が1件に決まっている前提なら、クエリに `limit(1)` を付けて読み取り/監視コストを抑えた方が安全です（重複データが発生した場合の挙動も明確になります）。
```

**コメント要約**: `subscribeOrderForUser` のクエリに `limit(1)` を付ける。

**判断結果**: ✅ 対応済み

**判断理由**: `subscribeOrderForUser` 関数自体が `base/src/stores/event.ts` から削除されている（grep で見つからない）。現在は `eventStore.orders` 配列で全注文を扱う設計に変更されており、本コメントの前提が成立しない。

---

**識別子**: RC-7（GitHub id: 2945803791）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `common` 側で `CreateStripeCheckoutSessionResponse { url }` を定義してフロントもその型で受けているので、Functions 側も Checkout Session 全体ではなく `url` のみ返す（＋onCall の戻り値型も合わせる）方が、型の整合性・通信量・意図の明確さの面で良いです。
```

**コメント要約**: `createStripeCheckoutSession` は `{ url }` のみ返すよう絞り、`onCall` の型引数で戻り値型を明示する。

**判断結果**: ✅ 対応済み

**判断理由**: `functions/default/src/stripe.ts` で `onCall<CreateStripeCheckoutSessionRequest, Promise<CreateStripeCheckoutSessionResponse>>` とし、戻り値は `return { url: session.url }` のみ。`common/src/apis/stripe.ts` の `CreateStripeCheckoutSessionResponse` と整合。

---

**識別子**: RC-8（GitHub id: 2945805033）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  注文購読で対象オーダーを一意に特定する**

`subscribeOrderForUser` は `event_id/user_id/community_account` で絞った結果から `snapshot.docs[0]` を返していますが、この条件では同一ユーザー・同一イベントに複数注文（過去の `canceled`/`ordered` と今回の注文）が共存できるため、Firestore のドキュメント名順で任意の注文を拾ってしまいます。結果として決済完了ダイアログが古い注文状態を参照し、今回の決済が `processing` でも `ordered` 表示になったり、その逆で表示不能になるため、`order_id` で特定するか、少なくとも `orderBy` + `limit(1)` で最新注文を明示的に選ぶ必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: `subscribeOrderForUser` で対象注文を `order_id` か `orderBy + limit(1)` で一意特定する。

**判断結果**: ✅ 対応済み

**判断理由**: RC-6 と同じく `subscribeOrderForUser` 関数自体が削除されており、本指摘の前提構造は消滅。同根の「複数 `processing` 混在で `isProcessing` が混線しうる」点は RC-11 / RC-12 に委ね、本 PR では 👌 非対応（見送り）とした。

---

**識別子**: RC-9（GitHub id: 2945805041）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  未確定注文でもダイアログの退避経路を残す**

この分岐だと `isLoadingOrder`・`isProcessing`・`showFullContent(ordered)` のいずれにも当てはまらない状態（例: リダイレクト直後でまだ `in_cart` のまま、Webhook 失敗で `null`/`canceled`）でカード本文が空になり、しかも `v-dialog` が `persistent` のためユーザーが閉じられないまま画面作不能になります。想定外ステータス向けのフォールバック表示と閉じる操作を追加しないと、決済直後の導線が詰まります。

Useful? React with 👍 / 👎.
```

**コメント要約**: 想定外ステータス時もダイアログ本文と閉じる操作を残す。

**判断結果**: ✅ 対応済み

**判断理由**: 現在の `UserSuccessJoinEventDialog.vue` (L78-170) は `v-card v-if="event != null"` で event が存在すれば常に描画され、L156-167 に「コピー」「閉じる」ボタンが常時表示される構成になっており、本指摘の「カード本文が空になり閉じられない」状態は発生しない。`isProcessing` の真偽でタイトル・本文の i18n キーが切り替わるだけ。`persistent` でも閉じるボタンで閉じられるため詰まらない。

---

**識別子**: RC-10（GitHub id: 3166129171）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  undefined ではなく deleteField で項目をクリアする**

`order.payment_intent = undefined` と `order.processing_at = undefined` を入れたまま `saveOrder(..., transaction)` に渡すと、`set(..., { merge: true })` 経由で Firestore に `undefined` が流れ、`ignoreUndefinedProperties` 未設定の現状では `Cannot use "undefined" as a Firestore value` でトランザクションが失敗します。特にこの確定処理は `checkout.session.completed` / `checkout.session.async_payment_succeeded` の通常経路でも実行されるため、Webhook が 5xx になって再送され続ける原因になります。削除したい場合は `FieldValue.delete()`（または `optionalDeleteField` を使うスキーマ変換）で明示的に削除してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: `order.payment_intent = undefined` で Firestore が `undefined` エラー → Webhook 5xx で再送ループ。`FieldValue.delete()` か `optionalDeleteField` で明示削除する。

**判断結果**: ✅ 対応済み

**判断理由**: 直近のコミット `3e20fe41` で `EventMemberOrderDbSchema.processing_at` / `processing_payment_intent` を `optionalDeleteField(...)` でラップ。`stripeWebhook.ts` 側のコード（`order.processing_at = undefined`）はそのままで、Zod の transform で `FieldValue.delete()` に変換される。レビューの提案そのものを採用した形。

---

**識別子**: RC-11（GitHub id: 3218545219）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  session_id で決済中判定対象を限定する**

`isProcessing` が `eventStore.orders` 内の「同一ユーザーの processing 注文が1件でもあるか」で判定しており、`sessionId` の値自体は空チェックにしか使っていません。このため、同一イベントで過去セッションの `processing` 注文が残っている状態で別セッションの決済が即時成功しても、成功ダイアログが誤って「決済処理中」表示になり、共有導線も抑止されます。今回のリダイレクトセッションに紐づく注文（または少なくとも最新セッション）に判定を絞らないと、ステータス表示が不正確になります。

Useful? React with 👍 / 👎.
```

**コメント要約**: `isProcessing` の判定対象を `session_id` に紐づく注文に限定する（過去 session の `processing` 注文と混線するため）。

**判断結果**: 👌 非対応（本 PR では見送り）

**判断理由**: 指摘どおり `UserSuccessJoinEventDialog.vue` の `isProcessing` は「同一ユーザーの `processing` が1件でもあるか」で判定しており、`sessionId` で対象を絞っていない。ただし P2 前提かつレアケース（別セッションの `processing` が同一イベント内に長期残存し、今回は即時成功している等）であり、`last_checkout_session_id` 追加や Callable 経由の session 解決など実装コストが大きい。本 PR では対応せず、必要なら将来別タスクで検討する。RC-12 と同根。

---

**識別子**: RC-12（GitHub id: 3218860434）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  session_idで処理中判定の対象注文を特定する**

`isProcessing` が `session_id` の有無しか見ず、実際の判定は「同一ユーザーの processing 注文が1件でもあるか」になっているため、同一イベント内で別セッションの遅延決済が残っていると今回の Checkout が即時成功していても「決済処理中」表示になり、共有導線も非表示になります。`session_id` に対応する決済（またはその注文群）に限定して判定しないと、成功画面の状態が誤表示されます。

Useful? React with 👍 / 👎.
```

**コメント要約**: RC-11 と同内容。`session_id` に紐づく注文に判定を限定する。

**判断結果**: 👌 非対応（本 PR では見送り）

**判断理由**: RC-11 と同根の再掲のため、方針は RC-11 と同様（本 PR 非対応・将来検討可）。

---

**識別子**: RC-13（GitHub id: 3219298077）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  processing項目で delete sentinel を常時生成しない**

`processing_at` と `processing_payment_intent` を `optionalDeleteField(...)` にしたことで、値を持たない通常の `in_cart` 注文でも `toFirestore()` 出力に `FieldValue.delete()` が常に含まれます。`createOrder`（`functions/default/src/stores/memberOrder.ts`）は新規作成時に `set(order)`（`merge: true` なし）を使うため、この delete sentinel が含まれると Firestore 書き込みが失敗し、カート追加処理が落ちます。`merge` なしの新規保存パスで delete sentinel を生成しない形に分ける必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: `optionalDeleteField` だと未設定値で `FieldValue.delete()` が常時出力され、`createOrder` の `set(order)`（merge なし）で失敗する。新規作成時は delete sentinel を出さない経路に分ける。

**判断結果**: ✅ 対応済み

**判断理由**: `functions/default/src/stores/memberOrder.ts` の `createOrder` で `typedRef.set(order, { merge: true })` / `transaction.set(typedRef, order, { merge: true })` とした。`optionalDeleteField` 由来の `FieldValue.delete()` が新規 `set` に混ざっても Firestore が受理する経路に揃え、カート追加（`addToCart`）が失敗しないようにした。

---

**識別子**: RC-14（GitHub id: 3219298082）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  決済処理中判定後にシェア表示を再確認する**

この `watch` は初回評価時に `isProcessing` が `false` なら即座に `hasShownSharePrompt = true` にし、1秒待機後にダイアログを表示しますが、`eventStore.orders` が未読込の間は `isProcessing` が常に `false` なので、リダイレクト直後に実際は `processing` 注文でもシェア促進が表示されます。待機後に `isProcessing` を再チェックして表示中止しないと、今回追加した「処理中はシェア導線を抑止する」挙動が崩れます。

Useful? React with 👍 / 👎.
```

**コメント要約**: `watch` 初回評価時に `eventStore.orders` 未読込だと `isProcessing === false` で評価されシェア促進が表示される。1 秒待機後に再判定する。

**判断結果**: ✅ 対応済み

**判断理由**: `UserSuccessJoinEventDialog.vue` の `watch` が `[model, event, isProcessing, isLoadingOrder]` を監視し、`isLoadingOrder` 中は return。`hasShownSharePrompt` は 1 秒待機・`getLoadedShops` 完了後、`model` / `isLoadingOrder` / `isProcessing` / `event` を再確認して問題なければその時点で true にしシェアプロンプトを開く。初回で orders 未読込のときに `hasShownSharePrompt` を立てないため、RC-5 の `isLoadingOrder` とセットでレースを解消。

---

## 評価セッション

- **評価日時**: 2026-05-12 01:15 JST
- **ブランチ名**: fix/1834
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/1845 (#1845)
- **Outdated 除外件数**: 0 件（`position == null` のインラインは API 上 0 件）

### RC 別対応チェックリスト（本セッション）

- [x] **RC-15**（GitHub id: 3220379003）— ✅ 対応済み
- [x] **RC-16**（GitHub トップコメント id: 4420697229）— ✅ 対応済み／👌 任意
- [x] **RC-17**（GitHub トップコメント id: 4422466703）— ✅ 対応済み／👌 任意

---

**識別子**: RC-15（GitHub id: 3220379003）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  非同期イベント順不同でもprocessingへ戻さないようにする**

`checkout.session.completed`（`payment_status=unpaid`）を受けたら常に `handleAsyncPaymentPending` を実行していますが、Stripe Webhook はイベント順序が保証されないため、`checkout.session.async_payment_failed` が先に到着した後でこの分岐が実行されると、失敗済み注文を再び `processing` に戻してしまいます。すると最終状態が `in_cart` ではなく `processing` のまま残り、注文一覧やキャンセル可否判定が誤ります。`completed(unpaid)` 側で最新状態（または同一 PaymentIntent の整合）を確認し、失敗済みセッションの遅延到着イベントは無視する必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: `async_payment_failed` が先に来て `in_cart` に戻したあと、遅延した `checkout.session.completed` + unpaid で `handleAsyncPaymentPending` が再度走ると注文が `processing` に戻る恐れがある。

**判断結果**: ✅ 対応済み

**判断理由**: 当初の指摘どおり、`async_payment_failed` 後に遅延した `checkout.session.completed`（`unpaid`）だけでは Firestore 上は `in_cart` と判別しづらかった。`handleAsyncPaymentFailed` で `failed_async_payment_intent` に失敗した PI を記録し、`handleAsyncPaymentPending` は同一 PI の場合は遷移をスキップする。別 PI で再チェックアウトした場合は `in_cart` → `processing` 時に当該フィールドをクリアし、`handleOrderConfirmation` でも `ordered` 確定時にクリアする（`common` の `EventMemberOrder` スキーマに `failed_async_payment_intent` を追加）。

---

**識別子**: RC-16（GitHub トップコメント id: 4420697229）

**レビュワー**: Copilot（copilot-swe-agent）

**レビューのコメント（原文）**:

```
> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

レビュー結果です（コミット `cba2469` で [must] 2 件を修正済み）。インライン投稿 API が利用できないため、ファイル・行番号付きで 1 指摘 1 ブロックで記載します。

---

**🔴 [must] `common/src/schemas/EventMemberOrder.ts:141`**
`toFirestore()` が `undefined` フィールドを含むオブジェクトを返す。`order.processing_payment_intent = undefined` の後 `saveOrder` → `set({ merge: true })` すると Firestore Admin SDK が "Cannot use 'undefined' as a Firestore value" を投げる（`ignoreUndefinedProperties` 未設定時）。
→ `Object.entries(data).filter(([, v]) => v !== undefined)` で undefined キーを除外するよう修正済み（コミット `cba2469`）。

---

**🔴 [must] `base/src/components/UserSuccessJoinEventDialog.vue:31-36`**
`eventStore.orders ?? []` により、orders が `null`（ローディング中）のとき `isProcessing` が `false` を返す。PayPay リダイレクト直後に "注文完了🎉" が一瞬表示される flash が発生する。`sessionId` + `userId` が指定されているが `eventStore.orders === null` のケースでフォールバック表示がない。
→ `isLoadingOrder` computed を追加し、ローディング中はスピナーを表示するよう修正済み（コミット `cba2469`）。

---

**🟡 [imo] `functions/default/src/stripeWebhook.ts:236-239`**
`handleAsyncPaymentFailed` で `order.processing_payment_intent = undefined` → `merge: true` としても、Firestore 上の既存フィールドは**削除されない**（merge はフィールドを上書きするが、undefined を「フィールド削除」と解釈しない）。`processing_payment_intent` / `processing_at` を DB から消したい場合は `FieldValue.delete()` を使う必要がある。
今回の `toFirestore()` fix で undefined が除外され DB エラーは防がれるが、フィールドが残存する点は仕様として許容するか、明示的な `FieldValue.delete()` で対応することを検討してください。

---

**🟡 [imo] `functions/default/src/stripe.ts:169`**
`return session` で `Stripe.Checkout.Session` 全体を返しているが、`common/src/apis/stripe.ts` は `CreateStripeCheckoutSessionResponse { url: string | null }` と定義している。`onCall` の型引数に戻り値型が未指定のため型エラーにはならないが、実際にはセッション全体がクライアントに送信される。`return { url: session.url }` に絞り、`onCall<Request, Response>` の型パラメータを揃えることを検討してください。

---

**🟡 [imo] `base/src/components/UserSuccessJoinEventDialog.vue:26`**
`const isPosted = props.isPosted` は非リアクティブなスナップショット。ダイアログが閉じてから再度開かれるシナリオで props が変化した場合に追随しない。`props.isPosted` を直接参照するか `computed(() => props.isPosted)` で包むことを検討してください（`watch` の条件で使用されているため）。
```

**コメント要約**: バンドルコメント。EventMemberOrder とダイアログの must、Webhook の FieldValue・stripe 戻り値・isPosted の imo。

**判断結果**: ✅ 対応済み（must および FieldValue・url の imo）。isPosted については 👌 修正不要

**判断理由**: must 相当は `optionalDeleteField` と `isLoadingOrder` 分離・watch 再確認で解消されている（Object.entries 方式から変更済みでも目的は達成）。imo の undefined 残存は `optionalDeleteField` で `FieldValue.delete` に変換するため実害なし。`return { url: session.url }` と `onCall` の第2型引数は現行ブランチで反映済み。`isPosted` のスナップショットはダイアログの再オープンで props が変わる用途が稀であり、優先度は低いと判断。

---

**識別子**: RC-17（GitHub トップコメント id: 4422466703）

**レビュワー**: Copilot（copilot-swe-agent）

**レビューのコメント（原文）**:

````
> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

レビュー結果です。インライン投稿 API が利用できないため、ファイル・行番号付きで 1 指摘 1 ブロックで記載します。

---

**✅ `common/src/schemas/EventMemberOrder.ts:79-80`**
`processing_at` / `processing_payment_intent` を `optionalDeleteField` でラップし、`undefined` 代入時に `FieldValue.delete()` へ自動変換される設計。前回の [must] 指摘が適切に解消されています。

---

**🟡 [imo] `functions/default/src/stores/memberOrder.ts:232-235`**
`createOrder` は新規ドキュメントを生成する関数ですが、`set({ merge: true })` に変更されました。`optionalDeleteField` が `FieldValue.delete()` を含む場合に `merge: true` が必要（`merge: false` だと Firestore が FieldValue.delete() を新規 doc に適用できずエラーになる）という理由があるのは理解できますが、「新規作成なのになぜ merge か」が読む側には分かりにくいです。コメントで理由を 1 行残すことを検討してください。

```ts
// optionalDeleteField が FieldValue.delete() を含む場合、merge: true が必要
await typedRef.set(order, { merge: true })
```

---

**🟡 [imo] `base/src/components/UserSuccessJoinEventDialog.vue:26`**
`const isPosted = props.isPosted` は初期値のスナップショットです。ダイアログが閉じてから再度開かれたシナリオで props が変化した場合に watch の `isPosted` 条件が追随しません。`props.isPosted` を直接参照するか `computed(() => props.isPosted)` に変更することを検討してください。

---

**🟢 [任意] `user/src/components/manage/event/member.vue:99-108`**
`getDateString` の `switch` に `default` 節がないため、TypeScript の exhaustive check が働いていません。`EVENT_MEMBER_ORDER_STATUS_VALUES` に新しいステータスが追加された際に気づきにくくなります。`default` に `satisfies never` や unreachable 系の型ガードを置くことを検討してください。

---

**✅ `functions/default/src/stripe.ts:21-24`**
`onCall<CreateStripeCheckoutSessionRequest, Promise<CreateStripeCheckoutSessionResponse>>` で型引数が明示され、`return { url: session.url }` のみを返す実装に整合。前回の [imo] 指摘が解消されています。

---

**✅ `base/src/components/UserSuccessJoinEventDialog.vue:31-95`**
`isLoadingOrder` / `isProcessing` の computed 分離と、非同期待ちの後に二重チェックする watch 設計。前回の [must] 指摘が適切に解消されています。
````

**コメント要約**: 再レビュー。optionalDeleteField・stripe・ダイアログを肯定。createOrder の merge に説明コメント推奨。isPosted・member.vue の exhaustive は任意。

**判断結果**: ✅ 対応済み／👌 任意（ドキュメント化・軽微改善は未着手でも可）

**判断理由**: 指摘どおり現行実装で主要な must と型整合は満たしている。merge true の1行コメントや `default satisfies never` は可読性・将来の拡張耐性向上のための提案であり、本 PR のマージを阻む不具合ではない。isPosted は RC-16 と同様 👌。

---

## 評価セッション

- **評価日時**: 2026-05-11 18:45 JST
- **ブランチ名**: fix/1834
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/1845 (#1845)
- **Outdated 除外件数**: 0 件（`position == null` のインラインは 0 件）

### RC 別対応チェックリスト（本セッション）

- [x] **RC-18**（GitHub id: 3220328261）— ✅ 対応済み
- [x] **RC-19**（GitHub id: 3220329505）— ✅ 対応済み
- [x] **RC-20**（GitHub id: 3220329939）— 👌 修正不要
- [x] **RC-21**（GitHub id: 3220330110）— 👌 修正不要
- [x] **RC-22**（GitHub id: 3220330434）— ✅ 対応済み
- [x] **RC-23**（GitHub id: 3220328451）— ✅ 対応済み
- [x] **RC-24**（GitHub id: 3220329297）— ✅ 対応済み
- [x] **RC-25**（GitHub id: 3220330283）— ✅ 対応済み
- [x] **RC-26**（GitHub id: 3220327311）— ✅ 対応済み
- [x] **RC-27**（GitHub id: 3220327798）— ✅ 対応済み
- [x] **RC-28**（GitHub id: 3220328619）— ✅ 対応済み
- [x] **RC-29**（GitHub id: 3220327509）— ✅ 対応済み
- [x] **RC-30**（GitHub id: 3220328003）— ✅ 対応済み
- [x] **RC-31**（GitHub id: 3220329683）— ✅ 対応済み

---

**識別子**: RC-18（GitHub id: 3220328261）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。UserSuccessJoinEventDialog に isLoadingOrder を追加し、sessionId と userId があり orders が未読込の間はローディング表示と閉じる導線のみにしました。
```

**コメント要約**: isLoadingOrder 追加と未読込時の UI について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: 第三者からの新規指摘ではなく、既存の RC-5 系レビューに対する実装者の解決コメント。内容は前セッションの「対応済み」結論と整合する。

---

**識別子**: RC-19（GitHub id: 3220329505）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。v-card 内にコピーと閉じるボタンを常時表示しており、persistent でも閉じられる導線があります。
```

**コメント要約**: 閉じる・コピー導線の常時表示について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-9 と同趣旨の解決表明に過ぎず、追加のコード対応は不要。

---

**識別子**: RC-20（GitHub id: 3220329939）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
ご指摘のとおり isProcessing が sessionId と注文の対応まで絞れておらず、理論上は別セッションの processing と混線し得ます。P2 かつレアケースと判断し、スキーマ拡張や Callable 追加が必要なため本 PR では見送りとしました。
```

**コメント要約**: session と注文の厳密な紐づけは P2・レアのため本 PR では見送り。

**判断結果**: 👌 修正不要

**判断理由**: RC-11 で同一論点を「本 PR 見送り」と評価済み。実装者の方針表明と一致し、追加評価で覆す理由はない。

---

**識別子**: RC-21（GitHub id: 3220330110）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
RC-11 と同根のため方針は同様です。本 PR では対応せず、必要なら別タスクで検討します。
```

**コメント要約**: RC-11 と同根の指摘は見送り。

**判断結果**: 👌 修正不要

**判断理由**: RC-12 と同様の結論。別タスク化は妥当。

---

**識別子**: RC-22（GitHub id: 3220330434）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。watch が isLoadingOrder を監視し読み込み中はシェア促進に進まないほか、1 秒待機後に isProcessing などを再確認してからシェア確認ダイアログを出すよう変更しました。
```

**コメント要約**: watch・シェアダイアログのレース対策について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-14 の対応内容と一致する実装者コメント。

---

**識別子**: RC-23（GitHub id: 3220328451）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。subscribeOrderForUser 自体を削除し、eventStore.orders で一覧を扱う設計に変更したため、limit 1 の指摘対象はなくなっています。
```

**コメント要約**: subscribeOrderForUser 削除により limit 1 指摘は前提消失と表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-6・RC-8 と整合。

---

**識別子**: RC-24（GitHub id: 3220329297）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。subscribeOrderForUser は削除済みで、当該指摘の前提は解消しています。同一ユーザーに複数 processing が残る場合の session_id との紐づけは別コメントで P2 とし、本 PR では見送りとしました。
```

**コメント要約**: ストア設計変更済み。session 紐づけ厳密化は P2・見送り。

**判断結果**: ✅ 対応済み

**判断理由**: 削除による解消と P2 見送りの二段は RC-6・RC-11 と矛盾しない。

---

**識別子**: RC-25（GitHub id: 3220330283）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。createOrder で set に merge true を指定し、optionalDeleteField 由来の FieldValue.delete を新規作成パスでも扱えるようにしました。
```

**コメント要約**: createOrder の merge true と FieldValue.delete 互換について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-13 と整合。

---

**識別子**: RC-26（GitHub id: 3220327311）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
ご指摘のとおり対応済みです。stripeWebhook で checkout.session.async_payment_succeeded と async_payment_failed を処理し、payment_status に応じて processing・ordered・in_cart へ遷移するよう実装しました。
```

**コメント要約**: 非同期決済 Webhook ハンドラの実装について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-1 と整合。RC-15 は `failed_async_payment_intent` により Webhook 順不同時の再 `processing` を防止済み。

---

**識別子**: RC-27（GitHub id: 3220327798）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。payment_method_types はコードから削除し、Stripe ダッシュボードの動的決済手段設定に委ねる形に変更しました。
```

**コメント要約**: payment_method_types 削除と動的設定委譲について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-3 と整合。

---

**識別子**: RC-28（GitHub id: 3220328619）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。createStripeCheckoutSession は CreateStripeCheckoutSessionResponse に合わせ onCall の第2型引数を明示し、戻り値は url のみ返すようにしました。
```

**コメント要約**: onCall 型と url のみ返却について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-7 と整合。

---

**識別子**: RC-29（GitHub id: 3220327509）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。stripeRefunds は削除し、cancelOrders では error instanceof Stripe.errors.StripeError で分岐してログ用フィールドを組み立てています。
```

**コメント要約**: stripeRefunds 削除と cancelOrders の分岐について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-2 と整合。

---

**識別子**: RC-30（GitHub id: 3220328003）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。EventMemberOrder の DbSchema で processing_at と processing_payment_intent を optionalDeleteField でラップし、undefined を FieldValue.delete に変換してから保存するようにしました。
```

**コメント要約**: optionalDeleteField による processing 系フィールドのクリアについて対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-4・RC-10 と整合。

---

**識別子**: RC-31（GitHub id: 3220329683）

**レビュワー**: nijuni-yasu（インライン返信）

**レビューのコメント（原文）**:

```
対応済みです。optionalDeleteField により、processing 系フィールドのクリア時も undefined を Firestore に送らず FieldValue.delete 相当で削除します。
```

**コメント要約**: optionalDeleteField による削除挙動について対応済みと表明。

**判断結果**: ✅ 対応済み

**判断理由**: RC-4・RC-10 と重複趣旨だが内容は正しく、追加対応は不要。
