# PR #2074 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-20 01:30 JST
- **ブランチ名**: fix/2063
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2074
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 11

レビュー非該当スキップ内訳:

- GitHub id 4701609307, 4701610105: Codex 利用上限メッセージ
- GitHub id 4701609804, 4750959257, 4753220374: レビュー依頼定型コメント
- GitHub id 4701619199, 4750984445, 4753243079: Codex 接続案内ボイラープレート
- GitHub id 4750997000, 4753243847: Codex 問題なしサマリのみ
- GitHub id 4753242398: Copilot の対応完了報告（新規指摘ではない）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 4701619010 | 👌 修正不要 | — | — | 👀 確認のみ | — | UserEventCard の v-img がリンク有無で重複<br>alt 対応済み。現状の明示分岐を維持 |
| [x] | RC-2 | 4701619010 | 👌 修正不要 | — | — | 👀 確認のみ | — | イベントプレビューの link/static ブロック重複<br>共通化は差分が大きく今回は見送り |
| [x] | RC-3 | 4701619010 | 👌 修正不要 | — | — | 👀 確認のみ | — | watch に非リアクティブな profileUserId を含む<br>同一インスタンスで userId 不変なら実害なし |
| [x] | RC-4 | 4701619010 | 👌 修正不要 | — | — | 👀 確認のみ | — | UserBioPanel の placeholder 判定が二重 computed<br>表示文言と状態の分離として許容 |
| [x] | RC-5 | 4750983520 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | failedEventCoverIds が event_id のみ<br>別 community の同一 event_id で誤非表示の恐れ |
| [x] | RC-6 | 4750983520 | 👌 修正不要 | — | 👤 UX | 👀 確認のみ | — | 限定公開チップ文言の表記ゆれ<br>URL限定公開 と 限定公開 は文脈差として今回は統一しない |
| [x] | RC-7 | 4750983520 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | friend sort の v-select に label と aria-label が重複<br>aria-label 削除で十分 |
| [x] | RC-8 | 4750983520 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | meet_count に friend-date-value クラスを使用<br>friend-meet-count-value に分離済み |
| [x] | RC-9 | 4750983520 | 👌 修正不要 | — | — | 👀 確認のみ | — | watch(tabs) の未使用引数 tab<br>tab は isFriendTab で使用中のため指摘不成立 |
| [x] | RC-10 | 3442067216 | 👌 修正不要 | — | 📑 仕様書 | 👀 確認のみ | — | 他者閲覧時の meet_count チップ非表示指摘<br>正本仕様はカードに meet_count を含む。isOwner 削除は意図的 |
| [x] | RC-11 | 3442067260 | 👌 修正不要 | — | 📑 仕様書 | 👀 確認のみ | — | 他者閲覧時の日付非表示指摘<br>正本仕様どおり全員表示を維持。非表示案は見送り確定 |
| [x] | RC-12 | 3442067278 | 👌 修正不要 | — | 📑 仕様書, 👤 UX | 👀 確認のみ | — | 他者閲覧時の meet log ボタン非表示指摘<br>§4.2.6 どおり全員表示を維持。isOwner ガードは導入しない |
| [x] | RC-13 | 3409465063 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | UserEventCard のリンク付き v-img に alt なし<br>イベント名を alt に設定すべき |
| [x] | RC-14 | 3442067295 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | RC-13 と同一。カバー画像リンクの a11y<br>v-img に :alt を付与 |
| [x] | RC-15 | 3409465059 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | RC-5 と同一。失敗キーに community_id が無い |
| [x] | RC-16 | 3409465060 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | RC-5 と同一。onEventCoverError の引数不足 |
| [x] | RC-17 | 3409465061 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | RC-5 と同一。2箇所目の @error 呼び出し |
| [x] | RC-18 | 4753374276 | 👌 修正不要 | — | 📑 仕様書 | 👀 確認のみ | — | 他者閲覧時も meet_count チップ表示<br>正本仕様どおり全員表示維持。追加案 L111 は修正済み |
| [x] | RC-19 | 4753374276 | 👌 修正不要 | — | 📑 仕様書 | 👀 確認のみ | — | 他者閲覧時も日付表示<br>RC-18 と同論点。非表示案は見送り確定 |
| [x] | RC-20 | 4753374276 | 👌 修正不要 | — | 📑 仕様書, 👤 UX | 👀 確認のみ | — | 他者閲覧時の meet log で A-B 履歴が見える<br>§4.2.6 どおり Phase 1 は全員表示。プライバシーは Phase 2 で検討 |
| [x] | RC-21 | 4753374276 | 👌 修正不要 | — | 👤 UX | 👀 確認のみ | — | 限定公開チップ文言の表記ゆれ<br>RC-6 と同一。URL限定公開 と 限定公開 は文脈差として維持 |
| [x] | RC-22 | 4753374276 | 👌 修正不要 | — | — | 👀 確認のみ | — | UserBioPanel の placeholder 判定重複<br>RC-4 と同一。表示文言と状態の分離として許容 |
| [x] | RC-23 | 3443711651 | ✅ 対応済み | 📌 スコープ内 | 👤 UX, 🐛 実害 | 🔧 微修正 | S | イベントプレビュー v-img の width=100 が 100px 固定<br>両 v-img から width prop を削除済み |

---

### RC-1（GitHub id: 4701619010）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/UserEventCard.vue:241`

**該当コード（レビュー時点の diff）**:

```diff
+    <router-link
+      v-if="eventDetailPath != null"
+      class="user-event-card-detail-link d-block text-reset text-decoration-none"
+      :to="eventDetailPath"
+    >
+      <v-img
+        cover
+        class="ma-0 pa-0"
+        aspect-ratio="1.91"
+        :src="convertStoragePathToURL(getEventCoverStoragePath(event.community_id, event.event_id))"
+      />
+    </router-link>
+    <v-img
+      v-else
+      cover
```

**レビュワーのコメント（原文）**:

[nits] `eventDetailPath` の有無で `<v-img>` が2箇所に重複して書かれています。`src` / `cover` / `class` / `aspect-ratio` のプロパティが完全に同一なので、wrapper の `<router-link>` / なし を切り替えるだけにして `<v-img>` は1つにまとめると保守性が上がります。

**コメント要約**: eventDetailPath の有無で v-img ブロックが二重化している。ラッパーだけ切り替えて v-img を1つにまとめると保守しやすい。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 重複は事実だが、リンクあり/なしの分岐が明示的で読みやすい。RC-13 で alt は両方に付与済み。今回の共通化は見送り。

---

### RC-2（GitHub id: 4701619010）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:656`

**該当コード（レビュー時点の diff）**:

```diff
+                      <v-card variant="outlined" class="h-100 preview-card overflow-hidden">
+                        <div class="event-preview-tile">
+                          <div class="event-preview-tile__cover overflow-hidden">
                             <v-img
-                              v-if="eventCoverUrl(event.community_id, event.event_id) != null"
+                              v-if="showEventCoverImage(event.community_id, event.event_id)"
```

**レビュワーのコメント（原文）**:

[nits] リンク可能なイベント（`router-link` wrapper）とリンク不可なイベント（`div` wrapper）で、カード内部のテンプレート（カバー画像・プレースホルダー・日付・イベント名）が完全に重複しています。wrapper だけを切り替えるように統合するか、内側を別コンポーネントへ切り出すことで重複を解消できます。

**コメント要約**: イベントプレビューで link 版と static 版の内部マークアップが重複。ラッパー切替または子コンポーネント化を推奨。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: テンプレート重複はあるが、共通化は差分が大きく本 PR のスコープ外。機能上の問題はない。

---

### RC-3（GitHub id: 4701619010）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:491`

**該当コード（レビュー時点の diff）**:

```diff
+watch(
+  () => [profileUserId, previewData.value] as const,
+  () => {
+    failedEventCoverIds.value = new Set()
+  },
+)
```

**レビュワーのコメント（原文）**:

[imo] `watch` の getter に非リアクティブな `profileUserId`（`const profileUserId = props.userId` の plain string）を含めていますが、Vue はこの変数を依存として追跡しません。実際には `previewData.value` の変化のみを監視しています。同一コンポーネントインスタンスで `userId` prop が変化する可能性がなければ実害はありませんが、意図が伝わりにくいため `profileUserId` の記述は除くか、`props.userId` を使う形にするとよいです。

**コメント要約**: watch 依存配列に非リアクティブな profileUserId が含まれ意図が不明瞭。削除または props.userId 参照がよい。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: ルート param 変更で同一インスタンスが再利用されるケースは稀で、previewData 変化時のリセット目的は達成されている。可読性改善の余地はあるがマージブロッカーではない。

---

### RC-4（GitHub id: 4701619010）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/UserBioPanel.vue:24`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[nits] `isDescriptionPlaceholder` と `userDescription` の computed が、「説明が空かつ本人」という同一の条件を重複して持っています。`isDescriptionPlaceholder` を廃止して `userDescription` の返値が `$t('user_profile.user_description_placeholder')` かどうかで判定するか、または `isDescriptionPlaceholder` に一本化することで重複を減らせます。

**コメント要約**: placeholder 判定ロジックが二つの computed に分散。一本化で重複削減可能。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 表示文言と placeholder スタイル判定の分離として許容。一本化のメリットより現状の明確さを優先。

---

### RC-5（GitHub id: 4750983520）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:531`

**該当コード（レビュー時点の diff）**:

```diff
+const onEventCoverError = (eventId: string) => {
+  if (failedEventCoverIds.value.has(eventId)) return
+  failedEventCoverIds.value = new Set([...failedEventCoverIds.value, eventId])
+}
+
+const showEventCoverImage = (communityId: string, eventId: string): boolean =>
+  eventCoverUrl(communityId, eventId) != null && !failedEventCoverIds.value.has(eventId)
```

**レビュワーのコメント（原文）**:

[must] `failedEventCoverIds` のキーが `event_id` 単体のため、別コミュニティで同じ `event_id` が存在した場合に一方の失敗が他方に伝搬します。`community_id + event_id` の複合キーで管理してください。

**コメント要約**: カバー読み込み失敗 Set のキーが event_id のみで衝突する。community_id と event_id の複合キーが必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: eventCoverKey で community_id と event_id の複合キー管理に変更済み。onEventCoverError と showEventCoverImage、2箇所の @error 呼び出しも更新済み。

---

### RC-6（GitHub id: 4750983520）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/EventCard.vue:35`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[imo] 限定公開チップの文言が2箇所で異なっています。

- `EventCard.vue`: `$t('private_event')` → `'URL限定公開'`（base locale）
- `UserProfilePage.vue` プレビュー: `$t('user_profile.private_event_chip')` → `'限定公開'`（user locale）

同一概念を指すチップなので、文言を統一するか、`UserProfilePage` 側も `$t('private_event')` を使うよう揃えた方が一貫性が保てます。

**コメント要約**: 限定公開チップが URL限定公開 と 限定公開 で表記ゆれ。base の private_event に揃える提案。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: EventCard の URL限定公開 とプレビューの 限定公開 は表示文脈が異なる。ユーザー判断により今回は統一しない。

---

### RC-7（GitHub id: 4750983520）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:1047`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[nits] `v-select` に `:label` と `:aria-label` が同一テキストで二重に設定されています。`label` は Vuetify が自動的に `<label for=...>` として描画し、スクリーンリーダーにも伝わるため、`:aria-label` は不要です。

**コメント要約**: ともだちソート v-select で label と aria-label が重複。aria-label は削除してよい。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: v-select から重複していた :aria-label を削除済み。label による読み上げは維持される。

---

### RC-8（GitHub id: 4750983520）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:75`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[nits] `<span class="friend-date-value">` で `$t('user.friend_meet_count', ...)` を囲んでいますが、`friend-date-value` は `font-variant-numeric: tabular-nums` を適用するためのクラスで、日付数値向けの意図と読めます。`meet_count` に tabular-nums が必要かどうかは文脈によりますが、クラス名の意図（日付値）と用途（同席回数）がずれています。`friend-meet-count-value` などより明示的なクラス名にするか、`friend-date-value` のスタイルを「数値全般用」と位置づけてコメントで補足するとよいです。

**コメント要約**: 食事回数表示に friend-date-value クラスを流用しており命名が日付専用に見える。リネームまたはコメント補足を推奨。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 食事回数表示を friend-meet-count-value に分離し、日付用の friend-date-value と役割を明確化済み。

---

### RC-9（GitHub id: 4750983520）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:262`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[nits] `watch(tabs, (tab) => { syncProfileQueryToUrl() })` と `watch(friendSortBy, () => { syncProfileQueryToUrl() })` の両方が `syncProfileQueryToUrl` を呼んでいますが、前者は引数 `tab` を使っていません。`syncProfileQueryToUrl` が `tabs.value` を直接読むため実害はありませんが、`watch(tabs, () => { ... })` と書けば「引数を無視している」という意図が明確になります。

**コメント要約**: tabs の watch コールバックで未使用引数 tab がある。引数省略で意図を明確化できる。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 現行コードでは watch コールバックの tab 引数を isFriendTab(tab) で使用しており、未使用ではない。指摘前提が成立しない。

---

### RC-10（GitHub id: 3442067216）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:72`

**該当コード（レビュー時点の diff）**:

```diff
+        <v-chip
+          size="small"
+          color="primary"
+          variant="tonal"
+          class="friend-meet-count-chip mt-2"
+          :prepend-icon="mdiSilverwareForkKnife"
+          label
+        >
```

**レビュワーのコメント（原文）**:

[must] 他者プロフィール閲覧時（isOwner=false）でも、同席回数チップが表示されてしまい、仕様（他者マイページでは詳細を非表示）と不整合です。isOwner のときだけ表示するよう v-if を付けてください。

**コメント要約**: 他者閲覧時も食事回数チップが見える。isOwner ガードが必要との指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 📑 仕様書

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: Copilot が参照する他者非表示は 02_友人一覧機能_追加案.md の未確定案。正本の 02_友人一覧機能.md はカードに meet_count を含む。本 PR は isOwner を廃止し全閲覧者に表示する意図的変更。追加案を採用するなら別判断だが現時点では指摘の前提が弱い。

---

### RC-11（GitHub id: 3442067260）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:80`

**該当コード（レビュー時点の diff）**:

```diff
+    <v-card-text class="pt-0 flex-grow-1 d-flex flex-column ga-1">
+      <div v-if="isSingleMeet && friend.last_met_at > 0" class="friend-date-primary text-body-2">
```

**レビュワーのコメント（原文）**:

[must] 他者プロフィール閲覧時（isOwner=false）でも、最後に会った日/初めて会った日が表示されてしまい、仕様（他者マイページでは詳細を非表示）と不整合です。日付ブロック全体を isOwner のときだけ表示するようにしてください。

**コメント要約**: 他者閲覧時に会った日が表示される。isOwner 時のみ日付ブロック表示が必要との指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 📑 仕様書

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-10 と同様。02_友人一覧機能.md は friends サブコレの日付をカードに表示する設計。isOwner ガード復活は追加案採用時の別タスク。

---

### RC-12（GitHub id: 3442067278）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:99`

**該当コード（レビュー時点の diff）**:

```diff
+    <v-card-actions v-if="hasMeetHistory" class="pt-0 pb-2 pe-2 justify-end">
```

**レビュワーのコメント（原文）**:

[must] 他者プロフィール閲覧時（isOwner=false）でも、同席イベントの記録（meet log）ボタンが表示されるため、他者同士の同席履歴を閲覧できる導線になります。仕様どおり isOwner のときのみ表示してください。

**コメント要約**: 他者がプロフィール所有者のともだち同席履歴を開ける。isOwner 時のみボタン表示が必要との指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 📑 仕様書, 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 製品判断確定（26/6/19）: 他者向け非表示は今回見送り、Phase 1 は `02_マイページ.md` §4.2.6 どおり meet log を全閲覧者に表示。`isOwner` ガードは導入しない。プライバシー懸念は Phase 2 に回す。

---

### RC-13（GitHub id: 3409465063）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/UserEventCard.vue:246`

**該当コード（レビュー時点の diff）**:

```diff
+      <v-img
+        cover
+        class="ma-0 pa-0"
+        aspect-ratio="1.91"
+        :src="convertStoragePathToURL(getEventCoverStoragePath(event.community_id, event.event_id))"
+      />
```

**レビュワーのコメント（原文）**:

[must] eventDetailPath ありのときも無しのときも、v-img に alt が無いとスクリーンリーダーで内容が伝わりません。イベント名を alt に設定してください。

**コメント要約**: カバー v-img に alt がなく a11y 不足。event.event_name を alt に設定すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: リンクあり・なし両方の v-img に :alt="event.event_name" を付与済み。

---

### RC-14（GitHub id: 3442067295）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/UserEventCard.vue:247`

**該当コード（レビュー時点の diff）**:

```diff
+        :src="convertStoragePathToURL(getEventCoverStoragePath(event.community_id, event.event_id))"
+      />
```

**レビュワーのコメント（原文）**:

リンク付きのカバー画像がスクリーンリーダーで無名のリンクになりやすいので、v-img に alt を付けてください（最低でもイベント名）。

**コメント要約**: RC-13 と同一。リンク付きカバーに alt 必須。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-13 と同一対応。UserEventCard の v-img に alt 付与済み。

---

### RC-15（GitHub id: 3409465059）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:504`

**該当コード（レビュー時点の diff）**:

```diff
+const onEventCoverError = (eventId: string) => {
+  if (failedEventCoverIds.value.has(eventId)) return
+  failedEventCoverIds.value = new Set([...failedEventCoverIds.value, eventId])
+}
+
+const showEventCoverImage = (communityId: string, eventId: string): boolean =>
+  eventCoverUrl(communityId, eventId) != null && !failedEventCoverIds.value.has(eventId)
```

**レビュワーのコメント（原文）**:

[must] 画像読み込み失敗判定を event_id だけで管理すると、別コミュニティで同じ event_id が存在した場合に誤ってカバー画像が非表示（プレースホルダー固定）になる可能性があります。community_id を含めたキーで失敗状態を管理してください。

**コメント要約**: RC-5 と同一。失敗 Set のキー設計不備。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-5 と同一。eventCoverKey による複合キー化で対応済み。

---

### RC-16（GitHub id: 3409465060）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:670`

**該当コード（レビュー時点の diff）**:

```diff
+                              @error="onEventCoverError(event.event_id)"
```

**レビュワーのコメント（原文）**:

[must] onEventCoverError の引数が event_id のみだと、失敗状態のキーを community_id 付きに変更できません（別コミュニティの同一 event_id 衝突対策）。community_id も渡してください。

**コメント要約**: @error 呼び出しに community_id が無い。RC-5 対応の一部。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-5 と同一。@error に community_id を渡す形に更新済み。

---

### RC-17（GitHub id: 3409465061）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:703`

**該当コード（レビュー時点の diff）**:

```diff
+                              @error="onEventCoverError(event.event_id)"
```

**レビュワーのコメント（原文）**:

[must] onEventCoverError の引数が event_id のみだと、失敗状態のキーを community_id 付きに変更できません（別コミュニティの同一 event_id 衝突対策）。community_id も渡してください。

**コメント要約**: static 側プレビューの @error も同様。RC-5 の2箇所目。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-16 と同一。static 側プレビューの @error も複合キー対応済み。

---

## 評価セッション

- **評価日時**: 2026-06-20 10:45 JST
- **ブランチ名**: fix/2063
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2074
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2

レビュー非該当スキップ内訳:

- GitHub id 4753342886: レビュー依頼定型コメント（ef9d9e3f push 後）
- GitHub id 4753374812: Codex 接続案内ボイラープレート

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-18 | 4753374276 | 👌 修正不要 | — | 📑 仕様書 | 👀 確認のみ | — | 他者閲覧時も meet_count チップ表示<br>正本仕様どおり全員表示維持。追加案 L111 は修正済み |
| [x] | RC-19 | 4753374276 | 👌 修正不要 | — | 📑 仕様書 | 👀 確認のみ | — | 他者閲覧時も日付表示<br>RC-18 と同論点。非表示案は見送り確定 |
| [x] | RC-20 | 4753374276 | 👌 修正不要 | — | 📑 仕様書, 👤 UX | 👀 確認のみ | — | 他者閲覧時の meet log で A-B 履歴が見える<br>§4.2.6 どおり Phase 1 は全員表示。プライバシーは Phase 2 で検討 |
| [x] | RC-21 | 4753374276 | 👌 修正不要 | — | 👤 UX | 👀 確認のみ | — | 限定公開チップ文言の表記ゆれ<br>RC-6 と同一。URL限定公開 と 限定公開 は文脈差として維持 |
| [x] | RC-22 | 4753374276 | 👌 修正不要 | — | — | 👀 確認のみ | — | UserBioPanel の placeholder 判定重複<br>RC-4 と同一。表示文言と状態の分離として許容 |

---

### RC-18（GitHub id: 4753374276）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:67-75`

**該当コード（レビュー時点の diff）**:

```diff
        <v-chip
          size="small"
          variant="tonal"
          class="friend-meet-count-chip mt-2"
          :prepend-icon="mdiSilverwareForkKnife"
          label
        >
          <span class="friend-meet-count-value">{{ $t('user.friend_meet_count', { count: friend.meet_count }) }}</span>
        </v-chip>
```

**レビュワーのコメント（原文）**:

[must] `isOwner` プロップを廃止したことで、他者プロフィール閲覧時でも同席回数チップが表示されてしまいます。仕様書（`documents/03_参加者獲得/02_友人一覧機能_追加案.md` L111）の確定事項「本人ページのみ `meet_count` / 日付 / イベントの記録を表示。他者ページはアバター・名前・プロフィールリンクのみ」に反しています。isOwner を prop として戻して `v-if="isOwner"` ガードを追加するか、仕様書側を現状の全員表示に合わせて更新してください。

**コメント要約**: 他者プロフィール閲覧時に同席回数チップが表示される。追加案確定メモ L111 を根拠に isOwner 復活を求める指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 📑 仕様書

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 製品判断確定（26/6/19）: 他者向け非表示は今回見送り、Phase 1 は正本 `02_友人一覧機能.md` §4.2.1 どおり全閲覧者に表示。追加案 L111 は「現状維持（全員表示）」に修正済み。`isOwner` ガードは導入しない。RC-10 と同一論点。

---

### RC-19（GitHub id: 4753374276）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:79-96`

**該当コード（レビュー時点の diff）**:

```diff
      <div v-if="isSingleMeet && friend.last_met_at > 0" class="friend-date-primary text-body-2">
        <span class="text-medium-emphasis">{{ $t('user.friend_first_met_at') }}</span>
        <span class="ms-1 friend-date-value">{{ convertToDate(friend.last_met_at) }}</span>
      </div>

      <template v-else>
        <div v-if="friend.last_met_at > 0" class="friend-date-primary text-body-2">
          ...
        </div>
        <div v-if="friend.first_met_at > 0" class="friend-date-secondary text-caption text-medium-emphasis">
          ...
        </div>
      </template>
```

**レビュワーのコメント（原文）**:

[must] 同様の理由で、他者プロフィール閲覧時でも「最後に会った日」「初めて会った日」が表示されます。仕様書の確定事項（L111）に違反しています。

**コメント要約**: 他者閲覧時も初回／最終同席日が表示される。追加案 L111 を根拠に非表示を求める指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 📑 仕様書

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-18 と同様。正本仕様どおり全員表示を維持。非表示案は見送り確定。RC-11 と同一論点。

---

### RC-20（GitHub id: 4753374276）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/FriendCard.vue:98-111`

**該当コード（レビュー時点の diff）**:

```diff
    <v-card-actions v-if="hasMeetHistory" class="pt-0 pb-2 pe-2 justify-end">
      <v-btn
        variant="outlined"
        ...
        @click="openMeetLogDialog"
      >
        {{ $t('user.friend_meet_log_open') }}
```

**レビュワーのコメント（原文）**:

[must] 「共通のイベント」ボタンも他者閲覧時に表示・クリック可能です。クリックすると `FriendMeetLogDialog` が開き、プロフィール所有者とその友人の同席イベント一覧が第三者に見えてしまいます（プライバシー漏洩）。`v-if="isOwner"` ガードが必要です。

**コメント要約**: 第三者 C が A のプロフィールで A と B の同席履歴を開ける。プライバシー懸念と isOwner ガードを求める指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 📑 仕様書, 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: `02_マイページ.md` §4.2.6 は meet log を本人・他者の閲覧者に表示する設計。製品判断確定（26/6/19）で Phase 1 は全員表示を維持し `isOwner` は導入しない。プライバシー懸念は Phase 2 に回す。RC-12 と同一論点。

---

### RC-21（GitHub id: 4753374276）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/EventCard.vue:35` / `user/src/components/profile/UserProfilePage.vue:747,793`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[imo] 限定公開チップの文言が2箇所で異なっています。`EventCard.vue` は `$t('private_event')` → **「URL限定公開」**、`UserProfilePage.vue` プレビューは `$t('user_profile.private_event_chip')` → **「限定公開」**。同一概念を指すチップなので文言を揃えることを推奨します。

**コメント要約**: EventCard とプロフィールプレビューで限定公開チップの文言が異なる。統一を推奨。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-6 と同一。マイページ文脈のプレビューは短い「限定公開」、一覧カードは「URL限定公開」と文脈差があり、06/19 追加仕様でもプレビュー用チップを別キーで定義済み。統一は任意改善。

---

### RC-22（GitHub id: 4753374276）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/UserBioPanel.vue:24-35`

**該当コード（レビュー時点の diff）**:

```diff
const isDescriptionPlaceholder = computed(
  () => props.userData.user_description === '' && currentUserStore.firebaseUser?.uid === props.userData.user_id,
)

const userDescription = computed(() => {
  if (props.userData.user_description !== '') {
    return props.userData.user_description
  }
  if (currentUserStore.firebaseUser?.uid !== props.userData.user_id) {
    return ''
  }
  return $t('user_profile.user_description_placeholder')
})
```

**レビュワーのコメント（原文）**:

[nits] `isDescriptionPlaceholder` と `userDescription` computed が、どちらも「`user_description === '' && uid === user_id`」という同一の条件判定を持っており、ロジックが重複しています。`isDescriptionPlaceholder` を廃止して `userDescription` の返値でテンプレート側から判定するか、`isDescriptionPlaceholder` に一本化すると見通しが良くなります。

**コメント要約**: placeholder 判定ロジックが二重 computed に分散。統合を推奨。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-4 と同一。`isDescriptionPlaceholder` はテンプレートのスタイル切替、`userDescription` は表示文言の責務分離として許容範囲。本 PR の主目的外。

---

## 評価セッション

- **評価日時**: 2026-06-20 11:05 JST
- **ブランチ名**: fix/2063
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2074
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 5

レビュー非該当スキップ内訳:

- GitHub id 4753430467: レビュー依頼定型コメント
- GitHub id 4753429722, 4753431592: Codex 利用上限メッセージ
- GitHub id 4753451919: Codex 接続案内ボイラープレート
- GitHub id 4753451123（2〜7 節目）: Copilot トップレベル再指摘（RC-1, RC-2, RC-3, RC-4, RC-6, RC-9, RC-21, RC-22 と同一論点のため新規 RC なし）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-23 | 3443711651 | ✅ 対応済み | 📌 スコープ内 | 👤 UX, 🐛 実害 | 🔧 微修正 | S | イベントプレビュー v-img の width=100 が 100px 固定<br>両 v-img から width prop を削除済み |

---

### RC-23（GitHub id: 3443711651）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:724` / `:771`（リンクあり・なし両方の `v-img`）

**該当コード（レビュー時点の diff）**:

```diff
                            <v-img
                              v-if="showEventCoverImage(event.community_id, event.event_id)"
                              :src="eventCoverUrl(event.community_id, event.event_id)"
                              :alt="event.event_name"
                              cover
                              aspect-ratio="1.91"
                              width="100"
                              @error="onEventCoverError(event.community_id, event.event_id)"
                            />
```

```diff
 .event-preview-tile__cover {
-  width: 100px;
+  width: 100%;
 }
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  イベントプレビュー画像の固定幅を解除する**

プロフィールタブのイベントプレビューを縦並びカードに変更してカバー枠を 100% 幅にしていますが、上のリンクあり/なし両方の `<v-img>` には既存の `width="100"` が残っています。Vuetify の `VImg` はこの prop をルート要素の `width: 100px` として適用するため、イベント画像がカード幅いっぱいに広がらず左側 100px だけの表示になります。新しいレイアウトでは `width` prop を外すか `width="100%"` にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: 縦並びレイアウトへ CSS を `width: 100%` に変更した一方、`v-img` の `width="100"`（100px）が残りカバーが細いサムネ表示になる。prop 削除または `100%` 化が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX, 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: リンクあり・なしの 2 箇所の `v-img` から `width="100"` を削除し、`.event-preview-tile__cover { width: 100% }` と整合させた。

---
