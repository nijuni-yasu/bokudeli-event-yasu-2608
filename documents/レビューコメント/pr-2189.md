# PR #2189 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-07-16 21:32 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: fix/2188
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2189
- **REVIEW_REQUEST_SINCE**: 2026-07-16T12:27:07Z
- **partial**: false
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 3（レビュー依頼定型文 1、Codex 接続案内 overview 1、Copilot overview（インライン参照のみ）1）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 3595393708 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | ルーム切替直後に空状態が一瞬フラッシュする<br>`hasMoreMessages` 等で初回スナップショット確定後のみ表示 |
| [x] | RC-2 | 3595393987 | 👌 修正不要 | 📌 スコープ内 | 👤 UX | 👀 確認のみ | — | 読み取り専用ルームで送信促進文言が矛盾<br>対応不要と判断（アーカイブ時のみ・頻度低） |
| [x] | RC-3 | 3595395418 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | RC-1 と同一: 初回購読完了前の空状態表示<br>`!hasMoreMessages` を条件に含める |
| [x] | RC-4 | なし・エージェントレビュー | 👌 修正不要 | — | — | 👀 確認のみ | — | `hasMoreMessages` を初回ロード完了の代理条件に使う設計<br>store の subscribe 契約と整合しフラッシュ防止として妥当 |
| [x] | RC-5 | 3595574155 | 👌 修正不要 | 📌 スコープ内 | 👤 UX | 👀 確認のみ | — | RC-2 と同根: 読み取り専用ルームで送信促進文言<br>RC-2 と同様に対応不要 |

---

**識別子**: RC-1（GitHub id: 3595393708）

**レビュワー**: Codex

**指摘箇所**: `base/src/components/chat/ChatLog.vue:342`

**該当コード（レビュー時点の diff）**:

```diff
@@ -338,6 +338,10 @@ const chatLogEntries = computed((): ChatLogEntry[] => {
   return entries
 })
 
+const showEmptyMessagesHint = computed(() => {
+  return store.messages.length === 0 && !store.isLoadingOlderMessages
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  初回ロード完了まで空状態表示を待ってください**

ルームを開いた直後や切り替え直後は、`subscribeMessages()` が `messages` を一度空にしてから Firestore の初回 `onSnapshot` を待つ一方、`isLoadingOlderMessages` は過去メッセージ追加読み込み用なので `false` のままです。そのため既存メッセージがあるルームでも初回スナップショット到着までこの条件が真になり、「まだメッセージがありません」が一瞬表示されます。初回購読完了を表す状態、または `hasMoreMessages === false` など、空であることが確定してから表示する条件にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: ルーム切替直後は `messages=[]` かつ `isLoadingOlderMessages=false` となり、既存メッセージがあるルームでも空状態が一瞬表示される。<br>初回 onSnapshot 確定後（例: `!hasMoreMessages`）に限定すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `subscribeMessages()` は `messages.value = []` と `hasMoreMessages.value = true` を即時実行し、初回 `onSnapshot` まで `showEmptyMessagesHint` が真になる。store 実装（`chat.ts` L476–495）上、指摘どおりフラッシュが起きうる。`!store.hasMoreMessages` を加えれば初回スナップショット前は非表示、0 件確定時のみ表示できる。

**対応内容**: `showEmptyMessagesHint` に `!store.hasMoreMessages` を追加。初回 onSnapshot 前（`hasMoreMessages === true`）は非表示、0 件確定時（`hasMoreMessages === false`）のみ空状態を表示。

---

**識別子**: RC-2（GitHub id: 3595393987）

**レビュワー**: Codex

**指摘箇所**: `base/src/components/chat/ChatLog.vue:358`

**該当コード（レビュー時点の diff）**:

```diff
@@ -349,6 +353,11 @@ onBeforeUnmount(() => {
       {{ t('chat.loading_older') }}
     </div>
 
+    <div v-else-if="showEmptyMessagesHint" class="text-center text-disabled text-sm my-4">
+      <p class="mb-0">{{ t('chat.empty.no_messages') }}</p>
+      <p class="mb-0">{{ t('chat.empty.no_messages_hint') }}</p>
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  読み取り専用ルームでは送信促進文言を避けてください**

終了済みなど `store.activeRoom.isReadonly === true` のメッセージ0件ルームでは入力欄が無効化され、下部にも読み取り専用の案内が出るのに、この文言だけは「最初のひとことを送ってみよう」と送信を促してしまいます。読み取り専用ルームでは補足文を出さない、または別文言に切り替える条件にしないと、ユーザーに実行できない操作を案内します。

Useful? React with 👍 / 👎.

**コメント要約**: 読み取り専用ルーム（`isReadonly`）では入力不可なのに送信促進の補足文が表示される。<br>補足文の条件分岐または別文言が必要。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 指摘の論点は理解できるが、本 PR では対応しない。`isReadonly` はイベント開催日時経過ではなくキャンセル/論理削除アーカイブ時（`is_active === false`）のみ。通常の終了後イベントでは読み書き可能なため矛盾は稀。入力欄下の `readonly_hint` もあり、P3 提案として許容する。

---

**識別子**: RC-3（GitHub id: 3595395418）

**レビュワー**: Copilot

**指摘箇所**: `base/src/components/chat/ChatLog.vue:343`

**該当コード（レビュー時点の diff）**:

```diff
@@ -338,6 +338,10 @@ const chatLogEntries = computed((): ChatLogEntry[] => {
   return entries
 })
 
+const showEmptyMessagesHint = computed(() => {
+  return store.messages.length === 0 && !store.isLoadingOlderMessages
+})
```

**レビュワーのコメント（原文）**:

[must] 空状態ヒントの表示条件が `messages.length === 0 && !isLoadingOlderMessages` だけだと、ルーム切替直後に `subscribeMessages()` が `messages=[]` にリセットするタイミングで、Firestore の最初の onSnapshot が返るまで一瞬だけ空状態が表示される（フラッシュする）可能性があります。初回スナップショット受領後にのみ空状態を出すよう、既存の `hasMoreMessages`（初回 onSnapshot で 0 件なら false になる）も条件に含めるのが安全です。

**コメント要約**: RC-1 と同一論点。初回 onSnapshot 前の `messages=[]` で空状態がフラッシュする。<br>`hasMoreMessages` を表示条件に含める修正案が store と整合する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: Copilot [must] かつ RC-1 と同根。`hasMoreMessages` は初回スナップショットで `oldestMessageSnapshot == null` のときのみ更新されるため、初回ロード完了の代理条件として妥当。

**対応内容**: RC-1 と同一修正（`!store.hasMoreMessages` を `showEmptyMessagesHint` に追加）。

---

## 評価セッション（2026-07-16 21:50 JST・shokujii-code-review）

- **評価日時**: 2026-07-16 21:50 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: fix/2188
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2189
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-4 | なし・エージェントレビュー | 👌 修正不要 | — | — | 👀 確認のみ | — | `hasMoreMessages` を初回ロード完了の代理条件に使う設計<br>store の subscribe 契約と整合しフラッシュ防止として妥当 |

---

**識別子**: RC-4（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/components/chat/ChatLog.vue:341`

**該当コード（レビュー時点の diff）**:

```diff
+const showEmptyMessagesHint = computed(() => {
+  return store.messages.length === 0 && !store.isLoadingOlderMessages && !store.hasMoreMessages
+})
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: `hasMoreMessages` は本来「過去メッセージの追加読み込み可否」だが、`subscribeMessages()` 初回は `true` にリセットされ、初回 `onSnapshot` のみ `oldestMessageSnapshot == null` のタイミングで更新される。0 件ルームでは `false` になり、メッセージありルームでは `messages.length > 0` で空表示条件を満たさないため、初回ロード完了の代理条件として問題ない。

**コメント要約**: `hasMoreMessages` の流用は store 実装（`chat.ts` L476–495）と整合する。<br>専用フラグ追加は不要。RC-1/RC-3 対応で十分。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: チェックリスト（Vue computed・store 経由参照・i18n・型）上問題なし。`ChatApp.vue` の `font-size: 1rem` は `:deep(textarea)` のみで iOS オートズーム対策として最小差分。

---

## 評価セッション（2026-07-16 21:57 JST・auto）

- **評価日時**: 2026-07-16 21:57 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: fix/2188
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2189
- **REVIEW_REQUEST_SINCE**: 2026-07-16T12:52:14Z
- **partial**: false
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 1、Codex 接続案内 overview 1）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-5 | 3595574155 | 👌 修正不要 | 📌 スコープ内 | 👤 UX | 👀 確認のみ | — | RC-2 と同根: 読み取り専用ルームで送信促進文言<br>RC-2 と同様に対応不要 |

---

**識別子**: RC-5（GitHub id: 3595574155）

**レビュワー**: Codex

**指摘箇所**: `base/src/components/chat/ChatLog.vue:358`

**該当コード（レビュー時点の diff）**:

```diff
@@ -349,6 +353,11 @@ onBeforeUnmount(() => {
       {{ t('chat.loading_older') }}
     </div>
 
+    <div v-else-if="showEmptyMessagesHint" class="text-center text-disabled text-sm my-4">
+      <p class="mb-0">{{ t('chat.no_messages_yet') }}</p>
+      <p class="mb-0">{{ t('chat.empty.no_messages_hint') }}</p>
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  読み取り専用では送信促進文言を隠す**

読み取り専用ルーム（`store.activeRoom.isReadonly === true`）でメッセージが0件の場合、`ChatApp.vue` 側では入力欄・添付・送信ボタンを無効化して `readonly_hint` を表示する一方、この新規文言だけは送信を促してしまいます。アーカイブ済み/終了済みの空ルームでは実行できない操作の案内になるため、`isReadonly` 時は2行目を非表示にするか別文言へ切り替えてください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-2（3595393987）と同一論点。`isReadonly` 時に「最初のひとことを送ってみよう」が送信不可と矛盾する。<br>2行目の条件分岐または別文言が必要。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-2（3595393987）と同一論点。ユーザー判断により RC-2 とともに対応不要。アーカイブ済みかつメッセージ 0 件のレアケースに限定され、下部 `readonly_hint` で送信不可は明示済み。

---
