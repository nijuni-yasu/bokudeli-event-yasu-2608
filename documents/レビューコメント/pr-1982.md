# PR #1982 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-07 JST
- **ブランチ名**: fix/1981
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/1982
- **Outdated 除外件数**: 0 件

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: 4396179339）— 👌 修正不要
- [x] **RC-2**（GitHub id: 4396185785）— 👌 修正不要
- [x] **RC-3**（GitHub id: 4396186566）— 👌 修正不要
- [x] **RC-4**（GitHub id: 3200666579）— 許容（案 C: [15_EventMemberOrder_複数注文の発火.md](../07_リファクタリング/15_EventMemberOrder_複数注文の発火.md) §7.1 に明記 2026-05-07）
- [x] **RC-5**（GitHub id: 3200666614）— ✅ 対応済み（2026-05-07）
- [ ] **RC-6**（GitHub id: 3200666514）— ❌ 修正推奨（コード未対応: 複雑性のため見送り 2026-05-07）
- [ ] **RC-7**（GitHub id: 3200631223）— ❌ 修正推奨（コード未対応: 複雑性のため見送り 2026-05-07）
- [ ] **RC-8**（GitHub id: 3200666550）— ❌ 修正推奨（コード未対応: 複雑性のため見送り 2026-05-07）

---

**識別子**: RC-1（GitHub id: 4396179339）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。
```

**コメント要約**: レビュー依頼の定型文であり、コードに対する指摘ではない。

**判断結果**: 👌 修正不要

**判断理由**: 実装変更を求める内容ではない。評価対象外に近いが、スキル手順に従い記録する。

---

**識別子**: RC-2（GitHub id: 4396185785）

**レビュワー**: Copilot（copilot-swe-agent）

**レビューのコメント（原文）**:

```

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

承知しました。以後のレビューは日本語で、Files changed の該当行に 1 指摘 1 コメントのインライン形式で対応します。対応コミット: aae0174
```

**コメント要約**: 依頼への応答であり、コードの問題提起ではない。

**判断結果**: 👌 修正不要

**判断理由**: マージ前のコード修正は不要。

---

**識別子**: RC-3（GitHub id: 4396186566）

**レビュワー**: Codex（chatgpt-codex-connector）

**レビューのコメント（原文）**:

```
To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).
```

**コメント要約**: Codex 利用のためのアカウント連携案内。

**判断結果**: 👌 修正不要

**判断理由**: リポジトリのコードに対するレビュー指摘ではない。

---

**識別子**: RC-4（GitHub id: 3200666579）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] member_orders を ordered にするトランザクションと、`event.members` 同期（`applyOrderConfirmedSideEffects`→`recalcEventMembers`）が別トランザクションになっているため、トランザクション成功後〜side effects 実行前に関数がクラッシュすると members/event_num_members が更新されないまま残ります（createEventMembers 廃止後は自動補正されません）。members 同期を注文確定トランザクション内に含める、またはリトライ時に members 同期だけ走らせる回復経路の追加を検討してください。
```

**コメント要約**: 注文確定トランザクションと recalc の分離により、中間クラッシュ時に event.members が古いまま残り得る。

**判断結果**: 🚨 修正推奨

**判断理由**: 旧トリガーは書き込み後に再集約が走る設計だった。現状も低頻度だが理論上の整合性ギャップはあり、リトライや同一トランザクション化などの回復策を検討する価値がある。必須度はプロダクト判断だが指摘は妥当。

**方針（2026-05-07）**: 指摘は妥当としつつ、トランザクション統合・専用リカバリは採用しない（案 C）。許容理由と収束経路を [15_EventMemberOrder_複数注文の発火.md](../07_リファクタリング/15_EventMemberOrder_複数注文の発火.md) の「7.1 `ordered` 確定と `event.members` 同期の間のクラッシュ（許容方針・PR #1982 RC-4）」に記載した。

---

**識別子**: RC-5（GitHub id: 3200666614）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] 人気イベントメール送信を `recalcResult.updated` に依存させると、「members 更新は完了したがメール送信前にクラッシュ」した場合に、リトライ時 `updated=false` となって送信が永遠にスキップされ得ます。`trySendPopularEventMailAfterMembersSync` は `sent_popular_event_mail_at` をトランザクションで立てており冪等なので、recalc が成功したら updated に関わらず呼ぶ（または memberCount で判定する）方が安全です。
```

**コメント要約**: recalc は DB 更新後にクラッシュすると再実行時に差分が無く updated が false になり、人気メールだけ永久スキップし得る。

**判断結果**: 🚨 修正推奨

**判断理由**: trySendPopularEventMailAfterMembersSync 側はフラグで冪等化されているため、recalc トランザクションが成功したケースでは updated だけで人気メールをゲートすると境界で取りこぼす。指摘は技術的に妥当。

**実装（2026-05-07）**: `orderConfirmedSideEffects.ts` で `recalcEventMembers` が例外なく完了したときに `trySendPopularEventMailAfterMembersSync` を呼ぶよう変更（`recalcResult.updated` 非依存）。`popularEventMail.ts` の JSDoc を呼び出し条件に合わせて更新。

---

**識別子**: RC-6（GitHub id: 3200666514）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `existingStripe != null` で早期 return して `community.addMember` のみ実行していますが、前回リクエストが「注文 ordered / stripes 書き込み」後にクラッシュして `recalcEventMembers`（event.members 同期）まで到達していない場合、createEventMembers 廃止後は members/event_num_members がずれたままになります。既存Stripeの分岐でも members 再集約だけは冪等に実行できるようにし、メール送信の重複回避は別途ガードする方が安全です。
```

**コメント要約**: existingStripe 早期 return で recalc が走らず、初回がコミット後クラッシュした場合に members がずれうる。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-4 や RC-8 と同系統のリカバリ不足。設計トレードオフとしてメール再送を避ける意図はあるが、少なくとも recalc のみ冪等実行する案は検討に値する。

**方針（2026-05-07）**: RC-6/7/8 向けの Stripe リカバリ実装は複雑性増加のため取り下げ。指摘のリスクは許容し、別注文の `applyOrderConfirmedSideEffects` 等で members が追従する前提とする。

---

**識別子**: RC-7（GitHub id: 3200631223）

**レビュワー**: Codex（chatgpt-codex-connector）

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  existingStripe分岐でも確定後副作用を再実行する**

createEventMembers を削除したこのコミットでは `event.members` 更新と人気イベントメール判定が `applyOrderConfirmedSideEffects` に集約されていますが、`existingStripe != null` の再実行経路は `addMember` のみで終了しています。初回 Webhook が「注文/stripe 書き込み成功後〜副作用実行前」で異常終了すると、再送はこの分岐に入り `recalcEventMembers` が一度も実行されないため、`event.members` が恒久的にずれたままになり、人気イベントメール判定も欠落します。

Useful? React with 👍 / 👎.
```

**コメント要約**: existingStripe 分岐では副作用が再実行されず、コミット後クラッシュ時に members と人気メールが欠落し得る。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-6 と論点はほぼ同一だが、指摘内容は正しい。P1 ラベルはチームの優先度判断に委ねる。

**方針（2026-05-07）**: RC-6 と同様にコード対応は見送り。

---

**識別子**: RC-8（GitHub id: 3200666550）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `txResult.kind !== 'processed'`（noop）で members 同期を行っていないため、`processed` 側がトランザクション後にクラッシュして side effects 未実行だったケースを回復できません（createEventMembers 廃止後は自動補正がありません）。noop/既存Stripe経路でも `recalcEventMembers` による members 同期だけは実行する等、リカバリ経路を用意してください。
```

**コメント要約**: noop 分岐で recalc しておらず、processed 側が副作用前に落ちた場合の回復がない。

**判断結果**: 🚨 修正推奨

**判断理由**: 並行で先に processed が成功していれば noop 側は不要という想定は多いが、processed がコミット後に副作用だけ失敗したケースでは noop 経路に来る second webhook が addMember のみでは members を直せない。指摘は妥当。

**方針（2026-05-07）**: RC-6 と同様にコード対応は見送り。
