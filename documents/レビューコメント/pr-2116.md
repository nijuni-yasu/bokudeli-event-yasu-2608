# PR #2116 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-25 19:14 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: fix/2115
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2116
- **REVIEW_REQUEST_SINCE**: 2026-06-25T10:04:22Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 4（Codex usage limits ×2、レビュー依頼定型文 ×1、Codex 接続案内 ×1）
- **partial evaluate**: Codex usage limits / connect のため Codex レビューなし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 4798097256 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `last_session_id` が session_end を優先せず最新 turn の conversation_id を返す<br>2 パス検索に修正が必要 |
| [x] | RC-2 | 4798097256 | ✅ 対応済み | 📌 スコープ内 | — | 📄 ドキュメントのみ | S | github-actions-deploy スキル 193/195 行に同一文が重複<br>片方を削除 |
| [x] | RC-3 | 4798097256 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `register_pid "$$"` が無引数関数に渡され誤解を招く<br>引数なし呼び出しに統一 |
| [x] | RC-4 | 4798097256 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `build_results_payload` の `success` 変数が未使用<br>削除または判定に利用 |
| [x] | RC-5 | 4798097256 | 👌 修正不要 | 📌 スコープ内 | — | 👀 確認のみ | — | pricing.json をターンごとに読む設計<br>hook 頻度・ファイルサイズから現状許容 |
| [x] | RC-6 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | followup 受信時に active task が上書きされ以降 turn が誤タグ付け |
| [x] | RC-7 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📐 設計 | 📐 リファクタ | M | followup_message が追加ターンを消費し計測目的と矛盾（¥50 閾値 + 文言短縮で Phase 1 対応） |
| [x] | RC-8 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | active-tasks.json に ledger と同様のファイルロックがない |
| [x] | RC-9 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | 非スキル prompt でも task_start が ledger に追記される |
| [x] | RC-10 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | hook スクリプトが 2>/dev/null で失敗を握りつぶす |
| [x] | RC-11 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | parse_since が不正入力で例外を投げうる |
| [x] | RC-12 | なし・エージェントレビュー | 👌 修正不要 | 📌 スコープ内 | — | 👀 確認のみ | — | Claude transcript フォールバックは Phase 1 許容 |
| [x] | RC-13 | 3479871218 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 📄 ドキュメントのみ | S | 手順 7 例が SINCE を発火後に再取得<br>run 特定失敗の原因 |
| [x] | RC-14 | 3479871221 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | parse_since が ISO8601 の Z を lower で壊す<br>`2026-06-26T00:00:00Z` が失敗 |
| [x] | RC-15 | 3479871223 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | deploy-pending-wake.json にファイルロックがない<br>並列 watcher で wake 消失 |
| [x] | RC-16 | 3479871227 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `$(watch_run_bg)` がコマンド置換で直列化<br>並列 watch 設計が機能しない |

---

**識別子**: RC-1（GitHub id: 4798097256・Copilot review 内）

**レビュワー**: Copilot

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:656–662`

**該当コード（レビュー時点の diff）**:

```diff
+def last_session_id(*, ledger_path: Path = LEDGER_PATH) -> str | None:
+    entries = load_ledger(ledger_path=ledger_path)
+    for e in reversed(entries):
+        if e.get("event") == "session_end":
+            sid = e.get("conversation_id")
+            if isinstance(sid, str):
+                return sid
+        sid = e.get("conversation_id")
+        if isinstance(sid, str):
+            return sid
+    return None
```

**レビュワーのコメント（原文）**:

**[must] `agent_usage_lib.py:656–662` — `last_session_id` のロジックバグ**

`①` の分岐に入って return しなかった場合（`sid` が str でない場合）でも、`②` で同じ条件を再判定しているため、事実上「`session_end` 優先」は機能せず、どのイベントでも最初に `conversation_id` が str のエントリが返ってしまいます。  
`--last-session` オプションで `session_end` 以外の直近エントリ（`turn_end` など）の会話 ID が返ることになり、意図したセッションのレポートが出ない場合があります。

**コメント要約**: `last_session_id` が reversed 走査中に session_end 以外のエントリで早期 return し、session_end 優先が機能しない。`report --last-session` が誤った会話 ID を返しうる。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: Copilot の指摘どおり、最新 `turn_end` が先にヒットすると `session_end` を見に行かない。CLI の `--last-session` の意図と矛盾する実バグ。

---

**識別子**: RC-2（GitHub id: 4798097256・Copilot review 内）

**レビュワー**: Copilot

**指摘箇所**: `.agents/skills/github-actions-deploy/SKILL.md:193–195`

**該当コード（レビュー時点の diff）**:

```diff
+`gh workflow run` は即座に返る。5 本を **watch 完了まで待たず** 連続発火する。
+
+`gh workflow run` は即座に返る。5 本を **watch 完了まで待たず** 連続発火する。
```

**レビュワーのコメント（原文）**:

**[nits] `.agents/skills/github-actions-deploy/SKILL.md:193–195` — 同一文が 2 行重複**

193 行と 195 行に同じ文が 2 回出現しています。片方を削除してください。

**コメント要約**: 手順 6 末尾の説明文がスキル分割時に重複コピーされた。1 行削除でよい。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 事実上の重複であり可読性を下げる。マージ必須ではないが修正コストは極小。

---

**識別子**: RC-3（GitHub id: 4798097256・Copilot review 内）

**レビュワー**: Copilot

**指摘箇所**: `.agents/scripts/github_actions_deploy_watch.sh:146`

**該当コード（レビュー時点の diff）**:

```diff
+register_pid "$$"
```

**レビュワーのコメント（原文）**:

**[nits] `github_actions_deploy_watch.sh:146` — `register_pid` への余分な引数**

`register_pid` 関数の定義は引数を受け取らず、内部で `--pid "$$"` を直接使用しているため、渡した `"$$"` は無視されます。読み手が引数として渡す必要があると誤解しやすいため、引数なしの `register_pid` に変更するか、関数内の `"$$"` を `"$1"` に変えて明示的に受け取る実装に統一するのがよいと思います。

**コメント要約**: 無引数関数に `"$$"` を渡しており動作上は問題ないが、読み手を混乱させる。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 実害はないが wait-ai-pr-review-watch.sh 等との一貫性のため `register_pid` 無引数呼び出しに揃えるのがよい。

---

**識別子**: RC-4（GitHub id: 4798097256・Copilot review 内）

**レビュワー**: Copilot

**指摘箇所**: `.agents/scripts/github_actions_deploy_check.py:127`

**該当コード（レビュー時点の diff）**:

```diff
+    success = all(r.get("success") for r in runs if r.get("run_id") is not None)
+    any_failed = any(r.get("success") is False for r in runs)
+    any_missing = any(r.get("run_id") is None for r in runs)
```

**レビュワーのコメント（原文）**:

**[imo] `github_actions_deploy_check.py:127` — `success` 変数が未使用**

`success` は計算されていますが、その後の `overall_status` 判定では `any_failed` と `any_missing` のみ使用されており、`success` は参照されていません。削除するか、意図があるなら使用箇所を追加してください（例: `elif not success: overall = "failure"` への置き換え）。

**コメント要約**: 未使用変数 `success` が残っている。削除が妥当。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: dead code。`any_failed` / `any_missing` で十分なため `success` 行は削除でよい。

---

**識別子**: RC-5（GitHub id: 4798097256・Copilot review 内）

**レビュワー**: Copilot

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:285`

**該当コード（レビュー時点の diff）**:

```diff
+def estimate_cost_usd(tokens: dict[str, int | None], model_id: str | None) -> float | None:
+    pricing = load_pricing()
```

**レビュワーのコメント（原文）**:

**[ask] `agent_usage_lib.py:285` — `estimate_cost_usd` が毎回 pricing ファイルを読む**

`record_turn_end` → `estimate_cost_usd` → `load_pricing` の経路で、ターン終了のたびに `agent-usage-pricing.json` をファイル読み込みしています。hook の中だけで完結するため実害は小さいですが、ファイル読み込みを `estimate_cost_usd` 外に出してキャッシュするか、あるいはこの頻度で問題ないと判断しているか確認したいです。

**コメント要約**: ターンごとに pricing JSON を読む。hook 用途では許容範囲かの確認。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: hook は stop 1 回あたり 1 読み込みで、ファイルは小さい。Phase 1 ではシンプルさを優先しキャッシュは不要。必要なら後続 PR で対応可。

---

---

## 評価セッション

- **評価日時**: 2026-06-25 22:40 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: fix/2115
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2116
- **REVIEW_REQUEST_SINCE**: 2026-06-25T13:19:48Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 ×1、Codex usage limits ×1）
- **partial evaluate**: Codex usage limits のため Codex 新規レビューなし
- **新規 RC なし**: 再レビュー依頼後に Copilot/Codex から新規コード指摘なし。前回 RC-1〜RC-4 は fixup で対応済み

### RC 一覧（サマリ）

新規 RC なし。

---

## 評価セッション（2026-06-26 15:59 JST・shokujii-code-review）

- **評価日時**: 2026-06-26 15:59 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: fix/2115
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2116
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 0
- **対応完了**: 2026-06-26 — RC-6〜RC-11 を実装反映（未コミット）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-6 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | followup 受信時に active task が上書きされ以降 turn が誤タグ付け |
| [x] | RC-7 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📐 設計 | 📐 リファクタ | M | followup_message が追加ターンを消費し計測目的と矛盾（¥50 閾値 + 文言短縮で Phase 1 対応） |
| [x] | RC-8 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | active-tasks.json に ledger と同様のファイルロックがない |
| [x] | RC-9 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | 非スキル prompt でも task_start が ledger に追記される |
| [x] | RC-10 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | hook スクリプトが 2>/dev/null で失敗を握りつぶす |
| [x] | RC-11 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | parse_since が不正入力で例外を投げうる |
| [x] | RC-12 | なし・エージェントレビュー | 👌 修正不要 | 📌 スコープ内 | — | 👀 確認のみ | — | Claude transcript フォールバックは Phase 1 許容 |

---

**識別子**: RC-6（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:398–413`, `573–591`

**該当コード**:

```python
    if is_usage_report_prompt(prompt):
        key = session_key(payload)
        tasks = load_active_tasks()
        tasks[key] = {
            "task_id": str(uuid.uuid4()),
            "task_skill": None,
            "phase": USAGE_REPORT_ACK_PHASE,
            ...
        }
        save_active_tasks(tasks)
        return
```

**レビュワーのコメント（原文）**:

🚨 **必須修正** [🔧微修正/S]: `stop` hook の `followup_message` 注入後、`beforeSubmitPrompt` で使用量レポート prompt を受け取ると active task 全体が `usage_report_ack` で上書きされる。ack ターン終了後も復元されないため、以降の通常ユーザー発言の `turn_end` が `phase=usage_report_ack` / `task_skill=None` で記録され、スキル単位の集計・レポートが壊れる → ack 判定を stop payload の prompt から直接行い active task を書き換えない、または上書き前スナップショットを ack 後に復元する。

**コメント要約**: followup による使用量表示がタスク紐付けを破壊する。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 使用量トラッキングの主目的（タスク・スキル別集計）に直結するデータ不整合。マージ前に修正推奨。

**対応内容**: `SKIP_NEXT_FOLLOWUP_KEY` フラグで ack 判定。usage report prompt 受信時は active task を上書きせずフラグのみ設定。ack ターン stop 後に `clear_skip_next_followup` で復帰。

---

**識別子**: RC-7（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:567–591`

**該当コード**:

```python
    return (
        f"{USAGE_REPORT_PREFIX} 今回のトークン使用量: {token_str}、"
        f"推定 {cost_str}（参考値）。返答は不要です。"
    )
...
    return {"followup_message": followup}
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📐リファクタ/M]: ターンごとの使用量を `followup_message` でチャットに注入すると、エージェントが 1 ターン処理し追加トークンを消費する（「返答不要」でも Thought が発生）。計測・削減を目的とする機能が逆にコストを増やす → followup をやめ ledger / セッション終了レポート / CLI のみにする、または閾値超過時のみ followup。

**コメント要約**: 表示手段が計測目的と矛盾する設計トレードオフ。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📐 設計

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: 実害はあるが運用方針の選択。Phase 1 維持も可能だが、コスト可視化の信頼性・効率の観点で見直し推奨。

**対応内容**: `followup_min_jpy: 50` を pricing.json に追加。推定 ¥50 未満は followup なし（ack ターンも発生しない）。文言を短縮し AGENTS.md に閾値説明を追記。

---

**識別子**: RC-8（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:86–93`, `398–412`

**該当コード**:

```python
def save_active_tasks(tasks: dict[str, dict[str, Any]], *, path: Path = ACTIVE_TASKS_PATH) -> None:
    ensure_state_dirs()
    _write_json(path, tasks)
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `ledger.jsonl` は `fcntl` 付き append だが、`active-tasks.json` は read-modify-write のみ。同一セッションで hook が重なった場合、後勝ちでタスク情報が失われる可能性がある → ledger と同様に `fcntl.LOCK_EX` で保護する。

**コメント要約**: active-tasks の競合対策不足。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 発生頻度は低いが、ledger 側と一貫性を持たせるのが望ましい。

**対応内容**: `_active_tasks_lock` + `update_active_tasks` で read-modify-write を `fcntl.LOCK_EX` 保護。`load_active_tasks` / `save_active_tasks` も同ロック経由。

---

**識別子**: RC-9（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:415–430`

**該当コード**:

```python
    task = start_task(payload, platform=platform, prompt=prompt)
    if not task:
        return

    append_ledger(
        {
            ...
            "event": "task_start",
            ...
        }
    )
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: スキル prompt でない通常発言でも active task が存在すれば `event: task_start` が ledger に追記される。イベント名と実態（ターン開始）がずれ ledger が冗長になる → スキル / wake 検出時のみ `task_start` を記録する。

**コメント要約**: task_start イベントの付け方が過剰。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 集計ロジックへの影響は限定的だが、ledger の意味論を明確にした方がよい。

**対応内容**: `detect_task_from_prompt` で skill / wake 未検出時は `record_task_start` を早期 return し ledger に `task_start` を追記しない。

---

**識別子**: RC-10（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.cursor/hooks/agent-usage-turn-record.sh:10–11`

**該当コード**:

```bash
  python3 "${repo_root}/.agents/scripts/agent_usage.py" stop \
    --platform "${AGENT_USAGE_PLATFORM:-cursor}" <<<"${input}" 2>/dev/null || echo '{}'
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: hook スクリプトが `2>/dev/null || echo '{}'` により Python 例外や JSON パース失敗を握りつぶす。記録漏れに気づきにくい → stderr を `.agents/state/agent-usage/hook-errors.log` 等へ追記するか、開発時のみ stderr を残す。

**コメント要約**: hook 失敗の可観測性不足。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 運用デバッグのしやすさ向上。マージ必須ではない。

**対応内容**: Cursor hook 5 本の stderr を `2>/dev/null` から `.agents/state/agent-usage/hook-errors.log` への追記に変更（Claude 側は `.cursor/hooks` を exec するため同様）。

---

**識別子**: RC-11（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:507–515`

**該当コード**:

```python
def parse_since(value: str) -> datetime:
    value = value.strip().lower()
    if value.endswith("d"):
        days = int(value[:-1])
        return datetime.now(timezone.utc) - timedelta(days=days)
    if value.endswith("h"):
        hours = int(value[:-1])
        return datetime.now(timezone.utc) - timedelta(hours=hours)
    return datetime.fromisoformat(value.replace("Z", "+00:00"))
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `7d` / ISO8601 以外の `--since` 入力で `ValueError` が未捕捉。CLI `report --since` が落ちる → try/except で stderr にメッセージを出して exit 1。

**コメント要約**: CLI 入力検証不足。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 手動 CLI 利用時の UX 改善。

**対応内容**: `parse_since` で不正入力を `ValueError` に正規化。`cmd_report` で捕捉し stderr + exit 1。

---

**識別子**: RC-12（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:188–250`

**該当コード**:

```python
    for line in path.read_text(encoding="utf-8").splitlines():
        ...
        u = msg.get("usage")
        if isinstance(u, dict):
            usage = u
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: payload に usage が無い場合、transcript の最後の usage 行を使う。必ずしも当該ターンの値ではない可能性がある → hook 用途・Phase 1 では許容。Cursor 側は payload 直読みが主経路。

**コメント要約**: Claude transcript フォールバックの精度限界。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 影響は限定的。必要なら後続 PR で改善可。

---

## 評価セッション（2026-06-26 16:50 JST・auto evaluate）

- **評価日時**: 2026-06-26 16:50 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: fix/2115
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2116
- **REVIEW_REQUEST_SINCE**: 2026-06-26T07:29:48Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 ×1、Codex About ボイラープレート ×1）
- **partial evaluate**: Copilot 新規レビューなし。Codex インライン 4 件

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-13 | 3479871218 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 📄 ドキュメントのみ | S | 手順 7 例が SINCE を発火後に再取得<br>run 特定失敗の原因 |
| [x] | RC-14 | 3479871221 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | parse_since が ISO8601 の Z を lower で壊す<br>`2026-06-26T00:00:00Z` が失敗 |
| [x] | RC-15 | 3479871223 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | deploy-pending-wake.json にファイルロックがない<br>並列 watcher で wake 消失 |
| [x] | RC-16 | 3479871227 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `$(watch_run_bg)` がコマンド置換で直列化<br>並列 watch 設計が機能しない |

---

**識別子**: RC-13（GitHub id: 3479871218）

**レビュワー**: Codex

**指摘箇所**: `.agents/skills/github-actions-deploy/SKILL.md:209`

**該当コード（レビュー時点の diff）**:

```diff
 DEPLOY_ID=$(uuidgen)
 SINCE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
 WORKFLOWS="deploy_user.yml,deploy_partner.yml,deploy_functions.yml,deploy_firestore.yml,deploy_storage.yml"
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  SINCEを発火後に上書きしないでください**

手順 6 では `SINCE` を workflow_dispatch の直前に取っていますが、この手順 7 の例をそのまま続けて実行すると発火後の時刻で `SINCE` を上書きします。`github_actions_deploy_check.py` は `gh run list --created ">=$SINCE"` で run を探しており、GitHub CLI manual でも `--created` は作成日時で絞り込むオプションなので、実行開始が速い run は新しい `SINCE` より前に作成済みとなって `run_id_not_found` になり、監視・結果報告が partial になります。手順 6 で取得した同じ `SINCE` を watcher に渡してください。

**コメント要約**: 手順 7 の bash 例が手順 6 の SINCE を上書きし、RUN_ID 特定に失敗しうる。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 手順 6 と 7 を連続実行すると意図と逆に SINCE が遅延し、deploy 監視の核心機能が壊れる。例から `SINCE=$(date...)` を削除し手順 6 の変数を再利用する修正が必要。

**対応内容**: 手順 7 の bash 例から `SINCE=$(date -u +%Y-%m-%dT%H:%M:%SZ)` を削除。手順 6 で控えた `$SINCE` をそのまま watcher に渡す旨を明記。

---

**識別子**: RC-14（GitHub id: 3479871221）

**レビュワー**: Codex

**指摘箇所**: `.agents/scripts/agent_usage_lib.py:577`

**該当コード（レビュー時点の diff）**:

```diff
 def parse_since(value: str) -> datetime:
     value = value.strip().lower()
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  UTC の ISO8601 時刻を壊さないでください**

`--since 2026-06-26T00:00:00Z` のような一般的な UTC の ISO8601 指定を渡すと、ここで文字列全体を小文字化して `Z` が `z` になり、その後の `replace("Z", "+00:00")` に引っかからず `datetime.fromisoformat` が失敗します。ヘルプ上は ISO8601 を受け付ける CLI なので、duration の suffix 判定だけを小文字化するか、`Z/z` を個別に正規化してください。

**コメント要約**: `parse_since` の全体 `.lower()` が ISO8601 UTC 末尾 `Z` を壊す。RC-11 の例外処理だけでは未解決。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: コード上 `value.strip().lower()` により `...Z` が `...z` となり、help 記載の ISO8601 入力が必ず失敗する実バグ。

**対応内容**: 全体 `.lower()` を廃止。duration 判定（`7d` / `24h`）は suffix のみ小文字化。ISO8601 は `Z`/`z` → `+00:00` 正規化後に `fromisoformat`。`test_parse_since_iso8601_utc` を追加。

---

**識別子**: RC-15（GitHub id: 3479871223）

**レビュワー**: Codex

**指摘箇所**: `.agents/scripts/github_actions_deploy_wake.py:52`

**該当コード（レビュー時点の diff）**:

```diff
    wakes = _load_wakes(wake_path)
    kept = [w for w in wakes if w.get("deploy_id") != deploy_id]
    kept.append(
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  pending wake の更新をロックしてください**

複数の sandbox deploy watcher が同じタイミングで完了すると、各プロセスが同じ `deploy-pending-wake.json` を読み込んでから別々に `kept` を書き戻すため、最後に保存したプロセス以外の wake が失われます。これにより片方のデプロイ完了がセッション開始時に報告されず、結果確認が漏れるので、active-tasks と同様にファイルロックまたは atomic な read-modify-write にしてください。

**コメント要約**: deploy-pending-wake.json の read-modify-write に競合対策がない。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-8 と同型の競合リスク。並列 deploy は稀だが wake 消失は報告漏れに直結。fcntl または update パターンで揃えるのが望ましい。

**対応内容**: `_wake_lock`（`fcntl` + `{wake_path}.lock`）と `update_wakes(mutator)` を追加。`write_pending_wake` / `consume_wake` / `list_unconsumed_wakes` を lock 経由に統一。`test_wake_multiple_writes` を追加。

---

**識別子**: RC-16（GitHub id: 3479871227）

**レビュワー**: Codex

**指摘箇所**: `.agents/scripts/github_actions_deploy_watch.sh:181`

**該当コード（レビュー時点の diff）**:

```diff
  STATUS_FILE="$(mktemp)"
  PID="$(watch_run_bg "${RUN_ID}" "${STATUS_FILE}")"
  WATCH_PIDS+=("${PID}")
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  並列 watch をコマンド置換で起動しないでください**

複数 workflow を監視する場合、`watch_run_bg` の中で `gh run watch` をバックグラウンド化していても、ここでコマンド置換 `$(...)` 経由で呼ぶと Bash はその置換用サブシェル内のバックグラウンドジョブ完了まで戻りません。そのため最初の run が終わるまで次の watch を起動できず、5 本一括時の「バックグラウンド並列 watch」にならないため、PID をコマンド置換以外で受け渡す形にしてください。

**コメント要約**: コマンド置換内のバックグラウンドジョブ待ちで watch が直列化する。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 本 PR の deploy バックグラウンド化の主目的（並列 watch でモデル消費削減）が、この書き方では達成できない。

**対応内容**: `watch_run_bg` を nameref（第3引数）で PID を返す形に変更。呼び出し側は `pid=0; watch_run_bg ... pid` でコマンド置換を除去。

---

