# PR #2032 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-23 02:00 JST
- **ブランチ名**: fix/2008
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2032
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 1 件、Codex 接続案内ボイラープレート 1 件）

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub review id: 4346730388）— 👌 修正不要
- [x] **RC-2**（GitHub id: 3289460523）— ✅ 対応済み（admin/src/pages/shop.vue）
- [x] **RC-3**（GitHub id: 3289443906）— ✅ 対応済み（base/src/components/DateInput.vue）
- [x] **RC-4**（GitHub id: 3289460488）— ✅ 対応済み（base/src/components/DateInput.vue・RC-3 と同一論点）

---

**識別子**: RC-1（GitHub review id: 4346730388）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
## Pull request overview

vue-i18n の `datetimeFormats` / `$d` による日時表示を廃止し、`common/src/utils/datetime.ts` の Luxon ベース `convertToXxx` 系ユーティリティへ統一するリファクタリングです（UI とメール等の日時表記の置換コスト低減が狙い）。

**Changes:**
- `common` に `convertToDatetime` を追加し、`convertToDatetimeWeekdayShort` の出力フォーマットを変更
- `base` / `user` / `admin` の Vue コンポーネントで `$d` 呼び出しを `convertToXxx` に置換
- `base` の i18n 初期化から `datetimeFormats` 読み込み・設定を削除し、レビュー規約に方針を明記

### Reviewed changes

Copilot reviewed 20 out of 20 changed files in this pull request and generated 2 comments.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| user/src/components/manage/event/member.vue | 注文ステータスごとの日時表示を `convertToDatetime` に統一 |
| user/src/components/manage/community/invoice.vue | イベント開催日の表示を `convertToDate` に統一 |
| user/src/components/manage/community/CopyEventDialog.vue | プレビュー表示の日時/時刻を `convertToDatetimeWeekdayShort` / `convertToTimeString` に統一 |
| common/src/utils/datetime.ts | `convertToDatetime` 追加、`convertToDatetimeWeekdayShort` のフォーマット変更 |
| base/src/plugins/i18n/index.ts | i18n 初期化から `datetimeFormats` 設定を削除 |
| base/src/locales/datetimeFormats/ja.ts | `datetimeFormats` 定義ファイルを削除 |
| base/src/components/UserSuccessJoinEventDialog.vue | ダイアログ内の日時表示を `convertToDatetimeWeekdayShort` / `convertToTimeString` に統一 |
| base/src/components/UserEventCard.vue | カード/ダイアログ内の日時表示を `convertToDatetimeWeekdayShort` に統一 |
| base/src/components/LetterTable.vue | 一覧の更新/予約日時表示を `convertToDatetime` に統一 |
| base/src/components/LetterCard.vue | カード内の送信/予約/更新日時表示を `convertToDatetime` に統一 |
| base/src/components/EventList.vue | イベント開始日時表示を `convertToDatetime` に統一 |
| base/src/components/EventDetailsCard.vue | 詳細カードの日時表示を `convertToDatetimeWeekdayShort` / `convertToTimeString` に統一 |
| base/src/components/eventcreate/EventShop.vue | 締切時刻表示を `convertToTimeString` に統一 |
| base/src/components/eventcreate/EventBasicInfoCard.vue | 終了日の表示を `convertToDate` に統一 |
| base/src/components/EventCard.vue | イベント日時表示を `convertToDatetimeWeekdayShort` / `convertToTimeString` に統一 |
| base/src/components/DateInput.vue | DateInput の表示日付生成を `parseDatetimeStrings` + `convertToDate` に変更 |
| admin/src/pages/shop.vue | 締切時刻選択肢の表示を `convertToTimeString` に統一 |
| admin/src/pages/order/index.vue | 注文一覧の日時表示を `convertToDatetimeWeekdayShort` に統一 |
| admin/src/pages/order/[eventId].vue | 注文詳細の日時/時刻表示を `convertToDatetime*` / `convertToTimeString` に統一 |
| .agents/skills/shokujii-code-review/shokujii-code-review.md | `$d` 非推奨・`convertToXxx` 使用のレビュー観点を追加 |
</details>




<details>
<summary>Comments suppressed due to low confidence (1)</summary>

**admin/src/pages/shop.vue:72**
* [must] 末尾の `endDate` も同様に `new Date(0)` → `setHours/setMinutes` → `getTime()` でローカルTZ依存のミリ秒を生成しています。ここだけ `convertToTimeString`（Asia/Tokyo）で表示しているため、JST 以外で `23:59` 等が別時刻に見える恐れがあります。`Asia/Tokyo` 固定でミリ秒を生成する実装に寄せてください。
```
  const endDate = new Date(0)
  endDate.setHours(start + num - 1)
  endDate.setMinutes(59)
  timeArray.push({ title: convertToTimeString(endDate.getTime()), value: endDate.getTime() })
  return timeArray.reverse()
```
</details>
```

**コメント要約**: PR の変更概要とファイル別サマリ。具体的な修正指摘はインラインコメント（RC-2〜4）側に分離されている。

**判断結果**: 👌 修正不要

**判断理由**: 変更内容の要約は PR の意図と一致しており、追加のコード対応は不要。low confidence の shop.vue 指摘は RC-2 と同一論点のため、対応要否は RC-2 に委ねる。

---

**識別子**: RC-2（GitHub id: 3289460523）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `date.getTime()` は実行環境（ブラウザのローカルタイムゾーン）依存の値になりますが、`convertToTimeString` は既定で `Asia/Tokyo` で解釈するため、JST 以外の環境だとドロップダウン表示と実値がずれる可能性があります。`makeTimeArray` と同様にミリ秒を算出するか、Luxon で `Asia/Tokyo` 固定の `DateTime` を作って `toMillis()` する等で環境非依存にしてください。

This issue also appears on line 68 of the same file.
```

**コメント要約**: `makeDeadlineTimeArray` が `new Date(0).setHours` + `convertToTimeString` の組み合わせで、表示（JST 固定）と millis 生成（ローカル TZ 依存）が非 JST 環境でずれる。

**判断結果**: 🚨 修正推奨

**判断理由**: shokujii-code-review の「`Date` オブジェクトを直接使わない」「タイムゾーン依存の計算を call site で組み立てない」に照らし、指摘は妥当。同一ファイルの `makeTimeArray` は既に millis 算術で環境非依存。`convertToTimeString` 導入により表示が JST 固定になった一方、`value` の millis 生成は従来どおりローカル TZ 依存のまま残り、非 JST 環境での title/value 不整合リスクが顕在化する。JST 前提の運用でも `makeTimeArray` パターンへ揃えるのが一貫した修正。

---

**識別子**: RC-3（GitHub id: 3289443906）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  DateInputの表示計算でDateを文字列パーサに渡さない**

`date.value` は直前の `adapter.parseISO(...)` から来る `Date` オブジェクトですが、ここで `parseDatetimeStrings`（`yyyy-MM-dd` 文字列前提）に渡しているため、`modelValue` が入った状態で `displayDate` 評価時に `input.match is not a function` の実行時例外が発生します。以前の `$d(...)` は `Date` を扱えたため、この置換で日付入力コンポーネントが表示不能になる回帰が入っています。

Useful? React with 👍 / 👎.
```

**コメント要約**: `DateInput` の `displayDate` が `date.value`（実体は `Date`）を `parseDatetimeStrings`（文字列専用）に渡しており、実行時例外による表示不能回帰が起きうる。

**判断結果**: 🚨 修正推奨

**判断理由**: 指摘は妥当。置換前は `$d(date.value, 'date')` で `Date` を扱えていたが、現行コードは `parseDatetimeStrings(date.value, null, null)` となっており、`parseDatetimeStrings` は `yyyy-MM-dd` 文字列を `fromFormat` する実装のため型不整合。`modelValue`（ISO 日付文字列）を直接渡す、または `adapter.toISO` / millis 変換経由に修正が必要。CopyEventDialog 等 DateInput 利用箇所全体に影響する回帰。

---

**識別子**: RC-4（GitHub id: 3289460488）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `date` は `v-date-picker` の `v-model` になっているため実体は `Date` になるケースがあり、その場合 `parseDatetimeStrings(date.value, ...)` は `yyyy-MM-dd` を期待しているので `Invalid DateTime` になって表示が壊れます。`adapter.toISO(date.value)` 等で必ず `yyyy-MM-dd` 文字列にしてから `parseDatetimeStrings` に渡すか、`DateTime.fromJSDate` で millis に変換して `convertToDate` する形にしてください。
```

**コメント要約**: `v-date-picker` 経由で `date.value` が `Date` になる場合、`parseDatetimeStrings` へそのまま渡すと表示が壊れる。ISO 文字列化または millis 変換が必要。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-3 と同一の根本原因（`Date` と文字列パーサの不整合）。Copilot の修正案（`adapter.toISO` または millis 経由）は shokujii-code-review の方針に沿う。実装は `modelValue.value`（常に `yyyy-MM-dd` 文字列）を `parseDatetimeStrings` に渡すのが最も単純で、setter との一貫性も保てる。
