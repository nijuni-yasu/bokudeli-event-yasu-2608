# PR #2057 レビューコメント評価

## 評価セッション（2026-06-04・shokujii-code-review）

- **評価日時**: 2026-06-04 JST
- **評価者**: Cursor Agent（shokujii-code-review）
- **ブランチ名**: dev/2056
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2057
- **Outdated 除外件数**: 該当なし（GitHub 外部レビュー未実施）
- **レビュー非該当スキップ件数**: 該当なし

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（github-actions-deploy スキル deploy_partner.yml 更新）
- [x] **RC-2**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（shokujii-firestore スキル user/partner 表記）
- [x] **RC-3**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（マージ前チェックリストをドキュメント化。GitHub/Firebase 設定はマージ担当者の手動作業）
- [x] **RC-4**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（clientError.test.ts partner/admin テスト追加）
- [x] **RC-5**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（デプロイ順序・並行 CI リスクをドキュメント化。スキーマ後方互換は入れず）
- [x] **RC-6**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（20_adminからpartnerへリネーム.md PR-A/B 整合）
- [x] **RC-7**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（SendGrid テンプレ変数 admin_url の維持）
- [x] **RC-8**（GitHub id: 4619508880）— 👌 修正不要（legacy `ADMIN_HOST` は手順書で例外明記済み）
- [x] **RC-9**（GitHub id: 4619508880）— ✅ 対応済み（package-lock から extraneous admin を除去）
- [x] **RC-10**（GitHub id: 3353573987）— ✅ 対応済み（git-create-pull-request スキルの partner 表記統一）
- [x] **RC-11**（GitHub id: 3353898783）— 👌 修正不要（ClientError admin 後方互換は運用順序で担保・スキーマ互換は意図的に非採用）
- [x] **RC-12**（GitHub id: 3353898777）— ✅ 対応済み（FUNCTIONS_ENV に PARTNER_HOST を含める手順をドキュメント化済み）
- [x] **RC-13**（GitHub id: 3353898785）— ✅ 対応済み（RC-9 と同一・lockfile 正規化）

---

**識別子**: RC-1（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/skills/github-actions-deploy/SKILL.md:125`

**該当コード（レビュー時点の diff）**:

（本 PR の diff 外・現行コード）

```diff
| ファイル名 | ざっくりした対象 |
|------------|------------------|
| deploy_user.yml | hosting user |
| deploy_admin.yml | hosting admin |
| deploy_functions.yml | functions |
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: AI スキル `github-actions-deploy` に `deploy_admin.yml` の表記が残っている。本 PR では `deploy_partner.yml` に置き換え済みのため、エージェントが古い workflow 名を参照する可能性がある。`lint-and-format` 等と同様に `deploy_partner.yml` へ更新すること。

**コメント要約**: デプロイ手動発火スキルがリネーム後の workflow 名を反映していない。

**判断結果**: 🟡 修正提案

**判断理由**: 本 PR の `[ai]` コミットで他スキルは更新済みだが、当該スキルのみ `deploy_admin.yml` が残存。マージ必須ではないが、エージェント運用の一貫性のため更新を推奨する。

---

**識別子**: RC-2（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/skills/shokujii-firestore/SKILL.md:24`

**該当コード（レビュー時点の diff）**:

（本 PR の diff 外・現行コード）

```diff
| **base** (user/admin の store) | [references/base-stores.md](references/base-stores.md) | ...
| **user / admin** (store 利用側) | base-stores.md | ...
- **user/admin で Firestore を使う**: base-stores.md を読み、既存 store を経由する形で実装する
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `shokujii-firestore` スキルに `user/admin` 表記が残っている（L24, L26, L55）。リネーム手順書 §3 の更新対象に近いが本 PR では未変更。`user/partner` へ揃えるとエージェントの Firestore 実装指示が正しくなる。

**コメント要約**: Firestore スキルのパッケージ名表記が partner リネームに追随していない。

**判断結果**: 🟡 修正提案

**判断理由**: コード動作への直接影響はないが、今後の store 実装時に誤ったパッケージ名を参照するリスクがある。同一 PR の `[ai]` コミットに含めるか、フォローアップで更新する。

---

**識別子**: RC-3（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.github/workflows/deploy_partner.yml:54`

**該当コード（レビュー時点の diff）**:

```diff
+      - name: Generate .env file
+        working-directory: ./partner
+        run: |
+          echo '${{ vars.PARTNER_ENV }}' | tr -d '\r' > .env
...
+          args: '--only hosting:partner'
```

**レビュワーのコメント（原文）**:

🚨 **必須修正**: マージ前にインフラ設定を完了しておくこと。マージ後 `development` への push で `deploy_partner.yml` と `deploy_functions.yml`（`functions/**` も変更）が並行実行される。`PARTNER_ENV` / `PARTNER_HOST`（Functions パラメータ）/ Firebase Hosting target `partner` が未設定のままマージすると、CI デプロイ失敗・空 `.env`・メール内 URL 障害が起き得る。PR 本文の「マージ後の手動作業」は記載済みだが、**実際にはマージ前（またはマージと同時）に完了必須**である。チェックリストを「マージ前必須」に書き換えるか、マージ担当者への明示を推奨する。

**コメント要約**: GitHub Variables と Firebase Hosting target の事前設定がマージの前提条件。

**判断結果**: 🚨 必須修正

**判断理由**: コード変更自体は正しいが、運用手順のタイミングを誤ると本番相当環境で即座にデプロイ障害が発生する。PR #2057 本文 §マージ後の手動作業を「マージ前必須」に修正するか、マージチェックリストで担保する必要がある。

---

**識別子**: RC-4（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `common/src/apis/clientError.test.ts:4`

**該当コード（レビュー時点の diff）**:

（本 PR の diff 外・`ClientErrorAppSchema` のみ変更、`clientError.test.ts` は未変更）

```diff
const validPayload = {
  app: 'user' as const,
  ...
}
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `clientError.test.ts` は `app: 'user'` のみ検証している。`app: 'partner'` が parse できること、および `'admin'` が拒否されることのテスト追加を推奨する（リネーム PR としての回帰防止）。

**コメント要約**: ClientError スキーマ変更に対する partner / admin のテストが不足。

**判断結果**: 🟡 修正提案

**判断理由**: マージブロッカーではないが、`ClientErrorAppSchema` の enum 変更は breaking change であり、単体テストで新値・旧値の挙動を固定しておくと安全。

---

**識別子**: RC-5（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `common/src/apis/clientError.ts:3`

**該当コード（レビュー時点の diff）**:

```diff
-export const ClientErrorAppSchema = z.enum(['user', 'admin'])
+export const ClientErrorAppSchema = z.enum(['user', 'partner'])
```

**レビュワーのコメント（原文）**:

🚨 **必須修正**: Functions と Hosting のデプロイ順序・ClientError の互換性。`ClientErrorAppSchema` が `admin` を受け付けなくなったため、Functions が先にデプロイされると旧 `admin` アプリ（ブラウザキャッシュ）から `app: 'admin'` で送られたエラー報告が Zod で拒否され、520 画面は出るが Cloud Logging に残らない窓ができる。**推奨デプロイ順序**: (1) `PARTNER_ENV` / `PARTNER_HOST` / Hosting target 設定 → (2) partner Hosting を先にデプロイ → (3) Functions を後からデプロイ。並行 CI を避けられない場合は、移行期間だけ `z.enum(['user', 'partner', 'admin']).transform(...)` で `admin` → `partner` に正規化する案も検討する。

**コメント要約**: ClientError の breaking change に伴うデプロイ順序と移行期間の互換性。

**判断結果**: 🚨 必須修正

**判断理由**: `functions/default/src/clientErrorReport.ts` は parse 失敗時 `invalid-argument` を返すため、旧クライアントからの報告が黙って失われる。Hosting → Functions の順序を運用で担保するか、スキーマ側で移行期間の後方互換を入れる必要がある。

---

**識別子**: RC-6（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/07_リファクタリング/20_adminからpartnerへリネーム.md:118`

**該当コード（レビュー時点の diff）**:

```diff
+## PR-B: 意味上の改名（コード変更済み・デプロイは手動）
+>
+> **マージ後の手動作業（必須）**
...
+## PR-B: 意味上の改名（詳細）
+...
+飲食店アプリを指す `admin` という**意味上の名前**を揃える。パッケージ改名とセットで実施すると一貫するが、デプロイ・ログ・メール URL に影響するため別 PR とする。
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: 設計ドキュメントでは PR-A と PR-B の分割を推奨していたが、本 PR #2057 は意図的に統合されている（6 コミットで論理分割は適切）。一方、§「PR-B（任意）」チェックリストと「別 PR とする」本文が実態（コード変更済み）とズレている。マージ後にチェックリストを `[x]` 更新し、「本 PR #2057 で PR-A+PR-B を実施済み」と明記するとよい。

**コメント要約**: リネーム手順書の PR-A/PR-B 記述と本 PR の実態の整合。

**判断結果**: 🟡 修正提案

**判断理由**: コード品質の問題ではないが、後続作業者が手順書だけ読むと「PR-B は未実施」と誤解しうる。ドキュメントのチェックリスト・見出しの更新を推奨。

---

**識別子**: RC-7（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/eventCancellation.ts:109`

**該当コード（レビュー時点の diff）**:

```diff
-              admin_url: getAdminOrderUrl(eventId),
+              admin_url: getPartnerOrderUrl(eventId),
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: メールテンプレート変数名 `admin_url` はそのまま、`getPartnerOrderUrl` の戻り値を渡している。SendGrid テンプレート側の変数名互換のため、キー名のリネームは不要である。

**コメント要約**: SendGrid dynamic template の `admin_url` キー名維持は意図どおり。

**判断結果**: 👌 修正不要

**判断理由**: テンプレート変数をリネームすると SendGrid 側の同期更新が必要になり、本 PR のスコープ外かつ不要な breaking change になる。

---

## 対応セッション（2026-06-04・RC-1〜6 実装）

- **対応内容**:
  - RC-1: `.agents/skills/github-actions-deploy/SKILL.md` — `deploy_partner.yml` に更新
  - RC-2: `.agents/skills/shokujii-firestore/` — SKILL.md + references の `user/partner` 表記
  - RC-3/5/6: `documents/07_リファクタリング/20_adminからpartnerへリネーム.md` — マージ前チェックリスト、デプロイ順序、PR-A/B 整合
  - RC-3/5: `documents/09_運営向け機能/01_520エラー通知.md`、`documents/デプロイ手順/デプロイ手順.md` — 相互リンク
  - RC-4: `common/src/apis/clientError.test.ts` — partner 受理・admin 拒否テスト
- **検証**: `npm -w common run build && npm -w common run test && npm -w common run lint && npm -w common run format:check` — すべて成功（135 tests）
- **残作業（手動）**: RC-3 の GitHub Variables（`PARTNER_ENV` / `PARTNER_HOST`）と Firebase Hosting target `partner` はマージ担当者が [マージ前チェックリスト](../07_リファクタリング/20_adminからpartnerへリネーム.md#マージ前チェックリスト必須) に従って設定

---

## 評価セッション（2026-06-04・Copilot / Codex 外部レビュー）

- **評価日時**: 2026-06-04 JST
- **ブランチ名**: dev/2056
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2057
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 7（レビュー依頼コメント 2 件、Copilot 了解返信 1 件、Codex 接続案内 4 件）

### RC 別対応チェックリスト

- [x] **RC-8**（GitHub id: 4619508880）— 👌 修正不要（legacy `ADMIN_HOST` は手順書で例外明記済み）
- [x] **RC-9**（GitHub id: 4619508880）— ✅ 対応済み（package-lock から extraneous admin を除去）
- [x] **RC-10**（GitHub id: 3353573987）— ✅ 対応済み（git-create-pull-request スキルの partner 表記統一）
- [x] **RC-11**（GitHub id: 3353898783）— 👌 修正不要（ClientError admin 後方互換は運用順序で担保・スキーマ互換は意図的に非採用）
- [x] **RC-12**（GitHub id: 3353898777）— ✅ 対応済み（FUNCTIONS_ENV に PARTNER_HOST を含める手順をドキュメント化済み）
- [x] **RC-13**（GitHub id: 3353898785）— ✅ 対応済み（RC-9 と同一・lockfile 正規化）

---

**識別子**: RC-8（GitHub id: 4619508880）

**レビュワー**: Copilot（copilot-swe-agent[bot]）

**指摘箇所**: `functions/legacy/src/utils/urls.js:10`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし・指摘対象ファイルの現行コード）

```diff
 export function getOrderUrl(eventId) {
-  return `https://${process.env.ADMIN_HOST}/order/${eventId}`
+  // Copilot は PARTNER_HOST への統一を提案
 }
```

**レビュワーのコメント（原文）**:

🚨 **必須修正**: `functions/legacy/src/utils/urls.js:10` が `process.env.ADMIN_HOST` のままです。今回の `admin → partner` 命名統一に合わせ、legacy 側も `PARTNER_HOST` へ揃えるか、legacy は例外であることをPR本文に明記してください。

**コメント要約**: legacy の `ADMIN_HOST` 参照がリネーム方針と不整合。統一するか例外を明記すべき。

**判断結果**: 👌 修正不要

**判断理由**: `documents/07_リファクタリング/20_adminからpartnerへリネーム.md` L207 で「Legacy のため優先度は低い（変更なし）」と明記済み。legacy は Deprecated で `deploy_functions.yml` も `FUNCTIONS_ENV` に `ADMIN_HOST` を残す運用（移行期間は `PARTNER_HOST` と併存）が妥当。default 側の `PARTNER_HOST` 化が本 PR のスコープであり、legacy まで揃える必要はない。

---

**識別子**: RC-9（GitHub id: 4619508880）

**レビュワー**: Copilot（copilot-swe-agent[bot]）

**指摘箇所**: `package-lock.json:31`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `package-lock.json:31` に `"admin": { "extraneous": true }` が残っています（旧 workspace 名）。`npm install` で lockfile を再生成し、不要な旧エントリを整理しておくと、リネーム後の依存状態が明確になります。

**コメント要約**: 削除済み `admin` workspace が lockfile に extraneous として残っている。

**判断結果**: 🟡 修正提案

**判断理由**: 指摘は妥当。`npm install`（ルート）で lockfile を正規化し `packages.admin` ブロックを除去すると依存グラフが明確になる。ビルド・実行への直接影響は小さいが、リネーム PR として lockfile をきれいにしておくのが望ましい（RC-13 と同一論点）。

---

**識別子**: RC-10（GitHub id: 3353573987）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `.agents/skills/git-create-pull-request/SKILL.md:107`

**該当コード（レビュー時点の diff）**:

```diff
@@ -61,7 +61,7 @@ gh pr comment --body '@copilot @codex review 日本語でお願いします。Fi
 変更内容を端的に表す日本語タイトルを生成する。Issue 番号は含めない。
 フォーマット: `[タグ] 変更内容を端的に表す日本語タイトル`
 タグは変更したディレクトリに対応するものを選ぶ。複数可。
-使用可能なタグ: `[user]` `[admin]` `[base]` `[common]` `[functions]` `[doc]` `[ai]`
+使用可能なタグ: `[user]` `[partner]` `[base]` `[common]` `[functions]` `[doc]` `[ai]`
```

（Codex 指摘の主対象は同ファイル L107 付近の「対象パッケージ」節で `user admin base common functions` 表記が残存している点）

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  PR スキルの対象パッケージ一覧も partner に統一してください**

この行で使用可能タグは `[partner]` に更新されていますが、同じ `git-create-pull-request` スキル内の「対象パッケージ」はまだ `user admin base common functions` から選ぶ指示のまま残っています。`partner` だけを触る PR 本文生成時に対象パッケージから partner が漏れ、旧 `admin` 表記を案内してしまうので、後続の対象パッケージ一覧も `partner` に更新してください。

Useful? React with 👍 / 👎.

**コメント要約**: PR 本文生成スキル内の「対象パッケージ」節に `admin` 表記が残っている。

**判断結果**: 🟡 修正提案

**判断理由**: L64 のタグ一覧は `[partner]` に更新済みだが、L68・L107 に `admin` が残存しており、指摘どおりエージェントが PR 本文生成時に旧表記を案内しうる。`[ai]` コミットに含める小さな追随修正として `partner` に統一するのがよい。マージブロッカーではない。

---

**識別子**: RC-11（GitHub id: 3353898783）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `common/src/apis/clientError.ts:3`

**該当コード（レビュー時点の diff）**:

```diff
@@ -1,6 +1,6 @@
 import { z } from 'zod'
 
-export const ClientErrorAppSchema = z.enum(['user', 'admin'])
+export const ClientErrorAppSchema = z.enum(['user', 'partner'])
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  移行期間は admin のエラー報告も受け付けてください**

Functions が先にデプロイされた場合や、利用者のブラウザに旧 `admin` ビルドの JS が残っている間は、旧クライアントが `app: 'admin'` のまま `reportClientError` を呼びますが、サーバー側の `clientErrorReport.ts` はこのスキーマで parse して失敗すると `invalid-argument` を返すだけです。その期間に発生した 520 相当のクライアントエラーが Cloud Logging に残らなくなるため、移行期間だけ `admin` を受けて `partner` に正規化する互換を入れるのが安全です。

Useful? React with 👍 / 👎.

**コメント要約**: 移行期間中、旧 `app: 'admin'` の ClientError 報告を受理し `partner` に正規化する後方互換を推奨。

**判断結果**: 👌 修正不要

**判断理由**: 技術的リスクは RC-5 と同一で指摘自体は妥当。本 PR では partner Hosting 先行デプロイの運用順序をドキュメント化済み（RC-5 対応）であり、スキーマで `admin` → `partner` 正規化を入れる案は検討のうえ意図的に非採用。移行期間のログ欠落窓は短く、520 画面自体は旧クライアントでも表示されるため、マージブロッカーではない。

---

**識別子**: RC-12（GitHub id: 3353898777）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `functions/default/src/utils/urls.ts:6`

**該当コード（レビュー時点の diff）**:

```diff
@@ -3,7 +3,7 @@ import * as common from '@shokujii/common/utils/urls.js'
 import { getStorage } from 'firebase-admin/storage'
 
 const EVENT_HOST = defineString('EVENT_HOST')
-const ADMIN_HOST = defineString('ADMIN_HOST')
+const PARTNER_HOST = defineString('PARTNER_HOST')
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  PARTNER_HOST を Functions の .env に渡してください**

このリネーム後の Functions デプロイで `PARTNER_HOST` を参照しますが、確認した `.github/workflows/deploy_functions.yml` では `functions/default/.env` を `vars.FUNCTIONS_ENV` だけから生成しており、GitHub Variables に `PARTNER_HOST` を追加しても自動では runner 環境や `.env` に入りません。`FUNCTIONS_ENV` 側に `PARTNER_HOST=...` を含める運用に統一するか、workflow で `PARTNER_HOST` を `.env` に追記しないと、マージ後の Functions deploy でパラメータ未設定になり、メール内の飲食店注文 URL 生成が壊れる可能性があります。

Useful? React with 👍 / 👎.

**コメント要約**: `PARTNER_HOST` が `.env` に入らず Functions デプロイが失敗する／URL が壊れるリスク。

**判断結果**: ✅ 対応済み

**判断理由**: Codex の指摘のうち「別 Variable `PARTNER_HOST` だけ追加しても workflow に入らない」は正しい。本プロジェクトでは `EVENT_HOST` 等と同様 **`FUNCTIONS_ENV`（GitHub Variable）の multiline 内容に `PARTNER_HOST=...` を含める**運用が前提であり、[マージ前チェックリスト](../07_リファクタリング/20_adminからpartnerへリネーム.md#マージ前チェックリスト必須) L69 で明記済み（RC-3 対応）。workflow 側の個別 `echo PARTNER_HOST=...` 追加は必須ではない。sandbox でも同設定で default codebase デプロイは成功している。

---

**識別子**: RC-13（GitHub id: 3353898785）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `package-lock.json:32`

**該当コード（レビュー時点の diff）**:

```diff
@@ -29,6 +29,7 @@
       }
     },
     "admin": {
+      "extraneous": true,
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  削除した admin ワークスペースを lock から外してください**

`admin/` を削除して root の workspaces も `partner` に変更していますが、lockfile には削除済み workspace の `packages.admin` が `extraneous` として残っています。このままだと依存関係・監査ツール上で存在しないパッケージの依存が残り続け、以後の lockfile 更新差分も不要に膨らみやすいため、`admin` エントリを消して `package-lock.json` を正規化してください。

Useful? React with 👍 / 👎.

**コメント要約**: lockfile から削除済み `admin` workspace の extraneous エントリを除去すべき。

**判断結果**: 🟡 修正提案

**判断理由**: RC-9（Copilot）と同一。ルートで `npm install` 後に lockfile をコミットすれば解消できる。マージ前の整理として推奨するが、実行時障害の直接原因ではない。

---

## 対応セッション（2026-06-04・RC-9 / RC-10 / RC-13）

- **対応内容**:
  - RC-10: `.agents/skills/git-create-pull-request/SKILL.md` — L68・L107 の `admin` → `partner`
  - RC-9 / RC-13: `package-lock.json` — extraneous `admin` workspace ブロックを除去し `npm install` で検証
- **検証**: `npm install` 成功、`package-lock.json` に `"admin"` 参照なし

---
