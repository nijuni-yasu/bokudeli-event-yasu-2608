# PR #2120 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-27 13:13 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 2026-06-27T03:51:38Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 1（レビュー依頼定型文 GitHub id: 4815112022）
- **partial evaluate**: なし（Codex 5 件）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 3485211459 | 👌 修正不要 | 📌 スコープ内 | 🐛 実害 | 👀 確認のみ | — | 既存 enterprise の tenant_id 欠落<br>データ新規作成のため backfill/フォールバックは対応なし |
| [x] | RC-2 | 3485211458 | 👌 修正不要 | 📌 スコープ内 | 🐛 実害 | 👀 確認のみ | — | 既存 member の user_email 欠落<br>データ新規作成のため backfill/フォールバックは対応なし |
| [x] | RC-3 | 3485211460 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | buildEventMonthMap が event_id のみキー<br>同一 enterprise 内で event doc ID 衝突時に月別集計が誤る |
| [x] | RC-4 | 3485211464 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | saveUser 失敗時 rollback が Auth のみ<br>members orphan が残り同一メール再インポート不可 |
| [x] | RC-5 | 3485211462 | 👌 修正不要 | 📌 スコープ内 | — | 👀 確認のみ | — | stripes CG は単一フィールド index で足りる<br>indexes.json 追加は deploy 400。f99707c3 で対応済 |
| [ ] | RC-6 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | ⚡ 性能 | 📐 リファクタ | M | admin が月別・メンバー別 Callable を並列呼び出し<br>fetchDashboardData が 2 回走り読み取りコスト 2 倍 |
| [x] | RC-7 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | ⚡ 性能 | 🔧 微修正 | S | dashboard が member.user_email 移行後も UPI を N+1 取得<br>getEnterpriseMembers との不整合 |
| [x] | RC-8 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | audit_logs の action + target_type 複合 where<br>#2128 で firestore.indexes.json に index 追加済み |
| [x] | RC-9 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📋 仕様追加 | M | assertEnterpriseAdmin に firebase.tenant 照合を追加<br>Rules と Callable の境界を整合 |
| [ ] | RC-10 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | — | 👀 確認のみ | — | WS-B / WS-C / E-4 / D-2 が 1 PR に同居<br>今後は workstream 単位分割を推奨 |
| [x] | RC-11 | 3491670805 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📋 仕様追加 | S | UI 期間上限を common から 12 ヶ月 import<br>D-12 を更新（2026-06-29 仕様変更） |
| [x] | RC-12 | 3491670853 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📋 仕様追加 | S | getDefaultDashboardPeriod を先月〜来月（3ヶ月）に設定<br>D-7 を更新（2026-06-29 仕様変更） |
| [x] | RC-13 | 3491670888 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | deploy_functions enterprise 群に getEnterpriseAuditLogs を追加 |
| [ ] | RC-14 | 3491670939 | 🟡 修正提案 | 📌 スコープ内 | 📑 仕様書 | 📐 リファクタ | M | fetchDashboardData が期間無視で全件取得<br>注文増でタイムアウト・コスト増。レンジ絞り込み推奨 |
| [x] | RC-15 | 4836306666 | 👌 修正不要 | 📌 スコープ内 | 📑 仕様書 | 👀 確認のみ | — | `?id=` PDF パスに JWT 不要<br>04_請求 §3.3 仕様どおり（PF 同趣旨） |
| [x] | RC-16 | 4836306666 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | 並行 PDF 生成競合時に GCS orphan<br>`already_exists` 分岐で generated ファイル削除推奨 |
| [x] | RC-17 | 4843249247 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 人気クエリ: inequality と orderBy フィールド不一致<br>`event_num_members >= 0` 追加推奨 |
| [x] | RC-18 | 4843249247 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 開催予定クエリ: event_end_datetime と orderBy 不一致<br>`event_num_members >= 0` 追加推奨 |
| [x] | RC-19 | 4843249247 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 終了クエリ: event_end_datetime と orderBy 不一致<br>`event_num_members >= 0` 追加推奨 |
| [x] | RC-20 | 4843249247 | 👌 修正不要 | 📌 スコープ内 | 📑 仕様書 | 👀 確認のみ | — | D-1 メール PF only skip は 04_メール配信と実装一致 |
| [x] | RC-21 | 4849077737 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | useEnterpriseAdmin が raw getDoc<br>store + withConverter 経由を推奨 |
| [x] | RC-22 | 4849077737 | ✅ 対応済み | 📌 スコープ内 | ⚡ 性能 | 📐 リファクタ | M | auditLogs buildDisplayContext が getCommunity を N 並列<br>バルク取得パターン推奨 |

---

**識別子**: RC-1（GitHub id: 3485211459）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `common/src/schemas/Enterprise.ts:54`

**該当コード（レビュー時点の diff）**:

```diff
@@ -51,6 +51,7 @@ export type EnterpriseBillingSettingsType = z.infer<typeof EnterpriseBillingSett
 
 const EnterpriseDbSchema = z.object({
   enterprise_id: z.string().nonempty(),
+  tenant_id: z.string().nonempty(),
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  既存企業に tenant_id を移行してください**

IdP Phase 1 前に作られた `/enterprises/{enterpriseId}` には `tenant_id` が無く、AppSchema では空文字にデフォルトされるため、既存企業で設定変更や補助設定変更が `saveEnterprise(...).toFirestore()` に入るとこの nonempty 検証で ZodError になります。既存企業を継続利用する環境では、tenant 作成 + `tenant_id` backfill を必須にするか、移行期間の保存フォールバックを用意してください。

Useful? React with 👍 / 👎.

**コメント要約**: IdP Phase 1 以前の enterprise ドキュメントに tenant_id が無く、保存時に DbSchema の nonempty 検証で失敗する。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 指摘自体は妥当（既存 enterprise に tenant_id が無いと save で ZodError）。ただし運用方針として **Firestore データはすべて新規作成し直す** ため、backfill・移行期フォールバックのコード対応は行わない（2026-06-29 ユーザー判断）。

---

**識別子**: RC-2（GitHub id: 3485211458）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `common/src/schemas/Enterprise.ts:165`

**該当コード（レビュー時点の diff）**:

```diff
@@ -158,6 +162,7 @@ const EnterpriseMemberDbSchema = z.object({
 
 const EnterpriseMemberAppSchema = z.object({
   user_id: z.string().nonempty(),
+  user_email: z.string().email(),
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  既存メンバーを読めるように user_email を移行してください**

IdP Phase 1 適用前に作られた `/enterprises/{enterpriseId}/members/{uid}` には `user_email` が無いため、ここを必須にすると `getEnterpriseMember` / `listEnterpriseMembers` の converter が `new EnterpriseMember(snapshot.id, snapshot.data())` で ZodError になり、既存メンバーの OTP ログイン、管理者メンバー一覧、利用状況取得がまとめて失敗します。既存データへ `user_email` を backfill するか、AppSchema 側は移行期間だけフォールバック可能にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: 既存 members に user_email が無いと converter パースが失敗し、認証・一覧・集計が連鎖的に壊れる。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 指摘自体は妥当（既存 members に user_email が無いと converter が ZodError）。ただし運用方針として **Firestore データはすべて新規作成し直す** ため、backfill・移行期 optional 化のコード対応は行わない（2026-06-29 ユーザー判断）。

---

**識別子**: RC-3（GitHub id: 3485211460）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `common/src/utils/dashboardAggregation.ts:107`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
export function buildEventMonthMap(events: readonly DashboardEventMonthInput[]): Map<string, string> {
  const map = new Map<string, string>()
  for (const event of events) {
    map.set(event.event_id, formatYearMonth(event.event_start_datetime))
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  イベント月キーに community_id も含めてください**

同一 enterprise 内の別コミュニティに同じ event doc ID がある場合、`getEventsInCommunities` は `community_id\tevent_id` で区別して返しているのに、ここで `event_id` だけをキーにするため後勝ちで上書きされ、片方の注文・Stripe・監査ログが別イベントの開催月へ集計されます。ダッシュボードの月別行と CSV が誤るので、注文/決済/audit の入力にも `community_id` を持たせて複合キーで解決してください。

Useful? React with 👍 / 👎.

**コメント要約**: eventMonthMap のキーが event_id のみで、複数 community 間の doc ID 衝突時に集計月が誤る。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: `dashboardEventKey(community_id, event_id)` を導入し `DashboardOrderLine` 等に `community_id` を載せて集計層を複合キー化。`dashboardData.ts` と衝突再現テストを追加（2026-06-30）。

---

**識別子**: RC-4（GitHub id: 3485211464）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `functions/default/src/enterprise/members.ts:154`

**該当コード（レビュー時点の diff）**:

```diff
     } catch (innerError) {
       try {
         await tenantAuth.deleteUser(authUser.uid)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  メンバー作成失敗時に Firestore も戻してください**

`saveEnterpriseMember` が成功した後に `saveUser` が失敗するケースでは、この rollback は tenant Auth ユーザーだけを削除するため、`members/{uid}` に `user_email` 付きの orphan が残ります。今回の lookup は `EnterpriseMember.user_email` 正本なので、以後同じメールの再インポートは「既に登録済み」で拒否され、しかも Auth ユーザーは存在しない状態になります。member/user ドキュメントも補償削除するか、Firestore 書き込みを一括で失敗時に戻せる形にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: 部分成功時の rollback が Auth のみで members orphan が残り、同一メール再作成が永久にブロックされる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `rollbackCreatedEnterpriseMember` で `deleteEnterpriseMember` / `deleteUserDocuments` / Auth 削除を best-effort 実行。`members.test.ts` に saveUser 失敗時の補償削除テストを追加（2026-06-30）。

---

**識別子**: RC-5（GitHub id: 3485211462）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `functions/default/src/stores/dashboard.ts:45`

**該当コード（レビュー時点の diff）**:

```diff
+export const listStripesByEnterprise = async (enterpriseId: string): Promise<EventStripe[]> => {
+  const db = getFirestore()
+  const snapshot = await db
+    .collectionGroup('stripes')
+    .where('enterprise_id', '==', enterpriseId)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  stripes の collection group index を追加してください**

この filtered collection group query は collection-group-scope の `stripes.enterprise_id` index が必要ですが、`firestore.indexes.json` には `stripes` の index/fieldOverride が無く、Stripe 決済済み注文がある企業でダッシュボード Callable が missing-index エラーになります。Firebase docs も filtered collection group query には対応する collection group scope index が必要と説明しているため、`member_orders` と同様に `stripes` 側もデプロイ対象へ追加してください。

Useful? React with 👍 / 👎.

**コメント要約**: firestore.indexes.json に stripes CG インデックスが無く missing-index になるとの指摘。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: `enterprise_id` のみの CG クエリは Firestore の単一フィールドインデックス制御で足り、`firestore.indexes.json` への複合定義追加は deploy 時 HTTP 400（`this index is not necessary`）になる。f99707c3 で indexes.json から削除し sandbox deploy_firestore 成功。`02_アーキテクチャ.md` §6.5 に明記済み。

---

## 評価セッション（2026-06-27 13:46 JST・shokujii-code-review）

- **評価日時**: 2026-06-27 13:46 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 該当なし（能動レビュー）
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし
- **備考**: RC-1〜RC-5 は Codex evaluate 済み。本セッションは能動レビューによる新規 RC-6〜RC-10。RC-1 / RC-2 はデータ新規作成方針により 👌 修正不要（2026-06-29）。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [ ] | RC-6 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | ⚡ 性能 | 📐 リファクタ | M | admin が月別・メンバー別 Callable を並列呼び出し<br>fetchDashboardData が 2 回走り読み取りコスト 2 倍 |
| [x] | RC-7 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | ⚡ 性能 | 🔧 微修正 | S | dashboard が member.user_email 移行後も UPI を N+1 取得<br>getEnterpriseMembers との不整合 |
| [x] | RC-8 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | audit_logs の action + target_type 複合 where<br>#2128 で firestore.indexes.json に index 追加済み |
| [x] | RC-9 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📋 仕様追加 | M | assertEnterpriseAdmin に firebase.tenant 照合を追加 |
| [ ] | RC-10 | なし・エージェントレビュー | 🟡 修正提案 | 📌 スコープ内 | — | 👀 確認のみ | — | WS-B / WS-C / E-4 / D-2 が 1 PR に同居<br>今後は workstream 単位分割を推奨 |

---

**識別子**: RC-6（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `enterprise/src/pages/admin/index.vue:30`

**該当コード（レビュー時点の diff）**:

```diff
+    const [monthlyResult, memberResult] = await Promise.all([
+      getDashboardMonthlyData(request),
+      getDashboardMemberData(request),
+    ])
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📐リファクタ/M]: `admin/index.vue` が `getDashboardMonthlyData` と `getDashboardMemberData` を `Promise.all` で並列呼び出ししており、各 handler が `fetchDashboardData` で enterprise 全 orders / stripes / audit_logs / members を毎回読み込みます。期間変更のたびに読み取りコストが 2 倍になります。→ 単一 Callable に統合するか、fetch 結果を handler 内で共有する構造にしてください（MVP 後でも可）。

**コメント要約**: ダッシュボード UI の並列 Callable 呼び出しにより `fetchDashboardData` が二重実行される。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: ⚡ 性能

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: D-6 / D-14 で Callable 2 本は仕様どおりだが、各 handler が同一フルスキャンを独立実行する実装は D-12（最大 24 ヶ月）・企業規模拡大でタイムアウト・コストリスク。MVP 必須ではないが改善推奨。

---

**識別子**: RC-7（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/enterprise/dashboard.ts:83`

**該当コード（レビュー時点の diff）**:

```diff
+  const memberEmails = await Promise.all(
+    rawMembers.map(async (member) => {
+      const personal = await getUserPersonalInformation(member.user_id)
+      return [member.user_id, personal?.user_email ?? ''] as const
+    }),
+  )
+  const emailByUserId = new Map(memberEmails)
+
+  const members: DashboardMemberMeta[] = rawMembers.map((member) => ({
+    user_id: member.user_id,
+    display_name: member.display_name ?? '',
+    email: emailByUserId.get(member.user_id) ?? '',
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `getEnterpriseMembers` は `member.user_email` に切り替わっていますが、`fetchDashboardData` は依然としてメンバーごとに `getUserPersonalInformation` を N+1 呼び出ししています。→ `member.user_email` を正本として使い N+1 を削除してください。`04_詳細_ダッシュボード.md` の email ソース記述も `EnterpriseMember.user_email` に更新が必要です。

**コメント要約**: ダッシュボード email 取得が user_email 移行方針と不整合で N+1 残存。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: ⚡ 性能

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `dashboardData.ts` で `member.user_email` を正本として利用し UPI N+1 を削除済み（a4832e8b 付近）。本セッションで RC 記録を更新（2026-06-30）。

---

**識別子**: RC-8（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/stores/dashboard.ts:52`

**該当コード（レビュー時点の diff）**:

```diff
+export const listOrderCreateAuditLogs = async (enterpriseId: string): Promise<AuditLog[]> => {
+  const snapshot = await getAuditLogsCollectionRef(enterpriseId)
+    .where('action', '==', 'order_create')
+    .where('target_type', '==', 'order_session')
+    .get()
+  return snapshot.docs.map((doc) => doc.data()).filter((log) => log.target_id == null || log.target_id === '')
+}
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `listOrderCreateAuditLogs` は `action` + `target_type` の複合 where ですが、`firestore.indexes.json` に `audit_logs` 用 composite index が見当たりません。→ sandbox デプロイ時に index エラーにならないか検証し、必要なら index を追加してください。

**コメント要約**: audit_logs サブコレクションの複合クエリに index 未定義の可能性。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: #2128 で `firestore.indexes.json` に `audit_logs` の `action + target_type` composite index を追加済み（`926c2217` 付近）。dashboard の `listOrderCreateAuditLogs` クエリをカバーする。

---

**識別子**: RC-9（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/utils/enterpriseAuthHelpers.ts:6`

**該当コード（レビュー時点の diff）**:

```diff
（本 PR diff 対象外・現行コード）
export async function assertEnterpriseAdmin(auth: CallableRequest['auth'], enterpriseId: string): Promise<void> {
  if (auth?.uid == null) {
    throw new HttpsError('unauthenticated', 'not logged in')
  }
  const tokenEnterpriseId = auth.token.enterprise_id as string | undefined
  if (tokenEnterpriseId !== enterpriseId) {
    throw new HttpsError('permission-denied', 'enterprise admin only')
  }
  const member = await getEnterpriseMember(enterpriseId, auth.uid)
  if (member == null || member.role !== 'admin' || !member.is_active) {
    throw new HttpsError('permission-denied', 'enterprise admin only')
  }
}
```

**レビュワーのコメント（原文）**:

🚨 **必須修正** [📋仕様追加/M]: Firestore Rules は `firebase.tenant` と `enterprises.tenant_id` を照合していますが、`assertEnterpriseAdmin` は `enterprise_id` claim と member ロールのみ確認しています。デフォルトプール（tenant 無し）の legacy ユーザーが `enterprise_id` claim を持っていれば、Rules では拒否されても Callable（ダッシュボード等）が通る可能性があります。→ `auth.token.firebase.tenant` と `enterprises/{id}.tenant_id` を照合してください（`05_認証・テナント.md` §0.5、クライアント `isEnterpriseAuthTenantConsistent` と同等）。

**コメント要約**: Callable 側に tenant 照合が無く Rules とセキュリティ境界が非対称。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: `enterpriseAuthHelpers.ts` に `user_type === 'enterprise'`、`firebase.tenant` と `enterprises.tenant_id` の照合、`getEnterpriseById` 読み取りを追加（2026-06-29）。Rules §0.5 とクライアント guard と同等の境界に整合。

---

**識別子**: RC-10（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `（PR #2120 全体）`

**該当コード（レビュー時点の diff）**:

```
（該当なし — プロセス指摘）
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [👀確認のみ/—]: WS-B / WS-C / E-4 / D-2 が 1 PR に同居しています。コミット分割は良好ですが、レビュー・ロールバック・部分マージのコストが高いです。→ 今後は workstream 単位の PR 分割を推奨します（本 PR の分割は必須ではない）。

**コメント要約**: 1 PR 多 workstream の運用コストに関する確認。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: マージブロッカーではない。MVP 全体計画の workstream 横断 PR として許容可能だが、以降の改善提案。

---

## 評価セッション

- **評価日時**: 2026-06-29 21:31 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 2026-06-29T12:18:15Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 GitHub id: 4832445208、RC-7 と同一指摘 GitHub id: 3491670917）
- **partial evaluate**: あり（Codex 未レビューまたは limits/connect のみ。Copilot 5 件）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-11 | 3491670805 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📋 仕様追加 | S | UI 期間上限を common から 12 ヶ月 import（D-12 更新） |
| [x] | RC-12 | 3491670853 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📋 仕様追加 | S | getDefaultDashboardPeriod を先月〜来月（3ヶ月）に設定（D-7 更新） |
| [x] | RC-13 | 3491670888 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | deploy_functions enterprise 群に getEnterpriseAuditLogs を追加 |
| [ ] | RC-14 | 3491670939 | 🟡 修正提案 | 📌 スコープ内 | 📑 仕様書 | 📐 リファクタ | M | fetchDashboardData が期間無視で全件取得<br>注文増でタイムアウト・コスト増。レンジ絞り込み推奨 |

---

**識別子**: RC-11（GitHub id: 3491670805）

**レビュワー**: Copilot

**指摘箇所**: `enterprise/src/utils/adminDashboardPeriod.ts:8`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,120 @@
+import { DateTime } from 'luxon'
+import { DEFAULT_TIME_ZONE } from '@shokujii/common/utils/datetime.js'
+import { parseYearMonth } from '@shokujii/common/utils/isEnterpriseMemberBillableInYearMonth.js'
+
+/** 選択肢に含める過去方向の月数（当月を含む）。MVP は12ヶ月、将来拡張予定 */
+export const DASHBOARD_YEAR_MONTH_OPTION_PAST_MONTHS = 12
+export const MAX_DASHBOARD_PERIOD_MONTHS = 12
```

**レビュワーのコメント（原文）**:

[must] ダッシュボード期間の上限が仕様（最大24ヶ月）と一致していません。`MAX_DASHBOARD_PERIOD_MONTHS = 12` のままだと 24 ヶ月レンジが UI/バリデーションで選択できず、`04_詳細_ダッシュボード.md` の D-12（最大24ヶ月）に反します。`buildYearMonthOptions` の選択肢も最大幅を満たす月数が必要です。

**コメント要約**: 期間上限定数が 12 のままで仕様 D-12（最大 24 ヶ月）と不一致。
UI の選択肢・clamp・validate も 24 ヶ月幅に揃える必要がある。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📋 仕様追加

**想定工数**: S

**判断理由**: 仕様変更（D-12: 最大12ヶ月）に合わせ `MAX_DASHBOARD_PERIOD_MONTHS=12` を common に定義し UI/Callable で共有（2026-06-29）。

---

**識別子**: RC-12（GitHub id: 3491670853）

**レビュワー**: Copilot

**指摘箇所**: `enterprise/src/utils/adminDashboardPeriod.ts:88`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・末尾抜粋）

+/** デフォルト: 先月〜来月（先月・今月・来月の3ヶ月） */
+export function getDefaultDashboardPeriod(nowMillis = Date.now()): DashboardPeriod {
+  const current = DateTime.fromMillis(nowMillis).setZone(DEFAULT_TIME_ZONE).startOf('month')
+  return {
+    start_year_month: current.minus({ months: 1 }).toFormat('yyyy-MM'),
+    end_year_month: current.plus({ months: 1 }).toFormat('yyyy-MM'),
+  }
+}
```

**レビュワーのコメント（原文）**:

[must] 期間デフォルトが仕様（直近12ヶ月）と異なります。現状は「先月〜来月（3ヶ月）」になっており、`04_詳細_ダッシュボード.md` の D-7「直近12ヶ月」を満たしません。

**コメント要約**: `getDefaultDashboardPeriod` が 3 ヶ月レンジを返しており D-7（直近 12 ヶ月デフォルト）と不一致。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📋 仕様追加

**想定工数**: S

**判断理由**: 仕様変更（D-7: 先月・当月・来月）に合わせ `getDefaultDashboardPeriod` を3ヶ月幅に設定。テスト更新済み（2026-06-29）。

---

**識別子**: RC-13（GitHub id: 3491670888）

**レビュワー**: Copilot

**指摘箇所**: `.github/workflows/deploy_functions.yml:87`

**該当コード（レビュー時点の diff）**:

```diff
@@ -83,7 +83,7 @@ jobs:
             enterprise)
-              echo "args=--force --only functions:createEnterprise,...(省略)...functions:acceptInvitationForEnterpriseCommunityManager" >> "$GITHUB_OUTPUT"
+              echo "args=--force --only functions:createEnterprise,...(省略)...functions:getDashboardMonthlyData,functions:getDashboardMemberData" >> "$GITHUB_OUTPUT"
               ;;
```

**レビュワーのコメント（原文）**:

[must] enterprise グループの selective deploy 対象に `getEnterpriseAuditLogs` が含まれていません。今回 `functions/default/src/index.ts` で export し、enterprise 側 UI/API も追加しているため、enterprise デプロイを回しても監査ログ Callable がデプロイされず動作しない可能性があります。

**コメント要約**: `deploy_functions.yml` の enterprise 群に `getEnterpriseAuditLogs` が未追加。
D-3 追加後は selective deploy で Callable が欠落する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `deploy_functions.yml` enterprise 群の `--only` に `functions:getEnterpriseAuditLogs` を追加（2026-06-29）。

---

**識別子**: RC-14（GitHub id: 3491670939）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/enterprise/dashboard.ts:45`

**該当コード（レビュー時点の diff）**:

```diff
+async function fetchDashboardData(enterpriseId: string): Promise<DashboardFetchResult> {
+  const [enterprise, rawOrders, rawStripes, rawAuditLogs, rawMembers] = await Promise.all([
+    getEnterpriseById(enterpriseId),
+    listOrderedMemberOrdersByEnterprise(enterpriseId),
+    listStripesByEnterprise(enterpriseId),
+    listOrderCreateAuditLogs(enterpriseId),
+    listEnterpriseMembers(enterpriseId),
+  ])
```

**レビュワーのコメント（原文）**:

期間指定（start/end）を受け取っているのに、`fetchDashboardData` 側が `member_orders` / `stripes` / `audit_logs` を期間で絞らず全件取得しています。企業の注文数が増えると Callable がタイムアウト・課金増・UI 体験悪化につながるので、最低限 `updated_at`/`timestamp` のレンジを追加して D-12（最大24ヶ月）前提の読み取り量に抑える設計に寄せたいです（追加した複合インデックスも活かせます）。

**コメント要約**: Callable は期間パラメータを受けるが store 読み取りが全件。
スケール時にタイムアウト・コスト増。期間レンジでの Firestore 絞り込みが望ましい。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: D-12 は読み取りコスト考慮を明記。MVP 初期データ量では動作するが設計上の負債。マージ必須ではないが、RC-11/12 と合わせて期間設計を揃える際に store クエリへレンジ filter を入れるのが妥当。RC-6（二重 fetch）とも関連。

---

## 評価セッション

- **評価日時**: 2026-06-30 04:42 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 2026-06-29T19:33:38Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id: 4836274819、Codex 接続案内 id: 4836308590）
- **partial evaluate**: あり（Codex limits/connect のみ。Copilot トップレベル 1 件を評価）
- **備考**: Copilot コメント id:4836306666 内の members rollback・dashboard 全件取得は既存 RC-4 / RC-14 と重複のため新規 RC 化せず。末尾の D-7/D-12・deploy・N+1 修正済み言及は確認情報のためスキップ。RC-7 は commit a4832e8b で対応済みの可能性あり（別途 RC 更新可）。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-15 | 4836306666 | 👌 修正不要 | 📌 スコープ内 | 📑 仕様書 | 👀 確認のみ | — | `?id=` PDF パスに JWT 不要<br>04_請求 §3.3 仕様どおり（PF 同趣旨） |
| [x] | RC-16 | 4836306666 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | 並行 PDF 生成競合時に GCS orphan<br>`already_exists` 分岐で generated ファイル削除推奨 |

---

**識別子**: RC-15（GitHub id: 4836306666・enterpriseBillInvoice 認証）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/enterpriseBillInvoice.ts:218–228`

**該当コード（レビュー時点の diff）**:

```diff
+    const invoiceId = req.query.id
+    if (typeof invoiceId === 'string') {
+      try {
+        res.status(200).setHeader('Content-Type', 'application/pdf')
+        await streamInvoicePdf(enterpriseId, yearMonth, invoiceId, res)
+      } catch {
+        if (!res.headersSent) {
+          res.status(404).send('Invoice not found')
+        }
+      }
+      return
+    }
```

**レビュワーのコメント（原文）**:

[must] `?id=<invoiceId>` による PDF ストリーミングパスに認証チェックがありません。`invoiceId` は 256 ビットのランダム文字列のため推測は困難ですが、有効期限なしのトークンとして機能するため、IDが漏洩した場合は永続的に未認証でダウンロードできます。`?id` パスでも JWT 検証 + `assertEnterpriseAdminFromUid` を通すか、GCS の Signed URL（有効期限付き）に差し替えることを推奨します。

**コメント要約**: `?id=` クエリでの PDF 取得に認証が無く、invoiceId 漏洩時に永続ダウンロード可能になるリスクを指摘。
JWT 検証または Signed URL 化を推奨。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: `04_詳細_請求.md` §3.3 で `id` ありは**認証不要**と明記（経理担当者への URL 共有可能、PF版 `eventBillInvoice` 同趣旨）。256bit ランダム ID による capability URL パターンは仕様どおり。有効期限付き Signed URL は Phase 2 検討でよい。

---

**識別子**: RC-16（GitHub id: 4836306666・enterpriseBillInvoice 並行生成）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/enterpriseBillInvoice.ts:182–193`

**該当コード（レビュー時点の diff）**:

```diff
+  const setResult = await setInvoiceFileMeta(enterpriseId, yearMonth, generatedInvoiceId)
+  if (setResult === 'already_exists') {
+    const existingInvoiceId = await resolveCachedInvoiceId(enterpriseId, yearMonth)
+    if (existingInvoiceId != null && existingInvoiceId !== generatedInvoiceId) {
+      logger.info('Concurrent invoice generation resolved to existing meta', {
+        enterpriseId,
+        yearMonth,
+        generatedInvoiceId,
+        existingInvoiceId,
+      })
+      return existingInvoiceId
+    }
+  }
```

**レビュワーのコメント（原文）**:

[nits] `setResult === 'already_exists'` かつ `generatedInvoiceId !== existingInvoiceId` のケース（並行生成の競合）で、今回生成して GCS に書き込んだ `generatedInvoiceId` のファイルが孤立したまま残ります。容量への影響は軽微ですが、`getInvoiceFile(..., generatedInvoiceId).delete()` で不要ファイルを削除しておくと GCS コストと管理が整理されます。

**コメント要約**: 並行 PDF 生成で `already_exists` 解決時、今回書き込んだ GCS ファイルが orphan になる。
best-effort で generated ファイルを削除すると整理できる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `already_exists` かつ別 invoiceId 解決時に `getInvoiceFile(...).delete({ ignoreNotFound: true })` を best-effort 実行。`enterpriseBillInvoice.test.ts` に競合ケーステストを追加（2026-06-30）。

---

## 評価セッション

- **評価日時**: 2026-06-30 21:17 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 2026-06-30T12:01:56Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id: 4843168722、Codex 接続案内 id: 4843250815）
- **partial evaluate**: あり（Codex connect のみ。Copilot トップレベル 1 件を評価）
- **備考**: #2137/#2139 UI 確認は Copilot コメント内で問題なし。RC 化せず本セッション備考に記録。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-17 | 4843249247 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 人気クエリ: inequality と orderBy フィールド不一致<br>`event_num_members >= 0` 追加推奨 |
| [x] | RC-18 | 4843249247 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 開催予定クエリ: event_end_datetime と orderBy 不一致<br>`event_num_members >= 0` 追加推奨 |
| [x] | RC-19 | 4843249247 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 終了クエリ: event_end_datetime と orderBy 不一致<br>`event_num_members >= 0` 追加推奨 |
| [x] | RC-20 | 4843249247 | 👌 修正不要 | 📌 スコープ内 | 📑 仕様書 | 👀 確認のみ | — | D-1 メール PF only skip は 04_メール配信と実装一致 |

---

**識別子**: RC-17（GitHub id: 4843249247・popularEventListStore）

**レビュワー**: Copilot

**指摘箇所**: `enterprise/src/pages/index.vue:38–39`

**該当コード（レビュー時点の diff）**:

```typescript
      where('event_deadline_datetime', '>', now),
      orderBy('event_num_members', 'desc'),
```

**レビュワーのコメント（原文）**:

[must] #2138 で `where('event_num_members', '>=', 1)` を削除した結果、Firestore クエリの制約違反が発生します。

Firestore では「range inequality フィルタ（`<`, `>` 等）を使う場合、最初の `orderBy` は同じフィールドでなければならない」というルールがあります。user 版 (`user/src/pages/index.vue`) は `where('event_num_members', '>=', 1)` + `where('event_deadline_datetime', '>', now)` + `orderBy('event_num_members', 'desc')` という構成で、`orderBy` が `event_num_members` の inequality と対応していました。enterprise 版から `event_num_members >= 1` を外すと、残る range フィルタは `event_deadline_datetime > now` のみとなり、`orderBy('event_num_members', 'desc')` が違うフィールドを指すためランタイムエラーになります。

修正案: `where('event_num_members', '>=', 0)` を追加することで 0 人イベントも表示しつつ Firestore の制約を満たせます。

**コメント要約**: #2138 で event_num_members 下限を外した結果、人気クエリの range フィルタと orderBy が不一致。
`event_num_members >= 0` を追加すれば 0 人表示と制約充足の両立が可能。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: Firestore の inequality と orderBy 同一フィールド規則に照らし指摘は妥当。PF 版は event_num_members >= 1 が orderBy と整合。エンプラ版は >= 0 追加が #2138 意図と両立する最小修正。`enterprise/src/pages/index.vue` の人気クエリに `where('event_num_members', '>=', 0)` を追加済み。

---

**識別子**: RC-18（GitHub id: 4843249247・upcomingEventListStore）

**レビュワー**: Copilot

**指摘箇所**: `enterprise/src/pages/index.vue:63–64`

**該当コード（レビュー時点の diff）**:

```typescript
      where('event_end_datetime', '>', now),
      orderBy('event_start_datetime', 'asc'),
```

**レビュワーのコメント（原文）**:

[must] 同様の理由です。`where('event_end_datetime', '>', now)` のみが range フィルタとなり、`orderBy('event_start_datetime', 'asc')` は別フィールドを指しているため Firestore エラーになります。user 版は `event_num_members >= 1` が存在してインデックスの起点を提供していました。

修正案: `where('event_num_members', '>=', 0)` を追加してください。

**コメント要約**: 開催予定クエリも event_end_datetime の inequality と event_start_datetime の orderBy が不一致。
event_num_members >= 0 追加を推奨。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-17 と同根。PF 版は event_num_members >= 1 によりクエリ形状が成立。エンプラ版は >= 0 追加が必要。`enterprise/src/pages/index.vue` の開催予定クエリに `where('event_num_members', '>=', 0)` を追加済み。

---

**識別子**: RC-19（GitHub id: 4843249247・pastEventListStore）

**レビュワー**: Copilot

**指摘箇所**: `enterprise/src/pages/index.vue:86–87`

**該当コード（レビュー時点の diff）**:

```typescript
      where('event_end_datetime', '<=', now),
      orderBy('event_start_datetime', 'desc'),
```

**レビュワーのコメント（原文）**:

[must] `where('event_end_datetime', '<=', now)` のみが range フィルタで、`orderBy('event_start_datetime', 'desc')` は別フィールドになっています。上記 2 件と同じ Firestore クエリ制約違反です。

修正案: `where('event_num_members', '>=', 0)` を追加してください。

**コメント要約**: 終了クエリも inequality フィールドと orderBy が不一致。
event_num_members >= 0 追加を推奨。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-17/18 と同根。3 クエリすべてに >= 0 を足すのが #2138 仕様を満たす修正。`enterprise/src/pages/index.vue` の終了クエリに `where('event_num_members', '>=', 0)` を追加済み。

---

**識別子**: RC-20（GitHub id: 4843249247・D-1 メール配信制御）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/utils/enterpriseMail.ts` 等（D-1 メール配信制御）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

**D-1 メール配信制御（#2135）の確認**

仕様書 `04_詳細_メール配信.md` の PF only リストと実装を照合しました。`popularEventMail.ts`・`remindUnorderedMail.ts`・`eventInformationMail.ts` 等の skip 処理は仕様通りです。`sendOrderCompletionMailToMember`（メール#1）はエンプラ skip なし（PF + Enterprise 対象）で、これも仕様に沿っています。問題なしと判断します。

**コメント要約**: D-1 メール PF only skip と #1 送信対象は 04_メール配信と実装が一致。
Copilot も問題なしと判断。

**判断結果**: 👌 修正不要

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 仕様書 04_詳細_メール配信 §3 と実装の照合結果として妥当。追加対応不要。

---

## 評価セッション

- **評価日時**: 2026-07-01 09:29 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 2026-07-01T00:16:43Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id: 4849045563、Codex 接続案内 id: 4849078784）
- **partial evaluate**: あり（Codex connect のみ。Copilot トップレベル id: 4849077737 を評価）
- **備考**: `?id=` PDF 認証・dashboard 期間無視全件取得は既存 RC-15 / RC-14 と重複のため新規 RC 化せず。Copilot「対応済みの確認」5 項目は確認情報のためスキップ。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-21 | 4849077737 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | useEnterpriseAdmin が raw getDoc<br>store + withConverter 経由を推奨 |
| [x] | RC-22 | 4849077737 | ✅ 対応済み | 📌 スコープ内 | ⚡ 性能 | 📐 リファクタ | M | auditLogs buildDisplayContext が getCommunity を N 並列<br>バルク取得パターン推奨 |

---

**識別子**: RC-21（GitHub id: 4849077737・useEnterpriseAdmin）

**レビュワー**: Copilot

**指摘箇所**: `enterprise/src/composable/useEnterpriseAdmin.ts:34–50`

**該当コード（レビュー時点の diff）**:

```typescript
export async function loadEnterpriseDocument(enterpriseId: string): Promise<EnterpriseDocumentData | null> {
  const snapshot = await getDoc(doc(db, 'enterprises', enterpriseId))
  if (!snapshot.exists()) return null
  const data = snapshot.data()
  return {
    company_name: typeof data.company_name === 'string' ? data.company_name : '',
    // ... 手動マッピング
  }
}
```

**レビュワーのコメント（原文）**:

[imo] `loadEnterpriseDocument` が `getDoc(doc(db, 'enterprises', enterpriseId))` でクライアント側から直接 Firestore を読んでいます。プロジェクト規約（`shokujii-firestore`）ではストア経由の `withConverter` 付き参照が必須であり、この箇所だけ raw `snapshot.data()` を手動マッピングするパターンになっています。`base/src/stores/enterprise.ts` に既存の store 関数があれば、そちらを再利用することで Zod バリデーションと converter を活かせます。

**コメント要約**: enterprise ドキュメント読み取りが store/converter 経由でなく raw getDoc + 手動マッピング。
withConverter 付き store 再利用を推奨。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: 規約上 store 経由が正本。`base/src/stores/enterprise.ts` に `getEnterpriseRef` / `getEnterpriseById`（withConverter）を追加し、`loadEnterpriseDocument` から利用するよう変更済み。

---

**識別子**: RC-22（GitHub id: 4849077737・auditLogs buildDisplayContext）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/enterprise/auditLogs.ts:68–90`

**該当コード（レビュー時点の diff）**:

```typescript
  await Promise.all(
    Array.from(communityIds).map(async (communityId) => {
      const community = await getCommunity(communityId)
      if (community == null || community.enterprise_id !== enterpriseId) {
        return
      }
      communityNames.set(communityId, community.community_name)
    }),
  )

  const eventsByKey = await getEventsInCommunities(eventRefs)
```

**レビュワーのコメント（原文）**:

[imo] `getCommunity` を `communityIds` の件数分 `Promise.all` で並列取得しています。コミュニティ数が多い場合（ページ当たりの監査ログ数×コミュニティ数で）Firestore 読み取りが集中します。`listCommunitiesByIds` のような一括取得 or `getEventsInCommunities` と同様のバルク参照パターンに切り替えると、Firestore の読み取りコストを削減できます。

**コメント要約**: 監査ログ表示名解決で community を 1 件ずつ getCommunity 並列取得。
event 側は getEventsInCommunities でバルク化済み。community も同様のバルク化を推奨。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: ⚡ 性能

**変更種別**: 📐 リファクタ

**想定工数**: M

**判断理由**: 指摘は妥当。`functions/default/src/stores/community.ts` に `getCommunitiesByIds`（getAll チャンク）を追加し、`buildDisplayContext` で利用。テスト追加済み。

---

## 評価セッション

- **評価日時**: 2026-07-01 10:21 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto）
- **ブランチ名**: dev/enterprise-mvp-v2
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2120
- **REVIEW_REQUEST_SINCE**: 2026-07-01T01:12:19Z
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 4（レビュー依頼定型文 id: 4849339243、Codex usage limits id: 4849340192、Codex connect 案内 id: 4849355075、Copilot 対応状況報告 id: 4849353928）
- **partial evaluate**: あり（Codex substantive なし。Copilot インライン 0 件）
- **備考**: 新規 RC なし。Copilot トップレベルは前回指摘の修正報告と RC-14 相当の [imo] 見送り言及のみ。dashboard 全件取得は既存 RC-14、auditLogs バルク化は RC-22 対応済み。

### RC 一覧（サマリ）

（本セッション: 新規 RC なし）

---
