# PR #2046 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-29 23:30 JST
- **ブランチ名**: feat/2045
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2046
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 3

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub review id: 4389925633）— 👌 修正不要
- [x] **RC-2**（GitHub id: 4575804278）— ✅ 対応済み（3値戻り値・URLクリア条件・HTTPエラー false・UserAvatar ガード。sha256Hex 共通化は RC-18 通り任意のまま）
- [x] **RC-3**（GitHub id: 3324645040）— ✅ 対応済み（cancelled と avatar.value 一致ガード）
- [x] **RC-4**（GitHub id: 3324641330）— 👌 修正不要（パフォーマンスは imo）
- [x] **RC-5**（GitHub id: 3324645029）— ✅ 対応済み（placeholder 確定時のみ URL クリア）
- [x] **RC-6**（GitHub id: 3324641366）— ✅ 対応済み（catch で URL クリア削除・createModuleLogger・provider 判定）
- [x] **RC-7**（GitHub id: 3324645032）— ✅ 対応済み（namesPrintPdf で URL 正規化）
- [x] **RC-8**（GitHub id: 3324641245）— ✅ 対応済み（RC-7 と同根）
- [x] **RC-9**（独立レビュー）— 👌 修正不要（3層構成・設計評価）
- [x] **RC-10**（独立レビュー）— ✅ 対応済み（RC-2/5/6 と同根）
- [x] **RC-11**（独立レビュー）— ✅ 対応済み（RC-7/8 と同根）
- [x] **RC-12**（独立レビュー）— ✅ 対応済み（RC-2/3 と同根）
- [x] **RC-13**（独立レビュー）— ✅ 対応済み（isGoogleUnavailableAvatar の HTTP エラー時 false）
- [x] **RC-14**（独立レビュー）— ✅ 対応済み（RC-6 と同根）
- [x] **RC-15**（独立レビュー）— ✅ 対応済み（コメントを実装に合わせて修正）
- [x] **RC-16**（独立レビュー）— 👌 修正不要（PDF/UI デフォルト不一致は意図的）
- [x] **RC-17**（独立レビュー）— 👌 修正不要（FOUC・UX 判断）
- [x] **RC-18**（独立レビュー）— 👌 修正不要（sha256Hex 共通化・テスト追加は任意）
- [x] **RC-19**（独立レビュー）— ✅ 対応済み（PR 差分は #2045 関連15ファイルのみに整理済み）
- [x] **RC-20**（GitHub id: 4576566826）— 👌 修正不要（Copilot 追記2点：/a-/ デッドコード指摘は誤り、sha256Hex は RC-18 通り任意）
- [x] **RC-21**（GitHub id: 3325120055）— ✅ 対応済み（isGoogleUnavailableAvatar で fetch 前正規化）
- [x] **RC-22**（GitHub id: 3325144348）— ✅ 対応済み（RC-21 と同根）
- [x] **RC-23**（GitHub id: 3328435302）— ✅ 対応済み（isGoogleProfileImageUrl の hostname 厳密検証）
- [x] **RC-24**（GitHub id: 3328559431）— 👌 修正不要（空文字は NonEmptyStringSchema で FieldValue.delete に変換）

**レビュー非該当スキップ（RC なし）**

- GitHub id: 4575713810 — レビュー依頼定型文
- GitHub id: 4575805992 — Codex 接続案内のみ
- GitHub review id: 4389930661 — Codex 接続案内ボイラープレート（具体指摘はインライン）

---

**識別子**: RC-1（GitHub review id: 4389925633）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
## Pull request overview

Google の停止・削除アカウント時に返るプレースホルダー（斜線）アバター画像を SHA-256 ハッシュで検出し、shokujii のデフォルトアバターに置き換える対応。表示層（UserAvatar）、ログイン時のサーバ処理（updateProfileFromProviders）、名札 PDF 生成（namesPrintPdf）の3層に同じ判定ユーティリティ（common/utils/googleProfileImage）を適用する。

**Changes:**
- `common/utils/googleProfileImage` に Google プロフィール画像 URL 判定・サイズ正規化・既知プレースホルダーの SHA-256 判定・Blob 取得 util と単体テストを追加
- `functions/default` の `updateProfileFromProviders` で Google 画像を取得・検証して Storage 保存、プレースホルダー時は `user_image_url=''` にクリア。`namesPrintPdf` の `resolvePhotoUrl` でもハッシュ判定してデフォルト画像にフォールバック
- `base` の `UserAvatar` で Google URL 表示前にプレースホルダー検証＋読み込み失敗時のフォールバック、`getDefaultAvatarUrl` で user_id ハッシュから8種のデフォルト画像を選択

### Reviewed changes

Copilot reviewed 6 out of 14 changed files in this pull request and generated 3 comments.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| common/src/utils/googleProfileImage.ts | URL 判定・サイズ正規化・SHA-256・プレースホルダー検証・Blob 取得 util を追加 |
| common/src/utils/googleProfileImage.test.ts | URL 判定・サイズ正規化・ハッシュ判定のユニットテストを追加 |
| functions/default/src/user.ts | Google 画像を取得し有効なときのみ Storage 保存、プレースホルダー/取得失敗時は user_image_url をクリア |
| functions/default/src/utils/namesPrintPdf.ts | 取得した画像のハッシュが既知プレースホルダーならデフォルト画像に置き換え |
| base/src/components/UserAvatar.vue | Google URL の表示前検証、エラー時のデフォルトフォールバック、宣言順整理 |
| base/src/utils/defaultAvatar.ts | user_id ハッシュで default_profile_01〜08.png から1枚を返すユーティリティを追加 |
| base/src/assets/images/avatars/default_profile_01..08.png | デフォルトアバター画像アセット追加 |
</details>
```

**コメント要約**: PR の変更概要とファイル別サマリ。具体的な修正要求は含まない。

**判断結果**: 👌 修正不要

**判断理由**: 変更内容の要約のみで、コード修正を求める指摘ではない。以降のインラインコメントで具体論が出ているため、この overview 自体への対応は不要。

---

**識別子**: RC-2（GitHub id: 4575804278）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

インラインコメント API の権限がないため、該当ファイル・行番号を明示した 1 指摘 1 ブロック形式でまとめます。

---

**`functions/default/src/user.ts` L213**

🔴 **必須修正**: `fetchGoogleProfileImage` は「プレースホルダー確定」と「HTTP エラー・一時ネットワーク障害」の両方で `null` を返しますが、どちらの場合も `user_image_url` をクリアしています。Google サーバーの一時障害（503 等）が発生するたびに既存ユーザーの画像 URL が消えるリスクがあります。

`fetchGoogleProfileImage` のシグネチャを `Blob | null | 'placeholder'` のように変更するか、HTTP エラー時は例外を throw して catch ブロックに委ね、「プレースホルダー確定時のみ `user_image_url = ''`」とする設計を検討してください。

---

**`functions/default/src/user.ts` L226**

🔴 **必須修正**: catch ブロックでも `user_image_url = ''` をセットしています。`fetchGoogleProfileImage` 内の `fetch` が一時的なネットワーク障害で例外を throw した場合もここに到達するため、一時エラーで既存 URL が消去されます。

Facebook / Twitter のエラー時は URL をクリアしていないことと挙動が一貫していません。Google も「プレースホルダーと確定できた場合のみクリア」になるよう、catch ブロックでは `user_image_url` を変更しない設計に修正することを推奨します。

---

**`common/src/utils/googleProfileImage.ts` L30**

🟡 **提案**: `!response.ok`（HTTP 4xx/5xx）の場合も `true`（プレースホルダー確定）を返しています。Google サーバーの一時障害（503 等）や URL 仕様変更時にも「無効アカウント」と誤判定し、有効なユーザーのアバターがデフォルト画像に置き換わる可能性があります。

「ネットワーク障害 = 判定不能」として `false` を返す（呼び出し側で判定保留）か、`throw` して呼び出し側で区別する設計を検討してください。

---

**`base/src/components/UserAvatar.vue` L70**

🟡 **提案**: `watch` の非同期コールバック内で `await isGoogleUnavailableAvatar(url)` を実行していますが、コンポーネントのアンマウント後に非同期処理が完了すると、アンマウント済みコンポーネントの `useDefaultAvatar.value` に書き込みが発生します。

`let cancelled = false` フラグと `onUnmounted(() => { cancelled = true })` を組み合わせるか、`AbortController` を使ってアンマウント時・watch 再実行時にフェッチをキャンセルする処理の追加を推奨します。

---

**`functions/default/src/utils/namesPrintPdf.ts` L132**

🟢 **任意**: `common` に `sha256Hex(data: Uint8Array): Promise<string>` が定義されているため、`import { createHash } from 'crypto'` による独自実装を避けて共通ユーティリティを利用できます。`Buffer` は `Uint8Array` のサブクラスなので `await sha256Hex(photoBuffer)` でそのまま渡せます。

```typescript
import { isGoogleProfileImageUrl, isGoogleUnavailableAvatarHash, sha256Hex } from '@shokujii/common/utils/googleProfileImage.js'
// ...
const hashHex = await sha256Hex(photoBuffer)
```
```

**コメント要約**: トップレベルに5点をまとめた指摘。user.ts の URL クリア条件、クライアント側 HTTP エラー時の誤判定、UserAvatar のアンマウント後書き込み、namesPrintPdf の sha256Hex 共通化。

**判断結果**: 🚨 修正推奨

**判断理由**: squash 後の現行コードでも user.ts L212-227 の URL クリア問題は未解消。isGoogleUnavailableAvatar の !response.ok 時 true 返却も L39-40 に残存。UserAvatar に cancelled ガードも未実装。RC-5・RC-6・RC-3 と内容が重複するが、いずれも妥当な指摘。namesPrintPdf の sha256Hex 共通化のみ任意で 👌 だが、必須修正を含むためコメント全体は修正推奨とする。

---

**識別子**: RC-3（GitHub id: 3324645040）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  古い Google 判定結果で新しい avatar を上書きしないでください**

同じ `UserAvatar` インスタンスがユーザー切替やリスト再利用で別の `avatar` に変わった後に、前の Google URL の `isGoogleUnavailableAvatar` が解決すると、その古い結果で `useDefaultAvatar` を上書きします。前の URL がプレースホルダーだった場合、新しいユーザーが通常画像でもデフォルト表示に固定されるため、解決時に現在の `avatar.value` と一致するか確認するか watcher の invalidate で古いリクエストを無視してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: watch 内の非同期判定が遅延完了したとき、古い URL の結果で新しい avatar 表示を上書きするレースがある。

**判断結果**: 🚨 修正推奨

**判断理由**: 参加者一覧で v-for 再利用時に実害が出うる。watch 開始時に url を capture し、await 後に avatar.value と一致する場合のみ useDefaultAvatar を更新する、または cancelled フラグで invalidate するのが Vue の定石。RC-2 のアンマウント指摘と合わせて1つのガード実装で両方解消できる。

---

**識別子**: RC-4（GitHub id: 3324641330）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `watch(avatar, ...)` で Google URL ごとに `isGoogleUnavailableAvatar` から `fetch` + `arrayBuffer` + SHA-256 を実行するため、参加者一覧など UserAvatar を多数並べる画面で、各アバターにつき `<v-img>` 用の画像取得とは別にもう1回バイナリ取得が走り、リクエスト数・帯域・CPU（ハッシュ計算）がユーザー数だけ増えます。

本 PR で `updateProfileFromProviders` がログイン時に Google 画像を Storage にアップロード（または `user_image_url=''` にクリア）するため、移行が一週すれば `user_image_url` は `gs://` か空のいずれかになり、クライアント側のハッシュ判定は基本的に不要になります。少なくとも以下のいずれかの緩和を検討してください:

- URL → 判定結果を module-scope の Map にキャッシュし、同一 URL の重複 fetch を避ける
- または、`user_image_url` が `https://lh3.googleusercontent.com/...` のまま残るのは未移行ユーザーのみであることを前提に、クライアント側ハッシュ判定を削り、`<v-img>` の `@error` フォールバック（既に追加されている `onError` → `useDefaultAvatar = true`）のみで対応する
```

**コメント要約**: 一覧画面で Google URL ごとに二重 fetch が発生するパフォーマンス懸念。キャッシュまたは @error のみへの簡略化を提案。

**判断結果**: 👌 修正不要

**判断理由**: imo レベルの提案。ログイン時 Storage 移行により Google URL 直リンクは未移行ユーザーのみに限定され、恒久運用での負荷は限定的。Map キャッシュは改善として有効だがマージ必須ではない。斜線プレースホルダーは HTTP 200 で返るため @error のみでは検出できず、現 PR のハッシュ判定は機能上必要。将来の最適化候補として PR 本文や Issue にメモする程度でよい。

---

**識別子**: RC-5（GitHub id: 3324645029）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Google 画像の取得失敗で既存 URL を消さないでください**

Googleusercontent が一時的に 429/500 を返す、またはネットワークエラーになる場合でも、`fetchGoogleProfileImage` は `null`（または catch 側で同様）として扱われ、この行で既存の有効な Google `user_image_url` を空文字で保存してしまいます。プレースホルダー判定と取得失敗を区別しないと、ログインしただけでプロフィール画像が失われ、次回成功するまで画面や PDF がデフォルト表示になります。

Useful? React with 👍 / 👎.
```

**コメント要約**: fetchGoogleProfileImage が null を返すすべてのケースで user_image_url をクリアしており、一時障害でも画像 URL が消える。

**判断結果**: 🚨 修正推奨

**判断理由**: 現行の fetchGoogleProfileImage は !response.ok とプレースホルダーハッシュ一致の両方で null を返す。user.ts L212-214 は null なら無条件クリアのため、503 等でも URL 消失する。Facebook / Twitter と非対称でデータ損失リスクが高い。プレースホルダー確定時のみクリアする戻り値設計が必要。

---

**識別子**: RC-6（GitHub id: 3324641366）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] catch 節で「Facebook / Twitter のいずれの prefix にもマッチしない」場合に provider を一律 `'Google'` と扱っていますが、try 内では Google プレフィックスにマッチしないと `fetch` 自体が走らないため、本来到達しないケースでも `imageUrl.startsWith('https://lh3.googleusercontent.com')` が false のまま `provider='Google'` のログが出る可能性があります（将来 try 内に別 provider の処理が追加された際に誤ったログになります）。

加えて、Google fetch の一時的なエラー（ネットワーク瞬断・5xx など）でも 225-227 行で `user.user_image_url = ''` にクリアしてしまうため、次回ログインで `provider.photoURL` から復元できなければ画像が消えたままになります。PR 説明にも「許容できるか確認したい」とある通り、ここはハッシュ一致で明示的にプレースホルダーと判定できた場合のみクリアし、fetch 例外時は既存値を維持するほうが安全です（`fetchGoogleProfileImage` 側でハッシュ不一致のときに区別できる戻り値にする、あるいは例外を分けるなど）。
```

**コメント要約**: catch で provider ログが誤りうるほか、Google の一時エラー時も user_image_url をクリアしている。

**判断結果**: 🚨 修正推奨

**判断理由**: L225-227 の Google 専用クリアは RC-5 と同根で未修正。catch 内 provider 推定も else Google フォールバックは保守性が低い。Google 分岐内で catch するか、imageUrl プレフィックスで provider を決めるよう修正すべき。RC-5 とまとめて対応可能。

---

**識別子**: RC-7（GitHub id: 3324645032）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  PDF 用の Google 画像を判定前に既知サイズへ正規化してください**

名札 PDF では保存済み URL をそのまま fetch してハッシュ化していますが、既知ハッシュは s50/s100/s500 のみなので、Firebase Auth 由来などで `=s96-c` の Google URL が保存されているユーザーでは無効アカウントのプレースホルダーを検出できず、そのまま PDF に埋め込まれます。ここも `normalizeGoogleProfileImageUrl(url, 500)` などで判定対象サイズを固定してから fetch してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: namesPrintPdf が URL を正規化せず fetch しているため、s96-c 等ではプレースホルダーを検出できない。

**判断結果**: 🚨 修正推奨

**判断理由**: fetchGoogleProfileImage は normalizeGoogleProfileImageUrl で s500-c に統一しているが、namesPrintPdf L131-135 は raw url で fetch している。既知ハッシュは s50/s100/s500 のみのため指摘は正しい。normalize 後 fetch に揃える必要がある。

---

**識別子**: RC-8（GitHub id: 3324641245）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] ここでは `url`（= `user.user_image_url` をそのまま渡したもの）を `fetch` してハッシュ判定していますが、`GOOGLE_UNAVAILABLE_AVATAR_HASHES` に登録されているのは `s50-c` / `s100-c` / `s500-c` の3サイズのみです。Firestore に保存されている Google URL がそれ以外のサイズ指定（例: `=s200-c`、サイズ未指定 など）の場合、プレースホルダー画像であってもハッシュが一致せず、斜線プレースホルダーがそのまま PDF に焼き込まれます。

`fetch` 前に `normalizeGoogleProfileImageUrl(url, 500)` で `s500-c` に正規化してから取得・ハッシュ判定するのが安全です（`fetchGoogleProfileImage` と判定基準を揃えられます）。
```

**コメント要約**: RC-7 と同内容。namesPrintPdf で fetch 前に s500-c へ正規化すべき。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-7 と同一論点。common の normalizeGoogleProfileImageUrl を namesPrintPdf の Google 分岐で useし、fetchGoogleProfileImage と判定基準を統一する修正が必要。

---

## 評価セッション

- **評価日時**: 2026-05-30 00:00 JST
- **ブランチ名**: feat/2045
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2046
- **レビュー種別**: 独立コードレビュー（`/shokujii-code-review`、GitHub コメント以外）
- **対象範囲**: #2045 / #2005 関連コミット5件（`common/googleProfileImage`、`base/UserAvatar`・`defaultAvatar`、`functions/user.ts`・`namesPrintPdf`）。ブランチ全体には client error 報告・community 整理等の無関係変更も含まれる

### RC 別対応チェックリスト

- [x] **RC-9**（独立レビュー）— 👌 修正不要（3層構成・設計評価）
- [x] **RC-10**（独立レビュー）— ✅ 対応済み（RC-2/5/6 と同根）
- [x] **RC-11**（独立レビュー）— ✅ 対応済み（RC-7/8 と同根）
- [x] **RC-12**（独立レビュー）— ✅ 対応済み（RC-2/3 と同根）
- [x] **RC-13**（独立レビュー）— ✅ 対応済み（isGoogleUnavailableAvatar の HTTP エラー時 false）
- [x] **RC-14**（独立レビュー）— ✅ 対応済み（RC-6 と同根）
- [x] **RC-15**（独立レビュー）— ✅ 対応済み（コメントを実装に合わせて修正）
- [x] **RC-16**（独立レビュー）— 👌 修正不要（PDF/UI デフォルト不一致は意図的）
- [x] **RC-17**（独立レビュー）— 👌 修正不要（FOUC・UX 判断）
- [x] **RC-18**（独立レビュー）— 👌 修正不要（sha256Hex 共通化・テスト追加は任意）
- [x] **RC-19**（独立レビュー）— ✅ 対応済み（PR 差分は #2045 関連15ファイルのみに整理済み）

---

**識別子**: RC-9（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
## 概要（設計評価）

3層で同じ問題に対処する構成は妥当。

| 層 | 役割 |
|---|---|
| common/googleProfileImage | Google 斜線プレースホルダーの SHA-256 判定（共通ロジック） |
| base/UserAvatar | 表示時の置き換え + 8種デフォルト |
| functions/user.ts | ログイン時に Storage 移行 or URL クリア |
| functions/namesPrintPdf | PDF 出力時の置き換え |

/a-/（青い Google デフォルト）は CORS 非対応のためクライアント側 fetch をスキップする判断は適切。user_id 固定ハッシュで8種から選ぶ defaultAvatar.ts もシンプルで意図が明確。
```

**コメント要約**: 問題の切り分けと common への集約は良い実装。`/a-/` スキップ・8種デフォルトの設計は妥当。

**判断結果**: 👌 修正不要

**判断理由**: 設計上の称賛・確認事項であり、コード修正は不要。RC-2〜RC-8 の指摘と矛盾しない前提評価。

---

**識別子**: RC-10（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `user.ts`: 一時的な fetch 失敗で `user_image_url` を消している

`fetchGoogleProfileImage` はプレースホルダー確定と HTTP エラー（503 等）の両方で `null` を返す。
L212-214 および catch L225-227 では `null` / 例外時に無条件で `user_image_url = ''` としている。
Facebook / Twitter は失敗時 URL を維持するのに対し非対称で、データ損失リスクがある。

修正方針: 戻り値を `'placeholder' | Blob | null`（または Result 型）に分離し、
ハッシュ一致でプレースホルダーと確定できた場合のみ `user_image_url = ''` にする。catch でも URL は維持する。
```

**コメント要約**: RC-2 / RC-5 / RC-6 と同根。ログイン時の Google URL クリア条件が厳しすぎる。

**判断結果**: 🚨 修正推奨

**判断理由**: 現行コード（`functions/default/src/user.ts` L210-227、`common/src/utils/googleProfileImage.ts` L51-56）を確認し、指摘は妥当。マージ前対応必須。

---

**識別子**: RC-11（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `namesPrintPdf.ts`: fetch 前に URL を正規化していない

既知ハッシュは s50-c / s100-c / s500-c のみ。
Firestore に `=s96-c` 等が残っているユーザーでは斜線プレースホルダーでもハッシュ不一致となり PDF にそのまま焼き込まれる。
`fetchGoogleProfileImage` と同様、`normalizeGoogleProfileImageUrl(url, 500)` 後に fetch する必要がある。
```

**コメント要約**: RC-7 / RC-8 と同根。PDF 側の Google URL 正規化漏れ。

**判断結果**: 🚨 修正推奨

**判断理由**: `namesPrintPdf.ts` L124-136 は raw url で fetch しており、`fetchGoogleProfileImage` と判定基準が不一致。修正必須。

---

**識別子**: RC-12（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `UserAvatar.vue`: 非同期 watch のレースコンディション

watch(avatar, async (url) => { ... useDefaultAvatar.value = await isGoogleUnavailableAvatar(url) })
参加者一覧などで同一コンポーネントインスタンスが別ユーザーに再利用されると、
古い URL の判定結果が後から返り、新しいユーザーを誤ってデフォルト表示に固定する。
アンマウント後の書き込みも同様。

修正方針: await 後に url === avatar.value を確認する、または watch 再実行 / onUnmounted で invalidate する。
```

**コメント要約**: RC-2 / RC-3 と同根。非同期判定の stale 結果問題。

**判断結果**: 🚨 修正推奨

**判断理由**: `UserAvatar.vue` L56-70 に cancelled / url 一致ガードが未実装。v-for 再利用で実害が出うる。

---

**識別子**: RC-13（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟡 **提案**: `isGoogleUnavailableAvatar`: HTTP エラー時に `true` を返している

common/src/utils/googleProfileImage.ts L38-40 で !response.ok の場合 true（プレースホルダー確定）を返す。
503 等の一時障害でも有効ユーザーがデフォルト表示に置き換わる。
サーバー側と同様、判定不能は false（現状維持）とする方が安全。
テスト（googleProfileImage.test.ts L54-58）もこの挙動を前提にしているため、仕様変更時はテスト更新が必要。
```

**コメント要約**: クライアント側でも HTTP エラーをプレースホルダー確定扱いしている。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-2 の 🟡 指摘と同一。表示層の誤判定リスクがあり、RC-10 と合わせて fetch 結果の三値（placeholder / ok / indeterminate）設計が望ましい。

---

**識別子**: RC-14（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟡 **提案**: `user.ts`: `console.warn` と provider 推定

Functions チェックリスト上は createModuleLogger を使うべき（既存も console.warn だが Google 分岐が増えている）。
catch 内の provider 推定は else が 'Google' 固定のため、将来 try 内に別処理を足すと誤ログになりうる。
Google 分岐内で try/catch するか、マッチした prefix から provider を決める方が保守しやすい。
```

**コメント要約**: ログ規約違反と catch 内 provider 推定の保守性。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-6 の provider ログ指摘と同根。RC-10 修正時にまとめて対応するのが効率的。console.warn は既存パターンだが、今回の変更範囲で createModuleLogger へ寄せる価値あり。

---

**識別子**: RC-15（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟡 **提案**: コメントと実装の不一致（既存含む）

user.ts L199-201「既存の画像がない場合のみ」とコメントがあるが、
実装は gs:// 以外の Google URL が残っている限り毎ログイン fetch + upload する（FB/Twitter も同様）。
意図どおりならコメント修正、そうでなければ gs:// または空のときだけ fetch するガードを検討。
```

**コメント要約**: コメント記述と upload 条件が一致していない。

**判断結果**: 🚨 修正推奨

**判断理由**: 誤解を招くコメントは修正すべき。毎ログイン upload が意図ならコメントを実装に合わせる。意図でなければ gs:// 判定ガードが必要。

---

**識別子**: RC-16（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟡 **提案**: PDF と UI のデフォルト画像が不一致

UI は8種 PNG（user_id 固定）、PDF は default_profile.jpeg 1枚のまま。
コミットメッセージで意図的と明記。名札と画面で見た目が揃わない点はプロダクト判断として認識しておく価値あり。
```

**コメント要約**: 名札 PDF と画面のデフォルト画像が異なる。

**判断結果**: 👌 修正不要

**判断理由**: 84919256 のコミットメッセージで functions 側は対象外と明記。意図的なスコープ切り出し。

---

**識別子**: RC-17（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟡 **提案**: プレースホルダー一瞬表示（FOUC）

判定完了まで useDefaultAvatar が false のため、斜線プレースホルダーが一瞬見えてから8種デフォルトに切り替わる。
6543b401 のコミットメッセージでも言及済み。許容範囲かどうかは UX 判断。
```

**コメント要約**: Google 判定中に斜線画像が一瞬表示される。

**判断結果**: 👌 修正不要

**判断理由**: 機能要件を満たすトレードオフ。PR 本文・手動確認項目として認識すれば足りる。

---

**識別子**: RC-18（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟢 **任意**:
- namesPrintPdf の SHA-256: common の sha256Hex を使えば Node / ブラウザで実装を揃えられる（RC-2 の 🟢 指摘と同一）
- テスト追加: fetchGoogleProfileImage のプレースホルダー / HTTP エラー区別、normalize のサイズ未指定 URL など
- パフォーマンス: 一覧画面で Google URL ごとに二重 fetch（RC-4 と同様、移行完了後は限定的。Map キャッシュは将来改善候補）
```

**コメント要約**: 共通化・テスト・パフォーマンスは任意改善。

**判断結果**: 👌 修正不要

**判断理由**: RC-4 と同様、マージ必須ではない。斜線プレースホルダーは @error のみでは検出不可のためハッシュ判定は必要。

---

**識別子**: RC-19（独立レビュー）

**レビュワー**: Cursor（shokujii-code-review）

**レビューのコメント（原文）**:

```
🟡 **提案**: コミット・PR — ブランチに #2045 以外の変更混在

feat/2045 ブランチ全体には client error 報告、community 整理、storage.rules 等 #2045 と無関係な変更が多数含まれる。
#2045 のレビューとマージ判断を分離するため PR 分割を検討。
```

**コメント要約**: 1 PR / 1 ブランチに複数責務が混在。

**判断結果**: 🚨 修正推奨

**判断理由**: shokujii-code-review チェックリスト「1つの PR に複数の責務を混在させていないか」に該当。アバター修正のマージ判断と無関係 diff のレビューコストを分離すべき。

---

### 独立レビュー総評

**問題の切り分けと common への集約は良い実装**。マージ前に特に次の3点は対応を推奨する。

1. **ログイン時の URL クリア条件**（プレースホルダー確定 vs 一時障害）— RC-10 / RC-13 / RC-14
2. **PDF の Google URL 正規化** — RC-11
3. **UserAvatar の非同期レースガード** — RC-12

修正後の手動確認推奨パターン: Google URL 残存ユーザー / 斜線プレースホルダー / 503 等の一時障害 / 参加者一覧でのユーザー切替。

### チェックリスト（#2045 関連・独立確認）

| カテゴリ | 結果 |
|---|---|
| TypeScript・型安全性 | ✅ 問題なし |
| falsy チェック | ✅ 適切 |
| Vue リアクティビティ | ✅ watch 非同期レース対応済み（RC-12） |
| Vue コンポーネント設計 | ✅ 問題なし |
| Firebase Functions | ✅ createModuleLogger・URL クリア条件修正済み（RC-10/14） |
| common util 再利用 | ✅ namesPrintPdf で normalize 使用済み（RC-11） |
| テスト | ✅ 基本あり、区別テスト追加済み（RC-18 の sha256Hex 共通化は任意のまま） |
| コミット・PR | ✅ PR 差分は #2045 関連のみ（RC-19） |

---

## 評価セッション

- **評価日時**: 2026-05-30 17:10 JST
- **ブランチ名**: feat/2045
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2046
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 5

### RC 別対応チェックリスト

- [x] **RC-20**（GitHub id: 4576566826）— 👌 修正不要（Copilot 追記2点：/a-/ デッドコード指摘は誤り、sha256Hex は RC-18 通り任意）
- [ ] **RC-21**（GitHub id: 3325120055）— 🚨 修正推奨（isGoogleUnavailableAvatar の正規化漏れ）
- [ ] **RC-22**（GitHub id: 3325144348）— 🚨 修正推奨（RC-21 と同根・googleProfileImage.ts）
- [ ] **RC-23**（GitHub id: 3328435302）— 🚨 修正推奨（Google URL ホスト名の厳密検証・SSRF）

**レビュー非該当スキップ（RC なし）**

- GitHub id: 4575713810 — レビュー依頼定型文（2回目 4576519464 も同様）
- GitHub id: 4575805992 — Codex 接続案内のみ
- GitHub id: 4576568623 — Codex 接続案内のみ
- GitHub review id: 4390564140 — Copilot 新規コメントなしサマリのみ

---

**識別子**: RC-20（GitHub id: 4576566826）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

前回指摘した 🔴 2件・🟡 2件はいずれも対応済みを確認しました。追加で2点のみ記載します。

---

**`common/src/utils/googleProfileImage.ts` L35**

🟢 **任意**: `pathname.startsWith('/a/')` が true の場合、`/a-/` は `/a/` で始まらないため `!pathname.startsWith('/a-/')` は常に true になり、デッドコードです。

```typescript
// 現在
return pathname.startsWith('/a/') && !pathname.startsWith('/a-/')
// → 下記で十分
return pathname.startsWith('/a/')
```

---

**`functions/default/src/utils/namesPrintPdf.ts` L137**

🟢 **任意**: `createHash('sha256')` が残っています（前回コメントと同じ指摘）。`common` の `sha256Hex` を import すれば統一できます。ただし `sha256Hex` は async なので `await` が必要です。

```typescript
import {
  isGoogleProfileImageUrl,
  isGoogleUnavailableAvatarHash,
  normalizeGoogleProfileImageUrl,
  sha256Hex,
} from '@shokujii/common/utils/googleProfileImage.js'
// ...
const hashHex = await sha256Hex(photoBuffer)
```

```

**コメント要約**: 追記2点。（1）`/a-/` 除外がデッドコード、（2）sha256Hex 共通化。

**判断結果**: 👌 修正不要

**判断理由**: （1）Copilot の指摘は誤り。JavaScript では `'/a-/foo'.startsWith('/a/')` は **true**（`/a-` は `/a/` で始まる）。`!pathname.startsWith('/a-/')` は `/a/` かつ `/a-/` でないパスのみを通すために必要。テスト（googleProfileImage.test.ts L39-41）でも `/a-/` を false とする意図が明記されている。（2）sha256Hex 共通化は RC-18 と同根の任意改善。マージ必須ではない。

---

**識別子**: RC-21（GitHub id: 3325120055）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Google画像判定前にサイズを正規化してください**

`UserAvatar` は `props.user` が文字列 URL の場合は `buildThumbnailsLinks` を通らず、そのまま `isGoogleUnavailableAvatar` に渡します。Firebase Auth 由来の Google `photoURL` は `=s96-c` など既知ハッシュ外のサイズになり得るため、このまま fetch してハッシュ化すると停止・削除アカウントのプレースホルダーを検出できず、デフォルト画像に置き換わりません。`fetchGoogleProfileImage` と同様に、判定前に `normalizeGoogleProfileImageUrl(url, 500)` などへ正規化してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: UserAvatar に string の Google URL が渡された場合、サイズ未正規化のためプレースホルダーを検出できない。

**判断結果**: 🚨 修正推奨

**判断理由**: 現行の `isGoogleUnavailableAvatar`（L43）は渡された URL をそのまま fetch する。`=s96-c` 等では既知ハッシュ（s50/s100/s500）と不一致。RC-7/11 で PDF 側は修正済みだがクライアント判定は未対応。`isGoogleUnavailableAvatar` 内で `normalizeGoogleProfileImageUrl` してから fetch するのが一箇所修正で済む。

---

**識別子**: RC-22（GitHub id: 3325144348）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  判定前に Google 画像 URL を既知サイズへ正規化してください**

`UserAvatar` に `string` の Google 画像 URL（Firebase Auth の `photoURL` などでよくある `=s96-c`）が渡された場合、この関数はその URL をそのまま fetch しますが、既知ハッシュは s50/s100/s500 だけなのでプレースホルダーを検出できません。`fetchGoogleProfileImage` や PDF 側と同様に、ハッシュ判定前に `normalizeGoogleProfileImageUrl(url, 500)` などへ揃える必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: RC-21 と同根。`isGoogleUnavailableAvatar` が fetch 前に URL を正規化していない。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-21 と同一論点。`common/src/utils/googleProfileImage.ts` L43 で raw url を fetch している。`fetchGoogleProfileImage`（L57）は正規化済み。RC-21/22 は `isGoogleUnavailableAvatar` 内の1行修正で両方解消可能。

---

**識別子**: RC-23（GitHub id: 3328435302）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Google 画像 URL のホスト名を厳密に検証してください**

`firestore.rules` の `match /users/{user_id}` では本人が自分のドキュメントを直接更新できるため、認証済みユーザーは `user_image_url` に任意文字列を保存してからこの Callable Function を呼べます。この分岐はホスト名ではなくスラッシュなしの prefix だけを確認しているので、例えば `https://lh3.googleusercontent.com@internal-service/path` や `https://lh3.googleusercontent.com.attacker.example/path` も Google URL として扱われ、Functions サーバーから攻撃者指定の HTTPS URL を fetch します。今回追加された Google 分岐で新たに SSRF が可能になるため、既に用意されている `isGoogleProfileImageUrl` を使うか、`new URL(imageUrl).hostname === 'lh3.googleusercontent.com'` で厳密に検証してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: `user.ts` の `startsWith` による Google URL 判定が SSRF リスク。ホスト名の厳密検証が必要。

**判断結果**: 🚨 修正推奨

**判断理由**: `user.ts` L211 は `imageUrl.startsWith('https://lh3.googleusercontent.com')` のみ。`https://lh3.googleusercontent.com.attacker.example` 等がマッチしうる。`isGoogleProfileImageUrl` も同様に `startsWith` のみ（L13）のため、共通 util の hostname 検証強化と `user.ts` での利用が望ましい。Firestore rules で本人が `user_image_url` を任意設定できる点の指摘も妥当。P1 のためマージ前対応推奨。

---

## 評価セッション

- **評価日時**: 2026-05-30 19:35 JST
- **ブランチ名**: feat/2045
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2046
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 0

### RC 別対応チェックリスト

- [x] **RC-21**（GitHub id: 3325120055）— ✅ 対応済み（isGoogleUnavailableAvatar で fetch 前正規化）
- [x] **RC-22**（GitHub id: 3325144348）— ✅ 対応済み（RC-21 と同根）
- [x] **RC-23**（GitHub id: 3328435302）— ✅ 対応済み（isGoogleProfileImageUrl の hostname 厳密検証・user.ts で利用）
- [x] **RC-24**（GitHub id: 3328559431）— 👌 修正不要（空文字は NonEmptyStringSchema で FieldValue.delete に変換）

---

**識別子**: RC-21（GitHub id: 3325120055）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Google画像判定前にサイズを正規化してください**

`UserAvatar` は `props.user` が文字列 URL の場合は `buildThumbnailsLinks` を通らず、そのまま `isGoogleUnavailableAvatar` に渡します。Firebase Auth 由来の Google `photoURL` は `=s96-c` など既知ハッシュ外のサイズになり得るため、このまま fetch してハッシュ化すると停止・削除アカウントのプレースホルダーを検出できず、デフォルト画像に置き換わりません。`fetchGoogleProfileImage` と同様に、判定前に `normalizeGoogleProfileImageUrl(url, 500)` などへ正規化してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: `isGoogleUnavailableAvatar` が URL 正規化せず fetch する問題。

**判断結果**: ✅ 対応済み

**判断理由**: `common/src/utils/googleProfileImage.ts` L50-51 で `normalizeGoogleProfileImageUrl(url, 500)` してから fetch するよう修正済み。テスト（googleProfileImage.test.ts）でも s96→s500 正規化を検証。

---

**識別子**: RC-22（GitHub id: 3325144348）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  判定前に Google 画像 URL を既知サイズへ正規化してください**

`UserAvatar` に `string` の Google 画像 URL（Firebase Auth の `photoURL` などでよくある `=s96-c`）が渡された場合、この関数はその URL をそのまま fetch しますが、既知ハッシュは s50/s100/s500 だけなのでプレースホルダーを検出できません。`fetchGoogleProfileImage` や PDF 側と同様に、ハッシュ判定前に `normalizeGoogleProfileImageUrl(url, 500)` などへ揃える必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: RC-21 と同根。fetch 前の正規化漏れ。

**判断結果**: ✅ 対応済み

**判断理由**: RC-21 と同一修正で解消済み。

---

**識別子**: RC-23（GitHub id: 3328435302）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Google 画像 URL のホスト名を厳密に検証してください**

`firestore.rules` の `match /users/{user_id}` では本人が自分のドキュメントを直接更新できるため、認証済みユーザーは `user_image_url` に任意文字列を保存してからこの Callable Function を呼べます。この分岐はホスト名ではなくスラッシュなしの prefix だけを確認しているので、例えば `https://lh3.googleusercontent.com@internal-service/path` や `https://lh3.googleusercontent.com.attacker.example/path` も Google URL として扱われ、Functions サーバーから攻撃者指定の HTTPS URL を fetch します。今回追加された Google 分岐で新たに SSRF が可能になるため、既に用意されている `isGoogleProfileImageUrl` を使うか、`new URL(imageUrl).hostname === 'lh3.googleusercontent.com'` で厳密に検証してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: Google URL 判定の SSRF リスク。hostname 厳密検証が必要。

**判断結果**: ✅ 対応済み

**判断理由**: `isGoogleProfileImageUrl` を `https:` + `hostname === 'lh3.googleusercontent.com'` に変更済み。`user.ts` L211/L226 でも同関数を使用。SSRF 想定 URL のテストを common に追加済み。

---

**識別子**: RC-24（GitHub id: 3328559431）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  プレースホルダー画像を消去できる値で保存してください**

Google の停止・削除アカウント用プレースホルダーを検出した場合、この代入で `user_image_url` が空文字列になり、直後の `saveUser(user)` が実行されます。しかし `common/src/schemas/User.ts` の `UserDbSchema.user_image_url` は `NonEmptyStringSchema.optional()` であり、`User.toFirestore()` は空文字列を除去せず `parse` に渡すため、保存時に Zod エラーになります。その結果、このケースではログイン用 Callable Function 自体が失敗し、プロフィール画像も消去されません。フィールドを省略するだけでは `{ merge: true }` により既存 URL が残るため、削除を表現できる store 側の更新方法にしてください。

Useful? React with 👍 / 👎.
```

**コメント要約**: `user_image_url = ''` が Zod で失敗し save できない、merge ではフィールド削除できないとの指摘。

**判断結果**: 👌 修正不要

**判断理由**: `UserDbSchema.user_image_url` は `NonEmptyStringSchema.optional()` で、空文字は `toFirestore()` の `parse` 内で `FieldValue.delete()` に変換される（common/src/schemas/firebase/index.ts L96-100）。`saveUser` は `user.toFirestore()` 経由の converter で `set(..., { merge: true })` しており、delete 値は merge でもフィールド削除として扱われる。同じ `user_image_url: ''` パターンは `anonymizeUser`（functions/default/src/stores/user.ts L185-196）でも採用済み。Zod エラーで Callable が落ちる指摘は schema 設計と不一致。

---
