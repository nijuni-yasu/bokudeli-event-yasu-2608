# PR #2084 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-16 23:45 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: ai/2083
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2084
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 3

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 3421626424 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | RUN_ID 取得が空のまま watch 実行し得る<br>リトライ＋空時の run list 提示フォールバックを追記済み |
| [x] | RC-2 | なし | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | --limit 1 が古い成功 run を掴み誤って成功報告し得る<br>基準時刻＋workflow_dispatch 絞り込みで run 特定を堅牢化済み |
| [x] | RC-3 | 3422553553 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | declare -A は bash 4+ 必須<br>WF:RUN_ID ペア列挙に差し替え済み |

---

**識別子**: RC-1（GitHub id: 3421626424）

**レビュワー**: Copilot

**指摘箇所**: `.agents/skills/github-actions-deploy/SKILL.md:160`

**該当コード（レビュー時点の diff）**:

```diff
@@ -146,18 +146,60 @@ gh workflow run deploy_user.yml --repo OWNER/REPO --ref BRANCH -f environment=de
 gh run list --repo OWNER/REPO --limit 10
 ```
 
-### 6. 結果をユーザーに伝える
+### 6. デプロイ結果の検知（成功/失敗の確認）
+
+`gh workflow run` は発火するだけで結果を返さない。発火した各ワークフローについて run を特定し、完了まで監視する。
+
+```bash
+# 発火直後は run がまだ作られていないことがあるため少し待つ
+sleep 5
+RUN_ID=$(gh run list --repo OWNER/REPO --workflow deploy_firestore.yml \
+  --branch BRANCH --limit 1 --json databaseId --jq '.[0].databaseId')
+
+# 完了まで監視。失敗時は非ゼロ終了する
+gh run watch "$RUN_ID" --repo OWNER/REPO --exit-status
```

**レビュワーのコメント（原文）**:

[must] RUN_ID の取得が空文字のままになり得ます（発火直後・キュー待ち・同一ブランチの過去 run が優先される等）。このままだと `gh run watch \"$RUN_ID\"` が空 ID で失敗し、原因が分かりにくいので、RUN_ID が取れるまでリトライするか、取れない場合は `gh run list` の結果を提示して手動で run を特定する手順を追記してください。

**コメント要約**: 手順 6 の RUN_ID 取得は sleep 5 のみで、発火直後・キュー待ち・過去 run 優先により空 ID や誤った run になり得る。
gh run watch が空 ID で失敗し、本 PR の目的である失敗検知・原因解析が機能しない。リトライまたは run list 提示による手動特定手順の追記が望ましい。

**対応内容**: 手順 6 を最大 10 回（約 30 秒）のリトライで RUN_ID を取得する形に変更し、空のまま `gh run watch` せず `gh run list` を提示して手動特定にフォールバックする分岐を追記した（`.agents/skills/github-actions-deploy/SKILL.md`）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 指摘は妥当。sleep 5 はタイミング問題を部分的にしか緩和せず、`limit 1` は直近 run を返すだけで今回発火分の保証がない。ただし対象はスキル文書の運用手順であり、アプリ本体のセキュリティ・データ不整合ではないため 🚨 ではなく 🟡 とする。Copilot の [must] は手順の信頼性に関する強調として解釈し、本 PR のスコープ内で RUN_ID 取得のリトライ例（例: 最大 N 回・間隔 sleep）と、取得失敗時に `gh run list` をユーザーへ提示して run URL を手動特定するフォールバックを追記すれば足りる。

---

## 評価セッション（2026-06-16 23:46 JST・shokujii-code-review）

- **評価日時**: 2026-06-16 23:46 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: ai/2083
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2084
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [ ] | RC-2 | なし | 🟡 修正提案 | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | --limit 1 が古い成功 run を掴み誤って成功報告し得る<br>workflow_dispatch・発火時刻での run 特定の堅牢化を推奨 |

---

**識別子**: RC-2（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/skills/github-actions-deploy/SKILL.md:160`

**該当コード（レビュー時点の diff）**:

```diff
+```bash
+# 発火直後は run がまだ作られていないことがあるため少し待つ
+sleep 5
+RUN_ID=$(gh run list --repo OWNER/REPO --workflow deploy_firestore.yml \
+  --branch BRANCH --limit 1 --json databaseId --jq '.[0].databaseId')
+
+# 完了まで監視。失敗時は非ゼロ終了する
+gh run watch "$RUN_ID" --repo OWNER/REPO --exit-status
+```
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📄ドキュメントのみ/S]: 手順 6 の RUN_ID 取得は `--limit 1`（最新 run）で選ぶため、発火直後でまだ今回の run が作成されていない場合、同一ブランチの過去に成功した run を掴むおそれがある。この場合 RUN_ID は空ではないため Copilot RC-1 の空文字列ガードをすり抜け、`gh run watch --exit-status` が古い成功 run を即座に成功として返し、実際の失敗を見逃して誤った成功報告をしてしまう。→ RC-1 のリトライ／手動特定対応に加え、`--event workflow_dispatch` での絞り込みや、発火時刻より後の run か（createdAt／run URL）を確認してから watch する注意を添えると堅牢。

**コメント要約**: `--limit 1` は今回発火した run を選ぶ保証がなく、古い成功 run を非空の RUN_ID として掴み得る。
その結果 watch が誤って成功と判定し本 PR の失敗検知が機能しない。RC-1 と同じく run 特定の堅牢化で対応するが、失敗モード（誤った成功報告）が異なる。

**対応内容**: 手順 6 で発火直前に基準時刻 `SINCE` を控え、`--event workflow_dispatch --created ">$SINCE"` で今回発火分のみを特定する形に変更した。`--created` 非対応環境向けに `startedAt`／`createdAt` での目視照合の注記も追加した（`.agents/skills/github-actions-deploy/SKILL.md`）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 本 PR の目的（失敗検知・原因解析）の信頼性に直結する。RC-1（Copilot）が「空 ID → watch 失敗（可視）」を扱うのに対し、本指摘は「非空の古い run → 誤った成功報告（不可視）」という別の失敗モードのため RC-2 として記録する。修正方針は run 特定の堅牢化に収束するため RC-1 とまとめて対応してよい。スキル文書のみの変更で工数 S。

---

## 評価セッション

- **評価日時**: 2026-06-17 15:17 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: ai/2083
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2084
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 9（Codex 利用上限通知 5 件、レビュー依頼定型文 4 件）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-3 | 3422553553 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | declare -A は bash 4+ 必須<br>WF:RUN_ID ペア列挙に差し替え済み |

---

**識別子**: RC-3（GitHub id: 3422553553）

**レビュワー**: Copilot

**指摘箇所**: `.agents/skills/github-actions-deploy/SKILL.md:193`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略）
+**フェーズ B: RUN_ID 特定（WF ごと）**
+
+```bash
+WORKFLOWS=(deploy_user.yml deploy_partner.yml deploy_functions.yml \
+           deploy_firestore.yml deploy_storage.yml deploy_manager.yml)
+declare -A RUN_IDS
+
+for WF in "${WORKFLOWS[@]}"; do
+  RUN_ID=""
+  for i in $(seq 1 10); do
+    RUN_ID=$(gh run list --repo OWNER/REPO --workflow "$WF" --branch BRANCH \
+      --event workflow_dispatch --created ">$SINCE" \
+      --limit 1 --json databaseId --jq '.[0].databaseId // empty')
+    [ -n "$RUN_ID" ] && RUN_IDS[$WF]=$RUN_ID && break
+    sleep 3
+  done
```

**レビュワーのコメント（原文）**:

`declare -A`（連想配列）は bash 4+ が必要で、macOS 標準の bash 3.2 だとこの手順をそのままコピペすると失敗します。運用手順として使うなら、bash の前提（bash 4+）を明記するか、bash 3.2 でも動く書き方にしてください。

**コメント要約**: フェーズ B/C の `declare -A RUN_IDS` と `${!RUN_IDS[@]}` は bash 4+ 専用。
macOS 標準 bash 3.2 や zsh 上でコピペ実行すると連想配列構文で失敗する。前提の明記か、bash 3.2 互換の書き方への変更が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 指摘は妥当。フェーズ B/C を `declare -A` から `WF:RUN_ID` ペア列挙（`RUN_ID_ENTRIES`）に差し替え、macOS 標準 bash 3.2 / zsh でもコピペ実行可能にした。

---
