# PR #2100 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-23 15:30 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: doc/2097
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2100
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（GitHub id: 4776131970 レビュー依頼定型文、4776155054 Codex 接続案内のみ）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 4776154363 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | 手順概要テーブルと本文で B 削除タイミングが逆<br>段 1 と §4 のどちらかに統一が必要 |
| [x] | RC-2 | 4776154363 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | 手順概要の段番号と本文見出し §0〜§10 がずれている<br>テーブルまたは見出し番号の再整理が必要 |
| [x] | RC-3 | 4776154363 | ✅ 対応済み | 📌 スコープ内 | — | 📄 ドキュメントのみ | S | 0004 util 自動選択の説明が実装依存<br>batch リポジトリ参照を足すと保守しやすい |
| [x] | RC-4 | 4776154363 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | Storage 全体フロー図と §5 の 0042 位置が不一致<br>フロー図を Storage 手順内の順序に合わせる |
| [x] | RC-5 | 3457403913 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📄 ドキュメントのみ | S | メール削除を Storage より前に実行すべき<br>import 後の誤送信リスク。順序見直しまたは Functions 停止を明記 |
| [x] | RC-6 | 3457403919 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | Console import では metadata ファイル指定が必要<br>CLI と Console の書き分けを追記 |
| [x] | RC-7 | 3457403917 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ, 💾 データ | 📄 ドキュメントのみ | S | pass_code.user_email が 0004 対象外のまま残る<br>削除必須手順または 0004 拡張を明記 |
| [x] | RC-8 | 3457403921 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | Storage バケット名を appspot.com で固定している<br>Console 確認後の変数利用と firebasestorage.app 注記 |
| [x] | RC-9 | 4550451275 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📄 ドキュメントのみ | S | application_default_credentials.json を cat 表示<br>トークン漏えいリスク。中身非表示の確認コマンドへ |
| [x] | RC-10 | 4550451275 | ✅ 対応済み | 📌 スコープ内 | — | 📄 ドキュメントのみ | S | 片付け rm -rf が日付フォルダをハードコード<br>EXPORT_FOLDER 変数に揃えると誤削除を防げる |

---

**識別子**: RC-1（GitHub id: 4776154363）

**レビュワー**: Copilot

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:23-34`（手順概要テーブル）および `§2`〜`§4`

**該当コード（レビュー時点の diff）**:

```diff
+| 段 | 内容 |
+|----|------|
+| 0 | 事前確認（IAM・バックアップフォルダの特定） |
+| 1 | B の Firestore 既存データ削除 |
+| 2 | A の export をローカルへダウンロード |
+| 3 | B の import 用バケットへアップロード |
…
+## 2. 「プロジェクト A」の export をローカルへダウンロードする
…
+## 3. ターミナルで移行先の「プロジェクト B」に設定し、アップロードする
…
+## 4. 「プロジェクト B」の Firestore 既存データを削除する
```

**レビュワーのコメント（原文）**:

**[must] 手順概要テーブルの「段 1」と本文「§4」で B 削除のタイミングが逆**

> 該当箇所: 手順概要テーブル（段 1〜段 3 の行）

テーブルでは「段 1: B の Firestore 既存データ削除 → 段 2: A からダウンロード → 段 3: B へアップロード」と B を先に削除する順序になっているが、本文では「§2: A からダウンロード → §3: B へアップロード → §4: B 削除」とダウンロード・アップロード後に B を削除する順序になっており、**テーブルと本文で手順順序が逆**になっている。

どちらの順序を正とするかを決め、テーブルと本文を一致させる修正が必要。

**コメント要約**: 手順概要テーブルは B の Firestore 削除を段 1 に置いているが、本文は §2 ダウンロード → §3 アップロード → §4 B 削除の順になっており、読者がどちらを実行すべきか判断できない。
テーブルと本文のどちらかに統一する修正が必要。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 現行ファイルを確認するとテーブル段 1 と §4 の順序が実際に矛盾している。import 前に B を空にする必要があるため、正しい順序を決めてテーブル・本文・§ 参照を揃えないと手順どおり実行できない。

---

**識別子**: RC-2（GitHub id: 4776154363）

**レビュワー**: Copilot

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:21-36`

**該当コード（レビュー時点の diff）**:

```diff
+Storage の移行は §6、[storageデータ移行手順_手動.md](./storageデータ移行手順_手動.md) を参照。**Storage コピー後**に §7 のメール削除を行う。
```

**レビュワーのコメント（原文）**:

**[must] 手順概要テーブルの段番号（0〜9）と本文見出しの番号（§0, §1〜§10）が体系的にずれている**

> 該当箇所: 手順概要テーブル全体

テーブルは 段 0〜9 の 10 段だが、本文の見出しは `## 0. 事前確認` の後に `## 1.`〜`## 10.` の 10 見出し（合計 11）がある。そのため対応関係がずれている（例：テーブル「段 4: import」→ 本文「§5」、テーブル「段 5: rules/indexes」→ 本文「§6」等）。

テーブルの段番号を本文の見出し番号（1〜10）に合わせるか、本文の `## 0. 事前確認` を `段 0` 扱いに統一して番号を整理するか、いずれかで揃える修正を推奨。

**コメント要約**: 手順概要の段 0〜9 と本文 §0〜§10 の対応が 1 段ずつずれており、§6 §7 などの相互参照も誤解を招く。
段番号と見出し番号を同一体系に再整理する必要がある。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: RC-1 と独立だが同ファイル内の番号体系問題。テーブル段 4 が import だが本文 import は §5 など、読者が段番号と § 番号を同一視すると誤実行する。PR 目的である手順整理の完成度に直結する。

---

**識別子**: RC-3（GitHub id: 4776154363）

**レビュワー**: Copilot

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:232`

**該当コード（レビュー時点の diff）**:

```diff
+> **注意（util の自動選択）**: `index.js` は `utils/` 内で **番号が最大のファイル**を実行する。`0031_delete_memberOrders_stripes.js` 等が存在する場合、0004 以外が選ばれる。**0004 を実行するときは、それより番号の大きい util を一時的に `utils/` 外へ退避**してから実行する。
```

**レビュワーのコメント（原文）**:

**[imo] `0004` の util 自動選択の注意書きは実装依存の情報**

> 該当箇所: §8「メールアドレス削除」の注意ブロック

「`index.js` は `utils/` 内で番号が最大のファイルを実行する」という記述は `bokudeli-event-batch` の実装に依存しており、将来変わりうる。注意書きとして残すなら「実行前に `index.js` の挙動を確認する」という形にし、batch リポジトリへの参照リンクを追加すると保守性が上がる（ドキュメント外変更になるため必須ではないが）。

**コメント要約**: 0004 実行方法の説明が bokudeli-event-batch の index.js 実装に依存しており、将来変更で手順が陳腐化しうる。
batch リポジトリへの参照や確認手順を足す改善提案。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 指摘は妥当だが、現状の退避手順は実運用で有効。マージ blocker ではないが、リンク追加で再発防止に有効。

---

**識別子**: RC-4（GitHub id: 4776154363）

**レビュワー**: Copilot

**指摘箇所**: `documents/firebaseプロジェクト/storageデータ移行手順_手動.md:29` および `206-217`

**該当コード（レビュー時点の diff）**:

```diff
+| 5 | （任意）Facebook 画像移行 `0042` |
…
+3. メール削除（firestoreデータ移行手順_手動.md §8）
+   → 0004_delete_emails_for_drytest.js
+4. （任意）0042 Facebook 画像移行
+5. 検証
```

**レビュワーのコメント（原文）**:

**[nits] 手順概要テーブルの段番号と本文見出しは一致しているが、§5（0042）の位置が全体フロー図と微妙にずれている**

> 該当箇所: 手順概要テーブル「段 5: （任意）Facebook 画像移行 `0042`」および本文最下部「全体フロー」

本文の手順は「§5: 0042（Storage 移行内）→ §6: 検証」の順だが、全体フロー図では「3. メール削除 → 4. （任意）0042 Facebook 画像移行」と **0042 がメール削除より後**に記載されている。Storage 手順内の §5 として 0042 を実施するなら、フロー図の順序も Firestore 手順の §8（メール削除）の前に持ってくる修正が分かりやすい。

**コメント要約**: Storage 手順内では 0042 が §5 で Storage コピー直後だが、全体フロー図ではメール削除の後に 0042 が来ており順序が食い違う。
Firestore 手順との連携を踏まえフロー図を修正すると読みやすい。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 0042 は Storage コピー後・Firestore 側メール削除前後どちらでも議論余地があるが、現行 Storage 手順 §5 とフロー図の矛盾は修正した方がよい。RC-5 のメール削除順序見直しと合わせて整理するのが望ましい。

---

**識別子**: RC-5（GitHub id: 3457403913）

**レビュワー**: Codex

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:36`

**該当コード（レビュー時点の diff）**:

```diff
+| 6 | Storage 移行（[storageデータ移行手順_手動.md](./storageデータ移行手順_手動.md)） |
+| 7 | メールアドレス削除（`0004_delete_emails_for_drytest.js`） |
…
+Storage の移行は §6、[storageデータ移行手順_手動.md](./storageデータ移行手順_手動.md) を参照。**Storage コピー後**に §7 のメール削除を行う。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  メール削除をStorage移行より前に実行してください**

この順序だと、Firestore import 後から Storage コピー完了まで本番メールアドレスが sandbox2604 に残ります。sandbox 側で Functions が有効な場合、`functions/default/src/pollingTask.ts:20-54` の毎分スケジュールや `eventInformationMail.ts:42-52` のスケジュール送信が実データのメールアドレスを参照し得るため、Storage 移行に時間がかかったタイミングで実ユーザーへ誤送信するリスクがあります。Firestore import 直後、少なくとも Storage 移行やスケジュール Functions 再開前に `0004_delete_emails_for_drytest.js` を実行する順序にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: 現行手順は Storage コピー後に 0004 を実行するが、import から Storage 完了まで本番メールが sandbox に残り、スケジュール Functions による誤送信リスクがある。
rules/indexes デプロイ直後に 0004 を実行するか、移行中 Functions 停止を明記すべき。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 個人 sandbox への本番相当データ載せ替えでは誤送信防止が最優先。Storage コピーは Firestore のメールフィールドを直接使わないが、Functions は import 直後から動きうる。0004 を import + rules デプロイ直後に移すか、移行ウィンドウ中の Functions 無効化を手順に含める必要がある。

---

**識別子**: RC-6（GitHub id: 3457403919）

**レビュワー**: Codex

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:185`

**該当コード（レビュー時点の diff）**:

```diff
+- Import/Export 画面: https://console.cloud.google.com/firestore/databases/-default-/import-export?hl=ja&project=bokudeli-event-yasu-2604
+- バケット `bokudeli-event-yasu-2604-firestore-imports` を選択し、上記 `GS_IMPORT_ROOT` に相当するフォルダを指定する
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Console import では metadata ファイルを指定してください**

GCP Console で import する場合、Firebase 公式手順は Filename に完了済み export の `.overall_export_metadata` ファイル名を指定すると説明しています（https://firebase.google.com/docs/firestore/manage-data/export-import#import_all_documents_from_an_export）。ここでフォルダを指定すると読者が Console で親ディレクトリを選んで import に失敗するため、CLI は親ルート、Console は `...overall_export_metadata` ファイルを選ぶ、という書き分けにしてください。

Useful? React with 👍 / 👎.

**コメント要約**: Console import では Firebase 公式どおり overall_export_metadata ファイルを選ぶ必要があるが、現行手順はフォルダ指定に読める。
CLI と Console の指定方法を書き分けると import 失敗を防げる。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: Firebase 公式手順との整合性があり、Console 利用者向けの実用的改善。CLI 手順は GS_IMPORT_ROOT のままでよい。

---

**識別子**: RC-7（GitHub id: 3457403917）

**レビュワー**: Codex

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:249`

**該当コード（レビュー時点の diff）**:

```diff
+`pass_code` コレクションの `user_email` は 0004 の対象外。残存していれば別途確認・削除を検討する。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  pass_code のメールも削除対象にしてください**

ここで対象外として確認・検討に留めると、`0004` 完了後も `pass_code.user_email` が本番メールアドレスのまま sandbox に残ります。`common/src/schemas/PassCode.ts:13` で必須のメールフィールドとして保存され、`functions/default/src/stores/passCode.ts:43-52` からメールで検索される PII なので、個人 sandbox へ本番データを載せる手順では任意ではなく削除必須の手順または `0004` の対象として扱ってください。

Useful? React with 👍 / 👎.

**コメント要約**: 0004 は pass_code.user_email を削除しない。ドキュメントが任意扱いだが本番 PII が sandbox に残る。
0004 拡張または別削除手順を必須化すべき。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ, 💾 データ

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: `0004_delete_emails_for_drytest.js` を確認し pass_code は EMAIL_TARGETS に含まれない。本 PR は doc のみだが、手順書上は削除必須と明記し 0004 拡張は別 PR または Issue 化を判断理由に書く必要がある。現状の任意表現は不適切。

---

**識別子**: RC-8（GitHub id: 3457403921）

**レビュワー**: Codex

**指摘箇所**: `documents/firebaseプロジェクト/storageデータ移行手順_手動.md:13`

**該当コード（レビュー時点の diff）**:

```diff
+- A のバケット名：`bokudeli-event-dev.appspot.com`
+- B のバケット名：`bokudeli-event-yasu-2604.appspot.com`
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Storage の実バケット名を固定しないでください**

sandbox2604 の Storage が 2024-10-30 以降に新規作成された default bucket の場合、Firebase 公式 FAQ どおり bucket 名は `PROJECT_ID.firebasestorage.app` 形式（https://firebase.google.com/docs/storage/faqs-storage-changes-announced-sept-2024）になり、ここ以降の削除・コピーコマンドが固定している `bokudeli-event-yasu-2604.appspot.com` では存在しない／別の legacy bucket を操作してしまいます。直前で Console 確認を促しているので、その実値を `BUCKET_B` に入れて全コマンドで使う形にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: appspot.com を固定すると 2024-10 以降の firebasestorage.app 形式バケットでコマンドが空振りする可能性がある。
Console で確認した実バケット名を変数化して全コマンドで使うべき。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 事前確認セクションで Console 確認を促しているが、例示が appspot.com 固定のため読者がそのまま実行しうる。変数例と FAQ リンクで firebasestorage.app も注記すべき。

---

**識別子**: RC-9（GitHub id: 4550451275）

**レビュワー**: Copilot（Pull request review・low confidence suppressed comment）

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:103`

**該当コード（レビュー時点の diff）**:

```diff
+```bash
+cat ~/.config/gcloud/application_default_credentials.json
+gcloud config list
+```
```

**レビュワーのコメント（原文）**:

* [must] `application_default_credentials.json` は機密情報（トークン等）を含みうるため、手順として `cat` で表示すると漏えいリスクがあります。存在確認は中身を表示しないコマンドに置き換えてください。

**コメント要約**: 設定確認手順で ADC JSON を cat 表示しており、トークン等が端末ログや画面共有で漏れるリスクがある。
gcloud auth list 等、中身を出さない確認方法へ置き換えるべき。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 移行手順ドキュメントに機密ファイル内容表示を載せるのは不適切。gcloud config list と gcloud auth list で十分。

---

**識別子**: RC-10（GitHub id: 4550451275）

**レビュワー**: Copilot（Pull request review・low confidence suppressed comment）

**指摘箇所**: `documents/firebaseプロジェクト/firestoreデータ移行手順_手動.md:266`

**該当コード（レビュー時点の diff）**:

```diff
+```bash
+rm -rf ~/Github/firestore-backups/2026-06-21T17:00:09_95526
+# または作業で使った EXPORT_FOLDER 全体
+```
```

**レビュワーのコメント（原文）**:

* [imo] 片付けの `rm -rf` が日付フォルダ名をハードコードしているため、手順中で `EXPORT_FOLDER` を差し替えた場合に削除対象がズレやすいです。`EXPORT_FOLDER` 変数を使う形に揃えると誤削除を減らせます。

**コメント要約**: 片付けコマンドが例の日付フォルダをハードコードしており、手順中に EXPORT_FOLDER を変更した場合に削除漏れや誤削除の原因になる。
変数を使った rm 例に統一すべき。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 手順全体で EXPORT_FOLDER 変数を使っているため、片付けも `${EXPORT_FOLDER}` に揃えるのが一貫している。コメント行だけでは不十分。

---
