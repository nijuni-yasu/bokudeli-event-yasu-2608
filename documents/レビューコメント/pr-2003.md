# PR #2003 レビューコメント評価

## RC 別対応チェックリスト（通し・全セッション）

- [x] **RC-1**（GitHub id: 4412465881）— 👌 修正不要
- [x] **RC-2**（GitHub id: 4412479589）— 👌 修正不要
- [x] **RC-3**（GitHub id: 4412479779）— 👌 修正不要
- [x] **RC-4**（GitHub id: 3213114793）— ✅ 対応済み
- [x] **RC-5**（GitHub id: 3213116900）— ✅ 対応済み
- [x] **RC-6**（GitHub id: 4414994590）— 👌 修正不要
- [x] **RC-7**（GitHub id: 4415001598）— ✅ 対応済み
- [x] **RC-8**（GitHub id: 4415001765）— 👌 修正不要
- [x] **RC-9**（GitHub id: 3214376411）— ✅ 対応済み
- [x] **RC-10**（GitHub id: 3214492217）— 👌 修正不要
- [x] **RC-11**（GitHub id: 3214670910）— ✅ 対応済み
- [x] **RC-12**（GitHub id: 3214765665）— 👌 修正不要
- [x] **RC-13**（GitHub id: 3214496114）— ✅ 対応済み
- [x] **RC-14**（GitHub id: 3214765666）— 👌 修正不要
- [x] **RC-15**（GitHub id: 3214667554）— ✅ 対応済み
- [x] **RC-16**（GitHub id: 3214667563）— 👌 修正不要
- [x] **RC-17**（GitHub id: 3214492203）— 👌 修正不要
- [x] **RC-18**（GitHub id: 3214492205）— 👌 修正不要
- [x] **RC-19**（GitHub id: 3214819653）— ✅ 対応済み
- [x] **RC-20**（GitHub id: 3214667574）— 👌 修正不要
- [x] **RC-21**（GitHub id: 3214667569）— ✅ 対応済み

## 評価セッション

- **評価日時**: 2026-05-09 23:20 JST
- **ブランチ名**: feat/1692
- **PR**: [#2003](https://github.com/nijuniinc/bokudeli-event-new/pull/2003)
- **Outdated 除外件数**: 0 件（pulls/2003/comments で position が null のコメントは 0 件）

### RC 別対応チェックリスト

このセッションで評価した **全 RC** を漏れなく並べる。判断は下部の 6 点セットと一致させる。

- [x] **RC-1**（GitHub id: 4412465881）— 👌 修正不要（レビュー依頼文）
- [x] **RC-2**（GitHub id: 4412479589）— 👌 修正不要（トップレベルに列挙された 7 点はいずれも imo または nits。必須ではない）
- [x] **RC-3**（GitHub id: 4412479779）— 👌 修正不要（Codex 接続案内のみ）
- [x] **RC-4**（GitHub id: 3213114793）— ✅ 実装完了（月次同じ日付の基準日）
- [x] **RC-5**（GitHub id: 3213116900）— ✅ 実装完了（weekNumber 1〜5 外れ）

---

**識別子**: RC-1（GitHub id: 4412465881）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。
```

**コメント要約**: Copilot と Codex に日本語インラインレビューを依頼する定型文。

**判断結果**: 👌 修正不要

**判断理由**: 運用上の依頼でありコード変更の指摘ではない。

---

**識別子**: RC-2（GitHub id: 4412479589）

**レビュワー**: Copilot (AI)（PR トップレベル。インライン投稿失敗のため列挙）

**レビューのコメント（原文）**:

````

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

インラインコメントでの投稿を試みましたが、本環境では GitHub API へのアクセスが制限されているため、以下に指摘を列挙します。1指摘1ブロックの形式で記載しています。

---

### 1. `functions/default/src/eventCopy.ts` L37

[imo] `Date.now()` を使って `created_at` / `updated_at` を設定しています。プロジェクトルール「`Date` オブジェクトを直接使わず `luxon` を使う」に従い `DateTime.now().toMillis()` に変更してください。

```ts
// NG
const now = Date.now()

// OK
import { DateTime } from 'luxon'
const now = DateTime.now().toMillis()
```

---

### 2. `functions/default/src/eventCopyRepeat.ts` L30・L105

[nits] 計測用途とはいえ、同ファイル内で `Date.now()` を 2 箇所使用しています。上記 1 と同様に luxon に統一してください。

```ts
// NG
const startedAt = Date.now()
elapsedMs: Date.now() - startedAt

// OK
const startedAt = DateTime.now().toMillis()
elapsedMs: DateTime.now().toMillis() - startedAt
```

---

### 3. `functions/default/src/eventCopyRepeat.ts` L79–81

[nits] `copyEventCore`（`eventCopy.ts:34`）内で既に `isInShopTime` チェックと同一の `HttpsError` が throw されています。この前置チェックは冗長です。

```ts
// 冗長: copyEventCore 内で同じチェックが走る
if (!isInShopTime(startTime, shop)) {
  throw new HttpsError('invalid-argument', 'Event time is not in shop time')
}
return copyEventCore(uid, srcEvent, shop, startTime)
```

---

### 4. `user/src/components/manage/community/CopyEventDialog.vue` L109

[imo] `Date.now()` を使って `searchStart` を計算しています。luxon に統一してください。

```ts
// NG
const searchStart = getStartOfDay(Date.now() + MIN_DAYS_AHEAD * 24 * 60 * 60 * 1000)

// OK
const searchStart = getStartOfDay(DateTime.now().toMillis() + MIN_DAYS_AHEAD * 24 * 60 * 60 * 1000)
```

---

### 5. `user/src/components/manage/community/CopyEventDialog.vue` L129

[imo] `allowedDates` 内で `Date.now()` を使って過去日チェックをしています。luxon に統一してください。

```ts
// NG
if (date.getTime() < Date.now()) {

// OK
if (date.getTime() < DateTime.now().toMillis()) {
```

---

### 6. `user/src/components/manage/community/CopyEventDialog.vue` L355

[imo] `console.error` が残っています。本番ログに出力されます。プロジェクトの `createModuleLogger` は Functions 専用ですが、Vue コンポーネントでも `console.error` を本番コードに残すのは避け、`emit('error')` だけで十分です（直前の行で既に emit しているため）。

```ts
// NG
console.error('Error creating events:', error)
emit('error')

// OK
emit('error')
```

---

### 7. `eventCopyRepeat.ts` L13 と `CopyEventDialog.vue` L48

[imo] サーバー側の `MAX_REPEAT = 12` とクライアント側の `MAX_REPEAT_COUNT = 12` が同じ値なのに別々に定義されています。`common` に定数として切り出して共有することで、将来の値変更時の整合ミスを防げます。

```ts
// common/src/constants/eventCopyRepeat.ts
export const MAX_EVENT_COPY_REPEAT = 12

// functions/default/src/eventCopyRepeat.ts
import { MAX_EVENT_COPY_REPEAT } from '@shokujii/common/constants/eventCopyRepeat.js'

// user/src/components/manage/community/CopyEventDialog.vue
import { MAX_EVENT_COPY_REPEAT } from '@shokujii/common/constants/eventCopyRepeat.js'
```


> [!WARNING]
>
> <details>
> <summary>Firewall rules blocked me from connecting to one or more addresses (expand for details)</summary>
>
> #### I tried to connect to the following addresses, but was blocked by firewall rules:
>
> - `https://api.github.com/graphql`
>   - Triggering command: `/usr/bin/gh gh pr list --head feat/1692` (http block)
>   - Triggering command: `/usr/bin/gh gh auth status` (http block)
> - `https://api.github.com/repos/nijuniinc/bokudeli-event-new/pulls/2003/reviews`
>   - Triggering command: `/usr/bin/curl curl -s -X POST -H Authorization: token ****** -H Accept: application/vnd.github.v3&#43;json REDACTED -d @-` (http block)
>
> If you need me to access, download, or install something from one of these locations, you can either:
>
> - Configure [Actions setup steps](https://gh.io/copilot/actions-setup-steps) to set up my environment, which run before the firewall is enabled
> - Add the appropriate URLs or hosts to the custom allowlist in this repository's [Copilot coding agent settings](https://github.com/nijuniinc/bokudeli-event-new/settings/copilot/coding_agent) (admins only)
>
> </details>
````

**コメント要約**: Date.now と luxon の統一、isInShopTime 前置チェックの冗長性、console.error、MAX 件数の common 共有など 7 点。

**判断結果**: 👌 修正不要

**判断理由**: 1 と 2 はエポック ms 取得であり Date オブジェクト直接操作ではない。AGENTS の luxon 方針は主にフロントの日付表現に寄る。3 は早期失敗として許容できる。4 と 5 は表記統一の提案レベル。6 は emit 済みで重複ログ抑制の改善余地はあるが必須ではない。7 は定数共有の改善提案で Phase 後追い可。

---

**識別子**: RC-3（GitHub id: 4412479779）

**レビュワー**: chatgpt-codex-connector[bot]

**レビューのコメント（原文）**:

```
To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).
```

**コメント要約**: Codex 利用のためのアカウント接続案内。

**判断結果**: 👌 修正不要

**判断理由**: コードレビューではない。

---

**識別子**: RC-4（GitHub id: 3213114793）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  月次「同じ日付」の基準日を固定してください**

月次 `date` パターンの次回日付計算で `dom` に毎回 `base.day` を使っているため、`1/31 -> 2/28` のように短い月を挟むと、その後も `3/28, 4/28...` と日付がずれ続けます。本来の「毎月同じ日付」（仕様書でも開始日の「日」を保持）と異なり、月末付近のイベントで継続的に誤ったコピー日時が生成されるので、開始日の day を固定値として保持して計算する必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: 月次 date で毎ステップ base.day を使うため 1/31 経由後に 3/28 のように本来の日付からずれ続けるという指摘。

**判断結果**: ✅ 対応済み

**判断理由**: 当初は `advanceRepeatCursorMillis` の月次 `date` で毎ステップ `base.day` 由来の dom を使っていたため指摘どおり。実装側で開始日の「日」を固定して繰り返し計算するよう修正済み。

---

**識別子**: RC-5（GitHub id: 3213116900）

**レビュワー**: Copilot（インライン）

**レビューのコメント（原文）**:

```
[must] 月次「毎月同じ曜日」用の weekNumber が仕様(1〜5)から外れるケースがあります。ここで `Math.ceil((dt.day + firstDow) / 7)` としているため、月初が金/土で31日がある月などで 6 を返し得ます（例: 月初が土曜で 31日(月) → weekNumber=6）。一方 `week_number` の文言は 1〜5 までで、`getNthWeekdayOfMonthDt` も (weekNumber-1)*7 前提のため weekNumber=6 だと `null` になりプレビュー/作成日時が崩れます。仕様どおり 1〜5 に収まるよう `Math.min(5, ...)` でクランプするか、weekNumber の定義を「第N回目の曜日(=nth occurrence)」に合わせた算出に揃えてください。
```

**コメント要約**: getWeekNumberInMonth の Math.ceil 式が週番号 6 を返し得るため、nth 週曜日計算が破綻し得るという指摘。

**判断結果**: ✅ 対応済み

**判断理由**: `getWeekNumberInMonthFromMillis` と `getNthWeekdayOfMonthDateTime` の週番号定義を揃え、1〜5 に収まるよう修正済み。

---

## 評価セッション（2026-05-10）

- **評価日時**: 2026-05-10 JST
- **ブランチ名**: feat/1692
- **PR**: [#2003](https://github.com/nijuniinc/bokudeli-event-new/pull/2003)
- **Outdated 除外件数**: 0 件（`pulls/2003/comments` で `position` が null のレビューコメントはなし）

### RC 別対応チェックリスト（本セッション）

- [x] **RC-6**（GitHub id: 4414994590）— 👌 修正不要
- [x] **RC-7**（GitHub id: 4415001598）— ✅ 対応済み
- [x] **RC-8**（GitHub id: 4415001765）— 👌 修正不要
- [x] **RC-9**（GitHub id: 3214376411）— ✅ 対応済み
- [x] **RC-10**（GitHub id: 3214492217）— 👌 修正不要
- [x] **RC-11**（GitHub id: 3214670910）— ✅ 対応済み
- [x] **RC-12**（GitHub id: 3214765665）— 👌 修正不要
- [x] **RC-13**（GitHub id: 3214496114）— ✅ 対応済み
- [x] **RC-14**（GitHub id: 3214765666）— 👌 修正不要
- [x] **RC-15**（GitHub id: 3214667554）— ✅ 対応済み
- [x] **RC-16**（GitHub id: 3214667563）— 👌 修正不要
- [x] **RC-17**（GitHub id: 3214492203）— 👌 修正不要
- [x] **RC-18**（GitHub id: 3214492205）— 👌 修正不要
- [x] **RC-19**（GitHub id: 3214819653）— ✅ 対応済み
- [x] **RC-20**（GitHub id: 3214667574）— 👌 修正不要
- [x] **RC-21**（GitHub id: 3214667569）— ✅ 対応済み

---

**識別子**: RC-6（GitHub id: 4414994590）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。
```

**コメント要約**: レビュー依頼の再投稿。

**判断結果**: 👌 修正不要

**判断理由**: コード指摘ではなく依頼文の繰り返し。

---

**識別子**: RC-7（GitHub id: 4415001598）

**レビュワー**: Copilot（`copilot-swe-agent`、PR トップレベル）

**レビューのコメント（原文）**:

````

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

インラインコメント投稿を試みましたが環境制約で GitHub API にアクセスできないため、1指摘1ブロックで以下に列挙します。

---

### 1. `functions/default/src/eventCopy.ts` L37

[imo] `Date.now()` を使って `created_at` / `updated_at` を設定しています。プロジェクトルール「`Date` オブジェクトを直接使わず `luxon` を使う」に従い `DateTime.now().toMillis()` に変更してください。

```ts
// NG
const now = Date.now()
// OK
const now = DateTime.now().toMillis()
```

---

### 2. `functions/default/src/eventCopyRepeat.ts` L30・L105

[nits] 計測用途の `Date.now()` を 2 箇所使用しています。上記 1 と同様に luxon に統一してください。

```ts
// NG
const startedAt = Date.now()
elapsedMs: Date.now() - startedAt

// OK
const startedAt = DateTime.now().toMillis()
elapsedMs: DateTime.now().toMillis() - startedAt
```

---

### 3. `functions/default/src/eventCopyRepeat.ts` L79–81

[nits] `copyEventCore`（`eventCopy.ts:34`）内で既に同一の `isInShopTime` チェックが走るため、この前置チェックは冗長です。

```ts
// 冗長: copyEventCore 内でも同じ HttpsError を throw する
if (!isInShopTime(startTime, shop)) {
  throw new HttpsError('invalid-argument', 'Event time is not in shop time')
}
return copyEventCore(uid, srcEvent, shop, startTime)
```

---

### 4. `user/src/components/manage/community/CopyEventDialog.vue` L143

[imo] `watch` 内で `Date.now()` を使っています。luxon に統一してください。

```ts
// NG
const searchStart = getStartOfDay(Date.now() + MIN_DAYS_AHEAD * 24 * 60 * 60 * 1000)
// OK
const searchStart = getStartOfDay(DateTime.now().toMillis() + MIN_DAYS_AHEAD * 24 * 60 * 60 * 1000)
```

---

### 5. `user/src/components/manage/community/CopyEventDialog.vue` L163

[imo] `allowedDates` 内で `Date.now()` を使っています。luxon に統一してください。

```ts
// NG
if (date.getTime() < Date.now()) {
// OK
if (date.getTime() < DateTime.now().toMillis()) {
```

---

### 6. `user/src/components/manage/community/CopyEventDialog.vue` L316

[imo] `console.error` が本番コードに残っています。`emit('error')` だけで十分です。

```ts
// NG
console.error('Error creating events:', error)
emit('error')

// OK
emit('error')
```

---

### 7. `functions/default/src/eventCopyRepeat.ts` L13 / `CopyEventDialog.vue` L61

[imo] `MAX_REPEAT = 12` と `MAX_REPEAT_COUNT = 12` が別々に定義されています。`common` に定数として切り出して共有することで、将来の変更時の不整合を防げます。

---

### 8. `common/src/utils/datetime.ts` L261

[nits] `Math.floor(Number(input.repeatInterval)) || 1` で数値に `||` を使っています。プロジェクトルール「数値に falsy チェックを使わない」に違反します（`0` が `1` に化ける）。`input.repeatInterval` の最小値は `1` に設定しているため実害はありませんが、意図を明示する書き方に変えてください。

```ts
// NG
Math.floor(Number(input.repeatInterval)) || 1

// OK
const rawInterval = Math.floor(Number(input.repeatInterval))
const interval = Number.isFinite(rawInterval) && rawInterval >= 1 ? rawInterval : 1
```

---

### 9. `user/src/components/manage/community/CopyEventDialog.vue` L290・L300

[nits] `createEvents()` は冒頭の `canCreate.value` チェックを通過した時点で `targetDateTime.value != null`（single 時）および `previewStartTimes.value.length > 0`（repeat 時）が `canCreate` によって保証されています。`try` ブロック内の `if (targetDateTime.value == null) { return }` と `if (startTimes.length === 0) { return }` は冗長です。`finally` で `dialog.value = false` / `isCreating.value = false` が走るため、これらの `return` が実行されるとダイアログが意図せず閉じる副作用も生じます。


````

**コメント要約**: `Date.now` と luxon、冗長な `isInShopTime`、`console.error`、`MAX_REPEAT` の共通化、`repeatInterval` の `||` 利用、`createEvents` の try 内早期 return と finally、など 9 ブロックの複合指摘。

**判断結果**: ✅ 対応済み

**判断理由**: `createEvents` の try 内早期 return をやめ `canCreate` 前提に整理し、catch の `console.error` を除去。バンドル内の Luxon 統一・定数共通化・`repeatInterval` の `||` 書き換え等は未対応のまま（任意）。

---

**識別子**: RC-8（GitHub id: 4415001765）

**レビュワー**: chatgpt-codex-connector[bot]

**レビューのコメント（原文）**:

```
To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).
```

**コメント要約**: Codex 接続の案内。

**判断結果**: 👌 修正不要

**判断理由**: RC-3 と同種でコードレビューではない。

---

**識別子**: RC-9（GitHub id: 3214376411）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  月内週番号を5以下に正規化する**

`getWeekNumberInMonthFromMillis` は `Math.ceil((dt.day + firstDow) / 7)` をそのまま返すため、月初が金/土で 30日・31日を選ぶケースで `6` を返します（例: 2026-05-31）。この値は後続の `getNthWeekdayOfMonthDateTime` に渡されますが、曜日の出現回数は最大5回なので常に `null` となり、月次「同じ曜日」プレビューが空になって作成できなくなります。第N曜日として扱う値は 1〜5 に丸める（または `(day-1)/7+1` 系で算出する）必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: 月内週番号が 6 になり得て nth 曜日計算が壊れる指摘。

**判断結果**: ✅ 対応済み

**判断理由**: `getWeekNumberInMonthFromMillis` が `firstOccurrence` 基準の `Math.floor` 式に変更済み。

---

**識別子**: RC-10（GitHub id: 3214492217）

**レビュワー**: nijuni-yasu（インライン）

**レビューのコメント（原文）**:

```
対応済みです。`Math.min(5, …)` でクランプするのではなく、第N該当曜日の定義に沿った算出（`firstOccurrence` 基準の `floor` 式）に変更し、6 になり得ない・`getNthWeekdayOfMonthDateTime` と常に整合するようにしました。ご指摘の 2026-05-31 のようなケースも含め、プレビューが空になる経路は解消される想定です。
```

**コメント要約**: 週番号指摘への対応完了の返信。

**判断結果**: 👌 修正不要

**判断理由**: 新規の技術指摘ではない。

---

**識別子**: RC-11（GitHub id: 3214670910）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月次曜日コピーでも秒・ミリ秒を保持する**

`advanceRepeatCursorMillis` の月次 `weekday` 分岐だけ `second` と `millisecond` を `0` に固定しており、開始時刻に秒・ミリ秒を含むイベント（移行データや外部連携で作成されたイベントなど）を繰り返しコピーすると、日次/週次/月次(日付)/年次と異なり時刻が前倒しされます。結果としてコピー後の `event_start_datetime` と、それに連動する終了時刻・締切時刻が元イベントの時刻粒度と一致しなくなるため、ここでも `base.second` / `base.millisecond` を引き継ぐべきです。

Useful? React with 👍 / 👎.
```

**コメント要約**: 月次・同じ曜日分岐だけ秒と ms を 0 にしている不整合。

**判断結果**: ✅ 対応済み

**判断理由**: 月次 `weekday` 分岐で `base.second` / `base.millisecond` を `cand.set` に引き継ぐよう修正済み。

---

**識別子**: RC-12（GitHub id: 3214765665）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月次曜日コピーで秒・ミリ秒を保持する**

月次 `weekday` 分岐だけ候補日時を `second: 0, millisecond: 0` で固定しており、開始時刻に秒やミリ秒を含むイベントを繰り返しコピーすると、日次/週次/月次(日付)/年次と異なって時刻が前倒しされます。特に移行データや外部連携由来のイベントで開始・終了・締切時刻の整合が崩れるため、ここでも元の秒・ミリ秒を引き継ぐべきです。再確認した最新差分でも `cand.set({ hour: h, minute, second: 0, millisecond: 0 })` の実装が残っています。

Useful? React with 👍 / 👎.
```

**コメント要約**: RC-11 と同趣旨の再掲。

**判断結果**: 👌 修正不要

**判断理由**: RC-11 と重複。判断は RC-11 に集約。

---

**識別子**: RC-13（GitHub id: 3214496114）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月次曜日の探索失敗時は条件外日付を返さない**

`advanceRepeatCursorMillis` の月次 `weekday` 分岐は、24回探索して候補が見つからない場合に `base.plus({ months: interval * 24 })` を返していますが、この戻り値は「第N曜日」の条件を満たす保証がありません。たとえば `interval=12` で「2月の第5曜日」のように該当年が長期間現れないケースでは、このフォールバックが条件外の日付を生成し、プレビューおよび実際の複製日が指定パターンとずれる可能性があります。見つからない場合は条件外日付を返さず、呼び出し側でスキップ扱いにできる形にする必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: 24 回探索失敗時の戻り値が第 N 曜日を保証しない。

**判断結果**: ✅ 対応済み

**判断理由**: 探索失敗時は `null` を返し、`computeEventRepeatCopyPreviewStartTimes` でループを打ち切るよう修正済み。

---

**識別子**: RC-14（GitHub id: 3214765666）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月次曜日探索失敗時に条件外日付を返さない**

月次 `weekday` の探索が24回で見つからない場合に `base.plus({ months: interval * 24 })` を返しており、この返り値は「第N曜日」の条件を満たしません。`interval=12` で「2月の第5曜日」のような出現間隔が長い系列では、探索上限を超えた時点で条件外の日付がプレビューや実コピーに混入し得ます。見つからない場合は条件外日付を返すのではなく、未検出として呼び出し側でスキップできる形にするべきです。最新差分でもこのフォールバック実装がそのまま残っています。

Useful? React with 👍 / 👎.
```

**コメント要約**: RC-13 と同趣旨の再掲。

**判断結果**: 👌 修正不要

**判断理由**: RC-13 と重複。

---

**識別子**: RC-15（GitHub id: 3214667554）

**レビュワー**: Copilot（インライン）

**レビューのコメント（原文）**:

```
[must] `rawStartTimes` を `dedupeStartTimesPreserveOrder` に渡す前に配列長の上限チェックが無いので、極端に大きい配列を渡されると Set 構築でメモリ/CPU を消費して DoS になり得ます。`Array.isArray` 後に `rawStartTimes.length` を上限（例: MAX_REPEAT もしくは MAX_REPEAT より少し大きい値）で早期 reject するのが安全です。
```

**コメント要約**: dedupe 前に巨大配列を拒否すべきという DoS 指摘。

**判断結果**: ✅ 対応済み

**判断理由**: dedupe 前に `rawStartTimes.length` 上限（`MAX_RAW_START_TIMES_INPUT`）で早期 reject するよう修正済み。

---

**識別子**: RC-16（GitHub id: 3214667563）

**レビュワー**: Copilot（インライン）

**レビューのコメント（原文）**:

```
[imo] `failureCount > 0` のときにログが集計値のみなので、運用時にどの `startTime` が何で失敗したか追えません。失敗がある場合は warn で `results`（もしくは失敗分のみ）をログ出力するなど、原因調査できる粒度のログを追加したいです。
```

**コメント要約**: 失敗時のログを詳細化したい imo。

**判断結果**: 👌 修正不要

**判断理由**: 必須ではない運用改善の提案。

---

**識別子**: RC-17（GitHub id: 3214492203）

**レビュワー**: nijuni-yasu（インライン）

**レビューのコメント（原文）**:

```
対応済みです。`advanceRepeatCursorMillis` に繰り返し系列の基準となる `fixedDayOfMonth`（開始イベントの「日」）を渡し、`computeEventRepeatCopyPreviewStartTimes` では常に `startMs` の日を渡すようにしました。短い月で日が末日に丸められた後も、次の月以降は元の日付に戻る挙動になっています。テストで 1/31→2/28→3/31 を確認しています。
```

**コメント要約**: 月次「同じ日付」の基準日指摘への対応完了の返信。

**判断結果**: 👌 修正不要

**判断理由**: 新規指摘ではない。

---

**識別子**: RC-18（GitHub id: 3214492205）

**レビュワー**: nijuni-yasu（インライン）

**レビューのコメント（原文）**:

```
対応済みです。`getWeekNumberInMonthFromMillis` を `getNthWeekdayOfMonthDateTime` と同じ定義（月初から最初の該当曜日までの日数 `firstOccurrence` を求め、`Math.floor((dt.day - firstOccurrence) / 7) + 1` で週番号を出す）に揃えました。`Math.ceil` 由来の 6 になり得るケースを解消し、仕様の 1〜5 と後続処理が整合します。
```

**コメント要約**: 週番号修正済みの返信。

**判断結果**: 👌 修正不要

**判断理由**: 新規指摘ではない。

---

**識別子**: RC-19（GitHub id: 3214819653）

**レビュワー**: chatgpt-codex-connector[bot]（インライン）

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  successイベントの破壊的変更に追従漏れを解消する**

`success` のペイロードを文字列IDからオブジェクトに変更したため、未更新の `user/src/pages/manage/event/index.vue` では `handleCopySuccess(newEventId: string)` にオブジェクトが渡され、`copiedEventId` に `[object Object]` が入ります。その結果、OK押下時の `getManageEventPath(copiedEventId)` が不正なURLになり、`/manage/event` 画面経由のコピー後遷移が壊れます。`CopyEventDialog` のイベント契約変更に合わせてこの呼び出し側も `CopyEventSuccessPayload` で受けるよう更新が必要です。

Useful? React with 👍 / 👎.
```

**コメント要約**: `manage/event/index.vue` が新しい success ペイロード型に未追従で URL が壊れる指摘。

**判断結果**: ✅ 対応済み

**判断理由**: `handleCopySuccess` を `CopyEventSuccessPayload` で受け、単発・繰り返しで完了ダイアログ文言を分岐するよう修正済み。

---

**識別子**: RC-20（GitHub id: 3214667574）

**レビュワー**: Copilot（インライン）

**レビューのコメント（原文）**:

```
[ask] `createEvents` の `finally` で `dialog.value = false` だけ行っており、`selectedEvent`/日時/繰り返し設定がリセットされません。再度モーダルを開いたときに前回の選択が残って意図せず同条件で再コピーできてしまうので、完了後も初期化したい場合は `closeDialog()` を呼ぶ等で状態を揃える想定でしょうか？
```

**コメント要約**: 成功後のフォーム初期化の是非の質問。

**判断結果**: 👌 修正不要

**判断理由**: 仕様・UX の確認であり必須修正の断定ではない。

---

**識別子**: RC-21（GitHub id: 3214667569）

**レビュワー**: Copilot（インライン）

**レビューのコメント（原文）**:

```
[must] 作成処理中でもキャンセルボタンが押せるため、ユーザーが閉じた後に `createEvents` 側の emit(success/error) が走って完了/エラーダイアログが出るなど、状態が競合し得ます。`isCreating` 中はキャンセルを `disabled` にするか、close を無効化/ガードして二重操作できないようにしてください。

```

**コメント要約**: 作成中にキャンセルで非同期完了と競合し得る指摘。

**判断結果**: ✅ 対応済み

**判断理由**: `closeDialog` 先頭で `isCreating` 中は return、キャンセルボタンに `:disabled="isCreating"` を付与済み。

---
