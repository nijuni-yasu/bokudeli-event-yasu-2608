# PR #2183 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-07-16 13:00 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / pending wake PR #2182 → 実体 PR #2183）
- **ブランチ名**: fix/2175
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2183
- **REVIEW_REQUEST_SINCE**: 2026-07-16T03:53:10Z
- **partial**: true（Codex: 接続案内のみ。Copilot substantive あり）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id:4987987569、Codex 接続案内 id:4987999889）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 3592543846, 4987999201 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `hasMore=true` かつ `nextCursor=null` でページング打ち切り<br>後方探索 + sentinel doc で cursor 組み立て、`hasMore` を `nextCursor` と整合 |
| [x] | RC-2 | 3592543846, 4987999201 | ✅ 対応済み | 📌 スコープ内 | 📊 観測 | 🔧 微修正 | S | segment ログに `targetUserId` / `viewerUid` / `isOwner` を追加 |
| [x] | RC-3 | — | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `toMillis` の `as` キャストを `isRecord` 型ガードに置換 |
| [x] | RC-4 | 4988174702, 3592649886 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | sentinel を `nextCursor` にすると有効友人を未返却のままスキップ<br>返却に含めるか cursor 基準を見直す |
| [x] | RC-5 | 4988281498 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `firestoreHasMore` と重複する `docs.length > limit` 条件を削除 |
| [x] | RC-6 | 4988281498 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `pageDocs.length === 0` の早期 return を先に置く |
| [x] | RC-7 | 4988281498, 3592706948 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 💾 データ | 🔧 微修正 | S | page+sentinel が全不正でも Firestore 続きがあるときスキャンを進める<br>`sort_value_null` skip cursor で `startAfter(null, id)` |
| [x] | RC-8 | 4988281498 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `timed` の例外時に `success: false` 等で区別可能にする |
| [x] | RC-9 | 3592695134, 3592706954 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | 不正 doc スキップログに `ownerUserId` を追加 |
| [x] | RC-10 | 3592695155 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | `mapDocToUserFriendListRow` の二重呼び出しを一度のパースに統合 |
| [x] | RC-11 | 3592706951 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | limit+1 取得の tradeoff として継続 cursor を維持（後続欠落防止）<br>末尾空 fetch は許容と判断 |
| [x] | RC-12 | 3592695162 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | segment `info` ログの閾値出力（調査目的のため全件は不要） |
| [x] | RC-13 | 3592695173 | 👌 修正不要 | — | — | 👀 確認のみ | — | `getUser` は segment 外・`totalDurationMs` で計測済みが意図 |
| [x] | RC-14 | — | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `timed` の `catch (error: unknown)` で rethrow（parser 互換） |
| [x] | RC-15 | — | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `last_met_at` ソートの skip cursor テスト追加 |
| [x] | RC-16 | — | 👌 修正不要 | — | — | 👀 確認のみ | — | RC-11 末尾偽 hasMore は limit+1 tradeoff として許容（空 fetch 1 回） |
| [x] | RC-17 | — | ✅ 対応済み | 📌 スコープ内 | — | 📋 仕様追加 | S | segment 計測ログの寿命方針をコードコメントと Issue #2175 follow-up に記載 |

---

**識別子**: RC-1（GitHub id: 3592543846, 4987999201）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/stores/userFriend.ts`（`listUserFriends` の `hasMore` / `nextCursor`）

**レビュワーのコメント（要約）**:

### 🚨 必須修正

`hasMore=true` なのに `cursorValueFromDoc(lastDoc, sortBy)` が `null` のとき `nextCursor=null` となり、`resolveUserFriendsList` がループを終了して以降の友人を取りこぼす可能性がある。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `findPagingCursorDoc` で page 末尾から後方探索し、不正 doc のみの page では `limit+1` 件目（sentinel）から cursor を組み立てる。`hasMore` は `nextCursor != null` に整合させ、cursor 不可時は打ち切りを明示。境界テスト 2 件を追加。

---

**識別子**: RC-2（GitHub id: 3592543846, 4987999201）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/userProfile.ts`（`timed` セグメントログ）

**レビュワーのコメント（要約）**:

### 🟡 修正提案

segment ログが `{ segment, durationMs }` のみで、504 タイムアウト時に `returned` ログが無いと `targetUserId` と Cloud Logging で相関しづらい。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📊 観測

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `timed` に `PreviewTimedContext`（`targetUserId` / `viewerUid` / `isOwner`）を渡し、各 segment ログに含める。

---

**識別子**: RC-3（shokujii-code-review 自己レビュー）

**レビュワー**: Cursor Agent（`/shokujii-code-review`）

**指摘箇所**: `functions/default/src/stores/userFriend.ts` / `functions/default/src/userProfile.ts`（`toMillis`）

**レビュワーのコメント（要約）**:

### 🟡 修正提案

`toMillis` 内の `(value as { toMillis?: unknown })` はプロジェクト方針上避けたいパターン。型ガード関数に切り出すとよりきれい。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `isRecord` + ブラケットアクセスで `as` を除去。`userFriend.ts` は vitest の Timestamp モック互換のため duck typing のみ（`instanceof` なし）。戻り値の有限数値チェックも追加。

---

## 対応セッション

- **対応日時**: 2026-07-16 13:04 JST
- **対応者**: Cursor Agent
- **対応 RC**: RC-1, RC-2
- **変更ファイル**:
  - `functions/default/src/stores/userFriend.ts` — `findPagingCursorDoc` 追加、`hasMore` / `nextCursor` 整合
  - `functions/default/src/stores/userFriend.test.ts` — RC-1 境界テスト 2 件追加
  - `functions/default/src/userProfile.ts` — `timed` に `targetUserId` 等をログ出力

---

## 対応セッション（追加）

- **対応日時**: 2026-07-16 13:07 JST
- **対応者**: Cursor Agent
- **対応 RC**: RC-3
- **変更ファイル**:
  - `functions/default/src/stores/userFriend.ts` — `toMillis` の `as` 除去、`isRecord` 型ガード化
  - `functions/default/src/userProfile.ts` — 同上

---

## 評価セッション（2026-07-16 13:32 JST・auto）

- **評価日時**: 2026-07-16 13:32 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / wait wake PR #2183）
- **ブランチ名**: fix/2175
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2183
- **REVIEW_REQUEST_SINCE**: 2026-07-16T04:25:25Z
- **partial**: false
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id:4988167381、Codex 接続案内 id:4988175332）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-4 | 4988174702, 3592649886 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | sentinel を `nextCursor` にすると有効友人を未返却のままスキップ<br>返却に含めるか cursor 基準を見直す |

---

**識別子**: RC-4（GitHub id: 4988174702, 3592649886）

**レビュワー**: Copilot / Codex

**指摘箇所**: `functions/default/src/stores/userFriend.ts`（`findPagingCursorDoc` / `listUserFriends` の sentinel 処理）

**該当コード（レビュー時点の diff）**:

```diff
+/** 次ページ用 cursor を組み立てられる doc を後方から探索する（RC-1） */
+const findPagingCursorDoc = (
+  ...
+  if (firestoreHasMore && docs.length > limit) {
+    const sentinelDoc = docs[limit]
+    if (cursorValueFromDoc(sentinelDoc, sortBy) != null) {
+      return sentinelDoc
```

**レビュワーのコメント（原文・Copilot 要約）**:

### 🚨 必須修正

RC-1 対応で `pageDocs` に有効行が無いとき `sentinelDoc(docs[limit])` を `nextCursor` に採用しているが、この cursor で次ページを `startAfter` すると sentinel 自身（最初の有効候補）を未返却のままスキップする。有効な友人を欠落させるため、ページング整合性が壊れる。`nextCursor` は「このレスポンスで返した最後の行」を基準にするか、sentinel を使う場合は同一レスポンスに含めて欠落しないようロジックを修正し、欠落再現テストを追加すること。

**Codex インライン（原文抜粋）**: sentinel を cursor にして有効な友人を飛ばさないで。追加テストの「page 末尾が不正でも sentinel doc...」ケースでも `friend2` が返却されないまま cursor だけ進む。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当。RC-1 の sentinel フォールバックは `hasMore`/`nextCursor` 不整合は解消するが、sentinel が有効友人 doc のとき `startAfter(sentinel)` で当該友人が永久スキップされる。現行テストも `friends` が空のまま `nextCursor=friend2` を期待しており、欠落を再現している。sentinel を `friends` に含める、または cursor を返却済み末尾に限定する修正が必要。

**判断結果（対応後）**: ✅ 対応済み — `collectReturnedDocs` で page が全不正のときのみ sentinel を返却に含め、`nextCursor` は返却済み最終 doc のみから組み立てる。欠落再現テストを RC-4 向けに更新・追加。

---

## 対応セッション（RC-4）

- **対応日時**: 2026-07-16 13:37 JST
- **対応者**: Cursor Agent
- **対応 RC**: RC-4
- **変更ファイル**:
  - `functions/default/src/stores/userFriend.ts` — `collectReturnedDocs` / `canReturnFriendDoc`、cursor は返却済み末尾のみ
  - `functions/default/src/stores/userFriend.test.ts` — sentinel 返却・非返却の境界テスト

---

## 評価セッション（RC-4 後・再レビュー）

- **評価日時**: 2026-07-16 13:52 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / pr review wake）
- **ブランチ名**: fix/2175
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2183
- **REVIEW_REQUEST_SINCE**: 2026-07-16T04:41:06Z
- **partial**: false（Copilot substantive + Codex inline あり）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼 id:4988250140、Codex 接続案内 id:4988282135）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-5 | 4988281498 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `firestoreHasMore` と重複する `docs.length > limit` 条件を削除 |
| [x] | RC-6 | 4988281498 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `pageDocs.length === 0` の早期 return を先に置く |
| [x] | RC-7 | 4988281498, 3592706948 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 💾 データ | 🔧 微修正 | S | page+sentinel が全不正でも Firestore 続きがあるときスキャンを進める<br>`sort_value_null` skip cursor で `startAfter(null, id)` |
| [x] | RC-8 | 4988281498 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `timed` の例外時に `success: false` 等で区別可能にする |
| [x] | RC-9 | 3592695134, 3592706954 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | 不正 doc スキップログに `ownerUserId` を追加 |
| [x] | RC-10 | 3592695155 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | `mapDocToUserFriendListRow` の二重呼び出しを一度のパースに統合 |
| [x] | RC-11 | 3592706951 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | limit+1 取得の tradeoff として継続 cursor を維持（後続欠落防止）<br>末尾空 fetch は許容と判断 |
| [x] | RC-12 | 3592695162 | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | segment `info` ログの閾値出力（調査目的のため全件は不要） |
| [x] | RC-13 | 3592695173 | 👌 修正不要 | — | — | 👀 確認のみ | — | `getUser` は segment 外・`totalDurationMs` で計測済みが意図 |

---

**識別子**: RC-5（GitHub id: 4988281498・ブロック1）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/stores/userFriend.ts:109`

**該当コード（レビュー時点の diff）**:

```diff
+  if (returned.length === 0 && firestoreHasMore && docs.length > limit) {
```

**レビュワーのコメント（原文）**:

`firestoreHasMore` は `docs.length > limit` として定義されているため（L185–186）、`&& docs.length > limit` は常に `true` となる冗長な条件です。`firestoreHasMore` だけで意図が伝わります。

**コメント要約**: `collectReturnedDocs` の sentinel 分岐で `docs.length > limit` が冗長。<br>可読性のため `firestoreHasMore` のみに簡略化可能。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は正しい。挙動は変わらないが、RC-4 後の条件式が読みづらくなっているため簡略化してよい。

---

**識別子**: RC-6（GitHub id: 4988281498・ブロック2）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/stores/userFriend.ts:187-192`

**該当コード（レビュー時点の diff）**:

```diff
+  const returnedDocs = collectReturnedDocs(pageDocs, docs, limit, firestoreHasMore, sortBy)
+  const friends = mapReturnedDocsToFriends(returnedDocs)
+
+  if (pageDocs.length === 0) {
+    return { friends, hasMore: false, nextCursor: null }
+  }
```

**レビュワーのコメント（原文）**:

`pageDocs` が空のとき（Firestore が 0 件を返したとき）も `collectReturnedDocs` と `mapReturnedDocsToFriends` を呼んでいます。機能的な問題はありませんが、`pageDocs.length === 0` のチェックを先に置くとショートサーキットして読みやすくなります。

**コメント要約**: 0 件時の早期 return を前に移動すると読みやすい。<br>機能差分はなくリファクタ程度。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 妥当。0 件は稀だが、意図が明確になる。

---

**識別子**: RC-7（GitHub id: 4988281498・ブロック3, 3592706948）

**レビュワー**: Copilot / Codex

**指摘箇所**: `functions/default/src/stores/userFriend.ts`（`collectReturnedDocs` / `listUserFriends` のページング終端）

**該当コード（レビュー時点の diff）**:

```diff
+  if (returned.length === 0 && firestoreHasMore && docs.length > limit) {
+    const sentinelDoc = docs[limit]
+    if (canReturnFriendDoc(sentinelDoc, sortBy)) {
+      returned.push(sentinelDoc)
+    }
+  }
```

**レビュワーのコメント（原文）**:

（Copilot）`pageDocs` に有効行がなく、かつ sentinel も `canReturnFriendDoc` で弾かれるケースでは、Firestore には sentinel より先にさらに有効な友人が存在していても `hasMore: false` を返してページングが打ち切られます。`resolveUserFriendsList` の retry は `hasMore=true` のときのみ機能するため、有効な友人が silently 取りこぼされます。

（Codex P2）`limit + 1` 件の中で page 側も sentinel 側も無効な friend doc だった場合、`returned` が空のまま `nextCursor` が `null` / `hasMore` が `false` になります。例: `limit=1` で `invalidA, invalidB, validC` のとき `validC` まで到達できず友人一覧が途中で打ち切られます。表示できる doc がない場合でも、最後に走査した doc をカーソルにするなどしてスキャンを進める必要があります。

**コメント要約**: page と sentinel がともに不正のとき、Firestore に続きがあっても `hasMore=false` で打ち切る。<br>不正 doc だけのページでも cursor を進め、後続の有効友人までスキャンする必要あり。

**判断結果**: 🚨 必須修正

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害, 💾 データ

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-4 修正後も残る実害。現行テスト「cursor を組み立てられないとき hasMore は false」がこの欠落を許容している。`docs[limit]` が不正でも `friend_user_id` + sort キー（欠損時は `FieldPath.documentId()` のみ等）で `startAfter` できる cursor を組み立て、`friends` は空のまま `hasMore=true` で次ページを要求する設計が必要。`resolveUserFriendsList` の skip 上限と整合させる。

---

**識別子**: RC-8（GitHub id: 4988281498・ブロック4）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/userProfile.ts:40-52`（`timed` ヘルパー）

**該当コード（レビュー時点の diff）**:

```diff
+const timed = async <T>(segment: string, context: PreviewTimedContext, fn: () => Promise<T>): Promise<T> => {
+  const startedAt = performance.now()
+  try {
+    return await fn()
+  } finally {
+    logger.info('getUserProfilePreview segment', {
```

**レビュワーのコメント（原文）**:

`finally` でログを出しているため、`fn()` が例外を投げた場合も同じフォーマット（`info` レベル）でセグメントログが出ます。504/タイムアウト調査の文脈で「セグメントが完了せずに投げた」ケースを区別したい場合、`success: false` フラグや `error` レベルのログを追加する余地があります。必須ではありませんが、504 調査時に役立つ可能性があります。

**コメント要約**: 例外時も `info` で segment ログが出る。`success` フラグ等で完了/失敗を区別すると調査しやすい。<br>本 PR の調査目的には有用だがマージ必須ではない。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 妥当な改善。Issue #2175 の 504 切り分けに寄与するが、RC-7 より優先度は低い。

---

**識別子**: RC-9（GitHub id: 3592695134, 3592706954）

**レビュワー**: Copilot / Codex

**指摘箇所**: `functions/default/src/stores/userFriend.ts:71`

**該当コード（レビュー時点の diff）**:

```diff
+    logger.warn('listUserFriends skipped invalid friend doc', { friendUserId: doc.id })
```

**レビュワーのコメント（原文）**:

（Copilot）[imo] `listUserFriends` の不正 doc スキップログが `friendUserId` のみだと「どのユーザーの friends サブコレか」が追えず、調査・クレンジング時に相関が取りづらいです。`ownerUserId` も一緒に出しておくと Cloud Logging で検索しやすくなります。

（Codex P3）破損した friend doc をスキップするケースでは、`friendUserId` だけを出しても実ドキュメントのパスは `users/{uid}/friends/{friendUserId}` なので、所有者 uid も一緒に記録しないと運用調査に使えなくなります。

**コメント要約**: 不正 doc スキップログに `ownerUserId`（`listUserFriends` の `uid`）を追加。<br>Cloud Logging で修復対象を特定しやすくする。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 同一指摘の重複。`mapDocToUserFriendListRow` に `ownerUserId` を渡すか warn 呼び出し側で付与すればよい。データ不整合調査の実用性が上がる。

---

**識別子**: RC-10（GitHub id: 3592695155）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/stores/userFriend.ts:97`

**該当コード（レビュー時点の diff）**:

```diff
+const canReturnFriendDoc = (doc: QueryDocumentSnapshot, sortBy: UserFriendsSortBy): boolean =>
+  mapDocToUserFriendListRow(doc) != null && cursorValueFromDoc(doc, sortBy) != null
+
+const mapReturnedDocsToFriends = (returnedDocs: QueryDocumentSnapshot[]): UserFriendListRow[] =>
+  returnedDocs
+    .map((doc) => mapDocToUserFriendListRow(doc))
```

**レビュワーのコメント（原文）**:

[nits] `canReturnFriendDoc` と `mapReturnedDocsToFriends` の両方で `mapDocToUserFriendListRow` を呼んでいるため、返却対象 doc について `doc.data()` / `toMillis` 変換が二重実行になります（友人数が多いときに地味に効きます）。`collectReturnedDocs` 側で「row + cursorValue」を一度だけパースして返す形に寄せると、意図（転送量・処理量削減）により素直になります。

**コメント要約**: 返却 doc で `mapDocToUserFriendListRow` が二重実行される。<br>一度パースした row を再利用するリファクタで処理量削減。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: 妥当。本 PR の性能改善意図と整合。RC-7 対応時にまとめて整理してもよい。

---

**識別子**: RC-11（GitHub id: 3592706951）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/stores/userFriend.ts:201`

**該当コード（レビュー時点の diff）**:

```diff
+  return {
+    friends,
+    hasMore: nextCursor != null,
+    nextCursor,
+  }
```

**レビュワーのコメント（原文）**:

**P3** page 側が全て無効で `docs[limit]` の sentinel だけを `friends` に含めた場合、この `firestoreHasMore` は「sentinel が存在した」ことしか示しておらず、sentinel の後続がある保証にはなりません。例: `limit=1` で取得結果が `invalid, validLast` の2件だけなら `validLast` はこのレスポンスで返却済みの最終行ですが、`hasMore=true` / `nextCursor=validLast` になり、クライアントは存在しない次ページを読み込んで空レスポンスを受けます。sentinel を消費して返したケースでは、後続を確認できない限り次カーソルを出さないようにしてください。

**コメント要約**: sentinel を返却に含めたが Firestore バッチ末尾のとき `hasMore=true` になり空ページが発生。<br>後続未確認なら `hasMore=false` にする。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当。データ欠落ではなく余分な 1 回 fetch だが、プロフィール友人プレビューの UX に影響。sentinel を返却消費した場合は `returnedDocs` に sentinel が含まれ `pageDocs` が全不正だったときのみ `hasMore` を再判定するロジックで解消可能。

---

**識別子**: RC-12（GitHub id: 3592695162）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/userProfile.ts:49`

**該当コード（レビュー時点の diff）**:

```diff
+    logger.info('getUserProfilePreview segment', {
+      segment,
```

**レビュワーのコメント（原文）**:

[imo] `getUserProfilePreview` は呼び出し頻度が高い想定なので、毎回 segment ごとに `info` ログが出るとログ量（コスト/ノイズ）が増えやすいです。調査目的なら「duration が閾値超えのときだけ出す」「総時間が一定以上のときだけ segment を出す」など、出力条件を入れるのを検討してもよさそうです。

**コメント要約**: 毎リクエストの segment `info` はログ量増。<br>閾値超え時のみ出力する条件付きログを検討。

**判断結果**: 🟡 修正提案

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 妥当。本 PR は 504 調査用の一時的計測が主目的。マージ後に閾値化または削除する方針でもよいが、本 PR 内で `totalDurationMs` 超過時のみ segment 出力にするとノイズを抑えられる。

---

**識別子**: RC-13（GitHub id: 3592695173）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/userProfile.ts:332`

**該当コード（レビュー時点の diff）**:

```diff
+    const totalStartedAt = performance.now()
+
     const targetUser = await getUser(targetUserId, false)
```

**レビュワーのコメント（原文）**:

[ask] segment 計測が `Promise.all([...])` の各 fetch のみなので、`getUser(targetUserId, false)` が遅いケース（users 読み取りや converter/parse 等）があってもログから切り分けにくいです。`getUser` も `timed('get_user', ...)` で計測対象に含める想定でしょうか？

**コメント要約**: `getUser` が segment 外で遅延の切り分けが難しい。<br>`timed` に含めるべきかの確認。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 意図的。`getUser` は `Promise.all` 前の直列処理で、完了ログの `totalDurationMs` が `totalStartedAt` から全体を計測している。並列 segment は友人・イベント等のボトルネック特定用。`getUser` を segment に入れるとログ件数が増え、今回の主因候補（friends の event_history 転送）の観測がぼやける。必要なら `totalDurationMs` と segment 合計の差分で推定可能。

---

## 対応セッション（RC-5〜12）

- **対応日時**: 2026-07-16 14:01 JST
- **対応者**: Cursor Agent
- **対応 RC**: RC-5, RC-6, RC-7, RC-8, RC-9, RC-10, RC-11, RC-12
- **変更ファイル**:
  - `functions/default/src/stores/userFriend.ts` — `parseFriendDoc` 単一パース、`sort_value_null` skip cursor、RC-6 早期 return
  - `functions/default/src/stores/userFriend.test.ts` — RC-7 / RC-11 境界テスト追加・更新
  - `functions/default/src/userFriends.ts` — `CursorSchema` に `sort_value_null` 追加
  - `functions/default/src/utils/userFriendsResolver.test.ts` — 不正 doc skip ページ連続 fetch テスト
  - `functions/default/src/userProfile.ts` — `timed` に `success` フラグ、segment 閾値ログ（200ms）

**RC-11 補足**: limit+1 取得では sentinel 存在時 `firestoreHasMore=true` となり、後続 doc の可能性を否定できない。`hasMore=false` 強制は friend3 欠落につながるため、継続 cursor を維持（末尾空 fetch は許容）。

---

## 評価セッション（2026-07-16 14:16 JST・shokujii-code-review）

- **評価日時**: 2026-07-16 14:16 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: fix/2175
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2183

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-14 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `timed` の `catch (error: unknown)` で rethrow（parser 互換） |
| [x] | RC-15 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 🔧 微修正 | S | `last_met_at` ソートの skip cursor 生成・適用テスト追加 |
| [x] | RC-16 | なし・エージェントレビュー | 👌 修正不要 | — | — | 👀 確認のみ | — | RC-11 末尾偽 hasMore は単一 `limit+1` では判定不能。tradeoff 許容 |

---

**識別子**: RC-14（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（`/shokujii-code-review`）

**指摘箇所**: `functions/default/src/userProfile.ts:48`

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📐リファクタ/S]: `catch (error)` で型未明示。`catch (error: unknown)` + rethrow に整理。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: bare `throw` は ESLint parser 非対応のため `throw error` を維持。`unknown` 型付けで意図を明示。

---

**識別子**: RC-15（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（`/shokujii-code-review`）

**指摘箇所**: `functions/default/src/stores/userFriend.test.ts`

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: RC-7 skip cursor テストが `meet_count` のみ。`last_met_at` + `startAfter(null, id)` / `Timestamp.fromMillis` 分岐の回帰防止テストを追加。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: skip cursor 生成・2 ページ目 `startAfter(null, id)` の 2 テストを追加。

---

**識別子**: RC-16（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（`/shokujii-code-review`）

**指摘箇所**: `functions/default/src/stores/userFriend.ts`（RC-11 sentinel 返却時の `hasMore`）

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: sentinel を返却した末尾ページで `hasMore=true` となり空 fetch が 1 回余分に走る可能性。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 単一 `limit+1` クエリでは sentinel 以降の doc 有無を判定できない。`hasMore=false` 強制は `[invalid, friend2, friend3]` で friend3 欠落（RC-4/RC-11 再発）。`getUserProfilePreview` は hasMore 不使用。`getUserFriends` の末尾空 fetch 1 回は許容 tradeoff。精密化は limit+2 等で別 Issue 化可。

---

## 対応セッション（RC-14 / RC-15）

- **対応日時**: 2026-07-16 14:16 JST
- **対応者**: Cursor Agent
- **対応 RC**: RC-14, RC-15
- **変更ファイル**:
  - `functions/default/src/userProfile.ts` — `catch (error: unknown)` + rethrow
  - `functions/default/src/stores/userFriend.test.ts` — `last_met_at` skip cursor テスト 2 件

---

## 評価セッション（2026-07-16 14:27 JST・shokujii-code-review）

- **評価日時**: 2026-07-16 14:27 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: fix/2175
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2183

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-17 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | — | 📋 仕様追加 | S | segment 計測ログの寿命方針をコードコメントと Issue #2175 follow-up に記載 |

---

**識別子**: RC-17（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（`/shokujii-code-review`）

**指摘箇所**: `functions/default/src/userProfile.ts`（`PROFILE_PREVIEW_SEGMENT_LOG_MIN_MS` / `timed` / returned ログ）

**レビュワーのコメント（原文）**:

🟡 **修正提案** [fyi/📋仕様追加/S]: 504 調査完了後、segment ログや閾値定数の削除・見直し方針を Issue / doc に残すとよい。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📋 仕様追加

**想定工数**: S

**判断理由**: 調査用一時計測であることを userProfile.ts コメント・Callable JSDoc・Issue #2175 follow-up チェックリストで明示。504 調査中は計測を維持。

---

## 対応セッション（RC-17）

- **対応日時**: 2026-07-16 14:27 JST
- **対応者**: Cursor Agent
- **対応 RC**: RC-17
- **変更ファイル**:
  - `functions/default/src/userProfile.ts` — 計測ログ寿命のコメント・JSDoc 追記
  - Issue #2175 — follow-up チェックリストをコメント投稿

---
