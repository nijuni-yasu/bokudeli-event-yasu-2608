# PR #2091 レビューコメント評価

## 評価セッション（2026-06-19・review-comments-evaluate）

- **評価日時**: 2026-06-19 01:00 JST（doc 修正対応: 2026-06-19）
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: `dev/enterprise`（評価作業ブランチ。対象 PR は `doc/enterprise-2071`）
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2091
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 6（issue コメント: レビュー依頼×2 `@copilot`/`@codex`、Codex 利用上限×2、Copilot 対応方針返信×1、Codex 接続案内×1。Codex review ボイラープレート review 本体は接続案内のみのためスキップ）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | なし | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | 30_リファクタ計画が v0.3 §3-1 と 10_仕様 の正本を揺らす<br>§26 警告を 10_仕様/02 へ統一 |
| [x] | RC-2 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | sampletech_communities CSV<br>自社ドメインのため匿名化不要 |
| [x] | RC-3 | なし | 👌 修正不要 | — | — | 👀 確認のみ | — | sampletech_members CSV<br>自社ドメインのため匿名化不要 |
| [x] | RC-4 | なし | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | v0.2/README の v0.3/02 リンクが移動後 404<br>10_仕様/02 へ張り替え |
| [x] | RC-5 | なし | ✅ 対応済み | ❓ 要確認 | 📑 仕様書 | 📄 ドキュメントのみ | S | v0.2/01 と 90_アーカイブ v0.2 原典が同一<br>アーカイブ正本への誘導注記を追記 |
| [x] | RC-6 | 3437055080 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書, 💾 データ | 📄 ドキュメントのみ | S | PF クエリの materialize 前提へ 02_アーキテクチャ を修正 |
| [x] | RC-7 | 3437055068 | ✅ 対応済み | 📌 スコープ内 | 💰 金銭 | 📋 仕様追加 | M | capped expected を isConsistent に反映 |
| [x] | RC-8 | 3437055074 | ✅ 対応済み | 📌 スコープ内 | 🔒 セキュリティ | 📋 仕様追加 | S | communities update に isManager fallback 追記 |
| [x] | RC-9 | 3437055096 | ✅ 対応済み | 📌 スコープ内 | 💰 金銭 | 📋 仕様追加 | S | orderCount を orders.length に修正 |
| [x] | RC-10 | 3437055104 | ✅ 対応済み | 📌 スコープ内 | 💰 金銭 | 📋 仕様追加 | M | PF 版 cancelOrders 同型の順序・失敗時扱いを明記 |
| [x] | RC-11 | 3437055061 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | store + createModuleLogger の雛形へ差し替え |
| [x] | RC-12 | 3437055086 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📋 仕様追加 | M | authForTenant 前提へ仕様統一（実装は別 Issue 注記） |
| [x] | RC-13 | 3437055053 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | workflow 相対リンクを ../../../ へ修正 |
| [x] | RC-14 | 3437055046 | ✅ 対応済み | 📌 スコープ内 | 📑 仕様書 | 📄 ドキュメントのみ | S | deploy_enterprise.yml は #2071 前提の注記を追記 |

---

**識別子**: RC-1（GitHub id: なし・Copilot review 4526302625 内・suppressed）

**レビュワー**: Copilot（copilot-pull-request-reviewer[bot]）

**指摘箇所**: `documents/08_エンタープライズ/30_リファクタ計画/02_Project分離なし_実装計画.md:26`

**該当コード（レビュー時点の diff）**:

```diff
+- **スコープ外**:
+  - PF版データ露出フィルタ（`enterprise_id == null` 必須化）→ **[10_仕様/02_アーキテクチャ](../10_仕様/02_アーキテクチャ.md)** が正本。
+…
+> **⚠️ 期待値合わせ（重要）**: …解消するには PF版クエリへの `enterprise_id == null` 必須化が必要で、**正本は v0.3 §3-1**。
```

**レビュワーのコメント（原文）**:

[must] この段落内で「PF版データ露出フィルタの正本」が `v0.3 §3-1` と書かれていますが、直前の箇条書きでは `10_仕様/02_アーキテクチャ.md` を正本としてリンクしています。正本の記載先を統一しないと参照がぶれます。

**コメント要約**: 同一ファイル内で PF 露出フィルタの正本が `10_仕様/02` と `v0.3 §3-1` で二重化している。再編後の正本体系（10_仕様）に合わせて §26 警告文を修正する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: doc 再編 PR の目的は正本の一本化。`00_計画/02_developmentマージ` も `10_仕様/02` を参照しており、v0.3 §3-1 表記は旧参照の残り。**2026-06-19**: `30_リファクタ計画/02_Project分離なし_実装計画.md` §26 を 10_仕様/02 正本へ統一済み。

---

**識別子**: RC-2（GitHub id: なし・Copilot review 4526302625 内・suppressed）

**レビュワー**: Copilot（copilot-pull-request-reviewer[bot]）

**指摘箇所**: `documents/08_エンタープライズ/samples/sampletech_communities_nijuni.csv:3`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし・PR diff 上の CSV 先頭行付近）

**レビュワーのコメント（原文）**:

[must] サンプルCSVに実在しそうな個人名・社内ドメインのメールアドレスが含まれています。メンバーCSVと合わせて匿名の値（example.com 等）に置き換え、外部共有しても問題ないサンプルにしてください。

**コメント要約**: コミュニティサンプル CSV に `@nijuni.jp` 等の実名風データがある。公開リポジトリのサンプルとして example.com 等へ匿名化する。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: サンプル用途なら匿名化が望ましいが、**@nijuni.jp は自社ドメイン**のためプロジェクト判断で修正不要。

---

**識別子**: RC-3（GitHub id: なし・Copilot review 4526302625 内・suppressed）

**レビュワー**: Copilot（copilot-pull-request-reviewer[bot]）

**指摘箇所**: `documents/08_エンタープライズ/samples/sampletech_members_nijuni.csv:3`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし・PR diff 上の CSV 先頭行付近）

**レビュワーのコメント（原文）**:

[must] サンプルCSVに実在しそうな個人名・社内ドメインのメールアドレスが含まれています。ドキュメントリポジトリとして公開/共有されうるため、個人情報・内部情報として扱われないよう匿名の値（example.com 等）に置き換えてください。

**コメント要約**: メンバーサンプル CSV も同様に `@nijuni.jp` 等を含む。RC-2 とセットで匿名化する。

**判断結果**: 👌 修正不要

**PRスコープ**: —

**ラベル**: —

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-2 と同一論点。**@nijuni.jp は自社ドメイン**のため修正不要。

---

**識別子**: RC-4（GitHub id: なし・Copilot review 4526302625 内・suppressed）

**レビュワー**: Copilot（copilot-pull-request-reviewer[bot]）

**指摘箇所**: `documents/08_エンタープライズ/v0.2/00_README.md:52`

**該当コード（レビュー時点の diff）**:

```diff
+| `../v0.3/02_エンプラv0.3_設計.md` | 機能拡張。**PF 版露出フィルタ（`enterprise_id == null` 必須化）は §3-1 が正本**（v0.2 スコープ外） |
```

**レビュワーのコメント（原文）**:

[must] 参照リンクが `../v0.3/02_エンプラv0.3_設計.md` になっていますが、このPRでは v0.3 原典が `90_アーカイブ/v0.3_機能拡張/02_設計.md` に移動しているためリンク切れになります。v0.2 の外部参照としては、現行正本の `10_仕様/02_アーキテクチャ.md` へリンクするのが整合します。

**コメント要約**: v0.2 索引の v0.3 リンクが再編後パスと不一致で 404。PF 露出は `10_仕様/02_アーキテクチャ` へ張り替える。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 再編 PR の最優先観点「リンク切れ」に該当。v0.2 は残存ディレクトリのため索引のリンク追随が必要。

---

**識別子**: RC-5（GitHub id: なし・Copilot review 4526302625 内・suppressed）

**レビュワー**: Copilot（copilot-pull-request-reviewer[bot]）

**指摘箇所**: `documents/08_エンタープライズ/v0.2/01_エンプラv0.2_再設計.md:6`

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

[ask] このファイルの内容が `90_アーカイブ/v0.2_アーキテクチャ再検討/01_経緯_再設計_partner_CI_functions.md` と同一になっています。2箇所に同内容があると後から差分が発生して参照が割れやすいので、どちらを正とするか（片方へ誘導するか）を明記したいです。

**コメント要約**: v0.2/01 とアーカイブ v0.2 原典が重複。正本の一方へ誘導する注記が欲しい。

**判断結果**: ✅ 対応済み

**PRスコープ**: ❓ 要確認

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: 指摘は妥当だが、v0.2 配下を残すかアーカイブのみ正とするかは再編方針の判断。README 冒頭に「参照用・正本は 90_アーカイブ」等の1行で足りる可能性があり、人間確認を ❓ とした。

---

**識別子**: RC-6（GitHub id: 3437055080）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/02_アーキテクチャ.md:24`

**該当コード（レビュー時点の diff）**:

```diff
+## PF 版データ露出フィルタ（MVP 必須）
+
+- PF 版の**全一覧・検索・collectionGroup** クエリに **`enterprise_id == null`（または `get('enterprise_id', null)`）を必須**とする。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  PF 判別子を materialize する前提に合わせてください**

PF 版の一覧クエリに単純な `enterprise_id == null` を必須化すると、同じコミットのマージ計画でも指摘している通り既存 PF ドキュメントは optional フィールドが欠落しているため Firestore クエリにマッチせず、トップ/一覧が全件ゼロになります。またクエリでは `get('enterprise_id', null)` も使えないため、正本側もバックフィル済みの判別子（例: `enterprise_id: null` の materialize または `is_enterprise == false`）を使う前提に修正してください。

Useful? React with 👍 / 👎.

**コメント要約**: 02_アーキテクチャが PF クエリに素朴な `== null` / `get()` を書いており Firestore 上成立しない。`00_計画/02_developmentマージ` §2.1 の T1 前提へ本文を揃える。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書, 💾 データ

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: `02_developmentマージ.md` §2.1 と同一論点。Rules 文脈の `get()` は別（966行付近）だが、**クエリ**への `get()` 言及は誤り。正本の 02_アーキテクチャ冒頭を T1 完了後の判別子前提に修正必須。`dev/enterprise` 上で §2.1 案 (a)(b) の注記は追記済みだが本行は未修正。

---

**識別子**: RC-7（GitHub id: 3437055068）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/02_アーキテクチャ.md:658`

**該当コード（レビュー時点の diff）**:

```diff
+export function isPaymentEnterpriseSubsidyAmountConsistent(
+  eventPayment: EventPaymentType,
+  settings: EnterpriseSubsidySettings | undefined,
+  order: EventMemberOrder,
+): boolean {
+  const expected = computePaymentEnterpriseSubsidyAmount(eventPayment, settings, order.menu_price)
+  const stored = order.pay_enterprise_subsidy_amount
+  return expected === stored   // 両方 undefined も一致とみなす
+}
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  月額上限で切り下げた補助額も整合チェックしてください**

月額残枠が少ないケースでは §3.6 で `pay_enterprise_subsidy_amount` を残枠分へ切り下げて保存するとしていますが、この整合チェックは月額上限を考慮せず `computePaymentEnterpriseSubsidyAmount` の満額と stored を比較します。そのため、例えば満額補助 ¥500・残枠 ¥300 の正しい注文が `confirmOrder` / `createStripeCheckoutSession` で不整合扱いになって確定できないため、残枠を含めた capped expected を検証する仕様にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: §3.6 の残枠 cap と整合チェック関数が矛盾。cap 後の expected を比較する API を仕様化する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 💰 金銭

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: §673–683 で cap 挙動を定義済みなのに §651–659 の整合関数が満額のみ比較すると実装不能。決済確定の P1 級の仕様欠落。

---

**識別子**: RC-8（GitHub id: 3437055074）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/02_アーキテクチャ.md:1011`

**該当コード（レビュー時点の diff）**:

```diff
+match /communities/{communityId} {
+  allow read: if docEnterpriseId(resource.data) == null
+              || isSameEnterprise(resource.data);
+  allow create: if (request.auth != null && request.resource.data.get('enterprise_id', null) == null)
+              || (request.auth.token.enterprise_id != null
+                  && request.resource.data.enterprise_id == request.auth.token.enterprise_id);
+  allow update: if isSupport() || isEnterpriseCommunityManager(communityId);
+}
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  PF 版コミュニティ manager の update を残してください**

この Rules 例を既存の `match /communities/{communityId}` に適用すると、現行 `firestore.rules` の PF 版 `allow update: if isSupport() || isManager()` にあった通常のコミュニティ manager 更新が消え、`enterprise_id` を持たない PF コミュニティでは `isEnterpriseCommunityManager` が成立しません。エンプラ対応後も PF 版店舗管理者が自分のコミュニティ設定を更新できるよう、既存 `isManager()` 条件を fallback として残してください。

Useful? React with 👍 / 👎.

**コメント要約**: communities の update Rules 例から PF `isManager()` が落ちる。`|| isManager()` を fallback として追記。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🔒 セキュリティ

**変更種別**: 📋 仕様追加

**想定工数**: S

**判断理由**: development マージ PR の後方互換要件（T4）と整合。PF manager が update 不能になる regression を正本が誘発する。

---

**識別子**: RC-9（GitHub id: 3437055096）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/04_詳細_割引・決済.md:325`

**該当コード（レビュー時点の diff）**:

```diff
+const orderCount = orders.filter((o) => o.status === 'ordered').length  // 通常は orders.length
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  確定前 status で注文数を数えないでください**

`confirmOrder` / `stripeWebhook` の usage 更新でこの式をそのまま使うと、通常は注文を `ordered` に変更する前の `in_cart` / `processing` の `orders` を集計するため `orderCount` が 0 になり、`monthly_order_count` が増えません。ここは確定対象の件数として `orders.length` を使うか、status 更新後の件数を加算する仕様にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: Transaction 内で status 更新前に `ordered` フィルタすると件数 0。コメント行も `orders.length` を示唆しておりコードと矛盾。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 💰 金銭

**変更種別**: 📋 仕様追加

**想定工数**: S

**判断理由**: 月次利用回数集計の仕様バグ。コメント「通常は orders.length」と実コードが食い違っており、Codex 指摘は明確に妥当。

---

**識別子**: RC-10（GitHub id: 3437055104）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/04_詳細_割引・決済.md:975`

**該当コード（レビュー時点の diff）**:

```diff
+--- Stripe Refund（Transaction 外）---
+
+5. stripe_id でグルーピングし、stripe_id ごとに:
+   - 当該グループの返金額 = Σ(menu_price - pay_enterprise_subsidy_amount)
+   - Stripe Refund を実行（最大3回 retry）
+   → 全 retry 失敗 → 管理者に通知 + HttpsError('internal') を返す
+
+--- Firestore 更新（Transaction 内）---
+
+6. Transaction 内で:
+   a. 全 order の status を 'canceled' に更新、canceled_at をセット
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  返金後に Firestore 更新する順序を避けてください**

この順序だと Stripe Refund が成功した後に後続の Firestore Transaction が競合・権限・一時エラーで失敗した場合、ユーザーには返金済みなのに `member_orders` は `ordered` のまま、`monthly_usage` も戻らない不整合が残ります。返金と Firestore 更新は current PF 実装同様に失敗時の扱いを明確にした順序へ揃えるか、返金成功後の更新失敗を再実行できる補償フローを仕様化してください。

Useful? React with 👍 / 👎.

**コメント要約**: Refund 先行だと Firestore 失敗時に返金済み・注文残の不整合。PF 実装に揃えるか補償フローを仕様化。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 💰 金銭

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: 論点は妥当だが、PF 版 cancel フローとの差分調査と補償設計が必要。マージ前に方針明記は推奨（🟡）。実装前ゲートでよいが正本に「失敗時」を書くべき。

---

**識別子**: RC-11（GitHub id: 3437055061）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/04_詳細_監査ログ.md:174`

**該当コード（レビュー時点の diff）**:

```diff
+  await getFirestore()
+    .collection(`enterprises/${params.enterpriseId}/audit_logs`)
+    .add(log)
+
+  logger.info('audit_log', log)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  監査ログ実装例を store と共通 logger 経由にしてください**

この実装例を `functions/default` にそのまま移す場合、`getFirestore().collection(...).add(log)` で store / converter / Zod バリデーションを迂回し、さらに `logger.info` もプロジェクト標準の `createModuleLogger` を使わない実装になります。監査ログは後続実装の雛形になるため、`functions/default/src/stores` 側の store 関数経由の書き込みと共通 logger を使う例に揃えてください。

Useful? React with 👍 / 👎.

**コメント要約**: 監査ログのコード例が AGENTS.md の store 経由ルールに反する。store + createModuleLogger の雛形へ。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: プロジェクト必須ルール「DB 操作は store 経由」。仕様書の実装例がそのままコピーされると規約違反実装の温床になる。

---

**識別子**: RC-12（GitHub id: 3437055086）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/05_認証・テナント.md:422`

**該当コード（レビュー時点の diff）**:

```diff
+2. Firebase Auth ユーザーを作成
+   getAuth().createUser({ email, emailVerified: true })
+
+3. custom claims を設定
+   getAuth().setCustomUserClaims(uid, {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  従業員作成は tenant-aware Auth を使ってください**

この仕様は従業員を Identity Platform の企業テナント内 uid として扱い、フロントも `auth.tenantId` を設定する前提ですが、ここで project-level の `getAuth().createUser` を使うとデフォルトテナントにユーザーが作られ、企業テナントでの OTP / カスタムトークンログイン対象になりません。`enterprise_id ↔ tenantId` から `tenantManager().authForTenant(tenantId)` を取得し、createUser / claims / custom token 発行を tenant-aware API に統一してください。

Useful? React with 👍 / 👎.

**コメント要約**: createEnterpriseMembers 手順が project-level Auth。IdP マルチテナンシー前提と矛盾し `authForTenant` へ統一が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📋 仕様追加

**想定工数**: M

**判断理由**: 本文・ADR-002 と手順の矛盾。現行 `functions/default/src/enterprise/members.ts` も `getAuth().createUser` であり、正本修正は実装タスクの前提になる P1 級。

---

**識別子**: RC-13（GitHub id: 3437055053）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/07_デプロイ・運用.md:287`

**該当コード（レビュー時点の diff）**:

```diff
+| Firestore ルール（`/enterprises`） | [`deploy_firestore.yml`](../../.github/workflows/deploy_firestore.yml) または `firebase deploy --only firestore` |
+| Storage ルール（`/enterprises`） | [`deploy_storage.yml`](../../.github/workflows/deploy_storage.yml) または `firebase deploy --only storage` |
+| Functions（`createEnterprise` 等） | [`deploy_functions.yml`](../../.github/workflows/deploy_functions.yml) |
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  デプロイ手順の workflow 相対リンクを直してください**

このファイルは `documents/08_エンタープライズ/10_仕様/` 配下なので、`../../.github/...` は `documents/.github/...` に解決され、実在するルート直下の `.github/workflows/deploy_firestore.yml` などに到達しません。GitHub 上で初回デプロイ手順を辿るとリンクが 404 になるため、ここは `../../../.github/workflows/...` にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: `10_仕様/` から `.github` へは `../../../` が必要。§5 表と冒頭 CI リンクを一括修正。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: パス計算上の明確なリンク切れ。P3 だが運用正本としてマージ前修正が妥当。

---

**識別子**: RC-14（GitHub id: 3437055046）

**レビュワー**: Codex（chatgpt-codex-connector[bot]）

**指摘箇所**: `documents/08_エンタープライズ/10_仕様/07_デプロイ・運用.md:306`

**該当コード（レビュー時点の diff）**:

```diff
+Phase 1 以降、`enterprise` パッケージを Firebase Hosting に初回デプロイする際の手順書。  
+CI は [`.github/workflows/deploy_enterprise.yml`](../../.github/workflows/deploy_enterprise.yml) が `hosting:enterprise` をデプロイする。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  存在しない enterprise デプロイ workflow を前提にしないでください**

初回デプロイや手動デプロイでこの手順を使う場合、リポジトリには `.github/workflows/deploy_enterprise.yml` が存在せず（`.github/workflows` には deploy_user/partner/firestore/functions/storage などのみ）、`Deploy enterprise` workflow も実行できません。このままだと運用手順の正本が実行不能な Actions を案内するため、workflow を追加するか未実装/別手順として明記してください。

Useful? React with 👍 / 👎.

**コメント要約**: #2091 単独マージ時点の `development` には workflow 未存在。未実装注記か #2071 同梱前提を明記。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📑 仕様書

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: `origin/development` に当該 workflow は無い（確認済み）。`dev/enterprise` には #2071 で存在するが、#2091 先出しマージでは手順が実行不能。正本に前提（#2071 マージ後 / 手動 deploy 代替）を書く必要がある。

---
