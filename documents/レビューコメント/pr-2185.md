# PR #2185 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-07-16 14:10 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / wait-ai-pr-review wake）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **REVIEW_REQUEST_SINCE**: 2026-07-16T05:00:03Z
- **partial**: false
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id:4988354639 ※SINCE より前、Codex 接続案内 id:4988370546）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-1 | 4988369934 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 本番 `linkWithRedirect` 後に pending クリアが走らず二重リンクの恐れ<br>redirect 前に pending を別状態へ遷移させる必要 |
| [x] | RC-2 | 3592770327 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | RC-3 対応時にテストのダミー credential に `operationType: 'link'` を付与<br>テストが意図どおりのケースを検証するため |
| [x] | RC-3 | 3592770301 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 予約トースト消費に `operationType === 'link'` を追加<br>通常 signIn と予約の偶然一致で誤トーストを防ぐ |
| [x] | RC-4 | 3592776086 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | ログイン済みで `router.replace('/login')` が `/` へ飛ばす<br>OAuth 復帰が `/` だと `handleRedirect` が走らず連携失敗 |
| [x] | RC-5 | 3592776088 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 本番では `linkWithRedirect` 後に `reserveLinkageCompletedToast` が実行されない<br>redirect 開始前に予約する必要 |
| [x] | RC-6 | 3592776089 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 通常メールログインでも stale pending で SNS 連携が走る<br>`linkProviderId` 等で自動連携フロー限定にする |
| [x] | RC-7 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `/pass-code` 初回ガードで未ログイン時に pending が消える<br>リロード後の OTP 自動連携が壊れる |
| [x] | RC-8 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | pid と pending 不一致時に stale pending が残る<br>後続 `/login` で意図しない link の恐れ |
| [x] | RC-9 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `startPendingProviderLink` / `handleRedirect` のユニットテスト不足 |
| [x] | RC-10 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | link 開始前 pending clear 後の同期失敗でフロー再開が必要<br>ロールバックと仕様追記 |
| [x] | RC-11 | 3592880899 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `pass-code.vue` で `clearPendingLinkRequest` 未 import<br>実行時 ReferenceError の恐れ |
| [x] | RC-12 | 3592905082 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | 自動連携途中離脱後、pid なし `/login` で stale pending が残る<br>通常 SNS ログイン成功時に意図しない link が走る恐れ |
| [x] | RC-13 | 3592905075 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | pid なし通常メール OTP で stale pending が残る<br>ログイン後の `handleRedirect` で意図しない link の恐れ |
| [x] | RC-14 | 3592905078 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | S | 未ログイン `/pass-code` リロードで `redirectPath` に逃げる<br>OTP 画面が表示されず自動連携が中断する |
| [x] | RC-15 | 3592905073 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | manage レイアウトで `consumePendingToast` 未実装<br>管理画面経由の自動連携完了トーストが欠落する |
| [x] | RC-16 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 👀 確認のみ | — | 初回 🚨 `startPendingProviderLink` 本番順序<br>RC-1/4/5/7/10 で解消済みを再確認 |
| [x] | RC-17 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 👀 確認のみ | — | 初回 🟡 `handleRedirect` 二重 link<br>RC-1/3 の `operationType !== 'link'` で解消済みを再確認 |
| [x] | RC-18 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 👀 確認のみ | — | 初回 🟡 `manage.vue` の `consumePendingToast`<br>RC-15 で解消済みを再確認 |
| [x] | RC-19 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 👀 確認のみ | — | 初回 🟡 本番 OAuth 順序のテスト不足<br>RC-9/2 で解消済みを再確認 |
| [x] | RC-20 | なし・エージェント再レビュー | 👌 修正不要 | 📤 スコープ外 | 📏 規約 | 🔧 微修正 | S | `register/index.vue` の pid 検証が `as ProviderIdType` のまま<br>`login.vue` と非統一（本 PR 範囲外） |
| [x] | RC-21 | 3593297500 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `login.vue` で query pid1/pid2 を未正規化のまま渡す<br>型エラーの恐れ |
| [x] | RC-22 | 3593323035 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | M | ログイン済み `/pass-code` リロードで OTP 画面が消える<br>メール変更フローのリグレッション |
| [x] | RC-23 | 3593323041 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | stale pending と login の pid1 不一致時に保持<br>意図しない SNS link の恐れ |
| [x] | RC-24 | 3593323047 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 連携開始失敗後 pending 復元のまま通常遷移<br>後続 handleRedirect で意図しない link |
| [x] | RC-25 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | §6.2 が RC-23 の pending 一致条件を未記載<br>仕様と実装の乖離 |
| [x] | RC-26 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | 連携失敗後も redirectPath へ遷移<br>エラー通知を見失う恐れ |
| [x] | RC-27 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | S | `login.vue` で query pid 正規化が重複<br>`loginQueryPids` に集約 |
| [x] | RC-28 | 4989364250 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | S | `pass-code.vue` 内側 if がデッドコード<br>`clearStale` 後は到達不能 |
| [x] | RC-29 | 3593467741 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 自動連携 dialog 表示時も stale pending 未検証<br>RC-23 の else 分岐のみ対応の漏れ |
| [x] | RC-30 | 3593449346 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | S | `router/index.ts` の i18n `@ts-expect-error`<br>`routerTranslate` ヘルパーで置換 |
| [x] | RC-31 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `login.vue` onMounted で `runLoginPageMountAutoLinkage` 戻り値未使用<br>computed 再評価の二重パース |
| [x] | RC-32 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `pass-code.vue` mount / OTP 後連携のテスト不足<br>`passCodeAutoLinkage.ts` 抽出 + vitest |
| [x] | RC-33 | 3593638542 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | S | 本番で `startPendingProviderLink` が null でも `'linked'` を返す<br>`/pass-code` に留まる恐れ。null 時は `skipped` |
| [x] | RC-34 | 4989564237 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | `login.vue` ConfirmDialog の `v-if` と `:model-value` 二重<br>ロジック誤りではないが冗長 |

---

**識別子**: RC-1（GitHub id: 4988369934）

**レビュワー**: Copilot

**指摘箇所**: `base/src/utils/redirect.ts:74`（トップレベル・インライン行なし）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

> @codex この PR の Files changed をコードレビューしてください。指摘は shokujii-code-review チェックリストに沿って日本語でお願いします。
> 
> @copilot この PR ...

Files changed を確認した結果、現時点の指摘は 1 件です。

🚨 **必須修正** `base/src/utils/redirect.ts:74`
`startPendingProviderLink()` は `await linkByProviderService(...)` の後で `clearPendingLinkRequest()` / `reserveLinkageCompletedToast()` を実行していますが、本番の `linkWithRedirect` ではこの行まで戻らないため pending が残ったままになります。すると SNS 連携から `/login` へ復帰した最初の `handleRedirect()` が stale な pending を見て同じ provider の `linkByProviderService()` をもう一度起動し、二重リダイレクトやループになり得ます。redirect 前に pending を別状態へ遷移させるか、`handleRedirect()` 側で OTP 後に開始済みの連携を再実行しないようにする必要があります。

**コメント要約**: 本番では `linkWithRedirect` 後に pending クリアが実行されず stale pending が残る。
`/login` 復帰時の `handleRedirect` が再リンクを起動し二重リダイレクトの恐れ。redirect 前の状態遷移が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `linkWithRedirect` はページ離脱のため `await` 以降が実行されない。現実装では pending が sessionStorage に残り、RC-4 と合わせてメール OTP 経路の連携が破綻しうる。指摘は妥当。

**対応内容**: `startPendingProviderLink` で `linkByProviderService` 前に `clearPendingLinkRequest` + `reserveLinkageCompletedToast` を実行。`handleRedirect` では `operationType !== 'link'` のときのみ pending 再リンク。

---

**識別子**: RC-2（GitHub id: 3592770327）

**レビュワー**: Copilot

**指摘箇所**: `base/src/utils/redirect.test.ts:103`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,126 @@
+  it('予約と不一致かつ /profile 以外では null を返し予約を保持する', () => {
+    reserveLinkageCompletedToast('facebook.com')
+    const result = resolveLinkageCompletedProviderId(
+      { providerId: 'google.com' } as import('firebase/auth').UserCredential,
+      '/login',
+    )
```

**レビュワーのコメント（原文）**:

[must] `resolveLinkageCompletedProviderId()` 側で `operationType === 'link'` を条件に追加する場合、このテストのダミー UserCredential にも `operationType: 'link'` を含めないと意図どおりのケースになりません。

**コメント要約**: RC-3 で `operationType` 条件を足す場合、テストのモック credential にも `operationType: 'link'` が必要。
成功ケース・不一致ケースの両方でモックを揃える。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-3 実装時のテスト追随指摘として妥当。単独ではロジックバグではないが、RC-3 とセットで対応すべき。

**対応内容**: `redirect.test.ts` のモック credential に `operationType: 'link'` を付与。signIn 誤消費防止テストを追加。

---

**識別子**: RC-3（GitHub id: 3592770301）

**レビュワー**: Copilot

**指摘箇所**: `base/src/utils/redirect.ts:62`

**該当コード（レビュー時点の diff）**:

```diff
+  const reserved = sessionStorage.getItem(LINKAGE_COMPLETED_TOAST_KEY)
+  if (reserved != null && reserved === userCredential.providerId) {
+    sessionStorage.removeItem(LINKAGE_COMPLETED_TOAST_KEY)
+    return reserved
+  }
```

**レビュワーのコメント（原文）**:

[must] `pendingLinkageCompletedProviderId` の予約が残ったまま通常ログイン（`operationType: 'signIn'`）が行われると、providerId が一致しただけで「連携完了」トーストが誤表示され得ます。予約の消費は `link` 完了時に限定するため、`userCredential.operationType === 'link'` も条件に入れるのが安全です。

**コメント要約**: 予約トーストは providerId 一致だけでは不十分。`operationType === 'link'` で連携完了に限定すべき。
stale 予約＋通常 signIn で誤トーストの恐れ。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: Firebase `UserCredential.operationType` で signIn と link を区別できる。予約消費条件の強化は妥当な防御。

**対応内容**: `resolveLinkageCompletedProviderId` の予約消費に `userCredential.operationType === 'link'` を追加。

---

**識別子**: RC-4（GitHub id: 3592776086）

**レビュワー**: Codex

**指摘箇所**: `base/src/utils/redirect.ts:79`

**該当コード（レビュー時点の diff）**:

```diff
+  await router.replace('/login')
+  const result = await linkByProviderService(user, pendingId)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  OAuth復帰先を処理対象ルートに固定して**

本番のメール OTP 自動連携では `signInWithCustomToken` 後にログイン済みなので、この `router.replace('/login')` は既存のログイン済みユーザーガード（`user/src/router/index.ts` の `/login` → `/` リダイレクト）に捕まり、`linkWithRedirect` の復帰先が `/` になります。一方で `getRedirectResult`/`handleRedirect` は初回ガードで `/login`・`/register`・`/register/complete`・`/profile` に着地した場合だけ実行されるため、`/` に戻るとリンク結果が処理されず SNS 連携も完了トーストも発火しません。

Useful? React with 👍 / 👎.

**コメント要約**: OTP 後はログイン済みのため `/login` へ replace すると router が `/` へリダイレクトする。
OAuth 復帰が `/` だと初回 `handleRedirect` が走らず連携・トーストが失敗する（P1）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: `router/index.ts` L395-396 でログイン済みユーザーの `/login` は `/` へ飛ばされる。指摘はコードと一致し、メール OTP 自動連携の本番経路に致命的。

**対応内容**: `router.replace('/login')` を削除し OAuth を `/pass-code` から開始。`isFirstTime` 対象に `/pass-code` を追加。

---

**識別子**: RC-5（GitHub id: 3592776088）

**レビュワー**: Codex

**指摘箇所**: `base/src/utils/redirect.ts:107`

**該当コード（レビュー時点の diff）**:

```diff
+      userCredential = await linkByProviderService(user, pendingLinkRequestProviderId)
+      reserveLinkageCompletedToast(pendingLinkRequestProviderId)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  リダイレクト前に完了トーストを予約して**

SNS 同士の自動連携で本番環境では `linkByProviderService` が `linkWithRedirect` を呼ぶため、ここでページ遷移が始まり、この `await` 以降の `reserveLinkageCompletedToast` は実行されません。その結果、リンク自体は次回 `/login` 復帰時に完了しても予約が存在せず、`resolveLinkageCompletedProviderId` が `/profile` 以外では `null` を返すので、今回追加した完了トーストが表示されません。

Useful? React with 👍 / 👎.

**コメント要約**: 本番 SNS 同士フローで `reserveLinkageCompletedToast` が `await linkByProviderService` 後にあり実行されない。
完了トースト予約は redirect 開始前に行う必要（P2）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: レビュー追随で「成功後に予約」へ移したが、本番 `linkWithRedirect` では後続行が走らない。`startPendingProviderLink` も同様。指摘妥当。

**対応内容**: `handleRedirect` と `startPendingProviderLink` の両方で `reserveLinkageCompletedToast` を `linkByProviderService` 前に移動。

---

**識別子**: RC-6（GitHub id: 3592776089）

**レビュワー**: Codex

**指摘箇所**: `user/src/pages/pass-code.vue:180`

**該当コード（レビュー時点の diff）**:

```diff
+      const linked = await completePendingLinkAfterEmailLogin()
+      if (linked) {
+        return
+      }
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  通常メールログインでは stale pending を消費しないで**

以前の SNS 連携フローを途中離脱したなどで `pendingLinkRequestProviderId` が sessionStorage に残っている場合、`pid` のない通常のメールログインでもこの呼び出しが無条件に走るため、OTP 入力後に突然 SNS 連携の OAuth が開始されます。現在の `/pass-code` が自動連携フローであること（例: `linkProviderId` があり pending と一致すること）を確認してから pending を消費しないと、通常ログインの導線まで stale な連携状態に引きずられます。

Useful? React with 👍 / 👎.

**コメント要約**: `linkProviderId`（自動連携）以外の通常メールログインでも pending があれば SNS 連携が走る。
stale pending のガードが必要（P2）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `completePendingLinkAfterEmailLogin` は `peekPendingLinkRequest` のみでフロー判定しておらず、通常 OTP ログインに副作用が及ぶ。指摘妥当。

**対応内容**: `linkProviderId` があり pending と一致するときのみ `completePendingLinkAfterEmailLogin` を実行。

---

## 評価セッション（2026-07-16 14:26 JST・shokujii-code-review）

- **評価日時**: 2026-07-16 14:26 JST
- **評価者**: Cursor Agent（`shokujii-code-review`）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-7 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `/pass-code` 初回ガードで未ログイン時に pending が消える |
| [x] | RC-8 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | pid と pending 不一致時に stale pending が残る |
| [x] | RC-9 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | handleRedirect / startPendingProviderLink のテスト不足 |
| [x] | RC-10 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | link 同期失敗時の pending ロールバックと仕様追記 |

---

**識別子**: RC-7（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/utils/redirect.ts:84-86`

**レビュワーのコメント（原文）**:

🚨 **必須修正** `/pass-code` を isFirstTime 対象にした結果、`handleRedirect(null)` が無条件に pending を clear し、OTP 入力前のリロードで自動連携が壊れる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**対応内容**: `user != null` のときのみ pending を read + clear するよう変更。

---

**識別子**: RC-8（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/pass-code.vue:124`

**レビュワーのコメント（原文）**:

🟡 **修正提案** 自動連携フローで `pid` と pending が不一致のとき stale pending を残すと、後続 `/login` の handleRedirect で意図しない link が起動しうる。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**対応内容**: 不一致かつ pending ありのとき `clearPendingLinkRequest()` を実行。

---

**識別子**: RC-9（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/utils/redirect.test.ts`

**レビュワーのコメント（原文）**:

🟡 **修正提案** redirect 前の clear + reserve や `operationType !== 'link'` ガードのユニットテストが不足。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**対応内容**: `startPendingProviderLink` / `handleRedirect` のテスト 6 件を追加。

---

**識別子**: RC-10（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/utils/redirect.ts:78-80`

**レビュワーのコメント（原文）**:

🟡 **修正提案** link 開始前に pending を clear するため、同期失敗時はフロー再開が必要。ロールバックと仕様追記を推奨。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**対応内容**: `startPendingProviderLink` と `handleRedirect` の link 分岐で catch 時に pending / 予約を復元。§6.2 に追記。

---

## 評価セッション

- **評価日時**: 2026-07-16 14:45 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / wait-ai-pr-review wake）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **REVIEW_REQUEST_SINCE**: 2026-07-16T05:30:58Z
- **partial**: true（Codex 接続案内 id:4988559067 のみ）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id:4988558535、Codex 接続案内 id:4988559067）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-11 | 3592880899 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | `pass-code.vue` で `clearPendingLinkRequest` 未 import<br>実行時 ReferenceError の恐れ |

### RC-11

**GitHub id**: 3592880899

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/pass-code.vue` L126（`completePendingLinkAfterEmailLogin`）

**該当コード**:

```diff
@@ -13,7 +13,15 @@ import {
   requestEmailRegistration,
 } from '@shokujii/base/apis/user'
 import { setLastLoginProvider } from '@shokujii/base/utils/lastLoginProvider.js'
-import { getRedirectPath } from '@shokujii/base/utils/redirect'
+import { setPendingToast } from '@shokujii/base/utils/pendingToast.js'
+import {
+  clearLinkageCompletedToast,
+  getRedirectPath,
+  isProviderIdType,
+  peekPendingLinkRequest,
+  startPendingProviderLink,
+} from '@shokujii/base/utils/redirect'
```

**レビュワーのコメント（原文）**:

[must] `completePendingLinkAfterEmailLogin()` 内で `clearPendingLinkRequest()` を呼んでいますが、このファイルでは import されていないため、実行時に ReferenceError（またはビルド時に未定義エラー）になります。`@shokujii/base/utils/redirect` の import に `clearPendingLinkRequest` を追加してください。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**対応内容**: `@shokujii/base/utils/redirect` の import に `clearPendingLinkRequest` を追加。

---

## 評価セッション

- **評価日時**: 2026-07-16 15:30 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` manual）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 5（レビュー依頼 id:4988354639 / 4988538132 / 4988558535、Codex 接続案内 id:4988370546 / 4988559067）
- **重複除外件数**: 1（id:3592905070 = RC-11 同一指摘・対応済み）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-12 | 3592905082 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | 自動連携途中離脱後、pid なし `/login` で stale pending が残る<br>通常 SNS ログイン成功時に意図しない link が走る恐れ |
| [x] | RC-13 | 3592905075 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | pid なし通常メール OTP で stale pending が残る<br>ログイン後の `handleRedirect` で意図しない link の恐れ |
| [x] | RC-14 | 3592905078 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | S | 未ログイン `/pass-code` リロードで `redirectPath` に逃げる<br>OTP 画面が表示されず自動連携が中断する |
| [x] | RC-15 | 3592905073 | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | manage レイアウトで `consumePendingToast` 未実装<br>管理画面経由の自動連携完了トーストが欠落する |

### RC-12

**GitHub id**: 3592905082

**レビュワー**: Codex

**指摘箇所**: `base/src/utils/redirect.ts:93`

**該当コード（レビュー時点の diff）**:

```diff
+export const handleRedirect = async (user: User | null) => {
+  // 未ログイン時は pending を保持する（/pass-code リロード後の OTP 自動連携で必要）
+  let pendingLinkRequestProviderId: string | null = null
+  if (user != null) {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  pending の保持を自動連携中の画面に限定してください**

未ログイン時に常に pending を保持するようになったため、SNS 自動連携の `/login?pid...` や `/pass-code?pid...` を途中離脱したユーザーが後日 pid なしの通常 `/login` を開いても `pendingLinkRequestProviderId` が消えません。その状態で通常の SNS ログインに成功すると、次の `handleRedirect(user)` が古い pending を見て別 SNS の `linkByProviderService` を開始し、本人が選んでいない連携リダイレクトに巻き込まれるため、pending 保持は自動連携中の `/pass-code` 等に限定し、通常ログイン画面では破棄してください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-7 対応で未ログイン時に pending を常時保持するようになった副作用として、自動連携フローを途中離脱したあと pid なしの通常 `/login` で SNS ログインすると、stale pending により意図しない link が走る恐れがある。自動連携中の画面に pending 保持を限定し、通常ログイン画面では破棄すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: RC-6 / RC-8 は pass-code 経路をガードしたが、`/login` で pid なし通常 SNS ログイン時の stale pending 破棄は未実装。再現は中間離脱が必要だが、指摘の論理は妥当。`login.vue` の `onMounted` または router で pid1/pid2/pending フロー外なら `clearPendingLinkRequest()` を検討。

**対応内容**: `redirect.ts` に `clearStalePendingLinkRequestOutsideAutoLinkage` を追加。`login.vue` の `onMounted`（pid1/pid2 なし時）で呼び出し。

---

### RC-13

**GitHub id**: 3592905075

**レビュワー**: Codex

**指摘箇所**: `user/src/pages/pass-code.vue:121`

**該当コード（レビュー時点の diff）**:

```diff
+const completePendingLinkAfterEmailLogin = async (): Promise<boolean> => {
+  if (linkProviderId.value == null) {
+    return false
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  通常メールログイン時は stale pending を破棄してください**

以前の SNS 自動連携を途中離脱して `pendingLinkRequestProviderId` だけが残った状態で、pid のない通常のメール OTP ログインに進むと、この早期 return により pending が残り続けます。その後ログイン済みで `/profile` など初回 `handleRedirect()` 対象ルートを開くと、`userCredential` が null でも pending を見て `linkByProviderService` が起動し、意図しない SNS 連携リダイレクトが始まり得るため、通常ログイン経路では pending を明示的に破棄してください。

Useful? React with 👍 / 👎.

**コメント要約**: `linkProviderId` が無い通常メール OTP ログインでは `completePendingLinkAfterEmailLogin` が早期 return し stale pending を残す。ログイン後に `/profile` 等で `handleRedirect` が走ると意図しない link が始まる。pid なし経路では pending を明示的に破棄すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-8 は pid と pending の不一致時のみ clear。pid なし通常 OTP では pending が残る穴が残っている。`linkProviderId == null` かつ `peekPendingLinkRequest() != null` のとき `clearPendingLinkRequest()` で足りる。

**対応内容**: `pass-code.vue` の `runAutoLinkageOnMount` 冒頭と `completePendingLinkAfterEmailLogin` 先頭で `clearStalePendingLinkRequestOutsideAutoLinkage` を呼び出し。

---

### RC-14

**GitHub id**: 3592905078

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:163`

**該当コード（レビュー時点の diff）**:

```diff
-    if (['/login', '/register', '/register/complete', '/profile'].includes(to.path)) {
+    if (['/login', '/register', '/register/complete', '/profile', '/pass-code'].includes(to.path)) {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-lightgrey?style=flat)</sub></sub>  /pass-code リロード時は redirectPath に逃がさないでください**

`/pass-code` を初回 OAuth 復帰処理の対象に追加したことで、未ログインのままパスコード画面をリロードした場合もこのガードに入ります。ログイン開始前に保存された `redirectPath` が sessionStorage に残っている一般的な導線では、下の `user == null` 分岐が `getRedirectPath()` を返して元ページへ遷移してしまい、OTP 入力・自動連携画面が表示されません。`/pass-code` の未ログイン復帰では pending を保持したまま画面を続行するよう除外してください。

Useful? React with 👍 / 👎.

**コメント要約**: `/pass-code` を `isFirstTime` 対象にした結果、未ログインリロード時に `user == null` 分岐が `getRedirectPath()` を返し OTP 画面から逃げる。保護ページ経由で `redirectPath` が残っていると自動連携が中断する。`/pass-code` 未ログイン時は `redirectPath` 遷移を抑止すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害, 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-4 で `/pass-code` を追加した副作用で、仕様 §6.2「未ログイン時の pending 保持・OTP 継続」と矛盾するリグレッション。`to.path === '/pass-code' && user == null` のとき `getRedirectPath()` を返さない修正が必要。

**対応内容**: `router/index.ts` の `user == null` 分岐で `to.path === '/pass-code'` のとき `undefined` を返し OTP 画面に留まる。

---

### RC-15

**GitHub id**: 3592905073

**レビュワー**: Codex

**指摘箇所**: `user/src/router/index.ts:312`

**該当コード（レビュー時点の diff）**:

```diff
+      const providerIdForToast = resolveLinkageCompletedProviderId(userCredential, to.path)
+      if (providerIdForToast != null) {
+        setLinkageCompletedPendingToast(providerIdForToast)
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  manage でも pendingToast を消費してください**

管理画面から未ログインで `/login` に飛ばされたあと SNS 自動連携が成功すると、ここで pendingToast を保存した上で `getRedirectPath()` により元の `/manage/...` に戻ります。しかし今回 `consumePendingToast` を追加したのは `blank` / `default` レイアウトだけで、`/manage/...` は `user/src/layouts/manage.vue` を使うためトーストが消費されず完了通知が欠落します。manage レイアウトでも同じ pendingToast を消費する必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: 管理画面 → ログイン → 自動連携成功後、`getRedirectPath()` で `/manage/...` に戻るが `manage.vue` は `consumePendingToast` 未実装のため完了トーストが表示されない。`blank` / `default` と同様に manage レイアウトでも消費が必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 指摘は妥当。再現は管理画面経由の自動連携に限定され優先度は P3 だが、§4.6.3 の完了トースト共通化の漏れ。`manage.vue` の `onMounted` に `consumePendingToast` を追加すれば解消。

**対応内容**: `manage.vue` の `onMounted` に `consumePendingToast` を追加（`blank.vue` / `default.vue` と同様）。

---

## 評価セッション（2026-07-16 15:48 JST・shokujii-code-review 修正検討）

- **評価日時**: 2026-07-16 15:48 JST
- **評価者**: Cursor Agent（`shokujii-code-review` 再レビュー / 修正検討）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし
- **備考**: fix/2184 差分に対する初回エージェントレビュー（🚨 1 件・🟡 3 件）について、RC-1〜15 対応後の現行コードで修正方針を検証。残 🚨 なし。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-16 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 👀 確認のみ | — | 初回 🚨 `startPendingProviderLink` 本番順序<br>RC-1/4/5/7/10 で解消済みを再確認 |
| [x] | RC-17 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 👀 確認のみ | — | 初回 🟡 `handleRedirect` 二重 link<br>RC-1/3 の `operationType !== 'link'` で解消済みを再確認 |
| [x] | RC-18 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 👀 確認のみ | — | 初回 🟡 `manage.vue` の `consumePendingToast`<br>RC-15 で解消済みを再確認 |
| [x] | RC-19 | なし・エージェント再レビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 👀 確認のみ | — | 初回 🟡 本番 OAuth 順序のテスト不足<br>RC-9/2 で解消済みを再確認 |
| [x] | RC-20 | なし・エージェント再レビュー | 👌 修正不要 | 📤 スコープ外 | 📏 規約 | 🔧 微修正 | S | `register/index.vue` の pid 検証が `as ProviderIdType` のまま<br>`login.vue` と非統一（本 PR 範囲外） |

### マージ前手動確認（コード変更なし・T-1〜T-12 抜粋）

| # | シナリオ | 期待 |
|---|---------|------|
| T-1 / T-2 | メール既存 + Facebook/Google（**本番ビルド**） | 初回 OTP → 連携完了 + 完了トースト |
| T-5 / T-10 | pid なし通常メールログイン | 開始トーストなし・意図しない link なし |
| T-9 | 自動連携完了後 `redirectPath` / `/manage/...` 着地 | 完了トースト表示 |
| T-12 | 自動連携途中離脱 → 後日 pid なし `/login` | stale pending による意図しない link なし |

---

**識別子**: RC-16（GitHub id: なし・エージェント再レビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/utils/redirect.ts:93`

**該当コード（レビュー時点の diff）**:

```diff
+export const startPendingProviderLink = async (user: User): Promise<UserCredential | null> => {
+  const pendingId = peekPendingLinkRequest()
+  if (pendingId == null || !isProviderIdType(pendingId)) {
+    return null
+  }
+  clearPendingLinkRequest()
+  reserveLinkageCompletedToast(pendingId)
+  try {
+    return await linkByProviderService(user, pendingId)
+  } catch (err: unknown) {
+    setPendingLinkRequest(pendingId)
+    clearLinkageCompletedToast()
+    throw err
+  }
+}
```

**レビュワーのコメント（原文）**:

🚨 **必須修正** [🔧微修正/S]: `startPendingProviderLink` で `clearPendingLinkRequest` / `reserveLinkageCompletedToast` が `linkWithRedirect` **後** にあり、本番では実行されない。pending 残存 → `handleRedirect` 二重 link、完了トースト欠落の恐れ。

**コメント要約**: 本番 OAuth では redirect 前に pending clear + トースト予約が必要。あわせて OAuth 復帰先を `/login` から `/pass-code` に留める（RC-4）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: 現行実装は link 前に clear + reserve。`router.replace('/login')` は削除済み（RC-4）。未ログイン時 pending 保持（RC-7）・同期失敗ロールバック（RC-10）もセットで本番経路をカバー。

**対応内容**: RC-1 / RC-4 / RC-5 / RC-7 / RC-10 にて対応済み。再レビューで解消を確認。

---

**識別子**: RC-17（GitHub id: なし・エージェント再レビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/utils/redirect.ts:131`

**該当コード（レビュー時点の diff）**:

```diff
+    if (
+      pendingLinkRequestProviderId != null &&
+      isProviderIdType(pendingLinkRequestProviderId) &&
+      userCredential?.operationType !== 'link'
+    ) {
+      reserveLinkageCompletedToast(pendingLinkRequestProviderId)
+      try {
+        userCredential = await linkByProviderService(user, pendingLinkRequestProviderId)
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `getRedirectResult` がすでに link 結果を返すとき、`handleRedirect` が再度 `linkByProviderService` を呼ぶ構造リスク。`operationType !== 'link'` ガードまたは pre-clear で防御すべき。

**コメント要約**: メール OTP 後 link 復帰と SNS 同士フローで pending / credential の関係が異なる。link 済み credential への再 link を防ぐ必要。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: `operationType !== 'link'` 条件と `startPendingProviderLink` の pre-clear が組み合わさり、メール OTP 本番経路と SNS 同士経路の両方をカバー。`redirect.test.ts` に再 link しないケースあり（RC-9）。

**対応内容**: RC-1 / RC-3 / RC-9 にて対応済み。再レビューで解消を確認。

---

**識別子**: RC-18（GitHub id: なし・エージェント再レビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/layouts/manage.vue:42`

**該当コード（レビュー時点の diff）**:

```diff
+import { consumePendingToast } from '@shokujii/base/utils/pendingToast.js'
…
+onMounted(() => {
+  const toast = consumePendingToast()
+  if (toast != null) {
+    notification.message = toast.message
+    notification.color = toast.color
+  }
+})
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: 完了トーストの `consumePendingToast` が `blank` / `default` のみ。管理画面経由の自動連携完了後にトーストが欠落する。

**コメント要約**: §4.6.3 の完了トースト共通化で `manage.vue` も対象。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-15 で `manage.vue` に `onMounted` + `consumePendingToast` を追加済み。

**対応内容**: RC-15 にて対応済み。再レビューで解消を確認。

---

**識別子**: RC-19（GitHub id: なし・エージェント再レビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/utils/redirect.test.ts:174`

**該当コード（レビュー時点の diff）**:

```diff
+describe('startPendingProviderLink', () => {
+  …
+  it('link 前に pending を clear し予約トーストを設定する', async () => {
+    …
+  })
+})
+
+describe('handleRedirect', () => {
+  …
+  it('operationType link のとき再 link しない', async () => {
+    …
+  })
+})
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: redirect 前 clear + reserve、`operationType !== 'link'` ガードのユニットテストが不足。

**コメント要約**: 本番 OAuth 順序バグは DEV の popup だけでは再現しにくい。モックで pre-clear / 再 link 抑止を検証すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 👀 確認のみ

**想定工数**: —

**判断理由**: RC-9 / RC-2 で `startPendingProviderLink` / `handleRedirect` 計 6 テストと `operationType: 'link'` モックを追加済み。

**対応内容**: RC-9 / RC-2 にて対応済み。再レビューで解消を確認。

---

**識別子**: RC-20（GitHub id: なし・エージェント再レビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/register/index.vue:30`

**該当コード（レビュー時点の diff）**:

（本 PR 差分外・現行コード）

```typescript
  return route.query.pid1 == null || route.query.pid2 == null
    ? null
    : {
        tryRegisterProviderId: route.query.pid1 as ProviderIdType,
        linkProviderId: route.query.pid2 as ProviderIdType,
      }
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `register/index.vue` の pid 検証が `as ProviderIdType` のまま。`login.vue` と同様に `isProviderIdType` で検証すると一貫する。本 PR #2184 のスコープ外。

**コメント要約**: 自動連携の register 入口は `/register` から credential 衝突時に進まない設計（§4.4）のため実害は低いが、規約上 `login.vue` と非統一。

**判断結果**: 👌 修正不要

**PRスコープ**: 📤 スコープ外

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: #2184 はメール OTP 経路の自動連携修正が中心。register 画面の pid 検証統一は別 PR / Issue で十分。現状 `/register` 起点は自動連携に進まないためマージブロッカーではない。

**対応内容**: 本 PR では対応しない。必要なら別 Issue 化。

---

## 評価セッション（2026-07-16 16:14 JST・wait-ai-pr-review wake / RC 対応）

- **評価日時**: 2026-07-16 16:14 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / wait-ai-pr-review wake → RC 対応）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **REVIEW_REQUEST_SINCE**: 2026-07-16T06:59:10Z
- **partial**: false
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 1（Codex 接続案内）
- **備考**: Copilot / Codex から新規 🟡 4 件。🚨 必須修正なし。本セッションで RC-21〜24 をコード対応。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-21 | 3593297500 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `login.vue` で query pid1/pid2 を未正規化のまま渡す<br>型エラーの恐れ |
| [x] | RC-22 | 3593323035 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | M | ログイン済み `/pass-code` リロードで OTP 画面が消える<br>メール変更フローのリグレッション |
| [x] | RC-23 | 3593323041 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | M | stale pending と login の pid1 不一致時に保持<br>意図しない SNS link の恐れ |
| [x] | RC-24 | 3593323047 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 連携開始失敗後 pending 復元のまま通常遷移<br>後続 handleRedirect で意図しない link |

---

**識別子**: RC-21（GitHub id: 3593297500）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/login.vue:60`

**該当コード（レビュー時点の diff）**:

```diff
+    clearStalePendingLinkRequestOutsideAutoLinkage({
+      loginPid1: route.query.pid1,
+      loginPid2: route.query.pid2,
+    })
```

**レビュワーのコメント（原文）**:

`clearStalePendingLinkRequestOutsideAutoLinkage` の引数が `string | null` 想定なのに対し、`route.query.pid1/pid2` は Vue Router の型上 `string | (string|null)[] | null | undefined` になり得るため、このままだと TypeScript の型エラーになります（ビルドを落とす可能性があります）。`typeof === 'string'` で正規化してから渡すのが安全です。

**コメント要約**: `login.vue`: query pid1/pid2 を `typeof === 'string'` で正規化してから `clearStalePendingLinkRequestOutsideAutoLinkage` に渡す。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: Vue Router の query 型と helper の引数型が不一致。build:types で落ちうるため指摘は妥当。

**対応内容**: `typeof pid1/pid2 === 'string'` で正規化してから helper に渡す。

---

**識別子**: RC-22（GitHub id: 3593323035）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `user/src/router/index.ts:163`

**該当コード（レビュー時点の diff）**:

```diff
+      if (user == null) {
+        if (to.path === '/pass-code') {
+          return undefined
+        }
+        return getRedirectPath() ?? undefined
+      }
+
+      if (to.path === '/login' && userCredential != null) {
```

**レビュワーのコメント（原文）**:

**/pass-code の通常OTP表示を維持する**

ログイン済みユーザーがメールアドレス変更の確認中に `/pass-code` をリロードした場合、OAuth の戻りではないため `handleRedirect(user)` は `null` を返しますが、この追加により初回ガードがそのまま `updateProfileFromProviders(null)` 以降へ進み、`/` や `/register/complete` へリダイレクトしてしまい、パスコード入力画面が表示されません。`/pass-code` では `userCredential == null` の通常OTP/メール変更フローをガード処理から抜けるようにしてください。

**コメント要約**: `router/index.ts`: ログイン済みメール変更中の `/pass-code` リロードで OTP 画面が消える（RC-14 未ログイン対応の副作用）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害, 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: 未ログイン `/pass-code` 保護（RC-14）と OAuth 復帰ガードの境界が曖昧。ログイン済み + `userCredential == null` は通常 OTP/メール変更であり、初回ガードをスキップすべき。

**対応内容**: `user != null && to.path === '/pass-code' && userCredential == null` のとき `return undefined` でガード処理を抜ける。

---

**識別子**: RC-23（GitHub id: 3593323041）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `base/src/utils/redirect.ts:43`

**該当コード（レビュー時点の diff）**:

```diff
+  const inPassCodeAutoLink = context.passCodePid != null && isProviderIdType(context.passCodePid)
+  const inLoginAutoLink =
+    context.loginPid1 != null &&
+    context.loginPid2 != null &&
+    isProviderIdType(context.loginPid1) &&
+    isProviderIdType(context.loginPid2)
+  if (!inPassCodeAutoLink && !inLoginAutoLink && peekPendingLinkRequest() != null) {
+    clearPendingLinkRequest()
+  }
```

**レビュワーのコメント（原文）**:

**stale pending と pid1 の不一致を破棄する**

自動連携を途中離脱して古い `pendingLinkRequestProviderId` が残った状態で、別プロバイダ組み合わせの `/login?pid1=...&pid2=...` に到達すると、この判定は query が有効なだけで pending を保持します。その後 pid2 側でログインすると `handleRedirect()` が query の pid1 ではなく古い pending を消費して、ユーザーが今回連携しようとしていない SNS の `linkWithRedirect` を開始し得るため、login 自動連携中も pending が `loginPid1` と一致しない場合は破棄してください。

**コメント要約**: `redirect.ts`: stale pending が `pid1` と不一致なら破棄（RC-12 の延長）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: M

**判断理由**: query 有効だけで pending を無条件保持すると、別組み合わせの自動連携で古い pending が消費される。pass-code 側も pending === pid の一致を要求すべき。

**対応内容**: `inPassCodeAutoLink` / `inLoginAutoLink` に `pending === context.passCodePid` / `pending === context.loginPid1` を追加。不一致テスト 2 件を追加。

---

**識別子**: RC-24（GitHub id: 3593323047）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `user/src/pages/pass-code.vue:173`

**該当コード（レビュー時点の diff）**:

```diff
+  } catch (error: unknown) {
+    clearLinkageCompletedToast()
+    if (error instanceof FirebaseError) {
+      notification.show($t('profile.linkage_failed', { snsName }), 'error')
+    } else {
+      notification.show($t('profile.linkage_failed_generic'), 'error')
+    }
+    return false
+  }
```

**レビュワーのコメント（原文）**:

**連携開始失敗後の pending を残さない**

`linkByProviderService` がリダイレクト開始前に拒否した場合や DEV の popup が失敗した場合、`startPendingProviderLink()` は pending を復元してから例外を投げますが、この catch は `false` を返すため呼び出し元が通常ログイン成功として `redirectPath` へ進みます。そのまま古い pending が残ると、後で `/profile` など初回 `handleRedirect()` 対象ルートを開いたときに意図しない SNS 連携が再開されるので、失敗を終端扱いするなら pending も破棄するか、この画面に留めて再試行させてください。

**コメント要約**: `pass-code.vue`: link 失敗後 pending 復元のまま通常遷移しない。失敗終端なら pending も破棄。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 連携開始失敗を通常ログイン成功扱いにすると stale pending が残り、後続 `handleRedirect` で意図しない link が走る。失敗時は pending を破棄して終端とするのが妥当。

**対応内容**: `completePendingLinkAfterEmailLogin` の catch で `clearPendingLinkRequest()` を追加（`clearLinkageCompletedToast` と併用）。

---

## 評価セッション（2026-07-16 16:22 JST・shokujii-code-review）

- **評価日時**: 2026-07-16 16:22 JST
- **評価者**: Cursor Agent（`shokujii-code-review` / RC-21〜24 追修正分）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし
- **備考**: RC-21〜24 対応後の再レビュー。🚨 0 件。🟡 3 件を本セッションで対応。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-25 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📄 ドキュメントのみ | S | §6.2 が RC-23 の pending 一致条件を未記載<br>仕様と実装の乖離 |
| [x] | RC-26 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 👤 UX | 🔧 微修正 | S | 連携失敗後も redirectPath へ遷移<br>エラー通知を見失う恐れ |
| [x] | RC-27 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | S | `login.vue` で query pid 正規化が重複<br>`loginQueryPids` に集約 |

---

**識別子**: RC-25（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/03_参加者獲得/17_SNSアカウント自動連携.md:384`

**該当コード（レビュー時点の diff）**:

```diff
-5. 自動連携フロー外（`/login` に pid1/pid2 なし、または `/pass-code` に pid なし）では `clearStalePendingLinkRequestOutsideAutoLinkage` で stale pending を破棄する（login.vue / pass-code.vue）
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📄ドキュメントのみ/S]: 仕様書 §6.2 項 5 は「pid1/pid2 **あり**なら自動連携フロー内」と読めるが、RC-23 以降は **pending が pid1（または pass-code の pid）と一致するときのみ**保持する。項 5 に「query が有効でも pending が pid1/pid と不一致なら破棄」を追記すると実装と揃う。

**コメント要約**: §6.2 と RC-23 の pending 一致条件の乖離を仕様書に反映する。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📄 ドキュメントのみ

**想定工数**: S

**判断理由**: RC-23 実装後、項 4/5 の説明だけでは「query 有効＝pending 保持」と誤読しうる。仕様正本の更新は妥当。

**対応内容**: §6.2 項 4/5 を整理し、login / pass-code それぞれで pending と query pid の一致が必要な条件を明記。

---

**識別子**: RC-26（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/pass-code.vue:197`

**該当コード（レビュー時点の diff）**:

```diff
+      const linked = await completePendingLinkAfterEmailLogin()
+      if (linked) {
+        return
+      }
+      const redirectPath = getRedirectPath() ?? '/'
+      await router.push(redirectPath)
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [👤 UX/🔧微修正/S]: RC-24 で pending は catch 内で破棄されるが、`return false` 後 `submit()` が通常ログイン成功扱いで `redirectPath` へ遷移する。エラートースト直後に画面が変わり、ユーザーが失敗に気づきにくい。失敗時は `/pass-code` に留めるべき。

**コメント要約**: 連携開始失敗後も redirectPath へ遷移する UX 問題（RC-24 の pending 破棄に加え、画面遷移も止める）。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: pending 破棄だけでは Codex 指摘の「失敗終端」の UX が不十分。連携失敗時に OTP 画面に留めるのが自然。

**対応内容**: `completePendingLinkAfterEmailLogin` の戻り値を `'linked' | 'skipped' | 'failed'` に変更。`submit()` は `'failed'` 時に遷移せず return。

---

**識別子**: RC-27（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/login.vue:28`

**該当コード（レビュー時点の diff）**:

```diff
+const linkRequestDialogParams = computed(() => {
+  const pid1 = route.query.pid1
+  const pid2 = route.query.pid2
+  if (typeof pid1 !== 'string' || typeof pid2 !== 'string') {
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📐リファクタ/S]: `linkRequestDialogParams` と `onMounted` else 分支で query pid の `typeof === 'string'` 正規化が重複。共有 computed にまとめられる。

**コメント要約**: `login.vue` の pid 正規化を `loginQueryPids` computed に集約。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: RC-21 対応で onMounted 側に正規化が増え、linkRequestDialogParams と二重化。DRY 化は低リスク。

**対応内容**: `loginQueryPids` computed を追加し、dialog 判定と stale pending 破棄の両方から参照。

---

## 評価セッション（2026-07-16 16:37 JST・wait-ai-pr-review / manual evaluate）

- **評価日時**: 2026-07-16 16:37 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` auto / manual）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **REVIEW_REQUEST_SINCE**: 2026-07-16T07:27:21Z
- **partial**: false
- **Outdated 除外件数**: 0（新規評価対象のみ）
- **レビュー非該当スキップ件数**: 1（レビュー依頼定型文 id:4989289449）
- **備考**: Copilot トップレベルは 🚨 新規なし。Codex substantive 1 件 + Copilot inline 1 件 + Copilot トップレベル 🟡 1 件。

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-28 | 4989364250 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | S | `pass-code.vue` 内側 if がデッドコード<br>`clearStale` 後は到達不能 |
| [x] | RC-29 | 3593467741 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害 | 🔧 微修正 | S | 自動連携 dialog 表示時も stale pending 未検証<br>RC-23 の else 分岐のみ対応の漏れ |
| [x] | RC-30 | 3593449346 | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 📐 リファクタ | S | `router/index.ts` の i18n `@ts-expect-error`<br>`routerTranslate` ヘルパーで置換 |
| [x] | RC-31 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `login.vue` onMounted で `runLoginPageMountAutoLinkage` 戻り値未使用<br>computed 再評価の二重パース |
| [x] | RC-32 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `pass-code.vue` mount / OTP 後連携のテスト不足<br>`passCodeAutoLinkage.ts` 抽出 + vitest |
| [x] | RC-33 | 3593638542 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | S | 本番で `startPendingProviderLink` が null でも `'linked'` を返す<br>`/pass-code` に留まる恐れ。null 時は `skipped` |
| [x] | RC-34 | 4989564237 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | `login.vue` ConfirmDialog の `v-if` と `:model-value` 二重<br>ロジック誤りではないが冗長 |

---

**識別子**: RC-28（GitHub id: 4989364250）

**レビュワー**: Copilot（copilot-swe-agent）

**指摘箇所**: `user/src/pages/pass-code.vue:133-138`（PR トップレベル）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし — 現行ブランチの該当箇所）

```typescript
  if (pendingId == null || pendingId !== linkProviderId.value) {
    if (pendingId != null && pendingId !== linkProviderId.value) {
      clearPendingLinkRequest()
    }
    return 'skipped'
  }
```

**レビュワーのコメント（原文）**:

Files changed を再レビューしました。

### 🚨 必須修正

新規なし。前回までの [must] 指摘（`operationType === 'link'` ガード、`clearPendingLinkRequest` import、`loginQueryPids` 型正規化）はすべて対応済みを確認しました。

### ✅ 前回 🟡 提案 → 対応済み

**`user/src/pages/pass-code.vue:133-138`**

`completePendingLinkAfterEmailLogin` 内の内側 `if` ブロックがデッドコードであることを確認し、除去しました。

`clearStalePendingLinkRequestOutsideAutoLinkage({ passCodePid: linkProviderId.value })` 呼び出し後、`pendingId` が取り得る状態は `null` または `=== linkProviderId.value` のみです。そのため `pendingId != null && pendingId !== linkProviderId.value` が `true` になることはなく、内側 `if` は不要でした。

**コメント要約**: `pass-code.vue` の `completePendingLinkAfterEmailLogin` で、RC-23 の `clearStale` 呼び出し後に内側 `if (pendingId != null && pendingId !== linkProviderId.value)` は到達不能なデッドコード。除去すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: `clearStalePendingLinkRequestOutsideAutoLinkage` が不一致 pending を先に破棄するため、後続の内側 if は論理上到達不能。Copilot ローカル修正の指摘は妥当。**現行ブランチには未反映**（Copilot コメント内の `146cff46` はリモート未マージ）。

**対応内容**: 内側 `if (pendingId != null && pendingId !== linkProviderId.value)` を削除。外側の early return のみに整理。

---

**識別子**: RC-29（GitHub id: 3593467741）

**レビュワー**: chatgpt-codex-connector[bot]

**指摘箇所**: `user/src/pages/login.vue:55`

**該当コード（レビュー時点の diff）**:

```diff
+onMounted(() => {
+  const params = linkRequestDialogParams.value
+  if (params != null) {
```

**レビュワーのコメント（原文）**:

**有効な自動連携URLでも stale pending を検証してください**

有効な `/login?pid1=...&pid2=...` でこの分岐に入ると `clearStalePendingLinkRequestOutsideAutoLinkage` が呼ばれないため、別フローで残った `pendingLinkRequestProviderId` が現在の `pid1` と不一致でも保持されます。その状態でユーザーが `pid2` 側でログインすると、`handleRedirect` は残っていた pending をそのまま消費して `linkByProviderService` を開始するので、現在のダイアログとは別の SNS を連携しようとする誤動作になります。通知表示の有無に関係なく、`pid1/pid2` と pending の一致確認は常に実行する必要があります。

**コメント要約**: `login.vue`: 自動連携 dialog 表示時（`params != null`）も `clearStalePendingLinkRequestOutsideAutoLinkage` を呼ぶ。RC-23 は else 分岐のみで漏れあり。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: RC-23/27 で else 分岐に stale 破棄を追加したが、有効 pid1/pid2 で通知を出す分岐では helper が呼ばれない。Codex 指摘は RC-12/23 と同系統で妥当。

**対応内容**: `onMounted` 冒頭で通知の有無に関わらず `clearStalePendingLinkRequestOutsideAutoLinkage({ loginPid1, loginPid2 })` を実行。

---

**識別子**: RC-30（GitHub id: 3593449346）

**レビュワー**: Copilot

**指摘箇所**: `user/src/router/index.ts:71`

**該当コード（レビュー時点の diff）**:

```diff
+  const setLinkageCompletedPendingToast = (providerId: string): void => {
+    const i18n = getI18n()
+    setPendingToast(
+      // @ts-expect-error i18n.global.t の型がユニオンになってしまう TODO 直し方確認
+      i18n.global.t('profile.linkage_completed', {
+        // @ts-expect-error i18n.global.t の型がユニオンになってしまう
+        snsName: i18n.global.t(`sns_name['${providerId}']`),
+      }),
+      'success',
+    )
+  }
```

**レビュワーのコメント（原文）**:

[imo] `@ts-expect-error` で `i18n.global.t` の型エラーを抑止しているため、意図しない型エラーの見落としにつながりやすいです。ここは一度だけ `t` を型キャストしたヘルパーに寄せて `@ts-expect-error` を撤去すると、抑止範囲を最小化できます。

**コメント要約**: `router/index.ts` の `setLinkageCompletedPendingToast` で i18n `@ts-expect-error` を型付きヘルパーに置き換える提案。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: 指摘自体は妥当。`setLinkageCompletedPendingToast` に限定して `routerTranslate` ヘルパーへ集約すれば `@ts-expect-error` を除去でき、本 PR で追加した関数の品質改善として対応可能。

**対応内容**: `routerTranslate()` を module スコープに追加し、`setLinkageCompletedPendingToast` から `@ts-expect-error` を撤去。

---

## 評価セッション（2026-07-16 16:48 JST・shokujii-code-review 追随）

- **評価日時**: 2026-07-16 16:48 JST
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **Outdated**: 該当なし
- **レビュー非該当**: 該当なし

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-31 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `login.vue` onMounted で戻り値未使用 |
| [x] | RC-32 | なし・エージェントレビュー | ✅ 対応済み | 📌 スコープ内 | 📏 規約 | 🔧 微修正 | S | `passCodeAutoLinkage.ts` 抽出 + vitest |

---

**識別子**: RC-31（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/login.vue:32`

**レビュワーのコメント（原文）**:

🟡 **修正提案** [🔧微修正/S]: `login.vue` の `onMounted` で `runLoginPageMountAutoLinkage` の戻り値を未使用

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: 戻り値と computed が同内容を二重評価しており DRY 違反。動作差はないが修正コストが低い。

**対応内容**: `onMounted` で `const params = runLoginPageMountAutoLinkage(...)` の戻り値を通知表示に使用。

---

**識別子**: RC-32（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/pages/pass-code.vue` / `user/src/utils/passCodeAutoLinkage.ts`

**レビュワーのコメント（原文）**:

🟡 **修正提案** [📏規約/S]: `pass-code.vue` mount / OTP 後連携経路のテスト未追加

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 📏 規約

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: login 側と同様、判定ロジックの純関数化と vitest 追加がテスト方針に合致。

**対応内容**: `passCodeAutoLinkage.ts` に mount / OTP 後 pre-check を抽出。`passCodeAutoLinkage.test.ts`（10 件）を追加。

---

## 評価セッション（2026-07-16 17:06 JST・review-comments-evaluate manual）

- **評価日時**: 2026-07-16 17:06 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate` manual）
- **ブランチ名**: fix/2184
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2185
- **REVIEW_REQUEST_SINCE**: 2026-07-16T07:58:08Z
- **partial**: true（Codex 接続案内のみ。substantive レビューなし）
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 id:4989523352、Codex 接続案内 id:4989565266）

### RC 一覧（サマリ）

| 対応 | RC | GitHub id | 判断 | PRスコープ | ラベル | 種別 | 工数 | 要約 |
|:----:|:---|:---|:---|:---|:---|:---|:---|:---|
| [x] | RC-33 | 3593638542 | ✅ 対応済み | 📌 スコープ内 | 🐛 実害, 👤 UX | 🔧 微修正 | S | 本番で `startPendingProviderLink` が null でも `'linked'` を返す<br>`/pass-code` に留まる恐れ。null 時は `skipped` |
| [x] | RC-34 | 4989564237 | ✅ 対応済み | 📌 スコープ内 | — | 📐 リファクタ | S | `login.vue` ConfirmDialog の `v-if` と `:model-value` 二重<br>ロジック誤りではないが冗長 |

---

**識別子**: RC-33（GitHub id: 3593638542）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/pass-code.vue:158`

**該当コード（レビュー時点の diff）**:

```diff
+const completePendingLinkAfterEmailLogin = async (): Promise<'linked' | 'skipped' | 'failed'> => {
+  ...
+    if (import.meta.env.DEV) {
+      if (userCredential == null) {
+        return 'skipped'
+      }
+      ...
+      return 'linked'
+    }
+    return 'linked'
```

**レビュワーのコメント（原文）**:

[must] 本番分岐（import.meta.env.DEV 以外）で `startPendingProviderLink()` が `null` を返しても常に `'linked'` を返してしまいます。pending が消えている等で `linkWithRedirect` が開始されなかった場合でも `submit()` 側が通常遷移を止めてしまい、`/pass-code` に留まり続ける可能性があります。`userCredential == null` の場合は `skipped` 扱いで通常の `redirectPath` 遷移に戻すのが安全です。

**コメント要約**: 本番分岐で `startPendingProviderLink` が `null` を返した場合も `'linked'` 固定のため、`submit()` が通常の `redirectPath` 遷移をスキップし `/pass-code` に留まる恐れがある。
DEV 分岐と同様、`userCredential == null` なら `'skipped'` を返すべき。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: 🐛 実害, 👤 UX

**変更種別**: 🔧 微修正

**想定工数**: S

**判断理由**: `preCheck` 通過後でも `startPendingProviderLink` は pending 無効時に `null` を返す（`redirect.ts:101-102`）。本番で `'linked'` を返すと `submit()` が早期 return しログイン後遷移が止まる。Copilot 指摘は妥当。

**対応内容**: DEV/本番共通で `userCredential == null` のとき `'skipped'` を返すよう変更。通常 `redirectPath` 遷移にフォールバック。

---

**識別子**: RC-34（GitHub id: 4989564237・トップレベル内 🟡 節）

**レビュワー**: Copilot

**指摘箇所**: `user/src/pages/login.vue:207`

**該当コード（レビュー時点の diff）**:

```diff
+  <confirm-dialog
+    v-if="linkRequestDialogParams !== null"
+    :model-value="linkRequestDialogParams !== null"
```

**レビュワーのコメント（原文）**:

**`user/src/pages/login.vue:207`**

`v-if` が真のとき `:model-value` は常に `true` と等価になり、二重チェックになっています。`v-if` のみで制御すれば意図が明確になります。ロジック上の誤りではありません。

**コメント要約**: ConfirmDialog で `v-if` と `:model-value` が同条件の二重指定になっており冗長。
`v-if` のみにすれば意図が明確。挙動上のバグではない。

**判断結果**: ✅ 対応済み

**PRスコープ**: 📌 スコープ内

**ラベル**: —

**変更種別**: 📐 リファクタ

**想定工数**: S

**判断理由**: 指摘どおり `v-if` 真のとき `:model-value` は常に true 相当。可読性改善のみでマージブロッカーではない。

**対応内容**: `:model-value="true"` に変更（`v-if` で表示制御、`ConfirmDialog` の必須 prop は維持）。

---
