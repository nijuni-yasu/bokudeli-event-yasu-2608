# PR #2040 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-25 15:30 JST
- **ブランチ名**: feat/1060
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2040
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 1 件、Codex 接続案内ボイラープレート 1 件）

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: 4532013531）— ✅ 対応済み（`clientErrorReport.ts` logPayload に `stack` 追加）
- [x] **RC-2**（GitHub id: 4532013531）— ✅ 対応済み（`community.ts` subscribeAlbumItems）
- [x] **RC-3**（GitHub id: 4532013531）— ✅ 対応済み（`user_id` max(128)）
- [x] **RC-4**（GitHub id: 4532013531）— 👌 修正不要（`fingerprint.length(64)` は任意改善）
- [x] **RC-5**（GitHub id: 4532013531）— 👌 修正不要（`unhandledrejection` severity: error は仕様通り）
- [x] **RC-6**（GitHub id: 4532013531）— 👌 修正不要（mount 後 setup は imo・実害小）
- [x] **RC-7**（GitHub id: 4532013531）— 👌 修正不要（テスト重複は nits）
- [x] **RC-8**（GitHub id: 3296398586）— ✅ 対応済み（`error_type` slice(0,100)）
- [x] **RC-9**（GitHub id: 3296385508）— ✅ 対応済み（`configureClientErrorReporting`）
- [x] **RC-10**（GitHub id: 3296398627）— ✅ 対応済み（サーバー fingerprint 再計算）
- [x] **RC-11**（GitHub id: 3296398679）— 👌 修正不要（テストファイル名リネームは nits）
- [x] **RC-12**（GitHub id: 3296398612）— ✅ 対応済み（dedup cache クリーンアップ）
- [x] **RC-13**（GitHub id: 3296385511）— ✅ 対応済み（RC-12 と同一論点）
- [x] **RC-14**（GitHub id: 3296398661）— 👌 修正不要（RC-6 と同一論点）
- [x] **RC-15**（GitHub id: 3296398646）— 👌 修正不要（RC-6 と同一論点）
- [x] **RC-16**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-17**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-18**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-19**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-20**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-21**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-22**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-23**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-24**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-25**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-26**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-27**（GitHub id: 3296677745）— 👌 修正不要
- [x] **RC-28**（GitHub id: 3296451106）— ✅ 対応済み
- [x] **RC-29**（GitHub id: 3296677701）— 👌 修正不要
- [x] **RC-30**（GitHub id: 3297236446）— 👌 修正不要
- [x] **RC-31**（GitHub id: 3297293905）— 👌 修正不要
- [x] **RC-32**（GitHub id: 3296451112）— ✅ 対応済み
- [x] **RC-33**（GitHub id: 3296451115）— ✅ 対応済み
- [x] **RC-34**（GitHub id: 3297293862）— ✅ 対応済み
- [x] **RC-35**（GitHub id: 3296436679）— 👌 修正不要
- [x] **RC-36**（GitHub id: 3296451108）— ✅ 対応済み
- [x] **RC-37**（GitHub id: 3296677686）— 👌 修正不要
- [x] **RC-38**（GitHub id: 3297025659）— 👌 修正不要
- [x] **RC-39**（GitHub id: 3296451117）— ✅ 対応済み
- [x] **RC-40**（GitHub id: 3297293931）— 👌 修正不要
- [x] **RC-41**（GitHub id: 3296451102）— ✅ 対応済み
- [x] **RC-42**（GitHub id: 3296436675）— 👌 修正不要
- [x] **RC-43**（GitHub id: 3297128076）— 👌 修正不要
- [x] **RC-44**（GitHub id: 3296677650）— 👌 修正不要
- [x] **RC-45**（GitHub id: 3296451118）— ✅ 対応済み
- [x] **RC-46**（GitHub id: 3296436673）— 👌 修正不要
- [x] **RC-47**（GitHub id: 3296668579）— 👌 修正不要
- [x] **RC-48**（GitHub id: 3297128081）— 👌 修正不要
- [x] **RC-49**（GitHub id: 3297285287）— 👌 修正不要
- [x] **RC-50**（GitHub id: 3296677722）— 👌 修正不要
- [x] **RC-51**（GitHub id: 4533419952）— 👌 修正不要（configureClientErrorReporting 二重呼び出し nits）
- [x] **RC-52**（GitHub id: 4533419952）— 👌 修正不要（getLoadedEvent 並行 ask）
- [x] **RC-53**（GitHub id: 4533419952）— 👌 修正不要（Callable 濫用 Phase 2 明記済み）
- [x] **RC-54**（GitHub id: 4533419952）— 👌 修正不要（clientError.test 重複 nits）
- [x] **RC-55**（GitHub id: 3297461109）— ✅ 対応済み（ZodError のみ null、それ以外は再送出）
- [x] **RC-56**（GitHub id: 3297472599）— 👌 修正不要（カート非 ZodError の warn 報告 imo）
- [x] **RC-57**（GitHub id: 3297472553）— 👌 修正不要（getLoadedEvent 単一スロット must）
- [x] **RC-58**（GitHub id: 3297472581）— 👌 修正不要（Callable 認証任意・dedup のみ Phase 1）

---

**識別子**: RC-1（GitHub id: 4532013531・トップレベル内指摘 1/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[must] `functions/default/src/clientErrorReport.ts` L27–38 — `stack` が `logPayload` に含まれていない**

```ts
const logPayload = {
  app: data.app,
  error_type: data.error_type,
  error_message: data.message,
  // stack がない！
  route: data.route,
  ...
}
```

`ClientErrorReportRequest` に `stack` フィールドが存在し、クライアントから送信されているにも関わらず、Cloud Logging に記録されません。デバッグ時にスタックトレースが参照できなくなるため、`logPayload` に `stack: data.stack` を追加してください。
```

**コメント要約**: Callable 側の `logPayload` に `stack` が含まれておらず、クライアントから送ったスタックトレースが Cloud Logging に残らない。

**判断結果**: 🚨 修正推奨

**判断理由**: 実装を確認すると `reportClientError.ts` は `stack` を送信しているが、`clientErrorReport.ts` の `logPayload` に未収録。520 調査の初動でスタックが必要なため、明確な実装漏れ。低コストで修正すべき。

---

**識別子**: RC-2（GitHub id: 4532013531・トップレベル内指摘 2/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[must] `base/src/stores/community.ts` L314–320 — `subscribeAlbumItems` のエラー catch パターンが他のstoreと不一致**

```ts
try {
  _albumItems.value = snapshot.docs.map((d) => d.data())
} catch (err) {
  reportClientError(err, { severity: 'warn' })  // ← documentPath なし
  _albumItems.value = []
}
```

`snapshot.docs.map()` で1件でも Zod エラーが起きると全件が `[]` になります。`eventList` / `communityList` / `partner` では `flatMap` + per-doc try-catch で失敗したドキュメントのみをスキップしており、設計が不一致です。また `documentPath` が渡されていないためどのドキュメントで失敗したか Cloud Logging から特定できません。

```ts
// 修正案
_albumItems.value = snapshot.docs.flatMap((d) => {
  try {
    return [d.data()]
  } catch (err) {
    console.error(err)
    reportClientError(err, { documentPath: d.ref.path, severity: 'warn' })
    return []
  }
})
```
```

**コメント要約**: `subscribeAlbumItems` が外側 try-catch + `map` のため1件失敗で全件欠落。`documentPath` も未付与で他 store と不一致。

**判断結果**: 🚨 修正推奨

**判断理由**: 同一 PR で追加した `eventList` / `partner` 等は per-doc `flatMap` + `documentPath` パターン。`community.ts` L516 の members 購読は `documentPath` 付き。album のみ旧パターンのまま残っており、Zod 失敗時の調査可能性・データ欠落範囲の両面で不整合。修正推奨。

---

**識別子**: RC-3（GitHub id: 4532013531・トップレベル内指摘 3/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[imo] `common/src/apis/clientError.ts` L20 — `user_id` に max 長の制限がない**

```ts
user_id: z.string().optional(),
```

他のフィールドはすべて `.max()` で上限を設けているのに `user_id` だけ未制限です。Firebase Auth UID は 28 文字程度ですが、認証不要 Callable のため不正な長い文字列が送れます。`.max(128)` 程度を追加してください。
```

**コメント要約**: 認証不要 Callable の payload で `user_id` に長さ上限がなく、他フィールドと防御が非対称。

**判断結果**: 🚨 修正推奨

**判断理由**: Phase 1 の防御は strict Zod バリデーション + dedup（仕様書 5.5.3）。他フィールドは max 制限あり。UID は通常 28 文字だが `.max(128)` 追加は低コストでスパム・ oversized payload 対策として妥当。

---

**識別子**: RC-4（GitHub id: 4532013531・トップレベル内指摘 4/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `common/src/apis/clientError.ts` L22 — `fingerprint` は `length(64)` が正確**

```ts
fingerprint: z.string().max(64),
```

SHA-256 hex は常に固定 64 文字です。`max(64)` では 1〜63 文字の不正な値も通過します。`z.string().length(64)` にすると意図が明確になります。
```

**コメント要約**: fingerprint は SHA-256 hex 固定 64 文字のため `max(64)` より `length(64)` が正確。

**判断結果**: 👌 修正不要

**判断理由**: 指摘は技術的に正しいが、クライアント側 `computeFingerprint` は常に 64 文字 hex を生成しており、短い fingerprint が通過しても dedup やログの実害は限定的。Phase 1 必須修正ではなく、入れるなら任意改善（nits）として Phase 2 でも可。

---

**識別子**: RC-5（GitHub id: 4532013531・トップレベル内指摘 5/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[imo] `base/src/utils/setupGlobalErrorHandling.ts` L23–27 — `unhandledrejection` の `severity: 'error'` は意図的か確認**

```ts
window.addEventListener('unhandledrejection', (event) => {
  reportClientError(event.reason, {
    severity: 'error',  // ← store の onSnapshot エラーは warn
  })
})
```

store の onSnapshot エラーはすべて `severity: 'warn'` で送信されています。`unhandledrejection` を `error` にすると 520 遷移しない（仕様通り）にもかかわらず `error` severity が記録され、アラートの閾値設定が紛らわしくなる可能性があります。仕様書の severity 設計と照らし合わせて `warn` への変更を検討してください。
```

**コメント要約**: `unhandledrejection` を `error` にすると store warn と混在し、アラート設計が紛らわしくなるのでは、との確認。

**判断結果**: 👌 修正不要

**判断理由**: 仕様書 5.2 表で `unhandledrejection` は **severity: error** と明記。520 に至らない未捕捉 Promise 拒否は store のサイレント失敗（warn）より深刻度が高く、Phase 1 の Log-based Alert（`severity>=ERROR`）対象とする設計意図と一致。warn に下げると検知漏れになる。

---

**識別子**: RC-6（GitHub id: 4532013531・トップレベル内指摘 6/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `user/src/main.ts` / `admin/src/main.ts` L35–38 — `setupGlobalErrorHandling` の設定が mount 後の非同期になっている**

```ts
app.mount('#app')

import('@shokujii/base/utils/setupGlobalErrorHandling.js').then(({ setupGlobalErrorHandling }) => {
  import('@shokujii/base/plugins/router/index.js').then(({ router }) => {
    setupGlobalErrorHandling(app, router, { app: 'user' })
  })
})
```

`app.mount('#app')` の直後から `setupGlobalErrorHandling` の設定完了までの間（非同期 import の解決中）に Vue が errorHandler なしの状態になります。動的 import のチェーンをなくし、`Promise.all` のチェーン内で `router` と一緒にセットアップするか、`router` を静的 import に変えて `mount` 前に設定することを検討してください。
```

**コメント要約**: mount 後の dynamic import により、短時間 errorHandler 未設定の窓が存在する。

**判断結果**: 👌 修正不要

**判断理由**: 指摘は理論上正しい（imo/nits）。ただし窓は import 解決の数十 ms 程度で、App は `defineAsyncComponent` のため mount 自体も非同期。既存 main.ts は plugin 適用後 mount のパターンを踏襲。Phase 1 必須修正ではなく、改善するなら RC-14/15 とまとめてリファクタ可能。

---

**識別子**: RC-7（GitHub id: 4532013531・トップレベル内指摘 7/7）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `common/src/apis/clientError.test.ts` L13–20 — 最初の2テストケースが重複**

```ts
it('必須フィールドが揃っていれば parse できる', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 次のテストと同じアサーション
})

it('severity のデフォルトは error', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')
})
```

2件目と全く同じアサーションです。1件目は「parse が成功して result が返る」ことの確認に留め、例えば `expect(result.app).toBe('user')` のような別観点のアサーションに変えると意図が明確になります。
```

**コメント要約**: 最初の2テストが同一アサーションで冗長。

**判断結果**: 👌 修正不要

**判断理由**: テスト品質の nits。機能上の問題はなく、1件目を `expect(result.app).toBe('user')` 等に変えるのは任意。Phase 1 マージブロッカーではない。

---

**識別子**: RC-8（GitHub id: 3296398586）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `error_type` に `err.constructor.name` をそのまま入れると、共通スキーマ側の `error_type: z.string().max(100)` を超えるクラス名で Callable 送信が失敗する可能性があります（結果としてエラー報告自体が落ちます）。`error_type` も max 長に合わせて切り詰めるなど、送信前にスキーマ制約を満たすように整形してください。

This issue also appears on line 67 of the same file.
```

**コメント要約**: `serializeError` が `error_type` を切り詰めず、100 文字超のクラス名で Callable バリデーション失敗→報告自体が失敗しうる。

**判断結果**: 🚨 修正推奨

**判断理由**: `message` / `stack` は slice 済みだが `error_type` のみ未制限。報告経路の信頼性を損なうため、`error_type.slice(0, 100)` 等で送信前整形すべき。RC-1 と同様、エラー報告機能の根幹に関わる。

---

**識別子**: RC-9（GitHub id: 3296385508）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  管理画面のエラー送信で app を誤分類しない**

`reportClientError` の既定値が `app: 'user'` になっているため、`base` の store から `app` 未指定で呼ばれる warn レポートは、admin 側で発生していても user として記録されます。今回の変更で admin は `useEventStore`/`useCommunityStore`/`usePartnerStore` など `base` store を直接使っているため、運用時の切り分け（app 別の監視・調査）と fingerprint の重複判定が誤る実害が出ます。`app` を必須にするか、起動時に現在アプリ種別を注入して store 経由レポートにも必ず反映してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: store からの `reportClientError` が `app` 未指定のため admin でも `user` として記録される。

**判断結果**: 🚨 修正推奨

**判断理由**: 全 store hook が `{ documentPath, severity: 'warn' }` のみで `app` 省略。`reportClientError.ts` L67 で `context.app ?? 'user'`。admin 運用時の Logs Explorer フィルタ（`jsonPayload.app="admin"`）と fingerprint dedup が誤る実害あり。`setupGlobalErrorHandling` 時にモジュールスコープへ app 注入、または store 呼び出しに app 必須化が必要。Phase 1 仕様書にも Phase 2 候補として `app: admin` 明示が記載されており、早期対応が妥当。

---

**識別子**: RC-10（GitHub id: 3296398627）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] dedup 判定に `data.fingerprint`（クライアント任意値）をそのまま使っているため、Callable を直接叩かれた場合に fingerprint を毎回変えるだけで簡単に dedup を回避でき、ログをスパムされ得ます。Phase 1 でも最低限、fingerprint はサーバ側で（app/error_type/message/route 等の検証済みフィールドから）再計算して上書きする、もしくは `request.auth?.uid`/IP/AppCheck 等と組み合わせてレート制限する形にして防御を成立させてください。
```

**コメント要約**: クライアント送信 fingerprint をそのまま dedup キーに使うと dedup 回避でスパム可能。

**判断結果**: 🚨 修正推奨

**判断理由**: 仕様書 Phase 1 はクライアント fingerprint を前提とするが、認証不要 Callable では dedup 回避の実害がある。サーバー側で検証済みフィールドから fingerprint を再計算して上書きすれば、クライアント改ざんによる dedup 回避を防げる。IP/UID レート制限は Phase 2 でも、サーバー再計算は Phase 1 でも小さな変更で効果が大きい。

---

**識別子**: RC-11（GitHub id: 3296398679）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[nits] このテストファイル名が `clientErrorReport.test.ts` ですが、実際にテストしているのは `shouldReportClientError`（dedup util）なので意図が伝わりづらいです。`utils/clientErrorDedup.test.ts` のように対象に合わせてリネームするか、Callable 側（`reportClientError`）の挙動テストも追加してファイル名と内容を一致させてください。
```

**コメント要約**: テストファイル名とテスト対象（dedup util）が不一致。

**判断結果**: 👌 修正不要

**判断理由**: 可読性の nits。dedup ロジックのテスト自体は有効。リネームまたは Callable 統合テスト追加は任意。Phase 1 マージブロッカーではない。

---

**識別子**: RC-12（GitHub id: 3296398612）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] dedup の `cache` が TTL 経過後も削除されないため、インスタンスが長生きすると fingerprint が増え続けてメモリが単調増加します（Callable が無認証な前提だと DoS 的にも悪用され得ます）。TTL を過ぎたエントリを間引く/サイズ上限を設けて古いものを削除する等、キャッシュのクリーンアップ戦略を入れてください。

```

**コメント要約**: dedup Map が TTL 超過エントリを削除せず、メモリが単調増加する。

**判断結果**: 🚨 修正推奨

**判断理由**: `shouldReportClientError` は TTL 判定のみで `cache.set` し続ける。ユニーク fingerprint が増えると Cloud Run インスタンスのメモリ圧迫リスクあり。認証不要 Callable との組み合わせで DoS 的悪用も理論上可能。TTL 経過エントリの削除または最大件数（LRU）の追加を推奨。

---

**識別子**: RC-13（GitHub id: 3296385511）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  dedup キャッシュの無制限増加を防止する**

この実装は fingerprint を `Map` に追加し続ける一方で TTL 超過エントリを削除していないため、ユニークな fingerprint が増えるほどメモリを無制限に消費します。`reportClientError` は認証不要 Callable として公開されており、通常運用でも攻撃的入力でも長寿命インスタンスで `dedupCache` が肥大化して Cloud Run のメモリ圧迫・再起動を招くリスクがあります。TTL に基づく定期クリーンアップか最大件数（LRU など）の上限を入れるべきです。

Useful? React with 👍 / 👎.
```

**コメント要約**: RC-12 と同様、dedup Map の無制限増加によるメモリリスク。

**判断結果**: 🚨 修正推奨

**判断理由**: RC-12 と同一論点。Copilot・Codex 双方から P1/must 相当の指摘。修正は RC-12 と一括対応。

---

**識別子**: RC-14（GitHub id: 3296398661）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `setupGlobalErrorHandling` を `app.mount` 後に dynamic import しているため、マウント直後のタイミングで発生したエラーがグローバルハンドラに拾われない可能性があります。router は既に `Promise.all([...])` で import しているので二重 import を避け、同じ module から `router` を参照してプラグイン適用直後（可能なら mount 前）に `setupGlobalErrorHandling` を設定する形に寄せてください。
```

**コメント要約**: RC-6 と同様、mount 後 dynamic import による errorHandler 未設定窓。

**判断結果**: 👌 修正不要

**判断理由**: RC-6 と同一論点。imo レベル。Phase 1 必須修正ではない。

---

**識別子**: RC-15（GitHub id: 3296398646）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `setupGlobalErrorHandling` を `app.mount` 後にさらに dynamic import しているため、マウント直後〜import 完了までの間はグローバルハンドラが未設定になります（初期化直後のエラーを取りこぼす可能性）。また router はすでに `Promise.all([...])` で import 済みなので再 import せず、同じ module から `router` を取り出して、プラグイン適用直後（できれば mount 前）に `setupGlobalErrorHandling` を同期的にセットする構造にすると安全です。
```

**コメント要約**: RC-6 / RC-14 と同様、mount 後 setup のタイミング問題。

**判断結果**: 👌 修正不要

**判断理由**: RC-6 / RC-14 と同一論点。user/admin 両方への同一指摘。Phase 1 必須修正ではない。
---

## 評価セッション

- **評価日時**: 2026-05-25 18:40 JST
- **ブランチ名**: feat/1060
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2040
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 4（レビュー依頼定型文 2 件: 4532367513, 4533172404 / Codex 接続案内 2 件: 4532383501, 4533189699）

### RC 別対応チェックリスト

- [x] **RC-16**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-17**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-18**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-19**（GitHub id: 4532382943）— 👌 修正不要
- [x] **RC-20**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-21**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-22**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-23**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-24**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-25**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-26**（GitHub id: 4533189106）— 👌 修正不要
- [x] **RC-27**（GitHub id: 3296677745）— 👌 修正不要
- [x] **RC-28**（GitHub id: 3296451106）— ✅ 対応済み
- [x] **RC-29**（GitHub id: 3296677701）— 👌 修正不要
- [x] **RC-30**（GitHub id: 3297236446）— 👌 修正不要
- [x] **RC-31**（GitHub id: 3297293905）— 👌 修正不要
- [x] **RC-32**（GitHub id: 3296451112）— ✅ 対応済み
- [x] **RC-33**（GitHub id: 3296451115）— ✅ 対応済み
- [x] **RC-34**（GitHub id: 3297293862）— ✅ 対応済み
- [x] **RC-35**（GitHub id: 3296436679）— 👌 修正不要
- [x] **RC-36**（GitHub id: 3296451108）— ✅ 対応済み
- [x] **RC-37**（GitHub id: 3296677686）— 👌 修正不要
- [x] **RC-38**（GitHub id: 3297025659）— 👌 修正不要
- [x] **RC-39**（GitHub id: 3296451117）— ✅ 対応済み
- [x] **RC-40**（GitHub id: 3297293931）— 👌 修正不要
- [x] **RC-41**（GitHub id: 3296451102）— ✅ 対応済み
- [x] **RC-42**（GitHub id: 3296436675）— 👌 修正不要
- [x] **RC-43**（GitHub id: 3297128076）— 👌 修正不要
- [x] **RC-44**（GitHub id: 3296677650）— 👌 修正不要
- [x] **RC-45**（GitHub id: 3296451118）— ✅ 対応済み
- [x] **RC-46**（GitHub id: 3296436673）— 👌 修正不要
- [x] **RC-47**（GitHub id: 3296668579）— 👌 修正不要
- [x] **RC-48**（GitHub id: 3297128081）— 👌 修正不要
- [x] **RC-49**（GitHub id: 3297285287）— 👌 修正不要
- [x] **RC-50**（GitHub id: 3296677722）— 👌 修正不要

**識別子**: RC-16（GitHub id: 4532382943）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `common/src/apis/clientError.ts` L22 — `fingerprint` は `length(64)` が正確**

```ts
fingerprint: z.string().max(64),
```

SHA-256 hex は常に固定 64 文字です。サーバー側では `data.fingerprint` を dedup に使わなくなりましたが、スキーマバリデーションとして 1〜63 文字の値が通過し得ます。`z.string().length(64)` にすると入力の形式保証が明確になります。
```

**コメント要約**: fingerprint を `length(64)` にすべき（nits）。

**判断結果**: 👌 修正不要

**判断理由**: RC-4 と同一論点。クライアントは常に 64 文字 hex を生成し、サーバーは dedup に自計算 fingerprint を使用。`length(64)` は任意改善（nits）。

---

**識別子**: RC-17（GitHub id: 4532382943）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `common/src/apis/clientError.test.ts` L13–20 — 最初の 2 テストが同一アサーション**

```ts
it('必須フィールドが揃っていれば parse できる', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 次と同じ
})
it('severity のデフォルトは error', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 同じ
})
```

1 件目の意図が「parse 成功の確認」であれば、`expect(result.app).toBe('user')` など別の観点のアサーションに変えると重複を解消できます。
```

**コメント要約**: clientError.test.ts の最初 2 テストが同一アサーション（nits）。

**判断結果**: 👌 修正不要

**判断理由**: RC-7 と同一論点。テスト品質の nits で Phase 1 マージブロッカーではない。

---

**識別子**: RC-18（GitHub id: 4532382943）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[imo] `user/src/main.ts` / `admin/src/main.ts` L37–41 — `setupGlobalErrorHandling` が二重 dynamic import + mount 後設定**

```ts
app.mount('#app')

import('@shokujii/base/utils/setupGlobalErrorHandling.js').then(({ setupGlobalErrorHandling }) => {
  import('@shokujii/base/plugins/router/index.js').then(({ router }) => {  // ← すでに上で import 済み
    setupGlobalErrorHandling(app, router, { app: 'user' })
  })
})
```

`router` は直上の `Promise.all` ですでに import されているのに再度 dynamic import しています。`app.config.errorHandler` が mount 完了後にようやく設定されるため、マウント中に Vue が throw したエラーを拾えない短い窓があります。

`Promise.all` のチェーン内で `router` を取り出して mount 前に `setupGlobalErrorHandling` を呼び出す構造にすることを検討してください。

また `setupGlobalErrorHandling` 内で `configureClientErrorReporting` を再度呼んでいますが（`setupGlobalErrorHandling.ts` L14）、`main.ts` の L31 でも mount 前に呼んでいるため二重呼び出しになっています。`setupGlobalErrorHandling` 内の `configureClientErrorReporting` 呼び出しを削除するか、`main.ts` 側の呼び出しを削除するか、どちらかに統一すると意図が明確になります。
```

**コメント要約**: setupGlobalErrorHandling の mount 後設定・router 二重 import・configureClientErrorReporting 二重呼び出し（imo）。

**判断結果**: 👌 修正不要

**判断理由**: mount 後 setup は RC-6/14/15 と同一で Phase 1 必須ではない。`configureClientErrorReporting` の二重呼び出しは同一値の idempotent 設定であり実害なし。改善は任意リファクタ。

---

**識別子**: RC-19（GitHub id: 4532382943）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `functions/default/src/utils/clientErrorDedup.ts` L37 — `pruneExpiredEntries` が毎回 O(n) 実行される**

```ts
export function shouldReportClientError(fingerprint: string, now: number, cache: DedupCache): boolean {
  pruneExpiredEntries(cache, now)  // ← 全エントリをスキャン
  ...
}
```

dedup チェックのたびに全 cache エントリを走査します。Phase 1 の 1000 件上限であれば実害は薄いですが、高頻度リクエスト時の無駄が気になる場合は一定間隔（例: prune 後に `lastPrunedAt` を記録し 1 分以上経過時のみ実行）に絞る実装も候補です。Phase 1 スコープ外としての受け入れ判断でも問題ありません。
```

**コメント要約**: dedup の prune が毎リクエスト O(n)（nits）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 1 は MAX 1000 件上限あり。毎回 O(n) prune は許容範囲。間引き間隔の最適化は Phase 2 以降の任意改善。

---

**識別子**: RC-20（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[must] `functions/default/src/clientErrorReport.ts` L38 — `user_id` にクライアント任意値を使用**

```ts
user_id: data.user_id,  // ← クライアントが自由に設定できる
```

Callable は未認証でも呼べるため、`data.user_id` は改ざん可能です。Cloud Logging に誤ったユーザー ID が記録されることで、520 事象の追跡時に原因ユーザーを誤特定するリスクがあります。

→ `user_id: request.auth?.uid ?? data.user_id` のようにサーバー認証UID を優先する、または別フィールドとして並記してください。
```

**コメント要約**: Callable の user_id にクライアント任意値を使用している（must）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 2 課題。Phase 1 では未認証呼び出しも想定し payload の `user_id` は参考情報。`request.auth?.uid` 優先は仕様書 Phase 2 に記載済みの改善候補。

---

**識別子**: RC-21（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[must] `common/src/apis/clientError.ts` L22 — `fingerprint` のバリデーションが不十分**

```ts
fingerprint: z.string().max(64),
```

`max(64)` では 1〜63 文字や非 hex 文字列も通過します。サーバー側で fingerprint を再計算するとはいえ、入力検証として SHA-256 hex（固定 64 文字）を前提にするなら `z.string().length(64)` に変更してください。
```

**コメント要約**: fingerprint バリデーションが max(64) のみで不十分（must）。

**判断結果**: 👌 修正不要

**判断理由**: RC-4 と同一論点。

---

**識別子**: RC-22（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[must] `base/src/stores/community.ts` L324–329 — `subscribeAlbumItems` の onSnapshot エラーコールバックが `reportClientError` を呼んでいない**

```ts
(err) => {
  console.error('subscribeAlbumItems snapshot error', err)  // ← reportClientError なし
  _albumItems.value = []
  unsubscribeAlbumItems?.()
  unsubscribeAlbumItems = null
},
```

データ取得自体は成功したがドキュメントのデシリアライズ失敗は per-doc try/catch（L317–320）で捕捉されますが、Firestore 権限エラーや接続切断はこのエラーコールバックに流れます。`reportClientError` が呼ばれないため、Phase 1 の「store warn 捕捉」から漏れます。

→ `reportClientError(err, { severity: 'warn' })` を追加してください。
```

**コメント要約**: subscribeAlbumItems の onSnapshot エラー callback が reportClientError 未呼び出し（must）。

**判断結果**: 👌 修正不要

**判断理由**: onSnapshot リスナーエラー（権限・接続）は Phase 1 の「converter/Zod 失敗 warn 捕捉」とは別カテゴリ。Phase 2 で error callback からの reportClientError 追加を検討。

---

**識別子**: RC-23（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `user/src/main.ts` L37–41 / `admin/src/main.ts` L37–41 — `router` を二重に dynamic import**

```ts
import('@shokujii/base/utils/setupGlobalErrorHandling.js').then(({ setupGlobalErrorHandling }) => {
  import('@shokujii/base/plugins/router/index.js').then(({ router }) => {  // ← 上の Promise.all で既に import 済み
    setupGlobalErrorHandling(app, router, { app: 'user' })
  })
})
```

L25 の `Promise.all` で `'@shokujii/base/plugins/router/index.js'` は既にインポート済みです。二重 import は ESM キャッシュで同一モジュールが返りますが、非同期ステップが増えます。`Promise.all` のタプルから `{ router }` を直接取り出して使ってください。
```

**コメント要約**: main.ts で router を二重 dynamic import（nits）。

**判断結果**: 👌 修正不要

**判断理由**: RC-15/RC-50 と同一。router 二重 dynamic import は ESM キャッシュで実害は限定的（nits）。

---

**識別子**: RC-24（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[nits] `common/src/apis/clientError.test.ts` L13–21 — 最初の 2 テストが同一アサーション**

```ts
it('必須フィールドが揃っていれば parse できる', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 次のテストと同じ
})
it('severity のデフォルトは error', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 同じ
})
```

1 件目の意図「parse 成功」を別観点のアサーション（例: `expect(result.app).toBe('user')`）に変えると重複を解消できます。
```

**コメント要約**: clientError.test.ts テスト重複（nits）。

**判断結果**: 👌 修正不要

**判断理由**: RC-7 と同一論点。

---

**識別子**: RC-25（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[ask] `base/src/stores/event.ts` L370–398 — `getLoadedEvent` の concurrent 呼び出し時の `pendingLoadedEventReject` 上書き**

```ts
pendingLoadedEventReject = reject  // ← 2 回目の呼び出しで上書き
pendingLoadedEventCleanup = cleanup
```

router guard と UI コンポーネントが同時に `getLoadedEvent` を呼ぶ場合、2 回目の呼び出しが `pendingLoadedEventReject` を上書きし、ZodError 発生時に 1 回目のプロミスが resolve も reject もされない状態になります（タイムアウトまで宙ぶらりん）。PR 説明でも言及されていますが、Phase 1 として受け入れる判断であれば `[ask]` としての確認で構いません。
```

**コメント要約**: getLoadedEvent の pending 上書きによる並行呼び出し問題（ask）。

**判断結果**: 👌 修正不要

**判断理由**: getLoadedEvent の single-slot は Phase 1 既知の制限。並行呼び出しは稀で、ZodError 時の /520 主要経路は動作。Phase 2 で singleflight 等を検討。

---

**識別子**: RC-26（GitHub id: 4533189106）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
**[fyi] `base/src/utils/reportClientError.ts` L66–71 — `resolveSeverity` が ZodError を自動で `error` に格上げする設計確認**

```ts
function resolveSeverity(err: unknown, contextSeverity?: 'error' | 'warn'): 'error' | 'warn' {
  if (err instanceof ZodError) {
    return 'error'
  }
  return contextSeverity ?? 'error'
}
```

store 層では `reportClientError(err, { severity: 'warn' })` と呼んでいますが、`err instanceof ZodError` であれば context の `severity` が `'warn'` でも `'error'` が記録されます。この設計が意図通りであれば問題ありませんが、仕様書の severity 設計（store warn / global error）と照らし合わせてコメントを追加しておくと、後から読んだ際の意図が明確になります。
```

**コメント要約**: resolveSeverity の ZodError 格上げ設計確認（fyi）。

**判断結果**: 👌 修正不要

**判断理由**: ZodError を error に格上げするのは意図的設計（Firestore スキーマ乖離は ERROR アラート対象）。コメント追記は任意だが挙動変更は不要。

---

**識別子**: RC-27（GitHub id: 3296677745）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[nits] Promise.all ですでに '@shokujii/base/plugins/router/index.js' を import 済みなので、setupGlobalErrorHandling 設定のために同モジュールを再 import するのは冗長です（import はキャッシュされますが非同期ステップが増えます）。Promise.all の戻り値から router を取り出して渡す形にすると読みやすくなります。
```

**コメント要約**: admin main.ts で router 再 import は冗長（nits）。

**判断結果**: 👌 修正不要

**判断理由**: RC-15 と同一。admin 側 router 再 import は nits。

---

**識別子**: RC-28（GitHub id: 3296451106）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `subscribeAlbumItems` だけ外側 `try/catch` + `map` のため、1 件の Zod 失敗で `_albumItems` 全体が `[]` になります。同一 PR の `eventList` / `partner` / `subscribeOrders` は `flatMap` + per-doc `try/catch` で失敗ドキュメントのみスキップしており設計が不一致です。また `documentPath` が無く、どのドキュメントか Logs Explorer から特定できません。

→ 他 store と同様に `flatMap` + `reportClientError(err, { documentPath: d.ref.path, severity: 'warn' })` に揃えてください。
```

**コメント要約**: subscribeAlbumItems を flatMap + documentPath に揃える（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: L314–322 で flatMap + per-doc `documentPath` + `severity: warn` に修正済み。

---

**識別子**: RC-29（GitHub id: 3296677701）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] onSnapshot のエラーコールバック側（subscribeAlbumItems snapshot error）は console.error のみで reportClientError に流れていないため、Phase 1 の「store warn 捕捉」から漏れます。ここも reportClientError(err, { documentPath: ... , severity: 'warn' }) 等で送るようにして追跡可能にしてください。

```

**コメント要約**: onSnapshot エラー callback から reportClientError へ（must）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 2 課題（RC-22/4533189106-3 と同一）。Phase 1 スコープ外。

---

**識別子**: RC-30（GitHub id: 3297236446）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  getLoadedEvent の待機を単一スロットにしない**

`pendingLoadedEventReject`/`pendingLoadedEventCleanup` を 1 件だけ保持しているため、同じ `EventStore` で `getLoadedEvent()` が並行呼び出しされると後続呼び出しが先行呼び出しの待機状態を上書きします。すると `subscribeEvent` 側で `ZodError` が出ても `rejectPendingLoadedEvent` は最後に登録された Promise しか reject できず、先行呼び出しはタイムアウトして `/404` 側に流れ、520 用の異常判定を取りこぼします。待機中の reject を複数保持して全件 reject するか、直近の `ZodError` を保持して以後の `getLoadedEvent` を即時 reject できるようにしてください。

Useful? React with 👍 / 👎.
```

**コメント要約**: getLoadedEvent 単一スロット問題（P1）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 1 既知制限（RC-25/4533189106-6 と同一）。

---

**識別子**: RC-31（GitHub id: 3297293905）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[ask] getLoadedEvent() 呼び出しごとに `pendingLoadedEventReject/cleanup` を上書きしているため、イベントがロードされる前に getLoadedEvent が並行に複数回呼ばれると、先に呼ばれた Promise が ZodError で即 reject されずタイムアウトまで残る可能性があります。並行呼び出しがあり得る前提なら、(a) 既存の pending Promise を返す(singleflight) か (b) reject/cleanup を配列で保持して全て reject する、等で一貫した挙動にしたいです。
```

**コメント要約**: getLoadedEvent 並行呼び出し時の pending 上書き（ask）。

**判断結果**: 👌 修正不要

**判断理由**: RC-25 と同一 [ask]。Phase 1 受け入れ。

---

**識別子**: RC-32（GitHub id: 3296451112）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `message` / `stack` は slice 済みですが、`error_type` は `err.constructor.name` をそのまま送っており、100 文字超のクラス名だと `ClientErrorReportRequestSchema` の `error_type.max(100)` で Callable が `invalid-argument` になり、**エラー報告自体が失敗**します。

→ `error_type: err.constructor.name.slice(0, 100)` のように送信前にスキーマ制約へ合わせてください（ZodError 側の `'ZodError'` も同様に上限を意識）。
```

**コメント要約**: error_type を slice してスキーマ制約に合わせる（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: L38 `err.constructor.name.slice(0, 100)` で修正済み。

---

**識別子**: RC-33（GitHub id: 3296451115）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `context.app ?? 'user'` のため、今回追加した store 経由の `reportClientError(..., { documentPath, severity: 'warn' })` は **admin でも `app: user` として記録**されます。admin は `useEventStore` 等で base store を直接使うため、Logs Explorer の `jsonPayload.app` フィルタと fingerprint dedup の切り分けが誤ります。

→ `setupGlobalErrorHandling` 時にモジュールスコープへ現在の `app` を注入し store からも必ず付与する、または store 呼び出し側で `app` を必須にするなど、起動アプリ種別が必ず payload に載るようにしてください。
```

**コメント要約**: admin でも app:user になる問題（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: `configureClientErrorReporting` を mount 前に呼び出し、store 経由でも正しい `app` が付与される。

---

**識別子**: RC-34（GitHub id: 3297293862）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] resolveSeverity のコメントが「store 層は既定 warn」となっていますが、実装は `contextSeverity ?? 'error'` で既定が error になっています。コメントと挙動が不一致なので、(1) 既定を warn に寄せる か (2) コメントを修正して既定が error であることを明記、のどちらかに揃えてください。
```

**コメント要約**: resolveSeverity コメントと実装の不一致（must）。

**判断結果**: 🚨 修正推奨

**判断理由**: L65 コメント「store 層は既定 warn」と L70 `contextSeverity ?? 'error'` が不一致。store は明示的に warn を渡すが、コメントは誤解を招く。コメント修正（ZodError 格上げと未指定時 error を明記）が必要。

---

**識別子**: RC-35（GitHub id: 3296436679）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  clientError の可変長フィールドに最大長を付ける**

`zod_issues` の `code`/`message` と `user_id` に長さ制限が無いため、認証不要 Callable に対してこれらへ非常に長い文字列を送る payload がスキーマ検証を通過します。トップレベル項目だけを制限する現在の設計だと、ログ肥大化やコスト増加を抑える意図を満たせません。各フィールドにも `max(...)` を付けてサイズ上限を閉じる必要があります。

Useful? React with 👍 / 👎.
```

**コメント要約**: zod_issues / user_id 等に max 制限（P2）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 2 課題。トップレベルは max 制限済み。`zod_issues` ネストの max は追加 hardening として Phase 2。

---

**識別子**: RC-36（GitHub id: 3296451108）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: 他フィールドは `.max()` で上限がある一方、`user_id` だけ長さ制限がありません。認証不要 Callable のため、意図的に oversized payload を送られる余地があります（Phase 1 の Zod 防御の非対称）。

→ Firebase Auth UID 想定で `.max(128)` 程度を追加してください。
```

**コメント要約**: user_id に max 制限（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: L20 `.max(128)` 追加済み。

---

**識別子**: RC-37（GitHub id: 3296677686）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] fingerprint が z.string().max(64) だと 64 桁未満や非 hex 文字列も通ってしまいます（サーバー側で再計算するとはいえ、入力検証として曖昧です）。SHA-256 hex であることを前提にするなら、length(64) + hex の正規表現などで厳密にバリデーションしてください。
```

**コメント要約**: fingerprint を length(64)+hex で厳密化（must）。

**判断結果**: 👌 修正不要

**判断理由**: RC-4 と同一論点。

---

**識別子**: RC-38（GitHub id: 3297025659）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  zod_issues.path にサイズ上限を設ける**

`reportClientError` は認証不要の Callable ですが、`ZodIssueSchema.path` は配列長・要素文字列長の上限がないため、`zod_issues` に極端に長い path を含む入力がバリデーションを通過してしまいます。これにより、意図している payload 制限を回避してログ肥大化やコスト増加を招けるため、`path` 配列の最大要素数と文字列要素の `max(...)` を追加して入力サイズを閉じるべきです。

Useful? React with 👍 / 👎.
```

**コメント要約**: ZodIssueSchema.path にサイズ上限（P2）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 2 課題。`ZodIssueSchema.path` の配列長・要素長上限は追加 hardening。

---

**識別子**: RC-39（GitHub id: 3296451117）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: dedup キーにクライアント送信の `data.fingerprint` をそのまま使っているため、Callable を直接叩いて fingerprint を毎回変えるだけで dedup を回避でき、ログスパムの余地があります。

→ Zod 検証済みの `app` / `error_type` / `message` / `route` / `document_path` 等から **サーバー側で fingerprint を再計算して上書き**し、クライアント値は dedup に使わない形にしてください（クライアント計算は参考程度に留める）。
```

**コメント要約**: dedup にクライアント fingerprint を使わない（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: L24 `computeServerFingerprint(data)` でサーバー再計算済み。

---

**識別子**: RC-40（GitHub id: 3297293931）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] reportClientError Callable が認証/App Check 等の呼び出し元検証なしで公開されています（`request.auth` 未確認）。Zod バリデーションと fingerprint dedup はありますが、fingerprint を変えればログを大量生成でき、Logging コスト増・アラートノイズ・DoS の踏み台になり得ます。少なくとも App Check の必須化、IP/UID ベースのレート制限、あるいは未認証時はサンプリング/より厳しい上限制御など、追加の濫用対策を入れてください。
```

**コメント要約**: Callable 濫用対策（App Check 等）（must）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 1 では Zod 上限・サーバー fingerprint dedup・cache 1000 件上限で基本防御。App Check / レート制限は Phase 2。

---

**識別子**: RC-41（GitHub id: 3296451102）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: `ClientErrorReportRequest` に `stack` がありクライアントも送信しているのに、`logPayload` に含まれていません。520 初動でスタックが Cloud Logging に残らず調査が遅れます。

→ `logPayload` に `stack: data.stack` を追加してください（warn/error どちらのログでも同様）。
```

**コメント要約**: logPayload に stack 追加（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: L34 `stack: data.stack` 追加済み。

---

**識別子**: RC-42（GitHub id: 3296436675）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  ログの user_id は request.auth.uid で確定する**

Callable 側で `user_id` を `request.auth` ではなくクライアント送信値 `data.user_id` からそのまま記録しているため、呼び出し元は任意の UID を偽装してログに残せます。これによりインシデント調査時のユーザー特定が不正確になり、運用上のトリアージを誤らせます。認証済みリクエストでは `request.auth?.uid` を優先し、送信 payload の `user_id` は信頼しない形にすべきです。

Useful? React with 👍 / 👎.
```

**コメント要約**: user_id は request.auth.uid 優先（P1）。

**判断結果**: 👌 修正不要

**判断理由**: Phase 2 課題（RC-20 と同一）。

---

**識別子**: RC-43（GitHub id: 3297128076）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  認証済みUIDを優先して user_id を記録する**

`user_id` を `request.auth?.uid` ではなくクライアント送信値 `data.user_id` からそのままログへ記録しているため、認証済み呼び出しでも任意の UID を偽装して記録できます。インシデント調査時に「誰の端末で発生したか」の特定を誤るので、`request.auth` がある場合はその UID を優先し、payload の `user_id` は未認証時の補助情報に限定してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: 認証済み UID を優先して user_id 記録（P1）。

**判断結果**: 👌 修正不要

**判断理由**: RC-20/3296436675 と同一論点。

---

**識別子**: RC-44（GitHub id: 3296677650）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] Callable は未認証でも呼べるため、payload の user_id は任意値を入れられてログが汚染されます。ログに出す user_id は request.auth?.uid を優先（必要なら payload の user_id は別フィールドで併記）する形にして、なりすまし/誤検知を避けてください。
```

**コメント要約**: payload user_id 改ざん防止（must）。

**判断結果**: 👌 修正不要

**判断理由**: RC-20 と同一論点。

---

**識別子**: RC-45（GitHub id: 3296451118）

**レビュワー**: nijuni-yasu

**レビューのコメント（原文）**:

```
🔴 **必須修正**: TTL 判定後も `cache.set` したエントリを削除しておらず、ユニーク fingerprint が増えるほど Map が単調増加します。認証不要 Callable と組み合わせると、長寿命インスタンスでのメモリ圧迫・DoS 的悪用リスクがあります。

→ TTL 経過エントリの定期削除、または最大件数（LRU 等）で古いエントリを間引くクリーンアップを入れてください。
```

**コメント要約**: dedup cache の TTL 後削除・上限（必須）。

**判断結果**: ✅ 対応済み

**判断理由**: `pruneExpiredEntries` + `evictOldestEntries(MAX 1000)` 実装済み。

---

**識別子**: RC-46（GitHub id: 3296436673）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  マウント前にグローバルエラーハンドラを設定する**

`setupGlobalErrorHandling` の呼び出しが `app.mount('#app')` の後に非同期で実行されるため、初回レンダリングや `mounted` 直後に発生した例外は `app.config.errorHandler` に捕捉されず、`/520` 遷移と `reportClientError` 送信のどちらも行われません。以前はハンドラをマウント前に設定していたため、この順序変更で「起動直後の障害を取りこぼす」回帰が入っています。

Useful? React with 👍 / 👎.
```

**コメント要約**: mount 前にグローバルエラーハンドラ設定（P1）。

**判断結果**: 👌 修正不要

**判断理由**: RC-6 と同一論点。Codex P1 表記だが Phase 1 では import 解決数十 ms の窓は許容。

---

**識別子**: RC-47（GitHub id: 3296668579）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  mount 前にグローバルエラーハンドラを設定する**

`app.mount('#app')` の後で `setupGlobalErrorHandling` を動的 import しているため、初回レンダリング直後〜import 解決までに発生した未捕捉例外は `app.config.errorHandler` に到達せず、`/520` 遷移と `reportClientError` の送信が漏れます。今回の目的が 520 事象の検知基盤化なので、起動直後の失敗が取りこぼされるこの窓は実害があり、`setupGlobalErrorHandling` は router と同じ読み込みタイミングで mount 前に設定すべきです（`admin/src/main.ts` も同様の構造です）。

Useful? React with 👍 / 👎.
```

**コメント要約**: mount 前に setupGlobalErrorHandling（P1）。

**判断結果**: 👌 修正不要

**判断理由**: RC-6/3296436673 と同一論点。

---

**識別子**: RC-48（GitHub id: 3297128081）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  mount前にグローバルエラーハンドラを設定する**

`app.mount('#app')` の後で `setupGlobalErrorHandling` を動的 import しているため、初回レンダリング〜import 解決までに起きた例外は `app.config.errorHandler` に捕捉されず、`/520` 遷移と `reportClientError` 送信の両方が漏れます。起動直後の障害ほど再現が難しく運用影響が大きいので、router と同じタイミングで mount 前にハンドラを設定してください。

Useful? React with 👍 / 👎.
```

**コメント要約**: mount 前にハンドラ設定（P1）。

**判断結果**: 👌 修正不要

**判断理由**: RC-6/3296436673 と同一論点。

---

**識別子**: RC-49（GitHub id: 3297285287）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  グローバルエラーハンドラを mount 前に設定する**

`app.mount('#app')` の後で `setupGlobalErrorHandling` を遅延 import しているため、初回レンダリングや起動直後に発生した例外は `app.config.errorHandler` に到達せず、`/520` への遷移も `reportClientError` 送信も行われません。今回の変更で旧実装（起動時に即 `errorHandler` 設定）より捕捉範囲が狭くなっており、特に App 初期化失敗時に白画面化したまま運営側ログにも残らない回帰になります（`admin/src/main.ts` も同様の順序です）。

Useful? React with 👍 / 👎.
```

**コメント要約**: グローバルエラーハンドラを mount 前に（P1）。

**判断結果**: 👌 修正不要

**判断理由**: RC-6/3296436673 と同一論点。

---

**識別子**: RC-50（GitHub id: 3296677722）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[nits] Promise.all ですでに '@shokujii/base/plugins/router/index.js' を import 済みなので、setupGlobalErrorHandling 設定のために同モジュールを再 import するのは冗長です（import はキャッシュされますが非同期ステップが増えます）。Promise.all の戻り値から router を取り出して渡す形にすると読みやすくなります。
```

**コメント要約**: user main.ts router 再 import 冗長（nits）。

**判断結果**: 👌 修正不要

**判断理由**: RC-15/3296677745 と同一 nits。

---

## 評価セッション

- **評価日時**: 2026-05-25 19:15 JST
- **ブランチ名**: feat/1060
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2040
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼定型文 4533392932、Codex 接続案内 4533421036）

### RC 別対応チェックリスト

- [x] **RC-51**（GitHub id: 4533419952）— 👌 修正不要（configureClientErrorReporting 二重呼び出し nits）
- [x] **RC-52**（GitHub id: 4533419952）— 👌 修正不要（getLoadedEvent 並行 ask）
- [x] **RC-53**（GitHub id: 4533419952）— 👌 修正不要（Callable 濫用 Phase 2 明記済み）
- [x] **RC-54**（GitHub id: 4533419952）— 👌 修正不要（clientError.test 重複 nits）
- [x] **RC-55**（GitHub id: 3297461109）— ✅ 対応済み（ZodError のみ null、それ以外は再送出）
- [x] **RC-56**（GitHub id: 3297472599）— 👌 修正不要（カート非 ZodError の warn 報告 imo）
- [x] **RC-57**（GitHub id: 3297472553）— 👌 修正不要（getLoadedEvent 単一スロット must）
- [x] **RC-58**（GitHub id: 3297472581）— 👌 修正不要（Callable 認証任意・dedup のみ Phase 1）

---

**識別子**: RC-51（GitHub id: 4533419952・トップレベル内指摘 1/4）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[nits] `user/src/main.ts` / `admin/src/main.ts` L32 — `configureClientErrorReporting` が二重呼び出し

```ts
configureClientErrorReporting({ app: 'user' })         // ← L32
setupGlobalErrorHandling(app, routerMod.router, { app: 'user' })  // ← setupGlobalErrorHandling 内部でも呼ぶ
```

`setupGlobalErrorHandling` の中（L14）で `configureClientErrorReporting` を呼んでいるため、L32 の呼び出しは冗長になりました。`setupGlobalErrorHandling` に統一するか、`setupGlobalErrorHandling` 内部の呼び出しを削除するかどちらかに整理してください。
```

**コメント要約**: main.ts と setupGlobalErrorHandling 内で configureClientErrorReporting が二重呼び出しになっている nits。

**判断結果**: 👌 修正不要

**判断理由**: 同一値の idempotent 設定であり実害なし。mount 前に store 経由レポート用 app を確定させる意図で main 側呼び出しを残している。整理は任意リファクタで Phase 1 マージを阻害しない。

---

**識別子**: RC-52（GitHub id: 4533419952・トップレベル内指摘 2/4）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[ask] `base/src/stores/event.ts` L385–387 — `getLoadedEvent` の concurrent 呼び出しで `pendingLoadedEventReject` が上書き

```ts
pendingLoadedEventReject = reject    // ← 2 回目の呼び出しで上書き
pendingLoadedEventCleanup = cleanup
```

ZodError で `rejectPendingLoadedEvent` が呼ばれたとき、最後に `getLoadedEvent` を呼んだ Promise のみが reject され、それ以前の呼び出し（router guard と UI コンポーネントが同時に呼ぶケース等）はタイムアウトまで宙ぶらりになる可能性があります。

Phase 1 として意図的に許容する設計でしょうか？許容する場合はコメントで明示しておくと後から読みやすいです。
```

**コメント要約**: getLoadedEvent 並行呼び出し時に pending reject が上書きされ、ZodError が一部の待機者に届かない ask。

**判断結果**: 👌 修正不要

**判断理由**: RC-30/RC-31 と同一論点。Phase 1 の主経路は router guard からの単一 await。並行待機の singleflight 化は Phase 2 改善候補。コメント明示は任意。

---

**識別子**: RC-53（GitHub id: 4533419952・トップレベル内指摘 3/4）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] `functions/default/src/clientErrorReport.ts` L15–22 — fingerprint dedup だけでは Logging スパムを完全には防げない

```ts
export const reportClientError = onCall<ClientErrorReportRequest, Promise<ClientErrorReportResponse>>(
  async (request) => {
    // request.auth / App Check の検証なし
```

サーバー側 fingerprint は `app/error_type/message/route/document_path` の組み合わせで生成されますが、いずれも自由テキストのため、攻撃者がこれらのフィールドを少しずつ変えれば無限に異なる fingerprint を生成でき、dedup を回避した大量ログ送信が可能です。Phase 1 の設計上の判断として受け入れているなら、関数ソース or 仕様書に「Phase 1 は dedup のみの防御であり App Check / rate limit は Phase 2 で対応」と明記しておくと意図が伝わりやすくなります。
```

**コメント要約**: fingerprint dedup だけでは Logging スパムを防ぎ切れない imo。Phase 2 で App Check / rate limit を検討。

**判断結果**: 👌 修正不要

**判断理由**: 仕様書 5.5.3 で Phase 1 は dedup + Zod max が主防御、IP/UID レート制限は Phase 2 と明記済み。既知リスクとして文書化されている。

---

**識別子**: RC-54（GitHub id: 4533419952・トップレベル内指摘 4/4）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[nits] `common/src/apis/clientError.test.ts` L13–21 — 最初の 2 テストが同一アサーション

```ts
it('必須フィールドが揃っていれば parse できる', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 次のテストと同じ
})
it('severity のデフォルトは error', () => {
  const result = ClientErrorReportRequestSchema.parse(validPayload)
  expect(result.severity).toBe('error')  // ← 同じ
})
```

1 件目の意図が「parse 成功の確認」であれば `expect(result.app).toBe('user')` 等に変えると重複が解消されます。
```

**コメント要約**: clientError.test.ts の最初 2 テストが同一アサーションの nits。

**判断結果**: 👌 修正不要

**判断理由**: RC-7/RC-19 と同一。テスト意図の重複は品質改善だが機能・セキュリティへの影響なし。

---

**識別子**: RC-55（GitHub id: 3297461109）

**レビュワー**: Codex

**レビューのコメント（原文）**:

```
**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  ZodError以外はカート行を握りつぶさない**

`getLoadedEvent()` の失敗理由を問わず `null` を返しているため、ネットワーク遅延や一時的な Firestore 失敗でも該当イベントのカート行が無言で除外されます。今回の変更意図（ZodError 行のみスキップ）と異なり、`ZodError` 以外まで欠落扱いになるので、実在する注文が UI から消えてユーザーが誤って空カートと判断する回帰になります。`ZodError` 以外はここで `return null` せず再送出して上位で扱うべきです。

Useful? React with 👍 / 👎.
```

**コメント要約**: カート構築で getLoadedEvent 失敗を理由問わず null にしており、タイムアウト等でも行が消える P1 指摘。

**判断結果**: ✅ 対応済み

**判断理由**: ZodError のみ return null、それ以外は throw するよう修正。Promise.all 失敗時は console.error のみで _cart を更新せず前回表示を維持する。

---

**識別子**: RC-56（GitHub id: 3297472599）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[imo] カート構築中に `getLoadedEvent()` が失敗した場合、ZodError 以外は `console.error` のみで Cloud Logging 側には送られません（他の store では `reportClientError(..., severity: 'warn')` を入れている箇所が多いです）。本番でカート欠落の原因追跡が難しくなるので、少なくとも ZodError 以外は `reportClientError` で warn 通知することを検討してください。
```

**コメント要約**: カートで非 ZodError 失敗時に reportClientError しないため運営追跡が難しい imo。

**判断結果**: 👌 修正不要

**判断理由**: RC-55 修正後は非 ZodError は throw または再送出され unhandledrejection / 上位 catch に流れる想定。意図的に warn で握りつぶす設計ではない。RC-55 対応後に warn 追加はノイズになりうる。

---

**識別子**: RC-57（GitHub id: 3297472553）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] `getLoadedEvent()` の待機中エラー通知が `pendingLoadedEventReject` 1件分しか保持できない実装になっており、同一イベントに対して `getLoadedEvent()` が並行に呼ばれると後から呼んだ方で上書きされます。ZodError 発生時に一部の呼び出し元がタイムアウトまで解決せず、router guard 側が `/520` 判定できずに誤って `/404` になる等の不整合が起き得ます（例: guard とページ初期化が同時に待つ）。待機 Promise を共有する（単一の in-flight Promise を返す）か、reject/cleanup を複数保持して全待機者へ通知できる形にしてください。
```

**コメント要約**: getLoadedEvent 単一スロットで並行待機者に ZodError が届かず 404 になる must。

**判断結果**: 👌 修正不要

**判断理由**: RC-30/RC-31/RC-52 と同一。Phase 1 主経路は router guard の単一 await。520 遷移は rejectPendingLoadedEvent + guard で動作確認済み。singleflight 化は Phase 2 改善。

---

**識別子**: RC-58（GitHub id: 3297472581）

**レビュワー**: Copilot

**レビューのコメント（原文）**:

```
[must] この Callable は認証必須ではなく、dedup もインスタンス内の fingerprint ベースのみなので、攻撃者が message/route 等を変えて fingerprint を毎回変えるだけで無制限にログを生成できてしまいます（Invocation/Logging コスト増・スパム）。Phase 1 でも最低限 `enforceAppCheck` の有効化、認証必須化、または IP/uid 単位のレート制限（Cloud Armor / Firebase App Check / 追加の in-memory カウンタ等）を検討してください。
```

**コメント要約**: 認証任意 Callable の Logging スパムリスク must。App Check / rate limit 検討。

**判断結果**: 👌 修正不要

**判断理由**: RC-23/RC-38/RC-53 と同一。仕様 5.5.1 で認証任意は意図的。Phase 1 防御は Zod max + dedup。App Check / rate limit は Phase 2 タスクとして仕様書に記載済み。

---

