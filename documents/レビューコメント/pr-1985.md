# PR #1985 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-05-22 JST
- **対応状況更新**: 2026-05-24 JST（RC-24 / 25 / 26 / 29 のコード対応を反映）
- **追記（コードレビュー）**: 2026-05-31 JST（feat/1248 仕様変更後の削除候補・既存 RC の見直し。RC-30〜）
- **対応状況更新**: 2026-05-31 JST（RC-20 / 30 / 31 / 32 / 33 Phase 1 / 34 / 35 の実装反映）
- **対応状況更新**: 2026-05-31 JST（RC-21 / RC-49 友人グラフ性能改善: anchor 化 + 並列度制御）
- **追記（Codex 再レビュー）**: 2026-05-31 JST（PR #1985 アクティブインライン 31 件のうち未登録 14 件を RC-41〜54 として評価。Outdated 0 件）
- **追記（Codex 新規指摘）**: 2026-05-31 JST（未登録 7 件を RC-55〜61 として評価。Outdated 0 件）
- **再評価（4区分）**: 2026-05-31 JST（旧「🚨 修正推奨」「❌ 未対応」を 🚨必須 / 🟡提案 / 👌不要 / ✅済み に統一。RC-47〜54・RC-19/27/28 本文を反映）
- **仕様変更**: 2026-06-02 JST（`02_マイページ.md` §4.2.0 — 限定公開は本人・他者とも表示、リンクは本人のみ、タグなし。meet log 同様。RC-19 / 27 / 28 / 47 / 48 の判断を見直し）
- **実装**: 2026-05-31 JST（RC-55 / RC-56 / RC-57 / RC-61 対応）
- **対応状況更新**: 2026-06-02 JST（§4.2.0 実装反映。RC-19 / 27 / 28 / 47 / 48 / 59 / 60 を ✅ に更新）
- **対応状況更新**: 2026-06-03 JST（RC-58 / RC-50 を ✅ に更新。コミット `49858d69`）
- **追記（2026-06-03 再レビュー）**: Codex / Copilot 新規インライン 4 件を RC-62〜65 として評価
- **対応状況更新**: 2026-06-03 JST（RC-62 / 63 / 64 / 65 / 66 / 67 / 69 / 71 / 72 を ✅ に更新。未コミット）
- **追記（2026-06-03 再レビュー・PR 更新後）**: Codex / Copilot 新規インライン 4 件を RC-74〜77 として評価
- **追記（2026-06-03 最新5件）**: 直近インライン 5 件を再評価（RC-75〜77 再確認、RC-78〜79 新規）
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **Outdated 除外件数**: 0 件
- **記載方針**: **レビュワーのコメント**は GitHub 上の原文をそのまま掲載。**該当コード**はレビュー時点の PR diff（`diff_hunk`、長い場合は末尾抜粋）または参照パス。RC-30〜40 は Cursor Agent レビュー（GitHub インラインなし）。

### RC 別対応チェックリスト

- **RC-1**（GitHub id: 4396701768）— 👌 修正不要
- **RC-2**（GitHub id: 4396715849）— 👌 修正不要
- **RC-3**（GitHub id: 4396716591）— 👌 修正不要
- **RC-4**（GitHub id: 4415170421）— 👌 修正不要
- **RC-5**（GitHub id: 4415172162）— 👌 修正不要
- **RC-6**（GitHub id: 4415172401）— 👌 修正不要
- **RC-7**（GitHub id: 4495036343）— 👌 修正不要
- **RC-8**（GitHub id: 4495045899）— 👌 修正不要
- **RC-9**（GitHub id: 4495047260）— 👌 修正不要
- **RC-10**（GitHub id: 4507400916）— 👌 修正不要
- **RC-11**（GitHub id: 4507410451）— 👌 修正不要
- **RC-12**（GitHub id: 4507413359）— 👌 修正不要
- **RC-13**（GitHub id: 3271577304）— ✅ 対応済み
- **RC-14**（GitHub id: 3280598692）— ✅ 対応済み（RC-13 と同根）
- **RC-15**（GitHub id: 3214774200）— ✅ 対応済み（RC-13 と同根）
- **RC-16**（GitHub id: 3201082733）— ✅ 対応済み（RC-13 と同根）
- **RC-17**（GitHub id: 3201105560）— ✅ 対応済み（RC-13 と同根）
- **RC-18**（GitHub id: 3201105675）— ✅ 対応済み（RC-52/53 の空ページ追読みと併せて解消）
- **RC-19**（GitHub id: 3214771200）— ✅ 対応済み（§4.2.0: `buildMeetLogItems` + `is_public` / `is_linkable`。`FriendMeetLogDialog` は `row.is_linkable`）
- **RC-20**（GitHub id: 3232841618）— ✅ 対応済み（一覧 API で events 読み取りを廃止。2026-05-31 実装）
- **RC-21**（GitHub id: 3201105651）— ✅ 対応済み（anchor 化 + `runPairsWithConcurrency`）
- **RC-22**（GitHub id: 3214481696）— ✅ 対応済み
- **RC-23**（GitHub id: 3232501381）— 👌 修正不要（型定義が exclusive と明記済み）
- **RC-24**（GitHub id: 3214481695）— ✅ 対応済み
- **RC-25**（GitHub id: 3201105609）— ✅ 対応済み（RC-24 と同根）
- **RC-26**（GitHub id: 3232841612）— ✅ 対応済み
- **RC-27**（GitHub id: 3240875088）— ✅ 対応済み（§4.2.0: `visibleUserEvents` 削除、`canLinkToDetail` でリンク分岐）
- **RC-28**（GitHub id: 3240875092）— ✅ 対応済み（§4.2.0: コミュニティタブの `is_public` 除外削除 + リンク分岐）
- **RC-30** — ✅ 対応済み（`userFriendsResolver` 簡略化・`UserFriendListItem` からイベント解決フィールド削除）
- **RC-31** — ✅ 対応済み（`meetLogMaxPerFriend` 分岐削除・未使用 i18n 3 キー削除）
- **RC-32** — ✅ 対応済み（RC-20 実装と pr-1985 記載の整合）
- **RC-33** — ✅ Phase 1 対応済み（re-export 削除。Phase 2 store 化は別 Issue）
- **RC-34** — ✅ 対応済み（`FriendCard` で `convertToDate` 使用）
- **RC-35** — ✅ 対応済み（友人タブ watch を `nextTick` 1 回に簡略化）
- **RC-29**（GitHub id: 3247944584）— ✅ 対応済み（リグレッション）
- **RC-41**（GitHub id: 3297987316）— ✅ 対応済み（`bokudeli-event-batch` symlink 削除）
- **RC-42**（GitHub id: 3329888509）— ✅ 対応済み（RC-41 と同根）
- **RC-43**（GitHub id: 3328599645）— ✅ 対応済み（members サブコレ集計・RC-44 同根）
- **RC-44**（GitHub id: 3329888503）— ✅ 対応済み（RC-43 と同根）
- **RC-45**（GitHub id: 3294000127）— ✅ 対応済み（部分キャンセル時は ordered 残存なら履歴削除しない）
- **RC-46**（GitHub id: 3329888507）— ✅ 対応済み（`userProfile.ts` の Firestore 直クエリを store 化）
- **RC-47**（GitHub id: 3329884054）— ✅ 対応済み（§4.2.0: `listEventsForProfilePreview` から他者向け `is_public` クエリ削除 + Callable で `is_linkable`）
- **RC-48**（GitHub id: 3329888506）— ✅ 対応済み（§4.2.0: `listCommunitiesForProfilePreview` 同上・RC-47 同根）
- **RC-49**（GitHub id: 3294258729）— ✅ 対応済み（RC-21 と同根）
- [x] **RC-50**（GitHub id: 3328599646）— ✅ 対応済み（`deleteUserAccount` 後に `listFriendUserIds` + `recountUserProfileCountsForUsers`）
- **RC-51**（GitHub id: 3329884056）— ✅ 対応済み（RC-30 と同根・一覧で events 未読）
- **RC-52**（GitHub id: 3329884055）— ✅ 対応済み（友人 resolver 空ページループ + 概要 UI）
- **RC-53**（GitHub id: 3303247303）— ✅ 対応済み（フードタブ 0 件でも `foodHasMore` 時は IncrementalLoader）
- **RC-54**（GitHub id: 3329888504）— ✅ 対応済み（RC-53 と同根）
- [x] **RC-55**（GitHub id: 3330003644）— ✅ 対応済み（`communities` 配列 count 集計）
- [x] **RC-56**（GitHub id: 3330003647）— ✅ 対応済み（`firestore.rules` カウント保護）
- [x] **RC-57**（GitHub id: 3330114203）— ✅ 対応済み（`countParticipatedEventsForUser`）
- [x] **RC-58**（GitHub id: 3330114200）— ✅ 対応済み（`resolveUserFriendMeetLog` で `getUser` + `is_deleted` 時 `null`）
- [x] **RC-59**（GitHub id: 3329971269）— ✅ 対応済み（`loginUser` 変化で `previewStore` / `userFoodsStore` の `reload`）
- [x] **RC-60**（GitHub id: 3329971268）— ✅ 対応済み（§4.2.0: 認証変化時 `reload` + foods の `is_public` / `is_linkable` と `canLinkToDetail`。非公開の「非表示」は不要）
- [x] **RC-61**（GitHub id: 3329923027）— ✅ 対応済み（コミュニティタブ空ページ追読み）
- [x] **RC-62**（GitHub id: 3345937500）— ✅ 対応済み（`userProfileCountsZeroOrAbsentOnCreate()` を create に適用）
- [x] **RC-63**（GitHub id: 3345939351）— ✅ 対応済み（`community.ts` JSDoc を各 export 直前へ整理）
- [x] **RC-64**（GitHub id: 3345939363）— ✅ 対応済み（`memberOrder.ts` の `event.js` を型のみ import + 動的 import 化）
- [x] **RC-65**（GitHub id: 3345937495）— ✅ 対応済み（`fetchUser` 撤去、`profileUserId = props.userId` に簡素化）
- [x] **RC-66**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`resolveUserPath` / `resolveEventPath` を props 化）
- [x] **RC-67**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（冗長な `as` キャスト 3 箇所を除去）
- [x] **RC-68**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（`updateUserProfileCounts` の部分 `update()` は意図的）
- [x] **RC-69**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`listFriendUserIds` + `countOrderedFoodsForUser` 経由に store 化）
- [x] **RC-70**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（meet log の非公開イベント名表示は §4.2.0 どおり）
- [x] **RC-71**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`convertToDate` / `convertToDateWeekdayShort` に統一）
- [x] **RC-72**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`setupAutoLoadWhenEmpty` ヘルパに集約）
- [x] **RC-73**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（`meet_count` と可視 meet_log 行数の差）
- [x] **RC-74**（GitHub id: 3346282189）— ✅ 対応済み（`userProfileCountFieldUnchanged` で未バックフィル既存ユーザーの update を許可）
- [x] **RC-75**（GitHub id: 3346285962）— ✅ 対応済み（meet log 取得不可時にプレースホルダ行を返却）
- [x] **RC-76**（GitHub id: 3346286000）— ✅ 対応済み（`userFriends` store の空 `targetUserId` 早期 return）
- [x] **RC-77**（GitHub id: 3346286021）— ✅ 対応済み（`hasCountsChanged` 引数を `UserProfileCountsUpdate` に変更）
- [x] **RC-78**（GitHub id: 3346383903）— ✅ 対応済み（resource にあるカウントフィールドの削除を拒否）
- [x] **RC-79**（GitHub id: 3346440755）— ✅ 対応済み（取得不可イベントではカバー画像を非表示）

---

**識別子**: RC-1（GitHub id: 4396701768）

**レビュワー**: nijuni-yasu

**レビュワーのコメント**（原文）:

@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。

**コメント要約**: レビュー依頼の定型文。コード指摘ではない。

**判断結果**: 👌 修正不要

**判断理由**: 実装変更を求める内容ではない。

---

**識別子**: RC-2（GitHub id: 4396715849）

**レビュワー**: Copilot

**レビュワーのコメント**（原文）:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

了解しました。以後のレビューは日本語で、Files changed の該当行に 1 指摘 1 コメントで投稿します。今回の対応でコード変更はなく、対象HEADは 4b59415 です。

**コメント要約**: レビュー手順への応答。

**判断結果**: 👌 修正不要

**判断理由**: コード修正は不要。

---

**識別子**: RC-3（GitHub id: 4396716591）

**レビュワー**: Codex

**レビュワーのコメント**（原文）:

To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).

**コメント要約**: ツール利用案内。

**判断結果**: 👌 修正不要

**判断理由**: コードに対する指摘ではない。

---

**識別子**: RC-4（GitHub id: 4415170421）

**レビュワー**: nijuni-yasu

**レビュワーのコメント**（原文）:

@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-5（GitHub id: 4415172162）

**レビュワー**: Copilot

**レビュワーのコメント**（原文）:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

了解しました。以後のレビューは日本語で、Files changed の該当行に 1 指摘 1 コメントで対応します。今回の対応でコード変更はなく、対象HEADは 796a058 です。UI変更はありません（スクリーンショット追加対象なし）。

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-6（GitHub id: 4415172401）

**レビュワー**: Codex

**レビュワーのコメント**（原文）:

To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-7（GitHub id: 4495036343）

**レビュワー**: nijuni-yasu

**レビュワーのコメント**（原文）:

@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-8（GitHub id: 4495045899）

**レビュワー**: Copilot

**レビュワーのコメント**（原文）:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

承知しました。以後のレビューは日本語で、Files changed の該当行に 1 指摘 1 コメントで対応します。今回の対応でコード変更はなく、対象HEADは 7386fea です。UI変更がないため、追加スクリーンショットはありません。

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-9（GitHub id: 4495047260）

**レビュワー**: Codex

**レビュワーのコメント**（原文）:

To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-10（GitHub id: 4507400916）

**レビュワー**: nijuni-yasu

**レビュワーのコメント**（原文）:

@copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数ファイル分をまとめないでください。

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-11（GitHub id: 4507410451）

**レビュワー**: Copilot

**レビュワーのコメント**（原文）:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

承知しました。以後のレビューは日本語で、Files changed の該当行に 1 指摘 1 コメントで対応します。今回の対応でコード変更はなく、対象HEADは cd610a9 です。UI変更がないため、追加スクリーンショットはありません。

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-12（GitHub id: 4507413359）

**レビュワー**: Codex

**レビュワーのコメント**（原文）:

To use Codex here, [create a Codex account and connect to github](https://chatgpt.com/codex/cloud/settings/connectors).

**コメント要約**: RC-1〜RC-3 と同種の依頼・応答・案内の繰り返し。

**判断結果**: 👌 修正不要

**判断理由**: いずれもコード指摘ではない。

---

**識別子**: RC-13（GitHub id: 3271577304）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/orderCanceledSideEffects.ts:43`

**該当コード（レビュー時点の diff）**:

```diff
@@ -30,4 +33,33 @@ export async function applyOrderCanceledSideEffects(params: { event: ShokujiiEve
       userId,
     })
   }
+
+  // 友達グラフから当該イベントの履歴を取り除く（キャンセルしたユーザーは ordered 集合に残らない前提だが、確実に除外するため明示的に含める）
+  try {
+    const orderedOrders = await getOrders(communityId, eventId, 'ordered')
+    const orderedUserIds = orderedOrders.map((order) => order.user_id)
+    await removeEventFromFriendHistory({
+      event_id: eventId,
+      user_ids: [...new Set([...orderedUserIds, userId])],
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  キャンセル時は取消ユーザー関連ペアのみ履歴削除する**

`removeEventFromFriendHistory` は `user_ids` に含まれる全組み合わせペアから `event_id` を削除しますが、ここで `orderedUserIds`（残っている参加者）とキャンセルユーザーをまとめて渡しているため、残存参加者同士の履歴まで消えてしまいます。例えば A/B/C で C がキャンセルした場合、本来は A-C と B-C だけ削除すべきところ、A-B も削除されて `meet_count` や友人一覧が不正になります。`userId` と各残存ユーザーのペアだけを対象に削除するようにしないと、キャンセルのたびに既存関係が壊れます。

Useful? React with 👍 / 👎.

**コメント要約**: 注文キャンセル時の友人履歴削除範囲が広すぎるデータ破壊バグ。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘は妥当だった。`removeEventFromFriendHistory` を `anchor_user_id` + `counterpart_user_ids` に変更し、`buildPairsWithAnchor`（`friendsPairUtils.ts`）でキャンセルユーザーと各相手のペアのみ削除するよう修正。`orderCanceledSideEffects.ts` では `getMemberIds` から相手一覧を取得して渡す。

---

**識別子**: RC-14（GitHub id: 3280598692）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/orderCanceledSideEffects.ts:43`

**該当コード（レビュー時点の diff）**:

```diff
@@ -30,4 +33,33 @@ export async function applyOrderCanceledSideEffects(params: { event: ShokujiiEve
       userId,
     })
   }
+
+  // 友達グラフから当該イベントの履歴を取り除く（キャンセルしたユーザーは ordered 集合に残らない前提だが、確実に除外するため明示的に含める）
+  try {
+    const orderedOrders = await getOrders(communityId, eventId, 'ordered')
+    const orderedUserIds = orderedOrders.map((order) => order.user_id)
+    await removeEventFromFriendHistory({
+      event_id: eventId,
+      user_ids: [...new Set([...orderedUserIds, userId])],
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  キャンセル時に残存参加者同士の履歴削除を避ける**

`removeEventFromFriendHistory` は渡された `user_ids` の全ペアから `event_id` を削除する実装なので、ここで `orderedUserIds` とキャンセルした `userId` をまとめて渡すと、まだ参加中のユーザー同士（例: A-B）の履歴まで削除されます。例えば C がキャンセルして A/B が継続参加中でも A-B の `event_history` が消えるため、友人関係データが壊れます。キャンセル時は「キャンセルユーザーを含むペア」のみに削除対象を限定する必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: RC-13 と同根。

**判断結果**: ✅ 対応済み（RC-13 と同根）

**判断理由**: RC-13 と同一修正で解消。

---

**識別子**: RC-15（GitHub id: 3214774200）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/orderCanceledSideEffects.ts:43`

**該当コード（レビュー時点の diff）**:

```diff
@@ -30,4 +32,21 @@ export async function applyOrderCanceledSideEffects(params: { event: ShokujiiEve
       userId,
     })
   }
+
+  // 友達グラフから当該イベントの履歴を取り除く（キャンセルしたユーザーは ordered 集合に残らない前提だが、確実に除外するため明示的に含める）
+  try {
+    const orderedOrders = await getOrders(communityId, eventId, 'ordered')
+    const orderedUserIds = orderedOrders.map((order) => order.user_id)
+    await removeEventFromFriendHistory({
+      event_id: eventId,
+      user_ids: [...new Set([...orderedUserIds, userId])],
+    })
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  残存参加者同士の友人履歴削除を防ぐ**

この `user_ids` の作り方だと、例えば A/B/C が同席したイベントで C だけがキャンセルしたときに `removeEventFromFriendHistory` が `(A,B)` まで削除対象に含めてしまいます。結果として、実際には同席履歴が残る A-B 間の `event_history`・`meet_count`・`last_met_at` まで失われ、友人一覧の集計が崩れます。キャンセルしたユーザーを含むペアだけを削除対象にするか、残存 `ordered` ユーザー間を再計算して履歴を再付与する必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: RC-13 と同根。

**判断結果**: ✅ 対応済み（RC-13 と同根）

**判断理由**: RC-13 と同一修正で解消。

---

**識別子**: RC-16（GitHub id: 3201082733）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userFriends.ts:117`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+      friend_user_id: normalized.friend_user_id,
+    }
+  } catch {
+    throw new HttpsError('invalid-argument', 'Invalid cursor')
+  }
+}
+
+const encodeCursor = (cursor: UserFriendsListCursor | null): string | null => {
+  if (cursor == null) {
+    return null
+  }
+  return Buffer.from(JSON.stringify(cursor), 'utf8').toString('base64')
+}
+
+const asOrderStatus = (status: unknown): EventMemberOrderStatusType | undefined => {
+  return status === 'in_cart' || status === 'ordered' || status === 'canceled' ? status : undefined
+}
+
+export const onMemberOrderWritten = onDocumentWritten(
+  {
+    document: 'communities/{communityId}/events/{eventId}/members/{memberId}/member_orders/{orderId}',
+    region: 'asia-northeast1',
+    timeoutSeconds: 60,
+    memory: '256MiB',
+  },
+  async (event) => {
+    const before = event.data?.before?.data()
+    const after = event.data?.after?.data()
+    const beforeStatus = asOrderStatus(before?.status)
+    const afterStatus = asOrderStatus(after?.status)
+    const action = judgeOrderTransition(beforeStatus, afterStatus)
+    if (action === 'NOOP') {
+      return
+    }
+
+    const communityId = String(after?.community_id ?? before?.community_id ?? '')
+    const eventId = String(after?.event_id ?? before?.event_id ?? '')
+    const targetUserId = String(after?.user_id ?? before?.user_id ?? '')
+    if (communityId === '' || eventId === '' || targetUserId === '') {
+      logger.warn('Skip trigger due to missing ids', {
+        community_id: communityId,
+        event_id: eventId,
+        target_user_id: targetUserId,
+      })
+      return
+    }
+
+    const orderedOrders = await getOrders(communityId, eventId, 'ordered')
+    const orderedUserIds = [...new Set(orderedOrders.map((order) => order.user_id))]
+
+    if (action === 'ADD') {
+      const eventData = await getEventInCommunity(communityId, eventId)
+      const eventAt = eventData?.event_start_datetime ?? Date.now()
+      await applyOrdered({
+        event_id: eventId,
+        community_id: communityId,
+        event_at: eventAt,
+        user_ids: orderedUserIds,
+      })
+      return
+    }
+
+    await revokeOrdered({
+      event_id: eventId,
+      user_ids: [...new Set([...orderedUserIds, targetUserId])],
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  キャンセル時は対象ユーザーのペアだけ剥奪する**

`revokeOrdered` に `orderedUserIds + targetUserId` をそのまま渡すと、`buildPairs` が残存参加者同士のペアまで生成してしまうため、1人のキャンセルで無関係な友人関係（同イベントに残っている参加者同士）も削除されます。例えば A/B/C が継続参加で D だけキャンセルしたケースでも AB・AC・BC の履歴が消え、友人一覧データが壊れます。キャンセル時は「target と他参加者の組み合わせのみ」を削除対象にする必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: REMOVE 時の revoke 対象ペアが広すぎる（RC-13 同根）。

**判断結果**: ✅ 対応済み（RC-13 と同根）

**判断理由**: `revokeOrdered` はリファクタで削除済み。同等ロジックだった `removeEventFromFriendHistory` を RC-13 と同様に anchor 方式へ修正済み。

---

**識別子**: RC-17（GitHub id: 3201105560）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/userFriends.ts:118`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+    }
+  } catch {
+    throw new HttpsError('invalid-argument', 'Invalid cursor')
+  }
+}
+
+const encodeCursor = (cursor: UserFriendsListCursor | null): string | null => {
+  if (cursor == null) {
+    return null
+  }
+  return Buffer.from(JSON.stringify(cursor), 'utf8').toString('base64')
+}
+
+const asOrderStatus = (status: unknown): EventMemberOrderStatusType | undefined => {
+  return status === 'in_cart' || status === 'ordered' || status === 'canceled' ? status : undefined
+}
+
+export const onMemberOrderWritten = onDocumentWritten(
+  {
+    document: 'communities/{communityId}/events/{eventId}/members/{memberId}/member_orders/{orderId}',
+    region: 'asia-northeast1',
+    timeoutSeconds: 60,
+    memory: '256MiB',
+  },
+  async (event) => {
+    const before = event.data?.before?.data()
+    const after = event.data?.after?.data()
+    const beforeStatus = asOrderStatus(before?.status)
+    const afterStatus = asOrderStatus(after?.status)
+    const action = judgeOrderTransition(beforeStatus, afterStatus)
+    if (action === 'NOOP') {
+      return
+    }
+
+    const communityId = String(after?.community_id ?? before?.community_id ?? '')
+    const eventId = String(after?.event_id ?? before?.event_id ?? '')
+    const targetUserId = String(after?.user_id ?? before?.user_id ?? '')
+    if (communityId === '' || eventId === '' || targetUserId === '') {
+      logger.warn('Skip trigger due to missing ids', {
+        community_id: communityId,
+        event_id: eventId,
+        target_user_id: targetUserId,
+      })
+      return
+    }
+
+    const orderedOrders = await getOrders(communityId, eventId, 'ordered')
+    const orderedUserIds = [...new Set(orderedOrders.map((order) => order.user_id))]
+
+    if (action === 'ADD') {
+      const eventData = await getEventInCommunity(communityId, eventId)
+      const eventAt = eventData?.event_start_datetime ?? Date.now()
+      await applyOrdered({
+        event_id: eventId,
+        community_id: communityId,
+        event_at: eventAt,
+        user_ids: orderedUserIds,
+      })
+      return
+    }
+
+    await revokeOrdered({
+      event_id: eventId,
+      user_ids: [...new Set([...orderedUserIds, targetUserId])],
+    })
```

**レビュワーのコメント**（原文）:

[must] `REMOVE`（ordered→非ordered）時に `revokeOrdered` へ `orderedUserIds` 全体を渡しているため、キャンセルした本人以外（同じイベントに残って ordered のままのユーザー同士）の友人関係からも当該 event が削除されてしまいます。`targetUserId` を含むペアだけ revoke するように（例: revoke 用の入力を「対象ユーザー + 相手一覧」に分ける／buildPairs を使わず対象ユーザー固定でペア生成する等）修正してください。

**コメント要約**: RC-13 / RC-16 と同根。

**判断結果**: ✅ 対応済み（RC-13 と同根）

**判断理由**: RC-13 と同一修正で解消。

---

**識別子**: RC-18（GitHub id: 3201105675）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/userFriends.ts:162`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+    }
+
+    const orderedOrders = await getOrders(communityId, eventId, 'ordered')
+    const orderedUserIds = [...new Set(orderedOrders.map((order) => order.user_id))]
+
+    if (action === 'ADD') {
+      const eventData = await getEventInCommunity(communityId, eventId)
+      const eventAt = eventData?.event_start_datetime ?? Date.now()
+      await applyOrdered({
+        event_id: eventId,
+        community_id: communityId,
+        event_at: eventAt,
+        user_ids: orderedUserIds,
+      })
+      return
+    }
+
+    await revokeOrdered({
+      event_id: eventId,
+      user_ids: [...new Set([...orderedUserIds, targetUserId])],
+    })
+  },
+)
+
+export const getUserFriends = onCall<GetUserFriendsRequest, Promise<GetUserFriendsResponse>>(
+  { region: 'asia-northeast1', invoker: 'public' },
+  async (request) => {
+    const input = GetUserFriendsRequestSchema.parse(request.data)
+    const targetUser = await getUser(input.target_user_id, false)
+    if (targetUser == null || targetUser.is_deleted) {
+      throw new HttpsError('not-found', '存在しないユーザーです')
+    }
+    // TODO Phase 2: 公開範囲設定（オプトアウト）を参照し、不可なら permission-denied
+
+    const limit = input.limit ?? 10
+    const sortBy: UserFriendsSortBy = input.sort_by ?? 'meet_count'
+    const decodedCursor = decodeCursor(input.cursor)
+
+    const page = await listUserFriends(input.target_user_id, sortBy, limit, decodedCursor)
+    if (page.friends.length === 0) {
+      return { friends: [], has_more: false, next_cursor: null }
+    }
+
+    const userMap = await getUsersByUserIds(page.friends.map((friend) => friend.id))
+    const friends = page.friends
+      .map((friend) => {
+        const user = userMap.get(friend.id)
+        if (user == null || user.is_deleted) {
+          return null
+        }
+        return {
+          user_id: friend.id,
+          user_name: user.user_name,
+          user_image_url: user.user_image_url,
+          meet_count: friend.meet_count,
+          first_met_at: friend.first_met_at,
+          last_met_at: friend.last_met_at,
+        }
+      })
+      .filter((item): item is NonNullable<typeof item> => item != null)
+
+    return {
+      friends,
+      has_more: page.hasMore,
+      next_cursor: encodeCursor(page.nextCursor),
```

**レビュワーのコメント**（原文）:

[must] 退会ユーザーを `users` 解決時に `filter` で除外していますが、`has_more` / `next_cursor` は除外前の `page` を基準に返しているため、1ページ目が全員退会済み等のケースで `friends: []` なのに `has_more: true` となり、クライアント側が「空」と判断しやすくなります。Callable 側で「表示可能な friends が一定数集まるまでページングを継続する」か、少なくとも `friends` が空のときは追加ページを読んでから返す等、レスポンス整合を取ってください。

**コメント要約**: 友人一覧のページング整合性。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘は妥当だった。`resolveUserFriendsList` で退会ユーザー除外後に `friends` が空かつ `hasMore` かつ `nextCursor` がある場合のみ、`nextCursor` で 1 回追加入力する最小修正を実装（`userFriendsResolver.ts`）。limit 充足までのループ結合は行わない。

---

**識別子**: RC-19（GitHub id: 3214771200）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userFriends.ts:115`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+      friend_user_id: normalized.friend_user_id,
+    }
+  } catch {
+    throw new HttpsError('invalid-argument', 'Invalid cursor')
+  }
+}
+
+const encodeCursor = (cursor: UserFriendsListCursor | null): string | null => {
+  if (cursor == null) {
+    return null
+  }
+  return Buffer.from(JSON.stringify(cursor), 'utf8').toString('base64')
+}
+
+export const getUserFriends = onCall<GetUserFriendsRequest, Promise<GetUserFriendsResponse>>(
+  { region: 'asia-northeast1', invoker: 'public' },
+  async (request) => {
+    const input = GetUserFriendsRequestSchema.parse(request.data)
+    const targetUser = await getUser(input.target_user_id, false)
+    if (targetUser == null || targetUser.is_deleted) {
+      throw new HttpsError('not-found', '存在しないユーザーです')
+    }
+    // TODO Phase 2: 公開範囲設定（オプトアウト）を参照し、不可なら permission-denied
+
+    const limit = input.limit ?? 10
+    const sortBy: UserFriendsSortBy = input.sort_by ?? 'meet_count'
+    const decodedCursor = decodeCursor(input.cursor)
+
+    const page = await listUserFriends(input.target_user_id, sortBy, limit, decodedCursor)
+    if (page.friends.length === 0) {
+      return { friends: [], has_more: false, next_cursor: null }
+    }
+
+    const userMap = await getUsersByUserIds(page.friends.map((friend) => friend.id))
+
+    // event_history は表示用に新しい順で扱い、cap をかけたうえでイベントマスタからまとめて解決する
+    type MeetLogHistoryEntry = { event_id: string; community_id: string; event_at: number }
+    const cappedHistoryByFriend = new Map<string, MeetLogHistoryEntry[]>()
+    const eventRefsToFetch: { community_id: string; event_id: string }[] = []
+    for (const friend of page.friends) {
+      const normalizedHistory: MeetLogHistoryEntry[] = friend.event_history.map((entry) => ({
+        event_id: entry.event_id,
+        community_id: entry.community_id,
+        event_at: Number(entry.event_at),
+      }))
+      const sortedHistory = normalizedHistory.sort((a, b) => b.event_at - a.event_at)
+      const capped = sortedHistory.slice(0, MEET_LOG_MAX_PER_FRIEND)
+      cappedHistoryByFriend.set(friend.id, capped)
+      capped.forEach((entry) => {
+        eventRefsToFetch.push({ community_id: entry.community_id, event_id: entry.event_id })
+      })
+    }
+    const eventMap = await getEventsInCommunities(eventRefsToFetch)
+
+    const friends = page.friends
+      .map((friend) => {
+        const user = userMap.get(friend.id)
+        if (user == null || user.is_deleted) {
+          return null
+        }
+        const history = cappedHistoryByFriend.get(friend.id) ?? []
+        const meetLog: UserFriendMeetLogItem[] = history.map((entry) => {
+          const event = eventMap.get(getCommunityEventKey(entry.community_id, entry.event_id))
+          const isAvailable = event != null && !event.is_deleted
+          return {
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  非公開イベントの情報を meet_log で返さない**

`getUserFriends` は未ログインを含む公開 Callable ですが、`isAvailable` 判定が `!event.is_deleted` のみのため、`is_public === false` のイベントでも `event_name` と `community_account` をそのまま返してしまいます。これにより URL 限定公開/非公開イベントの名称と導線が第三者プロフィール閲覧経由で漏えいします。`is_public` も判定に含め、非公開イベントは `null` を返す必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: meet_log 経由の非公開イベント情報漏えい。

**判断結果**: ✅ 対応済み（2026-06-02 実装）

**判断理由（2026-05-31）**: `isEventVisibleToViewer` / `buildVisibleMeetLogItems` で非公開イベントを除外済み。当時の P1「漏えい防止」は満たしていた。

**判断理由（2026-06-02）**: §4.2.0 に沿い `buildMeetLogItems` で限定公開も含め `is_public` / `is_linkable` を付与。`FriendMeetLogDialog` は `row.is_linkable` でリンクのみ制御（名称は常に表示）。単体テスト（他者 `is_linkable: false`、本人 `true`）あり。

---

**識別子**: RC-20（GitHub id: 3232841618）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userProfile.ts:200`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+    .get()
+
+  const items: UserProfileEventPreviewItem[] = []
+  for (const doc of snapshot.docs) {
+    const data = doc.data()
+    const isPublic = data.is_public === true
+    const memberRefs = (data.members ?? []) as { id: string }[]
+    const memberIds = memberRefs.map((ref) => ref?.id ?? '').filter((id) => id !== '')
+    items.push({
+      event_id: String(data.event_id ?? doc.id),
+      community_id: String(data.community_id ?? ''),
+      community_account: String(data.community_account ?? ''),
+      event_name: String(data.event_name ?? ''),
+      event_start_datetime: toMillis(data.event_start_datetime),
+      event_end_datetime: toMillis(data.event_end_datetime),
+      is_public: isPublic,
+      is_visible_to_viewer: computeEventVisibleToViewer({ isPublic, viewerUid, targetUserId, memberIds }),
+    })
+  }
+  return items
+}
+
+const fetchCommunityPreviews = async (
+  arrayField: 'members' | 'managers',
+  targetUserId: string,
+  viewerUid: string | null,
+  limit: number,
+): Promise<UserProfileCommunityPreviewItem[]> => {
+  const db = getFirestore()
+  const userRef = getUserRef(targetUserId)
+  const snapshot = await db.collection('communities').where(arrayField, 'array-contains', userRef).limit(limit).get()
+
+  const items: UserProfileCommunityPreviewItem[] = []
+  for (const doc of snapshot.docs) {
+    const data = doc.data()
+    const isPublic = data.is_public === true
+    const memberRefs = (data.members ?? []) as { id: string }[]
+    const managerRefs = (data.managers ?? []) as { id: string }[]
+    const memberIds = memberRefs.map((ref) => ref?.id ?? '').filter((id) => id !== '')
+    const managerIds = managerRefs.map((ref) => ref?.id ?? '').filter((id) => id !== '')
+    items.push({
+      community_id: String(data.community_id ?? doc.id),
+      community_account: String(data.community_account ?? ''),
+      community_name: String(data.community_name ?? ''),
+      community_desc: String(data.community_desc ?? ''),
+      is_public: isPublic,
+      is_visible_to_viewer: computeCommunityVisibleToViewer({
+        isPublic,
+        viewerUid,
+        targetUserId,
+        memberIds,
+        managerIds,
+      }),
+    })
+  }
+  return items
+}
+
+const fetchFriendPreviews = async (targetUserId: string): Promise<UserProfileFriendPreviewItem[]> => {
+  const { friends } = await resolveUserFriendsList({
+    targetUserId,
+    sortBy: 'meet_count',
+    limit: PREVIEW_LIMITS.friends,
+    cursor: undefined,
+  })
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  友人プレビュー取得で meet_log 展開を無効化する**

プロフィールの友人プレビューは `meet_log` を返さないのに、ここでは `resolveUserFriendsList` の既定動作（友人ごとに最大20件の履歴をソートし、対応イベントを一括取得）をそのまま呼んでいます。その結果、`getUserProfilePreview` が不要な `events` 読み取りを大量に実行し、友人が多いユーザーでレイテンシと read コストが悪化します。プレビュー用途では `meetLogMaxPerFriend` を 0 にするなど、履歴解決をスキップすべきです。

Useful? React with 👍 / 👎.

**コメント要約**: プレビュー API の過剰な Firestore 読み取り。

**判断結果**: ✅ 対応済み

**判断理由**: 

---

**識別子**: RC-21（GitHub id: 3201105651）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/utils/friendsService.ts:138`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+const saveOrDeleteFriend = async (
+  uid: string,
+  friendUid: string,
+  history: EventHistoryEntry[],
+  createdAtMillis: number,
+  transaction: Transaction,
+) => {
+  const derived = recomputeDerived(history)
+  if (derived == null) {
+    await deleteUserFriend(uid, friendUid, transaction)
+    return false
+  }
+
+  await saveUserFriend(
+    uid,
+    new UserFriend(friendUid, {
+      first_met_at: derived.first_met_at,
+      last_met_at: derived.last_met_at,
+      meet_count: derived.meet_count,
+      event_history: history,
+      created_at: createdAtMillis,
+    }),
+    transaction,
+  )
+  return true
+}
+
+const applyForPair = async (
+  userA: string,
+  userB: string,
+  updater: (history: EventHistoryEntry[]) => EventHistoryEntry[],
+): Promise<number> => {
+  const db = getFirestore()
+  return db.runTransaction(async (transaction) => {
+    const friendAB = await getUserFriend(userA, userB, transaction)
+    const friendBA = await getUserFriend(userB, userA, transaction)
+
+    const nextAB = updater((friendAB?.event_history as EventHistoryEntry[] | undefined) ?? [])
+    const nextBA = updater((friendBA?.event_history as EventHistoryEntry[] | undefined) ?? [])
+
+    const savedAB = await saveOrDeleteFriend(userA, userB, nextAB, friendAB?.created_at ?? Date.now(), transaction)
+    const savedBA = await saveOrDeleteFriend(userB, userA, nextBA, friendBA?.created_at ?? Date.now(), transaction)
+    return (savedAB ? 1 : 0) + (savedBA ? 1 : 0)
+  })
+}
+
+export const applyOrdered = async ({ event_id, community_id, event_at, user_ids }: OrderedInput): Promise<number> => {
+  const pairs = buildPairs(user_ids)
+  let updatedDocs = 0
+  const entry = { event_id, community_id, event_at }
+
+  for (const [userA, userB] of pairs) {
+    updatedDocs += await applyForPair(userA, userB, (history) => upsertHistoryEntry(history, entry))
+  }
+
+  return updatedDocs
+}
+
+export const revokeOrdered = async ({ event_id, user_ids }: RevokeInput): Promise<number> => {
+  const pairs = buildPairs(user_ids)
+  let updatedDocs = 0
+  for (const [userA, userB] of pairs) {
+    updatedDocs += await applyForPair(userA, userB, (history) => removeHistoryEntry(history, event_id))
+  }
+  return updatedDocs
```

**レビュワーのコメント**（原文）:

[must] `applyOrdered` / `revokeOrdered` が「ユーザーの全ペア」ごとに Firestore transaction を逐次実行しており、参加者が多いイベント（仕様上 `event_max_people` のデフォルト 25 だと 300 ペア）では `onMemberOrderWritten`（timeout 60s）がタイムアウト・高コストになりやすいです。少なくとも対象ユーザーに関係するペアのみに絞る（特に REMOVE 側）/ ある程度の並列実行（上限付き）/ 書き込み方法の見直し（BulkWriter や集約形のフィールド設計など）で、最悪ケースの実行時間と Firestore 書き込み数を抑える対応を検討してください。

**コメント要約**: 友人グラフ更新の Firestore transaction 性能。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: Phase 1: 注文確定は `addEventToFriendHistoryForAnchor`（`orderConfirmedSideEffects.ts`）に変更し、新規確定ユーザー×既存参加者のみ更新（O(n²)→O(n)）。全ペア版 `addEventToFriendHistory` は backfill 専用。Phase 2: `runPairsWithConcurrency`（`PAIR_TX_CONCURRENCY=8`、`Promise.allSettled`）を add / addForAnchor / remove の 3 経路に適用。`friendsService.test.ts` で anchor・冪等・並列・部分失敗を検証。

---

**識別子**: RC-22（GitHub id: 3214481696）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/friendsService.ts:175`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+    const nextAB = updater((friendAB?.event_history as EventHistoryEntry[] | undefined) ?? [])
+    const nextBA = updater((friendBA?.event_history as EventHistoryEntry[] | undefined) ?? [])
+
+    const savedAB = await saveOrDeleteFriend(userA, userB, nextAB, friendAB?.created_at ?? Date.now(), transaction)
+    const savedBA = await saveOrDeleteFriend(userB, userA, nextBA, friendBA?.created_at ?? Date.now(), transaction)
+    return (savedAB ? 1 : 0) + (savedBA ? 1 : 0)
+  })
+}
+
+export const addEventToFriendHistory = async ({
+  event_id,
+  community_id,
+  event_at,
+  user_ids,
+}: AddEventToFriendHistoryInput): Promise<number> => {
+  const pairs = buildPairs(user_ids)
+  let updatedDocs = 0
+  const entry = { event_id, community_id, event_at }
+
+  for (const [userA, userB] of pairs) {
+    updatedDocs += await applyForPair(userA, userB, (history) => upsertHistoryEntry(history, entry))
+  }
+
+  return updatedDocs
+}
+
+export const removeEventFromFriendHistory = async ({
+  event_id,
+  user_ids,
+}: RemoveEventFromFriendHistoryInput): Promise<number> => {
+  const pairs = buildPairs(user_ids)
+  let updatedDocs = 0
+  for (const [userA, userB] of pairs) {
+    updatedDocs += await applyForPair(userA, userB, (history) => removeHistoryEntry(history, event_id))
+  }
+  return updatedDocs
+}
+
+type EventCursor = {
+  event_id: string
+}
+
+const encodeCursor = (cursor: EventCursor): string => Buffer.from(JSON.stringify(cursor), 'utf8').toString('base64')
+
+const decodeCursor = (token?: string): EventCursor | undefined => {
+  if (token == null || token === '') {
+    return undefined
+  }
+  try {
+    const parsed = JSON.parse(Buffer.from(token, 'base64').toString('utf8')) as EventCursor
+    return typeof parsed.event_id === 'string' ? parsed : undefined
+  } catch {
+    return undefined
+  }
+}
+
+export const runBackfill = async (input: BackfillUserFriendsRequest): Promise<BackfillUserFriendsResponse> => {
+  const db = getFirestore()
+  const dryRun = input.dry_run ?? true
+  const cursor = decodeCursor(input.resume_token)
+  const scanFrom = input.event_id_from ?? cursor?.event_id
+
+  let query: Query = db.collectionGroup('events').orderBy('event_id').limit(MAX_EVENTS_PER_RUN)
+  if (input.community_id != null && input.community_id !== '') {
+    query = query.where('community_id', '==', input.community_id)
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  backfill の community_id 条件に必要な複合インデックスを追加する**

`runBackfill` は `collectionGroup('events')` に対して `where('community_id', '==', ...)` と `orderBy('event_id')` を組み合わせていますが、この組み合わせ用の複合インデックスが差分内の `firestore.indexes.json` にありません（追加されているのは `community_account + event_id`）。そのため `community_id` を指定した backfill 実行時に Firestore の missing index エラーで停止し、機能要件どおりに絞り込み実行できません。

Useful? React with 👍 / 👎.

**コメント要約**: backfill 実行時の missing index エラー。

**判断結果**: ✅ 対応済み

**判断理由**: `firestore.indexes.json` に `events` collectionGroup の `community_id ASC` + `event_id ASC` 複合インデックスを追加。`community_id` 指定の backfill 実行前にインデックスのデプロイ・構築完了が必要。

---

**識別子**: RC-23（GitHub id: 3232501381）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/userProfileBackfill.ts:51`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,137 @@
+import { getFirestore, type Query } from 'firebase-admin/firestore'
+import type {
+  BackfillUserProfileCountsRequest,
+  BackfillUserProfileCountsResponse,
+} from '@shokujii/common/apis/userProfile.js'
+import { computeUserProfileCounts, hasCountsChanged, type UserProfileCounts } from './recountUserProfileCounts.js'
+import { updateUserProfileCounts } from '../stores/user.js'
+import { createModuleLogger } from './logger.js'
+
+const logger = createModuleLogger('userProfileBackfill')
+
+const MAX_USERS_PER_RUN = 10
+
+type UserCursor = {
+  user_id: string
+}
+
+const encodeCursor = (cursor: UserCursor): string => Buffer.from(JSON.stringify(cursor), 'utf8').toString('base64')
+
+const decodeCursor = (token?: string): UserCursor | undefined => {
+  if (token == null || token === '') {
+    return undefined
+  }
+  try {
+    const parsed = JSON.parse(Buffer.from(token, 'base64').toString('utf8')) as UserCursor
+    return typeof parsed.user_id === 'string' ? parsed : undefined
+  } catch {
+    return undefined
+  }
+}
+
+/**
+ * `users` を `user_id` 昇順に最大 `MAX_USERS_PER_RUN` 件スキャンし、
+ * 各ユーザーについて `computeUserProfileCounts` を呼び、差分があれば書き込む（`dry_run` の場合は書き込まない）。
+ *
+ * 1 回呼ぶたびに `resume_token` を返し、続きをこの呼び出しに渡すと再開できる。
+ * 仕様書 5.2.2 の運用フロー（dev で dry_run、prod で resume_token を回す）に対応する。
+ */
+export const runUserProfileBackfill = async (
+  input: BackfillUserProfileCountsRequest,
+): Promise<BackfillUserProfileCountsResponse> => {
+  const db = getFirestore()
+  const dryRun = input.dry_run ?? true
+  const cursor = decodeCursor(input.resume_token)
+  const scanFrom = input.user_id_from ?? cursor?.user_id
+
+  let query: Query = db.collection('users').orderBy('user_id').limit(MAX_USERS_PER_RUN)
+  if (scanFrom != null && scanFrom !== '') {
+    // user_id_from は inclusive にしたいが、resume_token から復元する場合は exclusive（次のページ）にしたい
+    // 単純化のため startAfter を使い、user_id_from は exclusive 扱いにする
+    query = query.startAfter(scanFrom)
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  user_id_from を含む開始条件でスキャンする**

`runUserProfileBackfill` は `user_id_from` を受け取ると `startAfter` を使うため、開始ユーザーが必ず 1 件スキップされます。`BackfillUserProfileCountsRequest` では `user_id_from` は「含む」契約になっているため、この実装だと部分実行や再実行時に境界ユーザーのカウント再集計が永続的に漏れます。`resume_token` 再開時と `user_id_from` 指定時で開始条件を分ける必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: backfill 再開位置の off-by-one。

**判断結果**: 👌 修正不要

**判断理由**: `common/src/apis/userProfile.ts` L142-143 で `user_id_from` は **exclusive（含まない）** と型コメントで明記済み。`userProfileBackfill.ts` L49-51 のコメントとも整合。Codex が参照した「含む」契約は現行スキーマと不一致。

---

**識別子**: RC-24（GitHub id: 3214481695）

**レビュワー**: Codex

**指摘箇所**: `user/src/pages/u/UserProfilePage.vue:24`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,407 @@
+<script setup lang="ts">
+import { db } from '@shokujii/base/firebase.js'
+import { doc, getDocs, orderBy, query, where, limit, collection } from 'firebase/firestore'
+import { useRoute } from 'vue-router'
+import UserBioPanel from '@shokujii/base/components/UserBioPanel.vue'
+import UserEventCard from '@shokujii/base/components/UserEventCard.vue'
+import CommunityCard from '@shokujii/base/components/CommunityCard.vue'
+import IncrementalLoader from '@shokujii/base/components/IncrementalLoader.vue'
+import FriendCard from '@shokujii/base/components/FriendCard.vue'
+import { useCommunityListStore } from '@shokujii/base/stores/communityList.js'
+import { useUserEventListByUserId } from '@shokujii/base/stores/userEventList.js'
+import { useUserStore } from '@shokujii/base/stores/user.js'
+import { mdiAccountGroup, mdiBullhornVariantOutline, mdiCalendarHeart, mdiHeartOutline } from '@mdi/js'
+import { type BokudeliEvent } from '@shokujii/base/stores/event.js'
+import { getCommunityPath, getEventPath, getReceiptPath } from '@/router/utils'
+import { cancelOrders as callCancelOrders } from '@shokujii/base/apis/stripe.js'
+import UserSuccessJoinEventDialog from '@shokujii/base/components/UserSuccessJoinEventDialog.vue'
+import { useNotification } from '@shokujii/base/composable/notification.js'
+import { useCurrentUserStore } from '@shokujii/base/stores/currentUser.js'
+import { useUserFriendsStore, type UserFriendsStore } from '@shokujii/base/stores/userFriends.js'
+import type { UserFriendsSortBy } from '@shokujii/common/apis/userFriends.js'
+
+const route = useRoute()
+const userId = route.params.userId as string
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  ルート専用でないページを pages 配下から移動する**

`UserProfilePage.vue` を `user/src/pages/u/` に置いたため、ファイルベースルーターで `/u/UserProfilePage` が新規公開されます（`typed-router.d.ts` にも生成済み）。このURLでは `route.params.userId` が存在しないのに、ここで必須扱いして後続の Firestore クエリやストア初期化に進むため、手動アクセス時にプロフィール画面が壊れます。ルーティング対象でない部品は `pages` 以外へ移して、`/u/:userId` 経由だけで使うようにした方が安全です。

Useful? React with 👍 / 👎.

**コメント要約**: ファイルベースルータによる意図しないルート公開。

**判断結果**: ✅ 対応済み

**判断理由**: `UserProfilePage.vue` を `[user/src/components/profile/UserProfilePage.vue](../../user/src/components/profile/UserProfilePage.vue)` へ移動し、`pages/u/` から削除。`typed-router.d.ts` は `/u/[userId]` のみとなり `/u/UserProfilePage` は消えた。

---

**識別子**: RC-25（GitHub id: 3201105609）

**レビュワー**: Copilot

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:31`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,407 @@
+<script setup lang="ts">
+import { db } from '@shokujii/base/firebase.js'
+import { doc, getDocs, orderBy, query, where, limit, collection } from 'firebase/firestore'
+import { useRoute } from 'vue-router'
+import UserBioPanel from '@shokujii/base/components/UserBioPanel.vue'
+import UserEventCard from '@shokujii/base/components/UserEventCard.vue'
+import CommunityCard from '@shokujii/base/components/CommunityCard.vue'
+import IncrementalLoader from '@shokujii/base/components/IncrementalLoader.vue'
+import FriendCard from '@shokujii/base/components/FriendCard.vue'
+import { useCommunityListStore } from '@shokujii/base/stores/communityList.js'
+import { useUserEventListByUserId } from '@shokujii/base/stores/userEventList.js'
+import { useUserStore } from '@shokujii/base/stores/user.js'
+import { mdiAccountGroup, mdiBullhornVariantOutline, mdiCalendarHeart, mdiHeartOutline } from '@mdi/js'
+import { type BokudeliEvent } from '@shokujii/base/stores/event.js'
+import { getCommunityPath, getEventPath, getReceiptPath } from '@/router/utils'
+import { cancelOrders as callCancelOrders } from '@shokujii/base/apis/stripe.js'
+import UserSuccessJoinEventDialog from '@shokujii/base/components/UserSuccessJoinEventDialog.vue'
+import { useNotification } from '@shokujii/base/composable/notification.js'
+import { useCurrentUserStore } from '@shokujii/base/stores/currentUser.js'
+import { useUserFriendsStore, type UserFriendsStore } from '@shokujii/base/stores/userFriends.js'
+import type { UserFriendsSortBy } from '@shokujii/common/apis/userFriends.js'
+
+const route = useRoute()
+const userId = route.params.userId as string
+
+const userIdRef = ref('')
+const fetchUser = async (identifier: string) => {
+  const userCollection = collection(db, 'users')
+  const queryById = query(userCollection, where('user_id', '==', identifier), limit(1))
+
+  try {
```

**レビュワーのコメント**（原文）:

[must] `route.params.userId as string` としているため、`userId` パラメータが無いルートで描画されると（例: `pages/u/UserProfilePage.vue` がページ扱いになり `/u/UserProfilePage` が生成された場合）`identifier` が `undefined` のまま Firestore クエリに渡り例外になり得ます。`UserProfilePage.vue` をルート生成対象から外す（配置変更/命名変更）か、`userId` が無い場合は早期に「存在しないユーザー」扱いで return するなどガードを入れてください。

**コメント要約**: RC-24 と同根のルーティング・ガード問題。

**判断結果**: ✅ 対応済み

**判断理由**: `defineProps<{ userId: string }>()` で `[userId].vue` から渡す。`userId === ''` 時は `fetchUser` をスキップ。`route.params` への依存を排除。

---

**識別子**: RC-26（GitHub id: 3232841612）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:129`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+    userIdRef.value = ''
+  }
+}
+await fetchUser(userId)
+
+/** プロフィール表示・Firestore・注文一覧で共通の Shokujii user_id（`fetchUser` 解決後を優先） */
+const profileUserId = userIdRef.value !== '' ? userIdRef.value : userId
+
+const notification = useNotification()
+
+const { t: $t, d } = useI18n()
+
+const { user, exists } = storeToRefs(useUserStore(profileUserId))
+const currentUserStore = useCurrentUserStore()
+const { user: loginUser } = storeToRefs(currentUserStore)
+
+const cancelLoadingEventId = ref<string | null>(null)
+/** UserEventCard のキャンセルダイアログの開閉（開いているイベントの event_id、閉じているときは null） */
+const cancelDialogEventId = ref<string | null>(null)
+
+const isOwner = computed(() => loginUser.value?.user_id === profileUserId)
+/** Firestore 上に users ドキュメントが無い、または退会済み */
+const isInvalidProfile = computed(() => exists.value === false || (user.value != null && user.value.is_deleted))
+const isProfileLoading = computed(() => profileUserId !== '' && exists.value === null)
+
+const userEventListStore = useUserEventListByUserId(profileUserId)
+const { events: userEvents, totalCount: userEventsTotalCount, orderStateByEventId } = storeToRefs(userEventListStore)
+
+const friendSortBy = ref<UserFriendsSortBy>('meet_count')
+const userFriendsByMeetCountStore = ref<UserFriendsStore | null>(null)
+const userFriendsByLastMetStore = ref<UserFriendsStore | null>(null)
+
+watch(
+  () => [profileUserId, exists.value, user.value?.is_deleted] as const,
+  ([pid, ex, deleted]) => {
+    userFriendsByMeetCountStore.value = null
+    userFriendsByLastMetStore.value = null
+    if (pid === '') return
+    if (ex === null) return
+    if (ex === false || deleted === true) return
+    userFriendsByMeetCountStore.value = useUserFriendsStore(pid, 'meet_count', 10)
+    userFriendsByLastMetStore.value = useUserFriendsStore(pid, 'last_met_at', 10)
+  },
+  { immediate: true },
+)
+const activeUserFriendsStore = computed(() =>
+  friendSortBy.value === 'meet_count' ? userFriendsByMeetCountStore.value : userFriendsByLastMetStore.value,
+)
+const activeFriends = computed(() => activeUserFriendsStore.value?.friends ?? [])
+
+/**
+ * URL の `?tab=xxx` を初期タブとして読み取り、未指定や本人以外で `orders` を直叩きされた場合は
+ * プロフィールタブにフォールバックする（仕様 4.2.5）。
+ */
+const initialTab: TabKey = (() => {
+  const raw = String(route.query.tab ?? '')
+  switch (raw) {
+    case TAB_FRIENDS:
+    case TAB_EVENTS:
+    case TAB_COMMUNITIES:
+    case TAB_FOODS:
+      return raw
+    case TAB_ORDERS:
+      return loginUser.value?.user_id === profileUserId ? TAB_ORDERS : TAB_PROFILE
+    default:
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  本人の orders 直リンクを認証後に反映する**

`initialTab` はセットアップ時に一度だけ評価されるため、`/u/<自分>?tab=orders` をリロードした時点で `loginUser` が未解決だと `TAB_PROFILE` に固定されます。その後 `isOwner` が `true` になっても、この watch は「owner でなくなった時に orders を閉じる」処理しか持たないため `orders` へ戻らず、本人の直リンク要件を満たせません。`isOwner`/`route.query.tab` の変化時に `TAB_ORDERS` へ再評価する分岐が必要です。

Useful? React with 👍 / 👎.

**コメント要約**: 本人 orders タブ直リンクの認証後反映。

**判断結果**: ✅ 対応済み

**判断理由**: `loginUser` / `route.query.tab` / `profileUserId` を watch し、`?tab=orders` かつ本人のとき `tabs` を `TAB_ORDERS` に再設定。非 owner で orders を閉じる既存 watch は維持。

---

**識別子**: RC-27（GitHub id: 3240875088）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:178`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+
+/**
+ * URL の `?tab=xxx` を初期タブとして読み取り、未指定や本人以外で `orders` を直叩きされた場合は
+ * プロフィールタブにフォールバックする（仕様 4.2.5）。
+ */
+const initialTab: TabKey = (() => {
+  const raw = String(route.query.tab ?? '')
+  switch (raw) {
+    case TAB_FRIENDS:
+    case TAB_EVENTS:
+    case TAB_COMMUNITIES:
+    case TAB_FOODS:
+      return raw
+    case TAB_ORDERS:
+      return loginUser.value?.user_id === profileUserId ? TAB_ORDERS : TAB_PROFILE
+    default:
+      return TAB_PROFILE
+  }
+})()
+const tabs = ref<TabKey>(initialTab)
+
+watch(
+  () => isOwner.value,
+  (owner) => {
+    if (!owner && tabs.value === TAB_ORDERS) {
+      tabs.value = TAB_PROFILE
+    }
+  },
+)
+
+const previewStore = useUserProfilePreviewStore(profileUserId)
+const { data: previewData, loading: previewLoading, error: previewError } = storeToRefs(previewStore)
+
+/** ともだち追読みのためのフック（タブ表示・ソート切替時に next を試す） */
+const isFriendTab = (tab: TabKey | null) => tab === TAB_FRIENDS
+
+const tryLoadMoreUserFriends = () => {
+  if (!isFriendTab(tabs.value)) return
+  const store = activeUserFriendsStore.value
+  if (store == null) return
+  if (store.hasMore && !store.loading) {
+    store.next()
+  }
+}
+
+watch(tabs, (tab) => {
+  if (!isFriendTab(tab)) return
+  void nextTick(() => {
+    tryLoadMoreUserFriends()
+    requestAnimationFrame(() => tryLoadMoreUserFriends())
+    window.setTimeout(tryLoadMoreUserFriends, 300)
+  })
+})
+
+watch(friendSortBy, () => {
+  if (!isFriendTab(tabs.value)) return
+  void nextTick(() => {
+    tryLoadMoreUserFriends()
+    requestAnimationFrame(() => tryLoadMoreUserFriends())
+    window.setTimeout(tryLoadMoreUserFriends, 300)
+  })
+})
+
+const visibleUserEvents = computed(() =>
+  userEvents.value.filter((event: BokudeliEvent) => isOwner.value || event.is_public),
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  非公開イベントを一覧タブから除外しない**

`visibleUserEvents` が他者閲覧時に `event.is_public` で絞り込んでいるため、プロフィール先頭のプレビューでは表示される非公開イベントが「もっと見る」遷移先のイベントタブで消えてしまいます。これだと件数表示・プレビューと一覧の整合が崩れ、非公開は「表示はするが遷移不可」にする設計を満たせません。非公開イベントは除外せず、リンク可否だけを分岐させる必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: 非公開イベントは表示しリンクのみ制御すべき（仕様 4.2.2）。

**判断結果**: ✅ 対応済み（2026-06-02 実装）

**判断理由（2026-05-31）**: 本人以外は非公開イベントを表示しない方針（2026-05-25）に合わせ `is_public` フィルタを維持。

**判断理由（2026-06-02）**: `UserProfilePage.vue` で `visibleUserEvents` を廃止し `userEvents` をそのまま表示。`canLinkToDetail` で `router-link` を分岐（プレビューは Callable の `is_linkable`）。

---

**識別子**: RC-28（GitHub id: 3240875092）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:197`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+const tabs = ref<TabKey>(initialTab)
+
+watch(
+  () => isOwner.value,
+  (owner) => {
+    if (!owner && tabs.value === TAB_ORDERS) {
+      tabs.value = TAB_PROFILE
+    }
+  },
+)
+
+const previewStore = useUserProfilePreviewStore(profileUserId)
+const { data: previewData, loading: previewLoading, error: previewError } = storeToRefs(previewStore)
+
+/** ともだち追読みのためのフック（タブ表示・ソート切替時に next を試す） */
+const isFriendTab = (tab: TabKey | null) => tab === TAB_FRIENDS
+
+const tryLoadMoreUserFriends = () => {
+  if (!isFriendTab(tabs.value)) return
+  const store = activeUserFriendsStore.value
+  if (store == null) return
+  if (store.hasMore && !store.loading) {
+    store.next()
+  }
+}
+
+watch(tabs, (tab) => {
+  if (!isFriendTab(tab)) return
+  void nextTick(() => {
+    tryLoadMoreUserFriends()
+    requestAnimationFrame(() => tryLoadMoreUserFriends())
+    window.setTimeout(tryLoadMoreUserFriends, 300)
+  })
+})
+
+watch(friendSortBy, () => {
+  if (!isFriendTab(tabs.value)) return
+  void nextTick(() => {
+    tryLoadMoreUserFriends()
+    requestAnimationFrame(() => tryLoadMoreUserFriends())
+    window.setTimeout(tryLoadMoreUserFriends, 300)
+  })
+})
+
+const visibleUserEvents = computed(() =>
+  userEvents.value.filter((event: BokudeliEvent) => isOwner.value || event.is_public),
+)
+
+const memberCommunityListStore = useCommunityListStore(
+  [where('members', 'array-contains', doc(db, 'users', profileUserId)), orderBy('community_num_members', 'desc')],
+  5,
+)
+
+const managerCommunityListStore = useCommunityListStore(
+  [where('managers', 'array-contains', doc(db, 'users', profileUserId)), orderBy('community_num_members', 'desc')],
+  5,
+)
+
+const memberCommunities = computed(() =>
+  (memberCommunityListStore.communityStores ?? []).flatMap((communityStore) => {
+    if (communityStore.community == null || communityStore.members == null) {
+      return []
+    }
+    if (!isOwner.value && !communityStore.community.is_public) {
+      return []
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  非公開コミュニティをコミュニティタブで落とさない**

コミュニティタブのデータ生成で、他者閲覧時に `is_public === false` を丸ごと `return []` しているため、プロフィールプレビューに表示された非公開コミュニティが「もっと見る」先では欠落します。公開範囲の扱いがタブ間で不一致になり、非公開項目の閲覧体験が壊れるので、一覧から除外せずにリンク可否のみ制御する実装へ揃えるべきです（同じ条件が管理コミュニティ側にもあります）。

Useful? React with 👍 / 👎.

**コメント要約**: 非公開コミュニティは表示しリンクのみ制御すべき（仕様 4.2.3）。

**判断結果**: ✅ 対応済み（2026-06-02 実装）

**判断理由（2026-06-02）**: `memberCommunities` / `managerCommunities` の他者向け `is_public` による `return []` を削除。`canLinkToDetail(community.is_public)` でリンクのみ制御。

---

**識別子**: RC-29（GitHub id: 3247944584）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:931`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+            </v-row>
+            <v-row v-if="pagedFoods.length > 0" class="justify-center mt-2">
+              <v-col cols="auto">
+                <IncrementalLoader
+                  :loaded-count="pagedFoods.length"
+                  :total-count="foodHasMore ? Number.MAX_SAFE_INTEGER : pagedFoods.length"
+                  @load="userFoodsStore.next()"
+                />
+              </v-col>
+            </v-row>
+          </template>
+        </v-window-item>
+
+        <v-window-item v-if="isOwner" :value="TAB_ORDERS">
+          <div v-if="visibleUserEvents.length === 0" class="text-body-1 text-medium-emphasis pa-4">
+            {{ $t('user_profile.empty.orders') }}
+          </div>
+          <v-row>
+            <v-col v-for="event in visibleUserEvents" :key="`order_${event.event_id}`" sm="12" md="6" lg="4" cols="12">
+              <div class="event-card">
+                <router-link :to="getEventPath(event.community_account, event.event_id)">
+                  <UserEventCard
+                    v-model:cancel-dialog-event-id="cancelDialogEventId"
+                    :orders="orderStateByEventId[event.event_id]?.orders ?? []"
+                    :orders-loading="orderStateByEventId[event.event_id]?.loading ?? false"
+                    :orders-error="orderStateByEventId[event.event_id]?.error != null"
+                    :event="event"
+                    :isOwner="isOwner"
+                    :cancelLoading="cancelLoadingEventId === event.event_id"
+                    @downloadInvoice="downloadReceipt"
+                    @cancel="(orderIds: string[]) => cancel(orderIds, event.community_id, event.event_id)"
+                    @retry-orders="(eid: string) => userEventListStore.reloadOrdersForEvent(eid)"
+                  />
+                </router-link>
+
+                <div
+                  v-if="cancelLoadingEventId === event.event_id"
+                  class="progress-container d-flex justify-center align-center"
+                >
+                  <v-progress-circular :indeterminate="true" size="large" />
+                </div>
+              </div>
+            </v-col>
+          </v-row>
+          <v-row class="justify-center">
+            <v-col cols="auto">
+              <IncrementalLoader
+                :loaded-count="userEvents.length"
+                :total-count="userEventsTotalCount ?? Number.MAX_SAFE_INTEGER"
+                @load="userEventListStore.next()"
+              />
+            </v-col>
+          </v-row>
+        </v-window-item>
+      </v-window>
+    </v-col>
+  </v-row>
+  <!-- 存在しない eventId や communityAccount にも反応してしまう。 TODO 修正 -->
+  <!-- prettier-ignore -->
+  <UserSuccessJoinEventDialog
+    v-if="$route.query.eventId != null || $route.query.communityAccount != null"
+    v-model="isUserSuccessJoinEventDialogVisible"
+    :event-id="(($route.query.eventId ?? '') as string)"
+    :community-account="(($route.query.communityAccount ?? '') as string)"
+    :is-posted="($route.query.isPosted === 'true')"
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  参加完了ダイアログへ session/user を渡す**

`UserSuccessJoinEventDialog` は `sessionId` と `userId` が渡される前提で「注文読込中 (`isLoadingOrder`)」や「processing 決済中 (`isProcessing`)」を判定していますが、この呼び出しでは両方の props が渡されていないため、Stripe Checkout の `session_id` 付きリダイレクト直後でも処理中判定が常に無効になります。結果として、遅延決済中なのに通常の参加完了UIやシェア導線が即表示され、誤案内になります（旧実装の `/u/[userId].vue` では `:session-id` と `:user-id` を渡していました）。

Useful? React with 👍 / 👎.

**コメント要約**: 参加完了ダイアログの props 欠落によるリグレッション。

**判断結果**: ✅ 対応済み

**判断理由**: `UserSuccessJoinEventDialog` に `:session-id`（`route.query.session_id`）と `:user-id`（`profileUserId`）を復元。`isLoadingOrder` / `isProcessing` が Checkout 直後に再び有効。

---

## 評価セッション（2026-05-31・feat/1248 コードレビュー追記）

- **評価者**: Cursor Agent（`/shokujii-code-review` + 仕様 `02_マイページ.md`）
- **対象**: `origin/development...HEAD`（ブランチ `feat/1248`）
- **目的**: 仕様変更（友人カードは日付のみ・同席履歴は `FriendMeetLogDialog` + `getUserFriendMeetLog`）後の不要コード削除候補の整理、既存 RC の状態見直し

### 既存 RC の見直し（追記のみ・本文は上記のまま）


| RC    | 見直し後    | 理由                                                                                                                                                           |
| ----- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RC-19 | ✅ 対応済み | §4.2.0: `buildMeetLogItems` + `is_linkable`、`FriendMeetLogDialog` |
| RC-20 | ✅ 対応済み  | 一覧 API で events 読み取り廃止（RC-30 と同実装）                                                                                                                           |
| RC-21 | ✅ 対応済み  | `addEventToFriendHistoryForAnchor` + `runPairsWithConcurrency`（RC-49 と同根）                                                                                    |
| RC-49 | ✅ 対応済み  | RC-21 と同一実装                                                                                                                                                 |
| RC-27 | ✅ 対応済み | §4.2.0: `visibleUserEvents` 削除 + `canLinkToDetail` |
| RC-28 | ✅ 対応済み | §4.2.0: コミュニティタブの `is_public` 除外削除 + リンク分岐 |


---

**識別子**: RC-30

**レビュワー**: Cursor Agent（shokujii-code-review）

**該当コード（参照）**: `functions/default/src/utils/userFriendsResolver.ts`（一覧の events 解決 — 2026-05-31 削除済み）

**レビュワーのコメント**（原文）:

[must] `meetLogMaxPerFriend === 0`（`getUserFriends` / `fetchFriendPreviews` の既定）でも `userFriendsResolver.ts` が全友人の `event_history` 分を `getEventsInCommunities` で解決し、`first_meet_event` / `last_meet_event` を返している。一方 UI（`FriendCard.vue`）は `first_met_at` / `last_met_at` のみ使用し、`base` / `user` いずれも `first_meet_event` / `last_meet_event` を参照しない。仕様上、イベント名・リンクは `getUserFriendMeetLog` ダイアログに集約済み。

**コメント要約**: 友人一覧 API の過剰 Firestore read と未使用レスポンスフィールド。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `userFriendsResolver.ts` を一覧専用に簡略化。`common/src/apis/userFriends.ts` の `UserFriendListItem` から `first_meet_event` / `last_meet_event` / `meet_log` を削除。

---

**識別子**: RC-31

**レビュワー**: Cursor Agent（shokujii-code-review）

**該当コード（参照）**: 同上 + `meetLogMaxPerFriend` 分岐・locale

**レビュワーのコメント**（原文）:

[P2] 折りたたみ inline `meet_log` 用の `meetLogMaxPerFriend > 0` 分岐（`userFriendsResolver.ts` L102–109, L137–146）と `UserFriendListItem.meet_log` は、リポジトリ内で `meetLogMaxPerFriend` に 0 以外を渡す呼び出しがない。旧 UI 用 locale `friend_meet_log_title` / `friend_meet_log_expand` / `friend_meet_log_collapse`（`ja.ts` / `en.ts`）も Vue から未参照。

**コメント要約**: 仕様変更後のデッドコード・未使用 i18n。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `meetLogMaxPerFriend` 引数と `cap > 0` 分岐を削除。locale から `friend_meet_log_title` / `expand` / `collapse` を削除。テストを一覧で `getEventsInCommunities` 未呼び出しに更新。

---

**識別子**: RC-32

**レビュワー**: Cursor Agent（shokujii-code-review）

**レビュワーのコメント**（原文）:

[fyi] RC-20 の「判断理由」に「`cap === 0` のとき `getEventsInCommunities` をスキップ」とあるが、2026-05-31 時点の `userFriendsResolver.ts` ではスキップしていない。ドキュメントと実装の乖離。

**コメント要約**: RC-20 評価文の記載修正。

**判断結果**: ✅ 対応済み（2026-05-31）

**判断理由**: RC-20 / RC-30 / RC-31 実装完了に伴い、RC-20 本文とチェックリストを ✅ に更新済み。

---

**識別子**: RC-33

**レビュワー**: Cursor Agent（shokujii-code-review）

**該当コード（参照）**: `userProfile.ts` re-export

**レビュワーのコメント**（原文）:

[nits] `functions/default/src/userProfile.ts` L26 の `export { computeProfileItemVisibleToViewer }` は他モジュールから未使用。テストは `utils/profileItemVisibility.js` を直接 import。

**コメント要約**: 不要 re-export。

**判断結果**: ✅ Phase 1 対応済み（2026-05-31 実装）

**判断理由**: `userProfile.ts` L26 の `export { computeProfileItemVisibleToViewer }` を削除。Firestore 直クエリの store 化（Phase 2）は別 Issue。

---

**識別子**: RC-34

**レビュワー**: Cursor Agent（shokujii-code-review）

**該当コード（参照）**: `FriendCard.vue` 日付

**レビュワーのコメント**（原文）:

[nits] `FriendCard.vue` の `formatDate` が `new Date(epochMillis).toLocaleDateString('ja-JP')`。チェックリストでは日付は luxon 推奨。

**コメント要約**: 日付フォーマットの規約。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `FriendCard.vue` で `convertToDate`（`common/src/utils/datetime.ts`・luxon）を使用。

---

**識別子**: RC-35

**レビュワー**: Cursor Agent（shokujii-code-review）

**該当コード（参照）**: `UserProfilePage.vue` watch

**レビュワーのコメント**（原文）:

[imo] `UserProfilePage.vue` の友人タブ `watch`（`tabs` / `friendSortBy`）で `nextTick` + `requestAnimationFrame` + `setTimeout(300)` による `tryLoadMoreUserFriends` 多重呼び出し。`IncrementalLoader` 改善後も残存。

**コメント要約**: 追読みトリガーの冗長さ。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `UserProfilePage.vue` の `tabs` / `friendSortBy` watch を `nextTick` + `tryLoadMoreUserFriends` 1 回に統一。`requestAnimationFrame` / `setTimeout(300)` を削除。

---

**識別子**: RC-36（Firestore store 化・旧 RC-33 本文の Phase 2）

**レビュワー**: Cursor Agent（shokujii-code-review）

**該当コード（参照）**: `userProfile.ts` Firestore 直クエリ

**レビュワーのコメント**（原文）:

[P2] `userProfile.ts` が `getFirestore()` で `collectionGroup('events')` / `member_orders` / `communities` を直接クエリ。AGENTS / shokujii-firestore では Functions も store 経由が原則。

**コメント要約**: Firestore 操作の store 未経由。

**判断結果**: 🟡 提案（Phase 2・別 Issue）

**判断理由**: 新規 store 化または既存 store 拡張を検討。RC-33 Phase 1（re-export 削除）のみ本対応に含めた。

---

**識別子**: RC-37

**レビュワー**: Cursor Agent（shokujii-code-review）

**レビュワーのコメント**（原文）:

[imo] `FriendCard` の `isSingleMeet` は `meet_count <= 1 || first_met_at === last_met_at` のみ。旧修正点メモでは first/last が同一 `event_id` の場合も 1 行表示とあった。

**コメント要約**: 単回同席表示条件のエッジケース。

**判断結果**: 🟡 提案

**判断理由**: 同一日時・別イベント等で表示が 2 行になる可能性。仕様どおりなら `first_meet_event` / `last_meet_event` ではなく history の `event_id` 比較を検討（ただし RC-30 で一覧 API からイベント解決を外す場合は、サブコレのみで判定できるか要設計）。

---

**識別子**: RC-38

**レビュワー**: Cursor Agent（shokujii-code-review）

**レビュワーのコメント**（原文）:

[fyi] `FriendMeetLogDialog.vue` / `FriendCard.vue` の `getUserPath` / `getEventPath` は `@/router/utils` 依存。base 新規コンポーネントとしてはチェックリスト「ルートは emit で汎化」に反するが、既存 base コンポーネントでも同パターン多数（既存負債）。

**コメント要約**: base のルーティング依存。

**判断結果**: 🟢 任意

**判断理由**: 本 PR 単体での必須修正ではない。将来 `emit` + user 側リンク生成へ寄せる余地あり。

---

**識別子**: RC-39

**レビュワー**: Cursor Agent（shokujii-code-review）

**レビュワーのコメント**（原文）:

[fyi] `UserProfilePage.vue` L320 の `console.error` は user パッケージのクライアントエラー通知方針と整合するか要確認（Functions は `createModuleLogger` 必須だがフロントは別方針の可能性）。

**コメント要約**: エラーログ出力。

**判断結果**: 🟢 任意

**判断理由**: プロジェクトのグローバルエラーハンドラ / `reportClientError` へ寄せるかは別 Issue でも可。

---

**識別子**: RC-40

**レビュワー**: Cursor Agent（shokujii-code-review）

**レビュワーのコメント**（原文）:

[fyi] 削除してよいもの: `FriendMeetLogDialog` / `getUserFriendMeetLog` / `ProfileEventCard` / `profileItemVisibility.ts` は仕様どおり維持。`documents/レビューコメント/pr-1985.md` 自体は実行ロジックではないが PR に同居している（`[doc] #1985` コミット）。

**コメント要約**: 削除対象の整理（実装ファイル）。

**判断結果**: 👌 修正不要（本ファイルはレビュー記録として維持）

**判断理由**: ### 推奨対応順序（実装時）

1. RC-30 + RC-31（resolver 簡略化・型・locale・テスト）
2. RC-32（RC-20 を ✅ に更新）
3. RC-33〜36（任意・提案）

---

## 評価セッション（2026-05-31・Codex 再レビュー追記）

- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **対象**: PR #1985 アクティブインラインコメント（`position != null`）31 件。Outdated 0 件
- **トップレベル除外**: 13 件（レビュー依頼・Codex 接続案内）
- **備考**: RC-41〜54 は GitHub 上の新規 Codex インライン指摘（2026-05-31 時点）。RC-1〜40 は上記セクション参照

### 推奨対応順序（RC-41〜54）

1. **データ品質**: RC-50（退会時 friend_count）— 🟡 未対応のまま
2. ~~RC-46 / RC-47 / RC-48 / RC-52 / RC-53 / RC-54~~ — ✅ 対応済み（RC-47/48 は 2026-06-02 §4.2.0 で再実装）
3. **セキュリティ**: RC-58（退会友人 meet log）— 🚨 未対応

---

**識別子**: RC-41（GitHub id: 3297987316）

**レビュワー**: Codex

**指摘箇所**: `bokudeli-event-batch:1`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1 @@
+/Users/yasukawanaohiro/Github/bokudeli-event-batch
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  開発環境依存のシンボリックリンクを含めない**

この追加は `bokudeli-event-batch` をローカル絶対パス (`/Users/...`) へ向けたシンボリックリンクとしてコミットしており、他メンバーやCI環境では参照不能になります。結果として環境によってファイル構成が壊れ、再現性のある開発・検証ができなくなるため、リポジトリに含めるべきではありません（必要なら実体をリポジトリ管理するか、セットアップ手順として外出ししてください）。

Useful? React with 👍 / 👎.

**コメント要約**: ローカル絶対パス symlink は CI・他開発者で壊れる。

**判断結果**: ✅ 対応済み

**判断理由**: リポジトリから `bokudeli-event-batch` の絶対パス symlink を削除済み。他メンバー・CI で dangling symlink にならない。

---

**識別子**: RC-42（GitHub id: 3329888509）

**レビュワー**: Codex

**指摘箇所**: `bokudeli-event-batch:1`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1 @@
+/Users/yasukawanaohiro/Github/bokudeli-event-batch
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  開発機固有の絶対パスシンボリックリンクを削除する**

[must] 追加されたシンボリックリンクは `/Users/yasukawanaohiro/Github/bokudeli-event-batch` という特定開発機の絶対パスを参照しています。リポジトリ全体を `rg -n "bokudeli-event-batch" -uuu` で確認したところ、既存ドキュメントは別リポジトリの URL を参照しており、このローカルリンクを必要とするコードはありません。他の開発環境や CI では必ず dangling symlink になるため、コミット対象から削除してください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-41 と重複する symlink 削除依頼。

**判断結果**: ✅ 対応済み

**判断理由**: RC-41 と同一対応で解消。

---

**識別子**: RC-43（GitHub id: 3328599645）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/communityMemberCountsTrigger.ts:30`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,39 @@
+import { onDocumentWritten } from 'firebase-functions/v2/firestore'
+import { createModuleLogger } from './utils/logger.js'
+import { recountUserProfileCounts } from './utils/recountUserProfileCounts.js'
+
+const logger = createModuleLogger('communityMemberCountsTrigger')
+
+/**
+ * `communities/{communityId}/members/{userId}` の onDocumentWritten で、
+ * マイページ用の `joined_community_count` / `managed_community_count` を再集計する。
+ *
+ * - 参加・脱退（doc 追加・削除）と、`roles` の変更（manager 付与・剥奪）の両方を同 Trigger でカバーする
+ * - `community.members` / `community.managers` 配列フィールドは別 Trigger（legacy `on_write_community_members`）で同期されているため、
+ *   ここでは `recountUserProfileCounts` 内の `array-contains` クエリに任せる
+ *
+ * 注意: 集計時点で配列フィールドの同期が遅れている場合、1 トリガー周期だけ古い値が書き込まれる可能性があるが、
+ * 続く書き込みで自然に整合する（仕様書 5.1.4 整合性方針 / 5.1.3 注意）。
+ */
+export const onCommunityMemberWritten = onDocumentWritten(
+  {
+    document: 'communities/{communityId}/members/{userId}',
+    region: 'asia-northeast1',
+  },
+  async (event) => {
+    const userId = event.params.userId
+    if (userId === '') {
+      logger.warn('Empty userId, skip recount')
+      return
+    }
+    try {
+      await recountUserProfileCounts(userId)
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  コミュニティ配列の同期完了後にカウントを再集計する**

`communities/{communityId}/members/{userId}` の書き込み時は、この新規トリガーと legacy の `functions/legacy/src/community-members.js:47-64` が独立して並行実行されます。ここで先に再集計が走ると、`recountUserProfileCounts.ts:59-60` は legacy トリガーが更新する前の `community.members` / `community.managers` を数えるため、参加・退会や manager 権限変更直後の `joined_community_count` / `managed_community_count` が古い値で保存されます。legacy 側の親ドキュメント更新ではこのトリガーは再発火しないので、単発の変更では次回更新まで不整合が残ります。親配列の同期後に再集計するか、members サブコレクションを直接集計してください。

Useful? React with 👍 / 👎.

**コメント要約**: legacy とのレースでプロフィール件数が一時不整合。

**判断結果**: ✅ 対応済み

**判断理由**: `stores/community.ts` に `countJoinedCommunitiesForUser` / `countManagedCommunitiesForUser`（members collection group）を追加し、`recountUserProfileCounts` から利用。`firestore.indexes.json` に managed 用 index を追加。`communityMemberCountsTrigger` コメントを更新。

---

**識別子**: RC-44（GitHub id: 3329888503）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/communityMemberCountsTrigger.ts:30`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,39 @@
+import { onDocumentWritten } from 'firebase-functions/v2/firestore'
+import { createModuleLogger } from './utils/logger.js'
+import { recountUserProfileCounts } from './utils/recountUserProfileCounts.js'
+
+const logger = createModuleLogger('communityMemberCountsTrigger')
+
+/**
+ * `communities/{communityId}/members/{userId}` の onDocumentWritten で、
+ * マイページ用の `joined_community_count` / `managed_community_count` を再集計する。
+ *
+ * - 参加・脱退（doc 追加・削除）と、`roles` の変更（manager 付与・剥奪）の両方を同 Trigger でカバーする
+ * - `community.members` / `community.managers` 配列フィールドは別 Trigger（legacy `on_write_community_members`）で同期されているため、
+ *   ここでは `recountUserProfileCounts` 内の `array-contains` クエリに任せる
+ *
+ * 注意: 集計時点で配列フィールドの同期が遅れている場合、1 トリガー周期だけ古い値が書き込まれる可能性があるが、
+ * 続く書き込みで自然に整合する（仕様書 5.1.4 整合性方針 / 5.1.3 注意）。
+ */
+export const onCommunityMemberWritten = onDocumentWritten(
+  {
+    document: 'communities/{communityId}/members/{userId}',
+    region: 'asia-northeast1',
+  },
+  async (event) => {
+    const userId = event.params.userId
+    if (userId === '') {
+      logger.warn('Empty userId, skip recount')
+      return
+    }
+    try {
+      await recountUserProfileCounts(userId)
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  親コミュニティ同期後にプロフィール件数を再集計する**

[must] メンバー追加・削除・ロール変更時、この Trigger と legacy の `on_write_community_members` は同じ `communities/{communityId}/members/{userId}` 書き込みから並列に起動します。`recountUserProfileCounts` は親コミュニティの `members` / `managers` 配列を `array-contains` で数えますが、legacy 側が親ドキュメントを更新する前にこの行が実行されると古い件数が保存されます。legacy 側の後続処理は親ドキュメントだけを書き換えるため、この Trigger は再発火せず、次回のメンバー変更まで誤った件数が残ります。親配列の同期後に再集計するか、集計元を `members` サブコレクションに変更してください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-43 重複。

**判断結果**: ✅ 対応済み

**判断理由**: RC-43 と同一対応。

---

**識別子**: RC-45（GitHub id: 3294000127）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/orderCanceledSideEffects.ts:43`

**該当コード（レビュー時点の diff）**:

```diff
@@ -30,4 +33,33 @@ export async function applyOrderCanceledSideEffects(params: { event: ShokujiiEve
       userId,
     })
   }
+
+  // 友達グラフから当該イベントの履歴を取り除く（キャンセルユーザーと各相手のペアのみ。残存参加者同士は削除しない）
+  try {
+    const memberIds = await getMemberIds(communityId, eventId)
+    await removeEventFromFriendHistory({
+      event_id: eventId,
+      anchor_user_id: userId,
+      counterpart_user_ids: memberIds.filter((id) => id !== userId),
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  部分キャンセル時の友人履歴削除を回避する**

`cancelOrders` は `order_ids` 単位で実行されるため、同一ユーザーが同じイベントに複数注文している場合に一部だけキャンセルされるケースがあります。この実装は注文の残存有無を確認せず常に `removeEventFromFriendHistory` を実行するため、ユーザーがまだ `ordered` の注文を持ってイベント参加中でも当該イベントの同席履歴が消え、`meet_count` や友人一覧が過小になります。キャンセル後にそのユーザーの `ordered` 注文が 0 件になったときだけ履歴削除する分岐が必要です。

Useful? React with 👍 / 👎.

**コメント要約**: 一部注文キャンセルで友人履歴を誤削除。

**判断結果**: ✅ 対応済み

**判断理由**: `orderCanceledSideEffects.ts` で `getOrders(..., 'ordered')` を当該 `userId` でフィルタし、残存 0 件のときだけ `removeEventFromFriendHistory` を実行。`orderCanceledSideEffects.test.ts` を追加。

---

**識別子**: RC-46（GitHub id: 3329888507）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userProfile.ts:112`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+  updated_at: z.number().int().nonnegative(),
+  order_id: z.string().min(1),
+})
+type UserFoodsCursor = z.infer<typeof FoodCursorSchema>
+
+/**
+ * 各プレビューの最大件数（仕様書 4.2 のプレビュー目安）。
+ * 必要に応じてフラットな optional 引数を追加する余地があるため、サーバ既定として定数で持つ。
+ */
+const PREVIEW_LIMITS = {
+  events: 12,
+  friends: 30,
+  joinedCommunities: 12,
+  managedCommunities: 12,
+  foods: 12,
+  orders: 5,
+} as const
+
+/**
+ * Firestore Timestamp / number / undefined を安全に millis に変換する。
+ * 取得できないときは fallback を返す。
+ */
+const toMillis = (value: unknown, fallback = 0): number => {
+  if (value == null) return fallback
+  if (typeof value === 'number') return value
+  if (value instanceof Timestamp) return value.toMillis()
+  if (typeof (value as { toMillis?: unknown }).toMillis === 'function') {
+    return (value as { toMillis: () => number }).toMillis()
+  }
+  return fallback
+}
+
+const buildPublicProfile = (user: {
+  user_id: string
+  user_name: string
+  user_description: string
+  user_image_url: string
+  user_sns_facebook: string
+  user_sns_facebook_name: string
+  user_sns_twitter: string
+  user_sns_instagram: string
+  user_sns_website: string
+  is_deleted: boolean
+}): UserProfilePublicProfile => ({
+  user_id: user.user_id,
+  user_name: user.user_name,
+  user_description: user.user_description,
+  user_image_url: user.user_image_url,
+  user_sns_facebook: user.user_sns_facebook,
+  user_sns_facebook_name: user.user_sns_facebook_name,
+  user_sns_twitter: user.user_sns_twitter,
+  user_sns_instagram: user.user_sns_instagram,
+  user_sns_website: user.user_sns_website,
+  is_deleted: user.is_deleted,
+})
+
+const fetchEventPreviews = async (
+  targetUserId: string,
+  viewerUid: string | null,
+): Promise<UserProfileEventPreviewItem[]> => {
+  const db = getFirestore()
+  const userRef = getUserRef(targetUserId)
+  const snapshot = await db
+    .collectionGroup('events')
+    .where('members', 'array-contains', userRef)
```

**レビュワーのコメント**（原文）:

**![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)****  Firestore クエリを store 経由に移動する**

[must] `AGENTS.md` の「DB 操作は必ず store 経由」「xxxRef は必ず withConverter 付き」という必須ルールに反しています。この直接クエリは `doc.data()` を未検証のままプロフィール API のレスポンスへ変換するため、既存の store / converter が担う Zod バリデーションを迂回します。同じファイル内の communities・member_orders の直接クエリも含め、`functions/default/src/stores/` に取得関数を追加して converter 付きで呼び出してください。

Useful? React with 👍 / 👎.

**コメント要約**: Zod バリデーションを迂回する DB アクセス。

**判断結果**: 🟡 修正提案（RC-36 Phase 2・別 Issue。2026-05-31 再評価）

**判断理由**: AGENTS / shokujii-firestore どおり修正すべき。RC-33 Phase 1（re-export 削除）・RC-46（`userProfile` 主要クエリの store 化）は本 PR で対応済み。残りの store 化は意図的に後ろ倒し。

---

**識別子**: RC-47（GitHub id: 3329884054）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userProfile.ts:115`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+type UserFoodsCursor = z.infer<typeof FoodCursorSchema>
+
+/**
+ * 各プレビューの最大件数（仕様書 4.2 のプレビュー目安）。
+ * 必要に応じてフラットな optional 引数を追加する余地があるため、サーバ既定として定数で持つ。
+ */
+const PREVIEW_LIMITS = {
+  events: 12,
+  friends: 30,
+  joinedCommunities: 12,
+  managedCommunities: 12,
+  foods: 12,
+  orders: 5,
+} as const
+
+/**
+ * Firestore Timestamp / number / undefined を安全に millis に変換する。
+ * 取得できないときは fallback を返す。
+ */
+const toMillis = (value: unknown, fallback = 0): number => {
+  if (value == null) return fallback
+  if (typeof value === 'number') return value
+  if (value instanceof Timestamp) return value.toMillis()
+  if (typeof (value as { toMillis?: unknown }).toMillis === 'function') {
+    return (value as { toMillis: () => number }).toMillis()
+  }
+  return fallback
+}
+
+const buildPublicProfile = (user: {
+  user_id: string
+  user_name: string
+  user_description: string
+  user_image_url: string
+  user_sns_facebook: string
+  user_sns_facebook_name: string
+  user_sns_twitter: string
+  user_sns_instagram: string
+  user_sns_website: string
+  is_deleted: boolean
+}): UserProfilePublicProfile => ({
+  user_id: user.user_id,
+  user_name: user.user_name,
+  user_description: user.user_description,
+  user_image_url: user.user_image_url,
+  user_sns_facebook: user.user_sns_facebook,
+  user_sns_facebook_name: user.user_sns_facebook_name,
+  user_sns_twitter: user.user_sns_twitter,
+  user_sns_instagram: user.user_sns_instagram,
+  user_sns_website: user.user_sns_website,
+  is_deleted: user.is_deleted,
+})
+
+const fetchEventPreviews = async (
+  targetUserId: string,
+  viewerUid: string | null,
+): Promise<UserProfileEventPreviewItem[]> => {
+  const db = getFirestore()
+  const userRef = getUserRef(targetUserId)
+  const snapshot = await db
+    .collectionGroup('events')
+    .where('members', 'array-contains', userRef)
+    .where('is_deleted', '==', false)
+    .orderBy('event_start_datetime', 'desc')
+    .limit(PREVIEW_LIMITS.events)
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  非公開項目を除外してからイベントプレビューを最大件数まで埋める**

本人以外のプロフィールを閲覧する場合、ここで最新 12 件に制限した後に `is_public` を判定しているため、最新側に非公開イベントが含まれると、さらに古い公開イベントが存在していてもプレビューが不足または空になります。例えば最新 12 件が非公開で 13 件目が公開のユーザーは、イベントタブでは公開イベントを閲覧できるのにプロフィール概要では「イベントなし」と表示されます。2026-05-25 更新の公開範囲仕様に従って非公開項目を返さないまま、公開イベントが上限件数に達するまで取得を継続してください。

Useful? React with 👍 / 👎.

**コメント要約**: イベントプレビューの取得順と公開フィルタの順序。

**判断結果**: ✅ 対応済み（2026-06-02 実装）

**判断理由（2026-05-31）**: 本人以外で `is_public == true` をクエリに含めてから `limit` — 当時仕様（他者は公開のみ）向け。

**判断理由（2026-06-02）**: `listEventsForProfilePreview` から他者向け `is_public` 条件を削除。Callable で `computeProfileItemLinkableToViewer` により `is_linkable` を付与。

---

**識別子**: RC-48（GitHub id: 3329888506）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userProfile.ts:115`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+type UserFoodsCursor = z.infer<typeof FoodCursorSchema>
+
+/**
+ * 各プレビューの最大件数（仕様書 4.2 のプレビュー目安）。
+ * 必要に応じてフラットな optional 引数を追加する余地があるため、サーバ既定として定数で持つ。
+ */
+const PREVIEW_LIMITS = {
+  events: 12,
+  friends: 30,
+  joinedCommunities: 12,
+  managedCommunities: 12,
+  foods: 12,
+  orders: 5,
+} as const
+
+/**
+ * Firestore Timestamp / number / undefined を安全に millis に変換する。
+ * 取得できないときは fallback を返す。
+ */
+const toMillis = (value: unknown, fallback = 0): number => {
+  if (value == null) return fallback
+  if (typeof value === 'number') return value
+  if (value instanceof Timestamp) return value.toMillis()
+  if (typeof (value as { toMillis?: unknown }).toMillis === 'function') {
+    return (value as { toMillis: () => number }).toMillis()
+  }
+  return fallback
+}
+
+const buildPublicProfile = (user: {
+  user_id: string
+  user_name: string
+  user_description: string
+  user_image_url: string
+  user_sns_facebook: string
+  user_sns_facebook_name: string
+  user_sns_twitter: string
+  user_sns_instagram: string
+  user_sns_website: string
+  is_deleted: boolean
+}): UserProfilePublicProfile => ({
+  user_id: user.user_id,
+  user_name: user.user_name,
+  user_description: user.user_description,
+  user_image_url: user.user_image_url,
+  user_sns_facebook: user.user_sns_facebook,
+  user_sns_facebook_name: user.user_sns_facebook_name,
+  user_sns_twitter: user.user_sns_twitter,
+  user_sns_instagram: user.user_sns_instagram,
+  user_sns_website: user.user_sns_website,
+  is_deleted: user.is_deleted,
+})
+
+const fetchEventPreviews = async (
+  targetUserId: string,
+  viewerUid: string | null,
+): Promise<UserProfileEventPreviewItem[]> => {
+  const db = getFirestore()
+  const userRef = getUserRef(targetUserId)
+  const snapshot = await db
+    .collectionGroup('events')
+    .where('members', 'array-contains', userRef)
+    .where('is_deleted', '==', false)
+    .orderBy('event_start_datetime', 'desc')
+    .limit(PREVIEW_LIMITS.events)
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  公開イベントを絞り込んでからプレビュー上限を適用する**

[must] 他ユーザーのプロフィールを閲覧する場合、このクエリは公開可否を条件に含めず最新 12 件を取得し、その後で非公開イベントを除外します。対象ユーザーの新しい 12 件が非公開で、それより古い公開イベントがあるケースでは、公開イベントが存在するのにプレビューが空になります。本人以外では `is_public == true` をクエリ条件に追加するなど、表示可能なイベントに対して上限を適用してください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-47 重複。

**判断結果**: ✅ 対応済み（2026-06-02 実装・RC-47 と同根）

**判断理由**: `listCommunitiesForProfilePreview` から他者向け `is_public` クエリを削除。Callable で `is_linkable` を付与。

---

**識別子**: RC-49（GitHub id: 3294258729）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/friendsService.ts:124`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+    first_met_at: sorted[0].event_at,
+    last_met_at: sorted[sorted.length - 1].event_at,
+    meet_count: new Set(eventHistory.map((item) => item.event_id)).size,
+  }
+}
+
+const saveOrDeleteFriend = async (
+  uid: string,
+  friendUid: string,
+  history: EventHistoryEntry[],
+  createdAtMillis: number,
+  transaction: Transaction,
+) => {
+  const derived = recomputeDerived(history)
+  if (derived == null) {
+    await deleteUserFriend(uid, friendUid, transaction)
+    return false
+  }
+
+  await saveUserFriend(
+    uid,
+    new UserFriend(friendUid, {
+      first_met_at: derived.first_met_at,
+      last_met_at: derived.last_met_at,
+      meet_count: derived.meet_count,
+      event_history: history,
+      created_at: createdAtMillis,
+    }),
+    transaction,
+  )
+  return true
+}
+
+const applyForPair = async (
+  userA: string,
+  userB: string,
+  updater: (history: EventHistoryEntry[]) => EventHistoryEntry[],
+): Promise<number> => {
+  const db = getFirestore()
+  return db.runTransaction(async (transaction) => {
+    const friendAB = await getUserFriend(userA, userB, transaction)
+    const friendBA = await getUserFriend(userB, userA, transaction)
+
+    const nextAB = updater((friendAB?.event_history as EventHistoryEntry[] | undefined) ?? [])
+    const nextBA = updater((friendBA?.event_history as EventHistoryEntry[] | undefined) ?? [])
+
+    const savedAB = await saveOrDeleteFriend(userA, userB, nextAB, friendAB?.created_at ?? Date.now(), transaction)
+    const savedBA = await saveOrDeleteFriend(userB, userA, nextBA, friendBA?.created_at ?? Date.now(), transaction)
+    return (savedAB ? 1 : 0) + (savedBA ? 1 : 0)
+  })
+}
+
+export const addEventToFriendHistory = async ({
+  event_id,
+  community_id,
+  event_at,
+  user_ids,
+}: AddEventToFriendHistoryInput): Promise<number> => {
+  const pairs = buildPairs(user_ids)
+  let updatedDocs = 0
+  const entry = { event_id, community_id, event_at }
+
+  for (const [userA, userB] of pairs) {
+    updatedDocs += await applyForPair(userA, userB, (history) => upsertHistoryEntry(history, entry))
+  }
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  友人ペア更新を逐次トランザクション実行しない**

`pairs` の全件に対して `await applyForPair(...)` を直列実行しており、注文確定/キャンセルの同期フローから呼ばれるため、参加者が増えると処理時間が二乗で増大します（例: 60人で1,770ペア分の往復）。その結果、Functions の実行時間超過や高レイテンシでユーザーの確定/取消操作が失敗しやすくなるため、ペア更新のバッチ化・並列度制御・差分更新などで 1 リクエスト当たりのトランザクション数を抑える必要があります。

Useful? React with 👍 / 👎.

**コメント要約**: 友人ペア更新のスケーラビリティ。

**判断結果**: ✅ 対応済み（2026-05-31 実装・RC-21 と同根）

**判断理由**: RC-21 と同一対応。確定時の anchor 化と `runPairsWithConcurrency` により、1 リクエストあたりのペア数・実時間を削減。Phase 3（Cloud Tasks 非同期化）は別 Issue。

---

**識別子**: RC-50（GitHub id: 3328599646）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/recountUserProfileCounts.ts:28`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,133 @@
+import { getFirestore } from 'firebase-admin/firestore'
+import {
+  getUser,
+  getUserRef,
+  getUsersByUserIds,
+  updateUserProfileCounts,
+  type UserProfileCountsUpdate,
+  type ShokujiiUser,
+} from '../stores/user.js'
+import { createModuleLogger } from './logger.js'
+
+const logger = createModuleLogger('recountUserProfileCounts')
+
+export type UserProfileCounts = UserProfileCountsUpdate
+
+/**
+ * ユーザーの友人 ID 一覧から、退会済みユーザーを除いた件数を返す。
+ * `getUserFriends` Callable と同じ「退会者除外」ルールに揃える。
+ */
+export const computeActiveFriendCount = async (friendUserIds: string[]): Promise<number> => {
+  if (friendUserIds.length === 0) {
+    return 0
+  }
+  const userMap = await getUsersByUserIds(friendUserIds)
+  let active = 0
+  for (const id of friendUserIds) {
+    const user = userMap.get(id)
+    if (user != null && !user.is_deleted) {
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  退会時に残存ユーザーの friend_count を再集計する**

友人が論理削除された場合、この条件により次回再集計時には正しく除外できますが、`deleteUserAccount.ts:61` の退会処理では相手側ユーザーの再集計が呼ばれません。例えば A-B が友人で B が退会すると、`getUserFriends` は B を除外する一方、A の新規キャッシュ `friend_count` は以前の値のまま残り、プロフィール表示と一覧が不一致になります。退会成功時に対象ユーザーの friends を列挙し、残存する相手側 uid を再集計してください。

Useful? React with 👍 / 👎.

**コメント要約**: `deleteUserAccount` 後の友人側カウント不整合。

**判断結果**: 🟡 修正提案（2026-05-31 再評価）

**判断理由**: Codex P2。`deleteUserAccount.ts` は相手側への `recountUserProfileCounts` 未呼び出しでキャッシュ `friend_count` が古くなりうるが、セキュリティ改ざん（RC-56）とは別件の表示不整合。マージ前対応は推奨。

---

**識別子**: RC-51（GitHub id: 3329884056）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/userFriendsResolver.ts:101`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+  viewerUid: string | null,
+  targetUserId: string,
+): boolean => {
+  if (event == null || event.is_deleted) {
+    return false
+  }
+  return computeProfileItemVisibleToViewer({
+    isPublic: event.is_public === true,
+    viewerUid,
+    targetUserId,
+  })
+}
+
+const toMeetLogItem = (entry: MeetLogHistoryEntry, event: ShokujiiEvent | undefined): UserFriendMeetLogItem => {
+  const isAvailable = event != null && !event.is_deleted
+  return {
+    event_id: entry.event_id,
+    community_id: entry.community_id,
+    event_at: entry.event_at,
+    event_name: isAvailable ? event.event_name : null,
+    community_account: isAvailable ? event.community_account : null,
+  }
+}
+
+const buildVisibleMeetLogItems = (
+  history: MeetLogHistoryEntry[],
+  eventMap: Map<string, ShokujiiEvent>,
+  viewerUid: string | null,
+  targetUserId: string,
+  sortDesc: boolean,
+): UserFriendMeetLogItem[] => {
+  const sorted = [...history].sort((a, b) => (sortDesc ? b.event_at - a.event_at : a.event_at - b.event_at))
+  const visible: UserFriendMeetLogItem[] = []
+  for (const entry of sorted) {
+    const event = eventMap.get(getCommunityEventKey(entry.community_id, entry.event_id))
+    if (!isEventVisibleToViewer(event, viewerUid, targetUserId)) {
+      continue
+    }
+    visible.push(toMeetLogItem(entry, event))
+  }
+  return visible
+}
+
+const resolveUserFriendsListOnce = async (input: ResolveInput): Promise<ResolveOutput> => {
+  const { targetUserId, sortBy, limit, cursor } = input
+  const cap = input.meetLogMaxPerFriend ?? DEFAULT_MEET_LOG_MAX_PER_FRIEND
+  const viewerUid = input.viewerUid ?? null
+
+  const page = await listUserFriends(targetUserId, sortBy, limit, cursor)
+  if (page.friends.length === 0) {
+    return { friends: [], hasMore: page.hasMore, nextCursor: page.nextCursor }
+  }
+
+  const userMap = await getUsersByUserIds(page.friends.map((friend) => friend.id))
+
+  const historyByFriend = new Map<string, MeetLogHistoryEntry[]>()
+  const eventRefsToFetch: { community_id: string; event_id: string }[] = []
+
+  for (const friend of page.friends) {
+    const normalizedHistory = normalizeHistory(friend.event_history)
+    if (cap === 0) {
+      historyByFriend.set(friend.id, normalizedHistory)
+      normalizedHistory.forEach((entry) => {
+        eventRefsToFetch.push({ community_id: entry.community_id, event_id: entry.event_id })
+      })
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  友人プレビューでは全イベント履歴の解決を避ける**

以前のレビューで指摘された不要なイベント展開は、現在の `cap === 0` 分岐が全 `event_history` を `eventRefsToFetch` に追加するようになったため再発しています。`getUserProfilePreview` は `meetLogMaxPerFriend: 0` でこの経路を呼んだ後、友人プレビューへ氏名・画像・回数・日時だけを写しており、解決した `first_meet_event` / `last_meet_event` は利用しません。友人や同席履歴が多いプロフィールを開くだけで履歴中のイベントを全件読み取るため、Callable のレイテンシと Firestore read コストが履歴量に比例して増えます。プレビュー用途ではイベント参照を取得しないモードを分けてください。

Useful? React with 👍 / 👎.

**コメント要約**: 友人プレビューでの過剰 events 読み取り（RC-30 系）。

**判断結果**: ✅ 対応済み

**判断理由**: 現行 `resolveUserFriendsList` はサブコレの日付のみ返し events を読まない（RC-30 / RC-31 実装済み）。コメントは旧実装前提。

---

**識別子**: RC-52（GitHub id: 3329884055）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/userFriendsResolver.ts:170`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+      capped.forEach((entry) => {
+        eventRefsToFetch.push({ community_id: entry.community_id, event_id: entry.event_id })
+      })
+    }
+  }
+
+  const eventMap = eventRefsToFetch.length > 0 ? await getEventsInCommunities(eventRefsToFetch) : new Map()
+
+  const friends: UserFriendListItem[] = []
+  for (const friend of page.friends) {
+    const user = userMap.get(friend.id)
+    if (user == null || user.is_deleted) {
+      continue
+    }
+    const history = historyByFriend.get(friend.id) ?? []
+
+    if (cap === 0) {
+      const visibleAsc = buildVisibleMeetLogItems(history, eventMap, viewerUid, targetUserId, false)
+      friends.push({
+        user_id: friend.id,
+        user_name: user.user_name,
+        user_image_url: user.user_image_url,
+        meet_count: friend.meet_count,
+        first_met_at: friend.first_met_at,
+        last_met_at: friend.last_met_at,
+        first_meet_event: visibleAsc.length > 0 ? visibleAsc[0] : null,
+        last_meet_event: visibleAsc.length > 0 ? visibleAsc[visibleAsc.length - 1] : null,
+      })
+      continue
+    }
+
+    const visibleDesc = buildVisibleMeetLogItems(history, eventMap, viewerUid, targetUserId, true)
+    friends.push({
+      user_id: friend.id,
+      user_name: user.user_name,
+      user_image_url: user.user_image_url,
+      meet_count: friend.meet_count,
+      first_met_at: friend.first_met_at,
+      last_met_at: friend.last_met_at,
+      meet_log: visibleDesc,
+    })
+  }
+
+  return {
+    friends,
+    hasMore: page.hasMore,
+    nextCursor: page.nextCursor,
+  }
+}
+
+/**
+ * 共通の友人解決ロジック。
+ * `getUserFriends` Callable と `getUserProfilePreview` Callable で**同一**に使う。
+ *
+ * - 友人サブコレを `sortBy` 順に limit + 1 件読み出してページング判定
+ * - 友人ユーザーを 1 回の getUsersByUserIds でまとめて解決し、退会ユーザーを除外
+ * - `meetLogMaxPerFriend === 0`（既定）: first/last イベントのみ解決
+ * - `meetLogMaxPerFriend > 0`: meet_log を cap 件返す（後方互換）
+ */
+export const resolveUserFriendsList = async (input: ResolveInput): Promise<ResolveOutput> => {
+  const result = await resolveUserFriendsListOnce(input)
+
+  // RC-18 最小修正: 退会ユーザー除外で空になったが次ページがある場合のみ 1 回追加入力
+  if (result.friends.length === 0 && result.hasMore && result.nextCursor != null) {
+    return resolveUserFriendsListOnce({ ...input, cursor: result.nextCursor })
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  退会済み友人だけのページを連続して読み飛ばす**

`friends` サブコレクションには退会済みユーザーのドキュメントが残るため、ソート上位に退会者だけのページが 2 ページ以上続くケースがあります。この実装は空ページを 1 回だけ追読みするため、先頭 60 件が退会者で 61 件目以降に有効な友人がいる場合も空配列を返します。プロフィール概要側は友人が 0 件だと `IncrementalLoader` 自体を描画しないため、その後の有効な友人へ到達できず「ともだちなし」と誤表示されます。`hasMore` が続く限り、有効な友人が得られるか末尾へ到達するまで空ページを読み飛ばしてください。

Useful? React with 👍 / 👎.

**コメント要約**: 友人一覧の空ページ追読みが 1 回のみ。

**判断結果**: ✅ 対応済み（2026-05-31 再評価）

**判断理由**: `resolveUserFriendsList` が `MAX_PROFILE_PREVIEW_SKIP_PAGES`（20）で `hasMore` が続く間追読み。概要 UI も `profilePreviewFriendsHasMore` で 0 件時ローダー表示（RC-18/52 統合済み）。

---

**識別子**: RC-53（GitHub id: 3303247303）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:943`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+            >
+              <router-link :to="getCommunityPath(community.community_account)">
+                <CommunityCard :community="community" :members="members" />
+              </router-link>
+            </v-col>
+          </v-row>
+          <v-row class="justify-center">
+            <v-col cols="auto">
+              <IncrementalLoader
+                :loaded-count="managerCommunityListStore.communityStores?.length ?? 0"
+                :total-count="managerCommunityListStore.totalCount ?? Number.MAX_SAFE_INTEGER"
+                @load="managerCommunityListStore.next()"
+              />
+            </v-col>
+          </v-row>
+        </v-window-item>
+
+        <v-window-item :value="TAB_FOODS">
+          <div v-if="foodLoading && pagedFoods.length === 0" class="d-flex justify-center pa-6">
+            <v-progress-circular indeterminate color="primary" />
+          </div>
+          <div v-else-if="foodError != null" class="text-body-1 text-medium-emphasis pa-6">
+            {{ $t('user_profile.failed_to_load') }}
+          </div>
+          <template v-else>
+            <div v-if="visiblePagedFoods.length === 0" class="text-body-1 text-medium-emphasis pa-4">
+              {{ $t('user_profile.empty.foods') }}
+            </div>
+            <v-row v-else>
+              <v-col v-for="food in visiblePagedFoods" :key="food.order_id" cols="12" sm="6" md="4">
+                <v-card elevation="2" class="h-100 preview-card profile-panel-card">
+                  <v-img
+                    v-if="foodImageUrl(food) != null"
+                    :src="foodImageUrl(food)"
+                    :alt="food.menu_name"
+                    cover
+                    height="180"
+                  />
+                  <div
+                    v-else
+                    class="food-preview-placeholder d-flex align-center justify-center bg-surface-variant"
+                    style="height: 180px"
+                  >
+                    <v-icon :icon="mdiFood" size="40" class="text-medium-emphasis" />
+                  </div>
+                  <v-card-title class="text-truncate" :title="food.menu_name">{{ food.menu_name }}</v-card-title>
+                  <v-card-text>
+                    <div v-if="food.event_name !== ''" class="mb-2 min-width-0">
+                      <router-link
+                        class="food-event-link text-body-2 text-truncate d-block"
+                        :to="getEventPath(food.community_account, food.event_id)"
+                        :title="food.event_name"
+                        :aria-label="$t('user_profile.food_event_link_label')"
+                      >
+                        {{ food.event_name }}
+                      </router-link>
+                    </div>
+                    <div class="text-caption text-medium-emphasis">
+                      {{ formatProfileDate(food.ordered_at, 'date') }}
+                    </div>
+                  </v-card-text>
+                </v-card>
+              </v-col>
+            </v-row>
+            <v-row v-if="visiblePagedFoods.length > 0" class="justify-center mt-2">
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  フード0件表示時でも次ページ読込を可能にする**

`getUserFoods` はページ取得後に可視性フィルタをかけるため、`has_more` が `true` でもそのページの `foods` が 0 件になるケースがあります（例: 最新ページが非公開イベント由来ばかりで、公開イベント由来が次ページ以降にある場合）。この実装だと `visiblePagedFoods.length === 0` のときに `IncrementalLoader` 自体を非表示にしてしまうため、利用者は次ページを読めず、実際には表示可能なフードが残っていても「空」と誤表示されます。

Useful? React with 👍 / 👎.

**コメント要約**: フードタブの空ページ＋追読み UI。

**判断結果**: ✅ 対応済み（2026-05-31 再評価）

**判断理由**: `foodHasMore || visiblePagedFoods.length > 0` のとき `IncrementalLoader` を表示する実装に変更済み。

---

**識別子**: RC-54（GitHub id: 3329888504）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:979`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 抜粋・末尾 65 行）
+                    <CommunityCard :community="community" :members="members" />
+                  </router-link>
+                </v-col>
+              </v-row>
+              <v-row class="justify-center">
+                <v-col cols="auto">
+                  <IncrementalLoader
+                    :loaded-count="managerCommunityListStore.communityStores?.length ?? 0"
+                    :total-count="managerCommunityListStore.totalCount ?? Number.MAX_SAFE_INTEGER"
+                    @load="managerCommunityListStore.next()"
+                  />
+                </v-col>
+              </v-row>
+            </template>
+          </template>
+        </v-window-item>
+
+        <v-window-item :value="TAB_FOODS">
+          <div v-if="foodLoading && pagedFoods.length === 0" class="d-flex justify-center pa-6">
+            <v-progress-circular indeterminate color="primary" />
+          </div>
+          <div v-else-if="foodError != null" class="text-body-1 text-medium-emphasis pa-6">
+            {{ $t('user_profile.failed_to_load') }}
+          </div>
+          <template v-else>
+            <div v-if="visiblePagedFoods.length === 0" class="text-body-1 text-medium-emphasis pa-4">
+              {{ $t('user_profile.empty.foods') }}
+            </div>
+            <v-row v-else>
+              <v-col v-for="food in visiblePagedFoods" :key="food.order_id" cols="12" sm="6" md="4">
+                <v-card elevation="2" class="h-100 preview-card profile-panel-card">
+                  <v-img
+                    v-if="foodImageUrl(food) != null"
+                    :src="foodImageUrl(food)"
+                    :alt="food.menu_name"
+                    cover
+                    height="180"
+                  />
+                  <div
+                    v-else
+                    class="food-preview-placeholder d-flex align-center justify-center bg-surface-variant"
+                    style="height: 180px"
+                  >
+                    <v-icon :icon="mdiFood" size="40" class="text-medium-emphasis" />
+                  </div>
+                  <v-card-title class="text-truncate" :title="food.menu_name">{{ food.menu_name }}</v-card-title>
+                  <v-card-text>
+                    <div v-if="food.event_name !== ''" class="mb-2 min-width-0">
+                      <router-link
+                        class="food-event-link text-body-2 text-truncate d-block"
+                        :to="getEventPath(food.community_account, food.event_id)"
+                        :title="food.event_name"
+                        :aria-label="$t('user_profile.food_event_link_label')"
+                      >
+                        {{ food.event_name }}
+                      </router-link>
+                    </div>
+                    <div class="text-caption text-medium-emphasis">
+                      {{ formatProfileDate(food.ordered_at, 'date') }}
+                    </div>
+                  </v-card-text>
+                </v-card>
+              </v-col>
+            </v-row>
+            <v-row v-if="visiblePagedFoods.length > 0" class="justify-center mt-2">
```

**レビュワーのコメント**（原文）:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  空の公開フードページでも追読みを継続する**

[must] 本人以外がフードタブを見る場合、Callable は注文をページングした後で非公開イベント由来のフードを除外します。そのため、新しい 12 件がすべて非公開でも古い公開フードが残っているケースでは `visiblePagedFoods.length === 0` かつ `foodHasMore === true` になります。この条件でローダー自体を非表示にすると `userFoodsStore.next()` を呼ぶ手段がなくなり、画面は誤って空表示のままになります。`foodHasMore` が true の間は、表示済みフードが 0 件でもローダーを描画してください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-53 重複。

**判断結果**: ✅ 対応済み（2026-05-31 再評価・RC-53 と同根）

**判断理由**: RC-53 と同一対応済み。

---

## 評価セッション（2026-05-31・Codex 新規指摘 RC-55〜61）

- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **評価日時**: 2026-05-31 21:06 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **Outdated 除外件数**: 0 件
- **レビュー非該当スキップ件数**: 13 件（レビュー依頼・Copilot 応答・Codex 接続案内・Codex 定型サマリ等）
- **備考**: GitHub アクティブインライン 30 件のうち、既存 `pr-1985.md` 未登録 7 件を本セッションで評価。RC-50 は引き続き未対応

### RC 別対応チェックリスト

- [x] **RC-55**（GitHub id: 3330003644）— ✅ 対応済み（`functions/default/src/stores/community.ts`）
- [x] **RC-56**（GitHub id: 3330003647）— ✅ 対応済み（`firestore.rules`）
- [x] **RC-57**（GitHub id: 3330114203）— ✅ 対応済み（`countParticipatedEventsForUser`）
- [ ] **RC-58**（GitHub id: 3330114200）— 🚨 必須修正（`resolveUserFriendMeetLog` で友人側 `is_deleted` 未確認）
- [x] **RC-59**（GitHub id: 3329971269）— ✅ 対応済み（`UserProfilePage.vue` 認証変化で preview / foods reload）
- [x] **RC-60**（GitHub id: 3329971268）— ✅ 対応済み（§4.2.0: reload + `is_public` / `is_linkable` / `canLinkToDetail`）
- [x] **RC-61**（GitHub id: 3329923027）— ✅ 対応済み（`UserProfilePage.vue` コミュニティ追読み）

### 推奨対応順序（RC-55〜61）

1. ~~**RC-55**（P1）~~ — ✅ 対応済み
2. ~~**RC-56**~~ — ✅ 対応済み
3. **RC-58** — 退会友人 meet log（未対応）
4. ~~**RC-59** / **RC-60** / **RC-61**~~ — ✅ 対応済み（RC-60 は §4.2.0 解釈で完了）
5. **RC-50** — 退会時の相手側 `friend_count`（🟡 未対応）

---

**識別子**: RC-55（GitHub id: 3330003644）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/stores/community.ts:250`

**該当コード（レビュー時点の diff）**:

```diff
@@ -235,3 +237,54 @@ export const removeMemberFromCommunity = async (
   const memberRef = communityRef.collection('members').doc(userId).withConverter(communityMemberConverter)
   transaction.delete(memberRef)
 }
+
+/**
+ * 参加コミュニティ数（`communities/{id}/members/{userId}` の存在件数）。
+ * 親 `community.members` 配列ではなくサブコレを正本とする（legacy 配列同期とのレース回避・RC-43/44）。
+ */
+export const countJoinedCommunitiesForUser = async (userId: string): Promise<number> => {
+  if (userId === '') {
+    return 0
+  }
+  const db = getFirestore()
+  const snapshot = await db.collectionGroup('members').where(FieldPath.documentId(), '==', userId).count().get()
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  collectionGroup の documentId 条件をコミュニティ配下に限定する**

`FieldPath.documentId()` を使う collection group クエリには完全なドキュメントパスが必要なので、通常の `userId`（例: `user1`）を渡すと Admin SDK が `contains an odd number of segments` エラーを投げます。同じ条件を使う `countManagedCommunitiesForUser` も失敗するため、`computeUserProfileCounts` の `Promise.all` が常に reject し、注文確定・キャンセル・コミュニティ参加 Trigger・backfill のいずれでも新しいプロフィールカウントを保存できません。また、この `members` collection group には `communities/{communityId}/events/{eventId}/members/{userId}` も含まれるため、単に完全パスへ置換するだけでは参加イベント数まで加算されます。コミュニティ直下の members のみを数える store 実装に変更してください。

Useful? React with 👍 / 👎.

**コメント要約**: `collectionGroup('members')` に `documentId == userId` はパス不正・イベント members 混入の両方がある。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `countJoinedCommunitiesForUser` / `countManagedCommunitiesForUser` を `communities.members` / `managers` の array-contains count に差し替え（RC-55）。

---

**識別子**: RC-56（GitHub id: 3330003647）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/userProfile.ts:310`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・231 行）
+    if (item != null) {
+      foods.push(item)
+    }
+  }
+
+  return {
+    foods,
+    nextCursor: page.nextCursor,
+    hasMore: page.hasMore,
+  }
+}
+
+const fetchFoodPreviews = async (
+  targetUserId: string,
+  viewerUid: string | null,
+): Promise<UserProfileFoodPreviewItem[]> => {
+  const { foods } = await fetchFoodsPage({
+    targetUserId,
+    viewerUid,
+    limit: PREVIEW_LIMITS.foods,
+  })
+  return foods
+}
+
+const fetchOrderPreviews = async (targetUserId: string): Promise<UserProfileOrderPreviewItem[]> => {
+  const { orders, eventsByKey } = await listOrderedFoodsPreviewForProfile(targetUserId, PREVIEW_LIMITS.orders)
+  const items: UserProfileOrderPreviewItem[] = []
+  for (const order of orders) {
+    const event = eventsByKey.get(getCommunityEventKey(order.community_id, order.event_id))
+    items.push({
+      order_id: order.order_id,
+      community_id: order.community_id,
+      event_id: order.event_id,
+      event_name: event?.event_name ?? '',
+      event_start_datetime: event != null ? event.event_start_datetime : 0,
+      ordered_at: toMillis(order.ordered_at) || order.updated_at,
+    })
+  }
+  return items
+}
+
+/**
+ * プロフィールタブ初期表示用のプレビューデータを 1 往復で返す Callable。
+ * 仕様書 5.2.1 を参照。
+ *
+ * - 未ログインでも呼べる（`invoker: 'public'`）
+ * - 退会済み / 存在しないユーザーは `not-found`
+ * - `previews.orders` は本人のみ返し、それ以外は `null`
+ * - 各プレビューには `is_visible_to_viewer` を付与する（返却項目は常に true。本人以外には非公開を含めない）
+ * - App Check は Phase 1 では必須としない。Firebase Functions の Callable 既定どおり `enforceAppCheck` は付けず、未ログイン呼び出しや運用バッチを阻害しない。強制を有効化する場合は別イシューでクライアント対応と合わせて検討する（仕様書 5.2.1 F1）
+ */
+export const getUserProfilePreview = onCall<GetUserProfilePreviewRequest, Promise<GetUserProfilePreviewResponse>>(
+  { region: 'asia-northeast1', invoker: 'public' },
+  async (request) => {
+    const input = GetUserProfilePreviewRequestSchema.parse(request.data)
+    const targetUserId = input.target_user_id
+    const viewerUid = request.auth?.uid ?? null
+    const isOwner = viewerUid != null && viewerUid === targetUserId
+
+    const targetUser = await getUser(targetUserId, false)
+    if (targetUser == null || targetUser.is_deleted) {
+      throw new HttpsError('not-found', '存在しないユーザーです')
+    }
+
+    const [eventPreviews, friendPreviews, joinedCommunities, managedCommunities, foodPreviews, orderPreviews] =
+      await Promise.all([
+        fetchEventPreviews(targetUserId, viewerUid),
+        fetchFriendPreviews(targetUserId, viewerUid),
+        fetchCommunityPreviews('members', targetUserId, viewerUid, PREVIEW_LIMITS.joinedCommunities),
+        fetchCommunityPreviews('managers', targetUserId, viewerUid, PREVIEW_LIMITS.managedCommunities),
+        fetchFoodPreviews(targetUserId, viewerUid),
+        isOwner ? fetchOrderPreviews(targetUserId) : Promise.resolve(null as UserProfileOrderPreviewItem[] | null),
+      ])
+
+    const counts = {
+      participated_event_count: targetUser.participated_event_count ?? 0,
+      friend_count: targetUser.friend_count ?? 0,
+      joined_community_count: targetUser.joined_community_count ?? 0,
+      managed_community_count: targetUser.managed_community_count ?? 0,
+      ordered_food_count: targetUser.ordered_food_count ?? 0,
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  プロフィール用カウントをクライアント更新から保護する**

認証済みユーザーが自分のプロフィールを更新できる既存ルール `firestore.rules:103` は更新可能フィールドを制限していません。この Callable は追加された冗長カウントをそのまま公開プロフィールへ返すため、ユーザーは Firestore SDK から `users/{uid}` の `friend_count` や参加数を任意値へ書き換えて、他の閲覧者に偽の実績を表示できます。これらのフィールドと `counts_updated_at` は Functions 専用にし、Security Rules でクライアントによる変更を拒否してください。

Useful? React with 👍 / 👎.

**コメント要約**: クライアントが `users` の冗長カウントを書き換え可能で、偽カウント表示のリスクがある。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `userProfileCountFields` / `userProfileCountsUnchanged` を `firestore.rules` に追加し、クライアント update でカウント変更を拒否（RC-56）。

---

**識別子**: RC-57（GitHub id: 3330114203）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/recountUserProfileCounts.ts:57`

**該当コード（レビュー時点の diff）**:

```diff
@@ -0,0 +1,134 @@
+import { getFirestore } from 'firebase-admin/firestore'
+import { countJoinedCommunitiesForUser, countManagedCommunitiesForUser } from '../stores/community.js'
+import {
+  getUser,
+  getUserRef,
+  getUsersByUserIds,
+  updateUserProfileCounts,
+  type UserProfileCountsUpdate,
+  type ShokujiiUser,
+} from '../stores/user.js'
+import { createModuleLogger } from './logger.js'
+
+const logger = createModuleLogger('recountUserProfileCounts')
+
+export type UserProfileCounts = UserProfileCountsUpdate
+
+/**
+ * ユーザーの友人 ID 一覧から、退会済みユーザーを除いた件数を返す。
+ * `getUserFriends` Callable と同じ「退会者除外」ルールに揃える。
+ */
+export const computeActiveFriendCount = async (friendUserIds: string[]): Promise<number> => {
+  if (friendUserIds.length === 0) {
+    return 0
+  }
+  const userMap = await getUsersByUserIds(friendUserIds)
+  let active = 0
+  for (const id of friendUserIds) {
+    const user = userMap.get(id)
+    if (user != null && !user.is_deleted) {
+      active += 1
+    }
+  }
+  return active
+}
+
+/**
+ * 指定ユーザーの最新カウントを集計のみ行う（書き込みなし）。
+ * 仕様書 5.1.3 のクエリ・ルールに従う:
+ *   - friend_count: `users/{uid}/friends` から退会者を除外した件数
+ *   - participated_event_count: `events` collection group で `members array-contains userRef + is_deleted == false`
+ *   - ordered_food_count: `member_orders` collection group で `user_id == uid + status == 'ordered'`
+ *   - joined_community_count: `members` collection group（documentId == uid）
+ *   - managed_community_count: 同上 + `roles array-contains manager`
+ */
+export const computeUserProfileCounts = async (userId: string): Promise<UserProfileCounts> => {
+  const db = getFirestore()
+  const userRef = getUserRef(userId)
+
+  const friendsSnapshot = await db.collection('users').doc(userId).collection('friends').get()
+  const friendUserIds = friendsSnapshot.docs.map((doc) => doc.id)
+
+  const [participatedAgg, orderedAgg, joinedCount, managedCount, activeFriendCount] = await Promise.all([
+    db
+      .collectionGroup('events')
+      .where('members', 'array-contains', userRef)
+      .where('is_deleted', '==', false)
+      .count()
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  キャンセル済みイベントを参加イベント数から除外する**

`participated_event_count` の集計条件が `members` と `is_deleted` だけなので、`event_status.value === 'event_canceled'` のまま members が残っているイベントも実績に加算されます。`documents/03_参加者獲得/02_マイページ.md` の 5.1.2 ではキャンセル相当のイベントを含めない契約になっており、既存データの backfill やサポート操作後にプロフィールの参加イベント数が過大になります。イベントステータスも判定して集計対象から除外してください。

Useful? React with 👍 / 👎.

**コメント要約**: `participated_event_count` がキャンセル済みイベントを含みうる。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `countParticipatedEventsForUser`（`member_orders` ordered のユニークイベント + `is_deleted` / `event_canceled` 除外）を `recountUserProfileCounts` に接続（RC-57）。

---

**識別子**: RC-58（GitHub id: 3330114200）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/userFriendsResolver.ts:165`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・86 行）
+  const friends: UserFriendListItem[] = []
+  for (const friend of page.friends) {
+    const user = userMap.get(friend.id)
+    if (user == null || user.is_deleted) {
+      continue
+    }
+    friends.push({
+      user_id: friend.id,
+      user_name: user.user_name,
+      user_image_url: user.user_image_url,
+      meet_count: friend.meet_count,
+      first_met_at: friend.first_met_at,
+      last_met_at: friend.last_met_at,
+    })
+  }
+
+  return {
+    friends,
+    hasMore: page.hasMore,
+    nextCursor: page.nextCursor,
+  }
+}
+
+/**
+ * 友人一覧用の共通解決ロジック。
+ * `getUserFriends` Callable と `getUserProfilePreview` の友人プレビューで使う。
+ *
+ * - 友人サブコレを `sortBy` 順に limit + 1 件読み出してページング判定
+ * - 友人ユーザーを 1 回の getUsersByUserIds でまとめて解決し、退会ユーザーを除外
+ * - 退会者だけのページは hasMore が続く限り読み飛ばす（RC-52）
+ * - カード表示用の日付はサブコレの `first_met_at` / `last_met_at` をそのまま返す（events は読まない）
+ * - 同席履歴の詳細は `resolveUserFriendMeetLog`（ダイアログ用）で取得
+ */
+export const resolveUserFriendsList = async (input: ResolveInput): Promise<ResolveOutput> => {
+  const { limit } = input
+  let cursor = input.cursor
+  const collected: UserFriendListItem[] = []
+  let firestoreHasMore = false
+  let firestoreNextCursor: UserFriendsListCursor | null = null
+
+  for (let pageIndex = 0; pageIndex < MAX_PROFILE_PREVIEW_SKIP_PAGES; pageIndex++) {
+    const remaining = limit - collected.length
+    if (remaining <= 0) {
+      break
+    }
+
+    const page = await resolveUserFriendsListOnce({ ...input, limit: remaining, cursor })
+    collected.push(...page.friends)
+    firestoreHasMore = page.hasMore
+    firestoreNextCursor = page.nextCursor
+
+    if (collected.length >= limit) {
+      break
+    }
+    if (!page.hasMore || page.nextCursor == null) {
+      break
+    }
+    cursor = page.nextCursor
+  }
+
+  return {
+    friends: collected,
+    hasMore: firestoreHasMore && firestoreNextCursor != null,
+    nextCursor: firestoreHasMore ? firestoreNextCursor : null,
+  }
+}
+
+/**
+ * 1 友人分の同席イベント履歴を全件返す（ダイアログ用）。
+ */
+export const resolveUserFriendMeetLog = async (params: {
+  targetUserId: string
+  friendUserId: string
+  viewerUid: string | null
+}): Promise<{ meet_log: UserFriendMeetLogItem[]; meet_count: number } | null> => {
+  const { targetUserId, friendUserId, viewerUid } = params
+  const friend = await getUserFriend(targetUserId, friendUserId)
+  if (friend == null) {
+    return null
+  }
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  退会済みの友人に対する同席履歴の直接取得を拒否する**

`getUserFriendMeetLog` は未ログインでも呼べる公開 Callable ですが、この処理は `friends/{friendUserId}` の残存だけで履歴を返し、友人側の `users/{friendUserId}` が退会済みかを確認していません。退会時に friends ドキュメントを残置する設計のため、一覧では除外された退会ユーザーでも、既知の UID を指定すると第三者が `meet_count` と公開イベントの同席履歴を取得できます。`getUser` 等で友人側も解決し、存在しない場合や `is_deleted === true` の場合は `null` を返してください。

Useful? React with 👍 / 👎.

**コメント要約**: 退会済み友人の UID を指定すると meet log が取得できる。

**判断結果**: 🚨 必須修正（2026-05-31 再評価）

**判断理由**: 公開 Callable の漏洩。`resolveUserFriendMeetLog` は `getUserFriend` のみで友人側 `users` の `is_deleted` 未確認。退会 UID 指定で履歴取得しうる。

---

**識別子**: RC-59（GitHub id: 3329971269）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:238`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・141 行）
+/** プロフィール内ともだちアイコンは meet_count ストアとデータを共有しつつ表示だけ上限で切る */
+const profilePreviewFriends = computed(() => {
+  const store = userFriendsByMeetCountStore.value
+  if (store == null) return []
+  return store.friends.slice(0, profileFriendsPreviewMaxTotal.value)
+})
+const profilePreviewFriendsLoading = computed(() => userFriendsByMeetCountStore.value?.loading ?? false)
+const profilePreviewFriendsHasMore = computed(() => {
+  const store = userFriendsByMeetCountStore.value
+  const maxTotal = profileFriendsPreviewMaxTotal.value
+  return store != null && store.hasMore && store.friends.length < maxTotal
+})
+
+const loadMoreProfilePreviewFriends = () => {
+  const store = userFriendsByMeetCountStore.value
+  if (store == null) return
+  const maxTotal = profileFriendsPreviewMaxTotal.value
+  if (!store.hasMore || store.loading || store.friends.length >= maxTotal) return
+  store.next()
+}
+
+/**
+ * URL の `?tab=xxx` をタブに解決する。未指定や本人以外で `orders` を直叩きされた場合は
+ * プロフィールタブにフォールバックする（仕様 4.2.5）。
+ */
+const resolveTabFromQuery = (rawTab: string): TabKey => {
+  switch (rawTab) {
+    case TAB_FRIENDS:
+    case TAB_EVENTS:
+    case TAB_COMMUNITIES:
+    case TAB_FOODS:
+      return rawTab
+    case TAB_ORDERS:
+      return loginUser.value?.user_id === profileUserId ? TAB_ORDERS : TAB_PROFILE
+    default:
+      return TAB_PROFILE
+  }
+}
+
+const tabs = ref<TabKey>(resolveTabFromQuery(String(route.query.tab ?? '')))
+
+watch(
+  () => isOwner.value,
+  (owner) => {
+    if (!owner && tabs.value === TAB_ORDERS) {
+      tabs.value = TAB_PROFILE
+    }
+  },
+)
+
+const queryTabFor = (tab: TabKey): string | undefined => (tab === TAB_PROFILE ? undefined : tab)
+
+const syncTabToUrl = (tab: TabKey) => {
+  const nextTab = queryTabFor(tab)
+  const currentTab = route.query.tab == null || String(route.query.tab) === '' ? undefined : String(route.query.tab)
+  if (currentTab === nextTab) return
+  const query = { ...route.query } as Record<string, string | string[] | undefined>
+  if (nextTab === undefined) {
+    delete query.tab
+  } else {
+    query.tab = nextTab
+  }
+  void router.replace({ query })
+}
+
+/** ブラウザ戻る/進む・loginUser 解決後の URL 反映（仕様 4.2.5） */
+watch(
+  () => [route.query.tab, loginUser.value?.user_id, profileUserId] as const,
+  ([rawTab]) => {
+    const resolved = resolveTabFromQuery(String(rawTab ?? ''))
+    if (tabs.value !== resolved) {
+      tabs.value = resolved
+    }
+  },
+)
+
+const previewStore = useUserProfilePreviewStore(profileUserId)
+const { data: previewData, loading: previewLoading, error: previewError } = storeToRefs(previewStore)
+const userFoodsStore = useUserFoodsStore(profileUserId, 12)
+const { foods: pagedFoods, hasMore: foodHasMore, loading: foodLoading, error: foodError } = storeToRefs(userFoodsStore)
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  認証ユーザーの確定後にプロフィール用ストアを再取得する**

Firebase Auth の復元前に `/u/<自分>` をリロードした場合や、未ログインで自分のプロフィールを開いてからログインした場合、これらのストアは未認証の Callable レスポンスを初回ロードしたままになります。レスポンスは `request.auth?.uid` により本人向けと他者向けで異なる一方、ストア生成後に `loginUser` の変化で `reload()` する処理がないため、本人でも非公開イベント・コミュニティ・フードと注文プレビューが欠落します。認証 UID の変更時に両ストアを再取得してください。

Useful? React with 👍 / 👎.

**コメント要約**: ログイン後もプレビュー・フードストアが未認証レスポンスのまま。

**判断結果**: ✅ 対応済み（2026-06-02 実装）

**判断理由**: `watch(() => loginUser.value?.user_id, …)` で `previewStore.reload()` と `userFoodsStore.reload()` を呼ぶ実装を追加。

---

**識別子**: RC-60（GitHub id: 3329971268）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:416`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・319 行）
+  } finally {
+    cancelLoadingEventId.value = null
+  }
+}
+
+// Stripeからのリダイレクトでイベントに参加した場合の処理
+const isUserSuccessJoinEventDialogVisible = ref(false)
+if (route.query.eventId != null && route.query.communityAccount != null) {
+  isUserSuccessJoinEventDialogVisible.value = true
+}
+
+/** 注文完了で遷移したとき・既存 Pinia ストアが古い一覧のままになるのを防ぐ */
+watch(
+  () => [route.query.eventId, route.query.communityAccount] as const,
+  ([eventId, communityAccount]) => {
+    if (profileUserId === '') return
+    if (eventId != null && communityAccount != null) {
+      userEventListStore.reload()
+    }
+  },
+  { immediate: true },
+)
+
+const downloadReceipt = (eventId: string, stripeId: string) => {
+  window.open(getReceiptPath(eventId, stripeId), '_blank')
+}
+
+const counts = computed(() => previewData.value?.counts ?? null)
+
+type ProfileStatRow = {
+  key: 'participated_event' | 'friend_count' | 'joined_community' | 'managed_community' | 'ordered_food'
+  label: string
+  value: number
+}
+
+const profileStatRows = computed((): ProfileStatRow[] => {
+  const c = counts.value
+  return [
+    { key: 'friend_count', label: $t('user_profile.counts.stat_friends_count'), value: c?.friend_count ?? 0 },
+    {
+      key: 'participated_event',
+      label: $t('user_profile.counts.stat_participated_events_count'),
+      value: c?.participated_event_count ?? 0,
+    },
+    {
+      key: 'joined_community',
+      label: $t('user_profile.counts.stat_joined_communities_count'),
+      value: c?.joined_community_count ?? 0,
+    },
+    {
+      key: 'managed_community',
+      label: $t('user_profile.counts.stat_managed_communities_count'),
+      value: c?.managed_community_count ?? 0,
+    },
+    { key: 'ordered_food', label: $t('user_profile.counts.stat_foods_count'), value: c?.ordered_food_count ?? 0 },
+  ]
+})
+
+const previewEvents = computed(() => previewData.value?.previews.events ?? [])
+const previewJoinedCommunities = computed(() => previewData.value?.previews.joined_communities ?? [])
+const previewManagedCommunities = computed(() => previewData.value?.previews.managed_communities ?? [])
+const previewFoods = computed(() => previewData.value?.previews.foods ?? [])
+const previewOrders = computed(() => previewData.value?.previews.orders ?? [])
+
+/** 本人以外には非公開を表示しない（Callable 側でも除外。二重防御） */
+const isPublicProfileItemVisible = (isPublic: boolean) => isOwner.value || isPublic
+
+const visiblePreviewEvents = computed(() =>
+  previewEvents.value.filter((event) => isPublicProfileItemVisible(event.is_public)),
+)
+const visiblePreviewJoinedCommunities = computed(() =>
+  previewJoinedCommunities.value.filter((community) => isPublicProfileItemVisible(community.is_public)),
+)
+const visiblePreviewManagedCommunities = computed(() =>
+  previewManagedCommunities.value.filter((community) => isPublicProfileItemVisible(community.is_public)),
+)
+const visiblePreviewFoods = computed(() =>
+  previewFoods.value.filter((food) => isOwner.value || food.is_visible_to_viewer),
+)
+const visiblePagedFoods = computed(() => pagedFoods.value.filter((food) => isOwner.value || food.is_visible_to_viewer))
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  ログアウト時に取得済みの非公開フードを破棄する**

本人プロフィールを認証済みで開いた後に同じ画面上でログアウトすると、Callable が本人向けに返した非公開イベント由来のフードにも `is_visible_to_viewer: true` が設定済みであるため、`isOwner` が false に変わった後もこの条件を通過します。イベントとコミュニティは `is_public` で再判定していますが、フードには同等の不変フィールドがなく、ログアウト済み画面に非公開イベント名・メニュー名・価格などが残ります。認証状態の変更時にフードを消去して再取得するか、フードにも `is_public` を返して現在の閲覧者状態で再判定してください。

Useful? React with 👍 / 👎.

**コメント要約**: ログアウト後も非公開フードが `is_visible_to_viewer` で表示され続ける。

**判断結果**: ✅ 対応済み（2026-06-02 実装）

**判断理由**: RC-59 と同様に認証 UID 変化で preview / foods を `reload`。§4.2.0 ではログアウト後も限定公開フードの**名称は表示**し、`is_public` / `is_linkable` と `canLinkToDetail` で**リンクのみ**切り替える（旧指摘の「非公開フードを画面から消す」は不要）。`visiblePagedFoods` の `is_visible_to_viewer` フィルタは削除済み。

---

**識別子**: RC-61（GitHub id: 3329923027）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:919`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・800 行）
+                  </v-list-item>
+                </v-list>
+              </v-card-text>
+            </v-card>
+          </template>
+        </v-window-item>
+
+        <v-window-item :value="TAB_FRIENDS">
+          <v-row v-if="activeFriends.length > 0" class="align-center mb-2">
+            <v-col cols="12" sm="4">
+              <v-btn-toggle
+                v-model="friendSortBy"
+                mandatory
+                color="primary"
+                density="comfortable"
+                divided
+                class="friend-sort-toggle w-100"
+              >
+                <v-btn value="meet_count">{{ $t('user.friend_sort_meet_count') }}</v-btn>
+                <v-btn value="last_met_at">{{ $t('user.friend_sort_last_met_at') }}</v-btn>
+              </v-btn-toggle>
+            </v-col>
+          </v-row>
+          <v-row v-if="activeFriends.length > 0">
+            <v-col v-for="friend in activeFriends" :key="friend.user_id" cols="12" sm="6" md="4">
+              <FriendCard
+                :friend="friend"
+                :target-user-id="profileUserId"
+                :target-user-name="user.user_name"
+                :is-owner="isOwner"
+              />
+            </v-col>
+          </v-row>
+          <div v-else-if="!activeUserFriendsStore?.loading" class="text-body-1 text-medium-emphasis pa-4">
+            {{ isOwner ? $t('user.friend_empty_tab') : $t('user.friend_empty_other', { name: user.user_name }) }}
+          </div>
+          <v-row class="justify-center">
+            <v-col cols="auto">
+              <IncrementalLoader
+                :loaded-count="activeFriends.length"
+                :total-count="activeUserFriendsStore?.hasMore ? Number.MAX_SAFE_INTEGER : activeFriends.length"
+                @load="activeUserFriendsStore?.next()"
+              />
+            </v-col>
+          </v-row>
+        </v-window-item>
+
+        <v-window-item :value="TAB_EVENTS">
+          <div
+            v-if="isUserEventsLoaded && visibleUserEvents.length === 0"
+            class="text-body-1 text-medium-emphasis pa-4"
+          >
+            {{ $t('user_profile.empty.events') }}
+          </div>
+          <v-row v-else>
+            <v-col v-for="event in visibleUserEvents" :key="`event_${event.event_id}`" sm="12" md="6" lg="4" cols="12">
+              <div class="event-card">
+                <router-link :to="getEventPath(event.community_account, event.event_id)">
+                  <ProfileEventCard :event="event" />
+                </router-link>
+              </div>
+            </v-col>
+          </v-row>
+          <v-row class="justify-center">
+            <v-col cols="auto">
+              <IncrementalLoader
+                :loaded-count="userEvents.length"
+                :total-count="userEventsTotalCount ?? Number.MAX_SAFE_INTEGER"
+                @load="userEventListStore.next()"
+              />
+            </v-col>
+          </v-row>
+        </v-window-item>
+
+        <v-window-item :value="TAB_COMMUNITIES">
+          <div v-if="isCommunitiesTabReady && isCommunitiesTabEmpty" class="text-body-1 text-medium-emphasis pa-4">
+            {{ $t('user_profile.empty.communities') }}
+          </div>
+          <template v-else-if="isCommunitiesTabReady">
+            <template v-if="showJoinedSection">
```

**レビュワーのコメント**（原文）:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  非公開だけの先頭ページでもコミュニティを追読みする**

本人以外のプロフィールで、参加コミュニティの先頭 5 件がすべて非公開、かつ 6 件目以降に公開コミュニティがある場合、更新後仕様どおり非公開項目を `memberCommunities` から除外すると `showJoinedSection` が false になります。この `v-if` の内側に `IncrementalLoader` も置かれているため次ページを取得できず、公開コミュニティが残っていても一覧が空になります。管理コミュニティ側の `showManagedSection` にも同じ問題があります。非公開項目を表示するのではなく、表示項目が 0 件でも未取得ページが残る間は追読みできるようにしてください。

Useful? React with 👍 / 👎.

**コメント要約**: コミュニティタブで非公開のみの先頭ページだと `IncrementalLoader` が出ず追読みできない。

**判断結果**: ✅ 対応済み（2026-05-31 実装）

**判断理由**: `memberCommunityListHasMore` / `managerCommunityListHasMore` で visible 0 件でも `IncrementalLoader` を表示。`tryLoadMoreCommunitiesIfEmpty` を追加（RC-61）。

---

## 評価セッション（2026-05-31・4区分への再分類）

- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **評価日時**: 2026-05-31 21:31 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **Outdated 除外件数**: 0 件
- **レビュー非該当スキップ件数**: 0 件（既存 RC の判断ラベルのみ更新）
- **備考**: 旧ラベル「🚨 修正推奨」「❌ 未対応」を4区分に統一。コード照合により RC-47/48/52/53/54 を ✅ に昇格。RC-50/57/59/61 を 🟡 に降格。

### RC 別対応チェックリスト（本セッションで更新した判断）

- [ ] **RC-50**（GitHub id: 3328599646）— 🟡 修正提案
- [x] **RC-55**（GitHub id: 3330003644）— ✅ 対応済み
- [x] **RC-56**（GitHub id: 3330003647）— ✅ 対応済み
- [x] **RC-57**（GitHub id: 3330114203）— ✅ 対応済み
- [ ] **RC-58**（GitHub id: 3330114200）— 🚨 必須修正
- [x] **RC-59**（GitHub id: 3329971269）— ✅ 対応済み（2026-06-02 コード照合で更新）
- [x] **RC-60**（GitHub id: 3329971268）— ✅ 対応済み（2026-06-02 §4.2.0 解釈で更新）
- [x] **RC-61**（GitHub id: 3329923027）— ✅ 対応済み
- [x] **RC-47**（GitHub id: 3329884054）— ✅ 対応済み（2026-06-02 §4.2.0）
- [x] **RC-48**（GitHub id: 3329888506）— ✅ 対応済み（RC-47 同根）
- [x] **RC-52**（GitHub id: 3329884055）— ✅ 対応済み
- [x] **RC-53**（GitHub id: 3303247303）— ✅ 対応済み
- [x] **RC-54**（GitHub id: 3329888504）— ✅ 対応済み
- [x] **RC-19**（GitHub id: 3214771200）— ✅ 対応済み（2026-06-02 §4.2.0）
- [x] **RC-27**（GitHub id: 3240875088）— ✅ 対応済み（2026-06-02 §4.2.0）
- [x] **RC-28**（GitHub id: 3240875092）— ✅ 対応済み（2026-06-02 §4.2.0）

---

## 評価セッション（2026-06-02・§4.2.0 実装反映）

- **評価者**: Cursor Agent（`shokujii-code-review` / コード照合）
- **評価日時**: 2026-06-02 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **Outdated 除外件数**: 0 件
- **レビュー非該当スキップ件数**: 0 件
- **備考**: §4.2.0 実装を作業ツリーで照合。RC-19 / 27 / 28 / 47 / 48 / 59 / 60 を ✅ に更新。RC-58 は `resolveUserFriendMeetLog` の退会友人チェック未実装のため 🚨 維持。RC-50 は 🟡 維持。

### RC 別対応チェックリスト（本セッション）

- [x] **RC-19** — ✅ 対応済み
- [x] **RC-27** — ✅ 対応済み
- [x] **RC-28** — ✅ 対応済み
- [x] **RC-47** — ✅ 対応済み
- [x] **RC-48** — ✅ 対応済み
- [x] **RC-59** — ✅ 対応済み
- [x] **RC-60** — ✅ 対応済み
- [ ] **RC-58** — 🚨 必須修正（未対応）
- [ ] **RC-50** — 🟡 修正提案（未対応）

### 推奨対応順序（未対応）

1. **RC-58** — `resolveUserFriendMeetLog` で `getUser(friendUserId)` し `is_deleted` なら `null`
2. **RC-50** — `deleteUserAccount` 成功時に友人側の `friend_count` 再集計（別 Issue 可）

---

## 仕様変更（2026-06-02）— 限定公開のマイページ・meet log

**正本**: `documents/03_参加者獲得/02_マイページ.md` **§4.2.0**、`02_友人一覧機能.md` §5.2.1.1、エンプラ `07_エンタープライズ_マイページ・友人.md` EP-15

| 項目 | 2026-05-25〜05-31（旧） | 2026-06-02（新） |
| --- | --- | --- |
| 限定公開の表示（他者） | **非表示**（API から除外） | **表示**（メタ情報） |
| リンク（他者） | — | **なし**（`is_linkable === false`） |
| リンク（本人） | あり | **あり** |
| 限定公開タグ | — | **マイページ・meet log では出さない** |
| meet log（他者） | `is_public == false` を **除外** | **含める**、リンクのみ本人 |

**実装タスク（PR #1985 ブランチ）** — **2026-06-02 対応済み**:

- [x] `common/src/apis/userProfile.ts` / `userFriends.ts` — `is_linkable`（＋ foods に `is_public`）
- [x] `profileItemVisibility.ts` — `computeProfileItemLinkableToViewer`
- [x] `userFriendsResolver.ts` — `buildMeetLogItems` + `is_linkable`
- [x] `stores/event.ts` / `community.ts` プレビュー — 他者向け `is_public` クエリ廃止
- [x] `UserProfilePage.vue` — 一覧除外削除、`canLinkToDetail`、認証 watch で reload
- [x] `FriendMeetLogDialog.vue` — `row.is_linkable` でリンク
- [x] `UserEventCard` — `hidePrivateScopeChip`（マイページ）

**RC との関係**: RC-19 / 27 / 28 / 47 / 48 / 59 / 60 は **✅ 対応済み**（冒頭チェックリスト・各 RC 本文の判断結果を更新済み）。Codex 当初指摘（表示してリンクのみ制御）は **2026-06-02 仕様で採用し実装反映**。

---

## 実装メモ（2026-05-31・RC-55/56/57/61）

- **RC-55**: [`community.ts`](functions/default/src/stores/community.ts) — `communities.members` / `managers` の count 集計
- **RC-56**: [`firestore.rules`](firestore.rules) — プロフィールカウント 6 フィールドのクライアント update 拒否
- **RC-57**: [`memberOrder.ts`](functions/default/src/stores/memberOrder.ts) `countParticipatedEventsForUser` + [`recountUserProfileCounts.ts`](functions/default/src/utils/recountUserProfileCounts.ts)
- **RC-61**: [`UserProfilePage.vue`](user/src/components/profile/UserProfilePage.vue) — コミュニティタブの hasMore 追読み（RC-53/54 同型）
- テスト: `community.test.ts`, `memberOrder.test.ts`, `recountUserProfileCounts.test.ts` 更新

## 実装メモ（2026-06-02・§4.2.0 / RC-19,27,28,47,48,59,60）

- [`profileItemVisibility.ts`](functions/default/src/utils/profileItemVisibility.ts) — `computeProfileItemLinkableToViewer`
- [`userProfile.ts`](functions/default/src/userProfile.ts) — プレビュー / フードに `is_linkable`・foods に `is_public`
- [`userFriendsResolver.ts`](functions/default/src/utils/userFriendsResolver.ts) — `buildMeetLogItems`
- [`event.ts`](functions/default/src/stores/event.ts) / [`community.ts`](functions/default/src/stores/community.ts) — プレビュー query から `is_public` 削除
- [`memberOrder.ts`](functions/default/src/stores/memberOrder.ts) — `filterOrdersForProfile`（削除済みのみ除外）
- [`UserProfilePage.vue`](user/src/components/profile/UserProfilePage.vue) — `canLinkToDetail`、認証 watch
- [`FriendMeetLogDialog.vue`](base/src/components/FriendMeetLogDialog.vue) — `is_linkable`
- [`UserEventCard.vue`](base/src/components/UserEventCard.vue) — `hidePrivateScopeChip`
- テスト: `userProfile.test.ts`, `userFriendsResolver.test.ts`

---

## 評価セッション（2026-06-03・RC-58 / RC-50 実装反映）

- **評価日時**: 2026-06-03 14:12 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **Outdated 除外件数**: 0 件（アクティブインライン 54 件・paginate 取得）
- **レビュー非該当スキップ件数**: 0 件（本セッションは新規 GitHub コメントの採番なし。既存 RC のコード照合のみ）
- **備考**: コミット `49858d69`（`[doc][functions] #1248 退会友人の meet log 漏洩防止と friend_count 再集計を実装`）を作業ツリーで照合。RC-58 / RC-50 を ✅ に昇格。

### RC 別対応チェックリスト（本セッション）

- [x] **RC-58**（GitHub id: 3330114200）— ✅ 対応済み
- [x] **RC-50**（GitHub id: 3328599646）— ✅ 対応済み

---

**識別子**: RC-58（GitHub id: 3330114200）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/utils/userFriendsResolver.ts:162`

**該当コード（レビュー時点の diff）**:

```diff
+  const friendUser = await getUser(friendUserId, false)
+  if (friendUser == null || friendUser.is_deleted) {
+    return null
+  }
+
   const history = normalizeHistory(friend.event_history)
```

（出典: コミット `49858d69` の `userFriendsResolver.ts` 差分）

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  退会済みの友人に対する同席履歴の直接取得を拒否する**

`getUserFriendMeetLog` は未ログインでも呼べる公開 Callable ですが、この処理は `friends/{friendUserId}` の残存だけで履歴を返し、友人側の `users/{friendUserId}` が退会済みかを確認していません。退会時に friends ドキュメントを残置する設計のため、一覧では除外された退会ユーザーでも、既知の UID を指定すると第三者が `meet_count` と公開イベントの同席履歴を取得できます。`getUser` 等で友人側も解決し、存在しない場合や `is_deleted === true` の場合は `null` を返してください。

Useful? React with 👍 / 👎.

**コメント要約**: 退会済み友人 UID 指定で meet log が漏れる。友人側 `users` の `is_deleted` 確認が必要。

**判断結果**: ✅ 対応済み

**判断理由**: `resolveUserFriendMeetLog` で `getUser(friendUserId, false)` を追加し、`null` または `is_deleted` のとき早期 `null`（`getEventsInCommunities` 未呼び出し）。`userFriendsResolver.test.ts` に退会・不存在のテストを追加。公開 Callable の漏洩指摘は解消。

---

**識別子**: RC-50（GitHub id: 3328599646）

**レビュワー**: Codex

**指摘箇所**: `functions/default/src/deleteUserAccount.ts:79`

**該当コード（レビュー時点の diff）**:

```diff
+    // RC-50: 退会者の友人相手側 friend_count を再集計（一覧除外ルールとキャッシュを一致させる）
+    try {
+      const friendUserIds = (await listFriendUserIds(uid)).filter((id) => id !== '' && id !== uid)
+      await recountUserProfileCountsForUsers(friendUserIds)
+    } catch (error) {
+      logger.error('recountUserProfileCountsForUsers failed after deleteUserAccount', { userId: uid, error })
+    }
```

（出典: コミット `49858d69` の `deleteUserAccount.ts` 差分。`stores/userFriend.ts` に `listFriendUserIds` 追加）

**レビュワーのコメント（原文）**:

**![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)****  退会時に残存ユーザーの friend_count を再集計する**

友人が論理削除された場合、この条件により次回再集計時には正しく除外できますが、`deleteUserAccount.ts:61` の退会処理では相手側ユーザーの再集計が呼ばれません。例えば A-B が友人で B が退会すると、`getUserFriends` は B を除外する一方、A の新規キャッシュ `friend_count` は以前の値のまま残り、プロフィール表示と一覧が不一致になります。退会成功時に対象ユーザーの friends を列挙し、残存する相手側 uid を再集計してください。

Useful? React with 👍 / 👎.

**コメント要約**: 退会後、友人側の `friend_count` キャッシュが一覧除外ルールとずれる。

**判断結果**: ✅ 対応済み

**判断理由**: トランザクション成功後に `listFriendUserIds(uid)` で相手 uid を列挙し `recountUserProfileCountsForUsers` を呼ぶ。再集計失敗は `logger.error` のみで退会成功は維持（A1）。`deleteUserAccount.test.ts` で呼び出し・失敗許容を検証。仕様書（`02_マイページ.md` §5.1.1.1、`02_友人一覧機能.md` DS-4、`13_アカウント削除.md` §6.3）も同コミットで追記済み。

---

## 実装メモ（2026-06-03・RC-58 / RC-50）

- **RC-58**: [`userFriendsResolver.ts`](functions/default/src/utils/userFriendsResolver.ts) — `getUser` + `is_deleted` ガード
- **RC-50**: [`deleteUserAccount.ts`](functions/default/src/deleteUserAccount.ts) — `listFriendUserIds` + `recountUserProfileCountsForUsers`（try/catch）
- [`userFriend.ts`](functions/default/src/stores/userFriend.ts) — `listFriendUserIds`
- テスト: `userFriendsResolver.test.ts`, `deleteUserAccount.test.ts`
- コミット: `49858d69`

---

## 評価セッション（2026-06-03・Codex / Copilot 再レビュー）

- **評価日時**: 2026-06-03 14:16 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **Outdated 除外件数**: 0 件
- **レビュー非該当スキップ件数**: 1 件（GitHub id: `4609063350` — `@copilot @codex review` レビュー依頼定型文）
- **備考**: 2026-06-03T04:32 前後の新規インライン 4 件を RC-62〜65 として初回登録。RC-46（Functions `userProfile.ts` の store 化）とは別箇所。

### RC 別対応チェックリスト（本セッション）

- [x] **RC-62**（GitHub id: 3345937500）— ✅ 対応済み
- [x] **RC-63**（GitHub id: 3345939351）— ✅ 対応済み
- [x] **RC-64**（GitHub id: 3345939363）— ✅ 対応済み
- [x] **RC-65**（GitHub id: 3345937495）— ✅ 対応済み

### 対応内容（2026-06-03）

1. **RC-65** — `UserProfilePage.vue` の `fetchUser` / `getDocs` を撤去し `profileUserId = props.userId` に簡素化（route param は user_id = ドキュメント ID）
2. **RC-62** — `firestore.rules` に `userProfileCountsZeroOrAbsentOnCreate()` を追加し `allow create` に適用
3. **RC-64** — `memberOrder.ts` の `event.js` import を型のみにし `countParticipatedEventsForUser` を動的 import 化
4. **RC-63** — `community.ts` の JSDoc を各 export 直前へ移動

---

**識別子**: RC-62（GitHub id: 3345937500）

**レビュワー**: Codex

**指摘箇所**: `firestore.rules:118`

**該当コード（レビュー時点の diff）**:

```diff
@@ -100,7 +115,13 @@ service cloud.firestore {
         match /users/{user_id} {
             allow read: if true
             // ユーザー情報はログインユーザーでないと create/update/deleteできない
-            allow create, update: if request.auth != null && request.auth.uid == user_id
+            allow create: if request.auth != null && request.auth.uid == user_id
             allow update: if request.auth != null
                 && request.auth.uid == user_id
                 && userProfileCountsUnchanged()
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  新規作成時もカウント改ざんを拒否する**

この `create` は本人なら任意の `request.resource.data` を許可しているため、クライアントから `participated_event_count` や `friend_count` などを含む `users/{uid}` を新規作成すると、Functions 専用にしたい公開プロフィールの冗長カウントを任意値で作れてしまいます。`userProfileCountsUnchanged()` は update にしか掛かっていないので、create 側でもカウントフィールドを禁止するか 0/未設定に固定してください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-56 は update のみ保護。create ではプロフィールカウントを任意設定できる隙間がある。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: 指摘は妥当。`userProfileCountsUnchanged()`（9–22 行付近）は update 専用で、create は本人なら全フィールド許可のまま。権限昇格ではないがキャッシュ信頼性と RC-56 の意図がずれる。create でも `userProfileCountFields()` を含めない／未設定のみ許可するルール追加を推奨。P2 のためマージ必須度は RC-65 より低い。**対応**: `userProfileCountsZeroOrAbsentOnCreate()` を追加し `allow create` に適用。

---

**識別子**: RC-63（GitHub id: 3345939351）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/stores/community.ts:274`

**該当コード（レビュー時点の diff）**:

```diff
+/**
+ * 管理コミュニティ数（親 `communities.managers` 配列に userRef が含まれる件数）。RC-55 同上。
+ */
+/**
+ * マイページプロフィール用のコミュニティプレビュー（§4.2.0）。
+ * 限定公開も含め参加・運営実績から limit する。
+ */
+export const listCommunitiesForProfilePreview = async (params: {
+  ...
+}) => { ... }
+
+export const countManagedCommunitiesForUser = async (userId: string): Promise<number> => {
```

**レビュワーのコメント（原文）**:

[must] `countManagedCommunitiesForUser` の JSDoc が関数から離れており、直後に空の JSDoc ブロックが残っているため、意図が読み取りづらくなっています。`listCommunitiesForProfilePreview` と `countManagedCommunitiesForUser` のコメントをそれぞれの直前に配置してください。

**コメント要約**: JSDoc が `listCommunitiesForProfilePreview` に誤付与され、`countManaged` 用コメントが宙に浮いている。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: 現行コードでも Copilot 指摘どおり（254–274 行付近）。RC-55 実装時の編集ミスで動作影響はないが可読性・保守性の問題。各 export 直前に JSDoc を移すだけで足りる。**対応**: `countManagedCommunitiesForUser` / `listCommunitiesForProfilePreview` の JSDoc を各関数直前へ整理。

---

**識別子**: RC-64（GitHub id: 3345939363）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/stores/memberOrder.ts:12`

**該当コード（レビュー時点の diff）**:

```diff
@@ -8,6 +8,8 @@ import {
 } from 'firebase-admin/firestore'
 import { EventMember, EventMemberOrder, EventMemberOrderStatusType } from '@shokujii/common/schemas/EventMemberOrder.js'
 import { EventStripe } from '@shokujii/common/schemas/EventStripe.js'
+import { getCommunityEventKey, getEventsInCommunities, type ShokujiiEvent } from './event.js'
+import { MAX_PROFILE_PREVIEW_SKIP_PAGES } from '../utils/profileItemVisibility.js'
```

**レビュワーのコメント（原文）**:

[must] `stores/event.ts` が `./memberOrder.js` を import している一方で、本ファイルが `./event.js` を import しており、`event.ts` ⇄ `memberOrder.ts` の循環依存になっています（ESM だと初期化順によっては未初期化の export 参照リスクがあります）。`filterOrdersForProfile` は動的 import で回避しているので、`countParticipatedEventsForUser` 側も同様に動的 import に寄せる、もしくは `getCommunityEventKey/getEventsInCommunities` を循環しないユーティリティへ切り出す等で依存を一方向にしてください。

**コメント要約**: 循環依存。`countParticipatedEventsForUser`（RC-57）が静的 import でリスクが残る。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: `event.ts` → `memberOrder.ts`、`memberOrder.ts` → `event.ts` の双方向 import は事実。`filterOrdersForProfile` は動的 `import('./event.js')`（344 行付近）で回避済みだが `countParticipatedEventsForUser`（427 行〜）は静的 import を使用。テスト通過中でも ESM 初期化順で将来壊れうる。セキュリティ・データ不整合ではないため設計改善として 🟡。**対応**: `event.js` を型のみ import に変更し `countParticipatedEventsForUser` を動的 import 化。

---

**識別子**: RC-65（GitHub id: 3345937495）

**レビュワー**: Codex

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:81`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・76 行）
+const userIdRef = ref('')
+const fetchUser = async (identifier: string) => {
+  const userCollection = collection(db, 'users')
+  const queryById = query(userCollection, where('user_id', '==', identifier), limit(1))
+
+  try {
+    const queryByIdSnapshot = await getDocs(queryById)
+
+    if (!queryByIdSnapshot.empty) {
+      userIdRef.value = queryByIdSnapshot.docs[0].data().user_id
+    } else {
+      userIdRef.value = ''
+    }
+  } catch {
+    userIdRef.value = ''
+  }
+}
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  Firestore 読み取りを store 経由に移す**

AGENTS.md の「Firestore 操作の必須ルール」では DB 操作は必ず store 経由・withConverter 付き ref を使う必要がありますが、この `getDocs` はコンポーネントから `users` を直接読んでいます。プロフィール ID 解決のたびにプロジェクト共通のバリデーションや store ルールを迂回するため、`base/src/stores/` 側の関数に切り出してそこから呼ぶ形にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: URL 用 `user_id` 解決がコンポーネント直の `getDocs` で、store / withConverter ルール違反。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: AGENTS.md の Firestore 必須ルールに明確に抵触。RC-46 は `functions/default/src/userProfile.ts` の store 化であり本件は別箇所。`fetchUser`（76–91 行）のみが `collection` + `getDocs` で `users` を直読み。`base/src/stores/user.ts` に `user_id` フィールド検索 API を追加し withConverter 付き query に寄せるのが正しい対応。Codex P1 とプロジェクト規約の両方でマージ前対応が妥当。**対応**: store 追加ではなく `fetchUser` / `userIdRef` を撤去し `profileUserId = props.userId` に簡素化（route param = ドキュメント ID、`useUserStore.exists` で存在判定）。

---

## 評価セッション（2026-06-03・shokujii-code-review）

- **評価日時**: 2026-06-03 14:24 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **レビュー種別**: Cursor Agent による能動的な差分レビュー（`git diff origin/development...HEAD`、GitHub インラインなし）
- **Outdated 除外件数**: 0 件（API 取得なし）
- **レビュー非該当スキップ件数**: 0 件
- **補足**: `UserProfilePage.vue` の `fetchUser` 直読みは既存 **RC-65** と同一指摘のため本セッションでは重複採番しない。meet log の退会除外は RC-58、限定公開リンク制御は RC-19 でカバー済み。本セッションは未登録の新規指摘のみを RC-66〜73 として採番した。
- **総評**: 二層モデル（`friends` 正本 + `friend_count` キャッシュ）・退会者除外・トランザクションの read-before-write・`createModuleLogger` / `Promise.allSettled` の使い分け・Firestore ルールでのカウント保護など規約遵守は良好。新規ファイルに `any` / `@ts-ignore` / `console.*` なし。残課題はフロントの日時表示規約逸脱と一部の store 未経由・冗長 `as` 等。
- **対応状況更新**: 2026-06-03 JST — RC-66 / 67 / 69 / 71 / 72 を ✅ に更新（RC-65 は Codex セッション側で対応済み）

### RC 別対応チェックリスト

- [x] **RC-66**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`resolveUserPath` / `resolveEventPath` を props 化）
- [x] **RC-67**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（冗長な `as` キャスト 3 箇所を除去）
- [x] **RC-68**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（`updateUserProfileCounts` の部分 `update()` は意図的）
- [x] **RC-69**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`listFriendUserIds` + `countOrderedFoodsForUser` 経由に store 化）
- [x] **RC-70**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（meet log の非公開イベント名表示は §4.2.0 どおり）
- [x] **RC-71**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`convertToDate` / `convertToDateWeekdayShort` に統一）
- [x] **RC-72**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（`setupAutoLoadWhenEmpty` ヘルパに集約）
- [x] **RC-73**（GitHub id: なし・エージェントレビュー）— 👌 修正不要（`meet_count` と可視 meet_log 行数の差）

---

**識別子**: RC-66（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/components/FriendCard.vue:8`, `base/src/components/FriendMeetLogDialog.vue:9`

**該当コード（レビュー時点の diff）**:

```ts
// base/src/components/FriendCard.vue
import { getUserPath } from '@/router/utils'

// base/src/components/FriendMeetLogDialog.vue
import { getEventPath, getUserPath } from '@/router/utils'
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: base コンポーネント（`FriendCard.vue` / `FriendMeetLogDialog.vue`）が `@/router/utils`（アプリ側エイリアス）を import しており、base→app の依存反転になっている → AGENTS.md「パッケージ依存関係の注意」は新規コードでの依存反転回避を求めている。ルーティングは `emit` でアプリ側に委譲する形が望ましい。

**コメント要約**: 共有 base コンポーネントがアプリのルーターユーティリティに依存している（依存反転）。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: AGENTS.md は「`base` は本来 `common` のみに依存すべき」「新規コード作成時はこの依存反転を避けて設計すること」と明記。ただし既存 base コンポーネント（`EventMemberList.vue` 等多数）が同じ `@/` 依存を持つ既存パターンであり、マージ必須ではない。理想は `to`/`@click` を prop・emit で受ける汎化。低優先。**対応**: `FriendCard.vue` / `FriendMeetLogDialog.vue` から `@/router/utils` を撤去し `resolveUserPath` / `resolveEventPath` を props 化。`UserProfilePage.vue` から `getUserPath` / `getEventPath` を注入。型は `base/src/types/profilePathResolvers.ts` に切り出し。

---

**識別子**: RC-67（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/stores/userProfilePreview.ts:29`, `functions/default/src/stores/userFriend.ts:108`, `functions/default/src/userProfile.ts:236`

**該当コード（レビュー時点の diff）**:

```ts
// base/src/stores/userProfilePreview.ts:29
if (err instanceof FirebaseError && (err as { code?: string }).code === 'functions/not-found') {

// functions/default/src/stores/userFriend.ts:108
value: (lastDoc.data() as UserFriend)[sortBy],

// functions/default/src/userProfile.ts:236
cursor: cursor as ProfileFoodsPageCursor | undefined,
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: 型推論で足りる冗長な `as` キャストが 3 箇所ある → (1) `userProfilePreview.ts:29` は `instanceof FirebaseError` で絞れているので `err.code` で足りる。(2) `userFriend.ts:108` は `withConverter` 済みのため `lastDoc.data()` は既に `UserFriend`。(3) `userProfile.ts:236` は `UserFoodsCursor` と `ProfileFoodsPageCursor` が同一構造なので型を共有して `as` を排除できる。

**コメント要約**: 不要な `as` キャスト 3 箇所。型推論・型共有で除去可能。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: shokujii-code-review「`as` によるキャストを使用していないか（型推論で解決できるはず）」に該当。動作上の問題はないが型安全性・可読性のため除去を推奨。マージ必須ではない。**対応**: `userProfilePreview.ts` / `userFriend.ts` / `userProfile.ts` の冗長 `as` を除去。

---

**識別子**: RC-68（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/stores/user.ts:214`

**該当コード（レビュー時点の diff）**:

```ts
export const updateUserProfileCounts = async (
  userId: string,
  counts: UserProfileCountsUpdate,
  transaction?: Transaction,
): Promise<void> => {
  const userRef = getUserRef(userId)
  const update = {
    ...counts,
    counts_updated_at: Timestamp.now(),
  }
  if (transaction === undefined) {
    await userRef.update(update)
  } else {
    transaction.update(userRef, update)
  }
}
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: `updateXXX` 系は全フィールド書き戻し（Partial マージ禁止）という規約に対し、本関数は `update()` でカウント系フィールド＋`counts_updated_at` のみを部分更新している → ただし `updated_at` 等の監査フィールドを変えないための意図的な設計であり、JSDoc・`firestore.rules` の `userProfileCountsUnchanged()` とも整合。

**コメント要約**: カウントキャッシュ専用の部分 `update()`。全書き戻し規約の例外。

**判断結果**: 👌 修正不要

**判断理由**: コメントに「converter 経由の set だと updated_at まで書き換わるための例外的な扱い」と明記され、フィールド消失リスクもない（`update` は指定キーのみ変更）。RC-56 のルール保護とも整合し、意図的かつ安全。対応不要。

---

**識別子**: RC-69（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/utils/recountUserProfileCounts.ts:48`, `functions/default/src/utils/recountUserProfileCounts.ts:53`

**該当コード（レビュー時点の diff）**:

```diff
+export const computeUserProfileCounts = async (userId: string): Promise<UserProfileCounts> => {
+  const db = getFirestore()
+
+  const friendsSnapshot = await db.collection('users').doc(userId).collection('friends').get()
+  const friendUserIds = friendsSnapshot.docs.map((doc) => doc.id)
+
+  const [participatedCount, orderedAgg, joinedCount, managedCount, activeFriendCount] = await Promise.all([
+    countParticipatedEventsForUser(userId),
+    db.collectionGroup('member_orders').where('user_id', '==', userId).where('status', '==', 'ordered').count().get(),
+    countJoinedCommunitiesForUser(userId),
+    countManagedCommunitiesForUser(userId),
+    computeActiveFriendCount(friendUserIds),
+  ])
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `computeUserProfileCounts` が `friends` サブコレ列挙と `member_orders` の `count()` を `db` 直接クエリで実行している（store 未経由）→ 友人列挙は最新コミットで追加済みの `listFriendUserIds(userId)`（store）で置換可能。`ordered_food_count` の count も `countParticipatedEventsForUser` 等と同様に store 関数（例: `countOrderedFoodsForUser`）に切り出すと一貫する。

**コメント要約**: 再集計ユーティリティ内の Firestore 直クエリ 2 箇所を store 化すべき。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: AGENTS.md「DB 操作は必ず store 経由」に照らすと util から直接 `db.collection/collectionGroup` を叩いている。`listFriendUserIds` が既に存在するため友人列挙は即置換可能。count 集計も store へ寄せると RC-55/57 と統一的。集計 read で `withConverter` 不要な軽量クエリではあるが、規約一貫性のため提案。RC-65（フロント直読み）・RC-46（`userProfile.ts` 直読み）とは別箇所。**対応**: `memberOrder.ts` に `countOrderedFoodsForUser` を追加し `recountUserProfileCounts` を `listFriendUserIds` + store 経由に変更。

---

**識別子**: RC-70（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `functions/default/src/utils/userFriendsResolver.ts:36`

**該当コード（レビュー時点の diff）**:

```ts
const toMeetLogItem = (
  entry: MeetLogHistoryEntry,
  event: ShokujiiEvent | undefined,
  viewerUid: string | null,
  targetUserId: string,
): UserFriendMeetLogItem | null => {
  if (event == null || event.is_deleted) {
    return null
  }
  const isPublic = event.is_public === true
  return {
    ...
    event_name: event.event_name,
    is_public: isPublic,
    is_linkable: computeProfileItemLinkableToViewer({ isPublic, viewerUid, targetUserId }),
  }
}
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: meet log は非公開イベントでも `event_name` を返し、`is_linkable` でリンク可否のみを制御している。他者が同席ログを開くと非公開イベント名が見える → 仕様 §4.2.0「限定公開も本人・他者とも表示、リンクは本人のみ」の意図どおりであれば問題なし。

**コメント要約**: 非公開イベント名が他者にも表示される点が仕様意図どおりかの確認。

**判断結果**: 👌 修正不要

**判断理由**: `02_マイページ.md` §4.2.0 で「限定公開は本人・他者とも表示、リンクは本人のみ」と確定済み（RC-19 / 27 / 28 / 47 / 48 と同じ方針）。meet log もこの方針に従っており、名前表示は意図的。対応不要。

---

**識別子**: RC-71（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:507`

**該当コード（レビュー時点の diff）**:

```ts
/** プロフィールプレビュー用。イベントは曜日付き、それ以外は日付のみ（`datetimeFormats` と整合） */
const formatProfileDate = (epochMillis: number, kind: 'withWeekday' | 'date' = 'date') => {
  if (!epochMillis) return ''
  const date = new Date(epochMillis)
  return kind === 'withWeekday' ? d(date, 'date_weekday_short') : d(date, 'date')
}
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `formatProfileDate` が `new Date(epochMillis)` と vue-i18n の `d()`（=`$d`）で日時を整形している → AGENTS.md「日付・時刻の表示は `common/src/utils/datetime.ts` の `convertToXxx` を使う（`$d` / `datetimeFormats` は新規追加しない）」に反する。同 PR の `FriendCard.vue` / `FriendMeetLogDialog.vue` は `convertToDate` / `convertToDatetime` を使っているので揃えるべき。

**コメント要約**: 日時整形が `$d` + `new Date` で、i18n 規約（`convertToXxx` 使用）に反する。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: AGENTS.md「UI 文言・i18n」節で `$d` / `datetimeFormats` の新規追加を明示的に禁止し `convertToXxx` を指定。`new Date()` 直使用も luxon 推奨に反する。RC-34 で `FriendCard` は `convertToDate` に統一済みであり、本箇所も同様に寄せるのが整合的。曜日付き表示が必要なら `common` 側に対応する `convertToXxx` を用意して使う。**対応**: `formatProfileDate` を `convertToDate` / `convertToDateWeekdayShort` に置換。

---

**識別子**: RC-72（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `user/src/components/profile/UserProfilePage.vue:169`, `user/src/components/profile/UserProfilePage.vue:402`, `user/src/components/profile/UserProfilePage.vue:453`

**該当コード（レビュー時点の diff）**:

```ts
watch([profilePreviewFriends, profilePreviewFriendsHasMore, profilePreviewFriendsLoading], () => { ... })
watch([pagedFoods, foodHasMore, foodLoading, tabs], () => { tryLoadMoreFoodsIfEmpty() })
watch([memberCommunities, managerCommunities, memberCommunityListHasMore, managerCommunityListHasMore, tabs], () => {
  tryLoadMoreCommunitiesIfEmpty()
})
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: 「表示 0 件かつ追読み余地があれば次ページを読む」系の `watch` が複数あり、`watch` 多用になっている → 多くは正当な副作用トリガだが、共通の「空なら追読み」ロジックに集約できないか検討すると保守性が上がる。

**コメント要約**: 空ページ追読み用の `watch` が複数。集約の余地あり。

**判断結果**: ✅ 対応済み（2026-06-03 実装）

**判断理由**: shokujii-code-review「`watch` の多用をしていないか」に該当。ただしいずれもリアクティブ計算ではなく副作用（ページング `next()`）のトリガであり `computed` では代替できない。RC-35 で友人タブの watch は簡略化済み。プレビュー/フード/コミュニティで同型の処理が重複しているため、共通ヘルパ化を提案するに留める。マージ必須ではない。**対応**: `setupAutoLoadWhenEmpty` ヘルパに集約。

---

**識別子**: RC-73（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `base/src/components/FriendMeetLogDialog.vue:123`, `base/src/components/FriendCard.vue:60`

**該当コード（レビュー時点の diff）**:

```vue
<!-- FriendCard.vue / FriendMeetLogDialog.vue 共通: 件数は friend.meet_count をそのまま表示 -->
{{ $t('user.friend_meet_count', { count: friend.meet_count }) }}
```

**レビュワーのコメント（原文）**:

👌 **修正不要**: 削除済みイベントは `buildMeetLogItems` で meet log 行から除外される一方、カード／ダイアログの「○回」は `friend.meet_count`（削除前を含む）を表示するため、表示行数と件数がずれ得る → 仕様上許容なら問題なし。

**コメント要約**: `meet_count` と実際に表示される meet_log 行数が一致しない場合がある。

**判断結果**: 👌 修正不要

**判断理由**: `meet_count` はサブコレ正本の同席回数（イベント削除では再集計しない設計）で、二層モデルの一時ズレと同様に仕様上許容範囲。meet log の表示行は解決可能なイベントのみという挙動も意図的。UX 上問題が出れば別途検討でよく、現時点で対応不要。

---

## 評価セッション（2026-06-03・Codex / Copilot 再レビュー・PR 本文更新後）

- **評価日時**: 2026-06-03 15:15 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **対象範囲**: 直近 1 時間以内（2026-06-03T05:15 UTC 以降）のコメント
- **Outdated 除外件数**: 0 件
- **レビュー非該当スキップ件数**: 1 件（GitHub id: `4609532767` — `@copilot @codex review` レビュー依頼定型文）

### RC 別対応チェックリスト（本セッション）

- [x] **RC-74**（GitHub id: 3346282189）— ✅ 対応済み
- [x] **RC-75**（GitHub id: 3346285962）— ✅ 対応済み
- [x] **RC-76**（GitHub id: 3346286000）— ✅ 対応済み
- [x] **RC-77**（GitHub id: 3346286021）— ✅ 対応済み

---

**識別子**: RC-74（GitHub id: 3346282189）

**レビュワー**: Codex

**指摘箇所**: `firestore.rules:131`

**該当コード（レビュー時点の diff）**:

```diff
@@ -100,7 +123,15 @@ service cloud.firestore {
         match /users/{user_id} {
             allow read: if true
             // ユーザー情報はログインユーザーでないと create/update/deleteできない
-            allow create, update: if request.auth != null && request.auth.uid == user_id
+            allow create: if request.auth != null
+                && request.auth.uid == user_id
+                && userProfileCountsZeroOrAbsentOnCreate()
+            allow update: if request.auth != null
+                && request.auth.uid == user_id
+                && userProfileCountsUnchanged()
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  カウント未設定ユーザーのプロフィール更新を許可してください**

既存の `users` ドキュメントに今回追加されたカウントフィールドがまだ無い環境では、プロフィール編集時に `User` の default(0) が `setDoc(..., { merge: true })` で送られるため、`request.resource.data.diff(resource.data).affectedKeys()` にカウントフィールドが入り、この `allow update` が拒否します。バックフィル Callable は手動実行なので、ルールを先にデプロイすると未バックフィルの既存ユーザーが通常のプロフィール更新（`user/src/pages/profile.vue:177`）をできなくなります。

Useful? React with 👍 / 👎.

**コメント要約**: RC-56 の `userProfileCountsUnchanged()` が、DB にカウントフィールドが無い既存ユーザーがプロフィール編集するとき merge で 0 が送られ update を拒否する。ルール先行デプロイで既存ユーザーが編集不能になる。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘は妥当。`userProfileCountsUnchanged()`（20–22 行）は `diff(resource.data).affectedKeys()` にカウント系キーが含まれると拒否する。既存 doc にフィールドが無く、クライアントが AppSchema の default 0 付きで merge 更新すると affectedKeys にカウントが入り **権限エラー**になる。バックフィル前に rules を prod へ出すと全既存ユーザーのプロフィール編集が壊れる。P1 相当。`resource.data` にキーが無い場合は request 側の 0 追加を「変更なし」とみなす、または backfill 完了まで update 条件を緩める等の対応が必要。

**対応**: `userProfileCountFieldUnchanged` / `userProfileCountsUpdatedAtUnchanged` を追加し、resource に無いカウントフィールドへの request 側 0 追加を許可。`counts_updated_at` のクライアント追加は引き続き拒否。

---

**識別子**: RC-75（GitHub id: 3346285962）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/utils/userFriendsResolver.ts:39`

**該当コード（レビュー時点の diff）**:

```diff
+const toMeetLogItem = (
+  entry: MeetLogHistoryEntry,
+  event: ShokujiiEvent | undefined,
+  viewerUid: string | null,
+  targetUserId: string,
+): UserFriendMeetLogItem | null => {
+  if (event == null || event.is_deleted) {
+    return null
+  }
```

**レビュワーのコメント（原文）**:

[must] events ドキュメントが取得できない / 削除済みの場合に null を返して履歴から除外してしまうため、UI 側の「（公開されていない、または削除されたイベント）」表示分岐（event_name / community_account が null）に到達できません。`meet_count` と表示件数がズレる原因にもなるので、取得不可時は event_name/community_account を null にしたプレースホルダ行を返してください。

**コメント要約**: `toMeetLogItem` が event 未取得・削除時に null で行ごと除外。仕様・UI は `event_name: null` のプレースホルダ行を想定。

**判断結果**: ✅ 対応済み

**判断理由**: `02_友人一覧機能.md`（meet_log 要素: 取得不可は `event_name: null` 等）および `FriendMeetLogDialog` の `isMeetLogEventResolvable` / `friend_meet_log_event_unavailable` 文言と整合すればプレースホルダ行が正しい。現状は行除外で UI 分岐が dead code 化。RC-73 で meet_count と行数のズレは許容したが、**仕様書 389 行はプレースホルダ返却**を明示しており、Copilot 指摘は仕様追随として妥当。セキュリティ問題ではなく UX・仕様整合。削除済みは `is_linkable: false` でリンク不可のまま返す形が望ましい。

**対応**: `toMeetLogItem` が event 未取得・削除時に `event_name` / `community_account: null` のプレースホルダ行を返却。テスト 2 ケース追加。

---

**識別子**: RC-76（GitHub id: 3346286000）

**レビュワー**: Copilot

**指摘箇所**: `base/src/stores/userFriends.ts:32`

**該当コード（レビュー時点の diff）**:

```diff
+    const next = () => {
+      if (paginationExecutor.totalTaskLength > 0 || !hasMore.value) {
+        return
+      }
+      if (maxTotal !== undefined && friends.value.length >= maxTotal) {
+        hasMore.value = false
+        return
+      }
```

**レビュワーのコメント（原文）**:

[must] targetUserId が空文字のタイミングでも `getUserFriends` を呼びにいくため、Callable 側の `target_user_id: z.string().min(1)` により invalid-argument になり、初期描画で不要なエラー状態になりえます（`userFoods` ストアは同様のガードあり）。空 userId のときは早期 return してください。

**コメント要約**: `useUserFriendsStore` の `next()` が `targetUserId === ''` でも Callable を呼ぶ。`userFoods` と同様の早期 return が必要。

**判断結果**: ✅ 対応済み

**判断理由**: `userFoods.ts` 19 行目に `if (targetUserId === '') return` があり、同パターンの store として `userFriends` もガードすべき。RC-65 で `profileUserId` 簡素化後も、マウント直後など一瞬空文字で store が作られると invalid-argument になり `error` が立つ可能性がある。防御的修正で低コスト。マージ前必須ではないが推奨。

**対応**: `next()` 先頭に `if (targetUserId === '') return` を追加。

---

**識別子**: RC-77（GitHub id: 3346286021）

**レビュワー**: Copilot

**指摘箇所**: `functions/default/src/utils/userProfileBackfill.ts:97`

**該当コード（レビュー時点の diff）**:

```diff
+      const existing = {
+        participated_event_count: Number(userDoc.get('participated_event_count') ?? 0),
+        friend_count: Number(userDoc.get('friend_count') ?? 0),
+        joined_community_count: Number(userDoc.get('joined_community_count') ?? 0),
+        managed_community_count: Number(userDoc.get('managed_community_count') ?? 0),
+        ordered_food_count: Number(userDoc.get('ordered_food_count') ?? 0),
+      }
+      const changed = hasCountsChanged(existing as never, next)
```

**レビュワーのコメント（原文）**:

[must] `hasCountsChanged(existing as never, next)` の `as never` は型検査を完全に無効化してしまい、将来フィールド名変更などがあっても検知できません。最小限でも `Parameters<typeof hasCountsChanged>[0]` へのキャストにして、意図した型に寄せてください（可能なら hasCountsChanged 側の引数型を Pick にしてキャスト自体を消すのが理想です）。

**コメント要約**: backfill の `hasCountsChanged` 呼び出しで `as never` が型安全を無効化。引数型を絞るか Pick 型に変更すべき。

**判断結果**: ✅ 対応済み

**判断理由**: RC-67 と同根。`hasCountsChanged` は実際にはカウント5フィールドのみ参照（`ShokujiiUser` 全体は不要）。`Pick<ShokujiiUser, keyof UserProfileCounts>` や `Parameters<typeof hasCountsChanged>[0]` に寄せれば `as never` を除去できる。運用バックフィル経路のためマージ必須度は低いが、型保守性の改善として推奨。

**対応**: `hasCountsChanged` 第1引数を `UserProfileCountsUpdate` に変更し、backfill の `as never` を除去。

---

## 評価セッション（2026-06-03・直近インライン 5 件）

- **評価日時**: 2026-06-03 15:50 JST
- **ブランチ名**: feat/1248
- **PR**: [https://github.com/nijuniinc/bokudeli-event-new/pull/1985](https://github.com/nijuniinc/bokudeli-event-new/pull/1985)
- **対象範囲**: 作成日時が新しい順のインライン 5 件（RC-75〜77 は前セッション評価済みの再確認を含む）
- **Outdated 除外件数**: 0 件
- **レビュー非該当スキップ件数**: 0 件

### RC 別対応チェックリスト（本セッション）

- [x] **RC-75**（GitHub id: 3346285962）— ✅ 対応済み
- [x] **RC-76**（GitHub id: 3346286000）— ✅ 対応済み
- [x] **RC-77**（GitHub id: 3346286021）— ✅ 対応済み
- [x] **RC-78**（GitHub id: 3346383903）— ✅ 対応済み
- [x] **RC-79**（GitHub id: 3346440755）— ✅ 対応済み

---

**識別子**: RC-78（GitHub id: 3346383903）

**レビュワー**: Codex

**指摘箇所**: `firestore.rules:25`

**該当コード（レビュー時点の diff）**:

```diff
+        function userProfileCountFieldUnchanged(field) {
+            return !(field in request.resource.data)
+                || (field in resource.data && request.resource.data[field] == resource.data[field])
+                || (!(field in resource.data) && request.resource.data.get(field, 0) == 0);
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  カウントフィールドの削除も拒否する**

この条件だと `request.resource.data` から対象フィールドが消えている更新を「変更なし」と扱うため、本人が `updateDoc(..., { friend_count: deleteField() })` のように冗長カウントや `counts_updated_at` を削除できます。`getUserProfilePreview` は未設定カウントを 0/null として公開するため、バックフィル済みの実績をクライアント操作で隠せてしまいます。既存値があるフィールドは削除も拒否するようにしてください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-74 の `userProfileCountFieldUnchanged` 第1分岐 `!(field in request.resource.data)` により、resource にカウントがある状態で request からフィールドを省略（deleteField 相当）すると「変更なし」と判定され update が通る。クライアントが自分の冗長カウントを削除して公開プロフィールの数値を隠せる。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘は妥当。現行ルール（22–25 行）では request にフィールドが無い場合を無条件 OK としており、merge update で `deleteField()` した場合も通る。権限昇格ではないが、RC-56/RC-74 の「Functions 専用カウント」意図とずれ、バックフィル済みユーザーがプロフィール編集時にカウントを消してプレビュー上 0 表示にできる。P2 相当。`resource.data` にキーがあるときは `request.resource.data` にも同一キーが必須、または削除を明示拒否する条件追加を推奨。`counts_updated_at` も同根で `userProfileCountsUpdatedAtUnchanged` の第1分岐が同様。

**対応**: `userProfileCountFieldUnchanged` / `userProfileCountsUpdatedAtUnchanged` を、resource にキーがある場合は request にも同一値必須とする条件に変更（削除拒否）。

---

**識別子**: RC-79（GitHub id: 3346440755）

**レビュワー**: Codex

**指摘箇所**: `base/src/components/FriendMeetLogDialog.vue:153`

**該当コード（レビュー時点の diff）**:

```diff
+            <template #prepend>
+              <v-img
+                :src="eventCoverUrl(row.community_id, row.event_id)"
+                width="120"
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  取得不可イベントではカバー画像も伏せる**

削除済み・取得不能なイベントについて `resolveUserFriendMeetLog` は `event_name: null` / `community_account: null` のプレースホルダ行を返しますが、この行でも `community_id` と `event_id` から公開 Storage のカバー画像 URL を組み立てて表示してしまいます。タイトルは「取得できない」扱いでも画像だけは残るため、削除済みイベントの内容が meet log から見えてしまいます。`isMeetLogEventResolvable(row)` のときだけ画像パスを解決するようにしてください。

Useful? React with 👍 / 👎.

**コメント要約**: RC-75 対応後、タイトルはプレースホルダ文言だが `v-img` は常に `eventCoverUrl(community_id, event_id)` を解決する。Storage 上にカバーが残っていれば削除済みイベントの画像が meet log から見える。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘は妥当。`FriendMeetLogDialog.vue`（150–158 行）は `isMeetLogEventResolvable` をタイトル分岐にのみ使い、画像は無条件表示。公開 Storage のパスが `community_id` + `event_id` で推測可能なら、取得不可イベントの視覚情報が漏れる。§4.2.0 の「取得不可は名前 null」設計と UX 整合のため、`isMeetLogEventResolvable(row)` 時のみ `v-img` を出す（またはプレースホルダアイコン）修正を推奨。P2・マージ必須度は RC-78 より低いが、RC-75 追随として対応するとよい。

**対応**: `v-img` に `v-if="isMeetLogEventResolvable(row)"` を追加し、取得不可行ではカバー画像を表示しない。

---

