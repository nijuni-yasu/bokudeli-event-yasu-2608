# PR #2066 レビューコメント評価

## 評価セッション

- **評価日時**: 2026-06-12 17:51 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: doc/enterprise-spec-gap-phase1
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2066
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 5（レビュー依頼 2、Codex 接続案内 2、Copilot 承知返信 1）

### RC 別対応チェックリスト

- [x] **RC-1**（GitHub id: 4448569651）— 👌 修正不要（Copilot PR overview）
- [x] **RC-2**（GitHub id: 4689234638・1件目）— ✅ 対応済み（`status` → `billing_status` 統一）
- [x] **RC-3**（GitHub id: 4689234638・2件目）— ✅ 対応済み（manager 参照削除）
- [x] **RC-4**（GitHub id: 4689234638・3件目）— ✅ 対応済み（Phase 6-3 を §5.4 に揃え）
- [x] **RC-5**（GitHub id: 4689234638・4件目）— ✅ 対応済み（`meal_billing_amount` / `enterprise_billing_amount` 確定）
- [x] **RC-6**（GitHub id: 4483901292・suppressed 1件目）— ✅ 対応済み（MVP 500 円 / 営業標準 800 円）
- [x] **RC-7**（GitHub id: 4483901292・suppressed 2件目）— ✅ 対応済み（10名→1,000円/人に修正）
- [x] **RC-8**（GitHub id: 3401933978）— ✅ 対応済み（Shokujii スキルへ差替）
- [x] **RC-9**（GitHub id: 3372472643）— ✅ 対応済み（collectionGroup 再帰 match 追加）
- [x] **RC-10**（GitHub id: 3401933970）— ✅ 対応済み（`billing_settings.unit_price` 参照）
- [x] **RC-11**（GitHub id: 3396318260）— ✅ 対応済み（翌月 1 日 00:05 JST 確定）
- [x] **RC-12**（GitHub id: 3396318286）— ✅ 対応済み（停止企業もスナップショット対象）
- [x] **RC-13**（GitHub id: 3401933995）— ✅ 対応済み（§7.1 を §5.4 に揃え）
- [x] **RC-14**（GitHub id: 3396318275）— ✅ 対応済み（`custom_domain` 重複チェック）
- [x] **RC-15**（GitHub id: 3401934003）— ✅ 対応済み（RC-14 と同一修正）
- [x] **RC-16**（GitHub id: 3401933987）— ✅ 対応済み（RC-3 と同一修正）
- [x] **RC-17**（GitHub id: 3372472618）— ✅ 対応済み（D-3 と請求見込みを区別）
- [x] **RC-18**（GitHub id: 3401933981）— ✅ 対応済み（create 全員 / update manager）
- [x] **RC-19**（GitHub id: 3396318296）— ✅ 対応済み（getEnterpriseMembers API 拡張）
- [x] **RC-20**（GitHub id: 3396318251）— ✅ 対応済み（ゲスト self_pay 除外）
- [x] **RC-21**（GitHub id: 3372472628）— ✅ 対応済み（invoice.ts リンク修正）
- [x] **RC-22**（GitHub id: 3372472640）— ✅ 対応済み（Phase 2-5 App Check）
- [x] **RC-23**（GitHub id: 3372472638）— ✅ 対応済み（Phase 1-6 createEnterprise）
- [x] **RC-24**（GitHub id: 3396318271）— ✅ 対応済み（監査ログ Phase 1-5 前倒し）
- [x] **RC-25**（GitHub id: 3401933998）— ✅ 対応済み（805,000 円に修正）
- [x] **RC-26**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（finance-billing-ops の課金設計パス typo）
- [x] **RC-27**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（§5.4 月次 cron に統一）
- [x] **RC-28**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（getEnterpriseMembers email join 前倒し）
- [x] **RC-29**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（sort_by monthly_usage 月キー明記）
- [x] **RC-30**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（Rules helper 参照先コメント）
- [x] **RC-31**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（02_料金比較「20名未満」に修正）
- [x] **RC-32**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（RC-2〜32 判断結果を更新）
- [x] **RC-33**（GitHub id: 4690576946）— ✅ 対応済み（02_料金比較 §3.3 S2 説明文を §1.3 に整合）
- [x] **RC-34**（GitHub id: 3402900160）— ✅ 対応済み（返金仕様リンク 4 ファイル統一）
- [x] **RC-35**（GitHub id: 3402900172）— ✅ 対応済み（PF 版 communities create OR パス追加）
- [x] **RC-36**（GitHub id: 3402900168）— ✅ 対応済み（月内 ever active・`last_activated_at` / `last_deactivated_at`）
- [x] **RC-37**（GitHub id: 4691255325）— ✅ 対応済み（02_課金設計.md 太字＋コードスパン記法 7 箇所）

---

**識別子**: RC-1（GitHub id: 4448569651）

**レビュワー**: Copilot（copilot-pull-request-reviewer）

**指摘箇所**: PR トップレベル（Pull Request Review）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

## Pull request overview

Phase 1 着手前の「エンタープライズ仕様の抜け漏れ確定（A/B 群）」を各詳細仕様へ反映し、請求・オンボーディングを独立ドキュメントとして分割したうえで、実装計画（Phase 0〜7、クリティカルパス）を新規に整理する PR です（documents/ のみ変更）。

**Changes:**
- 実装計画（Phase 0〜7、横断タスク、将来項目、抜け漏れチェック）を新規作成
- 請求（集計 MVP / PDF Phase 2）とオンボーディング（createEnterprise）を詳細仕様として新規分割
- Security Rules / i18n / Callable（getEnterpriseMembers 等）に関する確定事項を各仕様へ反映・整合

### Reviewed changes

Copilot reviewed 13 out of 13 changed files in this pull request and generated no comments.

**コメント要約**: PR 変更の概要サマリのみ。具体的な修正指摘はなし。

**判断結果**: 👌 修正不要

**判断理由**: 変更内容の要約であり、コード・仕様への修正要求を含まない。対応作業は不要。

---

**識別子**: RC-2（GitHub id: 4689234638・1件目）

**レビュワー**: Copilot（copilot-swe-agent[bot]）

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md` §5.3

**該当コード（レビュー時点の diff）**:

```diff
+  status: 'final' | 'provisional' // 当月のみ provisional（通常は final）
```
（同 § の表・`04_ダッシュボード` では `billing_status` と記載）

**レビュワーのコメント（原文）**:

**[must] `billing_snapshots` スキーマの `status` フィールド名が `billing_status` と不統一**
ファイル: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md` §5.3

`billing_snapshots` の TypeScript スキーマ定義では `status: 'final' | 'provisional'` を使用しているが、同§の「ダッシュボード列定義」表・「参照ルール」表では `billing_status` と記載されている。`04_ダッシュボード.md` でも `billing_status` を使用しており、`02_全体構成` §3.9 のスキーマも `status` のまま。3ファイル間で名前が統一されていないため、実装時に混乱が生じる。`status` か `billing_status` に一本化して全ドキュメントを揃える必要がある。

**コメント要約**: Firestore スキーマは `status`、ダッシュボード API は `billing_status` とドキュメント間でフィールド名が不一致。

**判断結果**: ✅ 対応済み

**判断理由**: 手元の `02_課金設計` §5.2〜5.3、`04_ダッシュボード` §3.1、`02_全体構成` §3.9 で実際に混在している。Phase 1 common スキーマ確定前に正本を一本化しないと実装・API マッピングで二重定義になる。

---

**識別子**: RC-3（GitHub id: 4689234638・2件目）

**レビュワー**: Copilot

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_オンボーディング.md` §4

**該当コード（レビュー時点の diff）**:

```diff
+| 実行方法 | 運営が manager パッケージ or 一回限りスクリプトから `createEnterprise` を実行 |
```

**レビュワーのコメント（原文）**:

**[must] `04_オンボーディング.md` §4 で Deprecated の `manager` パッケージを参照**
ファイル: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_オンボーディング.md` §4

```
| 実行方法 | 運営が manager パッケージ or 一回限りスクリプトから `createEnterprise` を実行 |
```

`AGENTS.md` のディレクトリ構造で `/manager` は「レガシー (Deprecated) — 新規コード生成時の参照禁止」と明記されている。仕様書に `manager パッケージ` を正式な実行手段として記述すると、実装者が Deprecated パッケージを参照してしまう可能性がある。「Admin SDK スクリプト（一回限り）」または「`functions/default` の運営向け Functions 経由」等に変更すべき。

**コメント要約**: オンボーディング MVP 運用に Deprecated な `/manager` を記載している。

**判断結果**: ✅ 対応済み

**判断理由**: AGENTS.md の Deprecated 方針と矛盾。`04_オンボーディング` §4 に現状も同記載があり、仕様正本として修正が必要。

---

**識別子**: RC-4（GitHub id: 4689234638・3件目）

**レビュワー**: Copilot

**指摘箇所**: `documents/08_エンタープライズ/08_エンタープライズ_実装計画.md` Phase 6-3

**該当コード（レビュー時点の diff）**:

```diff
+- **6-3 課金スナップショット** `captureEnterpriseBillingSnapshots`（毎月末 23:55 JST で前月確定）。`billing_snapshots` 永続化。`02_課金設計` §5.4 正本。MVP-C 手動請求の根拠。
```
（`02_課金設計` §5.4 は「毎日 23:55 JST、翌日が 1 日のときだけ前月分を確定」）

**レビュワーのコメント（原文）**:

**[imo] `captureEnterpriseBillingSnapshots` のスケジュール表現が正本と実装計画で揺れている**
ファイル: `documents/08_エンタープライズ/08_エンタープライズ_実装計画.md` Phase 6

実装計画 Phase 6-3 では「毎月末 23:55 JST で前月確定」と記載しているが、正本の `02_課金設計.md` §5.4 では「**毎日** 23:55 JST に起動し、翌日が 1 日のときだけ前月分を確定」が正確な仕様。「毎月末に起動」という読み方をされると Scheduled Function の実装が `0 */1 * *`（月次）になりかねない。実装計画の記述を `02_課金設計` §5.4 の表現に揃えることを推奨する。

**コメント要約**: 実装計画 Phase 6-3 と `02_課金設計` §5.4 でバッチ起動条件の記述が不一致。

**判断結果**: ✅ 対応済み

**判断理由**: §5.4 が正本として詳細定義済み。Phase 6-3・§7.1・`02_全体構成` §3.9 の「毎月末 23:55」表記と併せて、Scheduled Function の cron 解釈が分岐する。実装計画・請求フロー表を §5.4 に揃える必要がある（RC-13 と連動）。

---

**識別子**: RC-5（GitHub id: 4689234638・4件目）

**レビュワー**: Copilot

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_全体構成・アーキテクチャ.md` §3.9

**該当コード（レビュー時点の diff）**:

```diff
+> フィールド名対応（ダッシュボードとの整合）: スナップショットの `meal_billing_amount` と、
+> ダッシュボード API / CSV の `enterprise_billing_amount` は同一の食事従量を指す。
+> 実装時はどちらか一方にマッピングする。
```

**レビュワーのコメント（原文）**:

**[ask] `meal_billing_amount`（スナップショット）と `enterprise_billing_amount`（ダッシュボード API）の名前統一方針が未決**
（中略：Phase 1 common スキーマ定義前に名前を一本化し、`02_課金設計` §5.3 のスキーマ・ダッシュボード列定義・実装計画 §抜け漏れチェック に反映しておくことを推奨）

**コメント要約**: スナップショットとダッシュボード API で食事従量フィールド名が異なり、「実装時に決める」まま。

**判断結果**: ✅ 対応済み

**判断理由**: 意図的なレイヤ分離（永続化 vs API）として許容可能だが、`02_全体構成` でも対応関係を注記しており Phase 1 前の命名確定が望ましい。マージ必須のバグではないが、スキーマ確定前の整理タスクとして対応推奨。

---

**識別子**: RC-6（GitHub id: 4483901292・suppressed 1件目）

**レビュワー**: Copilot（copilot-pull-request-reviewer・low confidence suppressed）

**指摘箇所**: `documents/10_競合調査・営業資料/01_競合調査_概要.md:25`

**該当コード（レビュー時点の diff）**:

```diff
+| プラットフォーム利用料 | **500円 / 有効アカウント / 月**（税込）。お試し3ヶ月無料 | `08_エンタープライズ/02_課金設計` |
```

**レビュワーのコメント（原文）**:

* [must] 料金比較（02_料金比較.md）を正本とする旨が書かれている一方で、ここではプラットフォーム利用料が「500円」と固定で書かれており、02_料金比較.md の「比較表は年間800円/人/月で試算」や 08_エンタープライズ/02_課金設計 の料金表（定価1,200・年間800等）と矛盾します。ここは「MVPデフォルト/早期導入は500円」など位置づけを明示し、正本の単価体系に合わせた表現へ更新した方が読み手の誤解を防げます。

**コメント要約**: 競合概要が 500 円固定表記のまま、課金設計・料金比較の営業単価体系と矛盾。

**判断結果**: ✅ 対応済み

**判断理由**: `02_課金設計` §2.1 で営業単価（800/1000/1200 等）と MVP デフォルト 500 を区別済み。概要表の文言を「MVP デフォルト 500 円（早期導入）／営業標準は年間 800 円」等に修正すれば足りる。RC-10 と同系統。

---

**識別子**: RC-7（GitHub id: 4483901292・suppressed 2件目）

**レビュワー**: Copilot（copilot-pull-request-reviewer・low confidence suppressed）

**指摘箇所**: `documents/10_競合調査・営業資料/02_料金比較.md:214`

**該当コード（レビュー時点の diff）**:

```diff
+50名未満でも最低10,000円のため、**20名なら 500円/人ではなく 500円/人（10,000÷20）** になる点に注意。
```

**レビュワーのコメント（原文）**:

* [must] 「最低10,000円/月」の注意喚起の例が「20名なら 500円/人ではなく 500円/人」となっていて、単価が変わらないため例として成立していません（20名だと 10,000÷20=500円で同じ）。最低料金の影響が出る人数（例: 10名→1,000円/人）に修正した方が誤読を防げます。

**コメント要約**: どこでも社食の最低料金説明例が typo で意味をなさない。

**判断結果**: ✅ 対応済み

**判断理由**: 手元ファイル L214 が「500円/人ではなく 500円/人」と同一値のまま。営業資料の算数例として明確な誤り。

---

**識別子**: RC-8（GitHub id: 3401933978）

**レビュワー**: Codex

**指摘箇所**: `.agents/skills/finance-billing-ops/SKILL.md:15`

**該当コード（レビュー時点の diff）**:

```diff
+Pull these ECC-native skills into the workflow when relevant:
+
+- `customer-billing-ops` for customer-specific remediation and follow-up
+- `research-ops` when competitor pricing or current market evidence matters
+- `market-research` when the answer should end in a pricing recommendation
+- `github-ops` when the billing truth depends on code, backlog, or release state in sibling repos
+- `verification-loop` when the answer depends on proving checkout, seat handling, or entitlement behavior
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  存在しないスキルの呼び出し前提を削除してください**

このリポジトリの `.agents/skills` を確認しましたが、ここで列挙されている `customer-billing-ops` / `research-ops` / `market-research` / `github-ops` / `verification-loop` は存在しません。このままだと請求・売上系の依頼でこの新規スキルが発火した際に、エージェントが未インストールのECC向けスキルを前提に進めようとして手順が破綻するため、Shokujiiで利用可能なスキルに差し替えるか、このスタック指示を削除してください。

**コメント要約**: ECC 由来スキルが存在しない Shokujii スキルを参照している。

**判断結果**: ✅ 対応済み

**判断理由**: `.agents/skills` に該当スキルは存在しない。AGENTS.md のスキル一覧（`/stripe-integration`、`/shokujii-firestore` 等）へ差し替えるか Skill Stack 節を削除すべき。

---

**識別子**: RC-9（GitHub id: 3372472643）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_全体構成・アーキテクチャ.md:887`

**該当コード（レビュー時点の diff）**:

```diff
+// EventMember (members コレクション): PF版公開 + エンプラ自社 + 本人（ゲスト含む）
+//   collectionGroup クエリ用ルールも同一条件（/{path=**}/member_orders/{oid}）
 match /communities/{cid}/events/{eid}/members/{uid} {
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  collectionGroup用の再帰matchも明記して**

ここでは collectionGroup 用ルールも同一条件と書いていますが、下のサンプルは通常のネストされた `match /communities/.../members/{uid}/member_orders/{oid}` だけなので、これを実装すると `collectionGroup('member_orders')` のクライアントクエリは再帰ワイルドカードの許可にマッチせず拒否されます。`/{path=**}/member_orders/{oid}`（events も同様）を別途定義するか、このサンプル自体を collectionGroup 対応の形にしてください。

**コメント要約**: コメントで collectionGroup 対応と書いているが、サンプル Rules に再帰 match が無い。

**判断結果**: ✅ 対応済み

**判断理由**: ダッシュボード・請求集計が `collectionGroup('member_orders')` を前提とする一方、§5.2 サンプルはネスト match のみ。Firestore Rules 実装時に集計クエリが拒否される。再帰 match または Functions 専用 read 方針を明記すべき。

---

**識別子**: RC-10（GitHub id: 3401933970）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:133`

**該当コード（レビュー時点の diff）**:

```diff
+unit_price = 500  // 円。/enterprises/{id}.billing_settings.unit_price で保持（デフォルト 500）
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  unit_price を固定値として書かないでください**

この計算式を実装の正本として参照すると、契約期間別単価（年間800円、6ヶ月1,000円、3ヶ月/月次1,200円）や企業別設定を無視して常に500円で請求集計してしまいます。同じ節の直前では `billing_settings.unit_price` に1本化すると定義しているため、ここは固定代入ではなく `/enterprises/{id}.billing_settings.unit_price` を読む式にしておく必要があります。

**コメント要約**: 計算式で `unit_price = 500` と固定代入しており §2.1 の可変単価と矛盾。

**判断結果**: ✅ 対応済み

**判断理由**: 直後のコメントでデフォルト 500 を説明しているが、擬似コードとしては `unit_price = billing_settings.unit_price` 形式に直すのが正しい。誤実装リスクはあるが意図（MVP デフォルト）は文脈から読み取れる。

---

**識別子**: RC-11（GitHub id: 3396318260）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:335`

**該当コード（レビュー時点の diff）**:

```diff
+| スケジュール | **毎日 23:55 JST** に起動し、**翌日が 1 日**のときだけ前月分を確定 |
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月末スナップショットは月確定後に実行して**

この指定だと 2026-06-30 23:55 JST のように月がまだ終わる前に確定処理が走るため、23:55〜23:59:59 に発生したアカウント停止・復活や食事注文が請求根拠から漏れます。さらに「前月分」を文字通り実装すると 6 月末に 5 月分を upsert してしまうので、月末時点の確定値を作るなら翌月 1 日 00:05 以降に直前月を確定するなど、対象月と実行時刻を揃えてください。

**コメント要約**: 月末 23:55 実行では当月最終数分のイベントが漏れる。実行タイミングを翌月初にすべき。

**判断結果**: ✅ 対応済み

**判断理由**: §5.4 の「翌日が 1 日」条件は Codex が指摘する §7.1 の「前月分」誤読（RC-13）とは別論点。月末 23:55〜24:00 の取りこぼしは妥当な指摘。翌月 1 日 00:05 JST 等への変更、または確定対象時刻を「月末 23:59:59.999 JST 時点」として再計算する旨の明記を推奨。

---

**識別子**: RC-12（GitHub id: 3396318286）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:337`

**該当コード（レビュー時点の diff）**:

```diff
+| 対象 | `is_active == true` の全 `Enterprise` |
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  停止企業もスナップショット対象に含めて**

対象を `is_active == true` に絞ると、月末時点で企業を停止したケースではスナップショット自体が作られず、直前月の食事従量や「企業停止時は 0」とするプラットフォーム利用料の確定値を請求根拠として残せません。企業停止中は①を 0 円にするだけで、少なくとも請求対象月に利用や未確定請求がある企業は snapshot を upsert する対象に含めてください。

**コメント要約**: バッチ対象が `is_active == true` のみだと、停止企業の食事従量確定値が残らない。

**判断結果**: ✅ 対応済み

**判断理由**: §2.3 では企業停止中は platform_fee=0 と明記。食事従量②は停止後も未請求分が残りうる。対象 Enterprise の条件を「当月に利用または未確定請求がある企業を含む」等に拡張する記述が望ましい。

---

**識別子**: RC-13（GitHub id: 3401933995）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:398`

**該当コード（レビュー時点の diff）**:

```diff
+| 1 | システム | 毎月末 23:55 JST バッチで前月分を `billing_snapshots` に確定 |
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月末バッチの対象月を当月分にしてください**

月末 23:55 JST に「前月分」を確定すると、例えば 2026-06-30 23:55 の実行で 2026-05 分をスナップショットする解釈になり、次行の「翌月 2〜5 営業日」に確認すべき 2026-06 分が確定されません。請求締めの根拠にするなら、月末実行では当月分を確定するか、翌月 1 日に前月分を確定するスケジュールへ明記してください。

**コメント要約**: §7.1 の「月末に前月分確定」は §5.4 と矛盾し、6 月末実行で 5 月分になる誤読を招く。

**判断結果**: ✅ 対応済み

**判断理由**: §5.4 正本は「翌日が 1 日のとき **前月分**」（= 6/30 実行で 6 月分確定）。§7.1・`02_全体構成` §3.9・実装計画 Phase 6-3 の「毎月末 23:55 で前月確定」表記と整合させる必要がある（RC-4 と同根）。

---

**識別子**: RC-14（GitHub id: 3396318275）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_オンボーディング.md:69`

**該当コード（レビュー時点の diff）**:

```diff
+2. enterprise_id / subdomain の重複チェック
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  custom_domain の重複も作成時に拒否して**

`getEnterpriseByDomain` は `custom_domain == hostname` でも企業を逆引きする仕様なのに、ここで重複チェック対象が `enterprise_id / subdomain` だけだと、同じカスタムドメインを持つ企業を複数作成できます。その場合ログイン画面の企業解決が不定になり、別企業のブランディングやメールドメインで OTP フローに進むため、`custom_domain` が指定された場合も一意性チェックに含めてください。

**コメント要約**: `createEnterprise` の重複チェックに `custom_domain` が含まれていない。

**判断結果**: ✅ 対応済み

**判断理由**: `getEnterpriseByDomain` が custom_domain でも逆引きする仕様と矛盾。テナント解決の一意性確保のため §3.3 処理フロー step 2 に追加必須。

---

**識別子**: RC-15（GitHub id: 3401934003）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_オンボーディング.md:69`

**該当コード（レビュー時点の diff）**:

（RC-14 と同一箇所）

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  custom_domain の重複も検査してください**

`getEnterpriseByDomain` は `custom_domain == hostname` でも企業を逆引きする仕様なので、ここで `custom_domain` の重複を許すと同じ独自ドメインを複数企業に割り当てられ、ログイン時に誤ったテナントへ解決されるか検索結果が不定になります。`custom_domain` が指定された場合は `enterprise_id` / `subdomain` と同様に一意性を検査してください。

**コメント要約**: RC-14 と同一論点（custom_domain 一意性）。

**判断結果**: ✅ 対応済み

**判断理由**: RC-14 と同じ。対応は 1 修正で両 RC 解消。

---

**識別子**: RC-16（GitHub id: 3401933987）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_オンボーディング.md:98`

**該当コード（レビュー時点の diff）**:

```diff
+| 実行方法 | 運営が manager パッケージ or 一回限りスクリプトから `createEnterprise` を実行 |
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  deprecated な manager パッケージを運用経路にしないでください**

AGENTS.md では `/manager` は Deprecated で新規コード生成時の参照禁止とされていますが、この新規仕様は MVP の `createEnterprise` 実行方法として manager パッケージを候補に入れています。実装者がこの通りに進めるとレガシー管理画面へ新規導線を追加してしまうため、運営用スクリプトまたは active な `functions/default` / `partner` 側の運用に限定してください。

**コメント要約**: RC-3 と同一（manager 参照）。

**判断結果**: ✅ 対応済み

**判断理由**: RC-3 と同根。§4 を「Firebase Admin SDK 一回限りスクリプト」等に修正。

---

**識別子**: RC-17（GitHub id: 3372472618）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_ダッシュボード.md:134`

**該当コード（レビュー時点の diff）**:

```diff
+| 企業請求見込額（食事） | shokujii から企業へ後請求する食事従量の見込み | ゲスト無料分 + `enterprise_subsidy` 補助分（`04_請求` §2 参照） |
```
（同 doc §6.2: ゲスト注文は集計対象外）

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  ゲスト無料分を集計対象外ルールと整合させて**

この列でゲスト無料分を企業請求見込額に含める仕様にしていますが、同じドキュメントの §6.2 では「ゲスト注文（`members.is_guest == true`）は集計対象外」と残っているため、実装時にダッシュボード/CSV の全集計からゲスト注文を除外してしまい、この列のゲスト無料分が常に欠落する可能性があります。D-3 は利用人数・利用回数の除外に限定し、請求見込額ではゲスト無料分だけ集計する旨を明記してください。

**コメント要約**: §3.1 はゲスト無料分を請求見込みに含むが §6.2 はゲストを全集計から除外。

**判断結果**: ✅ 対応済み

**判断理由**: `04_ゲスト参加` §7 も「ダッシュボード集計は別定義」とし、`04_請求` がゲスト無料分を含む。§6.2 の「集計対象外」を D-3（利用回数・人数・金額列）に限定する明記が必要。

---

**識別子**: RC-18（GitHub id: 3401933981）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_全社管理者画面.md:483`

**該当コード（レビュー時点の diff）**:

```diff
+| コミュニティ作成・更新（一般従業員） | /communities | 書き | クライアント（PF 版同等。`enterprise_id` 一致を Rules で検証） |
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P1 Badge](https://img.shields.io/badge/P1-orange?style=flat)</sub></sub>  一般従業員に全コミュニティ更新を許可しないでください**

このアクセスパターンは「PF版同等」としつつ `enterprise_id` 一致だけで一般従業員のコミュニティ更新を許す前提になっていますが、現行 `firestore.rules` の PF 版は `allow update: if isSupport() || isManager()` でコミュニティ管理者に絞っています。このまま実装すると同じ企業の任意の従業員が他部署・他サークルのコミュニティ設定を更新できるため、更新は PF 版同様に manager 権限へ限定する仕様にしてください。

**コメント要約**: エンプラ仕様は enterprise_id 一致のみで update 可だが、PF 現行 Rules は manager 限定。

**判断結果**: ✅ 対応済み

**判断理由**: 仕様内でも §510-517 の Rules サンプルは全従業員 update 可。一方 PF `firestore.rules` L91 は `isManager()` 必須。「PF版同等」と称しつつ権限モデルが異なる。意図的緩和なら「PF より広い MVP 方針」とセキュリティリスクを明記し、称述を修正。セキュリティ優先なら manager 限定へ Rules サンプル・§9 表を修正。

---

**識別子**: RC-19（GitHub id: 3396318296）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_認証・アカウント管理.md:574`

**該当コード（レビュー時点の diff）**:

```diff
+interface GetEnterpriseMembersRequest {
+  enterprise_id: string
+  limit?: number
+  startAfter?: string   // ページネーション用 userId
+}
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  メンバー一覧の絞り込みを API に渡して**

`/admin/members` は表示名検索・ロール/ステータスフィルタ・複数列ソートを維持したまま `getEnterpriseMembers` 経由に変更されていますが、このリクエストでは `limit` と `startAfter` しか渡せません。直接 Firestore read をやめた後にフロント側で 50 件だけ絞り込むと、後続ページにいる一致メンバーが欠落し総件数もずれるため、検索・フィルタ・ソート条件を Function 側で受けてページング前に適用できる仕様にしてください。

**コメント要約**: ページネーション後にクライアント側フィルタすると検索・フィルタが正しく動かない。

**判断結果**: ✅ 対応済み

**判断理由**: `04_全社管理者画面` §4.1 が表示名検索・ロール/ステータスフィルタ・複数列ソートを要求し、§180-184 では取得後クライアントフィルタを許容しているが、50 件ページングと両立しない。API に search/filter/sort パラメータを追加するか、仕様側の UX 要求を縮小する必要がある。

---

**識別子**: RC-20（GitHub id: 3396318251）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_請求.md:55`

**該当コード（レビュー時点の diff）**:

```diff
+- ゲスト無料分: 親 `members.is_guest == true` のメンバー配下の `member_orders` を `Σ menu_price` で集計
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  ゲスト self_pay を請求集計から除外して**

ゲスト分の集計条件が親 `members.is_guest == true` だけなので、`guest_payment: self_pay` でゲストが Stripe 即時決済した注文も `Σ menu_price` として企業請求見込額に含まれます。`04_ゲスト参加` では self_pay は請求書に含めない仕様になっているため、無料ゲスト注文だけを判定できる条件（例: イベントの `guest_payment == 'free'` や決済経路）をここに明記しないと二重請求の実装に誘導されます。

**コメント要約**: ゲスト全件を集計すると self_pay 分も企業請求に含まれてしまう。

**判断結果**: ✅ 対応済み

**判断理由**: `04_ゲスト参加` §7.1「ゲスト self_pay | 含まない」と明確。`04_請求` §2.2 に `guest_payment == 'free'` 等の条件追加が必要。

---

**識別子**: RC-21（GitHub id: 3372472628）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_請求.md:71`

**該当コード（レビュー時点の diff）**:

```diff
+- 既存 [common/src/utils/invoice.ts](../../../common/src/utils/invoice.ts)（PF版 `community_bill` 請求）を参考に、エンプラ向け請求書 PDF 生成 Function を追加
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  参照リンクを実在する相対パスに直して**

この Markdown リンクは `documents/08_エンタープライズ` から `../../../common/...` に解決されるため `/workspace/common/src/utils/invoice.ts` を指しますが、そのファイルは存在せず、実際のファイルはリポジトリ内の `common/src/utils/invoice.ts` です。Phase 2 の実装者が参照先へ辿れないので、相対パスを `../../common/src/utils/invoice.ts` に直してください。

**コメント要約**: Markdown リンクの相対パスが 1 階層深い。

**判断結果**: ✅ 対応済み

**判断理由**: `documents/08_エンタープライズ/` から repo root へは `../../`。`../../../` は誤り。`common/src/utils/invoice.ts` は実在する。

---

**識別子**: RC-22（GitHub id: 3372472640）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/08_エンタープライズ_実装計画.md:65`

**該当コード（レビュー時点の diff）**:

```diff
+- **2-1 ログインフロー**: メールドメイン制限 + 6桁OTP（`requestEnterpriseEmailLogin` / `confirmEnterpriseEmailLogin`）。ドメイン検証はフロント＋Functions 二重。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  App Check対応を認証フェーズのタスクに追加して**

認証仕様では `getEnterpriseByDomain` / `requestEnterpriseEmailLogin` の未認証 Callable に MVP で App Check を有効化すると確定していますが、この Phase 2 のログインフローには App Check の実装タスクが含まれていません。計画に従って実装すると、ドメイン列挙や OTP 送信濫用への MVP 対策が落ちた状態で認証機能が完成扱いになってしまうため、ここに App Check 有効化・検証を明示してください。

**コメント要約**: `04_認証` §8-2 の App Check 必須が Phase 2 タスク一覧に無い。

**判断結果**: ✅ 対応済み

**判断理由**: 抜け漏れチェック B-4 で「確定」と記載済みだが Phase 2 箇条書きに未反映。Phase 2-5 等として App Check（クライアント + Functions）を明記すべき。

---

**識別子**: RC-23（GitHub id: 3372472638）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/08_エンタープライズ_実装計画.md:77`

**該当コード（レビュー時点の diff）**:

```diff
+- **3-2 アカウント管理** `/admin/members`（一覧・個別追加・編集・停止/復活・CSV一括作成 `createEnterpriseMembers`）
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  オンボーディングをPhase計画に組み込んで**

この Phase 3 は `createEnterpriseMembers` を前提にアカウント管理へ進めていますが、追加された `04_オンボーディング` では初代 admin が存在しないと CSV 一括作成もできないため `createEnterprise` が鶏卵問題の解消策だと定義されています。実装計画の Phase 1〜3 に `createEnterprise`（および運営/スクリプト実行経路）が入っていないと、最初の企業テナントと初代 admin を作れず `/admin/members` の実装・検証に進めません。

**コメント要約**: 実装計画の Phase 一覧に `createEnterprise` タスクが無い。

**判断結果**: ✅ 対応済み

**判断理由**: B-2 抜け漏れ表と Phase 1-1 で `createEnterprise` 初期化に言及あるが、Functions 実装タスクとして Phase 2 前後に `onboarding.ts` / `createEnterprise` を明示した方が検証可能。鶏卵問題解消のため Phase 2 または Phase 1 末尾への追加推奨。

---

**識別子**: RC-24（GitHub id: 3396318271）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/08_エンタープライズ_実装計画.md:110`

**該当コード（レビュー時点の diff）**:

```diff
+- **6-1 監査ログ基盤**（MVP から必須・後付け不可）: `writeAuditLog` 共通ユーティリティ、`/enterprises/{eid}/audit_logs`、Cloud Logging 併用。各 Functions に記録を埋め込み。`/admin/audit-logs` 閲覧。`04_監査ログ` 正本。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  監査ログ基盤を最初に組み込んで**

Phase 6 で初めて `writeAuditLog` を作る計画だと、Phase 2〜5 で実装する `confirmEnterpriseEmailLogin`、`createEnterpriseMembers`、注文確定などに後からログ追記が必要になり、`04_監査ログ` が「後付け不可」としている login/account_create/order_create の欠損期間を作ります。監査ログ基盤だけは Phase 2 の前、または各 Function 実装と同じフェーズに移して、最初のログ発生箇所から記録できる計画にしてください。

**コメント要約**: 監査ログ基盤が Phase 6 だと Phase 2〜5 の操作ログが欠落する。

**判断結果**: ✅ 対応済み

**判断理由**: `04_監査ログ` が MVP から必須・後付け不可と明記。`confirmEnterpriseEmailLogin`（login）、`createEnterpriseMembers`（account_create）、`confirmOrder`（order_create）は Phase 2〜4 で発生。`writeAuditLog` 基盤を Phase 2 以前（または横断タスク）へ前倒しすべき。

---

**識別子**: RC-25（GitHub id: 3401933998）

**レビュワー**: Codex

**指摘箇所**: `documents/10_競合調査・営業資料/02_料金比較.md:204`

**該当コード（レビュー時点の diff）**:

```diff
+→ 固定550円/人型（82,500円）より **高くなる規模**。人数・チャージ額で損益分岐が変わる。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  固定550円型の比較額を桁違いにしないでください**

ここで比較している「固定550円/人型」は、100名フル利用なら A 55,000円 + B 750,000円で **805,000円** です（固定手数料だけを見ても 55,000円）。82,500円と記載すると 10%手数料型 825,000円との比較が一桁ずれて、営業資料上の優劣判断を誤らせます。

**コメント要約**: 固定 550 円型の 100 名フル利用合計は 805,000 円なのに 82,500 円と記載。

**判断結果**: ✅ 対応済み

**判断理由**: 同 §3.2 表で 100 名 S1 チケットレストラン合計 805,000 円と一致。82,500 は 10% 型 825,000 の 1/10 程度の typo。

---

## 評価セッション（2026-06-12 18:30 JST・shokujii-code-review）

- **評価日時**: 2026-06-12 18:30 JST
- **評価者**: Cursor Agent（`/shokujii-code-review`）
- **ブランチ名**: doc/enterprise-spec-gap-phase1
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2066
- **Outdated 除外件数**: 該当なし
- **レビュー非該当スキップ件数**: 該当なし（能動的差分レビュー。指摘 0 件の項目は RC 採番せず）

### RC 別対応チェックリスト

- [x] **RC-26**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（finance-billing-ops の課金設計パス typo）
- [x] **RC-27**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（§5.4 月次 cron に統一）
- [x] **RC-28**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（getEnterpriseMembers email join 前倒し）
- [x] **RC-29**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（sort_by monthly_usage 月キー明記）
- [x] **RC-30**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（Rules helper 参照先コメント）
- [x] **RC-31**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（02_料金比較「20名未満」に修正）
- [x] **RC-32**（GitHub id: なし・エージェントレビュー）— ✅ 対応済み（RC-2〜32 判断結果を更新）

---

**識別子**: RC-26（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `.agents/skills/finance-billing-ops/SKILL.md:21`

**該当コード（レビュー時点の diff）**:

```diff
+- `documents/08_エンタープライズ/02_課金設計.md` — プラットフォーム利用料・スナップショット正本
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `.agents/skills/finance-billing-ops/SKILL.md:21` — `documents/08_エンタープライズ/02_課金設計.md` というパスは存在しない（実ファイルは `02_エンタープライズ_課金設計.md`）→ RC-21 と同類のリンク切れ。正しいファイル名に修正する（L22 の `02_料金比較.md` は実在し問題なし）。

**コメント要約**: Skill Stack の課金設計ドキュメントパスが実ファイル名と不一致。

**判断結果**: ✅ 対応済み

**判断理由**: `documents/08_エンタープライズ/` 配下の正本は `02_エンタープライズ_課金設計.md`。エージェントが参照先を誤る。

---

**識別子**: RC-27（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:338`

**該当コード（レビュー時点の diff）**:

```diff
+| cron 例 | `5 0 * * *` + `timeZone: Asia/Tokyo` + 実行日が 1 日のガード |
```

**レビュワーのコメント（原文）**:

🟡 **修正提案** [nits]: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:338` — cron 例「`5 0 * * *` + 実行日が 1 日のガード」は、`5 0 1 * *`（毎月 1 日のみ起動）ならガード自体が不要 → 毎日起動 + ガード方式を採るなら理由（手動再実行との共通化等）を一言添えるか、月次 cron に簡素化を検討。※確定方針（翌月 1 日 00:05 JST・直前暦月）自体には影響しない。

**コメント要約**: §5.4 の cron 例が冗長。月次 cron で簡素化可能。

**判断結果**: ✅ 対応済み

**判断理由**: 仕様の正しさには影響しないが、実装者が cron とガードの両方を実装するか迷う可能性がある。

---

**識別子**: RC-28（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_認証・アカウント管理.md:572, 586-591`

**該当コード（レビュー時点の diff）**:

```diff
+  search?: string           // display_name 前方一致 + email 部分一致
 …
+3. Functions 内で search / role / is_active による filter
+4. Functions 内で sort_by / sort_order による sort
+5. limit + startAfter によるページング
+6. 各 userId で /users_personal_information/{userId} から user_email を取得（Admin SDK）して join
```

**レビュワーのコメント（原文）**:

🚨 **必須修正**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_認証・アカウント管理.md:572, 586-591` — `search` は「display_name 前方一致 + **email 部分一致**」と定義されているが、処理フローでは email の join が**ページング後の step 6**。step 3 のフィルタ時点で email を持っていないため、email 検索が仕様として成立しない自己矛盾がある → email 検索を行う場合は filter 前に `/users_personal_information` を join するフローに変更するか、`search` の対象を display_name のみに限定する（email 検索は Phase 2 等へ）。

**コメント要約**: getEnterpriseMembers の search が email 部分一致を謳うが、email join が filter より後。

**判断結果**: ✅ 対応済み

**判断理由**: `04_全社管理者画面` §4.1 も「メール部分一致」を API 経由と明記。フロー順序のままでは email 検索が実装不能。

---

**識別子**: RC-29（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_認証・アカウント管理.md:575`

**該当コード（レビュー時点の diff）**:

```diff
+  sort_by?: 'display_name' | 'department' | 'role' | 'is_active' | 'created_at' | 'monthly_usage'
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_認証・アカウント管理.md:575` — `sort_by: 'monthly_usage'` だが `monthly_usage` は `Record<string, number>`（YYYY-MM 辞書）であり、どの月キーでソートするか未定義 → 「当月キー（実行時の YYYY-MM）の値でソート」等を明記推奨。

**コメント要約**: monthly_usage ソートの対象月キーが仕様に無い。

**判断結果**: ✅ 対応済み

**判断理由**: `04_全社管理者画面` §4.1 は「当月利用額」列をソート可としている。辞書型フィールドのソートキーを明記しないと実装が分岐する。

---

**識別子**: RC-30（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_全社管理者画面.md:519`

**該当コード（レビュー時点の diff）**:

```diff
+  allow update: if isSupport() || isEnterpriseCommunityManager(communityId);
```

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `documents/08_エンタープライズ/04_エンタープライズ_詳細仕様_全社管理者画面.md:519` — `isEnterpriseCommunityManager(communityId)` を使用しているが、本ファイル内に定義がなく参照先の記載もない → 「helper 定義は `02_全体構成` §5.2 参照」の一言をコメントで追記すると実装者が迷わない。

**コメント要約**: Rules サンプルが helper を参照するが定義・参照先が本章に無い。

**判断結果**: ✅ 対応済み

**判断理由**: `02_全体構成` §5.2 に定義済み。本章 Rules は抜粋のため参照コメントの追加で足りる。

---

**識別子**: RC-31（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/10_競合調査・営業資料/02_料金比較.md:214`

**該当コード（レビュー時点の diff）**:

```diff
-50名未満でも最低10,000円のため、**20名なら 500円/人ではなく 500円/人（10,000÷20）** になる点に注意。
+10名未満でも最低10,000円のため、**10名なら 500円/人ではなく 1,000円/人（10,000÷10）** になる点に注意。
```

**レビュワーのコメント（原文）**:

🚨 **必須修正**: `documents/10_競合調査・営業資料/02_料金比較.md:214` — 「**10名未満**でも最低10,000円のため、**10名なら** 1,000円/人」は論理が破綻している。最低料金 10,000 円が効くのは **20 名未満**（500 円 × 20 = 10,000 円）であり、「10名」は「10名未満」に含まれない → 「**20名未満では最低 10,000 円が適用されるため、10名なら 500円/人ではなく 1,000円/人（10,000÷10）**」に修正。RC-7 の修正例（10名 → 1,000円/人）自体は正しいが、導入の条件文が新たな誤りになっている。

**コメント要約**: RC-7 対応で算数例は正しいが「10名未満」の条件文が誤り。

**判断結果**: ✅ 対応済み

**判断理由**: 営業資料の算数例として明確な論理エラー。RC-7 対応の副作用。

---

**識別子**: RC-32（GitHub id: なし・エージェントレビュー）

**レビュワー**: Cursor Agent（shokujii-code-review）

**指摘箇所**: `documents/レビューコメント/pr-2066.md`（RC-2〜25 ブロック）

**該当コード（レビュー時点の diff）**:

（メタ指摘。該当 diff なし）

**レビュワーのコメント（原文）**:

🟡 **修正提案**: `documents/レビューコメント/pr-2066.md` — 冒頭のチェックリストは `[x]` + ✅ に更新済みだが、**各 RC ブロック内の「判断結果」フィールド**（RC-2〜25）は元の 🚨/🟡 区分のまま → スキル規約（review-comments-evaluate）では対応完了後に各ブロックの判断結果も **✅ 対応済み** へ更新する。

**コメント要約**: チェックリストと RC ブロック本文の判断結果が不一致。

**判断結果**: ✅ 対応済み

**判断理由**: ドキュメント運用上、チェックリスト `[x]` と各 RC ブロックの判断結果は一致させるのが evaluate / shokujii-code-review の規約。

---

## 評価セッション（2026-06-12 再レビュー・Copilot / Codex）

- **評価日時**: 2026-06-12 20:15 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: doc/enterprise-spec-gap-phase1
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2066
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼 4690525704、Codex 接続案内 4690578275）

### RC 別対応チェックリスト

- [x] **RC-33**（GitHub id: 4690576946）— ✅ 対応済み（02_料金比較 §3.3 S2 説明文を §1.3 に整合）
- [x] **RC-34**（GitHub id: 3402900160）— ✅ 対応済み（返金仕様リンク 4 ファイル統一）
- [x] **RC-35**（GitHub id: 3402900172）— ✅ 対応済み（PF 版 communities create OR パス追加）
- [x] **RC-36**（GitHub id: 3402900168）— ✅ 対応済み（月内 ever active・`last_activated_at` / `last_deactivated_at`）

---

**識別子**: RC-33（GitHub id: 4690576946）

**レビュワー**: Copilot

**指摘箇所**: `documents/10_競合調査・営業資料/02_料金比較.md:139`（§3.3 S2）

**該当コード（レビュー時点の diff）**:

（インライン指摘なし）

**レビュワーのコメント（原文）**:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

レビューしました。RC-2〜32 の指摘はすべて最新コミットで対応済みであることを確認しました。

1点だけ新たな矛盾を発見し、修正済みです（`3b...` コミット）：

---

**[must] `02_料金比較.md` §3.3 S2 説明文が計算式・§1.3 定義と矛盾**

§1.3 では `全員の50%が上限まで利用`（avg = 7,500 × 50% = 3,750円/人）と定義しているにもかかわらず、§3.3 の本文が `全員の50%が上限の半分まで利用` と記載されていました。後者をそのまま計算すると avg = 3,750 × 50% = 1,875円/人 となり、同§の計算式 `B = 7,500 × 50% = 3,750円/人` や表の数値と一致しません。§3.3 の説明を `全員の50%が上限まで利用` に修正し、§1.3 定義・計算式と一致させました。

**コメント要約**: §3.3 S2 の説明が §1.3 定義・計算式 B=3,750円/人と矛盾。§3.3 を「上限まで利用」に揃える必要がある。

**判断結果**: ✅ 対応済み

**判断理由**: §3.3 L139 を「全員の50%が上限まで利用」に修正し §1.3 L36・表数値と一致させた。

---

**識別子**: RC-34（GitHub id: 3402900160）

**レビュワー**: Codex

**指摘箇所**: `documents/02_主催者獲得と継続/09_イベントの最小催行人数設定・自動中止.md:38`

**該当コード（レビュー時点の diff）**:

```diff
@@ -30,7 +32,8 @@
 ## 3. 前提条件
 ### 3.1 技術的前提条件
 - イベント作成・編集機能が実装済み
-- 決済・返金システムが実装済み（`12_イベント中止と返金.md`）
+- **EventMemberOrder 設計**（`members/{uid}/member_orders` + `stripes`）が本番反映済みであること（`07_リファクタリング/05`）
+- 決済・返金システムが実装済み（`08_イベント中止と返金.md`）
 
 ### 3.2 ビジネス的前提条件
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P3 Badge](https://img.shields.io/badge/P3-lightgrey?style=flat)</sub></sub>  存在しない返金仕様への参照を直してください**

この参照先 `08_イベント中止と返金.md` は `documents/02_主催者獲得と継続/` 配下に存在せず、実在する返金仕様は `08_イベントキャンセル_参加者あり_返金.md` です。このままだと最小催行人数・自動中止の実装者が前提となる返金仕様へ辿れないため、実在するファイル名に合わせてください。

Useful? React with 👍 / 👎.

**コメント要約**: 返金仕様へのリンク先ファイル名が存在しない。正しくは `08_イベントキャンセル_参加者あり_返金.md`。

**判断結果**: ✅ 対応済み

**判断理由**: `08_イベントキャンセル_参加者あり_返金.md` に統一。09_最小催行人数・04_イベント管理・08_実装計画・05_共通新機能の 4 ファイルを修正。

---

**識別子**: RC-35（GitHub id: 3402900172）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_全体構成・アーキテクチャ.md:895`

**該当コード（レビュー時点の diff）**:

```diff
@@ -818,33 +888,78 @@ match /enterprises/{enterpriseId}/members/{userId} {
   allow write: if false;   // Functions のみ
 }
 
-// コミュニティ: enterprise_id が一致する場合 or PF版コミュニティはアクセス可能
+// コミュニティ: MVP — create は同一 enterprise の全従業員、update は PF 同等（community manager 限定）
 match /communities/{communityId} {
   allow read: if resource.data.enterprise_id == null
               || resource.data.enterprise_id == request.auth.token.enterprise_id;
+  allow create: if request.auth.token.enterprise_id != null
+                && request.resource.data.enterprise_id == request.auth.token.enterprise_id;
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  PF版のコミュニティ作成ルールを残してください**

この Rules サンプルを実装すると、現在の `firestore.rules` にある PF 版の `allow create: if request.auth != null` が失われ、`enterprise_id` claim を持たない通常の PF ユーザーはコミュニティを作成できなくなります。エンプラ作成条件を追加する場合も、`request.resource.data.enterprise_id == null` の PF 作成パスを明示的に残してください。

Useful? React with 👍 / 👎.

**コメント要約**: communities の create ルールがエンプラ専用のみ。PF 版の `request.auth != null` 作成パスが欠落し、現行 firestore.rules と矛盾。

**判断結果**: ✅ 対応済み

**判断理由**: `02_全体構成` §5.2 と `04_全社管理者画面` §10 の Rules サンプルに PF create パス（`enterprise_id == null`）を OR で追加。現行 firestore.rules L89 と整合。

---

**識別子**: RC-36（GitHub id: 3402900168）

**レビュワー**: Codex

**指摘箇所**: `documents/08_エンタープライズ/02_エンタープライズ_課金設計.md:339`

**該当コード（レビュー時点の diff）**:

```diff
…（diff 先頭省略・ファイル全体追加 hunk）
+| 確定基準 | 直前暦月の **23:59:59.999 JST 時点**の状態を集計（月末 23:55〜24:00 の取りこぼしを回避） |
+| ① の集計 | 当該 `year_month` の `is_active == true` 件数 × `unit_price`（お試し・**企業停止時は 0**） |
+…
+> 月中にメンバーを停止・復活しても、**確定済み過去月のスナップショットは自動では書き換えない**。訂正は運営が手動 Callable または Firestore 更新（監査ログ必須）。
+
+### Phase 1.5 で追加検討（任意）
+
+`EnterpriseMember` に `deactivated_at` / `last_activated_at` を追加すると、スナップショットの再計算・ dispute 対応が容易になる。MVP では必須としない（`audit_logs` の `account_disable` / `account_enable` で手動補正可）。
```

**レビュワーのコメント（原文）**:

**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  月末時点の課金人数を再現できる設計にしてください**

この集計は「直前暦月の月末時点」を請求根拠にしていますが、MVP の `EnterpriseMember` には有効/停止の履歴時刻がなく、バッチも翌月 00:05 に走るため、月末後〜バッチ実行前または手動再実行前にメンバーを停止・復活すると、その変更後の `is_active` で前月分を確定してしまいます。手動請求額が変わるため、`deactivated_at` などの履歴を MVP に含めるか、月末時点を確実に保存する方式にしてください。

Useful? React with 👍 / 👎.

**コメント要約**: 確定基準は月末 23:59:59 時点だが、MVP は `is_active` のみで履歴がなく、バッチ実行時の現状態で前月を確定してしまうリスクがある。

**判断結果**: ✅ 対応済み

**判断理由**: P-4/P-8 を **月内 ever active** に変更。`EnterpriseMember.last_activated_at` / `last_deactivated_at` を MVP 必須とし §2.3 に `billable_in(YM)` 判定式・フィールド更新ルール・判定例を明記。§5.4 月次バッチはタイムスタンプで前月確定（日次スナップショット不採用）。dispute 時は `audit_logs` replay で検証・手動訂正。`02_全体構成`・`04_認証`・`08_実装計画`・`04_ダッシュボード` を整合。

---

## 評価セッション（3）

- **評価日時**: 2026-06-12 21:35 JST
- **評価者**: Cursor Agent（`/review-comments-evaluate`）
- **ブランチ名**: doc/enterprise-spec-gap-phase1
- **PR**: https://github.com/nijuniinc/bokudeli-event-new/pull/2066
- **Outdated 除外件数**: 0
- **レビュー非該当スキップ件数**: 2（レビュー依頼 4691219826、Codex 接続案内 4691256142）

### RC 別対応チェックリスト

- [x] **RC-37**（GitHub id: 4691255325）— ✅ 対応済み（02_課金設計.md 太字＋コードスパン記法 7 箇所）

---

**識別子**: RC-37（GitHub id: 4691255325）

**レビュワー**: Copilot

**指摘箇所**: PR トップレベル（`documents/08_エンタープライズ/02_エンタープライズ_課金設計.md` L63, L82, L135, L182, L195, L385, L565）

**該当コード（レビュー時点の diff）**:

```diff
-`**community_bill`（主催者請求書払い）はエンプラ版では選択不可**（P-13）。
+**`community_bill`（主催者請求書払い）はエンプラ版では選択不可**（P-13）。
…（同様の記法バグが L82, L135, L182, L195, L385, L565 に計 7 箇所）
```

**レビュワーのコメント（原文）**:

> @copilot @codex review 日本語でお願いします。Files changed で該当行にインラインの review comment を 1 指摘 1 コメントでお願いします。トップレベルに複数フ...

レビューしました。最新コミット（RC-33〜36 反映）で `02_課金設計.md` にマークダウンの太字＋コードスパン記法バグが 7 箇所導入されていたため修正しました（RC-37）。

**[must] `02_課金設計.md`: 太字＋コードスパン記法が 7 箇所で壊れていた**

`**\`...\`**`（太字でコードを囲む）の記法で `` ` `` と `**` の順序が入れ替わり、`` `**code` ** `` という壊れた形になっていました。

修正箇所:
- L63: `` `**community_bill`...** `` → `**\`community_bill\`...**`
- L82: `` `**billing_settings.unit_price`...** `` → `**\`billing_settings.unit_price\`...**`
- L135: `` `**unit_price`** `` → `**\`unit_price\`**`
- L182: `` `**Asia/Tokyo`** `` → `**\`Asia/Tokyo\`**`
- L195: `` `**/enterprises/{...}/members/{userId}`** `` → `**\`/enterprises/{...}/members/{userId}\`**`
- L385: `` `**/enterprises/{id}/billing_snapshots/{YYYY-MM}`...** `` → `**\`/enterprises/...\`...**`
- L565: `` `**captureEnterpriseBillingSnapshots`...** `` → `**\`captureEnterpriseBillingSnapshots\`...**`

他のファイルに同様のバグは確認されませんでした。commit: `[doc] #2066 02_課金設計.md のマークダウン記法バグ（RC-37）を修正`

**コメント要約**: RC-33〜36 反映後、`02_課金設計.md` の 7 箇所で太字＋コードスパンの Markdown 記法が `` `**code` ** `` と壊れていた。正しくは `**\`code\`**` である。

**判断結果**: ✅ 対応済み

**判断理由**: 指摘は妥当。Copilot がコミット d5e43129 で 7 箇所を `**\`...\`**` に修正済み。プレビュー上も意図どおり太字＋インラインコードとして表示される。他ファイルに同パターンはなし。

---
