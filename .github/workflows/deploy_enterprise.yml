name: Deploy enterprise

on:
  push:
    branches:
      - production
      - development
    paths:
      - 'enterprise/**'
      - 'base/**'
      - 'common/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - development
          - production

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'push' && (github.ref_name == 'production' && 'production' || github.ref_name == 'development' && 'development') || inputs.environment }}
    env:
      ENVIRONMENT: ${{ github.event_name == 'push' && (github.ref_name == 'production' && 'production' || github.ref_name == 'development' && 'development') || inputs.environment }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup node
        uses: actions/setup-node@v7
        with:
          node-version-file: ./.node-version
          cache: 'npm'
          cache-dependency-path: ./package-lock.json
      - name: Install dependencies
        run: |
          npm ci
      - name: Generate .env file
        working-directory: ./enterprise
        run: |
          echo '${{ vars.ENTERPRISE_ENV }}' | tr -d '\r' > .env
      - name: Check Format
        run: |
          npm -w common run format:check
          npm -w base run format:check
          npm -w enterprise run format:check
      - name: Build
        run: |
          npm -w enterprise run build
      - name: Lint
        run: |
          npm -w common run lint
          npm -w base run lint
          npm -w enterprise run lint
      - name: Generate .firebaserc
        run: |
          echo '${{ vars.FIREBASERC }}' > .firebaserc
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCLOUD_SERVICE_KEY }}
          project_id: ${{ vars.PROJECT_ID }}
      - name: Deploy to Firebase
        uses: ./.github/actions/deploy
        with:
          project_id: ${{ vars.PROJECT_ID }}
          args: '--only hosting:enterprise'
