name: Deploy user to development

on:
  push:
    branches:
      - development
    paths:
      - "user/**"
      - "base/**"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    environment: development
    defaults:
      run:
        shell: bash
        working-directory: ./user
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version-file: ./user/.node-version
          cache: "yarn"
          cache-dependency-path: ./user/yarn.lock
      - name: Install dependencies
        run: |
          yarn install --frozen-lockfile
      - name: Generate .env file
        run: |
          echo '${{ vars.USER_ENV }}' | tr -d '\r' > .env
      - name: Build
        run: |
          yarn run build-development
      - name: Deploy to Firebase
        uses: ./.github/actions/deploy
        with:
          project_id: bokudeli-event-test
          service_key: "${{ secrets.GCLOUD_SERVICE_KEY_BOKUDELI_EVENT_TEST_03CF59E61D6C }}"
          args: "--only hosting:user"
