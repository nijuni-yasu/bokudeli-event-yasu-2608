{
  "version": 1,
  "commands": {
    "lint:all": {
      "command": "npm -w common run lint; npm -w base run lint; npm -w user run lint; npm -w partner run lint; npm -w functions/default run lint",
      "description": "全パッケージでlintを実行（全パッケージの結果を確認可能）"
    },
    "format:check:all": {
      "command": "npm -w common run format:check; npm -w base run format:check; npm -w user run format:check; npm -w partner run format:check; npm -w functions/default run format:check",
      "description": "全パッケージでformat:checkを実行（全パッケージの結果を確認可能）"
    },
    "format:all": {
      "command": "npm -w common run format; npm -w base run format; npm -w user run format; npm -w partner run format; npm -w functions/default run format",
      "description": "全パッケージでformatを実行"
    }
  }
}
